# Optional user-local network environment for public market-data collectors.
# Copy to ~/.config/btc-quant-agent/network.env and keep credentials out of Git.
HTTP_PROXY=
HTTPS_PROXY=
ALL_PROXY=
NO_PROXY=localhost,127.0.0.1
