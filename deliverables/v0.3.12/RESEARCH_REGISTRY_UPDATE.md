# Research Registry Update — v0.3.12

The Registry advances to `v0.3.12` and adds `spot_perp_taker_flow_direction`:

| Field | Value |
|---|---|
| role | `DIRECTION` |
| feature family | `spot_perp_market_flow` |
| hypotheses | `H32/H33/H34` |
| research status | `INCONCLUSIVE` |
| runtime eligibility | `BLOCKED` |
| eligible for reuse | `false` |
| Final Holdout accessed | `false` |

H32 is `INCONCLUSIVE_MECHANISM`; H33 and H34 are `FALSIFIED`. `active_direction_engine.state` remains `NONE`, qualified actionable Direction count remains `0`, and `runtime_maximum_stage` remains `OPPORTUNITY_ONLY`.

