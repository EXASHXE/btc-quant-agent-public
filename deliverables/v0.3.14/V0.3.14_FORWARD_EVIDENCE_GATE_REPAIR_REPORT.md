# v0.3.14 Forward Evidence Gate Repair Report

The old gate calculated its longest failure run over the immutable all-time ledger, so
the historical 54-slot outage made `max gap <=4` permanently impossible. v0.3.14 keeps
every old row but preregisters `DERIVATIVES_PIT_EPOCH_V0314_001`, starting at the fixed
2026-08-31 12:30:00 UTC boundary. Only natural `SCHEDULED` runs tagged to that epoch
enter eligibility; manual, legacy, partial and failed archive rows remain visible but
cannot improve the denominator.

Expected slots are generated from the frozen start and 15-minute wall clock through the
latest due +20-second boundary. Missing invocations therefore count as bad slots. The
unchanged gate is 30d / 2500 full / 95% each required field / max 4 consecutive
failed, partial, or missing slots. At capture the future epoch had not begun: 0 expected,
0 full, 0 missing, 0 max gap, state `INITIALIZING`.

Public endpoints now use at most 3 total attempts with deterministic 0.1/0.2s backoff.
Telemetry persists attempt count, latency, outcome, final class and source timestamp.
Schema/validation and non-429 4xx failures do not retry. A deployment-context collection
and the 12:15:20 UTC natural collection both completed fully with zero endpoint errors;
the natural run was correctly PRE_EPOCH. No scheduled active-epoch result was available
before this delivery capture.

The existing campaign ID remains `OPPORTUNITY_FORWARD_V0313_20260831T050656Z`.
At capture: 29 slots, 19 successful, 10 missed, TP=0, BR=0, resolved opportunity
counts=0 and controls=0; H35 remains `FORWARD_CAMPAIGN_ACCUMULATING`. The new hourly
resolver timer is enabled/active and its first natural run appended 6 mature labels with
0 failures without mutating observations.

Quality: 355 tests passed; coverage 59%; Ruff, strict Mypy (58 source files), and
compileall PASS. Recommendation: `CONTINUE_FORWARD_EVIDENCE_ACCUMULATION`.

Safety: Direction claim NONE; Execution DISABLED; Holdout access false.
