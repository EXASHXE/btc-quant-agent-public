# v0.3.19 Official Derivatives Symbolic Qualification Report

## 结论

正式建议为 `STOP_OFFICIAL_DERIVATIVES_SYMBOLIC_FAMILY`。本轮真实扩展了官方衍生品/现货成交特征，但新公式族没有通过冻结的家族级多重检验，也没有任何候选通过全部公式门槛。不得扩大同族预算补救；如继续研究，应以真正不同的数据族（例如有记录时间审计的 vendor OI/L2/liquidation）建立新假设。

## 冻结与数据

- preregistration commit：`a2b20f6784e682a73a73fe73787905c4c305921320c`
- formal implementation commit：`a26b657379f1b2e0f463c6cd614d4849f1c1b833`
- protocol SHA-256：`52d58db8778e55e10e57c7516c6611fb64f337331f4c26d40476f7789d6f1bcd`
- raw manifest SHA-256：`cbeae209c5ded8aae265bdad5155dc34a20dc6c994ddc174aa7d57fb73e302f9`
- hourly input SHA-256：`77a8ad63fa0f1ead7c8481197661caa24171c7cf856c9b3326d72e376e0fcaed`
- 数据角色：`OFFICIAL_HISTORICAL_TIMESTAMPED`，不声称本地 receive-time PIT
- 官方归档：6 families × 61 months = 366；verified 366；missing 0
- 时间范围：`[2021-01-01, 2026-02-01)`；小时数 44,568
- Final Holdout rows loaded：0；synthetic/gap-fill：0/0

所有使用的 ZIP 均保留官方 `.CHECKSUM` 并逐文件验证。官方源内的四个异常包没有被掩盖：永续 2022-09 去除 4,316,651 条精确重复；永续 2023-05 与 2023-10 分别修复 1,357,200、3,988,367 次 aggregate-ID 回退；现货 2021-12 是整月双份拼接，去除 32,269,900 条重复。修复采用 aggregate ID 数值稳定排序、相邻 ID 去重、冲突检测；四包冲突数均为 0，修复后 ID/时间回退均为 0。小时聚合采用顺序无关桶，缺失区间保持 unavailable。

## P0 正确性

Bootstrap API 现在明确区分 block hours 与 block events；正式 168h / 8h sampling 被转换为 21 events，并同时写入 artifact。v0.3.18 已发布数值保持原样，没有追溯重写。

Candidate gate 使用 protocol → typed config → evaluator → per-gate result 的单一链路；28 个冻结键均恰好消费，未知/未使用键使正式运行 fail closed。Sandbox 修正并测试了 `net_r = gross_r - fees_r - slippage_r - funding_r`、planned reference 与 strictly-next-event actual entry、actual-entry risk denominator、gap-through-stop、LONG/SHORT funding 与成本对称性。由于全部候选在上游公式门槛失败，沙盒门槛依据冻结规则标记 `NOT_APPLICABLE / UPSTREAM_FORMULA_GATE_FAILED`，而非伪造零交易通过。

## 新特征族

冻结的 8 个特征为 Premium Index level/z、Mark-Index basis/change、Perp/Spot aggregate buy imbalance、Spot-Perp aggregate flow spread、Perp signed notional z。`buyer_is_maker=true` 表示卖方主动成交，quote notional 记入 sell；否则记入 buy。每个小时只使用该已闭合小时内的事件；滚动计算只引用尾随已闭合历史。

可用小时为：Mark basis 44,158；basis change 44,112；Perp imbalance 44,352；Perp signed-notional z 44,168；Premium level 44,393；Premium z 44,194；Spot imbalance 44,552；Spot-Perp spread 44,336。不可用值未填零。

## 正式搜索结果

- Random Grammar seed：冻结于 protocol
- attempts：1,237
- unique/evaluated formulas：512/512
- old-only rejected：530
- duplicate rejected：166；over-lookback：7；over-token：22；VM failures：0
- 所有正式公式包含至少一个 v0.3.19 新特征
- familywise procedure：200 次 circular-block empirical best-score permutation
- block：168 hours = 21 sampled events
- observed best t：2.9308137116
- null best t p95：8.7326518274
- adjusted p：0.4328358209；pass 0.05：false

Validation 后按预注册规则冻结 Top-5，freeze 前 pseudo-forward touches 为 0。Top-5 全部失败；样本量充分的首名公式在 Validation/Pseudo-forward 的成本后均值分别为 `-0.0003960230` 与 `-0.0014524087`。表面上正收益的稀疏公式只有 Validation 4 和 pseudo-forward 7 个事件，远低于各 500 的门槛。没有候选进入 event replay。

## Forward 与安全

历史运行写入 Forward stores 的记录数为 0；三个既有 campaign 的身份和开始时间未重置。运行结束时 Derivatives 与 H36 已按各自冻结质量门槛 terminal failure，Microstructure 因 stale heartbeat 呈不健康，但原 campaign 保持不变，详见 `FORWARD_CAMPAIGNS_STATUS.json`。

最终状态保持：`strategy=EXPERIMENTAL`、`qualified_direction_engine=NONE`、`runtime_maximum=OPPORTUNITY_ONLY`、`execution=DISABLED`、`auto_execute=false`、`candidate_freeze=NONE`、`final_holdout=SEALED`。

## 验证

- ruff：pass
- mypy：pass（72 source files）
- pytest：421 passed
- compileall：pass
- direct coverage：evaluate 97%、registry 98%、sandbox 94%、search 91%、VM 88%
- total package coverage：64%

Tiny Transformer 未实现：它是 P1 可选项，且本轮家族已触发停止规则，不应为了宣称神经方案而追加结果后预算。
