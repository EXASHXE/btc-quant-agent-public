# v0.3.17 Opportunity Successor Forward Stabilization Report

## 范围

本轮只修复和稳定 forward evidence，不测试 Alpha、不解释 future returns、不改变 TP/BR detector、不产生 LONG/SHORT 或交易参数。

## H35 archive

`OPPORTUNITY_FORWARD_V0313_20260831T050656Z` 的全部历史行、28 个 immutable misses 与 outcomes 分离存储保持不变。其 missingness 与网络/运行故障强相关，正式角色永久为 `DATA_QUALITY_AT_RISK_ARCHIVE`，不能作为 validated H35 evidence。

## H36 successor

`OPPORTUNITY_FORWARD_V0317_20260901T160000Z` 在 freeze commit `b098e25c8095e793310f439cf3e9d8e135a3599c` 中预注册，固定起点为 2026-09-01 16:00 UTC。它完全复制 H35 的 TP/BR definition、4h/8h labels、ATR/reference rules、matching rules、seed、strategy/feature/registry/config identity。

H36 新增冻结质量门：30 天、30/50 resolved opportunities、100 unique controls、20 UTC days、scheduled scan ratio ≥95%、最大连续 miss 4。分母由 wall-clock expected slots 决定；missing wall-clock slot 等同 `PROCESS_OR_HOST_GAP`。只有 systemd `SCHEDULED`、health OK、有 closed decision bar 且 durable stored 的行算成功。manual/legacy/backfill 不提高 gate；miss 不能被后来 success 替换；超过连续上限后不可恢复。

激活后的前两个自然槽位均为 successful scheduled scans：2 expected / 2 recorded / 2 successful / 0 missed，scan ratio 100%，最大连续 miss 0。两个槽位均无 TP/BR opportunity。16:07 UTC resolver 首轮 resolved 0、failed 0；16:08 UTC health timer 返回 `HEALTHY`。

## Derivatives successor

v0.3.16 successor 起点 `2026-09-01T09:30:00Z` 未移动。自然运行发现一个 10:00 UTC missing：服务已完成网络请求，但 source timestamp 领先 local receive clock，旧 guard 抛出 `source timestamp cannot be after observed_at_ms`。修复使用保守 effective observed time：不早于 local assembly 与任何已接收 source timestamp，并设置 5 秒最大 clock-extension bound。失败槽位没有回填。

## Microstructure reliability

结构化 health 首次运行发现 streams 仍在写入但 heartbeat stale。根因是高流量同步 SQLite 工作饿死 asyncio heartbeat，延迟 heartbeat 又把当前实例自身标成 orphan。修复后 heartbeat 使用独立线程，当前实例被 lease recovery 排除；真实 service restart 后 heartbeat 恢复。历史 coverage/gaps/orphans 保留，不改写 raw data。

24h/7d rolling summaries只从既有 coverage segments 与 gaps 因果计算，不写回 raw partitions。它们是 future-readiness 指标，不是 `RESEARCH_READY` 或 Alpha 结果。

最终快照中 trade coverage 为 99.70%，depth 为 90.15%，sequence-valid 为 90.15%。后两者低于未来 95% guideline，原因是历史 heartbeat/self-orphan 缺口被诚实保留；当前 heartbeat age 为 3.549 秒，service active，partition integrity OK。继续积累，不能称为 research ready。

## Operations

新增 `quantctl forward-evidence health` 与 systemd user timer。以下状态产生结构化 failure/warning 与非零退出：inactive schedulers、接近/触发 gap gate、H36 miss streak、inactive/stale micro daemon、partition drift、超时未解析 mature outcomes。没有通知后端、订单或策略写入。

## Final answers

- H35：`DATA_QUALITY_AT_RISK_ARCHIVE`
- H36：`FORWARD_CAMPAIGN_ACCUMULATING`（未达到任何 evaluation gate）
- Derivatives v0.3.16：`ACTIVE_ACCUMULATING`
- Microstructure v0.3.15：`ACTIVE_ACCUMULATING`
- Direction Candidate：`NONE`
- Execution：`DISABLED`
- Final Holdout：`SEALED`

本轮后执行 stop rule：若没有新的 operational defect，让真实时间积累数据，不因版本节奏创建 v0.3.18。
