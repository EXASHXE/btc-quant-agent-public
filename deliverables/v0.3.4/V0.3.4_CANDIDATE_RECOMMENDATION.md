# v0.3.4 Candidate Recommendation

## Decision

**NO_CANDIDATE**

Do not advance E-RETRACE to candidate freeze or v0.3.5 candidate validation.

## Evidence

- Only 2 of 18 prospective RR-compliant retrace limits filled within the frozen 45-minute TTL.
- Both causal fills stopped out: expectancy -0.824651 R, PF 0, win rate 0%.
- H10 is `INCONCLUSIVE_MECHANISM` because 2 valid fills are below the pre-registered 10-trade threshold.
- H9 is `FALSIFIED`: 4h and 8h matched-control signed-return deltas are -0.409837 and -0.109350 ATR.
- All five tested signed-return horizons have negative candidate-control deltas.

## Restrictions retained

Strategy remains `EXPERIMENTAL`; execution remains disabled. Do not consume Holdout, enable paper/testnet/live execution, extend TTL, move the limit, change stop/target, lower RR, or select a favorable horizon after observing the results.

A future research version is justified only by a new independently pre-registered mechanism, not by relaxing v0.3.4 gates.

