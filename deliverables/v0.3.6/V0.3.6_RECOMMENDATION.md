# v0.3.6 recommendation

`RECOMMEND_NEW_FEATURE_FAMILY_RESEARCH`

The frozen price-only Direction Engine foundation is falsified. D0 has negative signed-return medians at both primary horizons, all incremental Macro/Structure/Momentum comparisons are negative, and no nested layer is eligible. Therefore:

- `PROPOSED_DIRECTION_ARCHITECTURE = NONE`.
- Do not reverse failed TP/BR direction signals.
- Do not create or freeze a strategy candidate.
- Do not run paper, testnet, live, or Final Holdout evaluation.

Both TP and BR are supported as direction-agnostic Opportunity/Movement Event layers. They may be retained for future architecture research only if paired with a separately validated direction source.

The next preregistered research family should be genuinely new: point-in-time forward-collected derivatives, cross-asset/market breadth, or a separately defined higher-timeframe state family. v0.3.6 does not implement or mine these features.
