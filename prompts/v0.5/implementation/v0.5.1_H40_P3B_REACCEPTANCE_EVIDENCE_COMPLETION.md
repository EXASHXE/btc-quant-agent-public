# Agent Prompt — V0.5.1 H40 P3B Reacceptance Evidence Completion

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = EVIDENCE_AND_REPORTS_ONLY
PRODUCTION_CODE_CHANGES = FORBIDDEN
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required tested implementation commit:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

Independent controller code review:

```text
reviews/v0.5/V0.5.1_H40_P3B_RUNTIME_BEHAVIOR_SEAL_CONTROLLER_CODE_REVIEW.md
docs commit = 649ce9374e84faefb8c9d6afea6ce8f3908b48be
```

Current code-level state:

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_SAME_TYPE_BEHAVIOR_BLOCKER_CLOSED_AT_CODE_LEVEL
```

Current authority state remains:

```text
P3B_PRODUCTION_AUTHORITY_REACCEPTANCE_BLOCKED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 1. Mission

Produce the missing reproducible evidence required for independent controller reacceptance of commit:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

This task has three deliverables:

1. P3B runtime-behavior-seal implementation report;
2. H40 pytest runtime optimization/performance report;
3. machine-readable local test evidence manifest bound to the exact tested implementation commit.

Do not change production source code in this task.

Do not publish a new accepted lifecycle implementation authority yet.

---

## 2. Starting-state guard

Before doing anything:

```bash
git rev-parse HEAD
git status --short
```

Require the implementation checkout used for testing to resolve exactly:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

A dirty working tree is allowed only for generated test logs outside tracked source paths. No tracked source/test file edits are allowed.

If implementation HEAD differs:

```text
STOP
TESTED_IMPLEMENTATION_HEAD_MISMATCH
```

---

## 3. No-code-change restriction

Do not modify:

```text
src/**
tests/**
pyproject.toml
configs/**
```

Do not amend/rebase/cherry-pick the implementation commit.

Reports/evidence should be committed to:

```text
v0.5-refactor-docs
```

not to the implementation branch.

If a test failure requires source repair:

```text
STOP
P3B_REACCEPTANCE_REQUIRES_CODE_REPAIR
```

Report the exact failing test and root cause.

---

## 4. Focused P3B behavior-seal validation

Run:

```bash
.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   --durations=0

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_verifier_identity_seal.py   --durations=0

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_discovery_verifier.py   --durations=20
```

Record exact:

- passed/failed/skipped counts;
- elapsed time;
- Python version;
- pytest version.

Explicitly confirm coverage for:

```text
instance method shadow
object.__new__ forgery
partial initialization
resolver replacement
resolver root mutation
seal replacement
receipt replacement
split replacement
class method mutation
scientific-result-cache mutation
cold restore tamper
foreign type
subclass
proxy
synthetic production fence
synthetic mode
frozen hashes
```

---

## 5. Focused lifecycle/governance regression

Run:

```bash
.venv/bin/python -m pytest -q   tests/test_v051_h40_f01_lifecycle_authority.py   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py   tests/test_v051_h40_f01r3r4_lineage_durability.py   tests/test_v051_h40_implementation_authority_publication.py   --durations=30
```

The current accepted implementation-authority publication tests are expected to continue describing the PREVIOUS accepted implementation authority.

Do not modify them in this evidence task.

Their continued use of the old authority is evidence that the new implementation has not yet been independently published as accepted.

---

## 6. Execution/protected-surface fences

Run:

```bash
.venv/bin/python -m pytest -q   tests/test_execution.py   tests/test_execution_environment_authority.py
```

Record:

```text
CURRENT_EXECUTION_POLICY
ACCEPTED_EXECUTION_WRITE_AUTHORITY
```

Expected:

```text
RESEARCH_DISABLED_V1
None
```

No real H40 outcomes may be read.

---

## 7. Full repository validation

Run serial full suite:

```bash
/usr/bin/time -v .venv/bin/python -m pytest -q   --durations=50 --durations-min=0.25
```

Run it twice on the exact same tested commit.

Record each run separately:

- wall time;
- pytest reported duration;
- max RSS;
- CPU percent if available;
- pass/fail/skip/warning counts;
- top 50 slow tests.

Do not average away a failed or anomalous run.

Also run:

```bash
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

---

## 8. Performance comparison for the combined test restructuring

The implementation commit includes both the P3B seal and restructuring of expensive H40 governance test fixtures.

Measure performance against the immediate parent:

```text
baseline = 6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d
candidate = a858835ac3239f919bb2dcdc87f74db894f899cf
```

Use separate worktrees/checkouts with the SAME:

- interpreter;
- venv dependency versions;
- host;
- background collector state;
- pytest flags.

Run serial full pytest twice on each commit if practical.

At minimum record two candidate runs and one fresh baseline run.

Also measure the top previously slow H40 tests/functions on both commits.

Do not claim a performance improvement from changed test counts alone.

### Acceptance classification

Report one of:

```text
FULL_PYTEST_PERFORMANCE_ACCEPTED
FULL_PYTEST_PERFORMANCE_IMPROVED_BUT_TARGET_NOT_MET
FULL_PYTEST_PERFORMANCE_NOT_IMPROVED
PERFORMANCE_COMPARISON_INCONCLUSIVE
```

Use the previously frozen target for full performance acceptance:

```text
serial median <= 480 seconds
OR
>=25% reduction versus same-host starting serial median
```

and top-five H40 combined time reduction target:

```text
>=40%
```

Performance status is separate from P3B scientific/governance correctness.

---

## 9. Test-coverage equivalence audit for synthetic fixture restructuring

The controller observed that several lifecycle tests previously labelled production now use:

```text
synthetic lifecycle evidence + real production runtime seal
```

Document exactly why this is semantically valid.

The report must state:

1. which tests changed from the former forged exact-type production verifier setup;
2. why the former setup is invalid after the behavior seal;
3. which tests still exercise real production candidate-lock verifier identity/integrity;
4. which tests exercise real production cold restore/replay tamper rejection;
5. which tests verify production WF remains NOT_TESTABLE;
6. why no authorized production WF scientific-success path was removed.

If any previously required production behavior is no longer covered:

```text
STOP
TEST_COVERAGE_EQUIVALENCE_NOT_PROVEN
```

Do not repair it in this task.

---

## 10. Frozen identities

Record exact values from the tested commit:

```text
protocol_authority_hash
scientific_semantic_root
structural_ledger_hash
lifecycle_semantic_root
lifecycle_governance_authority
current ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
```

Expected scientific/governance identities remain:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

The current accepted implementation authority is expected to remain the old value during this evidence task.

Do not update it.

---

## 11. Machine-readable evidence manifest

Create:

```text
evidence/v0.5/h40/V0.5.1_H40_P3B_RUNTIME_BEHAVIOR_SEAL_LOCAL_EVIDENCE.json
```

on the docs branch.

The JSON must contain at least:

```text
schema_id
tested_commit_sha = a858835ac3239f919bb2dcdc87f74db894f899cf
parent_commit_sha = 6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d
generated_at_utc
python_version
pytest_version
focused_test_commands_and_results
full_pytest_runs
ruff_result
mypy_result
compileall_result
git_diff_check_result
frozen_identities
protected_surface_attestation
signed_network_requests = 0
real_h40_outcomes_accessed = false
wf1_calibration_outcomes_accessed = false
confirmation_accessed = false
h39_accessed = false
final_holdout_accessed = false
performance_comparison
coverage_equivalence
```

Use canonical stable JSON formatting.

Compute and report its SHA-256.

---

## 12. Required reports

Create on `v0.5-refactor-docs`:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_VERIFIER_RUNTIME_BEHAVIOR_SEAL_REPAIR_REPORT.md

reviews/v0.5/V0.5.1_H40_PYTEST_RUNTIME_OPTIMIZATION_REPORT.md
```

### Behavior-seal report

Must include:

- starting SHA;
- repair SHA;
- changed files;
- root cause;
- runtime behavior seal mechanism;
- constructor binding;
- class-bound dispatch;
- transitive authority-state checks;
- scientific result binding;
- all focused tests;
- lifecycle regression;
- full pytest;
- frozen identities;
- protected surface attestation.

### Performance report

Must include:

- baseline/candidate SHA;
- same-host methodology;
- raw timing samples;
- median/mean;
- max RSS;
- top 50 before/after where available;
- top-five H40 before/after;
- fixture restructuring explanation;
- coverage equivalence;
- performance acceptance classification.

---

## 13. Do NOT publish implementation authority

This task must NOT modify:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
tests/test_v051_h40_implementation_authority_publication.py
```

and must NOT create an artifact claiming final independent acceptance.

The next authority object will be computed only after controller review of this evidence.

---

## 14. Final allowed state

If every correctness gate passes and reports/evidence are published:

```text
P3B_REACCEPTANCE_EVIDENCE_READY
PENDING_INDEPENDENT_CONTROLLER_REACCEPTANCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

If any correctness gate fails:

```text
P3B_REACCEPTANCE_EVIDENCE_FAILED
P3B_REACCEPTANCE_REQUIRES_CODE_REPAIR
```

Performance failure alone must be reported separately and must not be disguised as correctness failure.

---

## 15. Final response

Return:

```text
tested_commit_sha:
implementation_tree_clean:

behavior_seal_report_path:
performance_report_path:
evidence_manifest_path:
evidence_manifest_sha256:
docs_commit_sha:

behavior_tests:
identity_tests:
p3b_science_tests:
focused_lifecycle_tests:
execution_fence_tests:

full_pytest_run_1:
full_pytest_run_2:
full_pytest_candidate_median:
baseline_full_pytest_samples:
baseline_median:
performance_delta:
top5_h40_delta:
performance_classification:

ruff:
mypy:
compileall:
git_diff_check:

coverage_equivalence_proven:
frozen_identities_unchanged:

accepted_implementation_authority_changed: NO

real_h40_outcomes_accessed: NO
wf1_calibration_outcomes_accessed: NO
confirmation_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
signed_network_requests: 0

final_state:
P3B_REACCEPTANCE_EVIDENCE_READY
PENDING_INDEPENDENT_CONTROLLER_REACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex Sol High
```
