# Agent Prompt — V0.5.1 H40 F01R2 Runtime Source/Split Attestation Implementation

## Mission

Implement the independently accepted F01R2 source/split runtime-authority amendment.

Repository:

```text
EXASHXE/btc-quant-agent
```

Implementation branch:

```text
v0.5-refactor
```

Required starting HEAD:

```text
3eec76fdbc451c6e8a17398b39866558abbd93a3
```

Accepted F01R2 semantics:

```text
reviews/v0.5/
V0.5.1_H40_F01R2_SOURCE_SPLIT_RUNTIME_AUTHORITY_COMPATIBILITY_CLOSURE.md

commit:
e1263f4babc067bb56623081ee27769ac61b7e00
```

Independent acceptance:

```text
reviews/v0.5/
V0.5.1_H40_F01R2_SOL_INDEPENDENT_ACCEPTANCE.md

commit:
c59e153b14d88dff26db705fc0c72218a1a2bb76
```

Read both completely before modifying code.

This task is implementation only. Do not redesign the accepted semantics.

---

# 1. Frozen identities

The scientific identities must remain exactly:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Accepted amended lifecycle identities:

```text
runtime_snapshot_seal_contract_hash =
76a0732742707c78f65da26263076bed7b586ea67d4f5ddd2dac33761e37e612

lifecycle_semantic_root_hash =
fad50703c12da9af35366ceb8a774794b4ff2f3163a1adb6b624d3b69316e406

lifecycle_governance_authority_hash =
7edce39c421ad6c487580483d2fa674b1f199e21640c33b1c08f85dddc6f64fc
```

Current accepted source/runtime projection remains:

```text
BTCUSDT_USD_M_1H                 NOT_TESTABLE
ETHUSDT_USD_M_1H                 VERIFIED
BTCUSDT_OFFICIAL_DERIVATIVES     NOT_TESTABLE
BTCUSDT_SPOT_FLOW                NOT_TESTABLE
ETHUSDT_DERIVATIVES_FLOW         NOT_TESTABLE

TOTAL = 168
REGISTERED = 18
NOT_TESTABLE = 150

active_required_source_ids =
["ETHUSDT_USD_M_1H"]
```

Do not promote BTC or change this projection.

---

# 2. Required implementation

Implement exactly the accepted F01R2 amendment.

## 2.1 New typed runtime source/split attestation

Add immutable canonical representation for:

```text
H40_RUNTIME_SOURCE_SPLIT_ATTESTATION_V1
```

It must implement exact-key parsing and canonical hashing.

Required nested structures:

### Source authority state entry

```text
source_id
production_authority_state
```

State must be one of:

```text
VERIFIED
NOT_TESTABLE
```

and equal the accepted RuntimeAuthoritySnapshot.

### Active source evidence entry

```text
source_id
source_record_hash
source_validation_receipt_hash
file_sha256
timestamp_membership_hash
timestamp_count
```

All fields must be derived/recomputed.

No caller authority from paths or raw hashes.

### Runtime attestation

Bind exactly the accepted contract fields from the F01R2 closure, including:

```text
accepted_reference_source_manifest_hash
accepted_reference_split_manifest_hash
protocol_identity_hash
runtime_authority_snapshot_id
runtime_authority_snapshot_hash
source_authority_state_entries
source_manifest_hash
structural_ledger_hash
sealed_registered_roster_hash
registered_slot_count
not_testable_slot_count
total_slot_count
active_required_source_ids
not_testable_source_ids
active_source_evidence
split_manifest_hash
schema_id
```

The attestation hash is canonical SHA-256 over the exact canonical preimage.

---

# 3. Separate canonical identity from production availability

Do not weaken existing:

```text
assert_canonical_source_record(...)
```

if it currently means:

```text
canonical identity + must be production VERIFIED
```

Instead add a clearly named identity-only verifier.

It must validate frozen source identity fields such as:

```text
source_id
product
cadence
locator
reference_file_sha256 where frozen
archive_set_sha256 where frozen
protocol identity
```

but it must not grant or infer production availability.

Production status comes only from the accepted RuntimeAuthoritySnapshot.

Required behavior:

```text
BTC identity may validate
while BTC production state remains NOT_TESTABLE
```

A locally present BTC artifact must not upgrade it.

---

# 4. Derive runtime source states from accepted authority

Do not accept caller-supplied source-state maps.

Use the existing accepted runtime authority projection:

```text
current_p1_authority_snapshot()
```

or its exact accepted equivalent.

Recompute:

```text
runtime_authority_snapshot_id
runtime_authority_snapshot_hash
complete source_authority_state_entries
not_testable_source_ids
```

from accepted authority.

Any mismatch must fail closed.

---

# 5. Derive exact REGISTERED roster and active source union

Continue deriving the exact 168-slot ledger from:

```text
materialize_h40_search_space_production()
```

Verify:

```text
structural ledger hash
total = 168
REGISTERED = 18
NOT_TESTABLE = 150
exact roster
roster hash
```

Then derive:

```text
active_required_source_ids
```

as the union of:

```text
project_required_sources(slot)
```

for every exact REGISTERED slot.

Do not hard-code ETH as the active source set merely because it is currently expected.

For the current accepted snapshot, natural derivation must yield:

```json
["ETHUSDT_USD_M_1H"]
```

For every REGISTERED slot, prove all required source IDs are VERIFIED in the accepted snapshot.

For every NOT_TESTABLE source, forbid active source evidence.

---

# 6. Runtime source manifest

Construct or verify a typed runtime source manifest containing the exact run evidence inventory.

Requirements:

- protocol identity must match;
- source identities must match the accepted reference manifest;
- source records may reflect runtime receipts/evidence;
- production state may not be inferred from local file presence;
- the exact runtime source manifest hash must be bound into the runtime attestation and run authority.

Do not mutate the preregistered reference manifest in place.

Do not relabel the reference manifest as runtime evidence.

---

# 7. Cold-validate every and only active required source

For each active required source:

1. require accepted runtime state VERIFIED;
2. require typed source record;
3. require typed VERIFIED source receipt;
4. identity-check against preregistered reference;
5. cold-validate local bytes/timestamps;
6. recompute:
   - file SHA;
   - timestamp membership hash;
   - timestamp count;
7. require equality with source record/receipt;
8. produce exact active source evidence entry.

For every NOT_TESTABLE source:

- identity may be checked;
- do not read its local artifact merely to grant authority;
- do not emit active source evidence;
- do not promote it.

For current snapshot:

```text
ETH is cold-validated
BTC is not active evidence
```

---

# 8. Runtime-scoped split authority

Do not reuse the legacy BTC/ETH pair H40SplitAttestation.

Do not treat preregistered schedule `is_authoritative=False` as production evidence.

Implement a runtime-scoped authoritative split path that:

## 8.1 Static calendar validation

Verifies immutable calendar semantics against the accepted preregistered split:

- partition IDs;
- fold;
- partition type;
- half-open start/end;
- lookback reservation;
- purge structure;
- horizon truncation rules.

## 8.2 Availability reconstruction

Reconstructs availability-dependent split fields from the intersection of exactly:

```text
active_required_source_ids
```

For the current run this is ETH-only.

Recompute:

- partition counts;
- first/last timestamps;
- timestamps_sha256;
- source-gap exclusions;
- base eligible count;
- other accepted deterministic exclusions.

## 8.3 Runtime split identity

Create/verify the typed runtime split manifest whose:

```text
source_manifest_hash
protocol_identity_hash
split_manifest_hash
```

are bound to the exact runtime run.

The runtime split may be marked authoritative only under the new typed runtime source/split attestation.

The boolean alone is never authority.

---

# 9. Runtime seal repair

Replace the F01R1 production seal path that requires the legacy pair:

```text
split_manifest.assert_authoritative(...)
```

with the accepted F01R2 runtime authority verifier.

Production seal construction must require typed objects sufficient to prove:

```text
accepted RuntimeAuthoritySnapshot
exact 168-row ledger
exact 18-row roster
runtime source manifest
runtime split manifest
H40_RUNTIME_SOURCE_SPLIT_ATTESTATION_V1
repo_root / cold evidence
```

Do not accept independent caller hashes.

The seal must bind:

```text
runtime_authority_snapshot_hash
source_manifest_hash
split_manifest_hash
split_attestation_hash
sealed_registered_roster_hash
registered_slot_count
not_testable_slot_count
total_slot_count
```

and continue using the F01R1 internal production construction token.

---

# 10. Update lifecycle canonical authority

Update only the accepted amended lifecycle surfaces.

The nine-child lifecycle tree must now naturally compute:

```text
runtime child =
76a0732742707c78f65da26263076bed7b586ea67d4f5ddd2dac33761e37e612

lifecycle root =
fad50703c12da9af35366ceb8a774794b4ff2f3163a1adb6b624d3b69316e406
```

Update lifecycle governance materialization to V3 and naturally compute:

```text
7edce39c421ad6c487580483d2fa674b1f199e21640c33b1c08f85dddc6f64fc
```

Use exact accepted V3 preimage from:

```text
V0.5.1_H40_F01R2_SOL_INDEPENDENT_ACCEPTANCE.md
```

Do not change the other eight child preimages.

Do not change any scientific hash.

---

# 11. Cold persistence / replay

Retain F01R1 cold restore.

Extend the resolver/context only as needed so restore can independently re-resolve and reverify:

```text
runtime source manifest
runtime split manifest
runtime source/split attestation
accepted snapshot
active-source cold evidence
```

A cold restart must not require original process-local authority objects.

On restore:

- recompute attestation hash;
- recompute source/split lineage;
- re-cold-validate active evidence where accepted semantics require;
- reject changed active-source bytes;
- reject source state changes under old run;
- reject cross-run source/split/attestation substitution.

Paths remain locators only.

---

# 12. Scientific verifier seam

Preserve the F01R1 typed:

```text
H40LifecycleEvidenceVerifier
```

Do not implement real Discovery statistical science in F01R2.

Production remains:

```text
no accepted scientific verifier
=> Candidate Lock/WF fail closed
```

A future P3 verifier will use this seam.

---

# 13. F02 / protected surfaces

Do not change:

```text
H40_CONFIRMATION_READY -> H40_CONFIRMATION_EVALUATED_ONCE
```

It remains prohibited.

Do not access:

```text
H39 protected outcomes
Final Holdout
Confirmation outcomes
```

Do not add confirmation nonce/unlock/evaluator.

---

# 14. Mandatory adversarial tests

Add at minimum:

```text
R01 amended runtime child hash recomputes exactly
R02 amended lifecycle root recomputes exactly
R03 V3 governance authority hash recomputes exactly
R04 three scientific hashes unchanged

R05 current runtime projection remains 18/150
R06 active source union naturally derives exactly ETH-only
R07 active source union is not hard-coded

R08 identity-only BTC validation does not promote BTC
R09 locally present BTC remains NOT_TESTABLE
R10 status-only BTC promotion rejected
R11 runtime snapshot substitution rejected

R12 source-state missing entry rejected
R13 source-state extra entry rejected
R14 source-state duplicate entry rejected
R15 source-state reorder/noncanonical representation rejected if contract requires canonical order
R16 not_testable_source_ids mismatch rejected

R17 missing ETH active evidence rejected
R18 extra BTC active evidence rejected
R19 active source record hash tamper rejected
R20 active source receipt hash tamper rejected
R21 active source file hash tamper rejected
R22 membership hash tamper rejected
R23 timestamp count tamper rejected

R24 legacy BTC/ETH pair attestation rejected as runtime authority
R25 synthetic attestation rejected
R26 preregistered schedule-only authority rejected

R27 static split boundary mutation rejected
R28 split fold/partition type mutation rejected
R29 runtime availability count mismatch rejected
R30 runtime timestamp membership mismatch rejected

R31 source manifest / split manifest cross-pair rejected
R32 split / attestation cross-pair rejected
R33 runtime snapshot / roster mismatch rejected

R34 post-seal source promotion cannot expand run
R35 post-seal source disappearance fails revalidation
R36 later accepted snapshot creates different seal/run identity

R37 cold restore succeeds after process-local objects discarded
R38 cold restore rejects changed active source bytes
R39 cold restore rejects runtime attestation substitution
R40 cross-run source/split/attestation replay rejected

R41 production seal succeeds through real F01R2 authority validation path without monkeypatching split assertion
R42 direct production-seal constructor remains blocked
R43 synthetic test objects remain synthetic-only

R44 F02 remains sealed
R45 H39 remains protected
R46 Final Holdout remains sealed
```

All tests must remain pre-outcome.

Do not read any H40 result labels/returns.

---

# 15. Local validation only

Run locally:

```bash
ruff check .
mypy
pytest -q tests/test_v051_h40_f01_lifecycle_authority.py
pytest -q tests/test_v051_h40_p1_scaffold.py tests/test_v051_h40_p2_search_space.py tests/test_v051_h40_p2r1_contract_materialization.py
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

Report exact local results.

Do not:

```text
trigger workflow_dispatch
rerun Actions
poll Actions
wait for Actions
modify CI
treat quota failure as a code defect
```

If an automatic workflow run fails due quota after normal push:

```text
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

and do nothing further to CI.

---

# 16. Git discipline

Modify code only on:

```text
origin/v0.5-refactor
```

Do not modify docs branch.

Do not merge:

```text
forward-evidence-health-lightweight
```

Keep the repair narrowly within the H40 authority/source/split surfaces and required tests.

Do not refactor unrelated repository architecture.

Commit and push.

---

# 17. Allowed completion claims

A successful executor may claim only:

```text
H40_F01R2_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_REVIEW
H40_RUNTIME_SOURCE_SPLIT_ATTESTATION_V1_IMPLEMENTED
H40_LIFECYCLE_SEMANTIC_ROOT_V2_IMPLEMENTED
H40_LIFECYCLE_GOVERNANCE_AUTHORITY_V3_IMPLEMENTED

H40_FSA_F01_IMPLEMENTATION_NOT_YET_FINAL_ACCEPTED
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_ACCEPTED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_FSA_F02_OPEN_SEALED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED

REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

Do not claim:

```text
F01_FINAL_ACCEPTED
P3_AUTHORIZED
DISCOVERY_AUTHORIZED
ALPHA_FOUND
PROFITABILITY_VALIDATED
```

---

# 18. Final response

Report:

```text
starting SHA
final implementation commit SHA
changed files

runtime attestation implementation
identity-only source verification implementation
active source union derivation
runtime split implementation
production seal implementation
cold restore changes
new canonical hashes

adversarial test list
local ruff
local mypy
targeted pytest
full pytest
compileall
git diff --check

confirmation BTC remains NOT_TESTABLE
confirmation current projection remains 18/150
confirmation active source union is derived, not hard-coded
confirmation no H40 outcomes accessed
confirmation F02 sealed
confirmation H39 protected
confirmation Final Holdout sealed
confirmation no CI action triggered

REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

Recommended executor:

```text
Codex
GPT-5.6 Sol
Reasoning: High
```

Do not downgrade this task to a lightweight coding model.
