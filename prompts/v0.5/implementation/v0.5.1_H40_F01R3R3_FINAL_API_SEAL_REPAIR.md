# V0.5.1 H40 F01R3R3 Final API Seal Repair

## Mission

Perform the final implementation-only API seal repair for F01R3.

Required starting implementation HEAD:

```text
e0e9bdbeccd221a9d685f0a01446692eb95b0b3b
```

Read first:

```text
reviews/v0.5/V0.5.1_H40_F01R3R2_IMPLEMENTATION_SOL_INDEPENDENT_REVIEW.md
```

This repair has exactly one objective:

> No caller may commit the durable H40 lifecycle head except through `persist_authorization(..., service=<exact issuing H40LifecycleAuthorityService>)`.

Do not change canonical hashes.
Do not start Track B.
Do not access real outcome data.

## Frozen hashes

```text
persistence_replay_contract_v2_hash =
5f014b867be17019c2be91ff48f8e23a43ed29678fc5589430ba174046fceaae

lifecycle_semantic_root_hash_v3 =
f0aa35eb91055a76129eae5c00be38174fcc13ae9fe08521d9202e93dc5d43d4

lifecycle_governance_authority_v4_hash =
72924dd22b3c9283964cdf368f7a754bd6f703a110989187620b36076a25a511
```

Three scientific hashes and 168/18/150 must remain unchanged.

## Required code changes

1. Remove `TEST_COMMIT_TOKEN` from all production exports.
2. Remove it from `btc_quant_agent.h40.__init__`.
3. Remove it from lifecycle-authority `__all__`.
4. Remove the public raw `advance_run_head(...)` authority surface.
5. Remove any optional `authorization=None` commit path.
6. Keep one private internal durable-head commit primitive requiring a non-null `VerifiedLifecycleAuthorization`.
7. That private primitive must be invoked only from `persist_authorization()` after:
   - exact issuing-service revalidation,
   - root-source revalidation,
   - immutable receipt publication,
   - exact envelope/receipt verification.
8. Keep all R2 transition adjacency/genesis/F02/head-integrity checks in the private primitive.
9. Do not add a public boolean/token/flag that recreates raw commit authority.
10. Do not expose a test-only capability from the production package.

## Test migration

Tests that currently use `TEST_COMMIT_TOKEN` or raw `advance_run_head()` must be rewritten.

Use:
- real synthetic `VerifiedLifecycleAuthorization`;
- the exact issuing synthetic service;
- `persist_authorization(..., service=...)`;
- direct SQLite corruption only for integrity/tamper tests.

Concurrency tests must race real `persist_authorization()` calls using valid sibling authorizations.

Do not simulate authority by writing mock envelopes and calling a raw commit API.

## Required tests R46-R52

```text
R46 TEST_COMMIT_TOKEN cannot be imported/exported from btc_quant_agent.h40
R47 H40LifecycleArtifactStore exposes no public raw head-commit method
R48 stale cached production authorization cannot directly commit without service revalidation
R49 orphan exact receipt cannot be committed merely from authorization possession
R50 valid transitions commit only through persist_authorization with exact issuer
R51 two valid sibling authorizations racing through persist_authorization yield one winner
R52 head-integrity corruption tests do not depend on raw commit authority
```

Keep R01-R45 and all previous F01/P1/P2 tests passing.

## Local gates

Run:

```text
ruff check .
mypy
pytest -q tests/test_v051_h40_f01_lifecycle_authority.py
pytest -q tests/test_v051_h40_f01r3r1_durable_authority.py
pytest -q tests/test_v051_h40_f01r3r2_commit_authority.py
pytest -q tests/test_v051_h40_f01r3r3_api_seal.py
pytest -q tests/test_v051_h40_p1_scaffold.py tests/test_v051_h40_p2_search_space.py tests/test_v051_h40_p2r1_contract_materialization.py
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

Do not trigger/rerun/poll/wait for GitHub Actions.

Normal push is allowed.

## Git scope

Allowed:
- src/btc_quant_agent/h40/lifecycle_authority.py
- src/btc_quant_agent/h40/__init__.py
- focused F01R3 tests

Do not modify execution Track B.
Do not modify docs branch.
Do not broad-refactor.

Suggested commit:

```text
fix(h40): close final F01R3 commit API bypass
```

## Allowed completion claim

```text
H40_F01R3R3_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_REVIEW
```

Do not claim F01 final acceptance, lifecycle implementation authority acceptance, Astra PASS, P3 authorization, Discovery authorization, or Track B completion.

## Final response fields

```text
starting_sha:
final_commit_sha:
changed_files:

public_raw_commit_surface_removed:
test_commit_token_export_removed:
persist_authorization_only_commit_path:
stale_authorization_direct_commit_impossible:
transition_adjacency_preserved:
f02_sealed:

R46_R52_result:
targeted_f01_result:
targeted_r1_result:
targeted_r2_result:
targeted_r3_result:
full_pytest_result:
ruff_result:
mypy_result:
compileall_result:
git_diff_check_result:

persistence_v2_hash:
lifecycle_root_v3_hash:
governance_v4_hash:
canonical_hashes_unchanged: YES

projection_168_18_150_unchanged: YES
btc_not_testable_unchanged: YES

outcome_surfaces_accessed: NO
execution_track_b_modified: NO
ci_triggered_or_polled: NO

final_state:
H40_F01R3R3_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_REVIEW
```

Recommended executor: Codex GPT-5.6 Sol High.
