# Task Prompt — H40 P2R1 Authorized Contract-Materialization Repair

## Role

Act as the implementation engineer for the now-accepted H40 protocol.

This task is **implementation conformance only**. Do not redesign H40, do not choose alternative semantics, and do not use outcomes to resolve ambiguity.

Repository:

```text
EXASHXE/btc-quant-agent
```

Code branch to modify:

```text
v0.5-refactor
```

Docs authority branch is read-only:

```text
v0.5-refactor-docs
```

## Mandatory preflight

Before editing anything, verify all of the following exact authorities.

### Code HEAD

```text
52ac1dcb0843458b1b20b943be920f139dc832d1
```

If `origin/v0.5-refactor` is not exactly this SHA at task start, stop and report the mismatch. Do not silently rebase this task onto a different implementation state.

### Final protocol acceptance

```text
acceptance commit:
cf69d5295e2b0227920e265d9afe7dcf831af44e

acceptance artifact:
reviews/v0.5/V0.5.1_H40_R3R4_SOL_FINAL_CUMULATIVE_ACCEPTANCE.md
```

### Independent ratification

```text
ratification commit:
9a00782f1d8cc3fb328a6e36e5febf402d49b226

ratification artifact:
reviews/v0.5/V0.5.1_H40_R3R4_SOL_INDEPENDENT_RATIFICATION.md
```

### Accepted immutable identities

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

configuration_ledger_identity / structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

The implementation must naturally reproduce these identities from canonical preimages. Do **not** hard-code a final root or ledger hash merely to satisfy a test.

## Governing protocol chain

Read the exact committed versions before implementation:

```text
Frozen kernel
6838e9db8d5c369b5da87354821d9c8e79c2a879

BASE
8b36cfde2ac14a37ed4eb244a8b5ae88882765b4

R1
59e9fe0ba361557c5df56f0d8f8afaa781cf0071

R2
4bdeb1a0102b3cb374e8ec7d01b41a79dcc8b021

R2 acceptance
b9c8531e719fd4f10cf27301719c9e066b95be13

P1 accepted code baseline
3fc89541ed0965fc0e2972af310e34ae1b838168

P1 final acceptance
95ab819d5300312b4d493b9585b4b369621504b6

R3
e45899bb0118b14127bc765f49315109cfd94ff0

R3R1
3f1bf28dc810ca4fd1bfd2bef566033cd15550fa

R3R2
77601342ac9055b5c0bb47639d4c8ef7d83da154

R3R3
f01062b4b1a20d12f93ee1351bfda67e19401f25

R3R4
7520a62d0516ee097c276451b9f8df5924c6408f
```

Where clauses conflict, later accepted amendments supersede only the conflicting clause. Do not infer semantics from the provisional P2 code when the accepted protocol says otherwise.

---

# Hard information boundary

This phase is still pre-outcome contract implementation.

You MUST NOT compute, inspect, load, derive, summarize, or use real H40:

- labels / forward returns;
- realized MFE/MAE;
- precision / expectancy / PF;
- ECE/Brier/Brier-skill results from market data;
- bootstrap/HAC research outcomes;
- WF1 validation or WF2-WF4 outcomes;
- confirmation outcomes;
- H39 protected outcomes/artifacts;
- Final Holdout data/outcomes.

Do not run a discovery/backtest/confirmation runner.

Synthetic unit-test values are allowed only to verify pure deterministic semantics and guards.

Any real outcome exposure is a hard stop:

```text
H40_P2R1_OUTCOME_EXPOSURE_ABORT
```

Execution remains disabled. Do not touch paper/testnet/live order paths.

---

# Scope

Repair the provisional P2 implementation so it becomes an exact contract/search-space materialization of accepted `H40_PROTOCOL_V1_R3`.

Primary existing surfaces include:

```text
src/btc_quant_agent/h40/__init__.py
src/btc_quant_agent/h40/configuration_ledger.py
src/btc_quant_agent/h40/feature_contracts.py
src/btc_quant_agent/h40/guards.py
src/btc_quant_agent/h40/protocol.py
src/btc_quant_agent/h40/search_space.py
src/btc_quant_agent/h40/source_manifest.py
src/btc_quant_agent/h40/split_manifest.py

tests/test_v051_h40_p1_scaffold.py
tests/test_v051_h40_p2_search_space.py
```

You may add narrowly scoped H40 modules/tests if this materially improves separation of authority/hash contracts. Do not refactor unrelated v0.5 code.

Do not modify collectors, Binance REST/WS ingestion, live runtime services, production execution adapters, or unrelated historical research modules.

---

# Required repair domains

## 1. Replace caller-forgeable authority with canonical production authority

The current provisional P2 accepts caller-supplied protocol/source/split identity strings in places where production authority must be derived or verified.

Repair this.

### Production materialization

Production-facing H40 materialization must derive/verify:

- accepted protocol identity from the exact cumulative authority chain;
- accepted P1 source authority/specs;
- accepted P1 split authority/attestation;
- accepted semantic root;
- accepted structural ledger identity.

Arbitrary caller strings must never become production authority simply because they are syntactically valid hashes.

### Synthetic/test builders

If tests require custom identities, provide an explicitly named synthetic/non-authoritative builder or fixture path.

Synthetic materialization must be impossible to confuse with production materialization.

Tests must prove that production APIs reject forged authority inputs.

---

## 2. Implement one canonical semantic-hash tree and produce an executable hash receipt

Use repository:

```python
btc_quant_agent.research_contract.canonical.canonical_json
btc_quant_agent.research_contract.canonical.canonical_sha256
```

exactly.

Implement canonical contract objects for every active semantic-root child required by R3R2/R3R3/R3R4, including:

- active feature registry;
- active direction registry;
- active gate registry;
- geometry contract;
- Platt calibration contract;
- isotonic calibration contract;
- strict closed-bar availability contract;
- mechanical refit policy;
- reserved-family vocabulary;
- R3R4 side-preserving-action contract;
- R3R4 isotonic-prediction-map contract;
- R3R4 ECE contract;
- R3R4 discovery-selection/correction contract;
- R3R4 p0-context contract;
- R3R4 training-sample-floor-population contract;
- R3R4 RANDOM_MATCHED benchmark contract;
- R3R4 side-specific-n_eff contract.

Every material semantic field must exist in the canonical object preimage. Textual IDs alone are not content hashes.

### Mandatory oracle proof

From these canonical objects, compute:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1
```

Then materialize the exact 168 structural slots and compute:

```text
structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

### No hard-coding loophole

It is acceptable to store the accepted hashes as expected-oracle constants.

It is NOT acceptable for the computation function to simply return those constants.

Tests must mutate at least one material semantic field under the same textual ID and demonstrate:

```text
child hash changes
-> semantic root changes
-> structural configuration hash changes
-> structural ledger hash changes
```

Generate a deterministic receipt object/artifact in tests containing:

```text
protocol_authority_hash
all semantic-root child hashes
semantic_root_hash
168 ordered structural_configuration_hash values
168 ordered slot_hash values
structural_ledger_hash
```

The receipt may contain no market outcomes.

---

## 3. Materialize the exact accepted 168-row search ledger

### Exact single-family variant order

```text
D1_V1_RETURN_4H
D1_V2_RETURN_12H
D2_V1_BREAKOUT_24H
D2_V2_BREAKOUT_72H
D3_V1_FAILED_BREAK_24H
D3_V2_FAILED_BREAK_72H
D4_V1_CONFIRMATION_4H
D4_V2_DIVERGENCE_8H
D5_V1_CROWDING_Q10_D1
D5_V2_CROWDING_Q20_D2
```

Canonical family meanings are:

```text
D1 = TREND_CONTINUATION
D2 = BREAKOUT_CONTINUATION
D3 = FAILED_MOVE_REVERSAL
D4 = BTC_ETH_CONFIRM_DIVERGE
D5 = FUNDING_DIRECTION_INTERACTION
```

Do not use legacy/shorthand family labels that swap D2/D3/D4/D5 meanings.

### Exact pair order

```text
PAIR_D1_D4_V1
PAIR_D1_D5_V1
PAIR_D2_D4_V1
PAIR_D3_D5_V1
```

Pair order is semantic: base owner first, secondary confirm/veto family second.

### Exact 12-row template for every variant/pair

```text
index  horizon  scope            threshold  calibration
0      4h       BTC_ONLY         0.55       CALIBRATION_PLATT_LOGISTIC_V1
1      4h       ETH_ONLY         0.55       CALIBRATION_PLATT_LOGISTIC_V1
2      4h       POOLED_BTC_ETH   0.55       CALIBRATION_PLATT_LOGISTIC_V1
3      8h       BTC_ONLY         0.55       CALIBRATION_PLATT_LOGISTIC_V1
4      8h       ETH_ONLY         0.55       CALIBRATION_PLATT_LOGISTIC_V1
5      8h       POOLED_BTC_ETH   0.55       CALIBRATION_PLATT_LOGISTIC_V1
6      12h      BTC_ONLY         0.55       CALIBRATION_PLATT_LOGISTIC_V1
7      12h      ETH_ONLY         0.55       CALIBRATION_PLATT_LOGISTIC_V1
8      12h      POOLED_BTC_ETH   0.55       CALIBRATION_PLATT_LOGISTIC_V1
9      4h       POOLED_BTC_ETH   0.60       CALIBRATION_ISOTONIC_IF_ELIGIBLE_V1
10     8h       POOLED_BTC_ETH   0.65       CALIBRATION_PLATT_LOGISTIC_V1
11     12h      POOLED_BTC_ETH   0.60       CALIBRATION_ISOTONIC_IF_ELIGIBLE_V1
```

Materialization order:

```text
slots 0..119   = 10 variants x template indices 0..11
slots 120..167 = 4 pairs x template indices 0..11
```

No replenishment, substitution, random expansion, Bayesian search, or outcome-dependent filtering.

Duplicate structural configuration identities must fail closed with `CONFIG_IDENTITY_CONFLICT`.

---

## 4. Correct D5 base ownership and pair semantics

There is no implicit D5 base-owner default.

A missing/mismatched D5 base family/variant/contract/feature must reject/fail closed.

Required accepted bindings include:

```text
D5_V1_CROWDING_Q10_D1
  base = D1_V1_RETURN_4H

D5_V2_CROWDING_Q20_D2
  base = D2_V1_BREAKOUT_24H

PAIR_D3_D5_V1
  base = D3_V1_FAILED_BREAK_24H
  D5 component = D5_PAIR_CROWDING_Q10_D3
```

Do not inject D1 when a D5 owner is omitted.

Required feature ordering is semantic and must not be blindly sorted when order expresses pipeline ownership.

---

## 5. Preserve exact P1 source authority and derive runtime testability mechanically

Do not promote any source.

Current accepted P1 production authority:

```text
BTCUSDT_USD_M_1H               NOT_TESTABLE
ETHUSDT_USD_M_1H               VERIFIED
BTCUSDT_OFFICIAL_DERIVATIVES   NOT_TESTABLE
ETHUSDT_DERIVATIVES_FLOW       NOT_TESTABLE
```

Source projection rules remain:

```text
OWN_ASSET_BY_SCOPE
CROSS_ASSET_ALWAYS
FUNDING_OWN_PRODUCT
```

Runtime status is derived from the projected required-source set:

```text
REGISTERED    iff every required source is production VERIFIED
NOT_TESTABLE  otherwise
```

The exact current pre-seal materialization must produce:

```text
18 REGISTERED
150 NOT_TESTABLE
```

These statuses must NOT alter structural slot inclusion or structural configuration identity.

No failed row is replaced or reallocated.

---

## 6. Structural identity and runtime authority must be separate objects

Structural configuration identity binds:

- protocol authority hash;
- semantic root hash;
- scope;
- horizon;
- family combination;
- exact direction/pair variant;
- calibration identity;
- threshold;
- abstention policy;
- ordered required feature IDs;
- exact feature/lookback parameters;
- logical source requirement identity;
- accepted availability/cost/refit contract identities.

It must not bind mutable `VERIFIED/NOT_TESTABLE` runtime state.

Runtime authority snapshot must separately bind the canonical per-source production authority states and its own snapshot identity.

Expose a materialized-run authority identity that binds:

```text
structural_ledger_hash
+
runtime_authority_snapshot_hash
```

Changing runtime authority without changing science may change run authority but must not mutate structural configuration/ledger identity.

---

## 7. Encode exact active feature semantics; do not invent alternatives

P2R1 must encode the accepted contract semantics for the active features and gates.

At minimum enforce the exact R3R3 relative-bar convention:

```text
B_j(t) = canonical 1h candle [t-(j+1)h, t-jh)
B_0    = just-completed bar before decision t
all candle-derived feature bars: closed == true and close_time_ms < t
```

Key accepted formulas include:

```text
D1 W in {4,12}:
  raw_score = log(C_0 / C_W)

D2 W in {24,72}:
  test B_0
  prior range B_1..B_W
  C_0 > max(H_1..H_W) => +1
  C_0 < min(L_1..L_W) => -1
  equality/no break => 0

D3 W in {24,72}:
  close-back B_0
  break B_1
  prior range B_2..B_(W+1)
  strict L_W < C_0 < U_W
  up-break then close-back => -1
  down-break then close-back => +1
```

Preserve R3R1/R3R3 exact D4 target-relative semantics, R_VOL formula, O_RANGE formula, percentile/quantile rules, D5 last-settled crowding and tail-veto semantics.

This phase need not execute these formulas over real market data. Pure functions over synthetic rows are allowed if useful to prove indexing/causality.

---

## 8. Encode accepted calibration/statistical-decision contracts without starting discovery

Represent the accepted contracts in canonical semantic objects and, where narrowly useful, pure deterministic helpers over synthetic inputs.

Required semantics include:

- Platt: `p_up = sigmoid(a*raw_score+b)`, `a >= 0`, specified degeneracies fail closed;
- isotonic prediction: clipped left-step map;
- proposed side = `sign(raw_score)`;
- calibration is confidence-only and cannot reverse side;
- exact ECE adaptive bins;
- exact p0 contexts;
- exact discovery correction/complexity/tie-break contract;
- exact training-fit sample-floor population;
- exact RANDOM_MATCHED contract;
- exact side-specific `n_eff` contract.

Do NOT build or run an H40 discovery evaluator in P2R1.

Do NOT compute labels, MFE/MAE, market ECE/Brier, bootstrap results, candidate rankings, or benchmark PnL from real data.

---

## 9. Harden protected/outcome guards recursively

Replace permissive lexical/top-level checks with exact structural validation.

At minimum tests must reject:

- nested outcome/label/forward-return keys;
- confirmation outcome paths/references;
- H39 protected references;
- Final Holdout references;
- future/equal-timestamp candle availability;
- caller-forged source/protocol/split authority;
- unauthorized family/pair;
- implicit D5 owner;
- source/hash mismatch.

Do not introduce new global H40 reason codes unless already authorized by the accepted protocol.

Use the existing stable reason vocabulary.

---

# Required adversarial tests

Add/repair tests that prove all of the following.

## Authority

1. Production materializer cannot accept arbitrary protocol/source/split hashes.
2. Synthetic/test-only authority cannot be mistaken for production authority.
3. Published `protocol_authority_hash` is independently recomputed exactly.

## Hash tree

4. Canonical semantic objects reproduce the accepted `semantic_root_hash`.
5. Exact 168 slots reproduce accepted `structural_ledger_hash`.
6. Hash receipt contains every semantic-root child and all 168 slot hashes.
7. A material semantic mutation under the same textual ID changes the semantic root and ledger.
8. Final accepted root/ledger constants are not used as the return value of the hash computation.

## Grid

9. Exactly 168 rows; exact variant/pair/template ordering.
10. Duplicate structural identities reject.
11. Family combination and required-feature semantic order survive round-trip.
12. Scope/asset invariants:
    - BTC_ONLY => BTCUSDT only
    - ETH_ONLY => ETHUSDT only
    - POOLED_BTC_ETH => (BTCUSDT, ETHUSDT) canonical order.

## Source authority

13. Current snapshot derives exactly 18 REGISTERED / 150 NOT_TESTABLE.
14. BTC-dependent/cross-asset/funding rows fail closed under current P1 authority.
15. Runtime status mutation does not change structural ledger hash.

## D5/pairs

16. Missing D5 base owner rejects.
17. Q20 D5 is D2-owned, not D1-owned.
18. D3+D5 uses D3-owned pair-scoped D5 contract.

## Causality/guards

19. Current/equal/future candle cannot enter a closed-bar feature.
20. Missing/gapped required windows fail closed.
21. Nested forbidden outcome/protected references reject.
22. Confirmation/H39/Final Holdout references reject.

## Calibration/statistical contract semantics (synthetic only)

23. Negative raw_score can never emit LONG; positive raw_score can never emit SHORT.
24. Isotonic prediction is clipped left-step, never interpolation.
25. ECE binning is deterministic under ties.
26. RANDOM_MATCHED seed preimage/RNG sequence is deterministic on synthetic universes.
27. Side-specific `n_eff` contract uses side subsets, not proportional pooled scaling.

---

# Validation discipline

First run narrow tests while repairing.

Before push, run repository-required quality gates:

```text
ruff
mypy
full pytest
```

Use the repository's actual configured commands if wrappers differ.

Do not claim Python 3.11/3.12 compatibility from a Python 3.13-only automatic workflow.

After local/full validation succeeds:

1. commit;
2. push to `origin/v0.5-refactor`;
3. inspect exact remote HEAD;
4. inspect exact-head automatic CI;
5. if the project still requires explicit Python 3.11/3.12 compatibility dispatch, do that only after semantic tests are green and report exact runs separately.

Do not change GitHub workflows merely to make this task pass unless an existing workflow itself is demonstrably broken and the change is separately justified.

---

# Deliverable report

Create/update an implementation report on the docs branch only if the established repository workflow requires it; otherwise report in the task response.

Report:

```text
exact final code commit SHA
changed files
new/changed tests
ruff result
mypy result
pytest result
exact CI run(s) and Python versions actually exercised
computed protocol_authority_hash
computed semantic_root_hash
computed structural_ledger_hash
REGISTERED/NOT_TESTABLE counts
confirmation that no H40/protected outcome data were accessed
confirmation that execution remained disabled
```

## Required final status

Only if all accepted oracles and tests pass:

```text
H40_P2R1_IMPLEMENTATION_REPAIR_COMPLETE
H40_P2_PENDING_INDEPENDENT_ACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

Do not claim P2 accepted. Independent review remains required after this implementation task.
