# Agent Prompt — V0.5.1 H40 P3B E402 Test-Lint Repair

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = MINIMAL_TEST_LINT_REPAIR_ONLY
PRODUCTION_CODE_CHANGES = FORBIDDEN
SCIENTIFIC_LOGIC_CHANGES = FORBIDDEN
TEST_SEMANTIC_CHANGES = FORBIDDEN
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required starting HEAD:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

Root cause confirmed by controller review:

```text
ruff check . fails only because commit a858835 removed
the pre-existing # noqa: E402 markers from three test imports
that intentionally occur after sys.path mutation.
```

Affected files only:

```text
tests/test_v051_h40_f01r3r1_durable_authority.py
tests/test_v051_h40_f01r3r2_commit_authority.py
tests/test_v051_h40_f01r3r3_api_seal.py
```

---

## 1. Mission

Restore the exact E402 suppression that existed in parent commit:

```text
6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d
```

for the three imports that intentionally follow:

```python
_TESTS_DIR = str(Path(__file__).resolve().parent)
if _TESTS_DIR not in sys.path:
    sys.path.insert(0, _TESTS_DIR)
```

The intended repair is:

```python
from test_v051_h40_f01_lifecycle_authority import (  # noqa: E402
```

in exactly the three affected files.

Do not move imports above the `sys.path` setup.

Do not restructure package/test imports.

Do not change test behavior.

---

## 2. Exact allowed diff

Allowed modifications:

```text
tests/test_v051_h40_f01r3r1_durable_authority.py
  add:  # noqa: E402

tests/test_v051_h40_f01r3r2_commit_authority.py
  add:  # noqa: E402

tests/test_v051_h40_f01r3r3_api_seal.py
  add:  # noqa: E402
```

Expected semantic diff:

```diff
-from test_v051_h40_f01_lifecycle_authority import (
+from test_v051_h40_f01_lifecycle_authority import (  # noqa: E402
```

No other source/test changes are authorized.

If any other change is required:

```text
STOP
E402_REPAIR_REQUIRES_UNEXPECTED_CHANGE
```

---

## 3. Forbidden changes

Do NOT modify:

```text
src/**
tests/test_v051_h40_p3b_*
tests/test_v051_h40_f01_lifecycle_authority.py
pyproject.toml
ruff configuration
mypy configuration
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
scientific/governance hashes
```

Do not add a global E402 ignore.

Do not disable Ruff.

Do not change import semantics merely to avoid the lint rule.

---

## 4. Validation

Before edit:

```bash
git rev-parse HEAD
git status --short
```

Require:

```text
HEAD = a858835ac3239f919bb2dcdc87f74db894f899cf
```

After edit, inspect:

```bash
git diff --   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py
```

Require exactly three `# noqa: E402` restorations and no semantic changes.

Run:

```bash
ruff check .

.venv/bin/python -m pytest -q   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   tests/test_v051_h40_p3b_production_verifier_identity_seal.py

mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

No full pytest is required for this tiny repair. The full suite will be rerun in the reacceptance-evidence task against the NEW repair SHA.

Do not inspect or trigger CI.

---

## 5. Frozen identities

Confirm unchanged:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

current accepted lifecycle implementation authority =
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

This task must not update the accepted implementation authority.

---

## 6. Commit

Suggested commit:

```text
fix(tests): restore E402 suppressions for local test imports
```

Push to:

```text
origin/v0.5-refactor
```

After push, record the NEW repair SHA.

---

## 7. Report

Create a short docs report:

```text
reviews/v0.5/V0.5.1_H40_P3B_E402_TEST_LINT_REPAIR_REPORT.md
```

on:

```text
v0.5-refactor-docs
```

Include:

- starting SHA;
- repair SHA;
- exact three changed lines/files;
- confirmation parent `6bd63db...` already carried the same E402 suppressions;
- `ruff check .` result;
- focused pytest results;
- mypy/compileall/diff-check;
- frozen identities;
- protected surfaces untouched.

---

## 8. Final state

Allowed conclusion:

```text
P3B_E402_TEST_LINT_REPAIR_COMPLETE
P3B_REACCEPTANCE_EVIDENCE_MUST_BE_RERUN_ON_NEW_SHA

P3B_PRODUCTION_AUTHORITY_REACCEPTANCE_NOT_YET_GRANTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Do NOT resume the old evidence run against `a858835...`.

The next controller-issued evidence prompt will bind the NEW repair SHA.

---

## 9. Final response

Return:

```text
starting_sha:
repair_sha:
changed_files:
exact_diff_summary:

ruff_all:
focused_lifecycle_tests:
p3b_behavior_identity_tests:
mypy:
compileall:
git_diff_check:

frozen_identities_unchanged:
accepted_implementation_authority_changed: NO

real_h40_outcomes_accessed: NO
wf1_calibration_outcomes_accessed: NO
confirmation_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
signed_network_requests: 0

repair_report_path:
docs_commit_sha:

final_state:
P3B_E402_TEST_LINT_REPAIR_COMPLETE
P3B_REACCEPTANCE_EVIDENCE_MUST_BE_RERUN_ON_NEW_SHA
P3B_PRODUCTION_AUTHORITY_REACCEPTANCE_NOT_YET_GRANTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## Recommended executor

```text
Codex Sol High
```

Reason:

```text
The repair is tiny but authority-sensitive.
Codex Sol High is preferred for exact minimal diffs and gate execution.
Gemini 3.8 Flash could perform the edit mechanically, but is not preferred
because this commit becomes the next tested implementation authority candidate.
Astra is not needed.
```
