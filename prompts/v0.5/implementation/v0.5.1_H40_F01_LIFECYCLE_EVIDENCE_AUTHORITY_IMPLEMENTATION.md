# Agent Prompt — V0.5.1 H40 F01 Lifecycle Evidence-Authority Implementation

## 0. Role and objective

Implement only the accepted H40 FSA-F01 lifecycle evidence-authority layer.

This is a bounded implementation task over already accepted protocol semantics. Do not redesign H40, do not run Discovery, and do not access any real H40 outcome.

Repository:

```text
EXASHXE/btc-quant-agent
```

Implementation branch:

```text
v0.5-refactor
```

Docs/review branch is read-only for this task:

```text
v0.5-refactor-docs
```

---

# 1. Mandatory preflight

Before editing, fetch current remote state and verify:

```text
expected code HEAD:
cfefdbabfeef8ab004e43bbda24038e4d5e3fca0

P3R0R1 repair:
89b014389b371ae1256d9b31ded86f10dd3b4783

P3R0R1 cumulative independent acceptance:
9a6811cbf4957a85ce5c4da38e8fe14fc8000c33
```

Read completely:

```text
reviews/v0.5/V0.5.1_H40_P3R0R1_LIFECYCLE_EVIDENCE_AUTHORITY_REPAIR.md

reviews/v0.5/V0.5.1_H40_P3R0R1_SOL_CUMULATIVE_ACCEPTANCE.md

reviews/v0.5/V0.5.1_H40_P2_FINAL_INDEPENDENT_ACCEPTANCE.md

reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3R4_STATISTICAL_DECISION_FINAL_CLOSURE.md
```

Read current implementation surfaces:

```text
src/btc_quant_agent/h40/lifecycle.py
src/btc_quant_agent/h40/protocol_authority.py
src/btc_quant_agent/h40/configuration_ledger.py
src/btc_quant_agent/h40/search_space.py
src/btc_quant_agent/h40/guards.py
src/btc_quant_agent/h40/__init__.py

tests/test_v051_h40_p1_scaffold.py
tests/test_v051_h40_p2_search_space.py
tests/test_v051_h40_p2r1_contract_materialization.py
```

If `origin/v0.5-refactor` has moved from the expected code HEAD, stop implementation and report the exact new HEAD and diff relevant to H40. Do not silently rebase this authority implementation onto unknown changes.

---

# 2. Accepted authority identities

These are immutable implementation oracles.

Scientific identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Lifecycle authority:

```text
lifecycle_semantic_root_hash =
d8c24b878b18426666ce46390e7363a0a2a5d7d3eb4d7636cc5cb2ecfaf4e9f5

lifecycle_governance_authority_hash =
ee619e16bb665ba377baa2a50f92ea1c36ee207282e3f7757b54a0a1adf7b828
```

Accepted Discovery scientific contract:

```text
family      = DISCOVERY_SELECTION
contract_id = DISCOVERY_SELECTION_V1

discovery_selection_correction_contract_hash =
f84c97050c7db813263e5ffda6b1c556af0b7b0e2876bb8616640d2c9d67084b
```

Do not change any of these values or alter the scientific hash preimages to make implementation convenient.

---

# 3. Current defect to remove

Current `H40LifecycleStateMachine.transition_to()` is state adjacency only:

```text
current state
-> VALID_TRANSITIONS
-> new state
```

That is insufficient.

After this task, privileged lifecycle advancement must require accepted evidence authority:

```text
state adjacency
+
accepted lifecycle governance authority
+
accepted implementation authority boundary
+
deterministic run authority
+
sealed runtime roster
+
canonical evidence
+
verifier recomputation
+
upstream receipt lineage
=
authorization receipt
=
state transition
```

A caller must never be able to advance state using:

- a boolean;
- a path;
- a summary hash alone;
- a rank;
- a candidate ID;
- a caller-chosen run ID;
- a forged receipt dictionary;
- a valid hash over a false claim.

---

# 4. Hard information boundary

This implementation is still pre-outcome.

Do not compute, load, inspect, derive, search, summarize, or use any real:

```text
H40 label
forward return
realized MFE / MAE
precision
expectancy
PF
ECE
Brier
bootstrap outcome
HAC outcome
candidate ranking
WF result
confirmation outcome
H39 protected result/artifact
Final Holdout data/result
```

Do not invoke:

```text
Discovery runner
backtest runner
WF runner
confirmation runner
H39 unblind
Final Holdout reader
paper/testnet/live execution
```

All new tests must use synthetic/governance fixtures only.

---

# 5. Explicit F02 boundary

F02 is not part of this implementation.

The transition:

```text
H40_CONFIRMATION_READY
->
H40_CONFIRMATION_EVALUATED_ONCE
```

must be prohibited.

Do not implement:

- confirmation nonce;
- confirmation unlock token/key;
- one-shot confirmation evaluator;
- confirmation outcome accessor;
- confirmation result receipt;
- Final Holdout access.

`H40_CONFIRMATION_READY` is only a sealed waiting state.

---

# 6. Required implementation architecture

Prefer a narrow new authority module instead of overloading `lifecycle.py`.

Recommended structure:

```text
src/btc_quant_agent/h40/lifecycle_authority.py
src/btc_quant_agent/h40/lifecycle.py
src/btc_quant_agent/h40/__init__.py

tests/test_v051_h40_f01_lifecycle_authority.py
```

Modify additional H40 files only when necessary to consume existing accepted identity objects.

Do not modify the frozen formal economic kernel outside `h40/`.

Do not add a framework, database abstraction, service layer, CLI runner, or outcome engine.

---

# 7. Canonical lifecycle semantic contracts

Implement canonical Python representations for all nine accepted child objects exactly as committed in P3R0R1:

```text
discovery_authorization_receipt_contract
candidate_lock_receipt_contract
wf_validation_receipt_contract
confirmation_ready_receipt_contract
termination_receipt_contract
transition_matrix_contract
persistence_replay_contract
runtime_snapshot_seal_contract
f02_boundary_contract
```

Requirements:

1. canonical object contents must reproduce the accepted child hashes naturally;
2. no function may simply return the expected hash constant as its computation;
3. changing any material semantic field must change the child hash;
4. the lifecycle root must be computed from named child hashes;
5. lifecycle hashes remain separate from scientific `semantic_root_hash`.

Expose expected values only as oracle constants for tests.

Required natural recomputation:

```text
compute_lifecycle_semantic_root_hash()
==
d8c24b878b18426666ce46390e7363a0a2a5d7d3eb4d7636cc5cb2ecfaf4e9f5
```

---

# 8. Governance authority

Implement the accepted governance authority object exactly:

```json
{
  "lifecycle_semantic_root_hash": "d8c24b878b18426666ce46390e7363a0a2a5d7d3eb4d7636cc5cb2ecfaf4e9f5",
  "p2_final_acceptance_identity": {
    "artifact_path": "reviews/v0.5/V0.5.1_H40_P2_FINAL_INDEPENDENT_ACCEPTANCE.md",
    "commit_sha": "5f1618ed73a91c954c1443f62f77152682b33ef4"
  },
  "p3r0r1_amendment_artifact_path": "reviews/v0.5/V0.5.1_H40_P3R0R1_LIFECYCLE_EVIDENCE_AUTHORITY_REPAIR.md",
  "p3r0r1_amendment_commit_sha": "89b014389b371ae1256d9b31ded86f10dd3b4783",
  "protocol_authority_hash": "a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce",
  "schema_id": "H40_LIFECYCLE_GOVERNANCE_AUTHORITY_V2",
  "semantic_root_hash": "71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1",
  "structural_ledger_hash": "483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f"
}
```

Natural recomputation must produce:

```text
ee619e16bb665ba377baa2a50f92ea1c36ee207282e3f7757b54a0a1adf7b828
```

Do not bind the implementation commit into this governance hash.

---

# 9. Implementation authority boundary

Define an exact typed/canonical representation of:

```text
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1
```

with the frozen field names from the accepted repair:

```text
accepted_lifecycle_governance_authority_hash
f01_implementation_acceptance_artifact_path
f01_implementation_acceptance_commit_sha
f01_implementation_commit_sha
required_test_ci_evidence_identity
schema_id
```

and nested evidence identity fields:

```text
evidence_manifest_artifact_path
evidence_manifest_sha256
tested_commit_sha
```

Important stage boundary:

During this implementation task the final accepted implementation-authority object **does not yet exist**, because:

- the final implementation commit does not yet exist;
- independent implementation acceptance does not yet exist;
- remote CI evidence is temporarily deferred.

Therefore:

- implement the schema;
- implement canonical validation/hash functions;
- implement fail-closed behavior for incomplete/unaccepted authority;
- do not fabricate placeholder values into an accepted hash;
- do not claim Discovery authority;
- do not publish a fake `lifecycle_implementation_authority_hash`.

Tests may instantiate fully synthetic authority objects using synthetic commit/evidence IDs solely to test mechanics, but production/default construction must not self-authorize.

---

# 10. Deterministic run authority

Implement:

```text
schema_id = H40_RUN_AUTHORITY_V1
```

with the exact accepted field contract:

```text
discovery_selection_correction_contract_hash
lifecycle_governance_authority_hash
lifecycle_implementation_authority_hash
materialized_run_authority_hash
protocol_authority_hash
sealed_registered_roster_hash
semantic_root_hash
source_manifest_hash
split_attestation_hash
split_manifest_hash
structural_ledger_hash
schema_id
```

Compute:

```text
run_authority_id =
canonical_sha256(exact run-authority fields)
```

No caller-selected nonce.

No free-form `run_id` authority.

A human-readable label may exist only outside the canonical authority object and must never affect persistence, replay, or transition authorization.

---

# 11. Runtime snapshot seal

Implement a typed immutable runtime-seal representation.

Roster entries must contain exactly:

```text
family_id
slot_hash
slot_index
structural_configuration_hash
```

Roster ordering:

```text
ascending structural_configuration_hash
```

Roster hash:

```text
canonical_sha256(exact ordered roster-entry array)
```

Seal must bind:

```text
runtime_authority_snapshot_hash
source_manifest_hash
split_manifest_hash
split_attestation_hash
sealed_registered_roster_hash
registered_slot_count
not_testable_slot_count
total_slot_count
```

Required checks:

- total slot count = 168 for accepted production H40 V1;
- registered + not-testable = total;
- roster length = registered;
- each roster member maps to an accepted structural slot;
- duplicate slot/config identity rejected;
- post-seal mutation cannot mutate the active roster object;
- changing runtime authority must produce different run authority while leaving scientific hashes unchanged.

Production roster must be derivable from the existing accepted materialized search-space/runtime authority objects, not from a caller-provided arbitrary list.

A test/synthetic constructor may exist but must be explicitly non-authoritative.

---

# 12. Canonical timestamps

Implement one parser/normalizer rule only:

```text
YYYY-MM-DDTHH:MM:SSZ
```

Exactly 20 ASCII characters.

Reject:

- timezone offsets;
- fractional seconds;
- missing Z;
- leap seconds;
- impossible dates/times;
- equivalent but noncanonical spellings.

These timestamps are:

```text
hash-bound audit metadata
```

They are never:

```text
freshness authority
wall-clock tolerance
transition authorization
```

Do not add `now()` comparison rules.

---

# 13. Canonical receipt/evidence schema discipline

For every lifecycle receipt/evidence type:

- exact required key set;
- reject missing fields;
- reject extra fields;
- validate enum/constants/type domains;
- canonical content hash;
- receipt/evidence hash outside its own preimage;
- immutable representation after construction;
- locator/path is not authority.

Implement enough parsing/serialization to prove deterministic persistence/reload mechanics with synthetic fixtures.

Avoid accepting generic `dict[str, Any]` directly at privileged transition APIs without exact schema validation.

---

# 14. Discovery authorization receipt

Implement:

```text
H40_RECEIPT_DISCOVERY_AUTH_V2
```

The exact field set is authoritative from P3R0R1.

Discovery entry must recompute/verify at least:

- protocol authority hash;
- scientific semantic root;
- structural ledger hash;
- lifecycle governance authority hash;
- accepted implementation authority object/hash;
- materialized run authority hash;
- source manifest hash;
- split manifest hash;
- split attestation hash;
- sealed roster hash/counts;
- run authority ID;
- execution-disabled invariant;
- accepted discovery selection correction contract hash;
- null upstream receipt.

No caller-created discovery receipt may be accepted merely because its own hash is valid.

The privileged API must construct the authorization receipt only after recomputation.

---

# 15. Candidate-lock evidence and authorization

Implement canonical types for:

```text
H40_DISCOVERY_RESULT_EVIDENCE_V1
H40_RECEIPT_CANDIDATE_LOCK_V2
```

This F01 task must implement lifecycle/evidence verification mechanics using synthetic inputs. It must not run real Discovery.

The Discovery result evidence must enforce:

- exact run authority;
- exact materialized run authority;
- exact sealed roster;
- exact accepted Discovery contract ID/hash;
- partition identity = `WF1_CALIBRATION`;
- complete candidate-result entry set for the sealed REGISTERED roster;
- no missing/duplicate/extra candidate configuration.

Candidate result entry fields must match the accepted repair exactly.

The candidate-lock verifier must deterministically derive the winner from canonical synthetic result evidence under the accepted ordering and correction-result evidence bindings.

It must not trust:

- `selection_rank`;
- caller selected slot;
- caller winner flag;
- summary path;
- summary hash alone.

No runner-up promotion.

Important boundary:

F01 does not implement or execute the real statistical estimator/bootstrap engine. It verifies that the complete canonical scientific result-input evidence and correction-input evidence are content-addressed and run/candidate/contract bound, and implements the deterministic lifecycle selection/recomputation boundary needed to prevent caller self-authorization.

Do not invent a second Discovery science implementation.

---

# 16. Walk-forward evidence and authorization

Implement canonical types for:

```text
H40_WF_VALIDATION_RESULT_EVIDENCE_V1
H40_RECEIPT_WF_VALIDATION_V2
```

Required behavior:

- candidate lock receipt must be verified;
- run authority must match;
- exact candidate identity must match;
- split manifest/attestation must match;
- authoritative fold set/order must derive from split authority;
- missing/duplicate/extra/reordered fold fails;
- accepted validation contract hash set is exact;
- fold evidence is content-addressed and candidate/run/split/fold bound;
- bare `all_wf_gates_passed = true` has zero authority;
- authorization receipt emitted only if the verified fold/gate evidence proves all accepted gates pass;
- failure produces termination/no-go authority, not WF validation authority;
- no runner-up.

Again: use synthetic fold/gate evidence only in this task. Do not run actual WF market evaluation.

---

# 17. Confirmation-ready receipt

Implement:

```text
H40_RECEIPT_CONFIRMATION_READY_V2
```

It may be constructed only from a reverified WF authorization receipt.

Bind exactly:

```text
CONFIRMATION_HOLDOUT
2025-02-01T00:00:00Z
2026-02-01T00:00:00Z
F02 status = OPEN_SEALED
candidate identity
run authority
split identity
WF receipt lineage
```

It authorizes only the waiting state:

```text
H40_CONFIRMATION_READY
```

Any attempt to construct/transition to:

```text
H40_CONFIRMATION_EVALUATED_ONCE
```

must fail with the accepted confirmation guard semantics.

---

# 18. Termination receipts

Implement:

```text
H40_RECEIPT_TERMINATION_V2
```

Reason codes must use the existing accepted `H40ReasonCode` vocabulary.

Do not create a second reason-code enum.

Allowed terminal states:

```text
H40_NO_GO
NOT_TESTABLE
```

Both are absorbing.

Termination never authorizes forward progress.

---

# 19. Lifecycle state-machine repair

Repair `H40LifecycleStateMachine`.

Do not leave a public privileged `transition_to(new_state)` path that can advance to outcome-bearing states by adjacency alone.

Preferred pattern:

```text
transition_* methods
or
transition_with_verified_authority(...)
```

where the state machine receives a verified authorization object produced by the accepted authority layer.

Requirements:

- adjacency remains necessary;
- authority receipt is required for privileged forward transition;
- receipt transition source/target must match current/requested state;
- run authority and upstream lineage must be reverified;
- terminal states absorbing;
- F02 transition prohibited;
- direct dictionary/primitive/string/boolean receipt substitution rejected.

If retaining `transition_to` for backward compatibility is necessary, it must fail closed for all F01-governed transitions and cannot provide a bypass.

---

# 20. Persistence / replay

Implement minimal deterministic filesystem persistence for lifecycle governance artifacts only if required to satisfy the accepted contract.

Canonical root:

```text
artifacts/h40/lifecycle/runs/<run_authority_id>
```

Do not add operational DB state.

Persistence requirements:

- canonical JSON;
- atomic create-if-absent;
- different bytes at existing transition key rejected;
- identical bytes are idempotent only after full revalidation;
- restore recomputes content hashes;
- restore revalidates exact schema;
- restore revalidates run authority;
- restore revalidates upstream lineage;
- restore revalidates candidate/split bindings;
- filename/path/caller label has no authority.

Tests must use `tmp_path`; do not write tracked/runtime artifacts during tests.

---

# 21. Mandatory adversarial tests

Create a dedicated test module, preferably:

```text
tests/test_v051_h40_f01_lifecycle_authority.py
```

At minimum cover all of the following.

## T01 — accepted hash tree

Natural recomputation produces all nine accepted child hashes and lifecycle root.

## T02 — semantic mutation propagation

Mutating each representative material child field changes child hash and root.

## T03 — scientific hashes unchanged

The three accepted P2 hashes remain exact.

## T04 — governance authority

Canonical governance object naturally hashes to:

```text
ee619e16bb665ba377baa2a50f92ea1c36ee207282e3f7757b54a0a1adf7b828
```

## T05 — adjacency alone denied

Direct state adjacency cannot enter Discovery.

## T06 — primitive/dict/path denied

Boolean, string, path, arbitrary dict, and caller-built receipt cannot authorize Discovery.

## T07 — implementation authority required

Missing/incomplete/unaccepted implementation authority fails.

## T08 — historical SHA is not current-HEAD equality

Valid F01 mechanics do not require current executable HEAD equal P1/P2 historical baseline.

## T09 — deterministic run authority

Changing any authority component changes `run_authority_id`; caller label does not.

## T10 — roster complete and sealed

Missing/extra/duplicate roster entry or count mismatch fails.

## T11 — post-seal source change

Cannot mutate an already sealed active roster; new snapshot creates new run authority.

## T12 — exact timestamp parser

Canonical UTC accepted; offsets/fractions/invalid dates rejected.

## T13 — candidate evidence completeness

Missing/extra/duplicate candidate result entry fails.

## T14 — forged candidate denied

Caller-supplied selected candidate, rank, summary, or hash cannot authorize lock.

## T15 — exact R3R4 selection order

Synthetic evidence proves deterministic ordering:
hard gates -> adjusted LCB net expectancy -> adjusted LCB precision -> lower complexity -> ascending structural config hash.

## T16 — no runner-up

Locked candidate failure cannot promote second-best under the same run.

## T17 — cross-run candidate replay denied

Candidate evidence from another run fails.

## T18 — cross-roster replay denied

Candidate evidence from another sealed roster fails.

## T19 — bare WF boolean denied

`all_wf_gates_passed=true` cannot authorize WF.

## T20 — incomplete WF fold set denied

Missing/duplicate/extra/reordered fold fails.

## T21 — cross-candidate WF replay denied

Foreign candidate fold evidence fails.

## T22 — cross-split WF replay denied

Foreign split/attestation/fold evidence fails.

## T23 — failed WF gate terminates

No WF validation authorization receipt is emitted.

## T24 — persistence tamper

Modified evidence/receipt bytes fail restore.

## T25 — upstream receipt substitution

Valid receipt from another chain fails.

## T26 — idempotent persistence

Identical bytes can be reused only after full verification.

## T27 — confirmation-ready is waiting only

Produces no outcome accessor/authorization.

## T28 — F02 transition denied

`H40_CONFIRMATION_READY -> H40_CONFIRMATION_EVALUATED_ONCE` is prohibited.

## T29 — terminal states absorbing

No forward transition from `H40_NO_GO` or `NOT_TESTABLE`.

## T30 — protected surfaces remain protected

H39 / Final Holdout access remains denied.

Add more adversarial tests if implementation creates additional attack surfaces.

---

# 22. Compatibility requirements

Existing P1/P2/P2R1 tests must remain semantically valid.

Do not change accepted expected scientific hashes.

Do not weaken existing guard tests just to accommodate the new state machine.

If an existing P1 scaffold test calls old adjacency-only `transition_to`, update the test only to reflect the newly accepted fail-closed lifecycle authority. Do not preserve an insecure bypass for backward compatibility.

---

# 23. Local quality gates — CI TEMPORARILY DEFERRED

GitHub-hosted Actions quota is currently exhausted.

For this task:

## Required local checks

Run locally:

```bash
ruff check .
mypy
pytest -q tests/test_v051_h40_f01_lifecycle_authority.py
pytest -q tests/test_v051_h40_p1_scaffold.py tests/test_v051_h40_p2_search_space.py tests/test_v051_h40_p2r1_contract_materialization.py
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

Record exact local Python version and exact test counts.

## Explicit CI prohibition for this task

Do **not**:

- invoke `workflow_dispatch`;
- manually trigger GitHub Actions;
- re-run failed Actions jobs;
- poll/wait for Actions completion;
- modify CI configuration merely to obtain a green badge;
- treat automatic quota failure as an implementation defect.

A normal `git push` is still required. If repository push hooks automatically create a GitHub Actions run that fails immediately due quota, report it only as:

```text
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

Do not attempt to repair or rerun it.

Local tests are evidence for implementation review, but do not silently rename them as GitHub-hosted CI evidence.

---

# 24. Implementation report

Create an implementation report on the code branch only if existing code-branch conventions require it; otherwise report the results in the final agent response and let the independent reviewer materialize the docs-branch acceptance.

The final response must include:

```text
starting exact SHA
final implementation commit SHA
changed files
new public authority types/functions
accepted child/root hashes reproduced
lifecycle governance hash reproduced
number of new adversarial tests
local Python version
ruff result
mypy result
targeted pytest result
full pytest result
compileall result
git diff --check result
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
confirmation no real H40 outcome accessed
confirmation H39 unchanged
confirmation Final Holdout sealed
confirmation F02 not implemented
confirmation execution disabled
```

---

# 25. Git discipline

Implementation branch only:

```text
origin/v0.5-refactor
```

Requirements:

1. start from exact expected baseline;
2. make bounded F01 changes;
3. run local gates;
4. commit implementation;
5. push to `origin/v0.5-refactor`;
6. report exact pushed SHA;
7. do not merge other branches;
8. do not amend/rebase unrelated accepted history;
9. do not modify `v0.5-refactor-docs`;
10. do not trigger/re-run GitHub Actions.

If local tests fail, repair locally before push.

---

# 26. Required end state

A successful implementation task may claim only:

```text
H40_P3R0_CUMULATIVE_ACCEPTED

H40_FSA_F01_PROTOCOL_SEMANTICS_CLOSED
H40_FSA_F01_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_ACCEPTANCE

H40_LIFECYCLE_GOVERNANCE_AUTHORITY_BOUND
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_FINAL_ACCEPTED

REMOTE_CI_DEFERRED_ACTIONS_QUOTA

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_FSA_F02_OPEN_SEALED
H40_CONFIRMATION_SEALED

H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED

NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```

Do not claim:

```text
F01_FINAL_ACCEPTED
P3_AUTHORIZED
DISCOVERY_AUTHORIZED
```

Those require a later independent implementation review and accepted implementation-authority publication.

---

# 27. Model recommendation

Recommended executor:

```text
Codex
model: GPT-5.6 Sol
reasoning: Medium or High
```

Use High if implementation begins touching persistence/replay or multiple authority boundaries simultaneously.

Alternative bounded executor:

```text
Gemini 3.8 Flash
```

only if it follows this prompt literally and does not redesign the authority model.

Independent acceptance should be performed by GPT-5.6 Sol High or GPT-6/Astra, not by the same implementation agent self-approving its work.
