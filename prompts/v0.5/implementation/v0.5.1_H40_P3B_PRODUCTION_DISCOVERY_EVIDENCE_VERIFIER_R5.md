# Agent Prompt — V0.5.1 H40 P3B Production Discovery Evidence Verifier R5

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = PRODUCTION_DISCOVERY_SCIENTIFIC_VERIFIER_ONLY
MODE = DELTA_OVER_R4
SUPERSEDES = v0.5.1_H40_P3B_PRODUCTION_DISCOVERY_EVIDENCE_VERIFIER_R4.md
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required starting HEAD:

```text
2e7c21c0f32f621321d56124c98cfb883946d4f3
```

R5 inherits EVERY requirement, prohibition, evidence rule, validation gate, test family, and fail-stop condition from R4, R3, and R2.

Do not omit prior requirements.

---

## 1. Newly accepted bootstrap start-sampler closure

Accepted artifacts:

```text
reviews/v0.5/V0.5.1_H40_P3B_BOOTSTRAP_START_SAMPLER_CLOSURE.md
commit 1d3ffe7b29e11cfe3b399df16be9ca91b16970ee

reviews/v0.5/V0.5.1_H40_P3B_BOOTSTRAP_START_SAMPLER_SOL_ACCEPTANCE.md
commit f98c4fdaaa1c39529ed2ab54277b0db2bfab39cc
```

Accepted policy:

```text
BOOTSTRAP_START_SAMPLER_POLICY_ID =
H40_P3B_BOOTSTRAP_START_SAMPLER_V1

BOOTSTRAP_START_SAMPLER_POLICY_HASH =
25d1af1277d45059d9cf474cacf0b59399b9f4af87ad3363a6dda570550ce8eb
```

Production scientific evidence/replay must bind this policy ID/hash.

Missing/foreign sampler policy identity fails closed.

---

## 2. Exact start-index sampler

For one candidate/partition/metric bootstrap evaluation:

```text
L = 168
M = 10000
S = H - L + 1
B = ceil(H / L)
```

If:

```text
H < 168
```

then:

```text
NOT_TESTABLE
no RNG draw
```

Otherwise instantiate exactly:

```python
rng = np.random.Generator(np.random.PCG64(seed))
```

with zero prior RNG consumption.

Then execute exactly one call:

```python
starts = rng.integers(
    low=0,
    high=H - 168 + 1,
    size=(10000, B),
    dtype=np.int64,
    endpoint=False,
)
```

This call completely defines the block-start matrix.

---

## 3. Matrix ownership/order

Require exact shape:

```text
(10000, B)
```

Interpret:

```text
starts[m,b]
```

as source block-start index for:

```text
replicate m
block draw b
```

Consume:
- m ascending 0..9999;
- b ascending 0..B-1.

Do not transpose, rechunk, reshape-reorder, or remap.

Repeated indices are ordinary with-replacement bootstrap multiplicity.

---

## 4. Forbidden samplers

Do NOT substitute:

```text
one Generator.integers call per replicate
one call per block
Generator.choice
Generator.random + scaling
random_raw
custom rejection sampling
raw uint64 modulo S
RandomState
default_rng shorthand
Python random
replicate-specific reseeding
```

Even if mathematically uniform, those methods are not the accepted deterministic mapping.

---

## 5. Exact RNG isolation

The first RNG-consuming operation after:

```python
np.random.Generator(np.random.PCG64(seed))
```

must be the single accepted `integers` matrix call.

No stochastic helper, shuffle, diagnostic sample, or test draw may consume state first.

The block-start generator must not be reused for unrelated stochastic work after the start matrix is produced unless another accepted contract explicitly authorizes it.

---

## 6. NumPy-version replay authority

Every bootstrap evidence object must persist exact:

```text
numpy_version
bit_generator = PCG64
bootstrap_start_sampler_policy_id
bootstrap_start_sampler_policy_hash
H
L
S
B
M
seed
start_matrix_hash
```

Formal production replay requires:

```text
producer numpy_version == verifier np.__version__
```

exactly.

Mismatch:

```text
REPLAY_ENVIRONMENT_MISMATCH
FAIL CLOSED
```

Do not silently assume NumPy `Generator.integers` bitwise compatibility across versions.

Do not modify project-wide dependency pins in this task.

---

## 7. Start-matrix hash

Define exactly:

```python
start_matrix_hash = canonical_sha256(starts.tolist())
```

where:
- outer sequence = replicate 0..9999;
- inner sequence = block 0..B-1;
- values = plain integers.

Production verifier independently regenerates the matrix and hash.

Caller-provided starts/hash are non-authoritative until recomputed.

---

## 8. Current five-policy P3B stack

All production Discovery verifier evidence must bind:

```text
FIT_INVALID_POLICY =
7de7c7060c150518805562a473b154d3e91ba5fbc13b5488342ad358744d728a

SEED_IDENTITY_POLICY =
7b2af0e0e7bde22ba10e81a7b356b6489614d42e6dc3a14a4b8d901423868d2c

PROXY_NET_RETURN_POLICY =
8a016bcff8e439090044e3003cb127bec3ed0e9c7f09fe8fd8307a2066ed76ba

BOOTSTRAP_BOUNDARY_POLICY =
0958dfa8bd61571983b2019b770edb29397da10e1df3f30c09b00362117320b6

BOOTSTRAP_START_SAMPLER_POLICY =
25d1af1277d45059d9cf474cacf0b59399b9f4af87ad3363a6dda570550ce8eb
```

No omission/substitution.

---

## 9. New mandatory sampler tests

Add to inherited adversarial suite:

### SAMP01 exact call signature

Instrument/mock `Generator.integers` and prove one call with:

```text
low=0
high=H-168+1
size=(10000,B)
dtype=np.int64
endpoint=False
```

### SAMP02 one call only

Require exactly one integer-draw call per candidate/metric bootstrap evaluation.

### SAMP03 no prior RNG consumption

Instrument generator state / methods and prove no RNG call occurs first.

### SAMP04 row/column interpretation

Synthetic matrix fixture proves:
- row = replicate;
- column = block draw.

### SAMP05 repeated starts retained

Repeated integer values remain repeated sampled blocks.

### SAMP06 wrong mapping rejected

Implement a synthetic alternative using `Generator.choice`.

Prove its sequence/hash is not accepted.

### SAMP07 random_raw rejection rejected

Synthetic raw-output rejection sampler may be statistically uniform but must not match authority unless accidental; verifier must reject foreign sampler policy/matrix evidence.

### SAMP08 matrix hash replay

Regenerate exact matrix and require canonical matrix hash equality.

### SAMP09 NumPy version mismatch

Producer version != verifier version -> fail closed before accepting matrix replay.

### SAMP10 H below block length

`H < 168`:
- NOT_TESTABLE;
- zero RNG calls.

### SAMP11 metric seed isolation

PRECISION and NET_EXPECTANCY accepted seeds produce independently generated start matrices under their exact metric IDs.

### SAMP12 deterministic replay

Two exact runs with same:
- seed;
- H;
- NumPy version;
- sampler policy

produce identical matrix and matrix hash.

---

## 10. Interaction with R4 boundary semantics

For each `starts[m,b]`, load the corresponding 168-hour source-decision block core.

Then use R4's source-anchored outcome transport.

Never:
- treat adjacent sampled blocks as contiguous market time;
- use next block for an earlier block's fixed-horizon endpoint;
- recompute actions/features across the synthetic boundary.

R5 changes only which accepted source block starts are deterministically selected.

---

## 11. Fail-stop remains mandatory

If another unresolved rule can materially change:

```text
sampled observation multiset
accepted trade population
precision
net expectancy
bootstrap vector
max-stat
p-value
LCB
hard gate
representative
candidate ranking
```

then:

```text
P3B_SPEC_GAP_STOP
code_modified = NO
```

Do not choose a conventional implementation.

---

## 12. Protected surfaces / frozen identities

All frozen scientific/lifecycle identities from R4 remain unchanged.

Do not access real:
- H40 labels;
- H40 returns;
- WF1_CALIBRATION outcomes;
- H39;
- Confirmation;
- Final Holdout.

No signed requests.

WF production verification remains fail-closed.

---

## 13. Implementation report delta

On successful implementation, report additionally:

```text
bootstrap_start_sampler_policy_id
bootstrap_start_sampler_policy_hash
producer_numpy_version
exact integers call contract
proof zero prior RNG consumption
start_matrix_hash definition
proof alternative sampler rejection
proof NumPy-version mismatch fail-closed
```

Allowed success state remains:

```text
H40_P3B_PRODUCTION_DISCOVERY_VERIFIER_IMPLEMENTED
PENDING_INDEPENDENT_SOL_ACCEPTANCE

H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex GPT-6 Sol High
```
