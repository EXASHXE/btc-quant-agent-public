# v0.5.0 R03B — Remove Legacy Benchmark Surface

## Mission

Perform one narrow, mechanically verifiable R03 simplification on `EXASHXE/btc-quant-agent` after R03A inventory and independent Sol review.

This round is NOT a redesign of formal benchmark semantics. Its only authorized target is the legacy diagnostic benchmark surface represented by `BenchmarkEngine` / `BenchmarkResult` in `src/btc_quant_agent/economic/benchmarks.py`, provided exact-HEAD reachability proves there are no active consumers beyond package export shims and obsolete tests/docs.

The canonical benchmark authority remains:

```text
build_formal_benchmark_suite
FormalBenchmarkSuite
FormalBenchmarkResult
RandomBenchmarkDistribution
canonical CASH reconstruction
canonical RANDOM_MATCHED reconstruction
```

Do not alter their behavior.

---

## Branch contract

Implementation branch:

```text
v0.5-refactor
```

Prompt/review branch:

```text
v0.5-refactor-docs
```

Expected implementation starting HEAD:

```text
78a5e38b7631b9926f5072b426cd4d24a706f39e
```

R03A inventory commit:

```text
0843bec2bff118d550d69a8f730ebc24669481ec
```

Independent Sol review:

```text
reviews/v0.5/V0.5.0_R03A_SOL_INDEPENDENT_REVIEW.md
```

Review commit:

```text
8bbb12674620e10b110c398082000fd3d325dd19
```

Before editing, fetch `origin/v0.5-refactor`, verify exact HEAD, and inspect any legitimate delta. Never reset newer work.

---

## Explicitly rejected Gemini classifications

Do NOT implement these R03A suggestions in this round:

```text
SIMP-02 legacy_directional_role removal      -> DEFER / SEMANTIC_CUT_REQUIRES_SOL
SIMP-03 legacy market-data v1 readability   -> KEEP_SECURITY_BOUNDARY
SIMP-04 registry semantic merge             -> DEFER
SIMP-05 round_trips removal                 -> DEFER
```

Reasons:

- active research registry entries still persist `legacy_directional_role: REJECTED` to distinguish retained opportunity evidence from rejected directional meaning;
- acceptance verifier deliberately fail-closes runtime market-data schema v1 because it lacks availability proof;
- registry separation and round-trip accounting are semantic authority issues.

Do not touch them.

---

# 1. Mandatory pre-edit reachability proof

Before deleting anything, inspect exact current HEAD and enumerate every reference to:

```text
btc_quant_agent.economic.benchmarks
BenchmarkEngine
BenchmarkResult
```

Classify every reference as:

```text
ACTIVE_PRODUCTION_CONSUMER
PACKAGE_EXPORT_ONLY
TEST_ONLY
DOC_ONLY
DEAD_OR_HISTORICAL
DYNAMIC_OR_UNKNOWN
```

Check at minimum:

- `src/**`
- `tests/**`
- `tools/**`
- `skill-template/**`
- `pyproject.toml` / entrypoints
- dynamic imports / string references
- package `__init__.py` exports
- formal facade
- CLI/API
- persisted schema readers
- registry/evidence readers

If any active production/formal/persistence consumer exists beyond an export shim, STOP and report:

```text
R03B_ESCALATION_REQUIRED
```

Do not adapt that consumer automatically.

---

# 2. Authorized implementation

Only if the preflight proves the legacy surface is inactive:

1. delete `src/btc_quant_agent/economic/benchmarks.py`;
2. remove `BenchmarkEngine` and `BenchmarkResult` imports/exports from `src/btc_quant_agent/economic/__init__.py`;
3. remove/update tests that exist solely to test the deleted legacy diagnostic implementation;
4. remove stale docs/import references only when they describe the deleted surface and are on the implementation branch;
5. add guards proving no active import/reference remains.

Do NOT create a compatibility alias, stub module, forwarding wrapper, deprecation facade, or replacement class. The point is to remove an inactive surface, not preserve it under another name.

---

# 3. Formal benchmark semantic freeze

The following must remain unchanged in behavior and identity:

- `ComparisonContract`;
- `BenchmarkMatchingRules`;
- `FormalBenchmarkResult`;
- `RandomBenchmarkDistribution`;
- `FormalBenchmarkSuite`;
- `build_formal_benchmark_suite()`;
- candidate replay identity and role binding;
- CASH reconstruction;
- RANDOM_MATCHED deterministic draw/reconstruction;
- benchmark suite hashes/IDs;
- qualification inputs and gates;
- registry attestation binding;
- B06/B07 adversarial semantics.

Do not edit `economic/qualification.py`, `formal_research.py`, replay bundle, acceptance verifier, registry authority, H39, Holdout, publication/finalization, or execution safety unless the only change is an import cleanup strictly caused by removing the legacy module. Any semantic requirement to touch these areas means STOP and report `R03B_ESCALATION_REQUIRED`.

---

# 4. Required tests

Add/retain tests proving:

### T1 — legacy module is gone

Importing:

```python
btc_quant_agent.economic.benchmarks
```

must fail, and `BenchmarkEngine` / `BenchmarkResult` must not be exported from `btc_quant_agent.economic`.

### T2 — no active references remain

A repository-level guard or deterministic reachability check must prove no current active `src/`, `tools/`, `skill-template/`, CLI/API or entrypoint reference remains.

Avoid brittle grep checks that match historical docs outside the active code branch.

### T3 — canonical CASH benchmark unchanged

Use existing formal benchmark fixtures to prove canonical CASH reconstruction still preserves initial capital, zero activity and expected identity.

### T4 — canonical RANDOM benchmark unchanged

Use existing deterministic RANDOM_MATCHED fixture/probe to prove identical draw/result identities for unchanged inputs.

### T5 — qualification binding unchanged

A formal candidate + benchmark suite fixture must still bind to the same candidate run result and qualification semantics.

### T6 — C-lite formal differential remains stable

Run the existing formal differential probe / equivalent accepted fixture. For unchanged synthetic input, canonical formal output hashes must remain equal to the accepted pre-R03B behavior.

Do not weaken or delete B01–B12, C-lite, H39, holdout or semantic-golden tests.

---

# 5. CI policy

This is a bounded removal of an inactive legacy module. Routine acceptance requires:

```text
FAST_CI_GREEN_PY313
```

with:

```text
install .[dev,research]
ruff check .
mypy
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

Escalate to manual full Python 3.11/3.12/3.13 compatibility only if the actual diff unexpectedly touches canonical benchmark/qualification/replay/serialization authority or another cross-version-sensitive protected surface.

---

# 6. Report

Implementation commits go only to `v0.5-refactor`.

Report goes only to `v0.5-refactor-docs`:

```text
reviews/v0.5/V0.5.0_R03B_IMPLEMENTATION_REPORT.md
```

Record:

- starting/final implementation SHAs;
- exact reachability inventory;
- files deleted/modified;
- confirmation whether any consumer existed beyond package exports/tests;
- canonical benchmark regression receipts;
- exact CI run;
- explicit confirmation that SIMP-02/03/04/05 were not implemented;
- confirmation H39/Final Holdout/execution state unchanged.

Finish with:

```text
V0_5_R03B_IMPLEMENTED_PENDING_INDEPENDENT_REVIEW
```

Do not self-declare R03 accepted.
