# v0.5.0 R04 — Final Kernel Convergence and Freeze

## Mission

Perform the final v0.5 architecture-convergence round on `EXASHXE/btc-quant-agent`.

R04 is **not** another broad cleanup round and is **not** an alpha-research round. Its purpose is to prove that, after R01–R03, the repository exposes one unambiguous formal research/economic authority path, no legacy path can still promote or masquerade as current authority, and the simplified v0.5 kernel can be frozen for an independent Astra adversarial gate.

The desired authority flow is:

```text
PIT / causal evidence
    -> registered signal producer + exact decision inputs
    -> ReplayInputBundle
    -> canonical execute_bound_run / EconomicSimulationEngine
    -> canonical accounting result
    -> independently reconstructed CASH / RANDOM_MATCHED benchmarks
    -> preregistered economic qualification
    -> append-only formal registry decision
```

Do not redesign this chain. Converge remaining surrounding paths onto it or make them explicitly diagnostic-only/fail-closed.

---

## 0. Branch contract and exact baseline

Implementation branch:

```text
v0.5-refactor
```

Expected implementation start HEAD:

```text
0beb85337bb8b9cac4e63ac9c53ac7b1d32084af
```

Prompt/review branch:

```text
v0.5-refactor-docs
```

Accepted semantic anchor:

```text
6710b6302bf224cdc178e6f3d817a95f79277f38
```

Accepted current state:

```text
B13_GO
P7_REPAIRED_KERNEL_GO
C_LITE_ACCEPTED
R01_ACCEPTED
R02_ACCEPTED
R02R_ACCEPTED
R03_ACCEPTED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
ALPHA_RESEARCH_NOT_STARTED
```

Before editing:

1. fetch `origin/v0.5-refactor`;
2. verify exact HEAD;
3. if branch legitimately advanced, inspect all deltas and record the new start SHA; do not reset later work;
4. verify the latest exact-head fast CI receipt;
5. read the R03C semantic decision and preserve its KEEP decisions.

Do not recreate `prompts/` or `reviews/` on the code branch.

---

## 1. R04 is an authority-convergence gate, not a LOC target

Do not delete code merely to reduce size.

Before any production change, construct a short current-authority inventory and classify every reachable research/economic path as exactly one of:

```text
FORMAL_CURRENT_AUTHORITY
PROTECTED_H39_AUTHORITY
DIAGNOSTIC_ONLY
OPERATIONAL_NON_PROMOTING
EXECUTION_DISABLED_SURFACE
DEAD_SAFE_TO_REMOVE
```

If a path does not fit one of these classifications, investigate it before editing.

The primary question is:

> Can any caller obtain, present, persist, or promote a research/economic result without going through the accepted formal replay + benchmark reconstruction + qualification authority?

If yes, eliminate or fail-close that bypass with the smallest change possible.

---

## 2. Hard semantic freeze

Unless a concrete bypass cannot be closed without a minimal change, do not alter:

- `src/btc_quant_agent/economic/qualification.py`
- `src/btc_quant_agent/economic/replay_bundle.py`
- `src/btc_quant_agent/economic/acceptance_verifier.py`
- canonical execution/accounting formulas
- fee/slippage/funding semantics
- candidate replay semantics
- CASH reconstruction semantics
- RANDOM_MATCHED reconstruction semantics
- benchmark matching rules
- qualification hurdles/operators
- run/result/suite/qualification identity preimages
- publication/finalization/CAS semantics
- SQLite schema authority
- `ResearchContractRegistry` decision semantics
- R03C-retained `SimulationSummary.round_trips`
- R03C-retained `legacy_directional_role`
- legacy market-data v1 fail-closed behavior.

Do not merge `ResearchContractRegistry` and operational `ResearchRegistry`.

If a required fix crosses one of these boundaries, stop that subtask and report:

```text
R04_SEMANTIC_ESCALATION_REQUIRED
```

Do not invent a replacement abstraction.

---

## 3. Mandatory convergence checks

### A. Public research entrypoints

Prove that current generic research-facing entrypoints such as:

```text
quantctl backtest
quantctl research
quantctl replay
formal research tool entrypoints
formal shadow entrypoint
```

route through the accepted formal facade and cannot silently dispatch to removed/legacy economics.

Implicit old CSV/legacy inputs must fail closed rather than select an old engine.

### B. Legacy/diagnostic economics

Inventory any remaining modules/functions that can still compute strategy-like performance, shadow results, old R metrics, or research recommendations.

For each:

- if unreachable and truly dead, remove it;
- if still needed for historical diagnostics, ensure its output is explicitly `DIAGNOSTIC_ONLY` / `NOT_TESTABLE_FOR_NEW_PROMOTION`;
- it must not emit a classification, registry event or artifact that can be mistaken for formal economic qualification.

Do not recreate the R03B legacy benchmark surface.

### C. Registry separation

Prove operational `ResearchRegistry` cannot directly or indirectly create a formal economic decision.

Operational states such as:

```text
SUPPORTED_MOVEMENT
VALIDATED_FORWARD
LIVE_ELIGIBLE
qualified_direction_engine_count
```

must never substitute for a cold-verified formal qualification/attestation.

Retain the two-registry separation decided in R03C.

### D. API and service surface

Retained HTTP API routes must remain behaviorally read-only.

Service/diagnostic/status paths may observe state but must not create formal promotion authority as a side effect.

### E. Execution safety

Execution remains disabled by default.

No R04 change may enable:

- live execution;
- testnet/paper execution;
- auto execution;
- automatic order/close side effects.

Execution interfaces may remain but must stay separately gated and fail closed.

### F. H39 and Final Holdout

H39 remains separate, frozen and protected.

Do not:

- alter H39 protocol identity;
- read protected outcomes;
- unblind H39;
- route H39 diagnostic state into formal promotion;
- migrate or regenerate H39 frozen artifacts.

Final Holdout remains SEALED. Do not read its outcomes or use them for design choices.

---

## 4. Attack-oriented acceptance requirements

R04 must preserve rejection of at least the following attack families:

```text
B1  forged candidate execution/accounting
B2  observable-time / intrabar LIMIT ambiguity
H1  incomplete/fake random reconstruction
H2  traded CASH substitution
H3  future-volume/liquidity causality
H4  unclosed/missing availability PIT evidence
H5  H39 diagnostic/outcome boundary bleed
B04 producer-contract / role / synchronized relabel attacks
B05 forged ledger / submitted fill-fee-funding-PnL attacks
B06 random benchmark substitution / seed-draw manipulation
B07 CASH reconstruction substitution
B09/B10 publication/finalization race and ownership
B11 unsupported/legacy schema fail-closed behavior
B12 bounded REST amplification semantics
```

At minimum include explicit adversarial tests/receipts for:

1. impossible candidate fill remains rejected;
2. candidate with forged submitted fee/funding/PnL remains rejected;
3. empty-signal forged-trade candidate remains rejected;
4. future-liquidity/availability violation remains rejected;
5. RANDOM trial relabel/clone/seed/direction manipulation remains rejected;
6. traded/nonzero CASH substitution remains rejected;
7. synchronized wrapper rehash does not bypass cold replay;
8. runtime registry state cannot produce a P5 economic qualification;
9. legacy market-data v1 remains non-promotable/fail-closed;
10. H39 and Final Holdout protections remain intact;
11. execution remains disabled.

Prefer existing accepted adversarial tests. Add new tests only for newly identified R04 boundaries.

---

## 5. Differential semantic parity

R04 is the final pre-Astra freeze, so smoke tests are not enough.

Produce a genuine cross-checkout semantic parity receipt for the accepted formal workflow.

At minimum compare the same deterministic fixture across:

```text
accepted C-lite semantic baseline:
31521b620292064997384261e90cd09a2e806f98

vs
final R04 exact HEAD
```

Use the same probe implementation/inputs for both checkouts and compare canonical snapshots/hashes covering, where compatible:

- candidate run semantic payload;
- benchmark suite;
- qualification result;
- evidence identities;
- registry decision/state;
- resample/funding compatibility outputs used by the accepted C-lite probe.

Report both hashes and require equality unless an R01–R03 accepted deletion changed only a non-authoritative legacy surface outside the snapshot.

If direct cross-checkout execution is technically incompatible, do not weaken the claim to “probe generated a file.” Explain the incompatibility and construct the strongest deterministic authority-level comparison available.

Do not modify frozen expected values merely to obtain parity.

---

## 6. Future research compatibility, without implementing H40

The post-v0.5 research program is expected to investigate low-frequency BTC/ETH perpetual directional opportunities, likely around:

```text
observation/decision cadence ~1h
candidate horizons around 4h / 8h / 12h
24h supporting horizon
LONG / SHORT / NO_TRADE
microstructure as optional entry confirmation
```

These are not architecture constants.

R04 must not hard-code BTC-only, 1h-only, H39-only, or one prediction horizon into generic formal authority.

Confirm the frozen kernel can continue to bind explicit product/horizon/protocol identities without collision. Report gaps for later research, but do not implement H40/H41/H42/H43, tune thresholds, or search alpha.

---

## 7. Scope discipline

Allowed production changes should normally be small and concentrated in orchestration/reachability surfaces discovered during the authority inventory.

Do not perform:

- repository-wide rewrite;
- new plugin/event-bus/DI architecture;
- new universal result envelope;
- ORM/storage migration;
- qualification redesign;
- simulator redesign;
- benchmark redesign;
- formal registry merge;
- broad H39 refactor;
- new strategy/feature research;
- alpha search;
- execution activation.

It is acceptable for R04 to conclude that no additional production deletion is justified and commit only narrowly scoped guards/docs on the code branch if the authority inventory proves convergence already exists.

Do not manufacture implementation work merely to have an R04 diff.

---

## 8. Validation and CI cost policy

During development, ordinary commits use the existing fast Python 3.13 gate.

Before declaring the final R04 implementation HEAD ready for review, run local gates:

```bash
ruff check .
mypy
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

Because R04 is the v0.5 stage-final pre-Astra freeze, the **final exact implementation HEAD** must then run one manual full compatibility gate via `workflow_dispatch` and obtain:

```text
FULL_COMPAT_CI_GREEN_PY311_PY312_PY313
```

This full matrix is required once on the final R04 HEAD, not on every intermediate commit.

Coverage is required only in the designated full-gate job according to the current v0.5 CI policy; do not duplicate `pytest -q` plus full coverage unnecessarily on every Python version.

---

## 9. Deliverable / report protocol

Implementation commits go only to:

```text
v0.5-refactor
```

Final implementation report goes only to:

```text
v0.5-refactor-docs
reviews/v0.5/V0.5.0_R04_FINAL_KERNEL_CONVERGENCE_REPORT.md
```

The report must include:

1. starting and final code SHA;
2. exact files changed/removed;
3. current-authority inventory and disposition of every remaining path;
4. proof generic entrypoints use formal authority;
5. proof legacy diagnostics cannot promote;
6. proof registry separation remains intact;
7. API read-only and execution-disabled receipts;
8. H39/Final Holdout preservation;
9. adversarial attack results;
10. genuine cross-checkout parity method + baseline/final hashes;
11. local gates;
12. exact fast CI run(s);
13. exact final manual 3.11/3.12/3.13 compatibility run and job conclusions;
14. all deferrals/known limitations.

Do not self-declare the Astra gate passed.

Finish with:

```text
V0_5_R04_IMPLEMENTED_PENDING_INDEPENDENT_REVIEW_AND_ASTRA_GATE
SIMPLIFIED_KERNEL_CANDIDATE_FROZEN
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
ALPHA_RESEARCH_NOT_STARTED
```

---

## 10. Model guidance

R04 should be executed with **GPT-5.6 Sol High** (or stronger equivalent reasoning), not a low-reasoning/Flash-only coding pass.

Gemini may be used only as an auxiliary worker for static reachability inventory or mechanical search. It must not independently decide authority cuts, schema/identity changes, or acceptance.

After R04 implementation is independently reviewed and its final full compatibility gate is green, the next required step is an **Astra High/Max v0.5 Simplified-Kernel Adversarial Gate** before alpha research resumes.
