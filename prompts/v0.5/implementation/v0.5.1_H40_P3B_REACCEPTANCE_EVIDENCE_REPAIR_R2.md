# Agent Prompt — V0.5.1 H40 P3B Reacceptance Evidence Repair R2

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = REACCEPTANCE_EVIDENCE_ONLY
PRODUCTION_CODE_CHANGES = FORBIDDEN
TEST_CODE_CHANGES = FORBIDDEN
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Exact tested implementation SHA:

```text
341a19287a90405aab6a196d68b17720ae1cfff3
```

Performance baseline SHA:

```text
6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d
```

Controller acceptance of the E402 repair:

```text
reviews/v0.5/V0.5.1_H40_P3B_E402_TEST_LINT_REPAIR_CONTROLLER_ACCEPTANCE.md
docs commit = f7841d96aa2a6cfe1dec8d789bd64a614f93a065
```

Current code state:

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_SAME_TYPE_BEHAVIOR_BLOCKER_CLOSED_AT_CODE_LEVEL
P3B_E402_TEST_LINT_REPAIR_ACCEPTED
```

Current authority state:

```text
P3B_PRODUCTION_AUTHORITY_REACCEPTANCE_NOT_YET_GRANTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 1. Mission

Produce the complete, reproducible evidence package required for independent controller reacceptance of:

```text
341a19287a90405aab6a196d68b17720ae1cfff3
```

The prior partial evidence collected against `a858835...` is diagnostic only and must not be used as final authority evidence.

Required deliverables:

1. corrected P3B runtime-behavior-seal report;
2. H40 pytest runtime optimization/performance report;
3. machine-readable evidence manifest bound to `341a192...`;
4. raw test/lint/provenance logs.

Do not modify source or tests.

Do not publish a new implementation authority yet.

---

## 2. Starting-state guard

Before tests:

```bash
git rev-parse HEAD
git status --short
git diff -- src tests pyproject.toml configs
```

Require:

```text
HEAD = 341a19287a90405aab6a196d68b17720ae1cfff3
```

Require no tracked implementation/test/config changes.

If any correctness failure requires code repair:

```text
STOP
P3B_REACCEPTANCE_REQUIRES_CODE_REPAIR
```

---

## 3. Import provenance — mandatory

The repository previously had editable-install path confusion.

Before every candidate/baseline benchmark, record:

```bash
.venv/bin/python - <<'PY'
import sys
import btc_quant_agent
print(sys.executable)
print(btc_quant_agent.__file__)
PY
```

Candidate import path must point into the `341a192...` checkout.

Baseline import path must point into the detached `6bd63db...` worktree.

If either resolves to the wrong checkout:

```text
STOP
PERFORMANCE_IMPORT_PROVENANCE_INVALID
```

Use a dedicated baseline venv or an explicitly controlled `PYTHONPATH=<baseline>/src`.

---

## 4. Exact focused collection + execution

Run collection and execution separately.

### Behavior seal

```bash
.venv/bin/python -m pytest   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   --collect-only -q

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   --durations=0
```

### Identity seal

```bash
.venv/bin/python -m pytest   tests/test_v051_h40_p3b_production_verifier_identity_seal.py   --collect-only -q

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_verifier_identity_seal.py   --durations=0
```

### Production Discovery verifier/science

```bash
.venv/bin/python -m pytest   tests/test_v051_h40_p3b_production_discovery_verifier.py   --collect-only -q

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_discovery_verifier.py   --durations=20
```

Record exact collected counts and exact execution summaries.

Do not infer counts from requirement labels.

---

## 5. Focused lifecycle/governance

Run:

```bash
.venv/bin/python -m pytest -q   tests/test_v051_h40_f01_lifecycle_authority.py   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py   tests/test_v051_h40_f01r3r4_lineage_durability.py   tests/test_v051_h40_implementation_authority_publication.py   --durations=30
```

Document coverage equivalence for the fixture restructuring:

```text
old forged exact-type production verifier
    ->
synthetic lifecycle governance evidence
+
real production source/split seal
+
focused real production verifier adversarial tests
```

Explicitly identify coverage for:

- exact-type production verifier identity/integrity;
- same-type constructor/method/state tamper;
- production cold restore/replay tamper;
- real source/split cold validation;
- production WF NOT_TESTABLE fence;
- durable lifecycle replay/lineage.

If any required production behavior is no longer covered:

```text
STOP
TEST_COVERAGE_EQUIVALENCE_NOT_PROVEN
```

---

## 6. Execution fences

Run:

```bash
.venv/bin/python -m pytest -q   tests/test_execution.py   tests/test_execution_environment_authority.py
```

Record:

```text
CURRENT_EXECUTION_POLICY
ACCEPTED_EXECUTION_WRITE_AUTHORITY
```

Expected:

```text
RESEARCH_DISABLED_V1
None
```

---

## 7. Candidate full-suite — two fresh serial runs

On exact SHA `341a192...`:

```bash
/usr/bin/time -v .venv/bin/python -m pytest -q   --durations=50 --durations-min=0.25
```

Run twice.

Capture separately:

- pytest summary;
- pytest duration;
- wall clock;
- user/system CPU;
- CPU %;
- max RSS;
- top 50 durations;
- exit code.

Both runs must use the verified candidate import path.

---

## 8. Baseline full-suite comparison

Create a detached worktree at:

```text
6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d
```

Prove baseline import provenance.

Run the same serial full-suite command.

Preferred:

```text
2 baseline runs
```

Minimum:

```text
1 fresh same-host baseline run
```

Record normal background collector/service state for candidate and baseline.

Do not stop collectors merely to improve benchmark numbers.

---

## 9. Top-five H40 comparison

Compare the previously dominant H40 tests between baseline and candidate.

Original reference set:

```text
test_a21_through_a29_model_a_capability_lifetime
test_r03_production_wf_cold_restore_avoids_caller_override_path
test_a09_foreign_verifier_cannot_cold_restore_candidate_lock
test_r35_r37_r38_production_cold_restore_and_active_source_change_detection
test_r02_production_wf_revalidation_avoids_caller_override_path
```

Some names/semantics were intentionally restructured in `a858835...`.

For every renamed/restructured item provide:

```text
baseline test
candidate replacement test(s)
semantic coverage mapping
baseline duration
candidate duration
delta
```

Do not compare unrelated tests solely for timing.

---

## 10. Performance classification

Primary target:

```text
candidate serial median <= 480 seconds
OR
>=25% reduction versus comparable baseline median
```

Top-five target:

```text
combined top-five reduction >=40%
```

Return exactly one:

```text
FULL_PYTEST_PERFORMANCE_ACCEPTED
FULL_PYTEST_PERFORMANCE_IMPROVED_BUT_TARGET_NOT_MET
FULL_PYTEST_PERFORMANCE_NOT_IMPROVED
PERFORMANCE_COMPARISON_INCONCLUSIVE
```

Performance status is separate from correctness.

Do not manipulate coverage or disable tests to hit a timing target.

---

## 11. Repository-wide static gates

Run exact:

```bash
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

Record exact commands and exit codes.

`ruff check .` must now pass after the accepted E402 repair.

If it fails for another reason:

```text
STOP
P3B_REACCEPTANCE_REQUIRES_CODE_REPAIR
```

unless the failure is provably pre-existing and unrelated, in which case record the exact baseline comparison and stop for controller decision rather than silently ignoring it.

---

## 12. Raw evidence directory

Create on `v0.5-refactor-docs`:

```text
evidence/v0.5/h40/p3b_runtime_behavior_seal_341a192/
```

Include:

```text
collect_behavior.txt
test_behavior.txt
collect_identity.txt
test_identity.txt
collect_production_discovery.txt
test_production_discovery.txt
focused_lifecycle.txt
execution_fences.txt
full_candidate_run_1.txt
full_candidate_run_2.txt
full_baseline_run_1.txt
full_baseline_run_2.txt        # if performed
ruff_all.txt
mypy.txt
compileall.txt
diff_check.txt
provenance_candidate.txt
provenance_baseline.txt
```

Each log must contain a small header:

```text
command
cwd
tested SHA
UTC timestamp
exit code
```

Do not dump secrets or unrelated environment variables.

---

## 13. Machine-readable evidence manifest

Create:

```text
evidence/v0.5/h40/V0.5.1_H40_P3B_RUNTIME_BEHAVIOR_SEAL_LOCAL_EVIDENCE_341a192.json
```

Required identity:

```json
{
  "tested_commit_sha": "341a19287a90405aab6a196d68b17720ae1cfff3",
  "behavior_seal_parent_sha": "a858835ac3239f919bb2dcdc87f74db894f899cf",
  "performance_baseline_sha": "6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d"
}
```

Also include:

- schema_id;
- generated_at_utc;
- Python/pytest versions;
- import provenance;
- collection results;
- focused test results;
- lifecycle result;
- execution fence result;
- candidate/baseline full pytest runs;
- performance comparison;
- top-five mapping/comparison;
- ruff/mypy/compileall/diff-check;
- frozen identities;
- coverage-equivalence statement;
- raw log SHA-256 map;
- protected-surface attestation.

Protected-surface object must include:

```json
{
  "real_h40_outcomes_accessed": false,
  "wf1_calibration_outcomes_accessed": false,
  "confirmation_accessed": false,
  "h39_accessed": false,
  "final_holdout_accessed": false,
  "signed_network_requests": 0
}
```

Use deterministic stable JSON formatting and compute the final manifest SHA-256.

---

## 14. Correct behavior-seal report

Update:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_VERIFIER_RUNTIME_BEHAVIOR_SEAL_REPAIR_REPORT.md
```

Correct all previously identified inaccuracies.

In particular:

- no nonexistent `_initialized` mechanism;
- resolver authority field is `_root`;
- write-once guard uses specific fields + `hasattr`;
- constructor binding described exactly;
- B01-B18 are requirement labels, not pytest case count;
- use actual collect-only/execution counts;
- bind final evidence to `341a192...`;
- link raw logs and manifest hash.

Final authority status must remain:

```text
P3B_PRODUCTION_VERIFIER_RUNTIME_BEHAVIOR_SEAL_REPAIRED
PENDING_INDEPENDENT_SOL_REACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 15. Performance report

Create:

```text
reviews/v0.5/V0.5.1_H40_PYTEST_RUNTIME_OPTIMIZATION_REPORT.md
```

Include:

- candidate SHA `341a192...`;
- baseline SHA `6bd63db...`;
- import provenance;
- versions/environment equivalence;
- raw timing samples;
- candidate and baseline median;
- max RSS;
- top-50 comparison;
- top-five semantic mapping;
- fixture restructuring explanation;
- coverage equivalence;
- performance classification.

---

## 16. Frozen identities

Verify unchanged:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

current accepted lifecycle implementation authority =
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

Do not update the accepted implementation authority in this task.

---

## 17. Docs commit only

All generated reports/evidence/logs go to:

```text
v0.5-refactor-docs
```

No implementation commit is allowed.

Suggested docs commit:

```text
docs(v0.5.1): complete P3B reacceptance evidence for 341a192
```

---

## 18. Allowed final state

If correctness evidence is complete and green:

```text
P3B_REACCEPTANCE_EVIDENCE_READY
PENDING_INDEPENDENT_CONTROLLER_REACCEPTANCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Report performance classification separately.

---

## 19. Final response

Return:

```text
tested_commit_sha:
implementation_changed: NO

behavior_report_path:
performance_report_path:
evidence_manifest_path:
evidence_manifest_sha256:
raw_evidence_directory:
docs_commit_sha:

behavior_collection:
behavior_execution:
identity_collection:
identity_execution:
production_discovery_collection:
production_discovery_execution:
focused_lifecycle:
execution_fences:

candidate_full_run_1:
candidate_full_run_2:
candidate_median:
baseline_full_run_1:
baseline_full_run_2:
baseline_median:
performance_delta:
top5_h40_delta:
performance_classification:

candidate_import_path:
baseline_import_path:

ruff_all:
mypy:
compileall:
git_diff_check:

coverage_equivalence_proven:
frozen_identities_unchanged:
accepted_implementation_authority_changed: NO

real_h40_outcomes_accessed: NO
wf1_calibration_outcomes_accessed: NO
confirmation_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
signed_network_requests: 0

final_state:
P3B_REACCEPTANCE_EVIDENCE_READY
PENDING_INDEPENDENT_CONTROLLER_REACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## Recommended executor

```text
Codex Sol High
```

Reason:

```text
This is exact repository/test/evidence work with strict provenance,
repeatable benchmarks, hashing, and docs-only publication.
Astra is not needed until implementation authority is republished
and the cumulative pre-Discovery audit begins.
```
