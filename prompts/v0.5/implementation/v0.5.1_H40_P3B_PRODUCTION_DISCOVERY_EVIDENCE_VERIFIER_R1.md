# Agent Prompt — V0.5.1 H40 P3B Production Discovery Evidence Verifier R1

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = PRODUCTION_DISCOVERY_SCIENTIFIC_VERIFIER_ONLY
SUPERSEDES = v0.5.1_H40_P3B_PRODUCTION_DISCOVERY_EVIDENCE_VERIFIER.md
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required exact starting code SHA:

```text
0131a9775912e868a4f66d0593e4ed425c699d9e
```

Accepted P3B gap closure:

```text
reviews/v0.5/V0.5.1_H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_CLOSURE.md
commit 21910cbb6b8a1f693caeccb46a705793e93fa8a4

reviews/v0.5/V0.5.1_H40_P3B_FIT_INVALID_CORRECTION_SOL_ACCEPTANCE.md
commit fdf452c4b73d10732e1da8ed64e8603315b02a6b
```

Accepted fail-closed policy:

```text
policy_id =
H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_V1

policy_hash =
7de7c7060c150518805562a473b154d3e91ba5fbc13b5488342ad358744d728a
```

H40 P3 and Discovery remain unauthorized.

---

## 1. Repository preflight is mandatory

The previous attempt stopped with an unreachable configured remote and a code checkout that did not match the required starting SHA.

Do not continue in a stale clone.

First verify:

```bash
git remote -v
git fetch origin --prune
git checkout v0.5-refactor
git rev-parse HEAD
git rev-parse origin/v0.5-refactor
git status --porcelain --untracked-files=no
```

Required:

```text
origin = EXASHXE/btc-quant-agent-public
HEAD = origin/v0.5-refactor =
0131a9775912e868a4f66d0593e4ed425c699d9e
tracked worktree = clean
```

If the current clone cannot fetch the public repo, create a fresh clone of:

```text
EXASHXE/btc-quant-agent-public
```

and continue there.

Do not implement from a stale or private-repository checkout.

Do not force-reset a working tree containing user work.

---

## 2. Mission

Resume the original P3B task:

implement the smallest production-safe non-synthetic H40 Discovery scientific verifier required for candidate-lock / NO-GO adjudication.

The implementation must satisfy the existing:

```text
H40LifecycleEvidenceVerifier
```

Protocol and expose a production verifier with:

```text
synthetic_only = False
```

Suggested class:

```text
H40ProductionDiscoveryEvidenceVerifier
```

Do NOT run real Discovery.

Do NOT access real H40 outcome-bearing data.

---

## 3. Phase-0 accepted-science extraction still applies

Before implementing statistical logic, reconstruct every valid-family scientific rule from the accepted H40 authority chain.

At minimum inspect:

```text
V0.5.1_H40_PROTOCOL_AMENDMENT_R1.md
V0.5.1_H40_PROTOCOL_AMENDMENT_R2.md
V0.5.1_H40_PROTOCOL_AMENDMENT_R3R2_FINAL_PREOUTCOME_CLOSURE.md
V0.5.1_H40_PROTOCOL_AMENDMENT_R3R3_LAST_MILE_DETERMINISM_REPAIR.md
V0.5.1_H40_PROTOCOL_AMENDMENT_R3R4_STATISTICAL_DECISION_FINAL_CLOSURE.md
V0.5.1_H40_R3R4_SOL_FINAL_CUMULATIVE_ACCEPTANCE.md
lifecycle_authority.py
protocol_authority.py
```

For every valid-family formula, threshold, null, bootstrap, seed, quantile, p-value, LCB, hard gate, and selection rule:

- identify exact authority;
- implement exactly;
- do not use library defaults not frozen by H40;
- do not infer from H39.

Any NEW ambiguity other than the already-closed fit-invalid case:

```text
P3B_SPEC_GAP_STOP
```

---

## 4. Binding fit-invalid closure

The previous blocker is now formally closed.

### 4.1 Sealed family behavior

For a given Discovery correction family:

If ANY sealed REGISTERED member is fit-invalid or cannot produce the complete accepted metric/bootstrap inputs:

```text
candidate:
  remains in sealed Discovery ledger
  failure is recorded
  no replacement
  no artificial metric
  no artificial bootstrap vector
  no artificial LCB
  no candidate marginal p-value

family:
  NO_REPRESENTATIVE for PRECISION
  NO_REPRESENTATIVE for NET_EXPECTANCY

within-family correction:
  DO NOT reduce K
  DO NOT drop invalid member
  DO NOT impute statistic
  DO NOT run max-stat on a reduced candidate set
```

Other complete families may continue.

### 4.2 Fixed cross-family universe

Cross-family multiplicity uses exactly six fixed slots:

```text
D1_TREND_CONTINUATION
D2_BREAKOUT_CONTINUATION
D3_FAILED_MOVE_REVERSAL
D4_BTC_ETH_CONFIRM_DIVERGE
D5_FUNDING_DIRECTION_INTERACTION
PAIR_DEPTH_TWO
```

For each metric separately:

```text
PRECISION
NET_EXPECTANCY
```

a NO_REPRESENTATIVE family contributes:

```text
raw_p = 1.0
```

for Holm bookkeeping.

Therefore:

```text
cross-family K = 6 always
```

Do not shrink multiplicity after fit failure or hard-gate failure.

### 4.3 Valid families unchanged

Only a complete family may execute the accepted R1/R2 within-family block max-stat procedure.

For complete families preserve exactly:

- M = 10,000;
- block length = accepted R1/R2 value;
- exact deterministic seed;
- exact null;
- exact nearest-rank quantile;
- exact marginal one-sided bootstrap p-value;
- alpha = 0.05;
- separate PRECISION and NET_EXPECTANCY correction;
- all existing hard gates;
- existing representative rule.

### 4.4 Terminal distinction

If no eligible representatives remain and at least one family is unavailable because accepted scientific inputs are fit-invalid/undefined:

```text
NOT_TESTABLE
reason = NOT_TESTABLE
```

If complete scientific evidence exists but no candidate survives accepted gates/corrections:

```text
H40_NO_GO
reason = THRESHOLD_UNMET
```

### 4.5 Policy binding

Every production Discovery correction/evidence lineage must bind:

```text
fit_invalid_policy_id =
H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_V1

fit_invalid_policy_hash =
7de7c7060c150518805562a473b154d3e91ba5fbc13b5488342ad358744d728a
```

Wrong/missing policy identity fails closed.

---

## 5. Frozen Discovery selection rules

Preserve:

```text
Discovery ranking partition = WF1_CALIBRATION

within-family correction universe =
sealed REGISTERED rows of the family

PRECISION max-stat = separate
NET_EXPECTANCY max-stat = separate

family representatives =
5 single families + 1 pair-family slot

cross-family Holm =
separate for PRECISION and NET_EXPECTANCY
fixed K = 6
alpha = 0.05

final ordering =
1. every accepted hard gate
2. both cross-family Holm controls
3. highest adjusted LCB net expectancy
4. highest adjusted LCB precision
5. lower complexity
6. ascending structural configuration hash / ID

runner-up promotion = forbidden
```

---

## 6. Production content-addressed evidence loader

Implement a strict production resolver under an approved evidence root.

Requirements:

- SHA-256 addressed objects only;
- exact schema_id and exact keys;
- canonical hash recomputation;
- wrong bytes/hash/schema/run/candidate/roster/source/split/contract => fail closed;
- path traversal/symlink escape => fail closed;
- arbitrary path/filename is not authority;
- caller winner/rank/summary is not authority.

Do not internally reuse the synthetic verifier.

---

## 7. Production verifier behavior

### verify_discovery_manifest

Independently verify:

- exact Discovery result schema;
- exact complete sealed 18-entry roster;
- sorting, no missing/extra/duplicate;
- run/seal/source/split lineage;
- correction manifest;
- accepted correction contract hash;
- fit-invalid policy ID/hash;
- complete fixed six-family grouping;
- lower-level content-addressed inputs.

### verify_candidate

For candidates in a complete valid family:

recompute from raw accepted evidence:

- hard gates;
- raw metrics;
- bootstrap statistics;
- family max-stat;
- adjusted LCBs;
- marginal p-values;
- representative eligibility;
- fixed-six cross-family Holm eligibility.

Do not copy a caller-supplied adjusted LCB or pass/fail summary.

For a candidate belonging to a NO_REPRESENTATIVE family:

return/encode a hard-failed verification result through the smallest typed change necessary, without inventing a metric estimate.

If the current `H40CandidateVerification` shape cannot represent "hard fail with undefined LCB" safely, make the smallest explicit typed adjustment required so invalid LCB values are never treated as scientific numbers.

Do not use NaN/Infinity or arbitrary extreme sentinels.

The lifecycle service must rank only candidates with complete valid verification values and passed gates.

### verify_wf_fold

Still fail closed.

Do NOT implement production WF scientific verification in this task.

---

## 8. Mandatory adversarial tests

Retain all V01-V25 tests from the original P3B prompt and add:

### V26 fit-invalid family poisoning

One fit-invalid REGISTERED candidate makes its complete correction family NO_REPRESENTATIVE.

### V27 no reduced-K correction

Prove removing the invalid candidate and running max-stat on K-1 is rejected.

### V28 no metric imputation

Prove 0, null, NaN, +/-Infinity, arbitrary LCB, or caller metric for fit-invalid candidate cannot be used as its scientific metric.

### V29 fixed six-family Holm

NO_REPRESENTATIVE family occupies p=1.0 and K remains exactly six.

### V30 no multiplicity shrinkage

Fixture that would pass if missing family were dropped must fail under fixed-six Holm when the accepted correction says so.

### V31 terminal distinction

- unavailable scientific inputs with zero reps => NOT_TESTABLE
- complete evidence with no accepted survivor => H40_NO_GO

### V32 policy hash binding

Wrong/missing fit-invalid policy ID/hash fails closed.

---

## 9. Frozen identities

No valid-family scientific formula changes.

Require the existing frozen identities to remain exactly unchanged:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

accepted implementation authority =
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

If implementation requires changing any valid-family scientific formula or frozen scientific hash:

```text
STOP
P3B_REQUIRES_UNAUTHORIZED_SCIENTIFIC_AMENDMENT
```

---

## 10. Protected surfaces

Forbidden:

- real H40 labels/outcomes;
- real forward returns;
- real MFE/MAE;
- real Precision / Net Expectancy;
- real candidate ranking;
- WF1_CALIBRATION production reads;
- H39;
- Final Holdout;
- Confirmation;
- signed exchange calls.

Execution remains:

```text
RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

---

## 11. Validation

Run:

```bash
pytest -q tests/test_v051_h40_p3b_production_discovery_verifier.py

pytest -q   tests/test_v051_h40_f01_lifecycle_authority.py   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py   tests/test_v051_h40_f01r3r4_lineage_durability.py   tests/test_v051_h40_implementation_authority_publication.py

pytest -q tests/test_execution.py tests/test_execution_environment_authority.py

pytest -q
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

Do not trigger or inspect CI.

---

## 12. Expected implementation scope

Expected:

```text
src/btc_quant_agent/h40/discovery_evidence.py
src/btc_quant_agent/h40/__init__.py
src/btc_quant_agent/h40/lifecycle_authority.py only if narrow typed integration is required
tests/test_v051_h40_p3b_production_discovery_verifier.py
```

No Discovery runner.

No WF implementation.

No unrelated refactor.

---

## 13. Report

On successful implementation create:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_DISCOVERY_VERIFIER_IMPLEMENTATION_REPORT.md
```

Bind:

- exact starting SHA;
- implementation SHA;
- changed files;
- accepted science authority map;
- fit-invalid policy ID/hash;
- evidence schemas;
- V01-V32 results;
- focused/full local gates;
- frozen hash checks;
- protected-surface attestation.

Conclusion:

```text
H40_P3B_PRODUCTION_DISCOVERY_VERIFIER_IMPLEMENTED
PENDING_INDEPENDENT_SOL_ACCEPTANCE

H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 14. Final response

Return:

```text
starting_sha:
implementation_sha:
changed_files:

production_verifier_class:
fit_invalid_policy_hash:
fixed_cross_family_k:
wf_behavior:

accepted_science_authority_map_complete:
new_valid_family_science_added: NO
frozen_hashes_unchanged:

verifier_tests:
focused_lifecycle:
execution_b04:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
confirmation_accessed: NO
signed_network_requests: 0

implementation_report_path:
docs_commit_sha:

final_state:
H40_P3B_PRODUCTION_DISCOVERY_VERIFIER_IMPLEMENTED
PENDING_INDEPENDENT_SOL_ACCEPTANCE
H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex GPT-6 Sol High
```
