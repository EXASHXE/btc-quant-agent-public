# v0.5.1 H40-P1R6 — Canonical File SHA Presence Repair

## Role

You are a bounded implementation agent. Repair exactly one remaining H40-P1 source-authority defect documented in:

```text
reviews/v0.5/V0.5.1_H40_P1R5_SOL_INDEPENDENT_REVIEW.md
```

Repository: `EXASHXE/btc-quant-agent`

Code branch: `v0.5-refactor`

Required starting code HEAD:

```text
8fbd9c92e4c52da4be51c65b12bddc46f9f33b50
```

Docs branch: `v0.5-refactor-docs`

Frozen semantic kernel baseline remains:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

Governing protocol remains `H40_PROTOCOL_V1_R2`.

## Required starting state

```text
P1F1_CONFIRMATION_AUTHORITY_CLOSED
P1F2_PROTECTED_SOURCE_RELABELLING_CLOSED
P1F3_SOURCE_SPLIT_RECONSTRUCTION_CORE_VALID
P1R_F1_DIRECT_ARRAY_BOOLEAN_BYPASSES_CLOSED
P1RR_F1_OFFLINE_ATTESTATION_FORGEABILITY_CLOSED
P1RRR_F1_CANONICAL_SOURCE_ROOT_SUBSTITUTION_CLOSED
P1R4_F1_PRODUCTION_TEST_AUTHORITY_BOUNDARY_CLOSED
P1R4_F2_EXACT_HEAD_CI_GREEN
P1R5_F1_CANONICAL_FILE_SHA_OMISSION_OPEN_MEDIUM
H40_P1_NOT_YET_ACCEPTED
H40_P2_NOT_AUTHORIZED
```

---

## Hard preflight

1. Fetch `origin/v0.5-refactor` and require exact HEAD `8fbd9c92e4c52da4be51c65b12bddc46f9f33b50`.
2. Read the P1R5 implementation report and Sol independent review.
3. Verify working tree clean.
4. Verify no H40 labels/features/search/backtests/confirmation outcomes have been introduced.
5. Do not rebase onto another code SHA.

If HEAD differs, stop with:

```text
H40_P1R6_ABORTED_HEAD_DRIFT
expected=8fbd9c92e4c52da4be51c65b12bddc46f9f33b50
actual=<sha>
```

---

## Mission

Close exactly this finding:

```text
P1R5-F1 MEDIUM — canonical reference file SHA can be omitted as None
```

P1R5 correctly separates production source authority from synthetic validator mechanics and explicitly marks BTC canonical production authority `NOT_TESTABLE`. However, `assert_canonical_source_record(...)` currently treats a missing `record.file_sha256` as permission to skip comparison even when the canonical spec freezes a `reference_file_sha256`.

This is a narrow fail-closed repair. Do not redesign source authority.

---

## Required semantics

If:

```python
spec.reference_file_sha256 is not None
```

then production canonical source validation MUST require:

```python
record.file_sha256 is not None
and record.file_sha256 == spec.reference_file_sha256
```

Therefore all of these must fail closed:

```text
file_sha256 = None
file_sha256 = ""
file_sha256 = malformed/non-64-hex value
file_sha256 = any unequal SHA
```

A precise `SOURCE_HASH_MISMATCH` is preferred.

The exact frozen SHA must continue to pass when all other canonical fields are valid.

Do not infer or replace source bytes. Do not change the canonical source locator or source identity.

---

## Important retained semantics

Keep all P1R5 closures intact:

1. Production `materialize_authoritative(...)` has no caller policy override.
2. Production `assert_authoritative(...)` has no caller policy override.
3. Synthetic validator splits remain `is_authoritative=False` and `is_production_canonical=False`.
4. `assert_authoritative(...)` rejects synthetic validator output.
5. BTC canonical production state remains `NOT_TESTABLE` under `H40_PROTOCOL_V1_R2`.
6. Current absence/incompatibility of the BTC canonical 1h artifact must NOT be repaired by substituting another file.
7. ETH reference SHA/archive identity stays as currently preregistered.
8. `repo_root` cold verification remains mandatory.
9. Split membership is cold-reconstructed from BTC/ETH timestamp intersection.
10. H39 and Final Holdout remain protected.
11. Execution remains disabled.

---

## Expected code scope

Production change should normally be confined to:

```text
src/btc_quant_agent/h40/source_manifest.py
```

Tests:

```text
tests/test_v051_h40_p1_scaffold.py
```

`__init__.py` or `split_manifest.py` should not need semantic changes. If broader production changes appear necessary, stop and report why rather than expanding scope.

---

## Mandatory adversarial tests

Add focused tests proving:

1. canonical ETH record with `file_sha256=None` fails canonical authority;
2. canonical ETH record with `file_sha256=""` fails canonical authority;
3. canonical ETH record with malformed/nonmatching SHA fails;
4. canonical ETH record with exact frozen SHA and exact canonical archive/locator/product/cadence passes `assert_canonical_source_record(...)`;
5. BTC remains `NOT_TESTABLE` even if caller supplies a valid-looking 1h file and a caller-computed SHA;
6. synthetic validator split remains non-authoritative and rejected by production assertion;
7. previously closed offline/cold/split-rehash attacks remain green.

Tests must be hermetic and must not depend on repository-local untracked market artifacts.

---

## Absolute prohibitions

Do not:

- authorize BTC production source under current protocol;
- alter H40 protocol/preregistration;
- compute H40 labels;
- compute forward returns;
- compute MFE/MAE;
- implement or inspect alpha features/signals;
- rank/search the 168 configurations;
- backtest;
- access confirmation outcomes;
- access H39 outcomes;
- access Final Holdout outcomes;
- alter economic/formal kernel semantics;
- enable execution;
- modify CI workflow configuration;
- start H40-P2.

---

## Validation

Run locally before push:

```text
pytest -q tests/test_v051_h40_p1_scaffold.py
ruff check .
mypy
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

Do not manually dispatch Actions. Push once after local gates pass. If normal push CI runs, record the exact run and require success.

---

## Deliverables

Push the narrow repair to `v0.5-refactor`.

Write:

```text
reviews/v0.5/V0.5.1_H40_P1R6_IMPLEMENTATION_REPORT.md
```

on `v0.5-refactor-docs` with:

- exact start/final code SHA;
- exact changed files;
- description of null/empty/malformed SHA closure;
- focused and full local test results;
- exact-head automatic CI status;
- confirmation that no H40 outcome/research was performed.

Successful implementation marker:

```text
H40_P1R6_IMPLEMENTED_PENDING_INDEPENDENT_REVIEW
P1R5_F1_CANONICAL_FILE_SHA_OMISSION_CLOSED
H40_LABEL_COMPUTATION_NOT_AUTHORIZED
H40_FEATURE_RESEARCH_NOT_AUTHORIZED
H40_CONFIGURATION_SEARCH_NOT_AUTHORIZED
H40_BACKTEST_NOT_AUTHORIZED
H40_CONFIRMATION_ACCESS_NOT_AUTHORIZED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

Stop after push/report. Do not start P2 until independent acceptance.
