# Agent Prompt — V0.5.1 H40 P3B Reacceptance Evidence Repair R1

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = DOCUMENTATION_AND_EVIDENCE_REPAIR_ONLY
PRODUCTION_CODE_CHANGES = FORBIDDEN
TEST_CODE_CHANGES = FORBIDDEN
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Exact tested implementation SHA:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

Evidence/docs branch:

```text
v0.5-refactor-docs
```

Latest independent controller review:

```text
reviews/v0.5/V0.5.1_H40_P3B_REACCEPTANCE_EVIDENCE_CONTROLLER_REVIEW.md
docs commit = a05accf976881b4fc588491d299823742e26575d
```

Current code-level state:

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_SAME_TYPE_BEHAVIOR_BLOCKER_CLOSED_AT_CODE_LEVEL
```

Current evidence state:

```text
P3B_REACCEPTANCE_EVIDENCE_REVIEW_BLOCKED
P3B_PRODUCTION_AUTHORITY_REACCEPTANCE_NOT_YET_GRANTED
FULL_PYTEST_PERFORMANCE_ACCEPTANCE_PENDING_EVIDENCE
```

No H40 P3 / Discovery authority is granted.

---

## 1. Mission

Repair and complete the reacceptance evidence for the already reviewed implementation:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

This is NOT a code-repair task.

Required outcomes:

1. Correct factual inaccuracies in the existing behavior-seal report.
2. Produce exact pytest collection and execution evidence.
3. Produce the missing pytest performance report.
4. Produce a machine-readable evidence manifest bound to the exact tested SHA.
5. Preserve the current implementation and tests byte-for-byte.

Do not modify implementation authority publication yet.

---

## 2. Hard no-change guard

The tested implementation tree must remain exactly:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

Before tests:

```bash
git rev-parse HEAD
git status --short
git diff -- src tests pyproject.toml configs
```

After all tests:

```bash
git rev-parse HEAD
git status --short
git diff -- src tests pyproject.toml configs
```

Require no tracked changes under:

```text
src/**
tests/**
pyproject.toml
configs/**
```

If a correctness failure requires changing source/tests:

```text
STOP
P3B_REACCEPTANCE_REQUIRES_CODE_REPAIR
```

Do not fix it inside this task.

---

## 3. Correct the existing behavior-seal report

Existing report:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_VERIFIER_RUNTIME_BEHAVIOR_SEAL_REPAIR_REPORT.md
```

Correct the report to match the exact implementation.

### 3.1 Remove nonexistent implementation details

Do not claim an `_initialized` guard.

Do not describe the resolver field as `_approved_evidence_root`.

The actual resolver authority field is:

```text
_root
```

The actual normal-assignment write protection is based on:

```text
hasattr(self, field)
```

for the specific write-once authority fields.

### 3.2 Describe the constructor binding exactly

Document that `_VERIFIER_BINDINGS` binds the actual:

```text
resolver object
resolver._root
seal object
seal.authority_context_hash
seal transitive context binding
receipt object
receipt.receipt_sha256
split object
canonical_sha256(split.to_dict())
```

Do not replace this with an inaccurate pseudo-tuple and call it exact.

### 3.3 Use exact failure messages only when quoted

If quoting an exception message, copy it from the actual tested code/output.

Otherwise paraphrase without quotation marks.

### 3.4 Distinguish requirement IDs from pytest cases

`B01 ... B18` are adversarial requirement labels.

Do NOT state that there are necessarily 18 pytest cases.

Report:

```text
requirements covered = B01-B18
collected pytest cases = <actual collect-only result>
```

separately.

---

## 4. Exact pytest collection evidence

For each focused suite, run BOTH collection and execution.

### 4.1 Behavior seal

```bash
.venv/bin/python -m pytest   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   --collect-only -q

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   --durations=0
```

### 4.2 Identity seal

```bash
.venv/bin/python -m pytest   tests/test_v051_h40_p3b_production_verifier_identity_seal.py   --collect-only -q

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_verifier_identity_seal.py   --durations=0
```

### 4.3 Production Discovery verifier/science

```bash
.venv/bin/python -m pytest   tests/test_v051_h40_p3b_production_discovery_verifier.py   --collect-only -q

.venv/bin/python -m pytest -q   tests/test_v051_h40_p3b_production_discovery_verifier.py   --durations=20
```

Record exact collected counts and exact execution summaries.

Do not infer counts from function names.

---

## 5. Focused lifecycle/governance validation

Run:

```bash
.venv/bin/python -m pytest -q   tests/test_v051_h40_f01_lifecycle_authority.py   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py   tests/test_v051_h40_f01r3r4_lineage_durability.py   tests/test_v051_h40_implementation_authority_publication.py   --durations=30
```

Record exact summary and wall time.

Also document the test-coverage restructuring:

```text
old forged exact-type production verifier
    ->
synthetic lifecycle governance path + real production source/split seal
```

Explicitly identify which tests still exercise:

- production exact-type candidate-lock rejection;
- production verifier runtime integrity;
- production cold-restore verifier tamper rejection;
- real source/split cold validation;
- production WF NOT_TESTABLE fence.

If any required production behavior disappeared from coverage:

```text
STOP
TEST_COVERAGE_EQUIVALENCE_NOT_PROVEN
```

---

## 6. Execution fences

Run:

```bash
.venv/bin/python -m pytest -q   tests/test_execution.py   tests/test_execution_environment_authority.py
```

Record exact current values:

```text
CURRENT_EXECUTION_POLICY
ACCEPTED_EXECUTION_WRITE_AUTHORITY
```

Expected:

```text
RESEARCH_DISABLED_V1
None
```

---

## 7. Candidate full-suite evidence — two serial runs

On exact candidate SHA:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

run twice:

```bash
/usr/bin/time -v .venv/bin/python -m pytest -q   --durations=50 --durations-min=0.25
```

Capture each run separately.

Required fields:

```text
pytest summary
pytest reported duration
wall clock
user CPU
system CPU
CPU %
max RSS
top 50 durations
exit code
```

Do not substitute a prior single run for this requirement.

---

## 8. Baseline performance measurement

Baseline SHA:

```text
6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d
```

Use a separate detached worktree.

Important: prior repository history exposed editable-install path confusion. Therefore prove import provenance before every candidate and baseline benchmark.

### Candidate provenance

Record:

```bash
.venv/bin/python - <<'PY'
import sys
import btc_quant_agent
print(sys.executable)
print(btc_quant_agent.__file__)
PY
```

Require the module path to resolve to the candidate checkout.

### Baseline provenance

Use either:

- a dedicated baseline venv with identical dependency lock/version state; or
- an explicitly controlled `PYTHONPATH=<baseline_worktree>/src` execution.

Before benchmarking baseline, print:

```python
import btc_quant_agent
print(btc_quant_agent.__file__)
```

Require it to resolve inside the baseline worktree.

If baseline imports candidate code:

```text
STOP
PERFORMANCE_BASELINE_IMPORT_PROVENANCE_INVALID
```

Run the same full-suite command on baseline.

Prefer two serial baseline runs.

Minimum acceptable evidence:

```text
candidate = 2 fresh runs
baseline = 1 fresh same-host run
```

Record background collector/service state for both benchmark sets.

Do not stop normal collectors merely to improve benchmark numbers.

---

## 9. Top-five H40 comparison

Measure the previously dominant slow H40 tests on baseline and candidate.

At minimum include:

```text
test_a21_through_a29_model_a_capability_lifetime
test_r03_production_wf_cold_restore_avoids_caller_override_path
test_a09_foreign_verifier_cannot_cold_restore_candidate_lock
test_r35_r37_r38_production_cold_restore_and_active_source_change_detection
test_r02_production_wf_revalidation_avoids_caller_override_path
```

If names were renamed by the candidate test restructuring, map old -> new explicitly.

Do not silently compare different semantics.

Record:

```text
baseline seconds
candidate seconds
delta %
coverage/semantic mapping
```

---

## 10. Performance classification

Use measured evidence only.

Primary acceptance target:

```text
candidate serial median <= 480 seconds
OR
candidate serial median improves >=25% versus comparable baseline median
```

Top-five target:

```text
combined top-five reduction >=40%
```

Return exactly one:

```text
FULL_PYTEST_PERFORMANCE_ACCEPTED
FULL_PYTEST_PERFORMANCE_IMPROVED_BUT_TARGET_NOT_MET
FULL_PYTEST_PERFORMANCE_NOT_IMPROVED
PERFORMANCE_COMPARISON_INCONCLUSIVE
```

Do not convert a failed performance target into a correctness failure.

---

## 11. Static quality gates

Run the exact repository-wide command:

```bash
ruff check .
```

Record:

```text
command
exit code
summary
exact failing files/rules if nonzero
```

If pre-existing unrelated lint fails, report:

```text
REPOSITORY_WIDE_RUFF = FAIL_PREEXISTING
```

and separately run a scoped changed-file lint if useful.

Do NOT claim `ruff = PASS` unless `ruff check .` exits 0.

Also run:

```bash
mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

Record exact exit codes.

---

## 12. Raw evidence logs

Create compact raw logs on docs branch:

```text
evidence/v0.5/h40/p3b_runtime_behavior_seal/
  collect_behavior.txt
  test_behavior.txt
  collect_identity.txt
  test_identity.txt
  collect_production_discovery.txt
  test_production_discovery.txt
  focused_lifecycle.txt
  execution_fences.txt
  full_candidate_run_1.txt
  full_candidate_run_2.txt
  full_baseline_run_1.txt
  full_baseline_run_2.txt      # if performed
  ruff_all.txt
  mypy.txt
  compileall.txt
  diff_check.txt
  provenance_candidate.txt
  provenance_baseline.txt
```

Logs must contain command output only plus a small header containing:

```text
command
cwd
implementation SHA
timestamp UTC
exit code
```

Do not include secrets/environment dumps.

---

## 13. Machine-readable evidence manifest

Create:

```text
evidence/v0.5/h40/V0.5.1_H40_P3B_RUNTIME_BEHAVIOR_SEAL_LOCAL_EVIDENCE.json
```

Required fields:

```text
schema_id
tested_commit_sha
parent_commit_sha
generated_at_utc
python_version
pytest_version
candidate_import_provenance
baseline_import_provenance
focused_test_results
collection_results
focused_lifecycle_result
execution_fence_result
candidate_full_pytest_runs
baseline_full_pytest_runs
performance_comparison
top5_h40_comparison
ruff_all_result
mypy_result
compileall_result
git_diff_check_result
frozen_identities
coverage_equivalence
protected_surface_attestation
raw_log_sha256
```

Required protected-surface fields:

```json
{
  "real_h40_outcomes_accessed": false,
  "wf1_calibration_outcomes_accessed": false,
  "confirmation_accessed": false,
  "h39_accessed": false,
  "final_holdout_accessed": false,
  "signed_network_requests": 0
}
```

Use deterministic stable JSON formatting.

Compute SHA-256 of the final manifest and report it.

---

## 14. Performance report

Create:

```text
reviews/v0.5/V0.5.1_H40_PYTEST_RUNTIME_OPTIMIZATION_REPORT.md
```

Must include:

- exact baseline/candidate SHA;
- import provenance;
- environment/version equivalence;
- raw timing samples;
- median/mean;
- max RSS;
- candidate and baseline top-50 lists;
- mapped top-five H40 comparison;
- fixture/test-mode restructuring;
- coverage-equivalence argument;
- performance classification;
- any benchmarking limitation.

Do not claim the performance target if measurements do not satisfy it.

---

## 15. Corrected behavior-seal report

Update:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_VERIFIER_RUNTIME_BEHAVIOR_SEAL_REPAIR_REPORT.md
```

to reference:

- actual collected counts;
- actual execution counts;
- raw log paths and hashes;
- exact implementation mechanism;
- evidence manifest path/hash;
- performance classification.

Do not change its final authority state beyond:

```text
P3B_PRODUCTION_VERIFIER_RUNTIME_BEHAVIOR_SEAL_REPAIRED
PENDING_INDEPENDENT_SOL_REACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 16. Frozen identities

Verify and record:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

Current accepted implementation authority must remain unchanged in this task:

```text
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

---

## 17. Forbidden authority changes

Do NOT modify:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
tests/test_v051_h40_implementation_authority_publication.py
```

Do NOT publish a replacement implementation authority.

Do NOT authorize:

```text
H40_P3
H40_DISCOVERY
real WF1_CALIBRATION reads
```

---

## 18. Commit discipline

All generated evidence/reports/logs go only to:

```text
v0.5-refactor-docs
```

Suggested docs commit:

```text
docs(v0.5.1): complete P3B reacceptance evidence
```

No implementation-branch commit should be created.

---

## 19. Allowed final state

If all correctness evidence is green and documentation is complete:

```text
P3B_REACCEPTANCE_EVIDENCE_READY
PENDING_INDEPENDENT_CONTROLLER_REACCEPTANCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Performance classification is reported separately.

---

## 20. Final response

Return:

```text
tested_commit_sha:
implementation_changed: NO

behavior_report_path:
performance_report_path:
evidence_manifest_path:
evidence_manifest_sha256:
raw_evidence_directory:
docs_commit_sha:

behavior_collection:
behavior_execution:
identity_collection:
identity_execution:
production_discovery_collection:
production_discovery_execution:
focused_lifecycle:
execution_fences:

candidate_full_run_1:
candidate_full_run_2:
candidate_median:
baseline_full_run_1:
baseline_full_run_2:
baseline_median:
performance_delta:
top5_h40_delta:
performance_classification:

candidate_import_path:
baseline_import_path:

ruff_all:
mypy:
compileall:
git_diff_check:

coverage_equivalence_proven:
frozen_identities_unchanged:
accepted_implementation_authority_changed: NO

real_h40_outcomes_accessed: NO
wf1_calibration_outcomes_accessed: NO
confirmation_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
signed_network_requests: 0

final_state:
P3B_REACCEPTANCE_EVIDENCE_READY
PENDING_INDEPENDENT_CONTROLLER_REACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## Recommended executor

```text
Codex Sol High
```

Reason:

```text
This task is deterministic repository/test/evidence work.
It requires exact command execution, worktree provenance control,
hashing, log capture, and docs-only commits.
Astra is not needed at this step.
```
