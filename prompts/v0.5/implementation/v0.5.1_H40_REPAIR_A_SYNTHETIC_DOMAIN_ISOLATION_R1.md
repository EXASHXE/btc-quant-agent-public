# V0.5.1 H40 Repair A — Synthetic Authority Domain Isolation R1

## Executor

**Recommended executor: Gemini 3.8 Flash**

This is a narrow targeted repair of the already-pushed Repair A implementation.

Do not redesign Repair A.
Do not implement Repair B.
Do not change Lifecycle V5 canonical contracts/hashes.
Do not authorize H40 P3 or Discovery.

---

# Repository / exact starting state

Repository:

```text
https://github.com/EXASHXE/btc-quant-agent-public
```

Implementation branch to modify:

```text
v0.5-refactor
```

Exact required starting implementation SHA:

```text
f8d00629fb7dd5d59d8e33a38b4d7ff6387156bf
```

Docs/governance branch:

```text
v0.5-refactor-docs
```

Independent controller blocker:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_A_F01_F04_CONTROLLER_BLOCKER.md

blocker commit =
606e74d1a1ffda872470a2c5f414ee00498b9b23
```

Before editing, verify `v0.5-refactor` remote HEAD is exactly
`f8d00629fb7dd5d59d8e33a38b4d7ff6387156bf`.

If not, stop and report branch drift.

---

# Governing accepted authority

Repair Contract Freeze R2:

```text
cf7b1c9b9547f3e9e6dfab964d7d2c6d1bb35de3
```

Controller Acceptance R2:

```text
2229d44c5cc9b88ba515d0b2931d20f05936c7e8
```

Lifecycle V5 materialization:

```text
e1f368e85bbf1831763c688ca75ef4249971cf59
```

Exact accepted identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

discovery_provenance_contract =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

lifecycle_semantic_root_v5 =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle_governance_authority_v5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf
```

These must remain unchanged.

---

# Current accepted review conclusion

The independent controller replayed the Repair A implementation and confirmed:

```text
all 9 lifecycle child hashes match V5 = YES
lifecycle semantic root matches V5 = YES
V3 Discovery receipt implemented = YES
V2 persistence context implemented = YES
current-anchor revalidation substantially implemented = YES
production accepted constants remain None = YES
Repair B implemented = NO
```

Repair A is blocked only on the synthetic/production authority-domain boundary and the missing
adversarial regression for that boundary.

---

# Exact blocker

Current implementation defines:

```text
_SYNTHETIC_CONTROLLER_REGISTRY
_SYNTHETIC_GRANT_REGISTRY
```

and the synthetic service path registers synthetic controller/grant objects there.

However, `H40LifecycleAuthorityService.production()` currently contains a fallback that reads
`_SYNTHETIC_CONTROLLER_REGISTRY` when:

```text
controller_authority is None
and ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH is not None
and the accepted hash is present in the synthetic registry
```

That permits test-only process state to become a source of a production service controller
object.

This violates the frozen rule:

```text
synthetic/test authority remains a distinct domain
synthetic state cannot be promoted into production authority
```

---

# Mission

Perform the **minimum implementation repair** needed to close that domain leak.

## Required production behavior

`H40LifecycleAuthorityService.production()` MUST NOT read or consult:

```text
_SYNTHETIC_CONTROLLER_REGISTRY
_SYNTHETIC_GRANT_REGISTRY
```

for any production object construction.

Production construction may still allow:

```text
controller_authority = None
discovery_run_grant = None
```

because the frozen contract permits construction before P3 authority exists.

But production `authorize_discovery()` must remain fail-closed unless the required current
production controller authority is explicitly available and validates against:

```text
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH
```

and the exact run grant validates against that controller/run/seal identity.

Do not silently auto-promote a synthetic registry object into production.

## Allowed synthetic behavior

The synthetic registries may remain if they are reachable only from explicitly synthetic/test
paths such as:

```text
H40LifecycleAuthorityService.synthetic_for_tests(...)
H40SyntheticAuthorityResolver
```

and those paths remain marked/validated as synthetic-only.

Do not add synthetic/test-only lookup to any production constructor, production resolver, cold
restore, persistence, or authoritative transition path.

## No schema change

Do not add fields to:

```text
H40_P3_CONTROLLER_AUTHORITY_V1
H40_DISCOVERY_RUN_GRANT_V1
H40_RECEIPT_DISCOVERY_AUTH_V3
H40_LIFECYCLE_PERSISTENCE_CONTEXT_V2
```

Do not change any exact V5 child object.

Therefore the following hashes MUST remain exact:

```text
discovery_authorization_receipt_contract =
3dd91827d4a26441bd5218dc299f196841c471eb60f20e03881d2ad56ce36898

transition_matrix_contract =
bea7b0593251204bbc9138c325f1c9f10ccf7a9b8df60f10bf991377e0ced42d

persistence_replay_contract =
51e4fe6155d388c076dd1788a100265eb17b232300481a3fbcf00150f1432de1

lifecycle semantic root =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle governance V5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf
```

---

# Mandatory adversarial regression

Add a focused test that proves the exact previously reachable path is closed.

At minimum:

1. construct an implementation authority and controller suitable for synthetic/governance testing;
2. call the explicit synthetic path so the synthetic controller is present in
   `_SYNTHETIC_CONTROLLER_REGISTRY`;
3. test-locally monkeypatch the accepted implementation/controller constants to matching hashes;
4. call:

```python
H40LifecycleAuthorityService.production(
    implementation_authority=impl,
    controller_authority=None,
    discovery_run_grant=None,
)
```

5. assert the production service did **not** import the controller from the synthetic registry;
6. attempt production Discovery without explicitly supplying/currently binding the controller;
7. assert fail-closed;
8. separately show the synthetic service can still use the synthetic controller in its synthetic
   domain.

The regression should fail on commit
`f8d00629fb7dd5d59d8e33a38b4d7ff6387156bf` and pass after this repair.

Also preserve existing tests for:

- accepted constants None;
- stale implementation anchor rejection;
- stale controller anchor rejection;
- V2 production receipt rejection;
- V1 production persistence-context rejection;
- synthetic verifier rejection from production;
- protected surfaces and execution disabled.

---

# Production constants remain fail-closed

Committed source after the repair must still contain:

```python
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH: str | None = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH: str | None = None
```

Do not temporarily publish placeholder values in committed source.

Do not create a new lifecycle implementation authority in this task.

---

# Explicit exclusions

Do not implement or modify Repair B / F02 / F03:

```text
H40_SOURCE_VALUE_EXTRACTION_V1
H40_P3B_SOURCE_DERIVATION_V2
H40_CAUSAL_PREFIT_RECONSTRUCTION_V1
H40_P3B_RAW_DECISIONS_V2
```

Do not change:

- D1/D2/D3;
- R_VOL;
- O_RANGE;
- raw-score science;
- percentile/quantile science;
- retrospective calibration;
- candidate universe;
- Holm correction;
- bootstrap/sampler semantics;
- outcome logic;
- Confirmation/H39/Final Holdout protections;
- execution policy.

Avoid unrelated microstructure or repository cleanup in this repair.

---

# Required review of changed surface

Expected production code change is very small and should primarily be:

```text
src/btc_quant_agent/h40/lifecycle_authority.py
tests/test_v051_h40_repair_a_lifecycle_authority.py
```

If another file must change, explain the exact dependency.

Do not refactor the 1,600-line Repair A diff broadly.

---

# Required gates

Run focused tests first, including the new synthetic-domain regression.

Then run:

```bash
python -m compileall src
ruff check .
mypy src
pytest -q
```

Use repository-standard equivalents if command naming differs.

Report exact pass/fail counts and commands.

Do not weaken existing tests.

---

# Commit / push requirements

Commit only the minimal repair to:

```text
v0.5-refactor
```

Suggested commit message:

```text
fix(h40): isolate synthetic authority from production
```

Then verify:

```bash
git rev-parse HEAD
git push origin v0.5-refactor
git ls-remote origin refs/heads/v0.5-refactor
```

The remote branch SHA must equal the local reported SHA.

Do not amend/rebase after reporting it.

---

# Protected state

The task must end with:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

REPAIR_B_NOT_IMPLEMENTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1
```

No protected outcome access is allowed.

---

# Successful final state

```text
REPAIR_A_SYNTHETIC_DOMAIN_ISOLATION_R1_IMPLEMENTED_PENDING_CONTROLLER_REVIEW

SYNTHETIC_CONTROLLER_REGISTRY_UNREACHABLE_FROM_PRODUCTION = YES
MANDATORY_SYNTHETIC_DOMAIN_REGRESSION = PASS

LIFECYCLE_V5_HASHES_UNCHANGED = YES
SCIENTIFIC_IDENTITIES_UNCHANGED = YES

ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

REPAIR_B_NOT_IMPLEMENTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

NEXT_STAGE =
INDEPENDENT_REPAIR_A_R1_REVIEW
```

---

# BLOCK behavior

If closing the synthetic-domain path requires changing any accepted schema, lifecycle child
contract, lifecycle root, governance V5 object, or scientific identity:

```text
REPAIR_A_SYNTHETIC_DOMAIN_ISOLATION_R1_BLOCKED
```

Stop and report the exact contradiction. Do not alter governance to make the implementation easier.

---

# Final response required from executor

Return:

```text
decision:
starting_implementation_sha:
repair_commit_sha:
remote_branch_sha:
remote_push_verified:

changed_files:
new_synthetic_domain_test:
focused_test_result:
full_test_result:
ruff_result:
mypy_result:
compileall_result:

synthetic_controller_registry_reachable_from_production: NO
lifecycle_v5_hashes_changed: NO
scientific_identity_changed: NO

accepted_lifecycle_implementation_authority_hash: None
accepted_h40_p3_controller_authority_hash: None

repair_b_implemented: NO
H40_P3_AUTHORIZED: NO
H40_DISCOVERY_AUTHORIZED: NO

formal_final_state:
next_stage: INDEPENDENT_REPAIR_A_R1_REVIEW
```
