# Agent Prompt — V0.5.1 H40 P3B Production Verifier Identity Seal Repair

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = PRODUCTION_VERIFIER_AUTHORITY_SEAL_ONLY
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required starting HEAD:

```text
3f85d5f1d4e09eeb5e3c8bdd5727ddeeec9ba346
```

Independent review:

```text
reviews/v0.5/V0.5.1_H40_P3B_SOL_INDEPENDENT_REVIEW.md
commit a783b3f38dcf26240c97bf267a04c5f28e0f3c5d
```

Accepted blocker:

```text
PRODUCTION_DISCOVERY_VERIFIER_IDENTITY_NOT_ENFORCED
```

This task does NOT change P3B science and does NOT authorize P3 or Discovery.

---

## 1. Mission

Close the production verifier-injection path.

Current production lifecycle service accepts any runtime object satisfying:

```text
H40LifecycleEvidenceVerifier
synthetic_only = False
```

That is not sufficient authority once one concrete P3B verifier has been independently reviewed.

Production candidate-lock must only accept the repository's exact:

```text
H40ProductionDiscoveryEvidenceVerifier
```

implementation.

A custom Protocol implementation, proxy, wrapper, subclass, or test double with `synthetic_only=False` must not confer production candidate-lock authority.

---

## 2. Minimal production guard

Preferred implementation:

Inside the production candidate-lock path, before invoking:

```text
verify_discovery_manifest
verify_candidate
```

perform a runtime-local import to avoid the existing import cycle:

```python
from .discovery_evidence import H40ProductionDiscoveryEvidenceVerifier
```

Then, when:

```text
self.synthetic_test_mode is False
```

require:

```python
type(verifier) is H40ProductionDiscoveryEvidenceVerifier
```

If not:

```text
fail closed
reason = NOT_TESTABLE or CONFIG_IDENTITY_CONFLICT
message clearly identifies unaccepted production Discovery verifier implementation
```

Use the reason code most consistent with the existing lifecycle authority model.

Do not use only:

```text
isinstance(verifier, H40LifecycleEvidenceVerifier)
synthetic_only == False
verifier_id string
module name
class name string
duck typing
```

as production authority.

Exact concrete type identity is the accepted repair unless an equally strong in-process seal is implemented.

---

## 3. Constructor behavior

It is acceptable for:

```python
H40LifecycleAuthorityService.production(...)
```

to be constructed with:
- no verifier;
- exact `H40ProductionDiscoveryEvidenceVerifier`.

A service with no verifier must continue to fail candidate-lock as:

```text
NOT_TESTABLE
no accepted scientific evidence verifier
```

A foreign non-synthetic verifier may either:
- be rejected during `production(...)` construction; or
- be stored but must be rejected before candidate-lock scientific methods are called.

Prefer early constructor rejection only if it does not introduce import-cycle or lifecycle test complexity.

The critical invariant is:

```text
foreign verifier can never reach candidate-lock authority
```

---

## 4. Synthetic mode remains separate

Preserve:

```text
H40LifecycleAuthorityService.synthetic_for_tests(...)
requires H40SyntheticEvidenceVerifier
```

Synthetic governance tests may continue to use synthetic verifier behavior.

Do NOT permit the production exact-type rule to weaken or merge production/synthetic modes.

---

## 5. Cold restore / persistence

Cold restore of a persisted candidate-lock transition calls back through production lifecycle verification.

Therefore require that restore/replay also fails closed if the active production service uses a foreign verifier.

A previously persisted receipt cannot make a foreign verifier authoritative.

Do not add a restore-only bypass.

---

## 6. Candidate-lock receipt

Preserve:

```text
selection_verifier_id =
H40_DISCOVERY_SELECTION_VERIFIER_V1
```

No receipt schema change is required for this repair.

The exact concrete runtime seal gives operational meaning to that existing verifier ID.

Do not change:
- candidate-lock receipt schema;
- discovery correction contract hash;
- scientific hashes;
- lifecycle semantic/governance hashes

unless absolutely required.

---

## 7. Mandatory adversarial tests

Add a narrowly scoped repair test file or extend the P3B suite.

At minimum:

### A01 exact production verifier accepted

Create the real:

```text
H40ProductionDiscoveryEvidenceVerifier
```

with the existing synthetic/adversarial content-addressed P3B fixture.

Require production candidate-lock reaches the normal scientific verification path.

It need not produce a successful candidate if the fixture intentionally terminates; the verifier implementation identity itself must be accepted.

### A02 arbitrary non-synthetic Protocol verifier rejected

Define:

```python
class RogueVerifier:
    synthetic_only = False
    ...
```

that satisfies the Protocol.

Require rejection before its scientific return values can confer authority.

### A03 all-pass rogue verifier rejected

Rogue verifier returns:

```text
hard_gates_passed=True
very favorable adjusted LCBs
```

for every candidate.

Require no candidate-lock receipt is issued.

Instrument call counters and prove candidate scientific methods are not used as authority.

### A04 subclass rejected

Subclass:

```text
H40ProductionDiscoveryEvidenceVerifier
```

without changing methods.

If exact-type sealing is used, require rejection.

### A05 wrapper/proxy rejected

Object delegating all calls to a real verifier but having different concrete type must be rejected.

### A06 synthetic verifier rejected in production

Preserve existing production synthetic fence.

### A07 synthetic service still works

Existing synthetic lifecycle test mode remains functional.

### A08 no verifier remains fail-closed

Production service with no verifier:

```text
candidate-lock -> NOT_TESTABLE
```

### A09 cold restore foreign verifier rejected

Attempt to restore/reissue a candidate-lock transition using a production service configured with a foreign verifier.

Require fail closed.

### A10 receipt ID unchanged

Real production verifier path still emits:

```text
selection_verifier_id =
H40_DISCOVERY_SELECTION_VERIFIER_V1
```

### A11 six P3B policy bindings unchanged

All existing P3B tests remain green.

### A12 frozen hashes unchanged

Require exact scientific/lifecycle identities remain unchanged.

---

## 8. Existing P3B science must remain untouched

Do NOT change:

- fit-invalid policy;
- seed identity policy;
- proxy net-return formula;
- block-boundary policy;
- start-sampler policy;
- Discovery coverage policy;
- calibration;
- geometry;
- HAC;
- bootstrap;
- p-values;
- max-stat;
- Holm;
- final ordering;
- evidence schemas unless needed solely for implementation seal (normally not needed).

Expected production change should be tiny and primarily in:

```text
src/btc_quant_agent/h40/lifecycle_authority.py
tests/...
```

Do not refactor `discovery_evidence.py` unless required for import/type identity.

---

## 9. Local validation

Run:

```bash
pytest -q tests/test_v051_h40_p3b_production_discovery_verifier.py
pytest -q <new authority-seal test file if separate>

pytest -q   tests/test_v051_h40_f01_lifecycle_authority.py   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py   tests/test_v051_h40_f01r3r4_lineage_durability.py   tests/test_v051_h40_implementation_authority_publication.py

pytest -q tests/test_execution.py tests/test_execution_environment_authority.py

pytest -q
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

Do not trigger or inspect CI.

---

## 10. Frozen identities

Require unchanged:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

accepted lifecycle implementation authority =
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

If this repair requires changing a frozen lifecycle contract instead of enforcing an existing verifier obligation:

```text
STOP
P3B_AUTHORITY_SEAL_REQUIRES_GOVERNANCE_AMENDMENT
```

---

## 11. Protected surfaces

No:
- real H40 outcomes;
- Discovery execution;
- WF outcomes;
- Confirmation;
- H39;
- Final Holdout;
- signed network requests.

Execution remains disabled.

---

## 12. Commit/report

Suggested code commit:

```text
fix(h40): seal production Discovery verifier identity
```

Push to:

```text
origin/v0.5-refactor
```

Create:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_VERIFIER_IDENTITY_SEAL_REPAIR_REPORT.md
```

on docs branch.

Report:
- starting SHA;
- repair SHA;
- exact changed files;
- chosen seal mechanism;
- rogue verifier rejection evidence;
- exact verifier acceptance evidence;
- restore/replay result;
- local gates;
- frozen hashes;
- protected surfaces.

Allowed conclusion:

```text
P3B_PRODUCTION_VERIFIER_IDENTITY_SEAL_REPAIRED
PENDING_INDEPENDENT_SOL_REACCEPTANCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 13. Final response

Return:

```text
starting_sha:
repair_sha:
changed_files:

production_seal_mechanism:
exact_production_verifier_accepted:
rogue_non_synthetic_verifier_rejected:
all_pass_rogue_rejected:
subclass_rejected:
wrapper_rejected:
synthetic_production_rejected:
synthetic_test_mode_preserved:
cold_restore_foreign_verifier_rejected:

p3b_verifier_tests:
authority_seal_tests:
focused_lifecycle:
execution_b04:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

frozen_hashes_unchanged:
real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
confirmation_accessed: NO
signed_network_requests: 0

repair_report_path:
docs_commit_sha:

final_state:
P3B_PRODUCTION_VERIFIER_IDENTITY_SEAL_REPAIRED
PENDING_INDEPENDENT_SOL_REACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex GPT-6 Sol High
```
