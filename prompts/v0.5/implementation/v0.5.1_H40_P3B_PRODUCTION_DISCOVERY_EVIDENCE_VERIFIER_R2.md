# Agent Prompt — V0.5.1 H40 P3B Production Discovery Evidence Verifier R2

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = PRODUCTION_DISCOVERY_SCIENTIFIC_VERIFIER_ONLY
SUPERSEDES =
v0.5.1_H40_P3B_PRODUCTION_DISCOVERY_EVIDENCE_VERIFIER.md
v0.5.1_H40_P3B_PRODUCTION_DISCOVERY_EVIDENCE_VERIFIER_R1.md
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required starting HEAD:

```text
2e7c21c0f32f621321d56124c98cfb883946d4f3
```

This HEAD contains only the independent microstructure stats-cache maintenance change after the previous H40 baseline. Do not revert it.

P3B remains pre-outcome. No real H40 outcome-bearing data may be read.

---

## 1. Accepted new closures

### 1.1 Fit-invalid correction closure

```text
policy_id =
H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_V1

policy_hash =
7de7c7060c150518805562a473b154d3e91ba5fbc13b5488342ad358744d728a
```

Accepted artifacts:

```text
reviews/v0.5/V0.5.1_H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_CLOSURE.md
reviews/v0.5/V0.5.1_H40_P3B_FIT_INVALID_CORRECTION_SOL_ACCEPTANCE.md
```

### 1.2 Bootstrap seed identity binding

```text
policy_id =
H40_P3B_BOOTSTRAP_SEED_IDENTITY_BINDING_V1

policy_hash =
7b2af0e0e7bde22ba10e81a7b356b6489614d42e6dc3a14a4b8d901423868d2c
```

Accepted artifacts:

```text
reviews/v0.5/V0.5.1_H40_P3B_BOOTSTRAP_SEED_IDENTITY_BINDING_CLOSURE.md
reviews/v0.5/V0.5.1_H40_P3B_BOOTSTRAP_SEED_IDENTITY_SOL_ACCEPTANCE.md
```

Both policy IDs/hashes must be bound into production Discovery evidence/replay.

---

## 2. Mandatory preflight

```bash
git fetch origin --prune
git checkout v0.5-refactor
git rev-parse HEAD
git rev-parse origin/v0.5-refactor
git status --porcelain --untracked-files=no
git remote -v
```

Require:

```text
HEAD =
origin/v0.5-refactor =
2e7c21c0f32f621321d56124c98cfb883946d4f3
```

and public origin:

```text
EXASHXE/btc-quant-agent-public
```

Tracked worktree must be clean.

If not, STOP rather than coding on a stale checkout.

---

## 3. Mission

Implement the smallest production-safe non-synthetic:

```text
H40ProductionDiscoveryEvidenceVerifier
```

satisfying:

```text
H40LifecycleEvidenceVerifier
synthetic_only = False
```

for Discovery candidate-lock / NO_GO / NOT_TESTABLE adjudication.

Do not implement a real Discovery runner.

Do not implement production WF verification.

Do not read real H40 outcomes.

---

## 4. Exact bootstrap seed identity

R2 seed derivation remains exactly:

```python
seed_preimage = canonical_json([
    protocol_id,
    candidate_id,
    partition_id,
    metric_id,
]).encode("utf-8")

digest = hashlib.sha256(seed_preimage).digest()
raw_u64 = int.from_bytes(digest[:8], byteorder="big", signed=False)
seed = raw_u64 % (2**63 - 1)

rng = np.random.Generator(np.random.PCG64(seed))
```

The exact variable bindings are now frozen.

### protocol_id

```text
protocol_id =
compute_protocol_authority_hash()
=
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce
```

Do NOT use:

```text
H40ProtocolIdentity.protocol_hash
6533...
```

### candidate_id

```text
candidate_id =
structural_configuration_hash
```

Do NOT use:

```text
config_id
slot_hash
slot_index
```

### Discovery partition_id

```text
partition_id =
"WF1_CALIBRATION"
```

Do NOT use split hashes, paths, timestamps, or aliases.

### Discovery metric_id

Exactly:

```text
"PRECISION"
"NET_EXPECTANCY"
```

For RANDOM_MATCHED when applicable:

```text
"RANDOM_MATCHED"
```

No alternate casing or enum repr strings.

### Runtime invariance

For the same:

```text
protocol_authority_hash
structural_configuration_hash
partition_id
metric_id
```

the seed MUST remain identical across:

```text
run_authority_id changes
runtime snapshot rematerialization
source/split materialization changes
filesystem paths
Git commit location
slot presentation/order
```

if structural candidate identity is unchanged.

---

## 5. Required seed audit metadata

Every production bootstrap evidence object must bind:

```text
seed_identity_policy_id
seed_identity_policy_hash
protocol_id
candidate_id
partition_id
metric_id
canonical_seed_preimage
seed_digest_sha256
raw_u64
seed
numpy_version
```

Require:

```text
seed_identity_policy_id =
H40_P3B_BOOTSTRAP_SEED_IDENTITY_BINDING_V1

seed_identity_policy_hash =
7b2af0e0e7bde22ba10e81a7b356b6489614d42e6dc3a14a4b8d901423868d2c
```

Missing or foreign policy identity fails closed.

Do not trust a caller-supplied integer seed without recomputing all metadata.

---

## 6. Fit-invalid family behavior

Preserve the accepted R1 closure exactly.

If ANY sealed REGISTERED member of one correction family cannot produce the complete accepted valid metric/bootstrap inputs:

```text
candidate remains in sealed ledger
candidate failure recorded
no replacement
no metric imputation
no NaN/Infinity sentinel
no reduced-K max-stat

whole family =
NO_REPRESENTATIVE
for PRECISION and NET_EXPECTANCY
```

Cross-family Holm uses exactly six fixed family slots.

A NO_REPRESENTATIVE family contributes:

```text
raw p = 1.0
```

for each metric.

Therefore:

```text
cross-family K = 6 always
```

---

## 7. Complete valid-family science

Before implementing each formula, recover its exact accepted authority from R1/R2/R3R3/R3R4.

For a complete valid family preserve exactly:

- Discovery partition = WF1_CALIBRATION;
- sealed REGISTERED family universe;
- 168-hour non-circular moving blocks;
- M = 10,000;
- canonical seed rule above;
- exact R2 nearest-rank quantiles;
- exact marginal one-sided bootstrap p-values;
- accepted p0 context;
- separate PRECISION max-stat;
- separate NET_EXPECTANCY max-stat;
- fixed-six cross-family Holm, separately per metric;
- alpha = 0.05;
- accepted hard gates;
- final deterministic ordering;
- no runner-up.

If ANY new valid-family formula/threshold/null/identity remains ambiguous:

```text
P3B_SPEC_GAP_STOP
```

Do not infer or invent.

---

## 8. Production evidence resolver

Implement strict content-addressed evidence resolution under an approved root.

Requirements:

- requested identities are lowercase SHA-256;
- path traversal/symlink escape fails closed;
- exact versioned schemas;
- exact key sets;
- canonical hash recomputation;
- recursive run/candidate/source/split/contract lineage;
- wrong hash/schema/run/candidate/roster/policy => fail closed;
- caller paths, ranks, winner flags, summaries, booleans are not authority.

Do not reuse `H40SyntheticEvidenceVerifier` internally.

---

## 9. verify_discovery_manifest

Must verify/recompute at least:

- exact H40_DISCOVERY_RESULT_EVIDENCE_V1;
- exact run authority;
- exact sealed 18-entry REGISTERED roster;
- no missing/extra/duplicate;
- structural hashes sorted as accepted;
- source/split lineage;
- correction manifest;
- DISCOVERY_SELECTION_V1 content hash;
- fit-invalid policy ID/hash;
- seed-identity policy ID/hash;
- six fixed family groups;
- all lower-level evidence hashes.

No caller-provided rank or representative is authoritative.

---

## 10. verify_candidate

For complete valid families recompute:

```text
hard gates
raw precision
raw net expectancy
bootstrap vectors
marginal p-values
within-family max-stat
adjusted LCB precision
adjusted LCB net expectancy
family representative eligibility
cross-family Holm eligibility
```

Do not copy caller-provided adjusted bounds.

For NO_REPRESENTATIVE families, encode hard failure without fake numerical LCB values.

If the current `H40CandidateVerification` shape cannot safely represent undefined scientific values, make the smallest explicit typed change required.

Do not use NaN, Infinity, or magic extreme sentinels.

Lifecycle ranking must only operate on complete valid candidate verification values.

---

## 11. verify_wf_fold

Remain fail closed.

Do not implement production WF scientific verification in this task.

---

## 12. Mandatory seed-specific tests

In addition to the original P3B R1 adversarial suite, add:

### S01 protocol authority binding

Verify seed uses:

```text
a83f8fc...
```

and rejects `protocol_hash=6533...`.

### S02 structural candidate binding

Verify seed uses `structural_configuration_hash` and rejects `config_id`.

### S03 runtime invariance

Same structural candidate/partition/metric under different run-authority IDs yields the exact same seed.

### S04 source/split invariance

Runtime source/split identity changes do not enter seed preimage.

### S05 metric separation

PRECISION and NET_EXPECTANCY produce different canonical seed preimages/digests for the same candidate.

### S06 candidate separation

Two distinct structural configuration hashes produce different preimages/digests.

### S07 exact partition string

Only `WF1_CALIBRATION` is accepted for Discovery bootstrap.

### S08 seed metadata replay

Persisted seed metadata must independently recompute to the exact same integer seed.

### S09 policy binding

Missing/wrong seed policy ID/hash fails closed.

### S10 no caller seed authority

A caller-provided seed that disagrees with recomputation is rejected.

---

## 13. Fit-invalid tests

Retain R1 V26-V32:

- family poisoning;
- no reduced K;
- no metric imputation;
- fixed-six Holm;
- no multiplicity shrinkage;
- NOT_TESTABLE vs H40_NO_GO distinction;
- fit-invalid policy hash binding.

Retain all original V01-V25 verifier tests as well.

---

## 14. Frozen identities

Must remain unchanged:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

accepted lifecycle implementation authority =
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

If implementation requires altering a valid-family scientific formula or any frozen scientific hash:

```text
STOP
P3B_REQUIRES_UNAUTHORIZED_SCIENTIFIC_AMENDMENT
```

---

## 15. Protected surfaces

Do not access:

```text
real H40 labels
real forward returns
real MFE/MAE
real Discovery metrics
WF1_CALIBRATION production outcomes
WF validation outcomes
Confirmation
H39
Final Holdout
signed exchange APIs
```

Execution remains:

```text
RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

---

## 16. Expected code scope

Prefer:

```text
src/btc_quant_agent/h40/discovery_evidence.py
src/btc_quant_agent/h40/__init__.py
src/btc_quant_agent/h40/lifecycle_authority.py   only for narrow typed integration
tests/test_v051_h40_p3b_production_discovery_verifier.py
```

Do not modify:

```text
src/btc_quant_agent/microstructure.py
tests/test_microstructure_stats_cache.py
```

The emergency cache repair is an independent accepted maintenance surface.

---

## 17. Local gates

Run:

```bash
pytest -q tests/test_v051_h40_p3b_production_discovery_verifier.py

pytest -q   tests/test_v051_h40_f01_lifecycle_authority.py   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py   tests/test_v051_h40_f01r3r4_lineage_durability.py   tests/test_v051_h40_implementation_authority_publication.py

pytest -q tests/test_execution.py tests/test_execution_environment_authority.py

pytest -q
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

Do not inspect or trigger CI.

---

## 18. Commit/report

Suggested commit:

```text
feat(h40): implement production Discovery evidence verifier
```

Push to:

```text
origin/v0.5-refactor
```

Create on docs branch:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_DISCOVERY_VERIFIER_IMPLEMENTATION_REPORT.md
```

Report must bind both policy IDs/hashes and the final implementation SHA.

Allowed conclusion:

```text
H40_P3B_PRODUCTION_DISCOVERY_VERIFIER_IMPLEMENTED
PENDING_INDEPENDENT_SOL_ACCEPTANCE

H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 19. Final response

On success return:

```text
starting_sha:
implementation_sha:
changed_files:

production_verifier_class:

fit_invalid_policy_hash:
seed_identity_policy_hash:

protocol_id_binding:
candidate_id_binding:
discovery_partition_id:
discovery_metric_ids:
fixed_cross_family_k:

seed_identity_tests:
verifier_tests:
focused_lifecycle:
execution_b04:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

new_valid_family_science_added: NO
frozen_hashes_unchanged:

real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
confirmation_accessed: NO
signed_network_requests: 0

implementation_report_path:
docs_commit_sha:

final_state:
H40_P3B_PRODUCTION_DISCOVERY_VERIFIER_IMPLEMENTED
PENDING_INDEPENDENT_SOL_ACCEPTANCE
H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex GPT-6 Sol High
```
