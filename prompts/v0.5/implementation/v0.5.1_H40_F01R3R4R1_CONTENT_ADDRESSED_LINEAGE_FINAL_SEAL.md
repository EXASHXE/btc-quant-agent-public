# Agent Prompt — V0.5.1 H40 F01R3R4R1 Content-Addressed Lineage Final Seal

## Status

\`\`\`text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = MINIMAL_FINAL_SEAL
\`\`\`

Repository:

\`\`\`text
EXASHXE/btc-quant-agent
\`\`\`

Implementation branch:

\`\`\`text
v0.5-refactor
\`\`\`

Required exact starting HEAD:

\`\`\`text
6c11ce944e5a7b9629d25cf3ba699f0d62a3a65a
\`\`\`

Before editing:

\`\`\`bash
git fetch origin
git checkout v0.5-refactor
git rev-parse HEAD
git rev-parse origin/v0.5-refactor
\`\`\`

Both must equal the required SHA.

If not, STOP and report the mismatch.

---

# 1. Mission

Close exactly one remaining implementation gap from:

\`\`\`text
reviews/v0.5/V0.5.1_H40_F01R3R4_SOL_INDEPENDENT_REVIEW.md
commit 86369b4e92f45d0e7792b37769f110e729d59c37
\`\`\`

Finding:

\`\`\`text
COMMITTED_LINEAGE_MAY_FALL_BACK_TO_NON_AUTHORITATIVE_LEGACY_MIRROR
\`\`\`

RG-H01-01 is already closed.

The substantive RG-B02-01 corrupted-link defect is already closed.

This task is only the final authority seal for committed-lineage artifact selection.

---

# 2. Exact required correction

Current lineage code conceptually does:

\`\`\`python
receipt_path = self._receipts_dir(run_authority_id) / f"{curr_hash}.json"
if not receipt_path.is_file():
    receipt_path = self._find_receipt_path(run_authority_id, curr_hash)
\`\`\`

Remove the fallback from authoritative committed-lineage traversal.

For \`_get_committed_lineage()\`:

\`\`\`text
ONLY
<run>/receipts/<expected_hash>.json
\`\`\`

may establish lineage membership.

If that exact authoritative content-addressed file is missing, unreadable, malformed, or not a regular file:

\`\`\`text
fail closed
\`\`\`

Do not search:

\`\`\`text
legacy NN_name.json files
run-directory mirrors
other filenames with matching receipt_sha256
\`\`\`

for committed-lineage authority.

---

# 3. Preserve compatibility outside lineage authority

Do NOT broadly delete compatibility behavior.

\`_find_receipt_path()\` may remain unchanged for non-authoritative transition-key lookup/compatibility paths where already used.

Legacy mirror creation may remain unchanged.

The only required rule is:

\`\`\`text
legacy mirrors cannot establish committed lineage membership
\`\`\`

---

# 4. Mandatory tests

Add focused tests to the existing R3R4 test file or a minimal R3R4R1 file.

At minimum:

## R63 — missing authoritative head receipt

\`\`\`text
persist Discovery using a legacy transition key so both:
- receipts/<hash>.json
- NN_name.json

exist

delete ONLY receipts/<hash>.json
keep legacy NN_name.json intact

restore_authorization(...)
=> fail closed
=> must NOT fall back to legacy mirror
\`\`\`

## R64 — missing authoritative non-head receipt

Build at least:

\`\`\`text
Discovery -> Candidate -> WF
\`\`\`

Delete the content-addressed Candidate receipt but leave its legacy mirror.

Attempt to restore head or legitimate ancestor.

Expected:

\`\`\`text
fail closed during full committed-lineage verification
\`\`\`

## R65 — intact authoritative receipts still restore

Control:

\`\`\`text
with content-addressed chain intact
head and legitimate ancestor restoration still succeeds
\`\`\`

Keep R01-R62 green.

---

# 5. Do not reopen anything else

Do not modify:

\`\`\`text
receipt durability ordering
SQLite head semantics
transition adjacency
Model-A source truth
B01
B03
B04
M01
Track B
scientific contracts
canonical lifecycle contracts
P3
F02
Confirmation
H39
Final Holdout
v0.6
\`\`\`

No canonical hash changes.

No refactor unrelated to this exact path.

---

# 6. Validation

Run at minimum:

\`\`\`bash
pytest -q \
  tests/test_v051_h40_f01_lifecycle_authority.py \
  tests/test_v051_h40_f01r3r1_durable_authority.py \
  tests/test_v051_h40_f01r3r2_commit_authority.py \
  tests/test_v051_h40_f01r3r3_api_seal.py \
  tests/test_v051_h40_f01r3r4_lineage_durability.py
\`\`\`

plus:

\`\`\`bash
pytest -q
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
\`\`\`

No real H40 outcomes.

No signed Binance/network access.

No CI trigger/rerun/poll/wait.

Normal push is allowed.

---

# 7. Git discipline

Work directly on:

\`\`\`text
v0.5-refactor
\`\`\`

from exact SHA:

\`\`\`text
6c11ce944e5a7b9629d25cf3ba699f0d62a3a65a
\`\`\`

Suggested commit:

\`\`\`text
fix(h40): require content-addressed lineage receipts
\`\`\`

Push normally.

Do not modify docs branch.

---

# 8. Allowed completion claim

If all local gates pass:

\`\`\`text
H40_F01R3R4R1_CONTENT_ADDRESSED_LINEAGE_SEAL_COMPLETE_PENDING_REVIEW

RG_H01_01_CLOSED
RG_B02_01_IMPLEMENTED
LEGACY_MIRROR_LINEAGE_AUTHORITY_REMOVED

NO_CANONICAL_HASH_CHANGE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
\`\`\`

Do not claim final F01 acceptance yourself.

---

# 9. Final response

Return:

\`\`\`text
starting_sha:
final_commit_sha:
changed_files:

content_addressed_lineage_only:
legacy_fallback_removed:
R63:
R64:
R65:

targeted_pytest:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

real_h40_outcomes_accessed: NO
signed_network_requests: 0
ci_triggered_or_polled: NO

frozen_hashes_unchanged: YES
projection_168_18_150_unchanged: YES

final_state:
H40_F01R3R4R1_CONTENT_ADDRESSED_LINEAGE_SEAL_COMPLETE_PENDING_REVIEW
\`\`\`

Recommended executor:

\`\`\`text
Codex GPT-5.6 Sol Medium/High
\`\`\`

This is a tiny authority-boundary correction. Do not expand scope.
