# Agent Prompt — V0.5.1 H40 Lifecycle Implementation Authority V1 Publication

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = ACCEPTED_HASH_PUBLICATION_ONLY
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required exact starting HEAD:

```text
d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa
```

Independent authority acceptance:

```text
reviews/v0.5/V0.5.1_H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1_SOL_ACCEPTANCE.md
commit e94f520ef6ed6dca17c5330b324d3b9d1563da58
```

Accepted implementation-authority hash:

```text
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

Accepted tested implementation baseline remains:

```text
d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa
```

This task does NOT authorize P3 or Discovery.

---

## 1. Preflight

Run:

```bash
git fetch origin --prune
git checkout v0.5-refactor
git rev-parse HEAD
git rev-parse origin/v0.5-refactor
git status --porcelain --untracked-files=no
```

Required:

```text
HEAD =
origin/v0.5-refactor =
d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa

tracked worktree = clean
```

Untracked local tooling may remain.

If mismatch: STOP.

---

## 2. Exact production change

In:

```text
src/btc_quant_agent/h40/lifecycle_authority.py
```

change only the publication boundary:

```python
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH: str | None = None
```

to the accepted exact hash:

```python
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH: str | None = (
    "6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5"
)
```

You may update the immediately adjacent comment to state that the independently accepted V1 authority is now published.

Do NOT modify:
- lifecycle semantic contracts;
- lifecycle governance hash;
- scientific hashes;
- source/split authority;
- execution policy;
- P3 logic;
- F02;
- Confirmation;
- H39;
- Final Holdout.

---

## 3. Mandatory publication regression tests

Add a narrowly scoped test file, suggested:

```text
tests/test_v051_h40_implementation_authority_publication.py
```

At minimum prove:

### P01 exact accepted constant

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
==
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

### P02 exact typed authority reconstructs accepted hash

Construct:

```text
H40RequiredTestCIEvidenceIdentity(
  evidence_manifest_artifact_path =
    evidence/v0.5/h40/V0.5.1_H40_F01_POST_SANITIZATION_AGGREGATE_IMPLEMENTATION_EVIDENCE.json

  evidence_manifest_sha256 =
    8b839f6b8ddbe433be6df069928f28f0c07a6a3b966c159af26d04b561e7da24

  tested_commit_sha =
    d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa
)
```

and:

```text
H40LifecycleImplementationAuthority(
  accepted_lifecycle_governance_authority_hash =
    7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

  f01_implementation_acceptance_artifact_path =
    reviews/v0.5/V0.5.1_POST_SANITIZATION_SOL_INDEPENDENT_ACCEPTANCE.md

  f01_implementation_acceptance_commit_sha =
    5deb937bcb84c18165de8f1938d0cac65300f1e4

  f01_implementation_commit_sha =
    d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa
)
```

Require:

```text
authority.lifecycle_implementation_authority_hash
==
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
```

### P03 production service accepts exact authority

Require:

```python
H40LifecycleAuthorityService.production(
    implementation_authority=exact_authority
)
```

constructs successfully.

Do not issue Discovery.

Do not touch real source/outcome data.

### P04 altered authority fails closed

Construct a validly typed but different authority identity where possible, or use an implementation authority object whose canonical hash differs from the accepted constant.

Require production construction fails with implementation authority object/hash mismatch.

Do not weaken typed validation to create this test.

### P05 governance mismatch fails closed

Verify an authority object with a foreign governance hash cannot instantiate as accepted authority.

### P06 tested commit mismatch fails closed

Verify:

```text
required_test_ci_evidence_identity.tested_commit_sha
!=
f01_implementation_commit_sha
```

is rejected.

### P07 synthetic verifier remains fenced

The accepted publication must not make a synthetic-only verifier valid in production.

Preserve existing production fail-closed behavior.

### P08 F02 remains sealed

No publication change may authorize:

```text
H40_CONFIRMATION_READY
->
H40_CONFIRMATION_EVALUATED_ONCE
```

### P09 execution remains disabled

Assert the existing execution policy remains:

```text
RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

No signed network capability.

### P10 scientific/lifecycle identities unchanged

Require exact:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

---

## 4. Important semantic rule

Do NOT change the implementation-authority candidate to point at this new publication commit.

The accepted authority remains bound to:

```text
f01_implementation_commit_sha =
d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa
```

This publication commit is a later gate-publication step.

Do not create a circular self-binding where the authority must contain the commit that publishes its own accepted hash.

---

## 5. Existing tests

Some existing tests may assert:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH is None
```

Mechanically update only assertions whose sole purpose was to prove the pre-acceptance gate remained closed.

Replace them with exact accepted-hash/publication semantics.

Do NOT weaken unrelated fail-closed assertions.

Do NOT rewrite old historical review/evidence artifacts.

---

## 6. Local validation

Run at minimum:

```bash
pytest -q tests/test_v051_h40_implementation_authority_publication.py

pytest -q   tests/test_v051_h40_f01_lifecycle_authority.py   tests/test_v051_h40_f01r3r1_durable_authority.py   tests/test_v051_h40_f01r3r2_commit_authority.py   tests/test_v051_h40_f01r3r3_api_seal.py   tests/test_v051_h40_f01r3r4_lineage_durability.py

pytest -q tests/test_execution.py tests/test_execution_environment_authority.py

pytest -q
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
```

All must PASS.

No real H40 outcome-bearing data.

No signed Binance requests.

---

## 7. Git commit

Allowed production/test files should remain narrowly scoped to:

```text
src/btc_quant_agent/h40/lifecycle_authority.py
tests/test_v051_h40_implementation_authority_publication.py
plus only mechanically necessary existing lifecycle tests that asserted None
```

Suggested commit message:

```text
feat(h40): publish accepted lifecycle implementation authority
```

Push to:

```text
origin/v0.5-refactor
```

Record exact:

```text
AUTHORITY_PUBLICATION_COMMIT_SHA
```

---

## 8. Public GitHub-hosted CI

After push, use the normal public-repository CI.

If push CI automatically runs and includes the required quality suite, inspect that run.

If compatibility 3.11/3.12 are only available through workflow_dispatch under the current workflow, trigger exactly ONE workflow_dispatch against the publication commit after push CI stabilizes.

Do not rerun failures automatically.

Required final evidence before completion:

```text
publication commit exact HEAD
Python 3.13 quality = success
Python 3.11 compatibility = success
Python 3.12 compatibility = success
```

If CI fails, classify and STOP.

Do not modify workflow in this task.

---

## 9. Publication evidence report

After local gates and public CI succeed, create on v0.5-refactor-docs:

```text
reviews/v0.5/V0.5.1_H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_PUBLICATION_REPORT.md
```

Record:

- authority acceptance artifact/commit:
  `e94f520ef6ed6dca17c5330b324d3b9d1563da58`
- accepted authority hash;
- implementation baseline `d0e1fe0...`;
- publication commit SHA;
- exact changed files;
- P01-P10 results;
- focused lifecycle result;
- B04 result;
- full pytest;
- ruff/mypy/compileall/diff;
- public CI run ID(s);
- py3.11/3.12/3.13 results;
- protected-surface attestations.

Allowed conclusion:

```text
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH_PUBLISHED
PUBLICATION_REGRESSION_PASS
PENDING_SOL_PUBLICATION_ACCEPTANCE

H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

## 10. Forbidden scope

Do not:
- implement P3 statistics;
- access Discovery outcome data;
- unlock WF validation data;
- implement F02;
- access Confirmation;
- access H39;
- access Final Holdout;
- enable signed execution;
- modify scientific authority;
- change candidate-selection science;
- refactor unrelated code.

---

## 11. Final response

Return:

```text
starting_sha:
publication_commit_sha:
changed_files:

accepted_authority_hash:
constant_published:
typed_authority_hash_match:
production_exact_authority_accepts:
foreign_authority_rejected:
governance_mismatch_rejected:
tested_commit_mismatch_rejected:
synthetic_production_fence_preserved:
f02_sealed:
execution_disabled:

publication_test:
focused_lifecycle:
execution_b04:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

ci_run_ids:
quality_3_13:
compatibility_3_11:
compatibility_3_12:

publication_report_path:
docs_commit_sha:

real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
signed_network_requests: 0

final_state:
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH_PUBLISHED
PENDING_SOL_PUBLICATION_ACCEPTANCE
H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex GPT-5.6 Sol High
```

This is a small code change, but it opens the production lifecycle authority gate, so use the stronger implementation model and keep scope exact.
