# Agent Prompt — V0.5.1 H40 Pytest Runtime Optimization Without Governance/Science Changes

PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = TEST_RUNTIME_OPTIMIZATION_ONLY

Repository: EXASHXE/btc-quant-agent-public
Implementation branch: v0.5-refactor
Required starting HEAD: 6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d

Controller review:
reviews/v0.5/V0.5.1_MICROSTRUCTURE_MAINTENANCE_CONTROLLER_REVIEW.md
docs commit = 6a46d52366209f28f308257b9c1e45deb7f552c2

Current blocker: FULL_SUITE_RUNTIME_NOT_REDUCED

This task does NOT authorize H40 P3 or Discovery.

## 1. Mission

Reduce repository-wide pytest wall time, especially repeated H40 lifecycle-authority setup/reconstruction cost, without changing H40 scientific semantics, lifecycle/governance semantics, frozen identities, protected-surface behavior, fail-closed cold validation, assertions, or adversarial coverage.

## 2. Critical governance restriction

Default rule: TEST FILES ONLY.

Do not modify src/btc_quant_agent/h40/* in this task.

If profiling proves meaningful improvement is impossible without changing H40 production implementation, STOP with:
H40_TEST_RUNTIME_OPTIMIZATION_REQUIRES_IMPLEMENTATION_AUTHORITY_CHANGE

Expected allowed files are H40 tests, tests/conftest.py, private test helpers, and optionally pyproject.toml for test-runner configuration.

## 3. Baseline and profiling

Before edits, run serial full pytest twice with:
/usr/bin/time -v .venv/bin/python -m pytest -q --durations=50 --durations-min=0.25

Record both wall times, CPU, max RSS, test count, and top 50 durations.

Time at least these tests individually:
- test_v051_h40_f01_lifecycle_authority.py::test_a21_through_a29_model_a_capability_lifetime
- test_v051_h40_f01r3r1_durable_authority.py::test_r03_production_wf_cold_restore_avoids_caller_override_path
- test_v051_h40_p3b_production_verifier_identity_seal.py::test_a09_foreign_verifier_cannot_cold_restore_candidate_lock
- test_v051_h40_f01r3r1_durable_authority.py::test_r02_production_wf_revalidation_avoids_caller_override_path
- test_v051_h40_f01r3r1_durable_authority.py::test_r04_persisted_split_mismatch_fails_closed

Profile at least the two slowest with standard-library cProfile and print the top 40 cumulative functions.

Inspect measured cost in:
generate_hourly_range
_typed_production_authority_fixture
materialize_runtime_source_split_authority
verify_runtime_source_split_authority
H40RuntimeSnapshotSeal.from_verified_authority
H40SplitManifest.build_runtime_authoritative
_compute_partitions_from_timestamps
_build_discovery_chain
_make_production_chain
restore_authorization
revalidate_authorization

Do not optimize from source guesses alone.

## 4. First-choice optimization: immutable TEST template caching

The deterministic governance timestamp population (roughly 2021-01 through 2026-02 hourly), membership hash, canonical validation receipt, canonical source manifest inputs, and other immutable setup may be constructed once and reused when those objects are not themselves the behavior under test.

Create an explicit test-only immutable template/helper if useful.

Every test must still get fresh cold_state, monkeypatch bindings, tmp_path, artifact stores, service mutable state, and any mutable resolver/verifier state.

Do not share mutable dict/list/store/service state between tests.

## 5. Separate fast setup from full cold reconstruction

Tests about persistence, stale-head behavior, wrong-run rejection, API sealing, or foreign verifier rejection do not all need to re-prove full source/split materialization during setup.

Where semantically valid, use a cached immutable authority template for setup while retaining the actual production transition/revalidation call that the test asserts.

Tests explicitly about cold reconstruction, source disappearance, source mutation, runtime split mutation, attestation mutation, or production restore revalidation MUST continue to execute the real cold reconstruction path.

Suggested explicit helper names:
build_full_cold_production_chain
build_cached_template_production_chain
install_fresh_cold_source_hooks

Do not call a cached path full/cold if it skips reconstruction.

## 6. Forbidden optimization

Never cache a cold-validation result across lifecycle authority consumption.

Source disappearance, source mutation, timestamp membership change, split change, and attestation change must still be detected on the next lifecycle operation.

Do not cache or bypass validate_source_artifact, extract_verified_source_timestamps, or verify_runtime_source_split_authority across transitions in tests that assert these properties.

## 7. Preserve full end-to-end paths

Keep explicit non-short-circuited tests for:
1. production seal construction from verified source/split authority
2. production candidate-lock transition
3. production WF revalidation
4. production cold restore
5. source disappearance failure
6. source mutation failure
7. split/attestation mismatch failure

If refactoring moves other tests to a fast template, these full-path tests must remain real.

## 8. Fixture equivalence tests

Prove cached/template setup matches full construction for immutable identities including:
source manifest hash
split manifest hash
split attestation hash
runtime authority snapshot hash
sealed registered roster hash
registered/not-testable counts
run authority ID for identical inputs
Discovery receipt identity where inputs are identical

Also prove cold_state and mutable test state are isolated between tests.

## 9. Optional parallel developer mode

Only after serial fixture optimization, if serial full-suite time still exceeds 8 minutes, evaluate pytest-xdist as an OPTIONAL developer mode.

Requirements:
- serial pytest -q remains authoritative and passes
- test 2 and 4 workers, not unbounded auto by default
- identical pass count
- run parallel mode twice for flakiness
- measure aggregate memory
- do not starve background collectors

Do not redefine the serial acceptance gate away.

## 10. Targets

Primary serial target: median <= 480 seconds.
Preferred serial target: median <= 360 seconds.
Minimum required: >=25% reduction versus measured starting serial median.
Also require >=40% reduction in combined wall time of the top 5 H40 tests.

If test-only optimization cannot reach 25% without weakening full cold-validation coverage, STOP with:
H40_TEST_RUNTIME_OPTIMIZATION_TEST_ONLY_LIMIT_REACHED

Do not change production H40 implementation in this task.

## 11. Quality gates

Run the focused H40 lifecycle/durable/commit/api/lineage/verifier suites with --durations=30.
Run full .venv/bin/python -m pytest -q --durations=50 --durations-min=0.25.
Run ruff check ., mypy, python -m compileall -q src skill-template/scripts, and git diff --check.

Verify frozen identities remain exact and unchanged.
Do not trigger CI unless explicitly requested.

## 12. Protected surfaces

No real H40 outcomes, Discovery execution, WF outcomes, Confirmation, H39 outcomes, Final Holdout, signed network requests, or execution enablement.

Live background data collection may remain running.

## 13. Commit/report

Suggested commit: perf(tests): reduce repeated H40 authority fixture cost
Push to origin/v0.5-refactor.

Create:
reviews/v0.5/V0.5.1_H40_PYTEST_RUNTIME_OPTIMIZATION_REPORT.md

Report starting SHA, repair SHA, changed files, two-run baseline timings, top-5 before/after, cProfile hotspots, optimization mechanism, cached-template/full-cold-path separation, fixture equivalence, retained full-path coverage, post-change serial timings, optional parallel timings, max RSS, full test count, 30 slowest tests, frozen identities, and protected surfaces.

Allowed success:
H40_TEST_RUNTIME_OPTIMIZED
FULL_PYTEST_PERFORMANCE_ACCEPTED
PENDING_CONTROLLER_REVIEW
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

Recommended executor: Codex Sol High