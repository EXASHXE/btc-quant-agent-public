# V0.5.1 H40 — Pre-P3 Controller Readiness Closure and Exact-SHA Revalidation

## Executor

**Recommended executor: Gemini 3.8 Flash**

This is one concentrated implementation + exact-SHA validation stage.

It closes the two Controller-triaged pre-P3 items and restores a clean quality baseline before any
P3 Controller Authority can exist.

Do not publish P3 Controller Authority.  
Do not run real Discovery.  
Do not access protected outcomes.  
Do not publish a new implementation authority yourself.

---

# 1. Exact starting state

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required implementation start SHA:

```text
c31586c80130693c118c150a4e06c9573ff0053d
```

Docs authority branch:

```text
v0.5-refactor-docs
```

Controller triage artifact:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_DEEP_AUDIT_CONTROLLER_TRIAGE.md
```

Controller triage commit:

```text
97480d4fa71588a10ea8d2f9276e989bb054f3b0
```

Deep-audit artifact:

```text
reviews/v0.5/V0.5.1_H40_ASTRA_PRE_DISCOVERY_F01_F04_DEEP_AUDIT.md
```

Deep-audit commit:

```text
00d8e339d51d3da66dcfbf1e39c4d62293c3a37c
```

Before changing anything:

```bash
git fetch origin
git checkout v0.5-refactor
git reset --hard c31586c80130693c118c150a4e06c9573ff0053d
git status --short
git rev-parse HEAD
git ls-remote origin refs/heads/v0.5-refactor
```

Stop if local/remote branch drift exists.

---

# 2. Current authority state and mandatory temporary revocation

Current published implementation authority:

```text
a23ceec50ff5703f4dff703a7b44e715501164a48ec9be48ae096f8bd4d51fca
```

It is bound to the previously accepted implementation, not to the new repaired SHA this task will
create.

Therefore, **the same implementation commit that changes production code MUST revoke the old
implementation authority**.

Change:

```python
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH: str | None = (
    "a23ceec50ff5703f4dff703a7b44e715501164a48ec9be48ae096f8bd4d51fca"
)
```

back to:

```python
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH: str | None = None
```

and keep:

```python
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH: str | None = None
```

Update the adjacent comment to reflect:

```text
implementation authority temporarily revoked pending exact-SHA reacceptance;
P3 controller remains unpublished
```

This is mandatory fail-closed staging.

At the end of this executor task:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

The independent Controller will later republish a new implementation authority bound to the new
exact SHA and new evidence.

---

# 3. Controller adjudication to implement

## MED-01

Status:

```text
CONFIRMED_MEDIUM
MUST_CLOSE_BEFORE_P3_CONTROLLER_PUBLICATION
```

Frozen R2-F02 invariant:

```text
Verifier re-extracts economic rows directly from sealed source bytes verified against file SHA-256.
```

Current Parquet behavior is structurally non-conforming because it:

```text
read_bytes -> hash raw_bytes -> pq.read_table(file_path)
```

and therefore reopens the path.

### Required exact repair

In:

```text
src/btc_quant_agent/h40/discovery_evidence.py
```

the Parquet branch of `_extract_economic_rows(...)` must parse the already-supplied
`raw_bytes`, not reopen `file_path`.

Use an in-memory Arrow/Parquet reader, e.g.:

```python
import pyarrow as pa
import pyarrow.parquet as pq

table = pq.read_table(pa.BufferReader(raw_bytes))
```

Equivalent in-memory parsing is acceptable only if the bytes parsed are exactly `raw_bytes`.

After the repair:

```text
source_file_sha256 authority bytes
==
bytes consumed by Parquet parser
```

The path may still be used to select the adapter from its suffix, but it must not be reopened to
obtain economic content after `raw_bytes` have been captured and hashed.

Do not change:

```text
support interval
canonical float representation
timestamp policy
OHLC geometry
economic_rows_sha256
source schemas
provenance contract hash
scientific formulas
```

### Mandatory MED-01 regressions

Add adversarial tests proving at least:

1. capture valid Parquet `raw_bytes`;
2. mutate/replace/delete the source path after capture;
3. call economic extraction with the original `raw_bytes`;
4. extracted economic rows remain those from original `raw_bytes`;
5. no filesystem reopen controls the extracted values.

Also prove:

```text
whole-file SHA is still over the exact full source bytes
economic_rows_sha256 is still recomputed from canonical support rows
JSON behavior is unchanged
unsupported adapter still -> NOT_TESTABLE
```

No operational/external attack procedure is needed; repository-local temporary fixtures only.

---

# 4. MED-02 Controller ratification — lock, do not redesign

Controller has ratified:

```text
H40_WF1_LOOKBACK_REFERENCE_INTERPRETATION_A
```

This is now governance authority for First Discovery / WF1.

Exact meaning:

```text
reserve interval:
[2021-01-01T00:00:00Z, 2021-01-31T00:00:00Z)

status:
non-evaluating source-history support

allowed:
causally reconstructed reserve feature scalars may contribute to the expanding WF1_TRAIN
percentile reference if observation_time < decision_t and feature inputs are PIT-valid

forbidden:
reserve hours may not enter base-eligible membership, decision-row membership,
evaluation denominator, labels/outcomes, calibration-row membership,
validation-row membership, candidate counts, or candidate ranking population

N >= 60:
may count causally available reserve reference scalars

effect:
the first WF1_TRAIN row is not automatically NO_TRADE merely because fewer than 60
post-2021-01-31 hours have elapsed

scope:
initial WF1 expanding reference only

later WF refits:
unchanged R3R2 rule — use that fold's chronologically past training partition only
```

### Important

**Do not alter the current production MED-02 behavior.**

This stage only adds regression coverage that locks the Controller-ratified interpretation.

### Mandatory MED-02 regressions

Add explicit tests proving:

1. reserve timestamps cannot enter base-universe membership;
2. reserve feature scalars can be present in the WF1 expanding percentile reference;
3. only scalars with `dt < decision_t` are included;
4. strict `close_time < observation_time` remains required by underlying feature reconstruction;
5. `N >= 60` can be satisfied using causal reserve feature observations;
6. no reserve timestamp becomes a decision/evaluation row;
7. future/equal-time observations cannot affect earlier percentile state;
8. calibration behavior remains frozen-training-reference semantics;
9. this test does not redefine later WF refits.

No threshold/formula/identity change is authorized.

---

# 5. LOW-01 — clean the existing Ruff baseline in the same closure

Astra/Gemini audit observed:

```text
ruff check . = 10 diagnostics
```

These are pre-existing and not caused by publication, but the new exact-SHA baseline must be clean.

The reported affected test files include:

```text
tests/test_v0320_forward_data.py
tests/test_v0323_blind_validation.py
tests/test_v0324_blind_accumulation.py
```

Reported rule families:

```text
FURB162
I001
RUF059
RUF100
```

Run `ruff check .` first and record the exact current diagnostics.

Fix **only** the current Ruff diagnostics required to make:

```bash
ruff check .
```

exit 0.

Rules:

- test-only lint cleanup;
- no behavior change;
- no unrelated formatting/refactor;
- do not add broad `noqa`;
- do not weaken lint configuration;
- if `ruff --fix` changes unrelated files, revert unrelated changes and apply only the necessary
  minimal edits.

---

# 6. Stage-state test migration for authority revocation

Because the implementation authority is temporarily revoked, update tests that currently assert the
published `a23ceec...` global stage state.

First enumerate:

```bash
rg -n   "a23ceec50ff5703f4dff703a7b44e715501164a48ec9be48ae096f8bd4d51fca|ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH"   src tests
```

Classify every occurrence.

### Current-global-stage assertions

Tests whose purpose is to assert the **current global accepted constant** must now expect:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH is None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH is None
```

### Historical publication evidence

Do **not** erase historical authority identity.

The prior published object/hash:

```text
a23ceec50ff5703f4dff703a7b44e715501164a48ec9be48ae096f8bd4d51fca
```

remains valid historical evidence for implementation:

```text
7413c96301067c59b9663d8eeaeddf975facb976
```

but is no longer the current accepted production authority after this repair commit.

Tests may retain historical reconstruction fixtures if clearly labeled historical.

### Mandatory fail-closed test

Prove:

```text
after repair-stage revocation:
production implementation authority = None
P3 controller authority = None
production Discovery authorization remains NOT_TESTABLE
old a23ceec... typed authority cannot become current merely because its historical object is valid
```

---

# 7. Frozen authority identities

The repair itself must preserve:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

discovery_provenance_contract =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

lifecycle semantic root V5 =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle governance authority V5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf
```

All 9 Lifecycle V5 child hashes remain exact.

MED-01 is implementation conformance to frozen F02.  
MED-02 is a Controller clarification locking existing behavior.

Therefore:

```text
protocol identity change = NO
scientific semantic root change = NO
Discovery provenance contract change = NO
Lifecycle V5 semantic change = NO
```

If your implementation appears to require changing any of those frozen identities, STOP and report
a blocker.

---

# 8. Repair A/B preservation

Re-run and preserve the accepted invariants:

## F01

```text
implementation authority alone cannot authorize Discovery
controller authority remains separately required
run grant separately required
receipt V3 required
persistence V2 required
historical V2 receipt rejected
historical V1 persistence rejected
```

## F02

```text
exact source bytes -> parser -> economic values
whole-file SHA authority
economic digest authority
active-source binding
receipt binding
support projection
lookback reserve non-evaluating
unsupported adapter fail-closed
```

## F03

```text
strict close_time < decision_t
D1/D2/D3
R_VOL
O_RANGE
percentile semantics
MED-02 Interpretation A
cache identity
r_h/r_net reconstruction
```

## F04

```text
stale service fails closed
current anchors revalidated
cold restore revalidates
synthetic/test authority isolated
```

---

# 9. Implementation commit

After code/test/lint changes and focused validation are green, commit to:

```text
v0.5-refactor
```

Suggested commit message:

```text
fix(h40): close pre-P3 source-byte and reference authority gaps
```

Push and verify exact remote SHA.

Call the resulting commit:

```text
NEW_IMPLEMENTATION_SHA
```

All later validation/evidence in this task must be tied to exactly that SHA.

Do not amend after evidence begins.

---

# 10. Exact-SHA validation phase

After the implementation commit is frozen and pushed, validate from a clean checkout/worktree of:

```text
NEW_IMPLEMENTATION_SHA
```

Record import provenance.

## Static gates

Required:

```bash
git diff --check
python -m compileall -q src
ruff check .
mypy src
```

All must exit 0.

This stage is not successful with baseline Ruff failures remaining.

## Focused tests

At minimum run:

```text
tests/test_v051_h40_repair_b_provenance_pit.py
tests/test_v051_h40_p3b_production_discovery_verifier.py
tests/test_v051_h40_p3b_production_verifier_behavior_seal.py
tests/test_v051_h40_p3b_production_verifier_identity_seal.py
tests/test_v051_h40_repair_a_lifecycle_authority.py
tests/test_v051_h40_f01_lifecycle_authority.py
tests/test_v051_h40_f01r3r1_durable_authority.py
tests/test_v051_h40_f01r3r2_commit_authority.py
tests/test_v051_h40_f01r3r3_api_seal.py
tests/test_v051_h40_f01r3r4_lineage_durability.py
tests/test_v051_h40_implementation_authority_publication.py
```

Include the new MED-01 and MED-02 regressions.

## Full suite

Run two separate full-suite executions:

```bash
/usr/bin/time -v pytest -q --durations=50 --durations-min=0.25
/usr/bin/time -v pytest -q --durations=50 --durations-min=0.25
```

Both must exit 0.

Do not average away one failure.

## Deterministic identity replay

Independently recompute twice:

```text
protocol authority
scientific semantic root
structural ledger
Discovery provenance contract
Lifecycle semantic root V5
Lifecycle governance V5
all 9 Lifecycle V5 child hashes
production search-space materialization
representative source economic digest
representative MED-02 percentile reconstruction
```

Expected authority stage:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

---

# 11. Exact-SHA evidence package

Use a separate docs worktree/checkout. Do not dirty the frozen implementation worktree.

Write raw logs under:

```text
evidence/v0.5/h40/pre_p3_closure_<NEW_SHA_SHORT>/
```

Write manifest:

```text
evidence/v0.5/h40/V0.5.1_H40_PRE_P3_CLOSURE_EXACT_SHA_EVIDENCE_<NEW_SHA_SHORT>.json
```

The manifest must include at minimum:

```text
schema_id
generated_at_utc
tested_commit_sha
implementation_remote_branch_sha
controller_triage_commit
deep_audit_commit
changed_files
med_01_closure
med_02_ratification_regression
ruff_cleanup
import_provenance
static_gate_results
focused_results
full_pytest_runs
identity_replay
determinism_replay
authority_stage_state
protected_surface_attestation
raw_log_sha256
```

Exact:

```text
tested_commit_sha = NEW_IMPLEMENTATION_SHA
implementation_remote_branch_sha = NEW_IMPLEMENTATION_SHA
```

For every persisted raw log:

1. re-read bytes from disk;
2. compute SHA-256;
3. store digest in manifest.

Do not self-hash the manifest inside itself.

After writing the final manifest, compute and report:

```text
EVIDENCE_MANIFEST_SHA256 =
SHA256(exact committed UTF-8 file bytes)
```

---

# 12. Protected surfaces

Throughout the task:

```text
real_h40_outcomes_accessed = false
wf1_calibration_outcomes_accessed_for_authoring_or_selection = false
confirmation_accessed = false
h39_accessed = false
final_holdout_accessed = false
signed_network_requests = 0
```

Synthetic/local fixtures are allowed.

No real Discovery run.

---

# 13. Docs evidence commit

Commit only evidence/log artifacts to:

```text
v0.5-refactor-docs
```

Suggested message:

```text
evidence(h40): validate pre-P3 closure exact implementation sha
```

Do not modify the Controller triage artifact.

Push and verify docs remote SHA.

---

# 14. BLOCK conditions

Block instead of improvising if:

```text
implementation start SHA drift
MED-01 requires provenance/scientific contract change
MED-02 current behavior differs from ratified Interpretation A
frozen identity mismatch
new Ruff failures cannot be isolated safely
focused test failure
full-suite failure
nondeterministic replay
implementation authority remains non-None after repair commit
P3 controller authority becomes non-None
protected data required
real Discovery required
evidence/log hash mismatch
```

Formal block state:

```text
PRE_P3_CONTROLLER_READINESS_CLOSURE_BLOCKED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

# 15. Successful end state

```text
PRE_P3_CONTROLLER_READINESS_CLOSURE_IMPLEMENTED_AND_VALIDATED
PENDING_CONTROLLER_EXACT_SHA_ACCEPTANCE

MED_01 = CLOSED
MED_02 = RATIFIED_INTERPRETATION_A_LOCKED_BY_REGRESSION
LOW_01_RUFF = CLOSED

STATIC_GATES = PASS
FOCUSED_TESTS = PASS
FULL_SUITE_RUN_1 = PASS
FULL_SUITE_RUN_2 = PASS
IDENTITY_REPLAY = PASS
DETERMINISM_REPLAY = PASS

ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1

NEXT_STAGE =
CONTROLLER_EXACT_SHA_ACCEPTANCE_AND_REPAIRED_IMPLEMENTATION_AUTHORITY_REPUBLICATION
```

---

# 16. Required final response

Return exactly enough information for Controller review:

```text
decision:

start_implementation_sha:
new_implementation_sha:
implementation_remote_sha:
implementation_changed_files:

med_01_status:
med_01_exact_change:
med_01_regressions:

med_02_status:
med_02_interpretation:
med_02_regressions:

ruff_before:
ruff_after:

static_gates:
focused_tests:
full_suite_run_1:
full_suite_run_2:
determinism_replay:

protocol_authority_hash:
scientific_semantic_root:
structural_ledger_hash:
discovery_provenance_contract_hash:
lifecycle_semantic_root_v5:
lifecycle_governance_authority_v5:
lifecycle_child_hashes_9_of_9:

accepted_lifecycle_implementation_authority_hash: None
accepted_h40_p3_controller_authority_hash: None
execution_policy: RESEARCH_DISABLED_V1

docs_evidence_commit_sha:
remote_docs_sha:
evidence_manifest_path:
evidence_manifest_sha256:

protected_surface_attestation:

formal_final_state:
next_stage:
CONTROLLER_EXACT_SHA_ACCEPTANCE_AND_REPAIRED_IMPLEMENTATION_AUTHORITY_REPUBLICATION
```
