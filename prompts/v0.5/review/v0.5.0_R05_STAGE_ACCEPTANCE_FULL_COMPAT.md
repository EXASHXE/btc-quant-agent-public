# v0.5.0 R05 — Exact-HEAD Stage Acceptance Full Compatibility Gate

## Mission

Run the one authorized manual full compatibility gate for the independently accepted R05 repair batch.

Code branch:

```text
v0.5-refactor
```

Exact accepted candidate SHA:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

Independent acceptance:

```text
R05A_ACCEPTED
R05B_ACCEPTED
R05C_CORE_ACCEPTED
R05CR_ACCEPTED
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
R05_ACCEPTANCE_CANDIDATE
```

Do not modify code before running this gate.

## Required preflight

1. Fetch `origin/v0.5-refactor`.
2. Assert exact remote HEAD equals:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

3. If HEAD differs, STOP and report:

```text
R05_FULL_COMPAT_ABORTED_HEAD_DRIFT
```

Do not run expensive compatibility CI on a different SHA.

## Full compatibility run

Trigger exactly one manual `workflow_dispatch` of the repository CI workflow on `v0.5-refactor` / exact accepted HEAD.

Required result:

```text
Python 3.11
- full pytest PASS
- compileall PASS

Python 3.12
- full pytest PASS
- compileall PASS

Python 3.13
- ruff PASS
- mypy PASS
- full pytest with coverage PASS
- compileall PASS
```

Expected marker:

```text
FULL_COMPAT_CI_GREEN_PY311_PY312_PY313
```

Do not rerun successful jobs unnecessarily. If the run fails, inspect the exact failing job/log and report the minimal failure set before changing code.

## No code changes on success

If all jobs are green:

- do not push code;
- do not edit CI configuration;
- do not begin H40;
- record the workflow run id, exact SHA, job conclusions, and final marker.

Final success state:

```text
R05_ACCEPTED
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
FULL_COMPAT_CI_GREEN_PY311_PY312_PY313
SIMPLIFIED_KERNEL_READY_FOR_RE_GATE
V0_5_NOT_FROZEN
H40_NOT_AUTHORIZED
```

The next step after this gate is a fresh simplified-kernel adversarial re-gate, not alpha research.
