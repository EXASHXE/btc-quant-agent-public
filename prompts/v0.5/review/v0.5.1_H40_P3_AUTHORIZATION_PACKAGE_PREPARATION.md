# Agent Prompt — V0.5.1 H40 P3 Authorization Package Preparation

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = P3_AUTHORIZATION_PREPARATION_ONLY
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Current implementation-authority publication commit:

```text
0131a9775912e868a4f66d0593e4ed425c699d9e
```

Accepted lifecycle implementation authority hash:

```text
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

Static publication review:

```text
reviews/v0.5/V0.5.1_H40_IMPLEMENTATION_AUTHORITY_PUBLICATION_STATIC_SOL_REVIEW.md
commit c6e6219169f99a48af6c73f96fb19656198daa11
```

Important:
publication local validation and remote CI review are intentionally deferred by user.

Therefore:

```text
H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

must remain true throughout this task.

---

## 1. Mission

Prepare the complete P3 authorization package so that, once the deferred publication validation is supplied, independent Sol can make the P3 authorization decision without another architecture/research-planning cycle.

This task is preparation only.

It may:
- inspect accepted contracts;
- reconstruct exact authority lineage;
- verify prerequisites that do not require real outcome-bearing data;
- prepare deterministic P3 run configuration;
- prepare P3 authorization checklist and candidate receipt preimage;
- prepare Discovery launch plan;
- prepare rollback/NO-GO conditions.

It must NOT:
- access real H40 labels/outcomes;
- execute Discovery;
- execute candidate selection;
- execute walk-forward;
- access Confirmation;
- access H39;
- access Final Holdout;
- issue signed exchange requests.

---

## 2. Reconstruct current frozen authority state

Verify and record exact current values from executable source:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

accepted_lifecycle_implementation_authority =
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

Verify search-space projection remains:

```text
TOTAL = 168
REGISTERED = 18
NOT_TESTABLE = 150
```

Verify active source union remains:

```json
["ETHUSDT_USD_M_1H"]
```

No post-outcome source/slot unlock is allowed.

---

## 3. Verify P3 prerequisite authority chain

Build a single prerequisite table containing:

- P1 source authority;
- P2 168-slot search-space authority;
- P3R0R1 lifecycle semantics;
- F01R2 source/split compatibility;
- F01R3 canonical semantics;
- F01R3/R4/R4R1 implementation closure;
- execution B04 under RESEARCH_DISABLED_V1;
- post-sanitation authority remap acceptance;
- lifecycle implementation authority V1 acceptance;
- accepted-hash publication commit;
- publication validation status.

For each prerequisite record:

```text
artifact path
acceptance commit
effective hash/identity
status
blocking_for_P3
```

Publication validation must explicitly remain:

```text
DEFERRED_BY_USER
blocking_for_final_P3_authorization = YES
```

Do not silently reinterpret static review as executed test evidence.

---

## 4. Prepare exact P3 runtime authority inputs

Without accessing real outcomes, reconstruct the runtime authority inputs needed immediately before first Discovery outcome-bearing read.

Prepare exact expected identities for:

- accepted protocol authority;
- accepted lifecycle governance authority;
- accepted lifecycle implementation authority;
- accepted source manifest;
- accepted split manifest;
- runtime source/split attestation;
- sealed registered roster;
- materialized run authority;
- execution disabled state.

Do not invent values that require real source materialization if not already available.

Classify each input:

```text
READY
REQUIRES_RUNTIME_MATERIALIZATION
BLOCKED
```

If runtime source/split attestation requires current local evidence, define exact deterministic commands/API path needed later, but do not open outcome-bearing partitions.

---

## 5. Prepare P3 authorization checklist

Create an exact checklist separating:

### A. Already accepted
- frozen scientific identities;
- source authority;
- search-space authority;
- lifecycle semantics;
- persistence/replay;
- implementation authority semantics;
- accepted implementation-authority hash;
- execution disabled.

### B. Deferred prerequisite
- publication local validation;
- publication CI review if still required by governance.

### C. Runtime materialization immediately before Discovery
- runtime source manifest verification;
- runtime split verification;
- runtime snapshot seal;
- registered roster seal;
- run authority materialization.

### D. Forbidden until P3 authorization
- real labels;
- forward returns;
- MFE/MAE;
- Precision;
- Net Expectancy;
- PF;
- ECE/Brier;
- bootstrap/HAC;
- candidate ranking;
- WF;
- Confirmation;
- H39;
- Final Holdout.

---

## 6. Prepare P3 authorization candidate artifact

Create:

```text
evidence/v0.5/h40/V0.5.1_H40_P3_AUTHORIZATION_CANDIDATE.json
```

This is a CANDIDATE only.

It should bind at minimum:

```text
schema_id
protocol_authority_hash
semantic_root_hash
structural_ledger_hash
lifecycle_governance_authority_hash
lifecycle_implementation_authority_hash
implementation_authority_publication_commit_sha
source_authority_projection
search_space_projection
execution_policy
publication_validation_status
protected_surface_attestation
authorization_status
```

Required:

```text
authorization_status =
PENDING_PUBLICATION_VALIDATION
```

Do NOT encode an authorized Discovery transition.

Do NOT create a real Discovery authorization receipt.

---

## 7. Prepare first-Discovery execution plan

Prepare the exact operational sequence that will run only AFTER final P3 authorization:

```text
1. verify accepted hashes
2. verify current source authority
3. reconstruct runtime source/split attestation
4. materialize sealed registered roster
5. materialize run authority
6. issue verified P3/Discovery authorization receipt
7. commit durable lifecycle head
8. only then permit first outcome-bearing Discovery read
9. run Discovery selection family
10. hard-gate candidate
11. either:
   H40_NO_GO
   or
   H40_CANDIDATE_LOCKED
```

This plan must explicitly preserve:
- deterministic run authority;
- no nonce;
- no runner-up;
- no post-outcome slot unlock;
- candidate selection ordering;
- separate max-stat correction;
- cross-family Holm;
- exact four WF folds later;
- Confirmation separate from WF.

---

## 8. Prepare immediate post-authorization research outputs

Define exactly what Discovery must produce, but do not compute it now.

At minimum specify schemas for:

- candidate result evidence;
- per-family corrected statistics;
- hard-gate outcomes;
- selected slot identity or NO_GO;
- selected structural configuration hash;
- adjusted LCB net expectancy;
- adjusted LCB precision;
- complexity tie-break;
- candidate-lock receipt input;
- failure/termination evidence.

Do not add new metrics not already accepted by H40 science.

---

## 9. Prepare fail-closed / NO-GO conditions

Explicitly enumerate the conditions that must stop P3/Discovery before outcome access, including:

- accepted hash mismatch;
- source authority drift;
- source manifest mismatch;
- split manifest mismatch;
- runtime snapshot mismatch;
- registered roster mismatch;
- implementation authority mismatch;
- publication validation still incomplete;
- execution policy not RESEARCH_DISABLED_V1;
- protected surface access attempt.

After Discovery begins, enumerate accepted NO_GO conditions without inventing new statistical thresholds.

---

## 10. Required artifacts

Create on v0.5-refactor-docs:

```text
reviews/v0.5/V0.5.1_H40_P3_AUTHORIZATION_READINESS_REPORT.md

evidence/v0.5/h40/V0.5.1_H40_P3_AUTHORIZATION_CANDIDATE.json

docs/h40/V0.5.1_H40_DISCOVERY_LAUNCH_RUNBOOK.md
```

Do not modify v0.5-refactor.

Do not run CI.

Do not run outcome-bearing research.

---

## 11. Allowed conclusion

Only if preparation is internally coherent:

```text
H40_P3_AUTHORIZATION_PACKAGE_READY

BLOCKING_PREREQUISITE =
PUBLICATION_VALIDATION_DEFERRED_BY_USER

H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

If any additional current blocker is found, classify it precisely.

Do not open a broad repair loop unless it meets the existing repair-stop rule.

---

## 12. Final response

Return:

```text
authority_chain_status:
publication_validation_status:

p3_candidate_path:
p3_readiness_report_path:
discovery_runbook_path:
docs_commit_sha:

runtime_inputs_ready:
runtime_inputs_requiring_materialization:
additional_blockers:

real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
signed_network_requests: 0

final_state:
H40_P3_AUTHORIZATION_PACKAGE_READY
H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex GPT-5.6 Sol Medium
```

This is deterministic authority/readiness preparation, not research execution.
