
# GPT-5.6 Sol High Prompt — H40 F01R2 Source/Split Runtime-Authority Compatibility Closure

## Mission

Close the single new authority blocker found after F01R1:

```text
F01R1-SOL-06
production runtime seal is unreachable under the accepted P1/P2 source-authority state
```

This is a **docs-first authority compatibility task**.

Do not implement code until the exact semantics are determined and documented.

Repository:

```text
EXASHXE/btc-quant-agent
```

Code branch:

```text
v0.5-refactor
```

Current code HEAD:

```text
3eec76fdbc451c6e8a17398b39866558abbd93a3
```

Docs branch:

```text
v0.5-refactor-docs
```

Read first:

```text
reviews/v0.5/V0.5.1_H40_F01R1_SOL_INDEPENDENT_REVIEW.md
reviews/v0.5/V0.5.1_H40_F01_SOL_INDEPENDENT_IMPLEMENTATION_REVIEW.md
reviews/v0.5/V0.5.1_H40_P3R0R1_LIFECYCLE_EVIDENCE_AUTHORITY_REPAIR.md
reviews/v0.5/V0.5.1_H40_P3R0R1_SOL_CUMULATIVE_ACCEPTANCE.md
reviews/v0.5/V0.5.1_H40_P2_FINAL_INDEPENDENT_ACCEPTANCE.md
reviews/v0.5/V0.5.1_H40_P1_FINAL_SOL_ACCEPTANCE.md
src/btc_quant_agent/h40/source_manifest.py
src/btc_quant_agent/h40/split_manifest.py
src/btc_quant_agent/h40/protocol_authority.py
src/btc_quant_agent/h40/search_space.py
src/btc_quant_agent/h40/lifecycle_authority.py
tests/test_v051_h40_p1_scaffold.py
tests/test_v051_h40_p2_search_space.py
tests/test_v051_h40_f01_lifecycle_authority.py
```

---

# 1. Frozen facts that must remain true

Current accepted P1 source authority:

```text
BTCUSDT_USD_M_1H                 NOT_TESTABLE
ETHUSDT_USD_M_1H                 VERIFIED
BTCUSDT_OFFICIAL_DERIVATIVES     NOT_TESTABLE
BTCUSDT_SPOT_FLOW                NOT_TESTABLE
ETHUSDT_DERIVATIVES_FLOW         NOT_TESTABLE
```

Current accepted P2 runtime projection:

```text
TOTAL = 168
REGISTERED = 18
NOT_TESTABLE = 150
```

Frozen scientific identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Accepted lifecycle root:

```text
lifecycle_semantic_root_hash =
d8c24b878b18426666ce46390e7363a0a2a5d7d3eb4d7636cc5cb2ecfaf4e9f5
```

No source may be silently promoted merely to make F01 constructible.

---

# 2. Exact incompatibility

Current F01R1 factory:

```text
H40RuntimeSnapshotSeal.from_verified_authority(...)
```

calls:

```text
H40SplitManifest.assert_authoritative(source_manifest, repo_root)
```

The P1 production split assertion requires canonical BTC and ETH source authority.

But P1 final acceptance explicitly preserves:

```text
BTCUSDT_USD_M_1H = NOT_TESTABLE
```

Therefore current accepted authority produces:

```text
from_verified_authority
-> assert_authoritative
-> assert_canonical_source_record(BTC)
-> SOURCE_UNVERIFIED
```

Meanwhile the accepted 18 REGISTERED slots exist precisely because the runtime source projection permits only the subset whose required sources are VERIFIED.

The current positive F01R1 production-factory test monkeypatches split authority and therefore does not resolve this contradiction.

---

# 3. Required authority analysis

Determine which of the following is correct from the accepted artifacts and code.

## Option A — existing lifecycle semantics already permit a runtime-scoped split/source binding

If the accepted P3R0R1 runtime seal contract can be implemented without changing its nine canonical child contracts, define the exact interpretation.

It must answer:

1. What does `source_manifest_hash` identify?
2. What does `split_manifest_hash` identify?
3. What exactly does `split_attestation_hash` attest when some canonical sources are NOT_TESTABLE?
4. Which sources require cold artifact verification before Discovery?
5. How is the required source set derived from the complete sealed REGISTERED roster?
6. How are NOT_TESTABLE source identities checked without falsely upgrading them?
7. How is the static split calendar verified independently from source availability?
8. How do later WF receipts bind the same split identity?
9. Can the current P1 split-attestation type represent this truthfully?
10. If not, what accepted existing authority object owns the needed identity?

If this interpretation is fully derivable from existing accepted semantics, publish a precise **implementation clarification** and prove the existing:

```text
lifecycle_semantic_root_hash
lifecycle_governance_authority_hash
```

remain unchanged.

## Option B — existing accepted semantics are insufficient

If no truthful existing object can provide the required `split_attestation_hash` for the 18/150 current snapshot, say so explicitly.

Then define the smallest docs-only amendment required.

Do not hide the change by reusing an old hash with new semantics.

If a lifecycle child object or lifecycle root must change:

- identify exactly which child;
- publish old/new canonical object;
- recompute affected child hash;
- recompute lifecycle semantic root;
- derive a new non-self-referential governance authority;
- explicitly supersede the old clause only where necessary.

Do not alter the three scientific hashes.

---

# 4. Scientific/authority principle

The correct closure must preserve this distinction:

```text
source is present on disk
!=
source is production-authoritative
```

and:

```text
source is NOT_TESTABLE
!=
all other independent slots must become NOT_TESTABLE
```

The runtime projection is intentionally per-slot.

For every REGISTERED slot:

```text
all sources required by that exact configuration
must be production VERIFIED
```

For a NOT_TESTABLE slot:

```text
its unavailable source must remain unavailable
and the slot must remain excluded from Discovery
```

The run seal must never use an unavailable source as evidence for an active candidate.

---

# 5. Required adversarial reasoning

Attack at least:

```text
- caller substitutes a locally present but NOT_TESTABLE BTC source
- caller upgrades BTC status without new accepted authority
- caller changes source manifest while retaining old runtime snapshot hash
- caller changes runtime snapshot but retains old roster
- caller omits a required ETH source from the active 18-row universe
- caller uses a synthetic split attestation
- caller uses preregistered schedule as if it were production evidence
- caller binds a split hash from one manifest to an attestation from another
- caller introduces a new source after seal
- source becomes VERIFIED after seal and tries to expand active roster
- source becomes unavailable after seal
- cross-run split/source receipt replay
- path/hash self-consistency without evidence truth
```

---

# 6. Protected outcome boundary

Strictly no access to:

```text
H40 labels
forward returns
MFE/MAE
Precision
Net Expectancy
PF
ECE
Brier
bootstrap/HAC outcomes
candidate rankings
WF results
Confirmation outcomes
H39 protected outcomes
Final Holdout
```

Do not run Discovery/backtest/WF/Confirmation.

---

# 7. Deliverable

Create:

```text
reviews/v0.5/
V0.5.1_H40_F01R2_SOURCE_SPLIT_RUNTIME_AUTHORITY_COMPATIBILITY_CLOSURE.md
```

The artifact must contain:

```text
- exact reviewed code SHA
- exact accepted upstream authority chain
- proof of current incompatibility
- chosen Option A or Option B
- canonical semantics
- exact object/hash implications
- adversarial analysis
- implementation requirements
- required tests
- explicit state after closure
```

Do not modify code in this task.

Commit and push only to:

```text
origin/v0.5-refactor-docs
```

Do not trigger, rerun, poll, or wait for CI.

---

# 8. Required final states

If Option A and the existing lifecycle hashes remain valid, the artifact may conclude:

```text
H40_F01R2_AUTHORITY_COMPATIBILITY_SEMANTICS_CLOSED
H40_LIFECYCLE_SEMANTIC_ROOT_UNCHANGED
H40_LIFECYCLE_GOVERNANCE_AUTHORITY_UNCHANGED
H40_F01R2_IMPLEMENTATION_REPAIR_AUTHORIZED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

If Option B requires an amendment, do not authorize implementation until an independent Sol acceptance of the new authority object/hash exists.

In either case:

```text
H40_FSA_F02_OPEN_SEALED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```

---

# 9. Recommended model

```text
GPT-5.6 Sol
Reasoning: High
```

This is a narrow authority-design task, not a mechanical coding task. Do not delegate the semantic decision to a lightweight executor.
