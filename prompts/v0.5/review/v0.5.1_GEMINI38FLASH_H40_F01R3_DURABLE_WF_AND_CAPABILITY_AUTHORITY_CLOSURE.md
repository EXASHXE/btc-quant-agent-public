# Gemini 3.8 Flash Execution Prompt — H40 F01R3 Durable WF / Run-Head / Capability Authority Closure

## ROLE

You are a constrained repository-analysis executor.

You are NOT the final scientific or authority reviewer.

Your job is to produce a precise, evidence-backed **candidate authority-closure document** for later independent GPT-5.6 Sol review.

Do not improvise new scientific semantics.
Do not authorize implementation yourself.
Do not modify implementation code.

Repository:

```text
EXASHXE/btc-quant-agent
```

Implementation branch:

```text
v0.5-refactor
```

Exact implementation SHA under analysis:

```text
1dad1b6eba6a1610a6af365d1fbeac0376dab5d1
```

Docs branch:

```text
v0.5-refactor-docs
```

Primary findings to close:

```text
ASTRA-B-01 BLOCKER
ASTRA-B-02 BLOCKER
ASTRA-B-03 BLOCKER
ASTRA-H-01 HIGH
ASTRA-M-01 MEDIUM where safe
```

Final artifact to create:

```text
reviews/v0.5/
V0.5.1_H40_F01R3_DURABLE_WF_AND_CAPABILITY_AUTHORITY_CLOSURE.md
```

The artifact is a PROPOSED closure pending independent Sol acceptance.

It MUST NOT claim implementation authorization.

---

# 0. HARD SAFETY / SCOPE RULES

Never access, derive, compute, summarize, search for, or run real:

```text
H40 labels
forward returns
MFE
MAE
Precision
Net Expectancy
Profit Factor
ECE
Brier
bootstrap/HAC outcomes
candidate rankings
candidate winner
real Walk-Forward outcomes
Confirmation outcomes
H39 protected outcomes
Final Holdout
```

Do not run:

```text
Discovery
real WF
Confirmation
H39 unblind
Final Holdout
paper trading
testnet
live execution
```

Do not:

```text
trigger CI
rerun CI
poll CI
wait for CI
modify CI
```

Remote CI policy:

```text
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

Do not change implementation branch.

Do not change frozen scientific identities.

---

# 1. REQUIRED READING ORDER

Read ALL of the following before writing the closure.

## 1.1 Current controller/handover

```text
docs/handover/
V0.5.1_QUANT_AGENT_PHASE_HANDOVER_2026-09-21.md
```

## 1.2 Astra audit

```text
reviews/v0.5/
V0.5.1_ASTRA_PRE_DISCOVERY_FULL_SYSTEM_AUDIT.md

commit:
ebc904c4b33f2b367c12a3725b418128631f928c
```

Read the full sections for:

```text
ASTRA-B-01
ASTRA-B-02
ASTRA-B-03
ASTRA-H-01
ASTRA-M-01
```

## 1.3 Sol adjudication

```text
reviews/v0.5/
V0.5.1_ASTRA_PRE_DISCOVERY_SOL_ADJUDICATION.md

commit:
b504fd58c3274549e8ffcaa99975f8d63f52a11e
```

## 1.4 F01R2 authority chain

```text
reviews/v0.5/
V0.5.1_H40_F01R2_SOURCE_SPLIT_RUNTIME_AUTHORITY_COMPATIBILITY_CLOSURE.md

reviews/v0.5/
V0.5.1_H40_F01R2_SOL_INDEPENDENT_ACCEPTANCE.md

reviews/v0.5/
V0.5.1_H40_F01R2_IMPLEMENTATION_SOL_INDEPENDENT_REVIEW.md
```

## 1.5 Accepted lifecycle authority

```text
reviews/v0.5/
V0.5.1_H40_P3R0R1_LIFECYCLE_EVIDENCE_AUTHORITY_REPAIR.md

reviews/v0.5/
V0.5.1_H40_P3R0R1_SOL_CUMULATIVE_ACCEPTANCE.md

reviews/v0.5/
V0.5.1_H40_P2_FINAL_INDEPENDENT_ACCEPTANCE.md

reviews/v0.5/
V0.5.1_H40_P1_FINAL_SOL_ACCEPTANCE.md
```

## 1.6 Exact source at implementation SHA

Read these files from exact SHA
`1dad1b6eba6a1610a6af365d1fbeac0376dab5d1`:

```text
src/btc_quant_agent/h40/lifecycle_authority.py
src/btc_quant_agent/h40/lifecycle.py
src/btc_quant_agent/h40/split_manifest.py
src/btc_quant_agent/h40/source_manifest.py
src/btc_quant_agent/h40/protocol_authority.py
src/btc_quant_agent/h40/search_space.py
tests/test_v051_h40_f01_lifecycle_authority.py
```

Do not analyze moving HEAD when exact SHA differs.

---

# 2. FROZEN IDENTITIES

These MUST remain unchanged unless the task explicitly proves a lifecycle-only amendment is required.

Scientific identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Current F01R2 lifecycle identities:

```text
runtime_snapshot_seal_contract_hash =
76a0732742707c78f65da26263076bed7b586ea67d4f5ddd2dac33761e37e612

lifecycle_semantic_root_hash =
fad50703c12da9af35366ceb8a774794b4ff2f3163a1adb6b624d3b69316e406

lifecycle_governance_authority_hash =
7edce39c421ad6c487580483d2fa674b1f199e21640c33b1c08f85dddc6f64fc
```

Current runtime projection:

```text
TOTAL = 168
REGISTERED = 18
NOT_TESTABLE = 150

active_required_source_ids =
["ETHUSDT_USD_M_1H"]

BTCUSDT_USD_M_1H = NOT_TESTABLE
ETHUSDT_USD_M_1H = VERIFIED
```

Do not alter these facts.

---

# 3. EXECUTION METHOD

Execute the task in EXACTLY these phases.

Do not skip a phase.

## PHASE 1 — Evidence table

Create an internal evidence table before making any proposal.

For EACH finding B-01/B-02/B-03/H-01/M-01 record:

```text
finding_id
exact file
exact class/function
current behavior
why behavior is unsafe/incomplete
accepted artifact clause governing expected behavior
existing tests that cover part of it
existing tests that fail to cover the paired/race/restart attack
```

Every closure decision must cite at least one source symbol and one accepted artifact.

If the code contradicts a prior summary, exact code wins unless it would contradict a higher-order accepted authority artifact; if so, flag the conflict explicitly.

## PHASE 2 — Reconstruct current authority graph

Write the exact chain:

```text
P1 source authority
-> RuntimeAuthoritySnapshot
-> P2 168-slot ledger
-> REGISTERED roster
-> active source union
-> runtime source/split attestation
-> runtime snapshot seal
-> H40_RUN_AUTHORITY_V1
-> lifecycle implementation authority
-> Discovery
-> Candidate Lock
-> expected WF authority
-> WF validation
-> Confirmation Ready
-> termination
-> persisted receipt chain
```

For every edge state whether authority is:

```text
DERIVED
CALLER_SUPPLIED
CONTENT_ADDRESSED
COLD_REVERIFIED
CACHED
PERSISTED
NOT_IMPLEMENTED
```

This graph is mandatory.

## PHASE 3 — Close B-01

Answer ALL of these with one exact proposed production derivation:

1. What exact runtime split partitions form the complete WF validation universe?
2. How are ordered fold IDs derived?
3. How are partition IDs bound?
4. How is each fold-definition hash derived?
5. What object owns accepted validation-contract IDs?
6. How is the exact contract map derived?
7. Can any production API accept a caller-constructed `H40ExpectedSplitAuthority`?
8. How does issuance reconstruct expected WF authority?
9. How does cold restore reconstruct it?
10. How are missing/extra/reordered folds rejected?
11. How are missing/extra contracts rejected?
12. How are cross-run expected authorities rejected?

Required target property:

```text
expected_wf_authority
=
derive(
  sealed_run_authority,
  accepted_runtime_split,
  accepted_validation_contract_authority
)
```

Never:

```text
expected_wf_authority
=
trust(caller_object)
```

Classify B-01 as exactly one:

```text
IMPLEMENTATION_ONLY
CANONICAL_CLARIFICATION_WITHOUT_HASH_CHANGE
CANONICAL_CHILD_AMENDMENT
NEW_AUTHORITY_OBJECT
```

Explain why.

## PHASE 4 — Close B-02 and H-01 together

Design one durable run-history model.

You MUST specify a canonical durable run-head object.

At minimum propose fields:

```text
schema_id
run_authority_id
head_receipt_hash
head_state
head_predecessor_receipt_hash
transition_sequence
terminal
```

If any field is unnecessary, justify removal.

Define exact transition rule:

```text
compare current durable head
verify expected predecessor
materialize receipt bytes completely
durably publish receipt
atomically/recoverably advance head
```

Define behavior for:

```text
exact retry
conflicting retry
stale writer
two writers racing
candidate-lock sibling
WF-success vs NO_GO sibling
terminal head
restart
orphan receipt
partial temp file
directory fsync failure
head update failure
```

You MUST explicitly identify the commit point.

You MUST state whether receipt-first or head-first is used.

You MUST provide crash recovery truth table for at least:

```text
C1 crash before temp complete
C2 crash after temp complete before receipt publish
C3 crash after receipt publish before head advance
C4 crash during head advance
C5 crash after head advance
```

For each case specify:

```text
visible committed state after restart
whether retry is allowed
what is quarantined/ignored
```

Do not invent a nonce/new run ID to escape terminal state.

Classify B-02 and H-01 independently using the four impact labels.

## PHASE 5 — Close B-03

Evaluate exactly two models.

### Model A

```text
recursive/current source truth revalidation at every production transition use
```

### Model B

```text
immutable protected run-input snapshot/content-addressed evidence store
```

For each provide:

```text
authority object
what bytes are trusted
what invalidates it
runtime I/O cost
restart semantics
deletion/mutation semantics
attack surface
implementation complexity
```

Then choose one.

For this repository version, prefer the smaller change unless it weakens accepted F01R2 evidence-truth semantics.

The selected model MUST define behavior for:

```text
source disappears after Discovery
source mutates after Candidate Lock
runtime source state changes after seal
cached Candidate reused
cached WF reused
cold-restored Candidate
cold-restored WF
```

Cached and cold semantics MUST match.

Classify B-03 using the impact labels.

## PHASE 6 — Close M-01

Inspect active runtime source record fields:

```text
row_count
gap_count
start_utc
end_utc
receipt
file_sha256
timestamp membership
```

For each field classify:

```text
AUTHORITATIVE
DERIVED_AUTHORITATIVE
ADVISORY
REFERENCE_ONLY
```

For active VERIFIED sources define exact equality requirements against cold validation receipt.

Do not modify preregistered identity fields.

Do not promote NOT_TESTABLE sources.

## PHASE 7 — Canonical impact

Inspect current canonical lifecycle children:

```text
wf_validation_receipt_contract
persistence_replay_contract
transition_matrix_contract
termination_receipt_contract
runtime_snapshot_seal_contract
```

For each proposed repair decide:

```text
NO_CHANGE
CLARIFICATION_ONLY
CANONICAL_CHANGE_REQUIRED
```

Rule:

If the existing canonical object already explicitly requires the behavior, do not change hash merely for implementation detail.

If the new normative authority concept is absent from the object, do not pretend the old hash covers it.

If canonical change is required:

1. quote exact old relevant canonical object;
2. show exact proposed new canonical object;
3. calculate canonical child hash using repository canonical SHA-256 implementation;
4. recompute full lifecycle semantic root;
5. propose new governance authority preimage;
6. preserve prior governance hash as lineage;
7. do NOT call it accepted.

If you cannot reliably compute the hash, write:

```text
HASH_RECOMPUTATION_REQUIRED_BY_SOL
```

Do not invent a digest.

## PHASE 8 — Adversarial matrix

Produce one table covering ALL A01-A29:

```text
A01 fold shrink + evidence shrink
A02 contract shrink + evidence shrink
A03 fabricated extra fold + matching evidence
A04 real split hash + fake fold definition
A05 cross-run expected authority
A06 stale-run expected authority
A07 two candidate locks
A08 WF success vs NO_GO sibling
A09 terminal then stale success
A10 success then stale terminal
A11 concurrent writers
A12 restore non-head sibling
A13 filename manipulation
A14 crash before receipt complete
A15 receipt durable/head not advanced
A16 head intent before receipt publish
A17 directory fsync failure
A18 exact retry
A19 conflicting retry
A20 source disappears after Discovery
A21 source disappears after Candidate Lock
A22 source mutates after Candidate Lock
A23 runtime source state changes
A24 cached WF after invalidation
A25 cold-restored WF after invalidation
A26 cached/cold equivalence
A27 row_count contradiction
A28 gap_count contradiction
A29 advisory/reference mutation
```

Columns:

```text
case
current vulnerability
proposed control
authority object enforcing it
required test
expected fail-closed result
```

No case may be omitted.

## PHASE 9 — Implementation requirements

Write exact implementation requirements by file/symbol.

At minimum cover:

```text
lifecycle_authority.py
lifecycle.py
artifact persistence/store
resolver
tests/test_v051_h40_f01_lifecycle_authority.py
```

For each change state:

```text
file
symbol
change
reason
new invariant
test IDs
```

Do not write implementation code.

## PHASE 10 — Self-audit

Before committing the artifact, answer every checklist item YES/NO.

```text
S01 exact implementation SHA used
S02 Astra findings read in full
S03 Sol adjudication read in full
S04 B-01 paired shrink attack closed
S05 caller cannot define production WF universe
S06 one durable run head defined
S07 terminal durable across restart
S08 sibling transitions rejected
S09 exact retry semantics defined
S10 conflicting retry semantics defined
S11 crash commit point defined
S12 crash recovery table complete
S13 cached and cold capability semantics identical
S14 source disappearance behavior defined
S15 source mutation behavior defined
S16 M-01 descriptor authority classified
S17 canonical hash impact analyzed child-by-child
S18 no invented hash
S19 all A01-A29 covered
S20 no outcome surface accessed
S21 no code modified
S22 no CI action
S23 P3 remains unauthorized
S24 Discovery remains unauthorized
S25 F02 remains sealed
S26 H39 remains protected
S27 Final Holdout remains sealed
```

If any of S01-S27 is NO, fix the document before committing.

---

# 4. REQUIRED OUTPUT DOCUMENT STRUCTURE

The artifact MUST use these exact top-level sections:

```text
1. Document status and exact audited state
2. Frozen authority lineage
3. Evidence table
4. Current lifecycle authority graph
5. ASTRA-B-01 closure
6. ASTRA-B-02 durable run-head closure
7. ASTRA-H-01 crash-atomic publication closure
8. ASTRA-B-03 capability lifetime closure
9. ASTRA-M-01 source descriptor closure
10. Canonical authority impact
11. Proposed canonical objects / hash implications
12. A01-A29 adversarial matrix
13. Implementation requirements by file/symbol
14. Required implementation tests
15. Residual risks / unresolved questions
16. Self-audit checklist S01-S27
17. Proposed post-closure state
```

Do not merge sections.

Do not omit tables.

---

# 5. REQUIRED DOCUMENT STATUS

The first section MUST contain exactly:

```text
DOCUMENT_TYPE = GEMINI_PROPOSED_AUTHORITY_CLOSURE
EXECUTOR = GEMINI_3_8_FLASH
REVIEW_STATUS = PENDING_INDEPENDENT_SOL_ACCEPTANCE
IMPLEMENTATION_AUTHORIZATION = NOT_GRANTED_BY_THIS_DOCUMENT
```

The document MUST NOT claim:

```text
H40_F01R3_IMPLEMENTATION_REPAIR_AUTHORIZED
H40_F01_FINAL_ACCEPTED
H40_P3_AUTHORIZED
H40_DISCOVERY_AUTHORIZED
```

Even if the analysis believes the closure is complete.

Allowed conclusion:

```text
H40_F01R3_PROPOSED_SEMANTICS_COMPLETE_FOR_SOL_REVIEW
```

If unresolved authority ambiguity remains:

```text
H40_F01R3_PROPOSED_SEMANTICS_INCOMPLETE
SOL_DECISION_REQUIRED
```

---

# 6. GIT DELIVERY

Create only:

```text
reviews/v0.5/
V0.5.1_H40_F01R3_DURABLE_WF_AND_CAPABILITY_AUTHORITY_CLOSURE.md
```

Commit and push only to:

```text
origin/v0.5-refactor-docs
```

Suggested commit message:

```text
docs(h40): propose F01R3 durable lifecycle authority closure
```

Do not modify implementation branch.

Do not create implementation commits.

---

# 7. FINAL CHAT RESPONSE FORMAT

Return exactly these fields:

```text
artifact_path:
artifact_commit_sha:
audited_implementation_sha:

B01_classification:
B02_classification:
B03_classification:
H01_classification:
M01_classification:

lifecycle_hash_change_proposed: YES|NO
new_lifecycle_root_hash: <hash|UNCHANGED|HASH_RECOMPUTATION_REQUIRED_BY_SOL>

chosen_capability_model: A|B|HYBRID
durable_head_defined: YES|NO
crash_protocol_defined: YES|NO
A01_A29_complete: YES|NO
self_audit_S01_S27_complete: YES|NO

implementation_modified: NO
outcomes_accessed: NO
ci_triggered: NO

final_state:
H40_F01R3_PROPOSED_SEMANTICS_COMPLETE_FOR_SOL_REVIEW
or
H40_F01R3_PROPOSED_SEMANTICS_INCOMPLETE
```

No extra narrative after these fields.
