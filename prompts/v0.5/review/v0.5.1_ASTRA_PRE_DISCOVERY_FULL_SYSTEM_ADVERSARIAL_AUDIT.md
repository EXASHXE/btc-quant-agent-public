# GPT-6 / Astra Prompt — V0.5.1 H40 Pre-Discovery Full-System Adversarial Audit

## Mission

Perform the final **pre-outcome, pre-Discovery, repository-wide adversarial audit** of `btc-quant-agent` before any future H40 P3 Discovery authorization is considered.

This is not a code-implementation task.

This is not a strategy-search task.

This is not a profitability review.

This is a systems, authority, scientific-integrity, PIT, replay, persistence, source-lineage, operational-boundary and future-architecture audit.

The objective is to answer:

> Is the current repository structurally and scientifically safe enough to proceed toward a separately authorized H40 Discovery implementation, or are there remaining BLOCKER/HIGH defects that must be closed first?

The audit must also determine what should survive into the planned v0.6 validated-playbook / Trading Policy architecture.

---

# 1. Repository state

Repository:

```text
EXASHXE/btc-quant-agent
```

Implementation branch:

```text
v0.5-refactor
```

Exact implementation SHA to audit:

```text
1dad1b6eba6a1610a6af365d1fbeac0376dab5d1
```

Commit:

```text
Implement H40 F01R2 runtime authority
```

Docs/review branch:

```text
v0.5-refactor-docs
```

Do not assume branch HEAD remains unchanged after this prompt. Anchor all code findings to the exact implementation SHA above.

If exact source at that SHA cannot be read, stop and report the evidence limitation.

---

# 2. Mandatory reading order

Read these artifacts in order.

## 2.1 Sol corrections to the Gemini pre-audit

```text
reviews/v0.5/astra-preaudit/
V0.5.1_SOL_VALIDATION_ADDENDUM.md
```

This document supersedes specific stale/incorrect claims in the Gemini package.

## 2.2 Current F01R2 implementation review

```text
reviews/v0.5/
V0.5.1_H40_F01R2_IMPLEMENTATION_SOL_INDEPENDENT_REVIEW.md
```

Review commit:

```text
37b46a334f2a470c0d04804088c99a47f67ab3ca
```

## 2.3 F01R2 authority amendment

```text
reviews/v0.5/
V0.5.1_H40_F01R2_SOURCE_SPLIT_RUNTIME_AUTHORITY_COMPATIBILITY_CLOSURE.md
```

Commit:

```text
e1263f4babc067bb56623081ee27769ac61b7e00
```

## 2.4 F01R2 independent authority acceptance

```text
reviews/v0.5/
V0.5.1_H40_F01R2_SOL_INDEPENDENT_ACCEPTANCE.md
```

Commit:

```text
c59e153b14d88dff26db705fc0c72218a1a2bb76
```

## 2.5 Earlier F01/F01R1 reviews

```text
reviews/v0.5/
V0.5.1_H40_F01_SOL_INDEPENDENT_IMPLEMENTATION_REVIEW.md

reviews/v0.5/
V0.5.1_H40_F01R1_SOL_INDEPENDENT_REVIEW.md
```

## 2.6 P3R0R1 accepted lifecycle semantics

```text
reviews/v0.5/
V0.5.1_H40_P3R0R1_LIFECYCLE_EVIDENCE_AUTHORITY_REPAIR.md

reviews/v0.5/
V0.5.1_H40_P3R0R1_SOL_CUMULATIVE_ACCEPTANCE.md
```

## 2.7 P1/P2 final authority

```text
reviews/v0.5/
V0.5.1_H40_P1_FINAL_SOL_ACCEPTANCE.md

reviews/v0.5/
V0.5.1_H40_P2_FINAL_INDEPENDENT_ACCEPTANCE.md
```

## 2.8 Gemini full repository/data pre-audit package

Read the complete directory:

```text
reviews/v0.5/astra-preaudit/
```

At minimum:

```text
V0.5.1_ASTRA_PREAUDIT_INDEX.md
V0.5.1_ASTRA_FULL_AUDIT_BRIEFING.md
V0.5.1_REPOSITORY_ARCHITECTURE_AND_CODE_MAP.md
V0.5.1_LOCAL_DATA_AND_LINEAGE_INVENTORY.md
V0.5.1_RESEARCH_HISTORY_AUTHORITY_AND_STAGE_MAP.md
V0.5.1_RUNTIME_FORWARD_EVIDENCE_AND_OPERATIONS_MAP.md
V0.5.1_TECH_DEBT_AND_REFACTOR_CANDIDATES.md
V0.5.1_VALIDATED_PLAYBOOK_POLICY_ARCHITECTURE_FIT.md
```

Treat Gemini documents as a high-value inventory/navigation layer, not primary scientific authority where they conflict with accepted artifacts or exact source.

## 2.9 Future strategic direction

```text
docs/roadmap/
V0.6_VALIDATED_PLAYBOOK_TRADING_POLICY_DIRECTION.md
```

This is planning input only, not current H40 authority.

---

# 3. Current accepted identities

Scientific identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Current accepted lifecycle identities:

```text
runtime_snapshot_seal_contract_hash =
76a0732742707c78f65da26263076bed7b586ea67d4f5ddd2dac33761e37e612

lifecycle_semantic_root_hash =
fad50703c12da9af35366ceb8a774794b4ff2f3163a1adb6b624d3b69316e406

lifecycle_governance_authority_hash =
7edce39c421ad6c487580483d2fa674b1f199e21640c33b1c08f85dddc6f64fc
```

Current accepted runtime projection:

```text
BTCUSDT_USD_M_1H                 NOT_TESTABLE
ETHUSDT_USD_M_1H                 VERIFIED
BTCUSDT_OFFICIAL_DERIVATIVES     NOT_TESTABLE
BTCUSDT_SPOT_FLOW                NOT_TESTABLE
ETHUSDT_DERIVATIVES_FLOW         NOT_TESTABLE

TOTAL = 168
REGISTERED = 18
NOT_TESTABLE = 150

active_required_source_ids =
["ETHUSDT_USD_M_1H"]
```

These are pre-outcome governance facts, not performance claims.

---

# 4. Current authorization state

The current state before this Astra audit is:

```text
H40_F01R2_IMPLEMENTATION_SOURCE_CONTRACT_REVIEW_ACCEPTED

H40_FSA_F01_IMPLEMENTATION_COMPLETE_PENDING_TEST_EVIDENCE_AND_FINAL_AUTHORITY

H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_ACCEPTED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_FSA_F02_OPEN_SEALED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED

REMOTE_CI_DEFERRED_ACTIONS_QUOTA
NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```

Astra must not silently upgrade any of these states.

---

# 5. Hard prohibited outcome surfaces

This audit is still pre-Discovery.

Do not access, derive, compute, summarize or search for real:

```text
H40 labels
H40 forward returns
real H40 MFE/MAE
Precision
Net Expectancy
Profit Factor
ECE
Brier
bootstrap/HAC outcomes
candidate rankings
candidate winner
real Walk-Forward results
Confirmation results
H39 protected outcomes
Final Holdout outcomes
```

Do not run:

```text
H40 Discovery
H40 backtest
candidate selection
real WF validation
Confirmation
H39 unblind
Final Holdout evaluation
paper trading
testnet
live execution
```

Synthetic/governance-only fixtures are allowed where necessary to reason about authority.

---

# 6. CI prohibition

GitHub Actions quota remains constrained.

Do not:

```text
trigger workflow_dispatch
rerun failed jobs
rerun workflows
poll Actions
wait for Actions
modify CI
treat quota failure as code failure
```

Do not use missing CI evidence to claim a code defect.

You may inspect already committed source/tests and any local test reports that already exist.

---

# 7. Audit methodology

Do not merely summarize prior reviews.

Independently attack the current exact code.

Use:

```text
source code
tests
configs
accepted protocol/review artifacts
git history
local non-protected data inventory
runtime/service definitions
dependency/caller graph
persistence schema
CLI reachability
```

If CodeGraph/equivalent is available:

- use it for dependency/caller/impact analysis;
- then inspect exact code;
- do not treat graph output as semantic truth.

For each important claim cite:

```text
path :: class/function
test path :: test
accepted artifact :: section
commit SHA
```

---

# 8. Audit track A — F01/F01R2 authority graph

Reconstruct the full authority path:

```text
P1 source authority
-> runtime authority snapshot
-> P2 168-slot materialization
-> REGISTERED roster
-> active required source union
-> runtime source manifest
-> runtime split
-> H40_RUNTIME_SOURCE_SPLIT_ATTESTATION_V1
-> runtime snapshot seal
-> H40_RUN_AUTHORITY_V1
-> lifecycle implementation authority
-> Discovery authorization receipt
-> future Candidate Lock
-> future WF
-> Confirmation Ready
```

Attack every boundary.

Questions:

1. Can any caller create production-looking source/split authority without evidence truth?
2. Can local data presence promote a NOT_TESTABLE source?
3. Can a source state be omitted or duplicated?
4. Can roster completeness be weakened?
5. Can active source union be caller-selected?
6. Can an active source be omitted?
7. Can a NOT_TESTABLE source enter active evidence?
8. Can a runtime split be accepted only because `is_authoritative=True`?
9. Can legacy BTC/ETH attestation be substituted?
10. Can preregistered schedule be relabelled as runtime authority?
11. Can source/split/attestation objects from different runs be cross-paired?
12. Can a changed source state mutate an existing run?
13. Can a newly available source expand an existing run?
14. Can a stale run be replayed after source authority changes?
15. Is every current H40 call path fail-closed before implementation authority is accepted?

Treat any production-authority path that depends only on a caller boolean/path/hash/string as BLOCKER.

---

# 9. Audit track B — exact runtime source semantics

Verify current natural derivation really produces:

```text
18 REGISTERED
150 NOT_TESTABLE
active source union = ETH-only
```

Do not trust constants alone.

Trace:

```text
H40ConfigurationSlot
family combination
scope
cross-asset flag
funding flag
derive_required_sources_for_slot / project_required_sources
RuntimeAuthoritySnapshot.derive_slot_status
```

Check that the 18 rows are exactly those expected by accepted P2 authority.

Check that no hidden source dependency exists outside the projection function.

Examples:

- feature implementation reading BTC despite slot source projection saying ETH-only;
- global market-state code accessing another source;
- calibration code implicitly loading unavailable source;
- common preprocessing reading all products before filtering.

If any REGISTERED candidate can indirectly read a NOT_TESTABLE source, mark BLOCKER.

This is one of the highest-value audit questions.

---

# 10. Audit track C — PIT / temporal leakage attack

Attack all time semantics.

Check:

- closed-candle semantics;
- decision timestamp;
- feature cutoff;
- horizon labels;
- purge windows;
- calibration/validation boundaries;
- 72h lookback;
- 24h diagnostic horizon;
- funding timestamp alignment;
- cross-asset timestamp alignment;
- timezone normalization;
- missing source gaps;
- forward-store freshness;
- future timestamps in joins;
- pandas/SQL merge direction;
- inclusive vs exclusive boundaries;
- data backfill rules.

Particularly verify:

```text
WF1_TRAIN
!= Discovery candidate-ranking outcome partition

WF1_CALIBRATION
= the only Discovery ranking outcome partition

WF1_VALIDATION
and later
= inaccessible before Candidate Lock
```

Look for both explicit and indirect leakage.

---

# 11. Audit track D — candidate-selection science interface

Do not implement or run real selection.

Audit whether F01 now provides a trustworthy seam for future P3.

Check:

```text
H40LifecycleEvidenceVerifier
H40SyntheticEvidenceVerifier
Candidate result evidence schema
correction input evidence manifest
Candidate Lock receipt
WF evidence schema
expected split authority
```

Verify that future P3 can implement accepted R3R4 science without modifying trusted lifecycle semantics.

Accepted selection science remains:

```text
within family:
separate max-stat correction for PRECISION
separate max-stat correction for NET_EXPECTANCY

cross family:
Holm over family representatives

final:
all hard gates
-> highest adjusted LCB net expectancy
-> highest adjusted LCB precision
-> lower complexity
-> ascending canonical structural configuration hash

no runner-up promotion
```

Attack:

- global M=18 shortcut;
- slot-index tie break;
- precomputed adjusted values trusted without evidence;
- missing family candidates;
- hidden candidate filtering;
- duplicated candidate IDs;
- partial roster;
- metric-family mixing;
- selection-rank authority field;
- runner-up substitution after WF failure.

---

# 12. Audit track E — persistence / replay / restart

Attack:

```text
H40LifecycleArtifactStore
H40LifecycleAuthorityResolver
runtime seal context
receipt chain
cold restore
atomic create-if-absent
canonical JSON
content-addressed identities
```

Determine whether a process restart can:

- load wrong run;
- substitute predecessor receipt;
- reuse candidate receipt across run;
- reuse split authority across run;
- skip active-source cold verification;
- restore an authorization whose source bytes changed;
- accept path identity instead of content identity;
- silently overwrite immutable evidence.

Explicitly distinguish:

```text
test resolver
future production resolver
persistence contract
actual implemented durable authority store
```

Do not overclaim production readiness if only the resolver interface exists.

---

# 13. Audit track F — H39 / Confirmation / Final Holdout isolation

Attack protected surfaces from:

- direct Python import;
- CLI;
- REST/API;
- generic file loader;
- glob;
- pathlib;
- symlink;
- relative path;
- normalized path;
- resolved path;
- config override;
- environment variable;
- SQLite connection string;
- report/export path;
- data-discovery tool.

Verify:

```text
H39 protected outcomes remain inaccessible
Final Holdout remains inaccessible
Confirmation outcome access remains denied
F02 transition remains prohibited
```

Do not read the protected outcomes while auditing the guard.

---

# 14. Audit track G — execution-disable boundary

Search every possible execution/order path.

Verify:

```text
EXECUTION_DISABLED
auto_execute = false
no production order surface authorized
```

Look for:

- Binance order clients;
- market/limit order functions;
- testnet paths;
- position mutation;
- cancel/replace;
- leverage setting;
- account API writes;
- hidden executable scripts;
- environment-driven bypass;
- legacy v0.2/v0.3 execution code.

Classify each:

```text
dead
legacy
test-only
paper-only
reachable-but-guarded
reachable-and-dangerous
```

Any unguarded real-exchange write path reachable from current runtime is BLOCKER.

---

# 15. Audit track H — economic kernel correctness

Independently inspect:

- fee model;
- slippage;
- funding;
- leverage;
- margin;
- position sizing;
- stop;
- target;
- intrabar ordering;
- fill timing;
- liquidation assumptions;
- accounting;
- cash flow;
- realized/unrealized PnL;
- benchmark behavior.

Do not claim profitability.

Distinguish:

```text
configs/default.toml runtime defaults
economic/FeeModel defaults
campaign-specific frozen research costs
H40 cost proxy
```

Do not collapse them into one universal cost model.

Identify any semantic mismatch that could make future OOS expectancy materially optimistic.

---

# 16. Audit track I — data lineage and operational reality

Use the Gemini local-data inventory as a map, then challenge it.

Do not equate:

```text
file exists
with
source is H40 authoritative
```

Audit:

- official Binance archive lineage;
- source hashes;
- Forward derivatives;
- microstructure SQLite;
- WAL/locking;
- gap behavior;
- duplicate handling;
- timestamp units;
- stale data;
- partial partitions;
- disk growth;
- health checks;
- service restart;
- collector duplication;
- resource exhaustion.

Keep the separate branch:

```text
forward-evidence-health-lightweight
743c936e6fcb93346435c99bc00abd0b4f69c969
```

architecturally separate from H40 scientific authority unless an accepted protocol explicitly binds it.

---

# 17. Audit track J — repository architecture and complexity

Evaluate current repository as a system.

Identify:

- duplicated semantic ownership;
- authority logic spread across modules;
- oversized modules;
- dead/legacy strategy surfaces;
- old runners;
- compatibility shims;
- duplicate persistence layers;
- duplicate health/diagnostic layers;
- config drift;
- package-version drift;
- tests coupled to implementation details;
- circular conceptual dependencies;
- abstractions that should be deleted after H40.

Do not refactor code in this audit.

For each finding give:

```text
path/symbol
current role
problem
severity
blast radius
suggested disposition:
KEEP
GENERALIZE
MERGE
RETIRE
MOVE
REVIEW
```

---

# 18. Audit track K — research governance vs actual trading intelligence

Challenge whether the project has over-invested in authority/governance relative to market/trading intelligence.

Separate:

```text
DATA
FEATURE / MARKET STATE
ALPHA / PLAYBOOK SCIENCE
POLICY / ROUTING
RISK / ACCOUNTING
EXECUTION / OPERATIONS
```

Quantify or characterize where repository complexity sits.

Answer:

1. What code actually attempts to produce edge?
2. What code only validates/governs experiments?
3. What historical hypotheses failed?
4. What remains a real open research question?
5. Is H40 correctly scoped as one directional playbook family rather than the whole product?
6. Has governance complexity begun to dominate research throughput?
7. Which governance pieces are essential?
8. Which could be generalized/simplified in v0.6 without weakening scientific integrity?

---

# 19. Audit track L — future v0.6 Validated Playbook / Trading Policy architecture

Read:

```text
docs/roadmap/V0.6_VALIDATED_PLAYBOOK_TRADING_POLICY_DIRECTION.md
```

Do not simply agree with it.

Adversarially assess whether the proposed direction is sound.

Proposed high-level flow:

```text
Validated Alpha / Edge Research
-> condition-specific validated playbooks
-> Validated Playbook Library
-> deterministic Trading Policy / Router
-> Risk Engine
-> Execution Engine
-> later Agent/LLM orchestration
```

Assess:

## Market State Engine

How should regime/state be represented without opening massive researcher degrees of freedom?

Possible states include:

```text
trend
range
compression
expansion
breakout
failed breakout
volatility regime
crowding
cross-asset confirmation/divergence
```

Should state be:

- deterministic features;
- separately validated classifier;
- latent probabilistic model;
- part of each playbook;
- routing layer?

## Playbook abstraction

Assess future families such as:

```text
Trend Continuation
Pullback Continuation
Breakout
Failed Breakout Reversal
Mean Reversion
NO_TRADE
```

Determine how current H40/P2 infrastructure can be generalized without making the search space unconstrained.

## Policy selector

Should initial policy routing be:

- deterministic;
- probabilistic;
- contextual bandit;
- learned gating model;
- LLM-assisted?

Do not recommend LLM direct numerical authority without evidence.

## Risk / execution

What current v0.4/v0.5 components are reusable?

What is missing before:

```text
Future Shadow
Paper/Testnet
Tiny Live
```

## LLM/Agent role

Assess where an LLM can add value:

- orchestration;
- explanation;
- conflict resolution;
- diagnostics;
- research planning;
- playbook synthesis;

without becoming an unverifiable source of expected return.

---

# 20. Specific architectural question Astra must answer

Give a clear recommendation on this design decision:

> After H40 reaches its research terminal state, should v0.6 formally move from a hypothesis-centric H-series architecture to a validated playbook / Trading Policy architecture?

Do not evaluate this as a trading recommendation.

Evaluate it as a research/software architecture decision.

Compare at least:

```text
A. Continue sequential H41/H42/H43 hypothesis campaigns
B. Move to validated regime-conditioned playbook families
C. Hybrid: hypothesis research remains below a playbook registry/policy layer
```

Discuss:

- research degrees of freedom;
- auditability;
- code reuse;
- scientific power;
- overfitting risk;
- operational realism;
- extensibility;
- maintainability.

Do not claim any architecture guarantees profit.

---

# 21. Severity rubric

Use:

```text
BLOCKER
HIGH
MEDIUM
LOW
INFO
```

BLOCKER examples:

- pre-outcome leakage;
- authority bypass;
- protected-outcome access;
- incomplete candidate universe;
- executable real-money path;
- unbound source/split truth;
- replay that changes scientific identity;
- ability to run Discovery before accepted implementation authority.

HIGH examples:

- major semantic duplication;
- durability/recovery defect;
- future P3 verifier cannot implement accepted science without altering lifecycle core;
- material cost/accounting optimism;
- source projection does not match actual feature dependencies.

Do not inflate severity for cosmetic structure.

---

# 22. Required final verdict

Astra must issue one of:

```text
ASTRA_PRE_DISCOVERY_GATE_PASS
```

or:

```text
ASTRA_PRE_DISCOVERY_GATE_FAIL
```

or:

```text
ASTRA_PRE_DISCOVERY_GATE_CONDITIONAL
```

This verdict is a **review gate only**.

Even PASS does not itself authorize P3 or Discovery because lifecycle implementation authority/test/CI evidence remains a separate authority condition.

Explicitly state:

```text
ASTRA PASS != P3 AUTHORIZATION
```

---

# 23. Required output artifact

Create on:

```text
origin/v0.5-refactor-docs
```

Path:

```text
reviews/v0.5/
V0.5.1_ASTRA_PRE_DISCOVERY_FULL_SYSTEM_AUDIT.md
```

Do not modify implementation branch.

Recommended report structure:

```text
1. Executive verdict
2. Exact audited state
3. Authority graph
4. BLOCKER findings
5. HIGH findings
6. MEDIUM/LOW findings
7. PIT/leakage audit
8. source/split/runtime audit
9. candidate-selection boundary
10. persistence/replay
11. protected surfaces
12. execution boundary
13. economic kernel
14. data/runtime operations
15. repository architecture
16. research-governance assessment
17. v0.6 Trading Policy architecture assessment
18. keep/generalize/retire map
19. required repairs before P3
20. post-H40 roadmap
21. final state matrix
```

For each BLOCKER/HIGH include:

```text
finding ID
severity
exact path/symbol
attack scenario
why current tests do/don't catch it
required closure
whether docs-first authority amendment is needed
```

---

# 24. No implementation

Do not fix findings.

Do not commit code.

Do not modify tests.

Do not write a repair prompt unless the audit artifact itself needs a short “required next closure” section.

The controller will convert accepted findings into separate versioned prompts after reviewing the Astra report.

---

# 25. Delivery

Commit and push only the audit artifact to:

```text
origin/v0.5-refactor-docs
```

Suggested commit message:

```text
docs(audit): add Astra pre-Discovery full-system audit
```

Do not trigger or rerun CI.

If an automatic workflow fails only due Actions quota, report:

```text
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

and do not act on it.

---

# 26. Final response

Report:

```text
exact implementation SHA audited
exact docs inputs used
protected surfaces not accessed
outcome surfaces not accessed

Astra verdict

BLOCKER count
HIGH count
MEDIUM count
LOW count

top authority findings
top PIT findings
top architecture findings
v0.6 architecture conclusion

audit artifact path
audit commit SHA

confirmation:
no implementation modified
no Discovery executed
no WF executed
no Confirmation executed
no H39/Final Holdout accessed
no CI triggered

REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

Recommended model:

```text
GPT-6 / Astra
maximum available reasoning depth
```

Do not delegate the final audit verdict to a lightweight sub-agent.
