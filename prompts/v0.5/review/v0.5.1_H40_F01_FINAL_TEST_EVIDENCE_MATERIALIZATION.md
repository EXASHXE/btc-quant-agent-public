# Agent Prompt — V0.5.1 H40 F01 Final Test Evidence Materialization

## Status

\`\`\`text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = EVIDENCE_MATERIALIZATION_ONLY
\`\`\`

Repository:

\`\`\`text
EXASHXE/btc-quant-agent
\`\`\`

Authoritative tested implementation commit:

\`\`\`text
83ebb74eaa38395dd497fd4ceb30fb457d1a429c
\`\`\`

Final implementation acceptance:

\`\`\`text
reviews/v0.5/V0.5.1_H40_F01R3R4R1_FINAL_IMPLEMENTATION_SOL_ACCEPTANCE.md
commit 940d4b1e69f1612e225f1a292128c40668fca69c
\`\`\`

This task must NOT change production or test code.
This task must NOT claim lifecycle implementation authority acceptance.

---

# 1. Mission

Materialize exact, independently inspectable local deterministic test evidence for:

\`\`\`text
f01_implementation_commit_sha =
83ebb74eaa38395dd497fd4ceb30fb457d1a429c
\`\`\`

and record the currently observed remote CI state without misrepresenting it as passing evidence.

This task does NOT authorize:

\`\`\`text
H40 P3
Discovery
real H40 outcomes
F02
Confirmation
H39
Final Holdout
execution
\`\`\`

---

# 2. Exact tested commit discipline

Create or use a clean worktree pinned exactly to:

\`\`\`text
83ebb74eaa38395dd497fd4ceb30fb457d1a429c
\`\`\`

Before running tests, record:

\`\`\`bash
git rev-parse HEAD
git status --porcelain
\`\`\`

Required:

\`\`\`text
HEAD = 83ebb74eaa38395dd497fd4ceb30fb457d1a429c
working tree clean
\`\`\`

If either condition fails:

\`\`\`text
STOP
do not create an evidence manifest
\`\`\`

Do not commit anything to \`v0.5-refactor\`.

The evidence artifact itself must be committed only to:

\`\`\`text
v0.5-refactor-docs
\`\`\`

after all testing is complete.

---

# 3. Required local evidence gates

Run on the exact tested commit.

## 3.1 Focused lifecycle gate

\`\`\`bash
pytest -q \
  tests/test_v051_h40_f01_lifecycle_authority.py \
  tests/test_v051_h40_f01r3r1_durable_authority.py \
  tests/test_v051_h40_f01r3r2_commit_authority.py \
  tests/test_v051_h40_f01r3r3_api_seal.py \
  tests/test_v051_h40_f01r3r4_lineage_durability.py
\`\`\`

Record exact exit code, passed/failed/skipped counts, duration, and final pytest summary line.

## 3.2 Execution B04 focused gate

\`\`\`bash
pytest -q tests/test_execution.py tests/test_execution_environment_authority.py
\`\`\`

Record the same fields.

## 3.3 Full test suite

\`\`\`bash
pytest -q
\`\`\`

Record exact final summary and exit code.

## 3.4 Static/quality gates

Run separately:

\`\`\`bash
ruff check .
mypy
python -m compileall -q src skill-template/scripts
git diff --check
\`\`\`

Record command, exit code, and concise exact result for every gate.

Do not omit failures. Do not rerun until green and hide the first result.

---

# 4. Environment identity

Record only non-secret environment facts:

\`\`\`text
python --version
python executable path
platform.system
platform.release
platform.machine
pytest --version
ruff --version
mypy --version
git --version
\`\`\`

Do not record credentials, API keys, tokens, environment secrets, or private account identifiers.

---

# 5. Protected-surface requirements

No real outcome-bearing H40 data may be opened or evaluated.

Record:

\`\`\`text
real_h40_outcomes_accessed = false
h39_accessed = false
final_holdout_accessed = false
signed_network_requests = 0
\`\`\`

Tests must use synthetic/governance fixtures only.

---

# 6. Remote CI observation

Do NOT trigger, rerun, poll, wait for, or modify GitHub Actions.

The currently observed automatic push run for the exact tested commit is:

\`\`\`text
run_id = 35719167685
workflow = CI
event = push
head_sha = 83ebb74eaa38395dd497fd4ceb30fb457d1a429c
status = completed
conclusion = failure

quality (3.13) = failure
compatibility = skipped
quality job executable step records = none observed
job log = unavailable
\`\`\`

Verify this state at most once if necessary for artifact accuracy. Do not repeatedly query it.

Classify this remote run exactly as:

\`\`\`text
REMOTE_CI_NOT_VALID_POSITIVE_EVIDENCE
\`\`\`

Do not claim CI green, CI passed, CI equivalent, or CI accepted.

Local deterministic evidence remains local evidence.

---

# 7. Evidence manifest

Create on \`v0.5-refactor-docs\`:

\`\`\`text
evidence/v0.5/h40/V0.5.1_H40_F01_FINAL_TEST_EVIDENCE_83ebb74.json
\`\`\`

Use this top-level structure:

\`\`\`json
{
  "schema_id": "H40_F01_IMPLEMENTATION_TEST_EVIDENCE_MANIFEST_V1",
  "tested_commit_sha": "83ebb74eaa38395dd497fd4ceb30fb457d1a429c",
  "implementation_acceptance_artifact_path": "reviews/v0.5/V0.5.1_H40_F01R3R4R1_FINAL_IMPLEMENTATION_SOL_ACCEPTANCE.md",
  "implementation_acceptance_commit_sha": "940d4b1e69f1612e225f1a292128c40668fca69c",
  "working_tree_clean_before_tests": true,
  "environment": {},
  "local_gates": [],
  "remote_ci_observation": {},
  "protected_surface_attestation": {},
  "overall_local_evidence_status": "PASS_OR_FAIL",
  "remote_ci_evidence_status": "REMOTE_CI_NOT_VALID_POSITIVE_EVIDENCE"
}
\`\`\`

Requirements:

- JSON only, UTF-8, no comments, no secrets.
- Serialize object keys in sorted order.
- Local gate entries preserve exact command and result.
- Do not include the manifest's own hash inside itself.
- Serialize in canonical compact JSON form compatible with repository canonical JSON semantics, followed by exactly one newline.

After writing, compute and report BOTH:

\`\`\`text
canonical_object_sha256 =
canonical_sha256(parsed_manifest_object)

file_bytes_sha256 =
sha256(exact committed UTF-8 file bytes)
\`\`\`

For the later \`H40RequiredTestCIEvidenceIdentity.evidence_manifest_sha256\`, do NOT choose which digest is authoritative yourself.

The later Sol evidence-chain review will decide the exact interpretation from frozen authority.

---

# 8. Human-readable evidence report

Also create:

\`\`\`text
reviews/v0.5/V0.5.1_H40_F01_FINAL_TEST_EVIDENCE_REPORT.md
\`\`\`

It must state:

\`\`\`text
tested commit
manifest path
canonical_object_sha256
file_bytes_sha256

focused lifecycle result
execution B04 result
full pytest result
ruff result
mypy result
compileall result
git diff --check result

remote CI run identity/status
REMOTE_CI_NOT_VALID_POSITIVE_EVIDENCE

real outcome/protected-surface attestations
\`\`\`

If all local gates pass, allowed local conclusion:

\`\`\`text
LOCAL_DETERMINISTIC_TEST_EVIDENCE_COMPLETE
REMOTE_CI_EVIDENCE_DEFERRED_OR_INVALID
LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_ACCEPTED
\`\`\`

If any local gate fails:

\`\`\`text
LOCAL_TEST_EVIDENCE_INCOMPLETE
LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_ACCEPTABLE
\`\`\`

Do not claim formal authority acceptance.

---

# 9. Docs-branch commit discipline

After tests finish, use a separate docs worktree for:

\`\`\`text
v0.5-refactor-docs
\`\`\`

Before creating artifacts:

\`\`\`bash
git fetch origin
git checkout v0.5-refactor-docs
git pull --ff-only
\`\`\`

Do not merge implementation branch into docs branch.

Commit only the evidence manifest/report.

Suggested commit:

\`\`\`text
docs(h40): materialize final F01 test evidence
\`\`\`

Push normally.

---

# 10. Final response

Return:

\`\`\`text
tested_commit_sha:
code_worktree_clean_before_tests:

focused_lifecycle:
execution_b04:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

manifest_path:
canonical_object_sha256:
file_bytes_sha256:
report_path:
docs_commit_sha:

remote_ci_run_id:
remote_ci_status:
remote_ci_evidence_status:

real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
signed_network_requests: 0
ci_triggered_rerun_or_polled: NO

final_state:
LOCAL_DETERMINISTIC_TEST_EVIDENCE_COMPLETE_OR_INCOMPLETE
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_ACCEPTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
\`\`\`

Recommended executor:

\`\`\`text
Codex GPT-5.6 Sol Medium
\`\`\`

This is evidence collection and serialization, not protocol design or implementation.
