# Astra Prompt — V0.5.1 Focused Pre-Discovery Correctness Re-Gate

## Role and authorized context

You are reviewing an authorized local research codebase owned by the requester.

This task is strictly:

```text
software correctness verification
research-governance verification
deterministic lifecycle invariant verification
local source-code review
synthetic/adversarial-fixture validation
```

This task is NOT:

```text
offensive security testing
vulnerability exploitation
credential testing
network intrusion
real exchange interaction
real account access
real signed API access
external target testing
```

Do not make real network requests.

Do not use real credentials.

Do not perform signed Binance requests.

Do not access outcome-bearing H40 research data.

All runtime checks must use local/synthetic fixtures only.

Repository:

```text
EXASHXE/btc-quant-agent
```

Code branch:

```text
v0.5-refactor
```

Required exact code HEAD:

```text
c873a4654513570992621d961b3de95eb52777ff
```

If `origin/v0.5-refactor` differs from that SHA, STOP and report the exact mismatch.

Docs/review branch:

```text
v0.5-refactor-docs
```

---

# 1. Required authority inputs

Read completely:

```text
reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_FULL_SYSTEM_AUDIT.md
commit ebc904c4b33f2b367c12a3725b418128631f928c

reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_SOL_ADJUDICATION.md
commit b504fd58c3274549e8ffcaa99975f8d63f52a11e

reviews/v0.5/V0.5.1_H40_F01R3_SOL_ADJUDICATION_AND_CANONICAL_AMENDMENT.md
commit ba41e186128e61e8162a7da6e2b2a0de03e4ba44

reviews/v0.5/V0.5.1_H40_F01R3_FINAL_IMPLEMENTATION_SOL_ACCEPTANCE.md
commit 473e451bbd902207232a83f50a7271d2aacef398

reviews/v0.5/V0.5.1_EXECUTION_B04_SOL_INDEPENDENT_ACCEPTANCE.md
commit cabb06be87c5874e5a32c37b5f4ddabe67a9e086

reviews/v0.5/V0.5.1_EXECUTION_B04_IMPLEMENTATION_SOL_ACCEPTANCE.md
commit d00692844c123cde48bc4e103c044e118ed2fdc3
```

Where earlier candidate documents differ from Sol adjudication or acceptance, Sol is normative.

---

# 2. Review objective

Perform a focused correctness re-gate of the previously identified critical/high pre-Discovery findings:

```text
B-01 expected WF authority completeness
B-02 durable terminal/run-head authority
B-03 cached descendant vs current root source truth
B-04 execution environment/account consistency
H-01 receipt publication/head crash consistency
M-01 runtime source descriptor consistency
```

This is NOT a fresh full-repository audit.

Do not reopen unrelated style, refactor, ergonomics, performance, future live-execution, or v0.6 architecture topics.

The purpose is to determine whether the previously identified pre-Discovery authority/correctness defects remain closed in the exact current implementation.

---

# 3. Current accepted research boundary

Current policy:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

No signed Binance capability is currently authorized.

Classify any newly observed issue using exactly one of:

```text
CURRENT_PRE_DISCOVERY_BLOCKER
CURRENT_PRE_DISCOVERY_HIGH
NON_BLOCKING_CURRENT_RESEARCH
FUTURE_EXECUTION_BLOCKER
POST_H40_BACKLOG
```

A finding is a current blocker only if there is a concrete reachable code path, under the current accepted policy, that can cause one of:

```text
scientific leakage
protected partition access
accepted authority invariant violation
non-reproducible committed lifecycle truth
signed capability becoming reachable under RESEARCH_DISABLED_V1
corruption of frozen search/source authority
```

Do not classify hypothetical future live/testnet execution hardening gaps as current H40 research blockers when signed capability remains unreachable today.

---

# 4. B-01 — WF authority completeness verification

Verify by source inspection and synthetic/local tests that production code cannot alter the accepted WF completeness semantics.

Confirm all of the following:

```text
WF1 -> WF1_VALIDATION
WF2 -> WF2_VALIDATION
WF3 -> WF3_VALIDATION
WF4 -> WF4_VALIDATION
```

There is no fifth WF fold.

Confirmation is not a WF fold.

Verify that production behavior does not permit:

```text
caller-defined reduced WF fold sets
omission of WF2/WF3/WF4
replacement of validation folds with Confirmation
persisted split evidence becoming authoritative instead of internally derived production authority
cold restore changing fold completeness
cached split authority overriding current internally derived production authority
```

Production P3 validation contracts may still be absent or fail closed.

Do not treat an intentionally unavailable future contract as a defect if current behavior remains fail closed.

---

# 5. B-02 — durable lifecycle head correctness verification

Verify the durable lifecycle implementation preserves:

```text
exactly one committed authoritative head per run
linearizable compare-and-advance
single committed successor for conflicting sibling transitions
exact predecessor equality
transition_sequence increments by exactly one
terminal absorption across restart/process
head row/head_json/head_hash integrity
restore limited to committed head lineage
accepted source->target lifecycle adjacency
F02 transition remains sealed
```

Use source inspection and synthetic/local concurrency tests only.

Verify these correctness cases:

```text
stale sibling transition cannot become the committed successor
two concurrent sibling transitions produce one committed winner
arbitrary sequence-zero transition is rejected
out-of-order lifecycle transition is rejected
H40_CONFIRMATION_READY -> H40_CONFIRMATION_EVALUATED_ONCE cannot commit
an unreferenced fully written receipt remains an orphan and confers no authority
a stale cached authorization cannot become committed without current service revalidation
missing/empty/mismatched head_hash fails closed
scalar durable-head fields cannot diverge from canonical head_json
terminal head cannot receive a successor
```

The supported public production persistence surface must remain:

```text
persist_authorization(
    authorization,
    service=exact issuing H40LifecycleAuthorityService
)
```

A private internal storage helper is not itself a defect if it is not exported as a supported production authority surface and is only reached from the verified persistence flow.

---

# 6. B-03 — current root source truth verification

Verify that a previously valid lifecycle authorization cannot remain consumable after its active root source truth is no longer valid.

Use only synthetic/local source fixtures.

Verification sequence:

```text
issue a valid production-shaped authorization
-> change/delete/invalidate the active ETH source fixture
-> attempt revalidation
-> attempt persistence
-> attempt cold restore
-> attempt descendant transition consumption
-> attempt termination issuance where applicable
```

Expected behavior:

```text
fail closed
```

Check semantic equivalence across:

```text
fresh transition issuance
explicit authorization revalidation
persistence
cold restore
descendant consumption
termination issuance
```

Do not require current truth checks for inactive NOT_TESTABLE sources.

---

# 7. B-04 — execution environment and signed-capability correctness verification

Verify that the current research policy prevents all production signed Binance capability.

Current authority:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

Review these service paths:

```text
submit_entry
reconcile
reconcile_open_plans
prepare_close
submit_close
cancel_entry
protective order flow
failsafe reduce-only MARKET flow
auto_submit
service signed-client construction path
```

Verify the following configuration/identity consistency cases with local synthetic plans/receipts/fake clients only:

```text
TESTNET persisted plan + LIVE current config
LIVE persisted plan + TESTNET current config
PAPER persisted plan + LIVE current config
allow_live=true
auto_execute=true
confirmation environment variables present
credentials present
fake/injected signed client present
old TESTNET/LIVE plan without environment authority
foreign environment order receipt
foreign plan hash
foreign symbol
foreign credential namespace
foreign account authority label
same numeric/string order ID from another environment
```

Expected current result:

```text
no signed Binance network interaction is reachable
```

Where observable, verify:

```text
signed client call count == 0
signed transport/network call count == 0
```

It is acceptable for:

```text
validate_execution_environment_preflight(...)
```

or:

```text
_signed_client()
```

to be the fail-closed boundary, provided no signed request occurs.

### Classification rule for future account isolation

Current implementation may use environment-scoped default account-authority labels such as:

```text
BINANCE_TESTNET_DEFAULT
BINANCE_LIVE_DEFAULT
```

If this is insufficient for future multi-account/subaccount production execution but cannot make signed capability reachable under the current research policy, classify it as:

```text
FUTURE_EXECUTION_BLOCKER
```

not:

```text
CURRENT_PRE_DISCOVERY_BLOCKER
```

Apply the same rule to future execution-only status/preflight centralization improvements.

---

# 8. H-01 — receipt publication and crash-consistency verification

Verify lifecycle persistence ordering and authority semantics around:

```text
temporary receipt materialization
receipt fsync
immutable content-addressed final publication
receipt-directory fsync
SQLite durable-head transaction
optional non-authoritative mirrors
restart/recovery
```

Expected semantics:

```text
temporary or partial artifact => no authority
fully published orphan receipt => no authority
authoritative directory durability failure => no head advance
conflicting existing content => fail closed
durable SQLite run head => sole lifecycle commit point
restore => only committed head/unique ancestor lineage
```

Check concurrent receipt publication and restart semantics using local/synthetic fixtures only.

---

# 9. M-01 — active source descriptor consistency

Verify active VERIFIED runtime source descriptors remain bound generically to cold source validation receipts:

```text
record.row_count == cold_receipt.timestamp_count
record.gap_count == cold_receipt.gap_count
record.start_utc == cold_receipt.first_timestamp_utc
record.end_utc == cold_receipt.last_timestamp_utc
```

Confirm:

```text
no hard-coded row-count assumption
no universal zero-gap assumption
BTCUSDT_USD_M_1H remains NOT_TESTABLE
ETHUSDT_USD_M_1H remains VERIFIED
inactive NOT_TESTABLE sources are not silently promoted
```

---

# 10. Frozen identities and projection

Verify there is no drift from:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

persistence_replay_contract_v2_hash =
5f014b867be17019c2be91ff48f8e23a43ed29678fc5589430ba174046fceaae

lifecycle_semantic_root_hash_v3 =
f0aa35eb91055a76129eae5c00be38174fcc13ae9fe08521d9202e93dc5d43d4

lifecycle_governance_authority_v4_hash =
72924dd22b3c9283964cdf368f7a754bd6f703a110989187620b36076a25a511
```

Projection must remain:

```text
TOTAL=168
REGISTERED=18
NOT_TESTABLE=150

BTCUSDT_USD_M_1H=NOT_TESTABLE
ETHUSDT_USD_M_1H=VERIFIED
active_required_source_ids=["ETHUSDT_USD_M_1H"]
```

---

# 11. Protected research surfaces

Do NOT access real H40 outcome-bearing data.

Do NOT execute or inspect:

```text
real forward labels
real forward returns
real MFE/MAE
real Precision outcomes
real Net Expectancy outcomes
PF
ECE/Brier
real candidate rankings
real candidate winner output
real WF validation outcomes
Confirmation outcomes
H39 protected outcome
Final Holdout
```

Synthetic fixtures and structural metadata only.

If any existing test would require real outcome-bearing data, do not run it.

Report that it was intentionally skipped to preserve the research boundary.

---

# 12. Convergence / repair-stop rule

This re-gate exists to converge the repository into real research.

Do not fail the current gate for:

```text
style
naming
minor code duplication
CLI ergonomics
future execution Policy B
multi-account production support while signed capability is disabled
non-authority performance work
forward-evidence-health side branch
future v0.6 architecture
non-critical refactoring
```

A new current blocker must identify a concrete reachable code path that violates an accepted current invariant.

Use this decision rule:

```text
Does the issue alter scientific validity,
protected-data isolation,
committed lifecycle authority,
or current signed-capability isolation?

YES -> current finding
NO  -> future/backlog classification
```

Do not start a new repair cycle for backlog-only issues.

---

# 13. Validation constraints

Use:

```text
source inspection
local static analysis
local synthetic fixtures
local unit tests
local concurrency tests
```

Do not:

```text
use real credentials
call Binance signed APIs
call external targets
perform network probing
perform offensive-security testing
trigger GitHub Actions
rerun GitHub Actions
poll GitHub Actions
wait for GitHub Actions
access real H40 outcomes
```

Normal repository reads are sufficient.

---

# 14. Required output artifact

Create:

```text
reviews/v0.5/V0.5.1_ASTRA_FOCUSED_PRE_DISCOVERY_RE_GATE.md
```

The report must contain:

```text
audited_code_sha:
audit_scope:

B01_status:
B02_status:
B03_status:
B04_status:
H01_status:
M01_status:

new_current_blockers:
new_current_highs:
future_execution_blockers:
non_blocking_current_research:
post_h40_backlog:

signed_network_requests_made: 0
real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
ci_triggered_or_polled: NO

frozen_hashes_unchanged:
projection_168_18_150_unchanged:

overall_verdict:
```

Allowed overall verdicts:

```text
ASTRA_FOCUSED_PRE_DISCOVERY_RE_GATE_PASS
ASTRA_FOCUSED_PRE_DISCOVERY_RE_GATE_FAIL
```

If FAIL, every current blocker must include:

```text
exact file/symbol
exact current-policy reachable code path
accepted invariant violated
why the issue can affect current H40 research validity or current signed-capability isolation
minimal required correction
```

Do not use hypothetical future signed execution as justification for a current research FAIL.

---

# 15. Expected next step if PASS

If the result is:

```text
ASTRA_FOCUSED_PRE_DISCOVERY_RE_GATE_PASS
```

then do NOT recommend another broad implementation-repair cycle.

The expected next sequence is:

```text
materialize lifecycle implementation/test evidence authority
-> evidence-chain review
-> H40 P3 authorization decision
-> H40 Discovery if explicitly authorized
```

The project goal is to enter outcome-bearing research after the accepted pre-Discovery invariants are closed.

Recommended model:

```text
GPT-6 Astra / Astra highest available reasoning
```
