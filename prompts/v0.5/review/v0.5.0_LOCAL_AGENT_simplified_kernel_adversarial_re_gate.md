# v0.5.0 Local-Agent Simplified-Kernel Adversarial Re-Gate

## Role

You are a bounded adversarial verification worker. Do not redesign the architecture and do not implement repairs unless explicitly instructed after the audit.

Repository: `EXASHXE/btc-quant-agent`
Code branch: `v0.5-refactor`
Required exact code HEAD: `6838e9db8d5c369b5da87354821d9c8e79c2a879`
Docs branch: `v0.5-refactor-docs`

Existing independent review basis:
- `reviews/v0.5/V0.5.0_ASTRA_SIMPLIFIED_KERNEL_ADVERSARIAL_GATE.md`
- `reviews/v0.5/V0.5.0_ASTRA_GATE_SOL_INDEPENDENT_REVIEW.md`
- `reviews/v0.5/V0.5.0_R05A_SOL_INDEPENDENT_REVIEW.md`
- `reviews/v0.5/V0.5.0_R05AR_SOL_INDEPENDENT_ACCEPTANCE.md`
- `reviews/v0.5/V0.5.0_R05B_SOL_INDEPENDENT_REVIEW.md`
- `reviews/v0.5/V0.5.0_R05C_SOL_INDEPENDENT_REVIEW.md`
- `reviews/v0.5/V0.5.0_R05CR_SOL_INDEPENDENT_ACCEPTANCE.md`
- `reviews/v0.5/V0.5.0_R05_STAGE_ACCEPTANCE_FULL_COMPAT.md`
- `reviews/v0.5/V0.5.0_SOL_SIMPLIFIED_KERNEL_ADVERSARIAL_RE_GATE.md`

## Hard preflight

Fetch `origin/v0.5-refactor` and verify the exact remote HEAD equals:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

If not, stop with:

```text
LOCAL_RE_GATE_ABORTED_HEAD_DRIFT
expected=6838e9db8d5c369b5da87354821d9c8e79c2a879
actual=<sha>
```

Do not audit another SHA.

## Safety / scope constraints

This is an audit-only task.

Do NOT:
- modify production code;
- modify tests already tracked in the code branch;
- push to `v0.5-refactor`;
- trigger GitHub Actions;
- run `workflow_dispatch`;
- access or unblind protected H39 outcomes;
- access Final Holdout;
- enable execution, paper trading, testnet, or live trading;
- begin H40 or any alpha search/tuning;
- weaken assertions to make probes pass.

Temporary local probe files are allowed, but keep them untracked or outside the repository. Delete them before finishing.

## Objective

Dynamically verify that the simplified kernel at the exact accepted HEAD resists the original Astra attack families and the R05 repair-specific second-order attacks.

The purpose is not to prove profitability. The purpose is to verify research authority, PIT integrity, replay determinism, evidence retention, storage side-effect boundaries, and protection boundaries.

A probe PASS means the attempted attack was rejected or the stated invariant was independently verified.

## Mandatory executable attack set

### A. AG-01 funding authority

Construct a normal formal candidate using preregistered funding evidence. Keep protocol/comparison/candle dataset/producer fixed and attempt each of the following:

1. funding-rate sign swap;
2. funding-event deletion;
3. funding-event addition;
4. timestamp substitution;
5. mark-price substitution;
6. persisted `funding_events` mutation followed by complete outer rehash;
7. replace the funding `EvidenceReference` with a different COMPLETE artifact while preserving the rest of the candidate family;
8. caller `funding_events=()` when authoritative evidence contains a settlement;
9. authoritative COMPLETE zero-event interval as a positive control;
10. missing funding evidence + empty caller list must fail closed.

For attacks 1–8, the candidate must not survive cold verification/P5 promotion under unchanged preregistered authority.

Explicitly verify that `ComparisonContract.funding_interval` is part of the comparison hash and that cold replay reloads evidence bytes through `load_and_validate_funding_evidence` rather than trusting persisted events.

### B. AG-02 material-input selector authority

Recreate the original momentum example with one full frozen contiguous candle dataset and `lookback_bars=2` at one fixed decision timestamp.

Attempt at minimum:

- authoritative latest two rows `[2,3]` — positive control;
- `[0,3]` with all row/input/bundle hashes recomputed;
- `[0,1,3]` with all outer hashes recomputed;
- `[1,2]` stale window;
- `[1,2,3]` extra row;
- future/late-availability substitution;
- cadence-gap window;
- duplicate open-time assertion where practical.

Cold verification must independently reconstruct the exact required latest-N contiguous available/closed rows and reject every non-authoritative set.

Do not merely call the builder and assert it rejects. At least one hostile case must mutate a persisted/serialized valid bundle, recompute its outer identities, and invoke the cold validator directly.

### C. AG-03 producer-contract immutability

Verify:

1. caller-owned nested dict/list aliases cannot mutate a registered contract;
2. direct top-level mutation is rejected/impossible;
3. nested mutation is rejected/impossible;
4. mutating `semantic_payload()`/`to_dict()` output cannot mutate registry authority;
5. exact duplicate registration is idempotent;
6. conflicting same-ID registration fails atomically;
7. hostile `object.__setattr__` stale-preimage replacement is detected by `get_contract()` or the authority boundary;
8. the original momentum threshold LONG→NO_TRADE mutation attack cannot keep the same declared contract hash/protocol authority;
9. fresh-process reconstruct/lookup smoke test preserves contract hash.

### D. AG-04 real GET `/health` read-only behavior

Use the actual application/service health path. Do not monkeypatch `QuantService.health` itself.

Isolate only external connectivity/scheduler calls as required.

Test at minimum:

1. forward parent directory absent → GET does not create it;
2. parent exists, DB absent → GET creates nothing;
3. valid checkpointed DB → DB/schema/row counts/directory membership unchanged;
4. incomplete/unsupported schema → DEGRADED/fail-closed, no migration and no sidecars;
5. real non-empty WAL + missing SHM → GET does not create SHM and does not modify DB/WAL;
6. real non-empty WAL + existing SHM → GET does not modify sidecars;
7. writer constructor still initializes normally as a positive control.

Do not use access-time metadata as a write detector.

### E. P5 evidence-chain truncation / substitution

Starting from one valid formal qualification, attempt to create a persisted qualification/attestation family that omits required evidence and rehashes all affected outer identities.

Verify atomic rejection for at least:

- omit bound funding evidence;
- omit bound dataset evidence;
- omit run evidence;
- omit benchmark evidence;
- replace funding evidence with unrelated COMPLETE evidence;
- replace run/benchmark reference with content-different evidence;
- duplicate or otherwise malformed required evidence IDs.

After each rejected registry attempt, verify registry generation, bytes, event count, and decision state are unchanged.

### F. Submitted accounting / benchmark forgery regressions

Re-run representative fully rehashed attacks from the prior gate:

- trade quantity;
- fee;
- slippage;
- event order;
- equity curve;
- round-trip attribution;
- terminal equity;
- pending-order count;
- random benchmark distribution/provenance where practical.

They must remain rejected by canonical replay/equality, not only by shallow schema validation.

### G. PIT / information-set regressions

Verify representative failures remain fail-closed:

- candle not closed;
- missing `available_at_ms`;
- availability after signal timestamp;
- product/interval mismatch;
- gap/overlap where formal sequence semantics forbid it;
- future row selection;
- legacy input that lacks current proof cannot silently gain promotion authority.

### H. RANDOM_MATCHED / CASH controls

Verify:

- CASH stays zero-activity/constant-equity under its intended semantics;
- RANDOM_MATCHED remains deterministic under identical preregistered seed/opportunity set;
- caller cannot swap candidate and benchmark producer roles;
- no submitted random trials override the reconstructed benchmark authority.

### I. Legacy / formal boundary

Verify diagnostic-only/legacy outputs cannot be relabeled or submitted as current formal qualification evidence.

### J. Persistence / publication regressions

Run representative existing tests for:

- immutable/idempotent publication;
- conflict detection;
- registry generation/CAS/append-only semantics;
- SQLite persistence/finalization where applicable.

No new implementation is requested.

### K. Protected surfaces

Use only safe synthetic/path-guard tests. Do not access protected outcomes.

Verify:

- H39 operational/protected paths remain inaccessible to formal jobs;
- Final Holdout paths remain sealed;
- R05 changed no H39 production code;
- no H40 research has started.

### L. Execution-disabled boundary

Verify configured execution remains disabled/default-deny and the formal research workflow cannot place orders.

### M. API observational read boundary

Verify retained GET/read endpoints do not mutate persistent research/runtime state within the tested scope, with special emphasis on `/health` and the forward store.

### N. Identity matrix / cross-product isolation

Run the existing identity-matrix or equivalent smoke checks across multiple products/horizons/contract identities and confirm distinct semantic inputs do not collapse to one authority identity.

Do not interpret identity diversity as alpha evidence.

## Regression suites

Run the relevant existing tests, at least:

```text
tests/test_v050_r05a_ag03_producer_contract_immutability.py
tests/test_v050_r05a_ag04_health_read_only_inspection.py
tests/test_v050_r05b_material_input_selector_authority.py
tests/test_v050_r05c_funding_evidence_authority.py
tests/test_v042_b04_verified_replay_input_bundle.py
tests/test_v042_b04r_replay_bundle_acceptance_repair.py
tests/test_v042_b04r2_authority_acceptance_repair.py
tests/test_v042_b04r3_protocol_producer_contract_fail_closed.py
tests/test_v042_b04r4_random_benchmark_authority_boundary.py
tests/test_v042_b04r5_candidate_identity_relabel_fail_closed.py
tests/test_v042_b05_candidate_execution_replay.py
tests/test_v042_b06_random_experiment_replay.py
tests/test_v043_c_lite_formal_consumers.py
tests/test_v050_r04_kernel_convergence.py
```

Then run local quality validation:

```text
ruff check .
mypy
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

Do not trigger GitHub CI. The accepted exact HEAD already has a successful full 3.11/3.12/3.13 Actions gate (`35001541879`).

## Existing CI receipt to verify, not rerun

Confirm GitHub run `35001541879` corresponds to exact SHA `6838e9db8d5c369b5da87354821d9c8e79c2a879` and records:

```text
Python 3.11 PASS
Python 3.12 PASS
Python 3.13 Ruff + mypy + coverage pytest + compileall PASS
```

Do not dispatch another run.

## Severity rules

Use:
- BLOCKER: protected-boundary breach, execution activation, or authority failure that invalidates the kernel as a research gate;
- HIGH: reproducible promotion/replay/PIT/evidence substitution bypass under unchanged preregistered authority;
- MEDIUM: material correctness or storage-side-effect defect that does not produce a formal authority bypass;
- LOW: operational/developer-experience issue with no formal authority impact;
- INFO: explicit non-blocking observation.

A green existing test suite is not sufficient to declare GO if any new executable attack succeeds.

## Expected report

Write a local report to:

```text
reviews/v0.5/V0.5.0_LOCAL_AGENT_SIMPLIFIED_KERNEL_ADVERSARIAL_RE_GATE.md
```

Commit that report to `v0.5-refactor-docs` only. Do not push changes to the code branch.

The report must include:

1. exact audited SHA and working-tree cleanliness;
2. model/agent used;
3. temporary probe inventory and whether it was deleted/untracked;
4. A–N attack matrix with PASS/FAIL/NOT_RUN_PROTECTED;
5. exact commands and test counts;
6. any successful attack with reproducible steps and affected files/functions;
7. verification that no production/test code changed;
8. verification that GitHub Actions was not triggered;
9. reference to existing full compatibility run `35001541879`;
10. final verdict.

If no BLOCKER/HIGH/MEDIUM is found, final verdict must be exactly:

```text
V0_5_LOCAL_EXECUTABLE_RE_GATE_GO
KERNEL_FREEZE_AUTHORIZED
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
NO_NEW_HIGH_OR_MEDIUM
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
H40_NOT_AUTHORIZED_UNTIL_FREEZE_RECORD
```

If a new material issue is found, do not repair it. Report:

```text
V0_5_LOCAL_EXECUTABLE_RE_GATE_BLOCKED
KERNEL_FREEZE_NOT_AUTHORIZED
```

with severity, attack, exact reproducer, and minimum repair scope.