# Astra Prompt — V0.5.1 Focused Pre-Discovery Re-Gate

## Role

You are performing a focused adversarial re-gate of previously accepted pre-Discovery blockers.

This is NOT a fresh full-repository audit.

Do not reopen research for unrelated style/refactor/future-live-execution issues.

Repository:

```text
EXASHXE/btc-quant-agent
```

Code branch:

```text
v0.5-refactor
```

Required exact code HEAD:

```text
c873a4654513570992621d961b3de95eb52777ff
```

If origin/v0.5-refactor differs, STOP and report exact mismatch.

Docs/review branch:

```text
v0.5-refactor-docs
```

---

# 1. Required authority inputs

Read completely:

```text
reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_FULL_SYSTEM_AUDIT.md
commit ebc904c4b33f2b367c12a3725b418128631f928c

reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_SOL_ADJUDICATION.md
commit b504fd58c3274549e8ffcaa99975f8d63f52a11e

reviews/v0.5/V0.5.1_H40_F01R3_SOL_ADJUDICATION_AND_CANONICAL_AMENDMENT.md
commit ba41e186128e61e8162a7da6e2b2a0de03e4ba44

reviews/v0.5/V0.5.1_H40_F01R3_FINAL_IMPLEMENTATION_SOL_ACCEPTANCE.md
commit 473e451bbd902207232a83f50a7271d2aacef398

reviews/v0.5/V0.5.1_EXECUTION_B04_SOL_INDEPENDENT_ACCEPTANCE.md
commit cabb06be87c5874e5a32c37b5f4ddabe67a9e086

reviews/v0.5/V0.5.1_EXECUTION_B04_IMPLEMENTATION_SOL_ACCEPTANCE.md
commit d00692844c123cde48bc4e103c044e118ed2fdc3
```

Where prior Gemini candidate documents differ from Sol adjudication/acceptance, Sol is normative.

---

# 2. Audit scope

Re-gate ONLY the previously identified critical/high pre-Discovery findings:

```text
B-01 expected WF authority shrinkage
B-02 durable terminal/run-head authority
B-03 cached descendant outliving current root source truth
B-04 execution TESTNET/LIVE/account/environment crossing
H-01 receipt publication/head crash consistency
```

Also verify M-01 remains closed:

```text
runtime source descriptor consistency against cold validation receipt
```

Do not perform a broad style/code-quality audit.

Do not treat future execution hardening as a current H40 research blocker unless it can make signed capability reachable under RESEARCH_DISABLED_V1.

---

# 3. Current accepted research boundary

Current policy:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

No signed Binance capability is currently authorized.

Therefore distinguish findings as:

```text
CURRENT_PRE_DISCOVERY_BLOCKER
CURRENT_PRE_DISCOVERY_HIGH
NON_BLOCKING_CURRENT_RESEARCH
FUTURE_EXECUTION_BLOCKER
POST_H40_BACKLOG
```

Example:

A missing configurable multi-account identity that cannot enable signed capability under RESEARCH_DISABLED_V1 is FUTURE_EXECUTION_BLOCKER, not a current H40 pre-Discovery blocker.

A path that actually reaches signed Binance under current policy is CURRENT_PRE_DISCOVERY_BLOCKER.

---

# 4. B-01 attack plan

Attempt to prove production code can:

- caller-supply/shrink expected WF fold universe;
- omit WF2/WF3/WF4;
- treat Confirmation as a WF fold;
- persist a different split authority and replay it as authority;
- use cached/persisted split authority instead of internally derived production authority;
- materially alter fold completeness through restore.

Expected accepted universe:

```text
WF1 -> WF1_VALIDATION
WF2 -> WF2_VALIDATION
WF3 -> WF3_VALIDATION
WF4 -> WF4_VALIDATION
```

No fifth fold.

Production P3 validation contracts may still be NOT_TESTABLE/unmaterialized; do not interpret fail-closed absence as a defect.

---

# 5. B-02 attack plan

Attempt to break:

```text
exactly one committed durable head per run
linearizable compare-and-advance
single winner for conflicting siblings
predecessor equality
sequence +1
terminal absorption across restart/process
head row/json/hash integrity
head-lineage-only authority restore
```

Attempt:

- stale sibling commit;
- two-thread/process race;
- arbitrary genesis;
- out-of-order transition;
- F02 transition;
- direct raw head commit;
- orphan receipt -> authority;
- stale cached authorization -> direct commit;
- malformed/empty head hash;
- scalar/head_json divergence;
- terminal successor.

Important:

```text
persist_authorization(..., service=exact issuer)
```

must be the sole public production commit surface.

Private internal helpers are not automatically defects unless reachable as public authority from supported production surfaces.

---

# 6. B-03 attack plan

Attempt to prove a previously valid authorization can still be consumed after active source truth changes.

Test conceptually:

```text
issue valid production authorization
-> mutate/delete/invalidate active ETH source truth
-> revalidate/persist/restore/consume
```

Expected result:

```text
fail closed
```

Check:

- transition issue;
- explicit revalidation;
- persistence;
- cold restore;
- termination issuance;
- descendant consumption.

Cached and cold restore paths must have equivalent root source truth semantics.

Do not require revalidation of inactive NOT_TESTABLE sources.

---

# 7. B-04 attack plan

Attempt to reach signed Binance capability through:

```text
submit_entry
reconcile
reconcile_open_plans
prepare_close
submit_close
cancel_entry
protective order path
failsafe reduce-only MARKET path
auto_submit
direct service signed-client seam
```

Try:

```text
TESTNET persisted plan + LIVE config
LIVE persisted plan + TESTNET config
PAPER persisted plan + LIVE config
allow_live=true
auto_execute=true
confirmation env vars present
credentials present
injected fake signed client
old plan without environment authority
foreign order receipt
foreign plan hash
foreign symbol
foreign credential namespace
foreign account label
same order ID in another environment
```

Expected current behavior:

```text
zero signed client construction/use before authority rejection
or
_signed_client itself rejects
```

No signed network capability may be reachable.

### Important classification rule

Current implementation uses environment-scoped default account labels.

If this prevents robust future multi-account production isolation but cannot make current signed capability reachable:

```text
classify FUTURE_EXECUTION_BLOCKER
not CURRENT_PRE_DISCOVERY_BLOCKER
```

Similarly, incomplete centralization of plan-status checking is not a current blocker unless it opens signed capability or corrupts H40 evidence.

---

# 8. H-01 attack plan

Attempt crash/publication failures around:

```text
temp receipt write
receipt fsync
hard-link publication
directory fsync
SQLite head commit
head mirror publication
```

Expected semantics:

```text
partial/temp artifact => no authority
published orphan receipt => no authority
directory fsync failure => no head advance
different-byte same-hash/path conflict => fail closed
head commit => sole lifecycle commit point
```

Check concurrent publishers and restart semantics.

---

# 9. M-01 check

Verify active source descriptor equality remains generic:

```text
row_count == cold_receipt.timestamp_count
gap_count == cold_receipt.gap_count
start_utc == cold_receipt.first_timestamp_utc
end_utc == cold_receipt.last_timestamp_utc
```

No fixed row count.
No universal zero-gap assumption.
No promotion of BTC NOT_TESTABLE source.

---

# 10. Frozen identities

Verify no drift:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

persistence_replay_contract_v2_hash =
5f014b867be17019c2be91ff48f8e23a43ed29678fc5589430ba174046fceaae

lifecycle_semantic_root_hash_v3 =
f0aa35eb91055a76129eae5c00be38174fcc13ae9fe08521d9202e93dc5d43d4

lifecycle_governance_authority_v4_hash =
72924dd22b3c9283964cdf368f7a754bd6f703a110989187620b36076a25a511
```

Projection:

```text
TOTAL=168
REGISTERED=18
NOT_TESTABLE=150

BTCUSDT_USD_M_1H=NOT_TESTABLE
ETHUSDT_USD_M_1H=VERIFIED
active_required_source_ids=["ETHUSDT_USD_M_1H"]
```

---

# 11. Protected research surfaces

This audit must NOT access real H40 outcomes.

Do not execute/read:

```text
real forward labels
real forward returns
real MFE/MAE
Precision outcomes
Net Expectancy outcomes
PF
ECE/Brier
candidate winner/ranking outputs
real WF validation results
Confirmation outcomes
H39 protected outcome
Final Holdout
```

Synthetic/adversarial fixtures only.

---

# 12. Repair-stop policy

Do not fail the current re-gate for:

```text
style
naming
minor duplication
future execution Policy B
multi-account production support when signed capability is disabled
CLI ergonomics
non-authority performance work
forward-evidence-health side branch
v0.6 architecture
```

A new CURRENT_PRE_DISCOVERY_BLOCKER must have an explicit causal path to one of:

```text
scientific leakage
protected partition access
authority bypass
non-reproducible committed lifecycle truth
signed capability reachable under current policy
corruption of frozen search/source authority
```

If no such path exists, classify it as backlog and do not block H40 research.

---

# 13. Validation approach

Use source inspection plus focused local adversarial tests if environment permits.

Do not trigger/rerun/poll/wait for GitHub Actions.

Do not use real exchange credentials.

Do not perform real signed network requests.

Do not access outcome-bearing H40 data.

---

# 14. Required output

Create:

```text
reviews/v0.5/V0.5.1_ASTRA_FOCUSED_PRE_DISCOVERY_RE_GATE.md
```

The report must contain:

```text
audited_code_sha:
audit_scope:

B01_status:
B02_status:
B03_status:
B04_status:
H01_status:
M01_status:

new_current_blockers:
new_current_highs:
future_execution_blockers:
post_h40_backlog:

signed_network_requests_made: 0
real_h40_outcomes_accessed: NO
h39_accessed: NO
final_holdout_accessed: NO
ci_triggered_or_polled: NO

frozen_hashes_unchanged:
projection_168_18_150_unchanged:

overall_verdict:
```

Allowed overall verdicts:

```text
ASTRA_FOCUSED_PRE_DISCOVERY_RE_GATE_PASS
ASTRA_FOCUSED_PRE_DISCOVERY_RE_GATE_FAIL
```

If FAIL, every blocker must include a concrete reachable current-policy exploit/contamination path.

Do not use hypothetical future signed execution as a reason to fail the current research gate.

---

# 15. Expected next step if PASS

If PASS:

```text
do not start another broad repair cycle
-> materialize lifecycle implementation/test evidence authority
-> perform evidence-chain audit
-> decide H40 P3 authorization
-> begin Discovery if explicitly authorized
```

This re-gate is intended to converge the project into research, not reopen infrastructure indefinitely.

Recommended model:

```text
GPT-6 Astra / Astra highest available reasoning
```
