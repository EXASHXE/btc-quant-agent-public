# V0.5.1 H40 Pre-Discovery Repair Contract — Controller Acceptance + Lifecycle V5 Hash Materialization

## Executor

**Recommended model: GPT-6 Sol High**

Reason: this stage is an authority-sensitive independent review plus deterministic canonical-hash materialization. It requires careful reasoning about lifecycle/governance identity, versioning, content-addressed lineage, and self-reference avoidance. Do not use Astra here; Astra is reserved for the later cumulative pre-Discovery audit after implementation repair and authority republication.

---

## Repository / exact starting state

Repository:

```text
https://github.com/EXASHXE/btc-quant-agent-public
```

Docs branch:

```text
v0.5-refactor-docs
```

Exact docs starting SHA:

```text
6dedefbd0776b36891cfaa553f3a27f23c604648
```

Implementation branch:

```text
v0.5-refactor
```

Exact implementation SHA under review / frozen code baseline:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Do **not** modify `v0.5-refactor` in this task.

Primary contract artifacts:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.md

evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.json
```

Their lineage:

```text
report commit:
fd44362ad6dbf5d35b2d5e938f7c30beac6e19c0

final docs tip containing both report + JSON:
6dedefbd0776b36891cfaa553f3a27f23c604648
```

Upstream blocking audit / triage:

```text
Astra R2 audit commit:
4f5786a25a5e25db32505a99445a3a42c62f7446

controller triage commit:
6f65db5fa7816a2fdbf340192b6a2a34847ae037
```

Accepted scientific protocol reference for F03 reconstruction:

```text
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3R2_FINAL_PREOUTCOME_CLOSURE.md

commit:
77601342ac9055b5c0bb47639d4c8ef7d83da154
```

---

# Mission

Perform an **independent controller acceptance review** of the Repair Contract Freeze R1.

If and only if the contract is semantically complete, internally consistent, preserves frozen science, closes the four Astra HIGH findings at the contract level, and introduces no hidden research freedom:

1. publish a controller acceptance artifact;
2. commit that acceptance artifact first;
3. use the resulting exact acceptance commit SHA as an immutable input to the new lifecycle-governance identity;
4. materialize the exact amended lifecycle child-contract hashes, lifecycle semantic root, and lifecycle governance authority V5 hash in a **second** docs commit;
5. publish a machine-readable hash-materialization JSON.

This is a docs/governance task only.

Do **not** implement Repair A or Repair B.

Do **not** authorize H40 P3 or Discovery.

---

# Hard protected-state constraints

Throughout this task:

```text
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

Forbidden:

- no real H40 label/outcome computation;
- no real WF1_CALIBRATION result computation;
- no real candidate ranking;
- no Confirmation access;
- no H39 outcome access;
- no Final Holdout access;
- no signed network/exchange requests;
- no execution authority;
- no implementation-branch code changes;
- no scientific threshold/formula changes;
- no weakening of any P3B closure.

If any task step would require protected outcome access, stop:

```text
H40_PRE_DISCOVERY_CONTRACT_ACCEPTANCE_ABORT_PROTECTED_SURFACE
```

---

# Frozen scientific identities

These must remain exact:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

prior lifecycle semantic root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

prior lifecycle governance authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

The repair contract asserts:

```text
protocol authority                UNCHANGED
scientific semantic root          UNCHANGED
structural ledger                 UNCHANGED

lifecycle semantic root           MUST CHANGE
lifecycle governance authority    MUST CHANGE / V5
lifecycle implementation authority MUST CHANGE after repaired implementation acceptance
```

Independently verify that this classification is correct.

---

# Required review scope

## A. Review R2-F01 contract

Verify the proposed:

```text
H40_P3_CONTROLLER_AUTHORITY_V1
H40_DISCOVERY_RUN_GRANT_V1
H40_RECEIPT_DISCOVERY_AUTH_V3
H40_LIFECYCLE_PERSISTENCE_CONTEXT_V2
```

Check:

1. exact fields are sufficient to bind:
   - controller acceptance;
   - protocol/scientific/structural identity;
   - lifecycle governance;
   - lifecycle implementation authority;
   - provenance contract;
   - run authority;
   - runtime source/split/seal;
   - permitted partitions;
   - execution-disabled state;

2. there is no caller-controlled string that can substitute for accepted authority;

3. old V2 Discovery receipts and V1 persistence contexts fail closed for new production;

4. the production verifier cannot read economic/outcome evidence before the accepted grant is validated;

5. the grant is content-addressed governance authority, not a secrecy/credential claim;

6. no circular dependency is introduced.

If a missing field materially permits replay/substitution, BLOCK rather than silently add research freedom.

---

## B. Review R2-F04 service-lifetime contract

Verify that the frozen semantics require:

- immutable/write-once authority configuration after construction;
- current in-process accepted implementation/controller anchors to be checked at every authoritative boundary;
- a long-lived service under superseded authority to fail;
- persist/restore not to revive historical authority;
- hot authority republication to require new process/service construction;
- synthetic authority to remain disjoint from production.

Confirm that this changes lifecycle semantics and therefore correctly belongs in the lifecycle child/root/governance identity.

---

## C. Review R2-F02 source-value provenance contract

Verify:

```text
H40_DISCOVERY_PROVENANCE_CONTRACT_V1

H40_SOURCE_VALUE_EXTRACTION_V1

H40_P3B_SOURCE_DERIVATION_V2
```

Check especially:

- verifier reconstructs economic values from exact sealed bytes;
- `file_sha256` and verified source receipt are authoritative;
- transported OHLC is no longer an authority input;
- timestamp field comes from verified source receipt;
- economic row ordering and numeric canonicalization are deterministic;
- exact source support:
  `[2021-01-01T00:00:00Z, 2023-02-01T00:00:00Z)`;
- pre-2021-01-31 data remains LOOKBACK_RESERVED support only;
- missing source adapter => NOT_TESTABLE;
- no source promotion can silently define a new economic adapter.

Check the frozen provenance contract hash:

```text
9a5a71101875829aff2959c93391648dbfd94ab26fd55ed31b720610fa7be81b
```

Recompute it only from the exact canonical contract object represented by the R1 JSON. If the JSON and report are inconsistent, BLOCK; do not invent a replacement hash.

---

## D. Review R2-F03 causal prefit contract

Verify:

```text
H40_CAUSAL_PREFIT_RECONSTRUCTION_V1
H40_P3B_RAW_DECISIONS_V2
```

The governing science must come from the accepted R3R2 protocol, not new reviewer invention.

Confirm that R3R2 already freezes, at minimum:

- D1 4h/12h return score;
- D2 24h/72h strict close-based Donchian breakout;
- D3 24h/72h immediate two-bar failed-break reversal;
- R_VOL 24h realized-volatility scalar/state;
- O_RANGE 24h true-range expansion/state;
- PIT-safe empirical percentile/quantile reference;
- raw-score ownership;
- missing data fail-closed;
- strict `close_time < decision_t`;
- retrospective calibration on the already-authorized calibration stage.

Confirm the proposed repair changes provenance/recomputation only and therefore does **not** change:

```text
protocol_authority_hash
scientific_semantic_root
structural_ledger_hash
```

Confirm the raw-decision V2 removal of transported:

```text
regime_state
opportunity_state
raw_score
secondary_filter_state
```

is scientifically equivalent because these become deterministic verifier-derived values.

Any discrepancy with accepted R3R2 science => BLOCK.

---

# Lifecycle V5 canonical contract materialization

Only execute this section after the contract review is PASS.

## Phase 1 — acceptance commit

Create:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTANCE.md
```

It must include:

- exact reviewed docs SHA `6dedefbd...`;
- exact implementation baseline `d63c1fbb...`;
- findings F01-F04 disposition;
- frozen-science identity verdict;
- identity-impact verdict;
- migration verdict;
- explicit statement that implementation remains unmodified;
- explicit statement that P3/Discovery remain unauthorized.

Commit this artifact **alone first** to `v0.5-refactor-docs`.

Record the exact acceptance commit SHA:

```text
<ACCEPTANCE_COMMIT_SHA>
```

Do not amend/rebase that commit afterward.

If review is BLOCKED, publish only the blocker review and STOP. Do not materialize V5 hashes.

---

## Phase 2 — exact child/root/governance V5 hashes

After the Phase-1 acceptance commit exists, derive the amended lifecycle contracts from the currently accepted nine-child lifecycle contract set.

The child-name set remains exactly:

```text
discovery_authorization_receipt_contract
candidate_lock_receipt_contract
wf_validation_receipt_contract
confirmation_ready_receipt_contract
termination_receipt_contract
transition_matrix_contract
persistence_replay_contract
runtime_snapshot_seal_contract
f02_boundary_contract
```

Only the contract content necessary for this accepted repair may change.

Expected changed children:

```text
discovery_authorization_receipt_contract
transition_matrix_contract
persistence_replay_contract
```

Expected unchanged children:

```text
candidate_lock_receipt_contract
wf_validation_receipt_contract
confirmation_ready_receipt_contract
termination_receipt_contract
runtime_snapshot_seal_contract
f02_boundary_contract
```

Independently confirm this exact changed/unchanged partition.

### Required canonical changes

#### discovery_authorization_receipt_contract

Amend to require production Discovery receipt V3 and bind:

```text
controller_authority_hash
discovery_run_grant_hash
```

in addition to existing Discovery authorization lineage.

It must state that production V2 Discovery receipts are not valid under V5.

#### transition_matrix_contract

Preserve state adjacency.

Add production authority preconditions for:

```text
H40_P1_SCAFFOLDED -> H40_DISCOVERY
```

requiring a currently accepted controller authority and matching exact run grant before the transition authorization can be issued.

Preserve all later predecessor/receipt lineage semantics and terminal transitions.

Do not add new lifecycle states.

#### persistence_replay_contract

Amend production replay/persistence semantics to require:

- persistence context V2;
- controller authority hash;
- Discovery run grant hash;
- revalidation against current in-process accepted authority anchors;
- V1 persistence context / V2 Discovery receipt production rejection;
- no historical authority resurrection.

Do not alter the scientific evidence formulas.

### Hash algorithm

Use repository canonical serialization exactly:

```text
canonical_json:
  sort_keys = true
  separators = (",", ":")
  ensure_ascii = false
  allow_nan = false

canonical_sha256(value):
  sha256(canonical_json(value).encode("utf-8")).hexdigest()
```

Compute:

```text
new child hash for all 9 children

lifecycle_semantic_root_preimage =
{
  "<child_name>_hash": "<hash>",
  ...
  "schema_id": "H40_LIFECYCLE_SEMANTIC_ROOT_V1"
}

NEW_LIFECYCLE_SEMANTIC_ROOT =
canonical_sha256(lifecycle_semantic_root_preimage)
```

No child ordering assumption may substitute for canonical JSON sorting.

---

# Lifecycle governance authority V5

Construct:

```text
H40_LIFECYCLE_GOVERNANCE_AUTHORITY_V5
```

Preserve the accepted V4 authority lineage fields unless the R1 contract explicitly supersedes them.

It must include at least:

```text
schema_id = H40_LIFECYCLE_GOVERNANCE_AUTHORITY_V5

prior_lifecycle_governance_authority_hash =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

lifecycle_semantic_root_hash =
<NEW_LIFECYCLE_SEMANTIC_ROOT>

protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

pre_discovery_repair_contract_acceptance_identity = {
  "artifact_path":
    "reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTANCE.md",
  "commit_sha":
    "<ACCEPTANCE_COMMIT_SHA>"
}
```

Preserve the existing V4 historical amendment/acceptance identities if their retention is required by the current governance object. Do not drop historical lineage merely to simplify the hash.

Compute:

```text
NEW_LIFECYCLE_GOVERNANCE_AUTHORITY_HASH =
canonical_sha256(<exact V5 authority object>)
```

This hash materialization is **not** self-referential because it uses the already-created Phase-1 acceptance commit.

---

# Required Phase-2 deliverables

Create:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_V5_HASH_MATERIALIZATION.md

evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_V5_HASH_MATERIALIZATION.json
```

The machine-readable JSON must contain at least:

```text
schema_id
reviewed_contract_docs_sha
acceptance_artifact_path
acceptance_commit_sha
prior_lifecycle_child_hashes
new_lifecycle_child_hashes
changed_child_names
unchanged_child_names
lifecycle_semantic_root_preimage
new_lifecycle_semantic_root_hash
prior_lifecycle_governance_authority_hash
lifecycle_governance_authority_v5_object
new_lifecycle_governance_authority_hash
frozen_protocol_authority_hash
frozen_scientific_semantic_root_hash
frozen_structural_ledger_hash
discovery_provenance_contract_hash
implementation_baseline_sha
authorization_state
```

Use exact lowercase SHA-256 values.

Commit Phase-2 deliverables to `v0.5-refactor-docs`.

---

# Acceptance criteria

PASS only if all of the following hold:

1. F01-F04 contract semantics are complete enough for deterministic implementation.
2. No new scientific choice remains hidden in implementation discretion.
3. F02 economic values originate from sealed bytes, not labels/metadata claims.
4. F03 prefit state originates from causal reconstruction under accepted R3R2 formulas.
5. protocol/scientific/structural identities remain exact and unchanged.
6. lifecycle child/root/governance versioning changes are explicit and canonical.
7. controller authority and run grant are required before production Discovery.
8. old production receipt/persistence/evidence versions fail closed.
9. no protected outcomes are accessed.
10. implementation remains untouched.
11. exact V5 hashes are reproducible from published canonical objects.
12. no self-reference exists in acceptance/governance identity.

---

# Expected PASS state

After both phases, if accepted:

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTED
H40_LIFECYCLE_V5_HASHES_MATERIALIZED

R2_F01_CONTRACT_ACCEPTED
R2_F02_CONTRACT_ACCEPTED
R2_F03_CONTRACT_ACCEPTED
R2_F04_CONTRACT_ACCEPTED

SCIENTIFIC_FROZEN_RULES_CHANGED = NO
PROTOCOL_AUTHORITY_HASH_UNCHANGED
SCIENTIFIC_SEMANTIC_ROOT_UNCHANGED
STRUCTURAL_LEDGER_HASH_UNCHANGED

LIFECYCLE_SEMANTIC_ROOT_V5_FROZEN
LIFECYCLE_GOVERNANCE_AUTHORITY_V5_FROZEN

REPAIR_A_IMPLEMENTATION_PROMPT_READY

IMPLEMENTATION_NOT_YET_CHANGED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Do not claim:

```text
H40_P3_AUTHORIZED
H40_DISCOVERY_AUTHORIZED
REPAIR_A_IMPLEMENTED
REPAIR_B_IMPLEMENTED
NEW_LIFECYCLE_IMPLEMENTATION_AUTHORITY_ACCEPTED
```

---

# BLOCK behavior

If any material ambiguity remains, do not improvise.

Publish a blocker review identifying:

- exact ambiguous contract field/semantic;
- affected finding;
- whether it would change scientific identity or lifecycle identity;
- exact minimal docs-only repair required.

End with:

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_BLOCKED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

and STOP before Phase 2.

---

# Final response required from executor

Return concise operational output:

```text
decision:
acceptance_commit_sha:
hash_materialization_commit_sha:
new_lifecycle_semantic_root_hash:
new_lifecycle_governance_authority_hash:
changed_children:
scientific_identity_changed: NO
implementation_changed: NO
H40_P3_AUTHORIZED: NO
H40_DISCOVERY_AUTHORIZED: NO
next_stage: REPAIR_A_F01_F04
```
