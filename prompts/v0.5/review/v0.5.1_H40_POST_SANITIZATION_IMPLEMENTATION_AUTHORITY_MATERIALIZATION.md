# Agent Prompt — V0.5.1 Post-Sanitization Implementation Authority Materialization

## Status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
SCOPE = EVIDENCE_AGGREGATION_AND_AUTHORITY_CANDIDATE_ONLY
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Accepted implementation baseline:

```text
v0.5-refactor
d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa
```

Accepted post-sanitation Sol review:

```text
reviews/v0.5/V0.5.1_POST_SANITIZATION_SOL_INDEPENDENT_ACCEPTANCE.md
commit 5deb937bcb84c18165de8f1938d0cac65300f1e4
```

Accepted lifecycle governance hash:

```text
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

Do NOT modify v0.5-refactor in this task.

Do NOT set ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH yet.

Do NOT authorize P3 or Discovery.

---

## 1. Mission

Materialize one unambiguous aggregate implementation/test/CI evidence manifest for the exact accepted implementation commit, then construct the exact candidate:

```text
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1
```

for later independent Sol acceptance.

Inputs:

```text
local evidence:
evidence/v0.5/h40/V0.5.1_H40_F01_POST_SANITIZATION_TEST_EVIDENCE_d0e1fe0.json

public CI evidence:
evidence/v0.5/h40/V0.5.1_H40_F01_POST_SANITIZATION_PUBLIC_CI_EVIDENCE.json

implementation/remap acceptance:
reviews/v0.5/V0.5.1_POST_SANITIZATION_SOL_INDEPENDENT_ACCEPTANCE.md
commit 5deb937bcb84c18165de8f1938d0cac65300f1e4
```

All evidence must bind:

```text
tested_commit_sha =
d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa
```

---

## 2. Verify inputs before materialization

Verify:

1. local evidence tested_commit_sha == d0e1fe0...
2. local evidence overall_local_evidence_status == PASS
3. focused lifecycle == PASS
4. execution B04 == PASS
5. full pytest == PASS
6. ruff/mypy/compileall/git diff check == PASS
7. public CI tested_commit_sha == d0e1fe0...
8. public CI run_id == 35744612505
9. event == workflow_dispatch
10. overall conclusion == success
11. compatibility (3.11) == success
12. compatibility (3.12) == success
13. quality (3.13) == success
14. protected-surface attestations remain false/zero
15. current code source has:
    ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None

If any mismatch: STOP.

---

## 3. Aggregate evidence manifest

Create on v0.5-refactor-docs:

```text
evidence/v0.5/h40/V0.5.1_H40_F01_POST_SANITIZATION_AGGREGATE_IMPLEMENTATION_EVIDENCE.json
```

Required semantic object:

```json
{
  "schema_id": "H40_F01_AGGREGATE_IMPLEMENTATION_EVIDENCE_V1",
  "tested_commit_sha": "d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa",
  "lifecycle_governance_authority_hash": "7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84",
  "implementation_acceptance_artifact_path": "reviews/v0.5/V0.5.1_POST_SANITIZATION_SOL_INDEPENDENT_ACCEPTANCE.md",
  "implementation_acceptance_commit_sha": "5deb937bcb84c18165de8f1938d0cac65300f1e4",
  "local_test_evidence": {
    "artifact_path": "evidence/v0.5/h40/V0.5.1_H40_F01_POST_SANITIZATION_TEST_EVIDENCE_d0e1fe0.json",
    "canonical_sha256": "<computed>",
    "status": "PASS"
  },
  "github_hosted_ci_evidence": {
    "artifact_path": "evidence/v0.5/h40/V0.5.1_H40_F01_POST_SANITIZATION_PUBLIC_CI_EVIDENCE.json",
    "canonical_sha256": "<computed>",
    "repository": "EXASHXE/btc-quant-agent-public",
    "run_id": "35744612505",
    "status": "PASS"
  },
  "protected_surface_attestation": {
    "real_h40_outcomes_accessed": false,
    "h39_accessed": false,
    "final_holdout_accessed": false,
    "signed_network_requests": 0
  },
  "aggregate_status": "PASS"
}
```

Compute the two input canonical hashes independently from parsed JSON objects.

Do not trust prose-only reported digests.

---

## 4. Eliminate evidence_manifest_sha256 ambiguity

The aggregate manifest MUST be serialized as exact repository canonical JSON:

```text
json.dumps(
  object,
  ensure_ascii=False,
  allow_nan=False,
  sort_keys=True,
  separators=(",", ":")
)
```

Critical rule:

```text
NO trailing newline
NO leading/trailing whitespace
NO pretty printing
UTF-8 exact bytes
```

Therefore require and prove:

```text
canonical_sha256(parsed_object)
==
sha256(exact_file_bytes)
```

If they differ: STOP.

Define the shared value as:

```text
AGGREGATE_EVIDENCE_MANIFEST_SHA256
```

This exact digest is the candidate value for:

```text
H40RequiredTestCIEvidenceIdentity.evidence_manifest_sha256
```

No semantic ambiguity remains because canonical object bytes and exact stored file bytes are identical.

---

## 5. Construct exact implementation authority candidate

Create:

```text
evidence/v0.5/h40/V0.5.1_H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1_CANDIDATE.json
```

Exact semantic object:

```json
{
  "accepted_lifecycle_governance_authority_hash": "7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84",
  "f01_implementation_acceptance_artifact_path": "reviews/v0.5/V0.5.1_POST_SANITIZATION_SOL_INDEPENDENT_ACCEPTANCE.md",
  "f01_implementation_acceptance_commit_sha": "5deb937bcb84c18165de8f1938d0cac65300f1e4",
  "f01_implementation_commit_sha": "d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa",
  "required_test_ci_evidence_identity": {
    "evidence_manifest_artifact_path": "evidence/v0.5/h40/V0.5.1_H40_F01_POST_SANITIZATION_AGGREGATE_IMPLEMENTATION_EVIDENCE.json",
    "evidence_manifest_sha256": "<AGGREGATE_EVIDENCE_MANIFEST_SHA256>",
    "tested_commit_sha": "d0e1fe0d87d4595640edc4d7ecfa5f287640f0aa"
  },
  "schema_id": "H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1"
}
```

Serialize candidate authority as canonical JSON with no trailing newline.

Compute:

```text
CANDIDATE_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH =
canonical_sha256(candidate authority object)
```

Also verify by instantiating repository:

```text
H40RequiredTestCIEvidenceIdentity
H40LifecycleImplementationAuthority
```

with these exact values.

Required:

```text
authority.lifecycle_implementation_authority_hash
==
CANDIDATE_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
```

---

## 6. Independent consistency checks

Verify from actual repository code:

```text
compute_lifecycle_governance_authority_hash()
=
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

Verify frozen scientific identities remain:

```text
protocol =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Do not use the previously erroneous prose values from the pre-correction migration report.

---

## 7. Candidate report

Create:

```text
reviews/v0.5/V0.5.1_H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1_CANDIDATE_REPORT.md
```

Include:

- exact implementation baseline;
- exact acceptance artifact/commit;
- local evidence path + recomputed canonical hash;
- CI evidence path + recomputed canonical hash;
- aggregate manifest path/hash;
- proof canonical hash == exact file-byte hash;
- candidate authority object path;
- candidate lifecycle implementation authority hash;
- governance hash;
- scientific hashes;
- current accepted constant remains None;
- P3/Discovery remain unauthorized.

Allowed conclusion:

```text
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1_CANDIDATE_MATERIALIZED
PENDING_INDEPENDENT_SOL_ACCEPTANCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Do NOT call the candidate accepted.

---

## 8. No code changes

Do NOT modify:

```text
v0.5-refactor
src/
tests/
.github/workflows/
```

This task is docs/evidence materialization only.

No CI rerun is required.

No additional Astra review is required.

---

## 9. Protected surfaces

Do not access:

```text
real H40 outcomes
real candidate results
real WF outcomes
Confirmation
H39
Final Holdout
signed Binance APIs
```

---

## 10. Git discipline

Work only on:

```text
v0.5-refactor-docs
```

Commit only:

```text
aggregate evidence manifest
candidate implementation-authority JSON
candidate report
```

Suggested commit:

```text
docs(h40): materialize post-sanitation implementation authority candidate
```

Push to public origin.

---

## 11. Final response

Return:

```text
tested_commit_sha:

local_evidence_canonical_sha256:
ci_evidence_canonical_sha256:

aggregate_manifest_path:
aggregate_manifest_canonical_sha256:
aggregate_manifest_file_sha256:
aggregate_hashes_identical:

authority_candidate_path:
candidate_lifecycle_implementation_authority_hash:
typed_schema_verification:

candidate_report_path:
docs_commit_sha:

code_modified: NO
ci_rerun: NO
real_h40_outcomes_accessed: NO
signed_network_requests: 0

final_state:
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1_CANDIDATE_MATERIALIZED
PENDING_INDEPENDENT_SOL_ACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Recommended executor:

```text
Codex GPT-5.6 Sol Medium
```

This task is deterministic evidence/authority serialization, not architecture design.
