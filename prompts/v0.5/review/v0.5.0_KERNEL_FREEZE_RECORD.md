# v0.5.0 Kernel Freeze Record

## Role

You are performing an authority/documentation freeze only. This is not an implementation task, not a new audit, and not an alpha-research task.

Repository: `EXASHXE/btc-quant-agent`

Code branch: `v0.5-refactor`

Required immutable kernel SHA:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

Docs branch: `v0.5-refactor-docs`

## Accepted basis

The following gates already exist and must be treated as evidence, not rerun:

- R05 stage acceptance full compatibility record:
  `reviews/v0.5/V0.5.0_R05_STAGE_ACCEPTANCE_FULL_COMPAT.md`
  - exact code SHA `6838e9db8d5c369b5da87354821d9c8e79c2a879`
  - GitHub Actions run `35001541879`
  - Python 3.11 / 3.12 / 3.13 all PASS
- Sol simplified-kernel adversarial re-gate:
  `reviews/v0.5/V0.5.0_SOL_SIMPLIFIED_KERNEL_ADVERSARIAL_RE_GATE.md`
- Local executable adversarial re-gate:
  `reviews/v0.5/V0.5.0_LOCAL_AGENT_SIMPLIFIED_KERNEL_ADVERSARIAL_RE_GATE.md`
  - report commit `a98b24c7972a7085bb3d6e5d45cb738c696ae97c`
- Sol independent acceptance of the executable re-gate:
  `reviews/v0.5/V0.5.0_LOCAL_EXECUTABLE_RE_GATE_SOL_ACCEPTANCE.md`

Closed findings:

```text
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
NO_NEW_HIGH_OR_MEDIUM
```

Protected state that must remain unchanged:

```text
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
```

## Hard preflight

Fetch `origin/v0.5-refactor` and verify its exact HEAD is still:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

If it differs, STOP and report:

```text
V0_5_KERNEL_FREEZE_ABORTED_HEAD_DRIFT
expected=6838e9db8d5c369b5da87354821d9c8e79c2a879
actual=<sha>
```

Do not freeze another SHA.

Also verify the accepted reports listed above exist on `v0.5-refactor-docs`.

## Strict scope

Do NOT:

- modify `v0.5-refactor`;
- modify production code;
- modify tests;
- modify CI configuration;
- trigger GitHub Actions;
- run `workflow_dispatch`;
- rerun the full test matrix merely for this freeze record;
- access or unblind H39 protected outcomes;
- access Final Holdout;
- enable execution / paper / testnet / live trading;
- start H40 research or alpha search;
- create a code tag if doing so would trigger CI;
- rewrite prior audit/review reports.

This task should consume essentially no CI runtime.

## Required output

Create exactly one new documentation record:

```text
reviews/v0.5/V0.5.0_KERNEL_FREEZE_RECORD.md
```

Commit it to `v0.5-refactor-docs` only.

The record must include:

### 1. Frozen identity

```text
Repository: EXASHXE/btc-quant-agent
Frozen code branch: v0.5-refactor
V0.5_KERNEL_BASELINE_SHA=6838e9db8d5c369b5da87354821d9c8e79c2a879
```

State explicitly that this SHA is the immutable semantic baseline for the accepted simplified research kernel.

### 2. Acceptance chain

List and bind the freeze to:

- R05 acceptance;
- `AG01_CLOSED` / `AG02_CLOSED` / `AG03_CLOSED` / `AG04_CLOSED`;
- exact-head full compatibility run `35001541879`;
- Sol simplified-kernel adversarial re-gate;
- local executable adversarial re-gate;
- Sol independent executable re-gate acceptance.

Do not claim that these gates prove profitability or alpha.

### 3. Frozen semantic boundaries

Record that the accepted kernel freezes, at minimum:

- PIT / information-set semantics;
- deterministic material-input reconstruction;
- producer-contract identity / immutability semantics;
- independent funding-evidence authority and coverage semantics;
- canonical execution/accounting replay;
- CASH and RANDOM_MATCHED benchmark semantics;
- qualification / evidence-chain / P5 promotion authority;
- publication/persistence fail-closed semantics;
- formal-vs-legacy boundary;
- read-only observational GET behavior within the accepted scope;
- protected H39 / Final Holdout separation;
- execution default-disabled boundary.

Future research may add new hypotheses/contracts on top of the baseline, but must not silently mutate these frozen semantics under the same identity.

### 4. Explicit non-claims

State clearly:

```text
NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
NO_LIVE_TRADING_AUTHORIZATION
NO_H39_UNBLIND_AUTHORIZATION
NO_FINAL_HOLDOUT_ACCESS
```

### 5. H40 authorization boundary

After the freeze record is committed successfully, authorize the next research phase only as:

```text
H40_RESEARCH_AUTHORIZED
H40_IMPLEMENTATION_NOT_YET_AUTHORIZED_UNTIL_PROTOCOL_PREREGISTRATION
```

H40 must be defined as an abstention-first / selective medium-horizon research program, not continuous forced LONG/SHORT prediction.

Its conceptual research funnel is:

```text
Regime
  -> Opportunity
  -> Direction evidence
  -> Expected excursion / MAE-MFE / cost-adjusted geometry
  -> confidence / calibration gate
  -> LONG / SHORT / NO_TRADE
```

`NO_TRADE` is the default admissible outcome. Do not hard-code a target such as 99% abstention; coverage must be selected by preregistered research and out-of-sample economics.

H40 must remain research-only and cannot modify the frozen kernel semantics without a separately versioned repair/migration process.

### 6. Final status

If and only if the code SHA has not drifted and all accepted evidence exists, end the freeze record with exactly:

```text
V0_5_KERNEL_FROZEN
V0_5_KERNEL_BASELINE_SHA=6838e9db8d5c369b5da87354821d9c8e79c2a879
R05_ACCEPTED
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
FULL_COMPAT_CI_GREEN_PY311_PY312_PY313
SIMPLIFIED_KERNEL_RE_GATE_GO
LOCAL_EXECUTABLE_RE_GATE_GO
NO_NEW_HIGH_OR_MEDIUM
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
H40_RESEARCH_AUTHORIZED
H40_IMPLEMENTATION_NOT_YET_AUTHORIZED_UNTIL_PROTOCOL_PREREGISTRATION
```

## Model guidance

This task is mechanical and bounded. Either Codex or Gemini is suitable. Prefer the cheaper/faster available model with normal-to-medium reasoning; no high-end architecture reasoning is needed.
