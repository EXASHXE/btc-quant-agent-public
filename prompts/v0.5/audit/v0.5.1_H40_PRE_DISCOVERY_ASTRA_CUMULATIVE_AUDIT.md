# Agent Prompt — V0.5.1 H40 Pre-Discovery Cumulative Scientific & Authority Audit

## Executor

```text
GPT-6 Astra
```

This is a major scientific/authority milestone audit.

Do NOT use this task for implementation work.

---

## 0. Status and hard boundary

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
TASK_TYPE = READ_ONLY_CUMULATIVE_AUDIT
IMPLEMENTATION_CHANGES = FORBIDDEN
SCIENTIFIC_RULE_CHANGES = FORBIDDEN
LIFECYCLE_GOVERNANCE_CHANGES = FORBIDDEN
AUTHORITY_REPUBLICATION_CHANGES = FORBIDDEN
REAL_DISCOVERY_EXECUTION = FORBIDDEN
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required implementation HEAD:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Docs branch:

```text
v0.5-refactor-docs
```

Controller publication acceptance:

```text
reviews/v0.5/V0.5.1_H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_REPUBLICATION_CONTROLLER_ACCEPTANCE.md
```

Controller acceptance docs commit:

```text
0a45886a07335aef3df83bf2223ffa00b03ffb8b
```

Authority republication implementation commit:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Authority republication docs commit:

```text
fb0852882f2333ccf718a56f8d627b059fa9e3f9
```

Published lifecycle implementation authority:

```text
d3a304ddc6bcb7b7fc398ccf45a751a0afa3f91634a09ddff8e199af1970e440
```

Accepted implementation baseline bound by that authority:

```text
d73e8984446f933059b978e36c4cabd55de517aa
```

Final exact-SHA evidence manifest:

```text
evidence/v0.5/h40/V0.5.1_H40_FINAL_REACCEPTANCE_EVIDENCE_d73e898.json
```

Final evidence manifest SHA-256:

```text
5cd591ef7f3f43c7897711ab4e75cb158ac048d328e1f8b0dac28a0a6bcb6540
```

Current state:

```text
P3B_PRODUCTION_VERIFIER_IMPLEMENTATION_REACCEPTANCE_PASS
FINAL_EXACT_SHA_REACCEPTANCE_EVIDENCE_ACCEPTED
FULL_PYTEST_PERFORMANCE_ACCEPTED
LIFECYCLE_IMPLEMENTATION_AUTHORITY_REPUBLICATION_ACCEPTED
NEW_LIFECYCLE_IMPLEMENTATION_AUTHORITY_ACTIVE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

# 1. Mission

Perform an independent, cumulative, adversarial audit of the complete H40 pre-Discovery scientific and authority chain.

The audit question is:

```text
Given the exact repository state at d63c1fbb..., is there any scientific,
PIT/look-ahead, authority, replay, lifecycle, evidence-binding, protected-surface,
or combined-bypass flaw that should block H40 P3 Final Authorization or the
first real Discovery run?
```

This is NOT a routine code review.

Treat previous PASS decisions as evidence to inspect, not facts to trust.

Re-derive critical conclusions independently from code, tests, and content-addressed evidence.

---

# 2. Required audit posture

Use an adversarial research-infrastructure mindset.

Assume a future caller may attempt to:

- substitute a verifier with the same nominal type;
- mutate runtime authority objects after construction;
- replay stale authority/evidence;
- redirect source/evidence roots;
- inject a caller-selected split;
- cross-bind source/split/evidence from another run;
- exploit synthetic/test-only objects in production;
- bypass cold revalidation;
- exploit persisted lifecycle artifacts;
- cross a protected partition boundary;
- observe or use WF1_CALIBRATION data outside authorized semantics;
- induce look-ahead through timestamps, labels, outcomes, or derived caches;
- turn metadata-only/readiness APIs into outcome or execution authority;
- exploit old implementation authority objects after republication;
- combine two individually-safe seams into one bypass.

Do not stop at isolated-unit reasoning. Look for composition attacks.

---

# 3. Repository and provenance guard

Before analysis, verify:

```bash
git rev-parse HEAD
git status --short
git branch --show-current
```

Require implementation checkout:

```text
HEAD = d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

The implementation tree must be clean.

Verify relevant docs/evidence are fetched from the accepted docs lineage containing at least:

```text
0a45886a07335aef3df83bf2223ffa00b03ffb8b
```

Do not audit an uncommitted or mixed tree.

If provenance is ambiguous:

```text
AUDIT_BLOCKED = REPOSITORY_PROVENANCE_AMBIGUOUS
```

---

# 4. Frozen identities to independently verify

Recompute and verify exact equality for:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

accepted lifecycle implementation authority =
d3a304ddc6bcb7b7fc398ccf45a751a0afa3f91634a09ddff8e199af1970e440
```

Any unexplained drift is a blocker.

---

# 5. Reconstruct the published implementation authority

Independently reconstruct:

```text
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1
```

from:

```text
accepted lifecycle governance hash =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

acceptance artifact =
reviews/v0.5/V0.5.1_H40_FINAL_EXACT_SHA_REACCEPTANCE_CONTROLLER_ACCEPTANCE.md

acceptance commit =
0cbb20a7806f2336864e430910bf6a4ff4a99df9

implementation baseline =
d73e8984446f933059b978e36c4cabd55de517aa

final evidence path =
evidence/v0.5/h40/V0.5.1_H40_FINAL_REACCEPTANCE_EVIDENCE_d73e898.json

final evidence SHA-256 =
5cd591ef7f3f43c7897711ab4e75cb158ac048d328e1f8b0dac28a0a6bcb6540

tested_commit_sha =
d73e8984446f933059b978e36c4cabd55de517aa
```

Require reconstructed hash:

```text
d3a304ddc6bcb7b7fc398ccf45a751a0afa3f91634a09ddff8e199af1970e440
```

Verify the production service accepts the exact new authority and rejects the superseded authority.

Audit the non-self-reference architecture:

```text
f01_implementation_commit_sha = accepted implementation baseline
publication_commit_sha = later publication commit
```

Confirm this distinction cannot be abused to authorize untested implementation changes.

---

# 6. Scientific protocol audit

Audit the full scientific contract, not only hashes.

Confirm:

- all scientific rules used by Discovery are frozen before real outcome access;
- candidate universe is structurally predeclared;
- family membership is deterministic;
- no candidate-specific post-outcome parameter introduction exists;
- calibration logic is frozen;
- hard gates are frozen;
- correction policy is frozen;
- bootstrap seed/sampler is frozen;
- coverage population is frozen;
- net-return definition is frozen;
- source/split semantics are frozen;
- later lifecycle stages cannot retroactively redefine Discovery.

Distinguish:

```text
protocol identity
scientific semantic identity
structural search-space identity
lifecycle governance identity
implementation authority identity
```

Look for any path where one identity can silently substitute for another.

---

# 7. Six P3B ambiguity closures

Re-audit all six closures independently.

## 7.1 Fit-invalid correction

Expected authority:

```text
7de7c706...
```

Required semantic:

```text
any fit-invalid REGISTERED member
=> whole family NO_REPRESENTATIVE for both metrics
family remains in correction universe
fixed K = 6 Holm
p = 1 for no representative
```

Audit implementation and tests for exact behavior.

## 7.2 Bootstrap seed identity

Expected authority:

```text
7b2af0e0...
```

Canonical seed preimage:

```text
[
  protocol_authority_hash,
  structural_configuration_hash,
  WF1_CALIBRATION,
  metric_id
]
```

Look for caller-controlled labels, ordering ambiguity, Python-hash dependence, runtime-random seed paths, or version-dependent replay ambiguity.

## 7.3 Proxy net return

Expected authority:

```text
8a016bcf...
```

Required:

```text
gross = action_sign * expm1(r_h)
r_net = gross - 0.0012
```

Check sign, unit, fee timing, and no double subtraction.

## 7.4 Block boundary

Expected authority:

```text
0958dfa8...
```

Required:

```text
source-anchored observation bundles
no synthetic endpoint crossing
```

Audit horizon/end-boundary behavior.

## 7.5 Start sampler

Expected authority:

```text
25d1af12...
```

Required:

```text
PCG64
one rng.integers call
M = 10000
L = 168
NumPy replay version match
```

Look for repeated calls, shape/order differences, implicit dtype differences, or replay drift.

## 7.6 Coverage population

Expected authority:

```text
a9f4d5a0...
```

Required:

```text
WF1_CALIBRATION final LONG/SHORT
over all base-eligible decision rows
active scope only
exact integer 0.25%-10% gates
```

Audit denominator/numerator, NO_TRADE handling, missing rows, and rounding.

Any ambiguity or implementation mismatch in these six closures is HIGH severity.

---

# 8. PIT / look-ahead audit

Trace all dataflow from source timestamps to:

```text
feature creation
raw score
calibration
p_up
final action
r_h
r_net
hard gates
bootstrap metrics
family correction
candidate result
candidate lock
WF validation boundary
```

For each step identify:

- observation timestamp;
- information availability timestamp;
- label/outcome timestamp;
- partition;
- allowed consumer;
- persistence boundary.

Look specifically for:

- future bars used by feature code;
- horizon outcome available before action decision;
- calibration label leakage into score construction;
- source membership including future timestamps;
- split manifests that can be recomputed with later data;
- persisted derived data missing source/split identity;
- cache reuse across partitions/runs;
- ordering/sorting that can incorporate post-event records;
- metadata APIs that leak protected outcome values.

A true or plausible look-ahead path is HIGH severity.

---

# 9. Source / split / runtime seal audit

Audit:

```text
H40SourceManifest
H40SourceValidationReceipt
H40SplitManifest
H40RuntimeSourceSplitAttestation
H40RuntimeSnapshotSeal
materialize_runtime_source_split_authority
cold validation
accepted production roster
```

Verify that production authority cannot be manufactured through:

- caller-supplied counts;
- synthetic source receipts;
- arbitrary runtime snapshot hashes;
- altered roster entries;
- stale source manifests;
- stale split manifests;
- cross-run split reuse;
- source-root substitution;
- symlink/root escape;
- mutable cached template contamination;
- caller-injected expected split authority.

Revisit the R2 test-only caching change specifically and determine whether any cached object can affect production semantics or weaken cold validation.

---

# 10. Production Discovery verifier audit

Audit exact current:

```text
H40ProductionDiscoveryEvidenceVerifier
H40DiscoveryEvidenceResolver
_VERIFIER_BINDINGS
_VERIFIER_RESULTS
_VERIFIER_METHODS
_RESOLVER_METHODS
assert_runtime_integrity
verify_discovery_manifest
verify_candidate
verify_wf_fold
```

Re-test the threat model against:

- exact-type forged instances;
- `object.__new__`;
- partial initialization;
- `object.__setattr__`;
- instance method shadowing;
- class method replacement;
- resolver root mutation;
- seal replacement;
- receipt replacement;
- split replacement;
- transitive authority-context mutation;
- result-cache mutation;
- resolver method replacement;
- stale or foreign bindings;
- cold restore with foreign exact-type verifier;
- persisted candidate lock replay.

Do not demand impossible protection against arbitrary same-process code execution unless the project explicitly claims that security boundary.

Judge against the intended in-process authority-substitution threat model.

---

# 11. Evidence resolver / filesystem audit

Audit content-addressed evidence reads for:

- root containment;
- symlink escape;
- relative/absolute path confusion;
- TOCTOU where materially relevant;
- canonical SHA verification;
- hash-before-parse vs parse-before-hash behavior;
- duplicate/foreign evidence file substitution;
- dependency hash binding;
- candidate result dependency binding;
- evidence root replacement after constructor;
- cold restore file mutation.

Check whether an attacker can preserve a top-level hash while changing semantically consumed dependencies.

---

# 12. Lifecycle audit

Audit lifecycle states and transitions from scaffold through the pre-Discovery boundary.

Focus on:

```text
H40_P1_SCAFFOLDED
H40_DISCOVERY
Candidate Lock
WF validation
termination/replay
durable committed head
lineage
cold restore
```

Verify:

- adjacency alone confers no authority;
- every transition reconstructs required authority;
- predecessor receipt hashes bind correctly;
- run authority cannot be substituted;
- split authority cannot be caller-selected;
- persisted lineage cannot be truncated/reparented/rehashed into acceptance;
- cold restore reruns required verification;
- stale committed heads fail closed;
- active source changes fail closed;
- superseded implementation authority fails closed.

Look for combined bypasses across persistence + resolver + service APIs.

---

# 13. Protected-surface isolation

Audit that the following remain sealed:

```text
real H40 outcomes
WF1_CALIBRATION outcomes except the explicitly authorized future Discovery science path
H39
Confirmation
Final Holdout
execution
```

Important distinction:

The audit itself must NOT open real H40 outcomes or WF1_CALIBRATION outcomes.

You are auditing code/authority boundaries only.

Verify that:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

No H40 lifecycle receipt may enable live/testnet execution.

---

# 14. WF1_CALIBRATION boundary audit

This is a critical pre-Discovery question.

Determine exactly what becomes newly permissible only after explicit:

```text
H40_P3_AUTHORIZED
AND
H40_DISCOVERY_AUTHORIZED
```

Verify there is no currently callable production path that already consumes real WF1_CALIBRATION outcomes merely because the implementation authority is now published.

Audit any:

- Discovery service entrypoints;
- CLI commands;
- helpers;
- restore APIs;
- verifier constructors;
- evidence loaders;
- source artifact readers.

The implementation-authority publication alone must not grant outcome-read authority.

---

# 15. H39 / Confirmation / Final Holdout isolation

Trace all references to:

```text
H39
Confirmation
Final Holdout
unblind/readiness
blind validation
final holdout loaders
```

Verify:

- H40 cannot read these outcomes;
- readiness/metadata APIs do not leak performance;
- no H40 receipt can substitute for unblind authority;
- deprecated diagnostic APIs cannot grant authorization;
- partition labels cannot be caller-forged into protected access.

---

# 16. Execution-disabled audit

Audit all H40-related paths into:

```text
execution service
write authority
exchange/testnet adapters
order submission
position mutation
```

Confirm no path from:

```text
published H40 implementation authority
Discovery authorization
Candidate Lock
WF validation
```

can produce execution authority.

No future P3 authorization should implicitly change execution policy.

Execution authorization must remain a separate explicit authority domain.

---

# 17. Combined bypass matrix

Create and analyze at least the following compositions:

1. old implementation authority + new verifier;
2. new implementation authority + stale evidence manifest;
3. new authority + foreign source root;
4. exact verifier type + mutated transitive seal context;
5. valid candidate lock + altered predecessor receipt;
6. valid persisted artifact + changed active source;
7. synthetic lifecycle service + production seal;
8. production service + synthetic verifier;
9. caller-selected split + valid run authority;
10. stale run authority + newly accepted implementation authority;
11. cached immutable test fixture concepts leaking into production assumptions;
12. metadata/readiness path + protected outcome loader;
13. Discovery authorization receipt + execution API;
14. one-run evidence + another-run resolver root;
15. valid top-level candidate result + mutated dependency evidence.

For each, state:

```text
REJECTED / NOT REACHABLE / AMBIGUOUS / BYPASS
```

and cite exact code/tests.

Any AMBIGUOUS item of medium/high scientific or authority impact blocks authorization.

---

# 18. Evidence-chain audit

Verify the chain:

```text
d73e898 accepted implementation baseline
↓
final exact-SHA evidence manifest
5cd591...
↓
controller acceptance
0cbb20a...
↓
typed implementation authority
d3a304...
↓
publication commit
d63c1fbb...
↓
controller publication acceptance
0a45886a...
```

Confirm no circular identity dependency.

Confirm docs paths and commit identities exist.

Confirm old authority remains historical only.

Note that post-publication runtime logs are summarized in the committed republication report rather than separately content-addressed raw logs. Decide whether this is:

```text
ACCEPTABLE_EVIDENCE_PACKAGING_LIMITATION
or
AUTHORIZATION_BLOCKER
```

Explain why.

Do not invent a stronger evidence requirement retroactively unless it is necessary for scientific/authority correctness.

---

# 19. Tests and targeted runtime checks

You may run tests/read-only commands to verify hypotheses.

Prefer targeted tests first.

At minimum run:

```bash
.venv/bin/python -m pytest -q   tests/test_v051_h40_implementation_authority_publication.py   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   tests/test_v051_h40_p3b_production_verifier_identity_seal.py
```

Run additional focused tests whenever needed for an audit claim.

You do NOT need to rerun the entire full suite unless a finding makes it necessary.

Do not alter test code.

---

# 20. Severity model

Use:

```text
CRITICAL
HIGH
MEDIUM
LOW
INFO
```

Definitions:

```text
CRITICAL:
real protected outcome exposure, execution bypass, direct scientific invalidation,
or authority bypass sufficient to invalidate the experiment.

HIGH:
credible PIT/look-ahead, candidate-selection, verifier, lifecycle, replay,
or evidence-binding flaw that can alter formal outcomes/authorization.

MEDIUM:
real ambiguity or insufficiently sealed behavior that may change formal interpretation,
but no demonstrated direct outcome/authority bypass yet.

LOW:
maintainability/diagnostic weakness with no current formal semantic impact.

INFO:
non-blocking observation.
```

Authorization rule:

```text
Any CRITICAL/HIGH finding => AUDIT_FAIL

Any unresolved MEDIUM finding in scientific semantics, PIT, protected surfaces,
or authority composition => AUDIT_BLOCKED

LOW/INFO alone may still allow AUDIT_PASS
```

---

# 21. Required outputs

Create:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_ASTRA_CUMULATIVE_AUDIT.md
```

Also create machine-readable:

```text
evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_ASTRA_AUDIT.json
```

JSON should include:

- audit_schema_id;
- audited_implementation_sha;
- audited_docs_min_commit;
- published_implementation_authority_hash;
- frozen identities;
- audit areas;
- findings with IDs/severity/status;
- combined bypass matrix;
- protected-surface status;
- PIT/look-ahead status;
- implementation-authority reconstruction status;
- evidence-chain status;
- test commands/results;
- final verdict;
- required repairs if any.

Use deterministic stable JSON formatting.

---

# 22. Final verdict

Exactly one primary verdict:

```text
PRE_DISCOVERY_ASTRA_AUDIT_PASS
PRE_DISCOVERY_ASTRA_AUDIT_BLOCKED
PRE_DISCOVERY_ASTRA_AUDIT_FAIL
```

If PASS:

```text
NO_CRITICAL_FINDINGS
NO_HIGH_FINDINGS
NO_UNRESOLVED_SCIENTIFIC_OR_AUTHORITY_MEDIUM_FINDINGS
ELIGIBLE_FOR_CONTROLLER_P3_FINAL_AUTHORIZATION_REVIEW

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Important:

Astra does NOT grant P3 or Discovery authority.

Only the later controller task may explicitly publish those authorizations.

If BLOCKED/FAIL:

List exact finding IDs and required repair scope.

Do not repair implementation code in this task.

---

# 23. Mandatory commit + push completion rule

This task is NOT complete when the files only exist locally.

After producing the audit report and JSON:

1. Commit them to:

```text
v0.5-refactor-docs
```

Suggested commit:

```text
docs(v0.5.1): record pre-Discovery Astra cumulative audit
```

2. Push the completed docs branch to:

```text
origin/v0.5-refactor-docs
```

3. Verify the remote branch actually contains the new commit, for example:

```bash
git push origin v0.5-refactor-docs
git ls-remote origin refs/heads/v0.5-refactor-docs
```

4. The reported remote SHA must equal the local docs commit SHA.

If push fails, authentication fails, or the remote SHA does not match:

```text
TASK_NOT_COMPLETE
DOCS_PUSH_NOT_VERIFIED
```

Do NOT claim completion until the push is verified.

Do not push or modify `v0.5-refactor` in this audit task.

---

# 24. Final response format

Return:

```text
audited_implementation_sha:
audited_docs_min_commit:
published_implementation_authority_hash:

protocol_identity:
scientific_semantic_root:
structural_ledger:
lifecycle_semantic_root:
lifecycle_governance_authority:

implementation_authority_reconstruction:
old_authority_rejection:
pit_lookahead_audit:
six_p3b_closures:
source_split_runtime_seal:
production_verifier:
evidence_resolver:
lifecycle_replay:
wf1_calibration_boundary:
h39_confirmation_final_holdout:
execution_disabled:
combined_bypass_matrix:
evidence_chain:

targeted_tests:

critical_findings:
high_findings:
medium_findings:
low_findings:
info_findings:

audit_report_path:
audit_json_path:

docs_commit_sha:
push_result:
remote_docs_sha:
remote_push_verified: YES/NO

final_verdict:

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

If `remote_push_verified != YES`, final task state must be:

```text
TASK_NOT_COMPLETE
DOCS_PUSH_NOT_VERIFIED
```

---

# Recommended model

```text
GPT-6 Astra
```

Reason:

```text
This is the first cumulative cross-layer scientific/authority audit after the
implementation authority has been formally republished. It requires adversarial
reasoning across PIT semantics, statistical protocol, content-addressed evidence,
runtime authority, persistence/replay, and protected-surface composition.
This is the intended Astra milestone.
```
