# Agent Prompt — V0.5.1 H40 Pre-Discovery Astra Cumulative Audit R2

## Executor

```text
GPT-6 Astra
```

## Purpose

Continue the interrupted H40 pre-Discovery audit as a **defensive research-correctness and software-assurance review**.

This task is about:
- scientific validity,
- point-in-time correctness,
- lifecycle authorization invariants,
- reproducibility,
- evidence consistency,
- protected-data separation.

It is NOT a penetration-testing task.

Do not construct exploit code, attack demonstrations, privilege-escalation examples, destructive mutations, or generalized bypass techniques.

---

## 0. Required repository state

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required implementation HEAD:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Docs branch:

```text
v0.5-refactor-docs
```

Required docs lineage must contain:

```text
99df7f134395a45391ce922d6b8f2dce267e26a0
0a45886a07335aef3df83bf2223ffa00b03ffb8b
```

Published lifecycle implementation authority:

```text
d3a304ddc6bcb7b7fc398ccf45a751a0afa3f91634a09ddff8e199af1970e440
```

Current authorization state remains:

```text
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Do not change implementation code or tests.

---

## 1. Resume rule

The previous Astra audit was interrupted after substantial progress.

Do NOT restart the entire investigation from zero.

Use the previous transcript/notes if locally available, but independently re-check the source locations needed to support every final finding.

Already completed and only needing lightweight confirmation:

- implementation HEAD/provenance;
- docs lineage;
- frozen identity recomputation;
- final evidence manifest SHA verification;
- required targeted tests;
- execution-disabled tests;
- protected-surface focused tests;
- publication authority reconstruction.

Do not rerun expensive work unless needed to resolve a finding.

---

## 2. Review method

Use only:

- source-code inspection;
- existing tests;
- existing committed evidence and reports;
- read-only repository commands;
- ordinary test execution;
- deterministic identity/hash reconstruction.

Do NOT:

- mutate private runtime fields to create a failure state;
- monkeypatch implementation classes to manufacture counterexamples;
- alter persisted evidence files;
- create scripts whose purpose is to circumvent an authorization check;
- probe external systems;
- access real protected outcomes;
- access real WF1_CALIBRATION outcomes;
- modify implementation/test files.

If an invariant appears absent from code, that is sufficient to record a finding.
You do not need to demonstrate an operational circumvention.

---

# 3. Audit question

Determine whether the exact repository state at:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

is scientifically and operationally ready for the controller to consider:

```text
H40_P3 final authorization
and later
first real Discovery
```

The audit must answer whether all required invariants are explicitly enforced by code and evidence.

---

# 4. Frozen identities

Confirm exact values:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

accepted lifecycle implementation authority =
d3a304ddc6bcb7b7fc398ccf45a751a0afa3f91634a09ddff8e199af1970e440
```

Any unexplained drift is blocking.

---

# 5. Previously identified questions that MUST be resolved

Treat the following as provisional findings from the interrupted audit.
Do not assume they are correct; confirm them through source inspection and existing tests.

## R2-F01 — Controller authorization boundary

Question:

Does the production lifecycle service independently require a controller-level P3/Discovery authorization object or equivalent invariant before it can construct an H40 Discovery authorization receipt/state transition?

Review:

- `H40LifecycleAuthorityService.production`
- `authorize_discovery`
- lifecycle state transition code
- guards
- receipt construction
- all caller entrypoints

Determine one of:

```text
ENFORCED
NOT_ENFORCED
NOT_APPLICABLE_BY_DESIGN
AMBIGUOUS
```

If the design intentionally separates controller authorization from runtime lifecycle authorization, identify exactly where that controller gate is guaranteed before any real outcome read can occur.

Do not create another counterexample script.

## R2-F02 — Source identity versus market-value content

Question:

Does a valid source identity/manifest bind the actual economically relevant source values used by H40 science, or only structural labels/timestamps/metadata?

Trace:

```text
source artifact
validation receipt
manifest identity
feature/science input values
Discovery evidence
```

Confirm whether the exact price/value content consumed by features and outcome calculations is content-addressed or otherwise deterministically bound.

Classify:

```text
CONTENT_BOUND
STRUCTURE_ONLY_BUT_SAFE_BY_OTHER_AUTHORITY
INSUFFICIENT_BINDING
AMBIGUOUS
```

Do not modify source data to test this.

## R2-F03 — Decision-time information invariant

Question:

Does the verifier prove that all score, probability, action, and metric inputs were computable using only information available at the decision timestamp?

Trace:

```text
features
raw score
calibration
p_up
final action
horizon return
net return
coverage
bootstrap metrics
candidate evidence
```

Identify the explicit code/evidence invariant that separates:

```text
decision-time inputs
from
future outcome inputs
```

Classify:

```text
EXPLICITLY_PROVEN
PROVEN_BY_UPSTREAM_CONTENT_IDENTITY
NOT_EXPLICITLY_PROVEN
AMBIGUOUS
```

No future-value modification experiments are needed.

## R2-F04 — Service state and superseded authority consistency

Question:

Can a production service instance ever operate with an implementation-authority object or accepted-authority identity inconsistent with the currently published authority?

Review normal constructor/state invariants and persistence/restore code.

Focus on whether authority fields are:

- immutable after valid construction;
- revalidated before authoritative transitions;
- reconstructed from persisted identity rather than trusted caller state.

Do not mutate private fields or monkeypatch objects.

Classify:

```text
CONSISTENCY_ENFORCED
REVALIDATED_BEFORE_USE
INSUFFICIENTLY_ENFORCED
AMBIGUOUS
```

---

# 6. PIT and scientific audit

Review the ordinary dataflow:

```text
source timestamps
→ feature inputs
→ score
→ calibration
→ probability
→ final action
→ future horizon return
→ net return
→ candidate metrics
→ family correction
```

For each stage state:

- data partition;
- timestamp semantics;
- whether it is decision-time or outcome-time information;
- how the boundary is enforced;
- which content/authority identity binds it.

Look specifically for accidental future-information dependency, not adversarial scenarios.

A credible unresolved future-information dependency is HIGH severity.

---

# 7. Six frozen P3B closures

Confirm code and tests match the accepted semantics for:

1. fit-invalid family correction;
2. bootstrap seed identity;
3. proxy net return;
4. block boundary;
5. start sampler;
6. coverage population.

Use the already accepted closure documents and code.

Do not derive alternate scientific rules.

For each return:

```text
MATCH
MISMATCH
AMBIGUOUS
```

Any MISMATCH is HIGH.

---

# 8. Source / split / runtime authority review

Review normal invariant enforcement for:

```text
H40SourceManifest
H40SourceValidationReceipt
H40SplitManifest
H40RuntimeSourceSplitAttestation
H40RuntimeSnapshotSeal
materialize_runtime_source_split_authority
```

Confirm:

- source membership identity is deterministic;
- split identity is deterministic;
- runtime attestation binds the intended source/split pair;
- cold validation rechecks current source state;
- active-source changes fail closed;
- test-only cached immutable fixtures do not affect production logic.

Do not perform filesystem manipulation experiments.

---

# 9. Production Discovery verifier review

Review:

```text
H40ProductionDiscoveryEvidenceVerifier
H40DiscoveryEvidenceResolver
assert_runtime_integrity
verify_discovery_manifest
verify_candidate
verify_wf_fold
```

Confirm that ordinary production construction and authoritative calls enforce:

- exact production verifier identity;
- correct resolver/root;
- correct runtime seal/receipt/split identity;
- frozen implementation/scientific identities;
- result/dependency consistency;
- cold restore consistency.

Use existing negative tests as evidence.

Do not write new runtime-substitution demonstrations.

---

# 10. Lifecycle and persistence review

Review:

- transition authorization;
- predecessor receipt binding;
- run authority binding;
- persisted committed head;
- lineage;
- restore;
- source revalidation;
- implementation-authority revalidation.

Confirm that a persisted lifecycle object cannot become authoritative solely because it was previously valid; current required identities must still be checked.

Use code and existing tests only.

---

# 11. Protected-data boundaries

Confirm all remain closed before explicit controller authorization:

```text
real H40 outcomes
WF1_CALIBRATION outcome access
H39
Confirmation
Final Holdout
execution
```

Do not open/read protected outcomes.

Identify the exact production code boundary that prevents each read.

Important question:

Does publication of the implementation authority alone make any protected outcome path callable?

Expected answer must be demonstrated from code:

```text
NO
```

or recorded as a blocking finding.

---

# 12. Execution separation

Confirm:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

Trace whether any H40 receipt/state can alter these values or satisfy execution-write authorization.

This is an architectural separation review only.

---

# 13. Evidence chain

Confirm this lineage:

```text
d73e898 accepted tested baseline
→ final exact-SHA evidence manifest 5cd591...
→ controller acceptance 0cbb20a...
→ lifecycle implementation authority d3a304...
→ publication commit d63c1fbb...
→ controller publication acceptance 0a45886a...
```

Confirm no circular dependency.

Also note:

Post-publication test results are summarized in the committed republication report rather than a second raw-log evidence manifest.

Classify this as:

```text
ACCEPTABLE_PACKAGING
or
BLOCKING_EVIDENCE_GAP
```

Do not introduce a new evidence standard unless correctness genuinely requires it.

---

# 14. Existing tests to run

At minimum:

```bash
PYTHONPATH=src .venv/bin/python -m pytest -q   tests/test_v051_h40_implementation_authority_publication.py   tests/test_v051_h40_p3b_production_verifier_behavior_seal.py   tests/test_v051_h40_p3b_production_verifier_identity_seal.py
```

And:

```bash
PYTHONPATH=src .venv/bin/python -m pytest -q   tests/test_execution.py   tests/test_execution_environment_authority.py
```

Run other existing focused tests only when needed to support a conclusion.

Do not add or edit tests.

---

# 15. Finding severity

Use:

```text
CRITICAL
HIGH
MEDIUM
LOW
INFO
```

For this audit:

- CRITICAL: protected data or execution is directly reachable contrary to the protocol.
- HIGH: scientific/PIT/authorization invariant is absent or inconsistent in a way that can invalidate H40 results.
- MEDIUM: meaningful ambiguity requiring clarification/repair before formal authorization.
- LOW: non-blocking maintainability/evidence weakness.
- INFO: observation only.

Decision:

```text
Any CRITICAL/HIGH => PRE_DISCOVERY_ASTRA_AUDIT_FAIL
Any unresolved scientific/PIT/authorization MEDIUM => PRE_DISCOVERY_ASTRA_AUDIT_BLOCKED
Only LOW/INFO => PRE_DISCOVERY_ASTRA_AUDIT_PASS
```

---

# 16. Output artifacts

Create on docs branch:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_ASTRA_CUMULATIVE_AUDIT_R2.md
```

and:

```text
evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_ASTRA_AUDIT_R2.json
```

The JSON must include:

- audited implementation SHA;
- docs lineage;
- frozen identities;
- R2-F01..R2-F04 disposition;
- PIT/scientific status;
- six P3B closure status;
- source/split/runtime authority status;
- production verifier status;
- lifecycle/persistence status;
- protected-data status;
- execution-separation status;
- evidence-chain status;
- tests run/results;
- findings;
- final verdict;
- repair scope if blocked/failed.

No implementation changes.

---

# 17. If a blocking issue is found

Do not repair it.

For every blocking finding provide:

```text
finding_id
severity
invariant_expected
what_code_currently_enforces
what_is_missing
affected files/functions
minimum repair scope
required regression test concept
whether scientific identities would need to change
```

Prefer a narrow repair plan.

Do not provide generalized circumvention instructions.

---

# 18. Mandatory commit + push

The task is not complete until the audit artifacts are remotely visible.

After writing the two files:

```bash
git add reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_ASTRA_CUMULATIVE_AUDIT_R2.md         evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_ASTRA_AUDIT_R2.json

git commit -m "docs(v0.5.1): record pre-Discovery Astra audit R2"

git push origin v0.5-refactor-docs

git ls-remote origin refs/heads/v0.5-refactor-docs
```

Required:

```text
remote docs SHA == local docs commit SHA
```

If push is not verified:

```text
TASK_NOT_COMPLETE
DOCS_PUSH_NOT_VERIFIED
```

Do not claim completion.

Do not modify or push `v0.5-refactor`.

---

# 19. Final verdict

Exactly one:

```text
PRE_DISCOVERY_ASTRA_AUDIT_PASS
PRE_DISCOVERY_ASTRA_AUDIT_BLOCKED
PRE_DISCOVERY_ASTRA_AUDIT_FAIL
```

Even on PASS:

```text
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Astra only audits.
The controller makes the later authorization decision.

---

# 20. Final response format

```text
audited_implementation_sha:
published_implementation_authority_hash:

R2_F01_controller_authorization_boundary:
R2_F02_source_value_binding:
R2_F03_decision_time_information:
R2_F04_service_authority_consistency:

pit_scientific_status:
six_p3b_closures:
source_split_runtime_authority:
production_discovery_verifier:
lifecycle_persistence:
protected_data_boundaries:
execution_separation:
evidence_chain:

critical_findings:
high_findings:
medium_findings:
low_findings:
info_findings:

repair_required:
repair_scope:

targeted_tests:

audit_report_path:
audit_json_path:

docs_commit_sha:
remote_docs_sha:
remote_push_verified: YES/NO

final_verdict:

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```
