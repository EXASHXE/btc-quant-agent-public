# v0.5.1 H40R3R1 — Protocol R3 Determinism Completion

## Role

You are a bounded **docs-only preregistration repair author**. Repair the remaining determinism defects in H40 Protocol R3 before any P2R1 code repair is allowed.

Repository:

```text
EXASHXE/btc-quant-agent
```

Code branch:

```text
v0.5-refactor
```

Docs branch:

```text
v0.5-refactor-docs
```

Required unchanged code HEAD:

```text
52ac1dcb0843458b1b20b943be920f139dc832d1
```

Required docs authority chain must include exactly:

```text
BASE preregistration:
8b36cfde2ac14a37ed4eb244a8b5ae88882765b4

R1 amendment:
59e9fe0ba361557c5df56f0d8f8afaa781cf0071

R2 amendment:
4bdeb1a0102b3cb374e8ec7d01b41a79dcc8b021

Protocol acceptance through R2:
b9c8531e719fd4f10cf27301719c9e066b95be13

P1 accepted code baseline:
3fc89541ed0965fc0e2972af310e34ae1b838168

P1 final acceptance:
95ab819d5300312b4d493b9585b4b369621504b6

R3 design prompt:
18932b91fda432faa435da955102885bea40e9cd

R3 amendment under repair:
e45899bb0118b14127bc765f49315109cfd94ff0

R3 Sol independent review:
afdcc30fbd86278df0f2e9ad370566dc4b62a2ee
```

The cumulative schema remains intended to become:

```text
H40_PROTOCOL_V1_R3
```

but **R3 is not accepted yet**. This R3R1 document is a repair companion to the pending R3 amendment; it does not authorize code work by itself.

## Mandatory first reads

Read the exact committed versions of:

```text
reviews/v0.5/V0.5.1_H40_SELECTIVE_MEDIUM_HORIZON_DIRECTIONAL_EDGE_PREREGISTRATION.md
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R1.md
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R2.md
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3.md
reviews/v0.5/V0.5.1_H40_PROTOCOL_R3_SOL_INDEPENDENT_REVIEW.md
reviews/v0.5/V0.5.1_H40_P2_SOL_INDEPENDENT_REVIEW.md
```

Also inspect the accepted P1 authority code only as needed to preserve canonical source/split authority. Do **not** read outcomes.

## Hard prohibitions

Do NOT:

- modify `v0.5-refactor`;
- repair P2 code yet;
- compute H40 labels or forward returns;
- compute MFE/MAE realizations;
- inspect precision, expectancy, PF, ECE, Brier, p-values, HAC, bootstrap, coverage, drawdown, or backtest outputs;
- inspect WF1 validation / WF2–WF4 outcomes;
- inspect confirmation outcomes;
- inspect H39 protected outcomes;
- inspect Final Holdout;
- use historical performance to choose a threshold, variant, state boundary, or sign convention;
- expand the 168-row budget;
- add a direction family, pair, primary horizon, or confidence threshold;
- alter the accepted P1 source-authority facts;
- enable execution.

Any outcome/performance access is a hard stop:

```text
H40_R3R1_OUTCOME_EXPOSURE_ABORT
```

## Required output

Create only:

```text
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3R1.md
```

Commit it to:

```text
v0.5-refactor-docs
```

Do not edit R3 in place. R3 + R3R1 will be reviewed cumulatively.

---

# Required repairs

## 1. Close R3-SOL-01: exact regime and opportunity numeric gates

BASE already states:

```text
R_VOL rolling percentile allowed cutoffs = {0.20, 0.40, 0.60, 0.80}
Opportunity output = NONE | WATCH | ELIGIBLE
Only ELIGIBLE proceeds
Numeric boundaries belong to the full configuration
```

R3 currently specifies percentile transforms but not exact pass/state boundaries.

R3R1 must freeze, with no implementation choice remaining:

### R_VOL_RANGE_V1_24H

Define exactly:

```text
numeric percentile input domain
all state labels
all state interval boundaries
which boundary endpoint owns equality
which state(s), if any, pass the regime owner into downstream gates
behavior for NaN/missing/out-of-domain values
fit window and one-decision lag semantics
```

Any percentile cutoff used must be chosen pre-outcome. Prefer the already BASE-authorized cutoff set and the smallest symmetric rule. Do not create performance-driven special cases.

### O_RANGE_EXPANSION_V1_24H

Define exactly:

```text
numeric transform
NONE interval
WATCH interval
ELIGIBLE interval
equality behavior at every boundary
only-ELIGIBLE proceed rule
NaN/missing behavior
training fit and one-decision lag semantics
```

If numeric boundaries were not previously fixed, choose them now solely by simplicity/symmetry and document that they were chosen without outcome access. Once committed, they are immutable H40 V1 protocol semantics.

Bind all boundaries into the opportunity/regime contract hashes and each applicable configuration identity.

## 2. Close R3-SOL-02: exact base direction for every single-family D5 variant

R3 currently freezes only the base **family** for the single-family D5 variants. That is insufficient because D1 and D2 each have two exact variants.

For each of:

```text
D5_V1_CROWDING_Q10_D1
D5_V2_CROWDING_Q20_D2
```

freeze exactly:

```text
base_direction_family
base_direction_variant_id
base_direction_contract_id
base_direction_feature_id
base lookback/range
side-generation operator
D5 crowding quantile
funding filter/veto rule
conflict behavior
missing behavior
required_feature_ids including the exact base direction feature
source projection
content-hash preimage
```

Prefer reuse of already-frozen first variants (`D1_V1_RETURN_4H` and `D2_V1_BREAKOUT_24H`) if no protocol conflict exists, because that is the smallest pre-outcome construction and aligns with existing pair semantics. If a different choice is necessary, justify it from protocol semantics only; never use outcomes.

A D5 row missing or mismatching any explicit base identity must fail closed with `UNAUTHORIZED_FAMILY`. No factory/default inference is authorized.

Preserve:

```text
funding never supplies side
D5 q values remain exactly 0.10 / 0.20
no third single-family D5 variant
D3+D5 pair uses its pair-scoped D3-bound D5 component
```

## 3. Close R3-SOL-03: exact D4 mathematical and target-scope projection

Freeze deterministic mathematical definitions for both:

```text
D4_V1_CONFIRMATION_4H
D4_V2_DIVERGENCE_8H
```

At minimum define:

```text
exact BTC return/state formula
exact ETH return/state formula
window endpoints and closed-bar indexing
normalization/fit, if any
target product projection for BTC_ONLY
target product projection for ETH_ONLY
per-product behavior for POOLED_BTC_ETH
positive/negative sign meaning
zero/equality behavior
agreement/disagreement behavior
missing-component behavior
whether divergence is continuation or reversal
exact output schema
```

For confirmation, state explicitly what happens when BTC and ETH signs disagree, when one side is zero, and when both agree.

For divergence, state an exact target-relative formula or another single formula that independently implementing agents will reproduce bit-for-bit. Do not leave the meaning of `BTC minus ETH` versus `target minus other` implicit.

Bind these formulas and scope-projection semantics into D4 feature/direction contract hashes.

## 4. Close R3-SOL-05: complete the full canonical regime/opportunity registry

The original R3 prompt requires one canonical semantic contract identity for every exact regime family:

```text
R_VOL_RANGE
R_TREND_RANGE
R_LIQ_ACTIVITY
R_CROSS_ASSET
R_FUNDING_CROWDING
```

and opportunity family:

```text
O_RANGE_EXPANSION
O_COMPRESSION_RELEASE
O_BREAKOUT_DISTANCE
O_EXPECTED_EXCURSION
O_TPBR_MAGNITUDE
```

R3R1 must fully freeze the remaining registry-only contracts even though the active 168-row V1 search uses only R_VOL_RANGE + O_RANGE_EXPANSION.

For each registry-only contract define at minimum:

```text
contract_id + version
owner family
semantic meaning
feature IDs
exact allowed/frozen lookback parameterization
raw/material source logical IDs
source projection rule
transform parameters
numeric state/threshold semantics where applicable
normalization/fit ownership
available_at / closed-bar semantics
missing-data behavior
product/scope compatibility
cross-asset or funding requirements
output schema
contract content-hash preimage
ACTIVE_IN_168_LEDGER = false
```

These contracts must **not** silently add configurations to the 168-row search. They are frozen registry authority only.

For `O_TPBR_MAGNITUDE`, historical direction remains discarded absolutely. Its schema must contain magnitude/opportunity evidence only.

## 5. Close R3-SOL-04: make the semantic hash hierarchy bitwise reproducible

### 5.1 Protocol authority identity

Define an exact, non-ambiguous procedure for producing the cumulative accepted `H40_PROTOCOL_V1_R3` authority identity after R3R1 commit and independent acceptance.

Avoid a self-referential document hash. A valid design is to define the preimage algorithm now and have the independent acceptance record the exact final commit/content identities and resulting protocol authority hash.

The procedure must bind at minimum:

```text
schema_id = H40_PROTOCOL_V1_R3
frozen kernel SHA
BASE exact authority identity
R1 exact authority identity
R2 exact authority identity
R3 exact authority identity
R3R1 exact authority identity after commit
P1 accepted baseline identity
P1 final acceptance identity
```

Specify whether each authority identity is a Git commit SHA, Git blob SHA, SHA-256 of raw UTF-8 bytes, or another exact representation. Do not say merely `identity/hash`.

The post-R3R1 independent acceptance must be able to publish one exact `protocol_authority_hash` consumed by P2R1 production materialization.

### 5.2 Registry membership and ordering

Freeze exactly what enters:

```text
full_authorized_registry_hash
active_168_registry_hash (if distinct)
```

If only one registry hash is used, list its exact membership. Every included contract must be fully specified.

Freeze deterministic ordering by stable contract identity. No implementation may choose registry inclusion.

### 5.3 Numeric canonicalization

Correct the R3 statement that implied repository `canonical_json` performs float rounding. It does not.

Freeze exact rules such as:

```text
- use repository canonical_json exactly for object/list/string/integer/boolean/null serialization;
- no implicit float rounding inside canonical_json;
- every semantic numeric field has one canonical representation before hashing;
- finite enumerated thresholds/quantiles may be represented as exact canonical decimal strings or explicitly normalized numeric literals, but choose one representation and freeze it;
- NaN/Infinity are forbidden;
- duration/lookback fields use integer hours;
- percentage/bps identities use one fixed canonical unit;
```

The rule must permit two independent implementations to produce identical contract/configuration/ledger hashes.

### 5.4 Ordered versus set-valued fields

Freeze explicitly which fields preserve semantic order and which are sorted as sets, including:

```text
family_combination
required_feature_ids
product_scope
source sets
contract membership lists
168 slot list
```

The configuration identity must continue excluding `slot_index`; `slot_hash` must include it; duplicate semantic `configuration_hash` must fail closed.

## 6. Reason/state vocabulary cleanup

Do not invent ambiguous global reason codes while repairing the protocol.

Clarify:

```text
INSUFFICIENT_CELL_N = geometry-owner output/state unless explicitly added to the stable reason vocabulary later
protected/outcome feature violations use an existing authoritative H40ReasonCode unless a protocol-authorized new reason is explicitly frozen
```

In particular, do not depend on an undefined `FEATURE_CORRUPTED` enum value unless R3R1 intentionally and explicitly authorizes that vocabulary change. Prefer existing `PROTECTED_SURFACE_DENIED`, `PIT_UNAVAILABLE`, `SOURCE_UNVERIFIED`, etc. where semantically correct.

---

# Preserve accepted R3 elements

Do not reopen these unless a direct logical conflict requires a minimal correction:

```text
10 single-family variant order
4 pair order
12-row template structure and 168 slot ordering
P1 source authority matrix
18 REGISTERED / 150 NOT_TESTABLE derived current count
structural inclusion vs runtime testability distinction
D3+D5 pair-scoped D3-bound D5 component
no implicit D5 base-owner injection
no slot replacement/reallocation
transitive content-hash principle
confirmation/H39/Final Holdout seals
EXECUTION_DISABLED
```

Do not use the existing P2 code as authority. Where code conflicts with cumulative R3+R3R1, later P2R1 must change code.

---

# Required self-audit before commit

Prove from cumulative R3 + R3R1 alone that:

1. exact R_VOL state boundaries/pass semantics are unique;
2. exact O_RANGE NONE/WATCH/ELIGIBLE boundaries are unique;
3. every D5 single-family variant has one exact base direction variant/feature;
4. every D4 row has one exact target-product side formula in every scope;
5. all five regime and all five opportunity canonical registry contracts are fully specified;
6. registry membership and ordering are unique;
7. protocol-authority hash procedure is executable and non-self-referential;
8. numeric canonicalization has no implicit rounding ambiguity;
9. every material semantic mutation changes a content hash that reaches configuration/ledger identity;
10. exactly 168 active rows remain; no registry-only contract adds a slot;
11. P1 source authority and 18/150 current testability count remain unchanged unless a purely semantic dependency correction mechanically changes the source requirement — if it does, explain and recompute from authority only, not outcomes;
12. no labels/performance/protected outcomes were accessed.

If any item cannot be frozen without outcome access, stop and report the unresolved conflict rather than inspecting outcomes.

---

# Model routing

```text
PREFERRED PRIMARY AUTHOR
GPT-5.6 Sol High or GLM-5.3 High

ACCEPTABLE COST-SAVING AUTHOR
GLM-5.2 with Deep/High reasoning, provided it reads the full authority chain

NOT RECOMMENDED FOR SCIENTIFIC CHOICES
Gemini Flash-class model

CHEAPER-MODEL-SAFE SUBTASKS ONLY
- markdown formatting after semantics are frozen
- row/count verification
- deterministic table cleanup

INDEPENDENT ACCEPTANCE REVIEW
GPT-5.6 Sol High in a separate pass

ASTRA
Not required for R3R1; reserve for candidate-LOCK / confirmation-unblind authority gates
```

---

# Success markers

Only if all repairs are fully closed include:

```text
H40_PROTOCOL_V1_R3R1_REPAIR_COMPLETE_PENDING_INDEPENDENT_REVIEW
H40R3R1_REGIME_OPPORTUNITY_NUMERIC_GATES_FROZEN
H40R3R1_D5_BASE_VARIANTS_FROZEN
H40R3R1_D4_SCOPE_PROJECTION_FROZEN
H40R3R1_FULL_REGISTRY_FROZEN
H40R3R1_PROTOCOL_HASH_PROCEDURE_FROZEN
H40R3R1_CANONICAL_NUMERIC_SERIALIZATION_FROZEN
H40_NO_LABELS_COMPUTED
H40_NO_PERFORMANCE_METRICS_COMPUTED
H40_NO_CANDIDATE_SELECTED
H40_P2_NOT_ACCEPTED
H40_P2R1_NOT_AUTHORIZED_PENDING_R3R1_REVIEW
H40_P3_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

After commit, report the exact docs commit SHA. Do not modify code and do not start P2R1 until an independent Sol review explicitly accepts cumulative R3 + R3R1.
