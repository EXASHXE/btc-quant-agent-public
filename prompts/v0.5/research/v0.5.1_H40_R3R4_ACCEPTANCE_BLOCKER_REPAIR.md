# H40 R3R4 Acceptance Blocker Repair Prompt

## Role

Act as implementation-neutral protocol repair agent.

Do NOT implement P2R1 code yet.

Your task is to repair only the remaining H40 R3R4 protocol acceptance blockers identified by independent review.

## Repository

Repository:
EXASHXE/btc-quant-agent

Docs branch:
v0.5-refactor-docs

Code branch:
v0.5-refactor (read-only)

## Objective

Move:

H40_PROTOCOL_V1_R3_PENDING_FINAL_CUMULATIVE_ACCEPTANCE

into:

H40_PROTOCOL_V1_R3_FINAL_ACCEPTED

Only if all remaining reproducibility blockers are closed.

## Required fixes

### 1. Authority publication

Create a final acceptance artifact only after all protocol identities are reproducible:

- protocol_authority_hash
- semantic_root_hash
- configuration_ledger_identity

Clearly separate:

- structural protocol identity
- runtime authority snapshot

A source promotion must not mutate historical structural identity.

### 2. Configuration reconstruction

Verify every active configuration can be rebuilt uniquely from:

- direction owner
- horizon
- scope
- regime
- opportunity
- geometry
- calibration
- threshold
- feature semantics

No implicit defaults.

### 3. Statistical decision closure

Verify:

- calibration only changes confidence;
- direction ownership cannot be reversed;
- ECE/p0/correction/ranking semantics are deterministic;
- RANDOM_MATCHED is reproducible.

### 4. Boundary

Do not:

- modify v0.5-refactor code;
- implement P2R1;
- run backtests;
- inspect outcomes;
- expand search space.

## Output

If accepted, create:

reviews/v0.5/V0.5.1_H40_R3R4_SOL_FINAL_CUMULATIVE_ACCEPTANCE.md

Containing:

H40_PROTOCOL_V1_R3_FINAL_ACCEPTED

protocol_authority_hash:
<value>

semantic_root_hash:
<value>

configuration_ledger_identity:
<value>

P2R1_AUTHORIZATION:
AUTHORIZED

If not accepted:

Only document exact blockers. Do not redesign H40.
