# Task Prompt — H40 P3R0R1 Lifecycle Evidence-Authority Acceptance-Blocker Repair

## Role

Act as the protocol/governance engineer for H40.

This task is a bounded docs-only repair of the rejected P3R0 lifecycle evidence-authority closure. Do not implement Python, do not run discovery, and do not inspect any H40 outcome data.

Repository:

~~~text
EXASHXE/btc-quant-agent
~~~

Code branch — read-only:

~~~text
v0.5-refactor
~~~

Docs branch — only branch you may modify:

~~~text
v0.5-refactor-docs
~~~

## Mandatory preflight

Verify:

~~~text
code HEAD:
cfefdbabfeef8ab004e43bbda24038e4d5e3fca0

rejected P3R0 closure:
96da14c22c8515431295687c3648052a390313c8

independent review:
4809fbb4c01ba78cb58de4854051204db292599b

review artifact:
reviews/v0.5/V0.5.1_H40_P3R0_SOL_INDEPENDENT_REVIEW.md
~~~

Read the rejected closure and independent review completely before editing.

If the exact code HEAD has moved, stop and report the mismatch. Do not silently reinterpret this protocol repair against a different executable baseline.

---

# Current accepted state

P2 is final accepted.

Scientific identities remain exactly:

~~~text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
~~~

Do not change them.

Current stage:

~~~text
H40_P2_FINAL_ACCEPTED
H40_P3R0_NOT_ACCEPTED
H40_P3R0R1_ACCEPTANCE_BLOCKER_REPAIR_REQUIRED
H40_FSA_F01_NOT_CLOSED_FOR_IMPLEMENTATION
H40_P3_NOT_AUTHORIZED
H40_FSA_F02_OPEN_SEALED
EXECUTION_DISABLED
~~~

---

# Objective

Repair only the blockers identified in the independent review so that lifecycle evidence authority is:

1. cryptographically bound to its own lifecycle semantics;
2. implementable without stale/self-contradictory code identities;
3. incapable of silently reopening R3R4 discovery/statistical semantics;
4. based on authoritative evidence rather than self-asserted booleans/paths;
5. deterministic enough for two independent implementers to build compatible verification logic.

Do not broaden the task into a redesign of H40.

---

# Hard information boundary

Do not compute, inspect, load, derive, summarize, or use real H40:

- labels;
- forward returns;
- realized MFE/MAE;
- precision / expectancy / PF;
- ECE / Brier / bootstrap / HAC outcomes;
- candidate rankings;
- WF validation results;
- confirmation outcomes;
- H39 protected outcomes/artifacts;
- Final Holdout data/outcomes.

Do not run any discovery/backtest/WF/confirmation/execution runner.

---

# Required repairs

## 1. Replace the incomplete lifecycle governance root

The rejected P3R0 hash dbae0f9f... is mathematically correct for its printed preimage but is not an authoritative lifecycle root because it does not bind lifecycle semantics.

Define a canonical lifecycle semantic hash tree.

At minimum define canonical content objects and child hashes for:

~~~text
discovery_authorization_receipt_contract
candidate_lock_receipt_contract
wf_validation_receipt_contract
confirmation_ready_receipt_contract
termination_receipt_contract
transition_matrix_contract
persistence_replay_contract
runtime_snapshot_seal_contract
f02_boundary_contract
~~~

Every material semantic field of each object must be present in its canonical preimage. Textual IDs alone are insufficient.

Define:

~~~text
lifecycle_semantic_root_hash =
canonical_sha256(
    canonical object of named child content hashes
)
~~~

The lifecycle semantic root is completely separate from the scientific semantic_root_hash.

### Final governance authority construction

Do not self-reference the repair commit.

The repair document must freeze the exact algorithm/preimage shape for a later independent acceptance to publish the final:

~~~text
lifecycle_governance_authority_hash
~~~

That post-commit acceptance hash must bind at least:

~~~text
schema_id
protocol_authority_hash
semantic_root_hash
structural_ledger_hash
P2 final acceptance identity
P3R0R1 amendment commit identity
lifecycle_semantic_root_hash
~~~

The repair itself may publish independently computable child/root semantic hashes if they do not require the future repair commit SHA.

The later independent acceptance publishes the final governance authority hash once the repair commit exists.

---

## 2. Fix the executable code authority boundary

Do not require future discovery code to equal the pre-F01 HEAD cfefdbab....

Freeze two distinct concepts:

~~~text
p2_accepted_code_baseline
lifecycle_implementation_authority
~~~

The first is historical lineage only.

The second is resolved only after F01 is implemented and independently accepted.

Define an exact future implementation-authority object whose canonical preimage binds at minimum:

~~~text
accepted lifecycle_governance_authority_hash
exact F01 implementation commit
F01 implementation acceptance commit/artifact identity
required implementation test/CI evidence identity if used as authority
~~~

Do not guess future SHAs in this repair.

Discovery entry must later bind the accepted lifecycle_implementation_authority_hash, not the historical cfefdb... code HEAD as the executable code identity.

Also repair the H40_PREREGISTERED -> H40_P1_SCAFFOLDED transition rule so current executable HEAD is not required to equal the historical P1 baseline commit.

---

## 3. Restore exact R3R4 candidate-selection authority

The rejected P3R0 accidentally changed scientific selection semantics.

Explicitly supersede those incorrect clauses.

Preserve exactly:

~~~text
within-family correction universe =
REGISTERED rows of that family in the sealed snapshot

PRECISION and NET_EXPECTANCY max-statistic corrections are separate

cross-family Holm applies to family representatives

final selection order:
1. pass every accepted hard gate
2. highest adjusted LCB net expectancy
3. highest adjusted LCB precision
4. lower complexity
5. ascending canonical structural_configuration_hash / configuration ID

no runner-up promotion
~~~

Do not use:

~~~text
global M = 18 FWER
ascending slot_index final tie
~~~

unless those appear only as explicitly rejected/superseded text.

Lifecycle governance must reference/bind the accepted discovery-selection content hash rather than restating new statistics.

Accepted canonical contract:

~~~text
family = DISCOVERY_SELECTION
contract_id = DISCOVERY_SELECTION_V1

discovery_selection_correction_contract_hash =
f84c97050c7db813263e5ffda6b1c556af0b7b0e2876bb8616640d2c9d67084b
~~~

If you independently recompute this hash, use repository canonical_json/canonical_sha256 semantics exactly.

---

## 4. Make transition evidence authoritative, not merely hash-consistent

A typed receipt is not authority merely because the caller can hash its own claims.

Repair the model so every privileged post-outcome transition has two layers:

~~~text
canonical result/evidence receipt
        ↓ verified/recomputed
transition authorization receipt
~~~

### Candidate lock

Define a canonical Discovery Result / Selection Evidence contract sufficient for the verifier to establish that the candidate was selected under the exact accepted discovery-selection contract and sealed discovery universe.

The CandidateLockReceipt must bind that authoritative result evidence hash.

A caller-supplied:

~~~text
selection_rank = 1
summary path
summary hash
~~~

must not by itself establish authority.

### Walk-forward validation

Define canonical WF validation result evidence sufficient for the verifier to establish:

- exact locked candidate;
- exact split authority;
- exact fold identities;
- accepted validation contract/gate identities;
- pass/fail determination.

The transition verifier must derive/verify pass status from canonical result evidence.

A caller-supplied:

~~~text
all_wf_gates_passed = True
~~~

must not be sufficient authority.

### Important scope rule

Do not compute real results now.

Freeze schemas, lineage, verifier obligations, and canonical hashes only.

---

## 5. Correct accepted identity and lineage defects

Correct all of the following explicitly.

### R3R4 ratification commit

Correct:

~~~text
R3R4 Sol cumulative acceptance:
cf69d5295e2b0227920e265d9afe7dcf831af44e

R3R4 independent ratification:
9a00782f1d8cc3fb328a6e36e5febf402d49b226
~~~

Do not add the ratification commit into the old scientific protocol hash preimage.

### Horizons

Correct preserved scientific authority to:

~~~text
primary horizons = 4h, 8h, 12h
diagnostic horizon = 24h
72h = feature/lookback window only
~~~

### Discovery receipt field set

The rejected artifact lists 21 fields but says 20.

In the repaired protocol, the normative source of truth is the exact canonical field-name set. Do not rely on an inconsistent prose count.

### Discovery-selection textual identity

Do not introduce DISCOVERY_SELECTION_CORRECTION_V1 as a new scientific contract.

Bind the accepted content hash. If a textual diagnostic value is used, it must be DISCOVERY_SELECTION_V1.

---

## 6. Freeze run-instance authority and timestamp semantics

A free-form caller run_id must not be the sole replay boundary.

Define one exact run authority rule.

Preferred approach:

~~~text
run_authority_id =
canonical hash derived from accepted lifecycle implementation authority
+ sealed materialized_run_authority
+ authoritative split attestation
+ accepted lifecycle/discovery authorization inputs
~~~

If a separate run-instance issuance nonce/id is truly needed, define exactly who/what issues it, its persistence semantics, and why it cannot create research degrees of freedom.

For timestamps:

- define exact ISO-8601 UTC normalization;
- define whether timestamps are hash-bound audit metadata or authorization-critical;
- if authorization-critical, define exact comparison semantics;
- do not use unspecified wall-clock tolerance.

---

## 7. Preserve F02 boundary

F02 remains separate.

The repaired F01 protocol may define H40ConfirmationReadyReceipt and its lineage, but:

~~~text
H40_CONFIRMATION_READY
  -> H40_CONFIRMATION_EVALUATED_ONCE
~~~

remains prohibited.

Do not invent a confirmation nonce, unlock key, or one-shot evaluation authority in this repair.

---

# Required repaired artifact

Create a new superseding artifact rather than mutating history.

Suggested path:

~~~text
reviews/v0.5/V0.5.1_H40_P3R0R1_LIFECYCLE_EVIDENCE_AUTHORITY_REPAIR.md
~~~

It must explicitly state which clauses of 96da14c... are superseded.

The original P3R0 document remains historical evidence and must not be rewritten in place.

---

# Mandatory self-audit

The repaired artifact must prove from text alone:

1. lifecycle semantic changes alter lifecycle_semantic_root_hash;
2. scientific P2 hashes remain unchanged;
3. final governance authority will bind the committed repair through later independent acceptance without self-reference;
4. future F01 implementation commit can be bound without contradicting the pre-implementation code baseline;
5. candidate selection exactly inherits R3R4;
6. candidate lock cannot be authorized by an arbitrary caller-created summary/hash;
7. WF validation cannot be authorized by a bare boolean;
8. cross-run/cross-candidate/cross-split replay fails closed;
9. P1 historical lineage does not require current executable HEAD equality;
10. F02 remains open and confirmation outcomes remain sealed;
11. no H40/protected outcomes were accessed;
12. code branch remains untouched.

---

# Deliverable and branch discipline

Commit and push only to:

~~~text
origin/v0.5-refactor-docs
~~~

Do not modify v0.5-refactor.

Report:

~~~text
exact docs commit SHA
artifact path
superseded P3R0 clauses
lifecycle semantic child hashes
lifecycle_semantic_root_hash
confirmation that final governance authority hash is intentionally deferred to independent acceptance
confirmation scientific hashes remain unchanged
confirmation code branch remains cfefdbabfeef8ab004e43bbda24038e4d5e3fca0
confirmation F02 remains open
confirmation no protected/outcome data were accessed
~~~

## Required end state

Only if all blockers are repaired:

~~~text
H40_P2_FINAL_ACCEPTED
H40_P3R0R1_REPAIR_AUTHORED
H40_P3R0_PENDING_CUMULATIVE_INDEPENDENT_ACCEPTANCE
H40_FSA_F01_PROTOCOL_SEMANTICS_CLOSED_PENDING_ACCEPTANCE
H40_FSA_F02_OPEN_SEALED
H40_P3_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
~~~

Do not claim P3R0 accepted, F01 implementation complete, or P3 authorized.
