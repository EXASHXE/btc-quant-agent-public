# v0.5.1 H40R — Preregistration Determinism Repair

## Role

You are a bounded research-protocol amendment author. This is a documentation-only preregistration repair performed **before any H40 implementation, label computation, search, backtest, candidate outcome, or confirmation access**.

Repository: `EXASHXE/btc-quant-agent`

Frozen code branch: `v0.5-refactor`

Required frozen kernel SHA:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

Docs branch: `v0.5-refactor-docs`

Base preregistration commit:

```text
8b36cfde2ac14a37ed4eb244a8b5ae88882765b4
```

Base preregistration artifact:

```text
reviews/v0.5/V0.5.1_H40_SELECTIVE_MEDIUM_HORIZON_DIRECTIONAL_EDGE_PREREGISTRATION.md
```

Required independent review:

```text
reviews/v0.5/V0.5.1_H40_PREREG_SOL_INDEPENDENT_REVIEW.md
```

Review commit:

```text
960e92a845726fc8fdada6e5e3229817a20e84f6
```

## Hard preflight

Before editing documentation:

1. fetch `origin/v0.5-refactor` and verify exact HEAD equals the frozen SHA above;
2. fetch `origin/v0.5-refactor-docs` and verify the base preregistration and Sol review exist;
3. verify no H40 implementation has appeared on the code branch;
4. do not inspect H39 protected outcomes, Final Holdout, H40 labels, candidate metrics or any alpha result.

If code HEAD drifted, stop with:

```text
H40R_ABORTED_KERNEL_HEAD_DRIFT
```

If any H40 outcome has already been computed or inspected, stop with:

```text
H40R_ABORTED_OUTCOME_CONTAMINATION_REVIEW_REQUIRED
```

## Scope

Create exactly one protocol amendment:

```text
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R1.md
```

Do **not** modify production code, tracked tests, workflow files, strategy configs, H39 artifacts, Final Holdout, or the frozen kernel. Do not trigger GitHub Actions.

Do not redesign H40. Preserve all accepted choices in the base preregistration unless this prompt explicitly supersedes an ambiguous clause.

The effective preregistration after this amendment is the immutable pair:

```text
BASE = base preregistration at commit 8b36cfde2ac14a37ed4eb244a8b5ae88882765b4
AMENDMENT = V0.5.1_H40_PROTOCOL_AMENDMENT_R1.md at its committed docs revision
SCHEMA = H40_PROTOCOL_V1_R1
```

Downstream H40-P1 must bind both the base preregistration identity and amendment identity. Do not silently rewrite history or claim the original artifact alone was unambiguous.

---

## R1 — Freeze the first candidate-lock information boundary

Close H40R-01 from the Sol review.

The amendment must explicitly state that the sole H40 V1 configuration search and first candidate lock use only the following pre-validation information:

```text
WF1 FIT/TRAIN:
2021-01-31T00:00:00Z <= t < 2022-10-31T00:00:00Z

PURGE:
2022-10-31T00:00:00Z <= t < 2022-11-01T00:00:00Z

WF1 CALIBRATION / DISCOVERY-SELECTION:
2022-11-01T00:00:00Z <= t < 2023-01-31T00:00:00Z

PURGE:
2023-01-31T00:00:00Z <= t < 2023-02-01T00:00:00Z
```

Required semantics:

1. All 168 configuration rows must exist and be content-hashed before the first H40 label/performance metric is computed.
2. Direction/regime/geometry fitting for first lock uses WF1 training only.
3. The calibrator is fit only on the WF1 calibration interval according to the base protocol.
4. Any discovery/selection statistic used to choose the sole candidate must be explicitly named and may use only WF1 training + WF1 calibration according to its role. It must not use WF1 validation, WF2-WF4 calibration/validation, confirmation, 24h diagnostic rescue, H39, or Final Holdout outcomes.
5. The deterministic winner rule must result in at most one `H40-CANDIDATE_LOCKED` identity before any walk-forward validation outcome is read.
6. The locked identity includes asset scope, primary horizon, all feature/gate semantics, configuration ID, numeric hyperparameters, confidence threshold, calibration method, abstention behavior and refit algorithm.
7. WF2-WF4 may mechanically re-fit/re-calibrate the already locked candidate using their preregistered chronological training/calibration windows because those observations are past at each simulated boundary. This must never change configuration identity or select a new family/variant/threshold/horizon/scope.
8. Re-use of calendar periods that were earlier validation observations is allowed only as **later chronological refit data** after the simulated clock has advanced; those outcomes can never retroactively influence the first candidate lock.
9. A walk-forward failure does not promote the runner-up. H40 V1 becomes `H40_NO_GO`/`NOT_TESTABLE` as applicable. Any successor requires a new protocol/confirmation authority.

Add an explicit information-flow diagram or table making this unambiguous.

---

## R2 — Fully specify dependence-adjusted effective sample size

Close H40R-02 from the Sol review.

The amendment must replace the phrase `HAC effective sample size` with a mathematically complete deterministic definition. Choose and lock **one** standard estimator now, before outcomes.

The definition must include, without placeholders:

1. exact input series used for the HAC effective-size calculation;
2. whether the index is the UTC hourly base grid, accepted-decision sequence, or a preregistered block aggregation;
3. exact handling of simultaneous BTC/ETH decisions;
4. exact centering and autocorrelation/autocovariance estimator;
5. exact kernel/weights (for example Bartlett if chosen);
6. exact lag rule with the base protocol's maximum lag 24;
7. exact finite-sample denominator convention;
8. exact conversion from dependence estimate to `n_eff`;
9. clipping behavior;
10. zero-variance, insufficient-length, non-finite, missing-row and negative-denominator behavior;
11. whether correctness/precision and proxy-net-expectancy use separate effective sizes and, if so, exactly how the conservative sample-floor `n_eff` is formed;
12. how the value is reconstructed and persisted.

The final formula must make Section 11 sample-floor pass/fail bitwise/deterministically reproducible from the same ordered input rows.

Do not weaken the existing raw-count, side-count, occupied-24h-block or confirmation floors.

---

## R3 — Freeze moving-block-bootstrap execution semantics

The base protocol already fixes 168-hour blocks, 10,000 resamples and deterministic seed derivation. Add the remaining execution semantics so two independent implementations cannot produce different confidence bounds from identical rows.

Define exactly:

- block-start universe;
- whether the moving blocks are overlapping and whether wrapping/circular blocks are permitted;
- ordering of UTC timestamps;
- treatment of gaps;
- treatment of simultaneous BTC/ETH rows as an inseparable UTC block unit;
- number of blocks drawn per replicate;
- how the final sampled sequence is trimmed to target length;
- whether sampling is by base-eligible UTC grid or accepted-trade rows;
- recomputation of accepted trades/statistics inside each replicate;
- handling of a replicate with undefined PF, missing side counts, zero variance or insufficient observations;
- quantile/interpolation convention for one-sided 90%, 95% and 97.5% lower bounds;
- max-statistic construction for within-family simultaneous inference;
- exact relationship between max-statistic confidence bounds and Holm-adjusted p-values.

No optional bootstrap variant may remain selectable after H40 outcome computation.

---

## R4 — Freeze the empirical directional null `p0`

The base protocol defines:

```text
p0 = max(0.50, precision of the empirical base-rate null at the locked side mix)
```

Make this executable and unambiguous.

The amendment must state:

1. exactly which allowed **training-only** partition supplies `LONG_LABEL`, `SHORT_LABEL`, and `NEUTRAL_LABEL` base rates for each evaluation stage;
2. exactly which non-outcome action counts supply the candidate LONG/SHORT weights;
3. the algebraic formula for the side-mix base-rate precision;
4. behavior when one emitted side has zero weight;
5. pooled BTC/ETH behavior;
6. whether `p0` is recomputed mechanically for each fold from that fold's training partition and for confirmation from the final pre-confirmation training set;
7. no evaluation/confirmation label may enter the null base-rate estimate.

A valid example shape is:

```text
base_precision = w_long * pi_long_train + w_short * pi_short_train
p0 = max(0.50, base_precision)
```

but the amendment must define the exact source of `w_*` and `pi_*` and any smoothing (preferably none unless explicitly justified and frozen now).

---

## R5 — Preserve all existing protocol gates

Do not change merely because of this repair:

- BTCUSDT / ETHUSDT perpetual scope;
- 1h decision cadence;
- 4h / 8h / 12h primary horizons and 24h diagnostic-only status;
- `LONG / SHORT / NO_TRADE`, default abstention;
- D1-D5 direction families and allowed pairings;
- finite total search budget of 168 complete configurations;
- TP/BR magnitude-only restriction;
- cost proxy formula and current frozen-kernel default 12 bps value;
- geometry thresholds;
- action threshold set `{0.55, 0.60, 0.65}`;
- coverage and raw/effective sample floors;
- numeric walk-forward and confirmation GO/NO-GO thresholds;
- one primary candidate;
- one-shot confirmation holdout;
- canonical replay as final economic authority;
- H39 protection, Final Holdout seal and execution-disabled boundary.

If you believe any of these must change to make R1-R4 coherent, do not change them. Stop and report:

```text
H40R_ESCALATION_REQUIRED_PROTOCOL_REDESIGN
```

with the exact conflict.

---

## Validation

Because this is documentation-only and must not inspect outcomes:

- do not run H40 labels;
- do not run candidate search/backtests;
- do not trigger GitHub Actions;
- no code change is expected;
- verify `origin/v0.5-refactor` remains the frozen SHA;
- verify `git diff` on the code branch is empty;
- verify the amendment contains no result-derived number or observed H40 performance.

## Required amendment structure

The amendment must include:

1. authority and relationship to base preregistration;
2. exact first-lock information boundary;
3. candidate-lock / later-refit state machine;
4. exact HAC/effective-sample-size definition;
5. exact moving-block-bootstrap algorithm;
6. exact empirical-null `p0` formula;
7. explicit statement that all other base protocol clauses remain unchanged;
8. protected-surface assertions;
9. final marker.

Final marker on successful completion:

```text
H40_PROTOCOL_V1_R1_PREREGISTERED_PENDING_INDEPENDENT_REVIEW
H40R01_CANDIDATE_LOCK_BOUNDARY_CLOSED
H40R02_EFFECTIVE_SAMPLE_SIZE_DETERMINISTIC
H40R03_BOOTSTRAP_DETERMINISTIC
H40R04_EMPIRICAL_NULL_DETERMINISTIC
H40_IMPLEMENTATION_NOT_YET_AUTHORIZED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_H40_OUTCOME_ACCESS
```

Commit the amendment to `v0.5-refactor-docs` only and stop. Do not start H40-P1 until independent acceptance.
