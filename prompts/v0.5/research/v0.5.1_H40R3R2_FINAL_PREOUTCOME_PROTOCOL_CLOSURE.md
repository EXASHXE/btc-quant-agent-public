# Task Prompt — H40 R3R2 Final Pre-Outcome Protocol Closure

## Role

Act as the **primary protocol author** for one final docs-only closure of `H40_PROTOCOL_V1_R3` before any P2 code repair or H40 outcome computation.

This task is deliberately broader than the earlier narrow R3R2 formula prompt. It is based on a full cross-layer Sol audit and is intended to end the R3/R3R1/R3R2 patch chain in one bounded amendment.

## Exact Repository / Branch State

Repository:

```text
EXASHXE/btc-quant-agent
```

Docs branch to modify:

```text
v0.5-refactor-docs
```

Code branch is **read-only evidence**:

```text
v0.5-refactor
exact code HEAD = 52ac1dcb0843458b1b20b943be920f139dc832d1
```

Do not modify the code branch.

The full-audit report is authoritative input for this repair:

```text
reviews/v0.5/V0.5.1_H40_SOL_FULL_PREOUTCOME_SYSTEM_AUDIT.md
commit = 1b16396cad2e821b4042fcdd2b3ffd96e3f99dd5
```

The older narrow prompt:

```text
prompts/v0.5/research/v0.5.1_H40R3R2_EXECUTABLE_FEATURE_FORMULA_COMPLETION.md
commit = 7277449df967e259e731d12af98dc038d5589c78
```

is **SUPERSEDED** by this prompt. Do not execute both and do not create two competing R3R2 amendments.

## Mandatory Authority Chain

Read and preserve the cumulative authority, using exact committed versions:

```text
Frozen kernel
6838e9db8d5c369b5da87354821d9c8e79c2a879

BASE preregistration
8b36cfde2ac14a37ed4eb244a8b5ae88882765b4

Amendment R1
59e9fe0ba361557c5df56f0d8f8afaa781cf0071

Amendment R2
4bdeb1a0102b3cb374e8ec7d01b41a79dcc8b021

Protocol Sol acceptance through R2
b9c8531e719fd4f10cf27301719c9e066b95be13

P1 accepted code baseline
3fc89541ed0965fc0e2972af310e34ae1b838168

P1 final acceptance
95ab819d5300312b4d493b9585b4b369621504b6

R3 amendment
e45899bb0118b14127bc765f49315109cfd94ff0

R3 Sol review
afdcc30fbd86278df0f2e9ad370566dc4b62a2ee

R3R1 amendment
3f1bf28dc810ca4fd1bfd2bef566033cd15550fa

R3R1 cumulative Sol review
bbd761fc3e778d7312daf017fdf507c5816ca5c0

Full pre-outcome system audit
1b16396cad2e821b4042fcdd2b3ffd96e3f99dd5
```

Also inspect the exact current H40 implementation at `52ac1dcb...` only to understand interface/authority constraints. **Current implementation is evidence, not protocol authority.** Never adopt an implementation choice merely because it already exists.

## Hard Information Boundary

This is strictly **pre-outcome** protocol authoring.

You MUST NOT compute, inspect, search for, derive, summarize, or use:

- H40 labels or forward returns;
- MFE/MAE realized values;
- H40 precision, expectancy, PF, ECE, Brier, p-values, HAC, bootstrap outcomes;
- WF1 validation or WF2-WF4 outcomes;
- confirmation outcomes;
- H39 protected outcomes/artifacts;
- Final Holdout data/outcomes;
- historical strategy performance to choose any formula, threshold, tail, model, score, or fallback.

If any such outcome is encountered, stop and report:

```text
H40_R3R2_OUTCOME_EXPOSURE_ABORT
```

Do not compute labels merely to “test” formulas.

## Output Surface

Create exactly one normative amendment:

```text
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3R2_FINAL_PREOUTCOME_CLOSURE.md
```

Do not edit R3/R3R1 in place. R3R2 supersedes only conflicting/ambiguous clauses and otherwise preserves them.

No code changes. No tests. No runner. No data acquisition. No backtest.

After authoring, commit and push to `origin/v0.5-refactor-docs` and report exact commit SHA.

---

# Required Closure Work

Close **all protocol-level findings H40-FSA-01 through H40-FSA-09** from the full audit. Do not attempt the code findings `H40-FSA-C*`; those are reserved for P2R1 after independent acceptance.

## 1. Freeze the Exact Direction-Score and Calibration Pipeline — H40-FSA-01

The cumulative protocol currently specifies calibration methods and thresholds but not the deterministic score/model sent into Platt/isotonic calibration.

For **every active single-family variant and every active pair**, freeze one exact `raw_score` contract.

Mandatory requirements:

1. `raw_score` is a single deterministic scalar with orientation:

```text
larger raw_score => more bullish
smaller raw_score => more bearish
zero => neutral/no directional evidence unless explicitly stated otherwise
```

2. Prefer the **simplest one-dimensional score derived directly from the already-authorized direction feature**. Do not introduce a new multifeature ML model, feature-selection procedure, regularization grid, optimizer grid, hidden model family, or new search degree of freedom.

3. For pairs, secondary D4/D5 components remain confirm/veto filters; they must not silently become additional learned predictors. Unless a scientifically necessary pre-outcome reason is documented, the pair's retained raw score should be the base direction owner's raw score after the secondary filter passes.

4. Freeze exact target encoding used to fit the calibrator, including treatment of the protocol's `NEUTRAL_LABEL`. Resolve the current mismatch between a ternary economic label and binary `p_up`/Platt/isotonic calibration. The solution must be coherent with BASE's rule that neutral outcomes are **not silently dropped from accepted-trade precision**. If calibration is conditional on non-neutral direction, state that explicitly and distinguish calibration population from later accepted-trade precision population.

5. Define exactly:

- target `y`;
- sample inclusion/exclusion;
- raw score sign/orientation;
- how `p_up` is interpreted;
- Platt form and parameter-fit objective;
- intercept policy;
- regularization policy (prefer none unless required; if any, freeze exact value and reason);
- deterministic solver/fit convention or mathematically unique optimum/fail-closed rule;
- isotonic input/output/tie convention;
- degenerate one-class/constant-score behavior;
- exact fallback from isotonic to Platt already introduced by R3;
- threshold rule `0.55/0.60/0.65` without additional learned cutoff.

6. No new configuration slots. Calibration method/threshold remain exactly those already frozen by R3.

The final text must allow two independent implementations to produce the same fitted mapping from identical training/calibration inputs.

---

## 2. Make Every Active Feature Executable — H40-FSA-02

Freeze exact math/pseudocode for all active feature contracts used by the 168 rows.

### D1

Freeze exact bar endpoint/index convention and formula for:

```text
D1_V1_RETURN_4H
D1_V2_RETURN_12H
```

Define zero/equality, missing endpoint and closed-bar behavior.

### D2

Freeze exact Donchian continuation semantics for 24h and 72h:

- exact reference bars;
- whether the latest closed signal bar is excluded from the prior range;
- range high/low formula;
- break tested by close/high/low;
- raw signed score formula/magnitude;
- equality behavior;
- no-break behavior;
- missing/gapped window behavior.

### D3

Freeze exact failed-move reversal semantics for 24h and 72h:

- exact reference range;
- exact break condition;
- exact close-back condition;
- whether the failure is same-bar or a fixed multi-bar sequence;
- if multi-bar, freeze exact maximum age and state-reset rule without adding a search parameter;
- reversal side/raw score;
- equality/missing behavior.

Choose the **simplest pre-outcome interpretation** consistent with BASE. Do not add a tunable state machine.

### D4

Preserve R3R1's exact target-relative formulas, but remove any remaining timestamp/bar-index ambiguity. Define closed-bar indices by exact UTC hourly bar open/close interval semantics so `C_X(t)` cannot be interpreted two ways.

### Regime R_VOL_RANGE

Replace “realized-volatility / true-range percentile” with one exact raw scalar formula. Do not leave an `or`, slash, composite-choice, optional transform, or implementation-selected estimator.

Freeze:

- OHLC inputs;
- return/range formula;
- lookback bars;
- ddof if variance/std is used;
- minimum complete bars;
- missing behavior;
- raw scalar units.

Preserve R3R1's state boundaries and MID-only pass rule.

### Opportunity O_RANGE_EXPANSION

Replace “range/volatility expansion percentile” with one exact raw scalar formula and exact lookback/reference behavior. Preserve R3R1 NONE/WATCH/ELIGIBLE boundaries and ELIGIBLE-only proceed rule.

---

## 3. Freeze a Single PIT-Safe Percentile/Quantile Algorithm — H40-FSA-02/H40-FSA-03

Create one reusable normative percentile/quantile contract for all active percentile/crowding features.

Freeze:

- exact empirical CDF / rank or quantile definition;
- exact interpolation method if interpolation is used;
- tie handling;
- equality handling;
- minimum reference-sample N;
- NaN/Infinity rejection;
- sample ordering irrelevance where mathematically appropriate;
- warm-up behavior;
- what is fit on WF1 training and how training rows themselves remain causal;
- how the fitted reference is frozen for calibration/validation;
- how later WF refits use chronologically past partitions only.

Critical PIT requirement:

> a feature value claimed available at decision `t` may not use future observations merely because those observations are inside the same offline training partition.

If training-time feature construction uses an expanding/lagged empirical reference and calibration/validation uses a frozen training reference, state it exactly.

---

## 4. Finish D5 Funding Interaction Semantics — H40-FSA-03

For:

```text
D5_V1_CROWDING_Q10_D1
D5_V2_CROWDING_Q20_D2
D5_PAIR_CROWDING_Q10_D3
```

freeze all of:

- source field used as the funding/crowding scalar;
- last-settled record selection at decision `t`;
- signed versus absolute representation;
- side-aware versus side-independent crowding;
- exact quantile/tail meaning of `0.10` and `0.20`;
- upper/lower tail mapping;
- continuation versus crowding-reversal hypothesis;
- veto/confirm rule relative to base side;
- equality/tie behavior;
- missing source behavior;
- training reference distribution and PIT-safe fitting;
- raw score after filter (normally base owner's raw score if filter passes).

Funding must never emit side by itself.

Do not introduce a third D5 single-family variant or any new q value.

---

## 5. Close Geometry Population/Fallback Determinism — H40-FSA-04

Preserve existing numeric gates:

```text
c_rt = 0.0012
median MFE >= 1.5 * c_rt
median MFE / max(median MAE, c_rt) >= 1.25
minimum N = 30
```

Freeze the exact estimator algorithm:

- precise training-observation eligibility for geometry cells;
- proposed-side mapping to `MFE_LONG/SHORT` and `MAE_LONG/SHORT`;
- exact cell keys including pooled/product semantics;
- exact fallback order already intended by R3;
- whether `N < 30` triggers fallback first or immediate failure;
- minimum N required at every fallback level;
- exact median convention for odd/even N;
- final fail-closed state if every level is thin;
- no use of calibration/validation future realization during a fold's geometry fit.

Resolve any conflict between BASE's `N<30 => NO_TRADE` sentence and R3's pooling hierarchy explicitly via supersession language.

Do not compute any MFE/MAE values in this task.

---

## 6. Separate Structural Config Identity From Runtime Source Authority — H40-FSA-05

Normatively split these concepts:

### Structural scientific identity

Must bind:

- required logical source IDs/source-projection rule;
- exact scientific contracts;
- source requirement set identity;
- but **not a mutable VERIFIED/NOT_TESTABLE state** if that state may change without changing the scientific configuration.

### Runtime authority attestation

Must bind:

- canonical P1 source specs/receipts;
- current production authority states;
- exact authority snapshot hash;
- derived `REGISTERED/NOT_TESTABLE` status.

### Freeze boundary

Freeze a rule such as:

```text
Before first H40 label/outcome:
  source authority may be repaired only through a new explicit pre-outcome authority snapshot
  that is independently accepted.

At first H40 label/outcome boundary:
  the runtime authority snapshot is sealed.

After seal:
  no source promotion may unlock additional rows or alter the discovery candidate set.
```

The 168 structural rows never get replaced/reallocated.

State whether the current `18 REGISTERED / 150 NOT_TESTABLE` count is a snapshot under current P1 authority, not a permanent scientific-grid property.

---

## 7. Freeze the P1-to-R3 Authority Bridge — H40-FSA-06

Do not reopen accepted P1 evidence.

Define exactly how R3 production materialization consumes accepted P1 authority:

- immutable P1 acceptance commit/code baseline;
- canonical source-spec authority;
- source receipt/manifest identity where applicable;
- split schedule/attestation identity;
- R3 authority snapshot wrapper/attestation;
- production APIs must verify/derive these identities, not accept arbitrary strings.

Clarify which identity is structural and which is runtime evidence.

Do **not** promote BTC or auxiliary sources. Preserve current canonical P1 states.

---

## 8. Freeze Scope and Pooled Construction — H40-FSA-07

Add exact invariants:

```text
BTC_ONLY       => asset_scope exactly (BTCUSDT)
ETH_ONLY       => asset_scope exactly (ETHUSDT)
POOLED_BTC_ETH => asset_scope exactly (BTCUSDT, ETHUSDT) in canonical order
```

For pooled candidates freeze:

- common trustworthy timestamp intersection;
- one per-product opportunity row at each common timestamp;
- no interpolation/forward fill;
- exact behavior when one product lacks a required feature at an otherwise common timestamp;
- weighting for feature-reference fitting, raw-score calibration and geometry estimation;
- whether product identity is included in the predictor (must be fixed yes/no; prefer no new feature unless required);
- simultaneous BTC/ETH decisions remain distinct observations but joint UTC dependence is preserved by the already-frozen bootstrap.

Do not add a fourth scope.

---

## 9. Define One Unambiguous Semantic Hash Root — H40-FSA-08

Reconcile R3 and R3R1 naming.

Freeze a single hierarchy, for example conceptually:

```text
protocol_authority_hash
semantic_root_hash
  active_feature_registry_hash
  active_direction_registry_hash
  active_gate_registry_hash
  geometry_contract_hash
  calibration_contract_hashes
  availability_contract_hash
  refit_policy_hash
  reserved_family_vocabulary_hash (if retained)
structural_configuration_hash
slot_hash
structural_ledger_hash
runtime_authority_snapshot_hash   # separate from structural config identity
materialized_run_authority_hash   # binds structural ledger + runtime snapshot
```

You do not have to use these exact names, but there must be exactly one meaning for every root and exact child membership/order.

Correct Git wording:

```text
40-hex values in the current repository are Git commit object IDs / commit SHAs.
Do not call them SHA-256 hashes.
```

Use repository `canonical_json/canonical_sha256` semantics exactly; no implicit float rounding.

The final independent acceptance will publish the exact final protocol-authority/root hashes after the R3R2 commit exists, avoiding self-reference.

---

## 10. Resolve Registry-Only Contracts Without Creating Unnecessary Research Freedom — H40-FSA-09

The eight non-active regime/opportunity contracts are not used by any of the 168 rows.

Prefer the simpler scientifically conservative solution:

- preserve their BASE-authorized family IDs/lookback domains as **reserved/non-active family vocabulary**;
- explicitly state `ACTIVE_IN_168_LEDGER=false`;
- do not claim an under-specified natural-language transform is a bitwise executable canonical feature contract;
- do not let reserved family descriptions enter active configuration semantic hashes;
- any later activation/formula choice requires a new protocol version/authority and cannot reuse V1 confirmation.

If you instead keep them as executable canonical contracts, then you must fully specify all formulas with zero ambiguity. Do not do this merely to preserve wording from R3R1.

---

# Things You Must Preserve

Do not reopen or change without a direct contradiction:

- exactly 168 structural slots;
- exact R3 10 single variants and 4 pair order;
- exact R3/R3R1 12-row template unless a direct mathematical contradiction is proven;
- scopes and horizons;
- cost proxy 12 bps;
- D4 R3R1 target-relative semantics;
- D5 exact base variants from R3R1;
- R_VOL and O_RANGE state boundaries from R3R1;
- BASE/R1/R2 WF boundaries and purge windows;
- one candidate lock/no runner-up;
- bootstrap/HAC/seed/quantile rules already accepted through R2;
- candidate-selection lexicographic rule/family-wise control;
- confirmation interval and one-shot principle;
- P1 source authority states;
- H39 and Final Holdout seals;
- `EXECUTION_DISABLED`;
- no alpha/profitability claim.

Do not redesign H40.

---

# Explicitly Out of Scope for This Docs Repair

Do not fix code findings yet:

```text
H40-FSA-C01 caller-forgeable production authority
H40-FSA-C02 current R2/P2 code divergence from R3
H40-FSA-C03 P2 hard-coded source testability
H40-FSA-C04 factory/scope/pair-order/status invariants
H40-FSA-C05 implementation hash/serialization drift
H40-FSA-C06 recursive/protected guard implementation
H40-FSA-C07 P2 adversarial test redesign
```

Do not modify lifecycle/confirmation code yet. Record that the full audit's future-stage findings `H40-FSA-F01/F02` remain mandatory before those stages are authorized.

---

# Mandatory Self-Audit in the R3R2 Document

Before declaring authoring complete, prove from the cumulative protocol text alone that:

1. Two independent implementations compute identical active feature values from identical input rows.
2. Two independent implementations compute identical raw directional score and side.
3. Two independent implementations construct identical calibration training pairs and fitted `p_up` mapping, or fail closed identically.
4. Percentile/quantile features are PIT-safe at training timestamps and deterministic at calibration/validation timestamps.
5. D5 tail/hypothesis behavior is unique.
6. Geometry cell population/fallback is unique.
7. Pooled observations and weights are unique.
8. Structural configuration identity is stable when only runtime source verification state changes.
9. Runtime authority snapshot fixes the testable candidate set before the first outcome.
10. P1 canonical authority is consumed without caller-forgeable identity.
11. Hash-tree/root membership is unique and non-self-referential.
12. Reserved non-active families cannot alter or enter the 168 search without a new protocol.
13. No H40 outcome/protected data was accessed.
14. P2 remains not accepted and no code work is authorized until independent review accepts cumulative R3.

If any item cannot be proven without an implementation choice, keep the amendment pending and explicitly identify the remaining ambiguity rather than pretending closure.

## Required End State

The authored document may claim only:

```text
H40_PROTOCOL_V1_R3R2_FINAL_PREOUTCOME_CLOSURE_AUTHORED
H40_PROTOCOL_V1_R3_PENDING_CUMULATIVE_INDEPENDENT_ACCEPTANCE
H40_P2_NOT_ACCEPTED
H40_P2R1_NOT_AUTHORIZED_PENDING_PROTOCOL_ACCEPTANCE
H40_P3_NOT_AUTHORIZED
H40_NO_LABELS_COMPUTED
H40_NO_PERFORMANCE_METRICS_COMPUTED
H40_NO_CANDIDATE_SELECTED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

Do not claim independent acceptance yourself.

## Commit / Push

Commit and push only the single R3R2 amendment on `v0.5-refactor-docs`.

Suggested commit message:

```text
docs(h40): close final pre-outcome protocol determinism gaps
```

Return:

- exact commit SHA;
- exact created path;
- confirmation that `v0.5-refactor` was not modified;
- statement that no outcomes/protected data were accessed.
