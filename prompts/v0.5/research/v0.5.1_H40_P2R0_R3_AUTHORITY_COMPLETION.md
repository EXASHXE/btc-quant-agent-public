# v0.5.1 H40-P2R0 — Complete R3 Protocol Authority Before P2 Repair

## Role

You are a bounded preregistration-repair author. This task is **docs-only** and exists to restore protocol ordering after an implementation was pushed before the exact 168-row grid had formal R3 authority.

Repository:

```text
EXASHXE/btc-quant-agent
```

Code branch:

```text
v0.5-refactor
```

Current code HEAD to inspect but **not modify**:

```text
52ac1dcb0843458b1b20b943be920f139dc832d1
```

Accepted P1 baseline:

```text
3fc89541ed0965fc0e2972af310e34ae1b838168
```

Docs branch:

```text
v0.5-refactor-docs
```

Required independent review:

```text
reviews/v0.5/V0.5.1_H40_P2_SOL_INDEPENDENT_REVIEW.md
```

Existing R3 design prompt:

```text
prompts/v0.5/research/v0.5.1_H40R3_EXACT_P2_GRID_AND_FEATURE_SEMANTICS_PREREGISTRATION_REPAIR.md
```

Existing P2 implementation report:

```text
reviews/v0.5/V0.5.1_H40_P2_IMPLEMENTATION_REPORT.md
```

## Status

Current P2 implementation is **not accepted**. Treat it only as pre-outcome implementation evidence.

No H40 label/performance outcome has been authorized or should be accessed.

Required current state:

```text
H40_P2_NOT_ACCEPTED
H40_P3_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
```

## Objective

Create the missing formal R3 protocol amendment that freezes the exact H40-P2 research degrees of freedom **before any H40 outcome is computed**.

The amendment must make an independent implementation able to reconstruct the same exact ordered 168-row search space without making any scientific choice of its own.

Because code already exists, you may inspect the current P2 implementation solely as a candidate pre-outcome specification. You may adopt a current implementation choice only if it is:

1. within BASE/R1/R2 authority;
2. scientifically coherent;
3. not outcome-derived;
4. made explicit in R3;
5. internally consistent across protocol, feature contracts, pair semantics, source authority, and hash identity.

Do **not** treat existing code as authority merely because it exists.

If a current implementation choice is internally inconsistent or violates prior authority, correct the protocol choice in R3 and state that P2R1 must conform code later.

## Hard prohibitions

Do NOT:

- modify `v0.5-refactor`;
- edit P2 code;
- run backtests;
- compute labels;
- compute forward returns;
- compute MFE/MAE realizations;
- compute precision/expectancy/PF/ECE/Brier/HAC/bootstrap metrics;
- inspect WF1 validation, WF2-WF4 outcome values, confirmation outcomes, H39, or Final Holdout;
- use historical performance to choose variants/grid points;
- select a candidate;
- trigger expensive compatibility CI;
- enable execution.

Any accidental outcome access is a hard stop:

```text
H40_R3_OUTCOME_EXPOSURE_ABORT
```

## Required output

Create exactly:

```text
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3.md
```

on:

```text
v0.5-refactor-docs
```

Do not modify existing BASE/R1/R2 documents in place.

The effective protocol schema after this amendment becomes:

```text
H40_PROTOCOL_V1_R3
```

Status after authoring must remain:

```text
PREREGISTERED_PENDING_INDEPENDENT_REVIEW
```

Do not claim P2 accepted and do not start P2R1 code work.

## Mandatory authority chain

R3 must bind and preserve:

```text
Frozen kernel:
6838e9db8d5c369b5da87354821d9c8e79c2a879

BASE:
8b36cfde2ac14a37ed4eb244a8b5ae88882765b4

R1:
59e9fe0ba361557c5df56f0d8f8afaa781cf0071

R2:
4bdeb1a0102b3cb374e8ec7d01b41a79dcc8b021

Protocol Sol acceptance through R2:
b9c8531e719fd4f10cf27301719c9e066b95be13

P1 accepted code baseline:
3fc89541ed0965fc0e2972af310e34ae1b838168

P1 final acceptance:
95ab819d5300312b4d493b9585b4b369621504b6
```

R3 supersedes only the previously ambiguous P2 exact-grid/contract details. All non-conflicting BASE/R1/R2 rules remain authoritative.

## Exact findings R3 must close

Read the Sol P2 independent review and close the **protocol-level** portions of:

```text
P2-SOL-01 exact-grid authority ambiguity
P2-SOL-03 source/scope compatibility ambiguity
P2-SOL-04 D5 exact base-owner / pair semantics ambiguity
P2-SOL-05 semantic contract hash-preimage ambiguity
P2-SOL-07 normalization / scope-compatibility ambiguity
```

Do not attempt to close the code-specific caller-forgeability or guard-implementation defects in this docs-only task. Those remain for P2R1 after R3 acceptance.

## 1. Freeze exact two variants per direction family

Freeze exactly two variants for each D1-D5 family.

Use only R2-authorized parameter sets:

```text
D1 lookback ∈ {4h, 8h, 12h, 24h}
D2 range ∈ {24h, 72h, 168h}
D3 range ∈ {24h, 72h}
D4 state window ∈ {4h, 8h, 12h}
D5 funding quantile ∈ {0.10, 0.20}
```

Each variant must freeze:

```text
variant_id
family_id
semantic rule
exact feature IDs
exact lookback/state parameters
side-generation rule
normalization / train-fit rule
availability rule
closed-bar rule
missing-data behavior
scope compatibility
source authority requirements
cross-asset/funding requirements
mechanical refit identity
```

D5 rules:

- funding never supplies side;
- every D5 variant must bind an explicit base owner in D1/D2/D3;
- base owner is part of identity;
- no implicit/default base owner is allowed;
- if two D5 variants use different base owners, encode that explicitly;
- if both use the same base owner, document that deliberately.

Do not carry forward the current implementation-report/code mismatch (`Q20_D2` vs `Q20_D1`). Pick one exact authoritative identity and require later code/report alignment.

## 2. Freeze exact pair semantics

For exactly:

```text
D1 + D4
D1 + D5
D2 + D4
D3 + D5
```

freeze one exact pair-variant identity and specify:

```text
base side owner
secondary owner role
confirmation/veto/interaction behavior
conflict behavior
missing-data behavior
required feature IDs
exact base-owner binding
scope/source compatibility
```

The D3+D5 pair must not reference a D5 feature whose content identity says its base owner is D1. Every pair feature identity and pair payload must agree exactly.

## 3. Freeze exact regime and opportunity contracts actually used by all 168 rows

R3 must distinguish:

- the full authorized family registry; and
- the exact contracts selected into the 168-row V1 search space.

If the intended 168-row grid uses only `R_VOL_RANGE` plus `O_RANGE_EXPANSION`, state that explicitly as a pre-outcome narrowing decision and explain the simplicity/symmetry rationale. Do not imply the unused authorized families are being searched.

If the grid uses additional regime/opportunity families, enumerate exactly where they occur.

No implementation discretion may remain.

For every used contract freeze:

```text
contract_id/version
semantic meaning
feature IDs
source logical IDs
lookback
threshold/state parameters
normalization
fit ownership
lag/freeze semantics
closed-bar semantics
missing-data behavior
scope compatibility
source-authority requirements
output schema
content-hash preimage
```

## 4. Freeze exact feature registry with per-scope source requirements

Every feature contract used by the 168 rows must include:

```text
feature_id/version
owner family
raw/material source IDs
lookback
transform parameters
normalization
available_at rule
closed-bar rule
missing-data behavior
product compatibility
candidate-scope compatibility
cross-asset requirement
funding requirement
output schema
content-hash preimage
```

Do not make own-asset features require both BTC and ETH merely because the registry supports both products.

Specify source requirements by candidate scope or via an exact deterministic projection rule, e.g.:

```text
BTC_ONLY own-asset feature -> BTC source only
ETH_ONLY own-asset feature -> ETH source only
POOLED_BTC_ETH own-asset feature -> each row uses its own product source
cross-asset feature -> requires both synchronized BTC + ETH sources
```

Use whichever exact rule is scientifically intended, but freeze it unambiguously.

## 5. Freeze exact source-authority compatibility matrix using accepted P1 facts

R3 must preserve current canonical P1 authority, including at minimum:

```text
BTCUSDT_USD_M_1H
production_authority_state = NOT_TESTABLE

ETHUSDT_USD_M_1H
production_authority_state = VERIFIED
```

Auxiliary sources must not be promoted merely because a reference source exists. If P1 canonical authority has not granted production `VERIFIED`, runtime eligibility remains fail-closed.

R3 must explicitly distinguish:

```text
STRUCTURAL_LEDGER_INCLUSION
vs
CURRENT_RUNTIME_TESTABILITY
```

A slot may remain one of the immutable 168 rows while its runtime status is `NOT_TESTABLE`.

No failed slot is replaced or reallocated.

Provide an exact matrix sufficient for P2R1 to mechanically derive slot testability from:

```text
candidate scope
required feature/source IDs
canonical source authority state
```

## 6. Freeze geometry contract completely

Retain:

```text
c_rt = 0.0012
median estimated MFE >= 1.5 * c_rt
median estimated MFE / max(median estimated MAE, c_rt) >= 1.25
minimum cell N = 30
```

Additionally freeze:

```text
estimation-cell keys
grouping hierarchy
fallback hierarchy
INSUFFICIENT_CELL_N -> NO_TRADE behavior
fit ownership
mechanical refit identity
geometry contract content-hash preimage
```

No MFE/MAE realization may be computed here.

## 7. Freeze calibration identities completely

Freeze exact identities for:

```text
PLATT_LOGISTIC
ISOTONIC_IF_ELIGIBLE
```

Retain:

```text
isotonic min total = 200
isotonic min per predicted side = 40
thresholds = {0.55, 0.60, 0.65}
```

Specify exact fallback when isotonic is ineligible and make fallback deterministic and identity-preserving.

Do not accidentally impose isotonic-only sample floors on Platt unless that is explicitly intended and justified from protocol authority.

## 8. Freeze exact ordered 168-row construction

R3 must make all 168 rows uniquely reconstructable.

Required totals:

```text
single-family = 120
pairs = 48
total = 168
```

Required structure:

```text
5 families × 2 exact variants × 12 exact configs = 120
4 exact pair variants × 12 exact configs = 48
```

For every row freeze at minimum:

```text
slot_index 0..167
scope
asset/product projection rule
one primary horizon
direction family/pair
direction variant/pair variant
regime contract hash
opportunity contract hash
direction contract hash
geometry contract hash
calibration contract hash
action threshold
NO_TRADE policy
required feature contract hashes
feature parameters
lookback windows
availability contract hash
cost proxy hash
mechanical refit policy hash
source-requirement identity
```

If adopting the current 12-row symmetric template, state it exactly and explain why it is a simplicity/symmetry commitment rather than an outcome-derived choice.

If correcting it, provide the exact replacement template and require P2R1 to update code later.

## 9. Freeze exact semantic hash hierarchy

R3 must define canonical preimages for:

```text
feature_contract_hash
regime_contract_hash
opportunity_contract_hash
direction_contract_hash
pair_contract_hash
geometry_contract_hash
calibration_contract_hash
availability_contract_hash
refit_policy_hash
source_requirement_hash
configuration_hash
slot_hash
ledger_hash
```

A configuration hash must transitively bind actual semantic contract content, not merely human-readable IDs.

It is acceptable to bind a frozen registry hash if the registry hash transitively covers every referenced contract and per-row references are still unambiguous.

Any material semantic change under the same textual ID must change configuration and ledger identity.

## 10. Canonical serialization

Freeze deterministic canonical JSON requirements and list-vs-set ordering rules.

Clarify whether order is semantic for:

```text
family combinations
required features
assets
contract references
```

Duplicate semantic rows must not be distinguishable only by slot number.

## 11. Explicit pre-outcome provenance statement

The amendment must state:

- current P2 code was inspected only as pre-outcome implementation evidence;
- no H40 labels/performance metrics were computed or inspected;
- no existing implementation choice was adopted because of realized outcome quality;
- R3 is the authority; P2R1 must conform code to R3, not vice versa;
- candidate LOCK remains untouched;
- confirmation/H39/Final Holdout remain sealed.

## Self-audit before commit

Prove from R3 alone:

1. 168 ordered rows are uniquely reconstructable;
2. 120 single-family + 48 pair rows exactly;
3. exactly 2 variants per D1-D5 family;
4. no illegal pair/depth/horizon/scope/threshold;
5. D5 always has explicit base owner;
6. D3+D5 semantics are internally consistent;
7. TP/BR never supplies side;
8. source testability is derived from P1 authority, not hard-coded upgrades;
9. own-asset vs cross-asset source requirements are deterministic by scope;
10. normalization is explicit;
11. contract content hashes are part of transitive configuration identity;
12. no outcome/protected information was used.

If any item remains ambiguous, do not claim completion.

## Recommended model routing

```text
PRIMARY AUTHOR
GPT-5.6 Sol High

REASONING
HIGH

CHEAPER-MODEL-SAFE SUBTASKS
- formatting a fully fixed 168-row table
- deterministic row counting
- markdown cleanup after semantics are fixed

NOT CHEAP-MODEL SAFE
- protocol interpretation
- exact grid choice
- D5/pair semantics
- source authority matrix
- hash preimages
- normalization/fit semantics

INDEPENDENT REVIEWER
GPT-5.6 Sol High in a separate pass

ASTRA
Not required for R3/P2R0
Reserve for candidate LOCK and pre-confirmation unblind gates
```

## Success markers

Only if complete:

```text
H40_PROTOCOL_V1_R3_PREREGISTERED_PENDING_INDEPENDENT_REVIEW
H40R3_EXACT_168_ROW_GRID_FROZEN_PRE_OUTCOME
H40R3_SOURCE_SCOPE_COMPATIBILITY_FROZEN
H40R3_D5_PAIR_SEMANTICS_FROZEN
H40R3_CONTRACT_HASH_HIERARCHY_FROZEN
H40_NO_LABELS_COMPUTED
H40_NO_PERFORMANCE_METRICS_COMPUTED
H40_NO_CANDIDATE_SELECTED
H40_P2_STILL_NOT_ACCEPTED_PENDING_R3_REVIEW
H40_P3_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

After commit, report only the exact docs commit SHA and changed file. Do not begin code repair until R3 independent acceptance.
