# v0.5.1 H40R3R2 — Executable Feature-Formula and D5 Interaction Completion

## Role

You are the bounded docs-only protocol-repair author for H40. This task repairs the remaining executable-formula ambiguity in cumulative `H40_PROTOCOL_V1_R3` after R3 + R3R1.

Repository:

```text
EXASHXE/btc-quant-agent
```

Code branch (must remain unchanged):

```text
v0.5-refactor
```

Required code HEAD:

```text
52ac1dcb0843458b1b20b943be920f139dc832d1
```

Docs branch:

```text
v0.5-refactor-docs
```

Required docs authority chain must include:

```text
BASE                         8b36cfde2ac14a37ed4eb244a8b5ae88882765b4
R1                           59e9fe0ba361557c5df56f0d8f8afaa781cf0071
R2                           4bdeb1a0102b3cb374e8ec7d01b41a79dcc8b021
R2 protocol acceptance       b9c8531e719fd4f10cf27301719c9e066b95be13
P1 accepted code baseline    3fc89541ed0965fc0e2972af310e34ae1b838168
P1 final acceptance          95ab819d5300312b4d493b9585b4b369621504b6
R3                           e45899bb0118b14127bc765f49315109cfd94ff0
R3 independent review        afdcc30fbd86278df0f2e9ad370566dc4b62a2ee
R3R1                         3f1bf28dc810ca4fd1bfd2bef566033cd15550fa
R3R1 cumulative Sol review   bbd761fc3e778d7312daf017fdf507c5816ca5c0
```

Effective schema remains:

```text
H40_PROTOCOL_V1_R3
```

R3R2 is a repair companion. It does not create `V2`, does not expand the search budget, and does not authorize code by itself.

## Required output

Create only:

```text
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3R2.md
```

Commit/push it to:

```text
v0.5-refactor-docs
```

Do not modify any existing authority document in place.

## Hard prohibitions

Do NOT:

- modify `v0.5-refactor`;
- implement or repair P2 code;
- compute labels, forward returns, MFE/MAE realizations, candidate metrics, precision, expectancy, PF, ECE, Brier, p-values, HAC, bootstrap results, drawdown, coverage, or backtests;
- inspect WF1 validation, WF2–WF4 outcomes, confirmation outcomes, H39 outcomes, Final Holdout, or any prior performance report to select formulas;
- change the frozen 10-variant order, four pair order, 12-row template, 168 slot ordering, scopes, horizons, action thresholds, calibration assignments, source-authority states, 18/150 current testability derivation, or execution policy;
- add a new active family, pair, variant, grid point, source substitution, or candidate;
- reopen already-closed R3R1 semantics unless necessary to state formulas consistently.

Any outcome/performance access is a hard stop:

```text
H40_R3R2_OUTCOME_EXPOSURE_ABORT
```

---

## Objective

After R3R2, an independent implementation must be able to implement every feature and gate referenced by the active 168 rows **without choosing a formula, endpoint convention, tail orientation, equality rule, fit population, or event-state interpretation**.

Natural-language labels such as `donchian_breakout_sign`, `true_range_expansion`, `failed_break_and_close_back_reversal`, `realized_volatility_percentile`, or `crowding_quantile_interaction` are insufficient unless accompanied by exact executable mathematics/pseudocode.

Use only pre-outcome scientific coherence, simplicity, symmetry, and the already-authorized family semantics. Never justify a choice by realized performance.

---

## 1. Freeze one canonical hourly bar-index convention

Define one notation used by all active candle features.

At minimum freeze:

```text
decision timestamp t
hourly bar indexing relative to t
which bar is the last admissible closed bar
open_time / close_time semantics
strict condition close_time < t
window [start,end] or half-open membership
exact endpoint for W-hour returns/ranges
minimum number of required hourly bars
missing hour behavior
duplicate timestamp behavior
boundary/equality behavior
```

Provide executable pseudocode sufficient to prove no off-by-one difference exists between independent implementations.

Preserve P0(t) semantics from BASE: the linear-perpetual 1h open at t is an event observable only and is not silently inserted into lagged features.

---

## 2. Freeze executable D1 formulas

For both:

```text
D1_V1_RETURN_4H
D1_V2_RETURN_12H
```

freeze exactly:

```text
input bars
return formula
lookback endpoint convention
sign operator
zero/equality behavior
missing/duplicate behavior
required source projection
output schema
```

The result must be mechanically reproducible and remain pure lagged trend continuation.

---

## 3. Freeze executable D2 Donchian breakout formulas

For:

```text
D2_V1_BREAKOUT_24H
D2_V2_BREAKOUT_72H
```

freeze exactly:

```text
prior range start/end indices
whether the signal bar is excluded from the range
upper boundary definition
lower boundary definition
whether signal uses last closed-bar close only or any high/low touch
LONG condition
SHORT condition
NO_TRADE condition
upper/lower equality ownership
impossible/dual-break handling
missing/duplicate handling
output schema
```

Do not introduce the unused 168h variant.

---

## 4. Freeze executable D3 failed-move reversal state machine

For:

```text
D3_V1_FAILED_BREAK_24H
D3_V2_FAILED_BREAK_72H
```

freeze one exact chronology. The document must explicitly decide, pre-outcome, whether the hypothesis is for example:

```text
same closed bar breaches prior range intrabar and closes back inside
```

or another single deterministic BASE-consistent definition.

Freeze:

```text
prior range membership
which bar/event establishes the break
which bar/event establishes close-back
minimum/maximum event separation if more than one bar is used
LONG reversal condition
SHORT reversal condition
boundary equality behavior
dual upper+lower breach behavior
missing/duplicate behavior
output schema
```

No implementation choice may remain.

---

## 5. Preserve D4 formulas from R3R1

R3R1 D4 confirmation and target-relative divergence formulas are already frozen and must remain unchanged unless a purely notational clarification is required to align them with Section 1's bar-index convention.

Do not change:

```text
D4_V1_CONFIRMATION_4H common-sign semantics
D4_V2_DIVERGENCE_8H target-relative continuation semantics
POOLED per-product projection
zero -> NO_TRADE
cross-asset source requirement
```

---

## 6. Freeze active R_VOL_RANGE raw scalar and percentile algorithm

For:

```text
R_VOL_RANGE_V1_24H
```

R3R1 state boundaries are preserved:

```text
LOW  [0.00,0.40)
MID  [0.40,0.60)   <- only pass state
HIGH [0.60,1.00]
```

R3R2 must freeze the exact scalar BEFORE percentile ranking. The phrase `realized-volatility / true-range` is prohibited as an unresolved alternative.

Choose exactly one BASE-consistent scalar and define it mathematically. Freeze at minimum:

```text
raw inputs
exact 24h aggregation formula
return/log-return or true-range formula as applicable
population/sample denominator if variance is used
whether values are normalized by price
fit population: per product vs pooled
training sample universe
empirical percentile/rank algorithm
tie handling
small-sample behavior
lag-one-decision semantics
frozen interval semantics
missing/non-finite behavior
output schema
```

No label or future outcome may enter this feature.

---

## 7. Freeze active O_RANGE_EXPANSION raw scalar and percentile algorithm

For:

```text
O_RANGE_EXPANSION_V1_24H
```

R3R1 states are preserved:

```text
NONE      [0.00,0.60)
WATCH     [0.60,0.80)
ELIGIBLE  [0.80,1.00]   <- only proceed state
```

Freeze one exact pre-outcome range-expansion scalar. At minimum specify:

```text
current lagged 24h range statistic
baseline/reference statistic if expansion is a ratio/difference
all exact windows and whether they overlap
normalization
fit population
empirical percentile/rank algorithm
tie handling
lag rule
missing/non-finite behavior
output schema
```

The opportunity owner remains magnitude-only and non-directional.

---

## 8. Freeze D5 funding/crowding interaction completely

This is mandatory and must explicitly satisfy BASE Section 5.3:

```text
D5 must name its base directional family and whether the frozen hypothesis is
continuation or crowding reversal.
```

Already-fixed base variants remain:

```text
D5_V1_CROWDING_Q10_D1 -> D1_V1_RETURN_4H
D5_V2_CROWDING_Q20_D2 -> D2_V1_BREAKOUT_24H
D3+D5 pair component   -> D3_V1_FAILED_BREAK_24H
```

For every D5 component freeze exactly:

```text
raw funding/crowding source field
last-settled funding selection at t
scalar crowding formula
whether funding is signed, absolute, or aligned with base side
fit population (per product; pooled only if explicitly authorized)
training empirical distribution
quantile algorithm and tie behavior
meaning of q=0.10 / q=0.20
upper/lower/two-sided tail orientation
exact threshold comparison including equality
CONTINUATION or CROWDING_REVERSAL identity
exact pass/confirm/veto behavior
side ownership (base owner only)
missing/non-finite behavior
output schema
```

Do not retain the ambiguous rule `crowding >= 0.10 percentile` unless it is expanded into a mathematically unambiguous tail definition and is intentionally the frozen hypothesis.

Prefer the smallest coherent interpretation that uses q as a tail parameter and does not cause a semantic contradiction with the term `crowding`. Do not use outcomes to decide.

Funding never supplies independent side.

---

## 9. Pair formulas

Preserve the four authorized pair identities/order from R3.

Once active components are defined, state exact pair operator pseudocode for:

```text
D1 + D4
D1 + D5
D2 + D4
D3 + D5
```

Each must deterministically map component outputs to `LONG/SHORT/NO_TRADE`.

Preserve:

```text
base owner supplies side
secondary component only confirms/vetoes/conditions
conflict -> NO_TRADE
missing component -> NO_TRADE
no depth > 2
```

---

## 10. Registry-only contracts: exact or explicitly non-implementable stubs

Do not invent unused formulas merely to make the document look complete.

For the eight registry-only contracts not active in the 168 ledger, choose one uniform policy:

### Preferred policy

Mark them:

```text
AUTHORIZED_FAMILY_STUB_NOT_ACTIVE_NOT_IMPLEMENTABLE_IN_H40_V1
ACTIVE_IN_168_LEDGER = false
```

Freeze only the BASE-authorized family envelope, allowed lookbacks/source class, and prohibition on entering H40 V1 without a new protocol authority. Explicitly state that their incomplete transform semantics are **not executable contracts** and are excluded from claims that every registry-only formula is frozen.

OR, if you elect to keep them executable, every one must receive exact mathematics at the same determinism standard as Sections 2–8.

No unused registry contract may silently become active.

Hash semantics must distinguish active executable contracts from inactive family stubs.

---

## 11. Correct Git object-id and canonical JSON wording

Supersede R3R1 wording that calls 40-hex commit IDs `Git commit SHA-256`.

Required rule:

```text
Every authority commit identity is treated as the exact opaque Git object-id string
returned by GitHub for that authority commit. Do not infer or validate the hash
algorithm from string length in protocol semantics.
```

For canonical objects state:

```text
repository canonical_json uses sort_keys=True;
object insertion order is not semantic;
arrays/lists remain ordered where protocol says order is semantic.
```

Preserve the non-self-referential acceptance-published `protocol_authority_hash` procedure, updated to include the eventual exact R3R2 commit identity in the cumulative authority preimage if needed. Define that addition explicitly and without self-reference.

---

## 12. Hash closure

Update the cumulative semantic hash preimages only as necessary so every new executable field introduced by R3R2 is content-bound.

At minimum prove changes to any of these change the appropriate feature/direction/configuration/ledger identity:

```text
bar indexing
raw scalar formula
range endpoints
percentile algorithm
tie rule
fit population
D3 state chronology
D5 funding scalar
D5 tail orientation
D5 continuation-vs-reversal identity
threshold equality rule
pair operator
active/stub registry state
```

Do not create caller-forgeable production authority. P2R1 remains responsible for deriving/verifying runtime authority from accepted P1 objects.

---

## 13. Required self-audit

Before commit prove from cumulative R3 + R3R1 + R3R2 alone:

1. all active 168 feature transforms are executable without scientific choice;
2. D1/D2/D3 have exact bar/event formulas;
3. D4 remains exactly R3R1-authorized;
4. R_VOL raw scalar + percentile procedure is unique;
5. O_RANGE raw scalar + percentile procedure is unique;
6. each D5 component has one exact base variant and one exact funding-tail interaction;
7. every D5 explicitly states continuation or crowding reversal;
8. all four pair operators are unique;
9. inactive registry families cannot enter V1 and are honestly classified as executable or stubs;
10. every material semantic field is hash-bound;
11. Git object-id wording is algorithm-agnostic;
12. no outcome/performance/protected surface was accessed;
13. exact 168 structure/order/template/source status remains unchanged;
14. P2R1 remains unauthorized pending independent acceptance.

## Success markers

If and only if all above are closed, include:

```text
H40_PROTOCOL_V1_R3R2_REPAIR_COMPLETE_PENDING_INDEPENDENT_REVIEW
H40R3R2_ACTIVE_FEATURE_FORMULAS_FROZEN
H40R3R2_D5_INTERACTION_FROZEN
H40R3R2_BAR_INDEXING_FROZEN
H40R3R2_REGISTRY_STUB_POLICY_FROZEN
H40R3R2_HASH_PREIMAGES_FROZEN
H40_NO_LABELS_COMPUTED
H40_NO_PERFORMANCE_METRICS_COMPUTED
H40_NO_CANDIDATE_SELECTED
H40_P2R1_NOT_AUTHORIZED_PENDING_R3R2_REVIEW
H40_P3_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

After commit/push, report the exact docs commit SHA. Do not modify code and do not start P2R1.

## Model routing

```text
PRIMARY AUTHOR
GLM-5.3 High/Deep OR GPT-5.6 Sol High

GLM-5.2
Acceptable if run in strongest reasoning mode and instructed to read BASE/R3/R3R1/reviews fully.

GEMINI FLASH
Not preferred for choosing the final formulas/tail semantics. Safe only for formatting or consistency/count checks after semantics are fixed.

INDEPENDENT REVIEW
GPT-5.6 Sol High

ASTRA
Not required. Reserve for candidate LOCK / confirmation-unblind authority gates.
```
