# Gemini 3.8 Flash Execution Prompt — Execution Reconcile Environment / Account Authority Closure

## ROLE

You are a constrained execution-safety analysis executor.

Your task is to produce a detailed, evidence-backed **candidate policy closure** for ASTRA-B-04.

You are NOT authorized to enable trading.

You are NOT the final reviewer.

Your output will be independently reviewed by GPT-5.6 Sol before implementation.

Repository:

```text
EXASHXE/btc-quant-agent
```

Exact implementation SHA:

```text
1dad1b6eba6a1610a6af365d1fbeac0376dab5d1
```

Implementation branch:

```text
v0.5-refactor
```

Docs branch:

```text
v0.5-refactor-docs
```

Finding:

```text
ASTRA-B-04 BLOCKER
ExecutionService.reconcile() may use current LIVE client against a persisted TESTNET plan/order without environment/account agreement.
```

Required artifact:

```text
reviews/v0.5/
V0.5.1_EXECUTION_RECONCILE_ENVIRONMENT_AUTHORITY_CLOSURE.md
```

This document is PROPOSED policy pending independent Sol acceptance.

---

# 0. HARD SAFETY RULES

Current posture remains:

```text
EXECUTION_DISABLED
auto_execute=false
live trading unauthorized
```

Do NOT:

```text
enable live mode
enable auto_execute
place any real order
cancel any real order
call Binance network APIs
use real API keys
read secrets
modify implementation code
modify CI
trigger/rerun/poll/wait CI
```

If tests are inspected, use existing source only.

If synthetic reasoning is needed, use fake transports conceptually; do not add tests in this docs-only task.

---

# 1. REQUIRED READING

Read in this order.

## 1.1 Controller handover

```text
docs/handover/
V0.5.1_QUANT_AGENT_PHASE_HANDOVER_2026-09-21.md
```

## 1.2 Astra audit

```text
reviews/v0.5/
V0.5.1_ASTRA_PRE_DISCOVERY_FULL_SYSTEM_AUDIT.md

commit:
ebc904c4b33f2b367c12a3725b418128631f928c
```

Read ASTRA-B-04 in full.

## 1.3 Sol adjudication

```text
reviews/v0.5/
V0.5.1_ASTRA_PRE_DISCOVERY_SOL_ADJUDICATION.md

commit:
b504fd58c3274549e8ffcaa99975f8d63f52a11e
```

## 1.4 Exact execution code at audited SHA

Read:

```text
src/btc_quant_agent/execution/service.py
src/btc_quant_agent/execution/guard.py
src/btc_quant_agent/execution/models.py
src/btc_quant_agent/execution/binance_signed.py
src/btc_quant_agent/execution/repository.py
src/btc_quant_agent/cli.py
tests/test_execution.py
tests/test_execution_guard.py
configs/default.toml
```

Also search exact source for all signed/write-capable execution methods and all call sites of:

```text
reconcile
prepare_close
submit_close
cancel_entry
place_order
place_protective_order
cancel_order
reduceOnly
allow_live
auto_execute
execution.mode
```

---

# 2. EXECUTION METHOD

Perform exactly these phases.

## PHASE 1 — Build write-surface inventory

Create a table:

```text
path
symbol
operation
signed_read_or_write
can_open_or_increase_exposure
can_reduce_exposure
can_cancel
environment_source
plan_environment_checked
account_checked
plan_hash_checked
guard_called
reachable_from_CLI
```

Include every signed query/write path you can find.

At minimum inspect:

```text
submit entry
cancel entry
reconcile
protective stop
take profit
failsafe market close
prepare close
submit close
```

Do not assume ASTRA-B-04 is the only environment mismatch.

## PHASE 2 — Reproduce the authority mismatch logically

Trace exact current reconcile sequence:

```text
persisted plan load
persisted order load
current config mode read
signed client selected
exchange query/write
```

Identify the first signed interaction.

List every authority fact NOT proven before that first signed interaction.

At minimum test the logic against:

```text
TESTNET plan + LIVE current config
LIVE plan + TESTNET current config
PAPER plan + LIVE current config
same numeric order ID in different environment
different account same environment
mutated plan payload
stale plan
```

## PHASE 3 — Choose deployment policy

Evaluate exactly these two policies.

### Policy A

```text
Research-disabled deployment exposes zero signed-write capability.
Any emergency recovery of an old live position is performed by a separate emergency execution tool/deployment outside the research process.
```

### Policy B

```text
Research-disabled deployment may perform only provenance-bound risk-reducing writes for a position/order that was previously created under accepted execution authority in the same environment/account.
No opening/increasing exposure write is allowed.
```

For each evaluate:

```text
safety
operational recovery
complexity
account/environment spoofing risk
implementation scope
auditability
failure modes
```

Then choose one.

If unsure, choose the more fail-closed policy and mark the operational tradeoff.

Do not let existing implementation convenience decide the policy.

## PHASE 4 — Define canonical execution environment identity

Propose exact schema:

```text
schema_id
mode
exchange_family
environment_id
normalized_rest_base_url
credential_namespace_id
account_authority_id
position_mode
margin_mode
```

For EACH field specify:

```text
required_or_optional
source_of_truth
secret_or_non_secret
normalization_rule
when_verified
whether persisted
what mismatch means
```

Rules:

- never persist API key/secret;
- TESTNET and LIVE must always produce different environment identities;
- same order ID across environments must not collide;
- account identity must be stable enough to prevent cross-account reconciliation.

Choose account identity strategy:

```text
SIGNED_REMOTE_ACCOUNT_ID
LOCAL_CONFIGURED_ACCOUNT_AUTHORITY_ID
BOTH
```

Explain why.

## PHASE 5 — Define persisted plan authority

Specify exact plan fields that MUST be bound into a canonical plan hash.

At minimum evaluate:

```text
plan_id
signal_id
symbol
direction
mode
execution_environment_id
created_at
expires_at
quantity
notional
leverage
entry geometry
stop geometry
take-profit geometry
authorization identity
confirmation identity
```

For each field:

```text
include_in_hash YES/NO
reason
mutation effect
```

Required property:

```text
mutated persisted plan + old plan_hash
=> fail before signed interaction
```

## PHASE 6 — Define persisted order binding

Specify canonical entry-order receipt fields.

At minimum:

```text
plan_id
plan_hash
execution_environment_id
account_authority_id
symbol
exchange_order_id
client_order_id
mode
submitted_at
order_side
order_type
quantity
```

For each state whether required.

Required property:

```text
same numeric exchange_order_id in another environment/account
!= same authority
```

## PHASE 7 — Define reconcile preflight

Write exact ordered preflight algorithm.

It MUST complete before first signed query/write.

Minimum order:

```text
R1 load persisted plan
R2 recompute/verify plan hash
R3 verify plan type/status
R4 verify not expired unless policy explicitly allows protected unwind
R5 resolve persisted execution_environment_id
R6 resolve current runtime execution_environment_id
R7 require equality
R8 resolve current account identity
R9 require account equality
R10 load persisted entry order
R11 require order -> same plan_hash
R12 require order -> same environment/account
R13 require symbol equality
R14 classify intended action
R15 require action allowed by deployment policy
R16 only now create/use signed exchange interaction
```

If client construction itself causes signed/network interaction in current architecture, state that environment authority must be checked before such construction.

No signed exchange call may occur before R1-R15 succeed.

## PHASE 8 — Define risk-reducing authority

Classify operations:

```text
ENTRY_OPEN
ENTRY_INCREASE
CANCEL_UNFILLED_ENTRY
PROTECTIVE_STOP
PROTECTIVE_TAKE_PROFIT
REDUCE_ONLY_CLOSE
FAILSAFE_MARKET_REDUCE
MANUAL_CLOSE
```

For each specify:

```text
opens exposure?
can increase exposure?
requires original entry authority?
requires environment equality?
requires account equality?
requires current position verification?
allowed under EXECUTION_DISABLED?
```

Use the chosen Policy A/B.

## PHASE 9 — Define failsafe semantics

Answer ALL:

1. Must same account/environment be verified?
2. Must position be queried first?
3. Must side match current position direction?
4. Must quantity be capped to current reducible position?
5. What if another system changed position?
6. What if position is zero?
7. What if position flipped?
8. What if one protective order already exists?
9. What if plan expired?
10. What audit evidence is required?
11. Can failsafe ever increase absolute exposure?
12. How is `reduceOnly=true` verified in request construction?
13. How is exchange response verified?
14. What if exchange rejects reduceOnly?
15. What happens under environment mismatch?

Required invariant:

```text
failsafe must never increase absolute exposure
```

## PHASE 10 — Close/cancel consistency

Analyze:

```text
prepare_close
submit_close
cancel_entry
reconcile
```

For each provide exact preflight requirements.

Do not permit a repair that secures reconcile while leaving equivalent cross-environment writes elsewhere.

## PHASE 11 — Adversarial matrix E01-E27

Produce one complete table for:

```text
E01 TESTNET plan + LIVE config
E02 LIVE plan + TESTNET config
E03 PAPER plan + LIVE config
E04 DISABLED config + stale plan
E05 corrupted plan_hash
E06 mutated plan + old hash
E07 order from another environment
E08 order from another plan
E09 same numeric order ID other environment
E10 wrong symbol
E11 wrong account
E12 credential namespace changed
E13 base URL changed
E14 account identity changed
E15 position mode changed
E16 stale/expired plan
E17 partial fill
E18 protective-order partial failure
E19 failsafe quantity > position
E20 failsafe direction mismatch
E21 live authority absent
E22 auto_execute false
E23 allow_live false after prior authorized live fill
E24 replay old reconcile receipt
E25 cancel-entry cross-environment
E26 close-plan cross-environment
E27 fake client receives no write before authority preflight
```

Columns:

```text
case
current risk
required check
fail-closed behavior
required test
```

No case may be omitted.

## PHASE 12 — Implementation requirements

Write exact requirements by file/symbol.

At minimum:

```text
execution/models.py
execution/repository.py
execution/guard.py
execution/service.py
execution/binance_signed.py
cli.py
tests/test_execution.py
tests/test_execution_guard.py
```

For every change state:

```text
file
symbol
change
why
new invariant
tests
```

Do not implement code.

## PHASE 13 — Self-audit

Before commit, mark YES/NO:

```text
X01 exact SHA used
X02 Astra B-04 read
X03 Sol adjudication read
X04 all signed write surfaces inventoried
X05 deployment policy A/B chosen
X06 environment schema explicit
X07 TESTNET != LIVE guaranteed
X08 account identity strategy chosen
X09 plan hash schema explicit
X10 order binding explicit
X11 reconcile preflight ordered
X12 no signed call before preflight
X13 risk-reducing policy explicit
X14 failsafe cannot increase exposure
X15 position verification policy explicit
X16 close/cancel paths covered
X17 E01-E27 complete
X18 live trading remains unauthorized
X19 auto_execute remains false by default
X20 no code modified
X21 no real network calls
X22 no secrets read
X23 no CI triggered
```

If any item is NO, repair document before commit.

---

# 3. REQUIRED ARTIFACT STRUCTURE

Use exactly these top-level sections:

```text
1. Document status and audited state
2. ASTRA-B-04 evidence and current call graph
3. Signed execution surface inventory
4. Deployment policy decision
5. Execution environment authority schema
6. Plan authority and canonical plan hash
7. Entry-order receipt/environment binding
8. Reconcile authority preflight
9. Risk-reducing action policy
10. Failsafe market-reduce policy
11. Close/cancel consistency
12. E01-E27 adversarial matrix
13. Implementation requirements by file/symbol
14. Required implementation tests
15. Residual risks
16. Self-audit X01-X23
17. Proposed post-closure state
```

---

# 4. REQUIRED DOCUMENT STATUS

First section MUST contain:

```text
DOCUMENT_TYPE = GEMINI_PROPOSED_EXECUTION_AUTHORITY_CLOSURE
EXECUTOR = GEMINI_3_8_FLASH
REVIEW_STATUS = PENDING_INDEPENDENT_SOL_ACCEPTANCE
IMPLEMENTATION_AUTHORIZATION = NOT_GRANTED_BY_THIS_DOCUMENT

EXECUTION_DISABLED
LIVE_TRADING_NOT_AUTHORIZED
```

Do NOT claim:

```text
EXECUTION_B04_IMPLEMENTATION_REPAIR_AUTHORIZED
LIVE_TRADING_AUTHORIZED
AUTO_EXECUTION_AUTHORIZED
```

Allowed final state:

```text
EXECUTION_B04_PROPOSED_SEMANTICS_COMPLETE_FOR_SOL_REVIEW
```

or, if unresolved:

```text
EXECUTION_B04_PROPOSED_SEMANTICS_INCOMPLETE
SOL_DECISION_REQUIRED
```

---

# 5. GIT DELIVERY

Create only:

```text
reviews/v0.5/
V0.5.1_EXECUTION_RECONCILE_ENVIRONMENT_AUTHORITY_CLOSURE.md
```

Commit and push only to:

```text
origin/v0.5-refactor-docs
```

Suggested commit message:

```text
docs(execution): propose reconcile environment authority closure
```

Do not modify `v0.5-refactor`.

---

# 6. FINAL CHAT RESPONSE FORMAT

Return exactly:

```text
artifact_path:
artifact_commit_sha:
audited_implementation_sha:

chosen_policy: A|B
account_identity_strategy: SIGNED_REMOTE_ACCOUNT_ID|LOCAL_CONFIGURED_ACCOUNT_AUTHORITY_ID|BOTH

environment_schema_complete: YES|NO
plan_hash_schema_complete: YES|NO
order_binding_complete: YES|NO
reconcile_preflight_complete: YES|NO
failsafe_policy_complete: YES|NO
close_cancel_covered: YES|NO
E01_E27_complete: YES|NO
self_audit_X01_X23_complete: YES|NO

implementation_modified: NO
network_calls_made: NO
secrets_read: NO
ci_triggered: NO
live_trading_authorized: NO

final_state:
EXECUTION_B04_PROPOSED_SEMANTICS_COMPLETE_FOR_SOL_REVIEW
or
EXECUTION_B04_PROPOSED_SEMANTICS_INCOMPLETE
```

No extra narrative after these fields.
