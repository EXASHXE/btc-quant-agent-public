# Agent Prompt — V0.5.1 Execution B-04 Research-Disabled Environment Authority Implementation

## Execution status

```text
PROMPT_STATUS = READY_BUT_SEQUENCED_AFTER_F01R3_IMPLEMENTATION
```

Do NOT execute this prompt concurrently with the F01R3 implementation against the same `v0.5-refactor` branch.

This prompt must be executed only after:

1. F01R3 implementation has been committed and pushed to `origin/v0.5-refactor`;
2. the executor has fetched that new HEAD;
3. the executor records that exact HEAD as `starting_sha`.

If `origin/v0.5-refactor` is still:

```text
1dad1b6eba6a1610a6af365d1fbeac0376dab5d1
```

then Track A implementation has not landed; STOP with:

```text
WAITING_FOR_F01R3_IMPLEMENTATION_BASE
```

Do not implement Track B from the stale base.

---

# 1. Mission

Close:

```text
ASTRA-B-04 BLOCKER
```

under the independently accepted current execution policy:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

The current repository must expose no production signed Binance capability.

This repair also adds offline environment/account/plan/order binding so a future separately authorized execution phase cannot accidentally reconcile a persisted TESTNET plan through a LIVE environment.

Repository:

```text
EXASHXE/btc-quant-agent
```

Implementation branch:

```text
v0.5-refactor
```

Required authority inputs:

```text
reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_FULL_SYSTEM_AUDIT.md
commit ebc904c4b33f2b367c12a3725b418128631f928c

reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_SOL_ADJUDICATION.md
commit b504fd58c3274549e8ffcaa99975f8d63f52a11e

reviews/v0.5/V0.5.1_EXECUTION_RECONCILE_ENVIRONMENT_AUTHORITY_CLOSURE.md
commit d6b7d71073a13b2b7aea4240fb28b3721badbe91

reviews/v0.5/V0.5.1_EXECUTION_B04_SOL_INDEPENDENT_ACCEPTANCE.md
commit cabb06be87c5874e5a32c37b5f4ddabe67a9e086
```

The Sol acceptance is normative where it differs from Gemini.

---

# 2. Non-negotiable policy

Current v0.5.1 authority is:

```text
RESEARCH_DISABLED_V1
```

Therefore production code must not authorize:

```text
signed Binance order query
signed account query
signed position query
place order
cancel order
protective STOP
protective TAKE_PROFIT
reduce-only close
failsafe market close
testnet write
live write
automatic execution
```

Do NOT implement Gemini's future Policy B as currently active authority.

Risk-reducing signed writes are future design only.

Current research code is not the emergency execution surface.

Paper/local simulation operations may remain available only if they instantiate no signed Binance client and make no signed exchange request.

---

# 3. Required offline environment authority

Implement typed immutable:

```text
EXECUTION_ENVIRONMENT_AUTHORITY_V1
```

Use a clear Python type, e.g.:

```text
ExecutionEnvironmentAuthority
```

Required fields:

```text
schema_id
mode
exchange_family
environment_id
normalized_rest_base_url
credential_namespace_id
account_authority_id
position_mode_assumption
margin_mode_assumption
```

## 3.1 Mode

Accepted values must derive from existing `ExecutionMode`:

```text
paper
testnet
live
```

Do not treat `disabled` as a persisted exchange environment for an executable plan.

## 3.2 Environment IDs

At minimum distinguish:

```text
local_paper
binance_usdm_testnet
binance_usdm_live
```

TESTNET and LIVE must never produce the same environment identity.

## 3.3 URL normalization

Canonicalize only enough to identify the accepted environment.

Do not accept arbitrary equivalent-looking URLs as authoritative.

Prefer an allowlisted mapping from execution mode/environment to canonical endpoint identity rather than trusting caller URL text.

Persist the normalized environment identity used when the plan is created.

## 3.4 Credential namespace

Persist only a non-secret namespace label, e.g.:

```text
NONE
BINANCE_TESTNET
BINANCE_LIVE
```

Never persist:

```text
API key
API secret
hash(API secret)
reversible credential material
```

## 3.5 Account authority

Use an explicitly configured non-secret stable account/subaccount authority ID or opaque label.

Do NOT make long-lived plan authority depend on hashing the API key.

Do NOT perform a remote signed account query in the current repair.

Future remote account attestation requires a separately accepted execution authority.

---

# 4. Plan models

Extend:

```text
ExecutionPlan
ClosePlan
```

to bind at minimum:

```text
mode
execution_environment_id
credential_namespace_id
account_authority_id
```

into their existing canonical `calculated_hash()` preimage.

Preserve all existing risk/geometry fields already bound by the plan hash.

Required invariant:

```text
persisted plan payload mutation + old plan_hash
=> fail closed before any signed capability
```

Do not infer historical plan environment from current runtime config.

## 4.1 Compatibility

Existing persisted plans missing the new authority fields MUST NOT be silently upgraded into exchange authority.

For paper-only migration, an explicit safe compatibility path may be allowed if no signed client is reachable.

Historical testnet/live plans without new authority fields must fail closed.

---

# 5. Order receipt binding

Upgrade the persisted order receipt/schema to bind at minimum:

```text
plan_id
plan_hash
role
mode
execution_environment_id
credential_namespace_id
account_authority_id
symbol
exchange_order_id
client_order_id when available
status
submitted_at
raw
```

Do not trust:

```text
order_id
```

alone.

Required identity relationship:

```text
plan_hash
+
environment_id
+
account_authority_id
+
symbol
+
exchange/client order identity
```

must match before an order receipt can be used.

Migrate storage schema carefully and deterministically.

Do not overwrite/misinterpret old exchange receipts as V2 authority.

---

# 6. Shared offline preflight

Implement one common offline authority preflight used by every exchange-capable service method.

Recommended type/function:

```text
validate_execution_environment_preflight(...)
```

Exact naming may differ.

Before constructing or using any production `BinanceSignedClient`:

```text
P01 load persisted plan
P02 parse typed plan
P03 recompute plan hash
P04 require exact stored plan_hash equality
P05 verify plan kind/status
P06 resolve persisted mode/environment/account/credential namespace
P07 derive current configured offline environment/account/credential namespace
P08 require exact persisted/current equality
P09 load relevant persisted order receipt when action requires one
P10 require receipt.plan_hash == plan.plan_hash
P11 require receipt environment/account/credential namespace == plan/current authority
P12 require receipt symbol == plan.symbol
P13 classify requested action
P14 require an accepted execution capability authority
P15 under RESEARCH_DISABLED_V1: fail closed
P16 therefore production signed client construction/network interaction is unreachable
```

The current repository has:

```text
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

Represent this explicitly in code rather than relying only on defaults like `allow_live=false`.

Configuration is not authority.

Environment variables are not authority.

Credential presence is not authority.

---

# 7. Hard signed-client capability fence

The current production service must have an unambiguous capability boundary.

A method must not reach:

```text
_signed_client()
BinanceSignedClient(...)
query_order
positions
position_mode
realized_pnl
place_order
place_protective_order
cancel_order
cancel_protective_order
```

unless a future accepted execution capability exists.

For v0.5.1 that capability does not exist.

## 7.1 Injected clients

Existing injected fake clients are useful for tests.

Ensure:

- clearly test-only construction can exercise mechanics;
- production authorization cannot be created merely because a client object is injected;
- no service method treats “client exists” as execution authority.

Prefer explicit test-only seams rather than magic runtime detection.

## 7.2 Paper mode

Paper execution may:

- create local paper receipts;
- mutate local paper plan status;
- simulate local protections.

Paper mode must not:

- instantiate `BinanceSignedClient`;
- read signed Binance account/order state;
- issue exchange writes.

Public unauthenticated market-data calls are outside this signed-capability boundary.

---

# 8. Apply the fence consistently

Audit and repair all current service paths:

```text
submit_entry
reconcile
reconcile_open_plans
prepare_close
submit_close
cancel_entry
auto_submit
```

and any other current caller of signed-client methods.

## 8.1 submit_entry

Production TESTNET/LIVE submit must fail at execution-capability authority before signed client construction.

Paper behavior may remain local.

Keep existing plan validation, hash, TTL and safety checks.

## 8.2 reconcile

First verify:

```text
plan hash
plan status
historical environment
current offline environment
account authority
credential namespace
entry receipt binding
symbol
action
execution capability
```

Under current policy it then fails closed before any signed query/write.

This closes the TESTNET-plan/LIVE-client defect.

## 8.3 reconcile_open_plans

Must not turn a disabled capability into multiple attempted signed calls.

Under current research-disabled authority it should return a clear blocked/no-op operational result without accessing signed clients.

Do not silently reinterpret errors as successful reconciliation.

## 8.4 prepare_close

If it requires a signed position query for TESTNET/LIVE, it is blocked under current authority.

Paper/local close preparation may remain local only if it has sufficient local simulated state.

## 8.5 submit_close / cancel_entry

Both require the same plan/environment/account/receipt binding and execution-capability fence.

The fact that an operation is risk-reducing does not grant signed authority in v0.5.1.

---

# 9. Current failsafe behavior

Current `reconcile()` contains a reduce-only MARKET failsafe after protective-order failure.

Under:

```text
RESEARCH_DISABLED_V1
```

that production signed path must be unreachable.

Do not spend this repair implementing a new live failsafe protocol.

Future separately authorized execution may reintroduce/enable provenance-bound failsafe mechanics.

Preserve code only if it is behind a provably unreachable capability gate; alternatively isolate/retire it if that is cleaner without broad refactor.

---

# 10. No endpoint-specific remote account authority

Do not freeze a signed Binance account endpoint into this implementation.

Do not require:

```text
/fapi/v2/account
/fapi/v3/account
```

or another remote account API for current authority.

Remote signed attestation is future execution-policy scope.

Current authority is intentionally offline and fail-closed.

---

# 11. Storage migration

Inspect existing repository/storage implementation before modifying schema.

Required properties:

- migration is deterministic;
- old paper records may remain readable without granting signed authority;
- old testnet/live records missing environment/account binding fail closed;
- new records contain exact environment authority fields;
- order receipt binding is preserved through persistence/reload;
- plan hash survives round trip;
- no secret is stored.

Do not delete user data as a migration shortcut.

---

# 12. Mandatory adversarial tests

Add tests using fake/in-memory transports only.

At minimum:

```text
E01 TESTNET plan + LIVE current config blocked before signed client construction
E02 LIVE plan + TESTNET config blocked
E03 PAPER plan + LIVE config blocked
E04 execution disabled/research authority blocks signed capability

E05 corrupted plan hash blocked
E06 mutated persisted plan + old hash blocked

E07 order receipt foreign environment blocked
E08 order receipt foreign plan hash blocked
E09 same numeric order ID different environment blocked
E10 wrong symbol blocked
E11 account authority mismatch blocked
E12 credential namespace mismatch blocked
E13 canonical endpoint/environment mismatch blocked
E14 order/plan account mismatch blocked
E15 position/margin assumption mismatch where relevant blocked
E16 expired/stale plan cannot grant capability

E17 current failsafe signed path unreachable
E18 current protective STOP signed path unreachable
E19 current protective TP signed path unreachable
E20 current reduce-only close signed path unreachable

E21 allow_live=true does not create authority
E22 auto_execute=true/confirmation env var does not create authority
E23 historical live/testnet plan cannot bypass RESEARCH_DISABLED_V1

E24 old reconcile receipt replay blocked
E25 cancel-entry cross-environment blocked
E26 close cross-environment blocked

E27 fake/injected client has zero calls when any offline preflight or capability check fails

E28 production _signed_client direct service path is authority-blocked
E29 signed order query blocked
E30 signed position query blocked
E31 signed account/user-trade query blocked if reachable
E32 paper path never instantiates BinanceSignedClient
E33 paper local behavior still works
E34 no secret persisted in plan/order records
E35 new environment/account fields survive repository round trip
E36 old testnet/live plan without V1 environment authority fails closed
```

Test both:

```text
client object not constructed
transport call count == 0
```

where technically observable.

No real network test is permitted.

---

# 13. Preserve H40 authority

Track B must not change:

```text
protocol_authority_hash
semantic_root_hash
structural_ledger_hash
persistence_replay_contract_v2_hash
lifecycle_semantic_root_hash_v3
lifecycle_governance_authority_v4_hash
H40 source projection
H40 search space
```

Do not edit H40 lifecycle authority except for mechanical compatibility imports if unavoidable.

No H40 outcomes may be accessed.

---

# 14. Local validation

Run:

```bash
ruff check .
mypy
pytest -q tests/test_execution.py tests/test_execution_guard.py
pytest -q tests/test_execution_environment_authority.py
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

If you extend existing test files rather than create the named test file, report exact equivalent targeted commands.

Do not:

```text
trigger CI
rerun CI
poll CI
wait for CI
modify CI
```

Normal push is allowed.

If automatic Actions cannot run because of quota:

```text
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

and do nothing further.

---

# 15. Git discipline

At execution time:

1. fetch `origin/v0.5-refactor`;
2. ensure Track A implementation is already present;
3. record exact current HEAD as `starting_sha`;
4. make Track B changes on the same branch;
5. do not rewrite Track A history;
6. commit;
7. push `origin/v0.5-refactor`.

Do not modify docs branch.

Do not merge `forward-evidence-health-lightweight`.

---

# 16. Allowed completion claims

If local gates pass:

```text
EXECUTION_B04_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_REVIEW

RESEARCH_DISABLED_V1_IMPLEMENTED
OFFLINE_EXECUTION_ENVIRONMENT_AUTHORITY_V1_IMPLEMENTED
PLAN_ORDER_ENVIRONMENT_BINDING_IMPLEMENTED
SIGNED_CAPABILITY_FENCE_IMPLEMENTED

EXECUTION_DISABLED
LIVE_TRADING_NOT_AUTHORIZED
TESTNET_WRITES_NOT_AUTHORIZED
AUTO_EXECUTION_NOT_AUTHORIZED
```

Do NOT claim:

```text
LIVE_EXECUTION_SAFE
LIVE_TRADING_AUTHORIZED
TESTNET_EXECUTION_AUTHORIZED
POLICY_B_AUTHORIZED
H40_P3_AUTHORIZED
```

---

# 17. Final response

Return:

```text
starting_sha:
final_commit_sha:
changed_files:

execution_policy:
environment_authority_schema:
plan_binding:
order_receipt_binding:
signed_capability_fence:
paper_path_status:
legacy_plan_migration:

targeted_tests:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

signed_network_calls_in_tests: 0
secrets_read_or_persisted: NO
live_authorized: NO
testnet_writes_authorized: NO
auto_execution_authorized: NO
ci_triggered_or_polled: NO

h40_hashes_unchanged: YES

final_state:
EXECUTION_B04_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_REVIEW
```

Recommended executor after Track A lands:

```text
Codex GPT-5.6 Sol Medium/High
or Gemini 3.8 Flash for this bounded implementation
```

Final independent review should still use GPT-5.6 Sol.
