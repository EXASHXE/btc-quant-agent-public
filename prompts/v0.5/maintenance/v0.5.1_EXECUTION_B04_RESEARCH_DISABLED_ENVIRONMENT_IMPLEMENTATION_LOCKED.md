# Agent Prompt — V0.5.1 Execution B-04 Research-Disabled Environment Authority Implementation (Locked Baseline)

## Execution status

```text
PROMPT_STATUS = AUTHORIZED_TO_EXECUTE
TRACK_A_F01R3 = FINAL_IMPLEMENTATION_ACCEPTED
```

Repository:

```text
EXASHXE/btc-quant-agent
```

Implementation branch:

```text
v0.5-refactor
```

Required exact starting HEAD:

```text
e5ef1e1b76125527dc6c524e21aa294ea1504a15
```

Before editing:

```bash
git fetch origin
git checkout v0.5-refactor
git rev-parse HEAD
git rev-parse origin/v0.5-refactor
```

Both values MUST equal:

```text
e5ef1e1b76125527dc6c524e21aa294ea1504a15
```

If not, STOP and report the mismatch. Do not rebase, reset, merge, or silently continue from another code state.

---

# 1. Mission

Close:

```text
ASTRA-B-04 BLOCKER
```

under the independently accepted execution policy:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

The current repository must expose no production signed Binance capability.

This repair also adds offline environment/account/plan/order binding so a future separately authorized execution phase cannot accidentally reconcile a persisted TESTNET plan through a LIVE environment/account.

This task must preserve the newly accepted H40 F01R3 trusted lifecycle core.

---

# 2. Required authority inputs

Read completely, in this order:

```text
reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_FULL_SYSTEM_AUDIT.md
commit ebc904c4b33f2b367c12a3725b418128631f928c

reviews/v0.5/V0.5.1_ASTRA_PRE_DISCOVERY_SOL_ADJUDICATION.md
commit b504fd58c3274549e8ffcaa99975f8d63f52a11e

reviews/v0.5/V0.5.1_EXECUTION_RECONCILE_ENVIRONMENT_AUTHORITY_CLOSURE.md
commit d6b7d71073a13b2b7aea4240fb28b3721badbe91

reviews/v0.5/V0.5.1_EXECUTION_B04_SOL_INDEPENDENT_ACCEPTANCE.md
commit cabb06be87c5874e5a32c37b5f4ddabe67a9e086

reviews/v0.5/V0.5.1_H40_F01R3_FINAL_IMPLEMENTATION_SOL_ACCEPTANCE.md
commit 473e451bbd902207232a83f50a7271d2aacef398
```

The Sol execution acceptance is normative where it differs from the Gemini proposal.

---

# 3. Non-negotiable execution policy

Current accepted v0.5.1 policy:

```text
RESEARCH_DISABLED_V1
```

Production code must not authorize:

```text
signed Binance order query
signed account query
signed position query
place order
cancel order
protective STOP
protective TAKE_PROFIT
reduce-only close
failsafe market close
testnet write
live write
automatic execution
```

Do NOT implement future Policy B as active authority.

Risk-reducing signed writes are future design only.

Current research code is not the emergency execution surface.

Paper/local simulation may remain available only if it instantiates no signed Binance client and makes no signed exchange request.

Configuration values, environment variables, confirmation flags and credential presence do not create authority.

---

# 4. Mandatory code inventory before editing

Inspect exact code at starting SHA.

At minimum inspect:

```text
src/btc_quant_agent/execution/models.py
src/btc_quant_agent/execution/repository.py
src/btc_quant_agent/execution/guard.py
src/btc_quant_agent/execution/service.py
src/btc_quant_agent/execution/binance_signed.py
src/btc_quant_agent/cli.py
configs/default.toml
tests/test_execution.py
tests/test_execution_guard.py
```

Search all production call sites for:

```text
_signed_client
BinanceSignedClient
query_order
positions
position_mode
realized_pnl
place_order
place_protective_order
cancel_order
cancel_protective_order
submit_entry
reconcile
reconcile_open_plans
prepare_close
submit_close
cancel_entry
auto_submit
allow_live
auto_execute
execution.mode
```

Build an internal inventory before implementation.

Do not assume only `reconcile()` needs repair.

---

# 5. Implement offline execution environment authority

Implement immutable typed:

```text
EXECUTION_ENVIRONMENT_AUTHORITY_V1
```

Recommended type:

```text
ExecutionEnvironmentAuthority
```

Required fields:

```text
schema_id
mode
exchange_family
environment_id
normalized_rest_base_url
credential_namespace_id
account_authority_id
position_mode_assumption
margin_mode_assumption
```

## 5.1 Mode

Accepted execution modes derive from existing `ExecutionMode`:

```text
paper
testnet
live
```

Do not treat disabled as an executable persisted exchange environment.

## 5.2 Canonical environment identity

At minimum distinguish:

```text
local_paper
binance_usdm_testnet
binance_usdm_live
```

TESTNET and LIVE must never produce the same identity.

Prefer allowlisted canonical environment/base-url mapping.

Do not let arbitrary caller URL strings define environment authority.

## 5.3 Credential namespace

Persist only a non-secret namespace such as:

```text
NONE
BINANCE_TESTNET
BINANCE_LIVE
```

Never persist:

```text
API key
API secret
hash(API secret)
reversible credential material
```

## 5.4 Account authority

Use an explicitly configured non-secret stable account/subaccount authority ID or opaque label.

Do not derive long-lived account authority from hashing API credentials.

Do not perform signed remote account attestation in this repair.

Future remote account attestation requires separately accepted execution authority.

---

# 6. Bind authority into ExecutionPlan / ClosePlan

Extend both models to bind at minimum:

```text
mode
execution_environment_id
credential_namespace_id
account_authority_id
```

inside their canonical calculated hash.

Preserve all existing risk/geometry fields already bound by the plan hash.

Required invariant:

```text
mutated persisted plan + old plan_hash
=> fail closed before any signed capability
```

Historical environment/account authority must come from persisted plan identity, never be inferred only from current config.

## Compatibility

Existing plans missing new authority fields:

```text
paper-only record:
may be migrated only through explicit safe local-only compatibility logic

testnet/live record:
must fail closed
must not receive inferred execution authority
```

No silent privilege upgrade.

---

# 7. Bind environment/account to persisted exchange order receipts

Upgrade persisted order receipt/schema to bind at minimum:

```text
plan_id
plan_hash
role
mode
execution_environment_id
credential_namespace_id
account_authority_id
symbol
exchange_order_id
client_order_id when available
status
submitted_at
raw
```

Do not treat order ID alone as globally authoritative.

Required compound relationship:

```text
plan_hash
+
execution_environment_id
+
account_authority_id
+
symbol
+
exchange/client order identity
```

must match before persisted order state can participate in any exchange-capable operation.

Migration must be deterministic and non-destructive.

---

# 8. Shared offline preflight

Implement one shared offline authority preflight used by every exchange-capable service path.

Recommended conceptual API:

```text
validate_execution_environment_preflight(...)
```

Before constructing or using any production `BinanceSignedClient`:

```text
P01 load persisted plan
P02 parse exact typed plan
P03 recompute plan hash
P04 require stored plan_hash equality
P05 verify plan kind/status
P06 resolve persisted mode/environment/account/credential namespace
P07 derive current configured offline environment/account/credential namespace
P08 require exact persisted/current equality
P09 load relevant order receipt if action requires it
P10 require receipt.plan_hash == plan.plan_hash
P11 require receipt environment/account/credential namespace == plan/current authority
P12 require receipt symbol == plan.symbol
P13 classify requested action
P14 require accepted execution capability authority
P15 RESEARCH_DISABLED_V1 => fail closed
P16 signed client/network interaction remains unreachable
```

No injected production client may bypass P01-P15.

---

# 9. Hard signed-client capability fence

Represent explicitly in code:

```text
ACCEPTED_EXECUTION_WRITE_AUTHORITY = NONE
```

or an equivalently explicit typed constant/capability representation.

Do not rely only on:

```text
allow_live=false
auto_execute=false
mode=paper
missing credentials
```

Those are configuration, not authority.

Production code must not reach:

```text
_signed_client()
BinanceSignedClient(...)
query_order
positions
position_mode
realized_pnl
place_order
place_protective_order
cancel_order
cancel_protective_order
```

under current accepted policy.

## Injected test clients

Fake clients/transports may be used for tests.

But:

- existence of an injected client never grants authority;
- production service methods still require the accepted capability boundary;
- use an explicit test-only seam where needed.

## Paper mode

Paper/local simulation may continue to:

- create local paper receipts;
- mutate local simulated plan state;
- simulate local protections.

Paper mode must never instantiate `BinanceSignedClient` or perform signed Binance queries/writes.

Public unauthenticated market-data behavior is outside this signed-capability gate.

---

# 10. Apply consistently across all execution methods

Audit and repair at minimum:

```text
submit_entry
reconcile
reconcile_open_plans
prepare_close
submit_close
cancel_entry
auto_submit
```

and any other signed-client call sites discovered in the inventory.

## submit_entry

TESTNET/LIVE production submit must fail at execution capability authority before signed client construction.

Paper behavior may remain local.

## reconcile

Before any signed activity validate:

```text
plan hash
plan status
persisted environment
current environment
account authority
credential namespace
entry receipt binding
symbol
action classification
execution capability
```

Under current policy it then fails closed.

This is the direct ASTRA-B-04 closure.

## reconcile_open_plans

Under research-disabled authority, do not fan out into repeated signed attempts.

Return an explicit blocked/no-op operational result with zero signed client calls.

Do not silently label blocked reconciliation as success.

## prepare_close

If TESTNET/LIVE preparation requires signed position/account state, block it.

Paper/local preparation may work only from local simulated state.

## submit_close / cancel_entry

Both require the exact same environment/account/receipt/capability boundary.

Risk-reducing intent does not grant authority in v0.5.1.

---

# 11. Failsafe path

Current reduce-only MARKET fallback is not authorized under:

```text
RESEARCH_DISABLED_V1
```

It must be unreachable through production current policy.

Do not implement future live/testnet failsafe semantics here.

Future Policy B may separately authorize provenance-bound risk-reducing operations.

---

# 12. Remote account attestation remains out of scope

Do not freeze a signed Binance endpoint such as:

```text
/fapi/v2/account
/fapi/v3/account
```

into current authority.

The accepted architecture is:

```text
Stage A current repair:
offline environment/account/plan/order authority

Stage B future:
signed remote account/position attestation after separate execution authorization
```

Current Stage B is unauthorized.

---

# 13. Storage migration

Required properties:

- deterministic migration;
- no destructive data deletion;
- new plans/order receipts persist authority fields;
- round-trip preserves calculated hashes;
- old paper data may remain locally usable only without signed authority;
- old testnet/live rows missing authority fields fail closed;
- secrets are never persisted.

If existing DB migration needs new columns, make it idempotent.

---

# 14. Mandatory adversarial tests E01-E36

Use fake/in-memory transports only.

```text
E01 TESTNET plan + LIVE config blocked before signed client construction
E02 LIVE plan + TESTNET config blocked
E03 PAPER plan + LIVE config blocked
E04 RESEARCH_DISABLED_V1 blocks signed capability

E05 corrupted plan hash blocked
E06 mutated persisted plan + old hash blocked

E07 foreign-environment order receipt blocked
E08 foreign-plan receipt blocked
E09 same numeric order ID in different environment blocked
E10 wrong symbol blocked
E11 account authority mismatch blocked
E12 credential namespace mismatch blocked
E13 canonical endpoint/environment mismatch blocked
E14 order/plan account mismatch blocked
E15 position/margin assumption mismatch where relevant blocked
E16 expired/stale plan cannot grant capability

E17 failsafe signed path unreachable
E18 protective STOP signed path unreachable
E19 protective TP signed path unreachable
E20 reduce-only close signed path unreachable

E21 allow_live=true does not create authority
E22 auto_execute=true / confirmation variable does not create authority
E23 historical live/testnet plan cannot bypass research-disabled policy

E24 old reconcile receipt replay blocked
E25 cancel-entry cross-environment blocked
E26 close cross-environment blocked

E27 fake/injected client call count remains zero on any failed preflight/capability gate

E28 production _signed_client path authority-blocked
E29 signed order query blocked
E30 signed position query blocked
E31 signed account/user-trade query blocked if reachable
E32 paper path never instantiates BinanceSignedClient
E33 paper local behavior remains functional
E34 no secret persisted in plan/order records
E35 new authority fields survive persistence round-trip
E36 old testnet/live plan without V1 environment authority fails closed
```

Where observable, assert both:

```text
client construction count == 0
transport/signed call count == 0
```

No real network test.

---

# 15. Preserve accepted H40 F01R3 authority

Track B MUST NOT modify:

```text
protocol_authority_hash
semantic_root_hash
structural_ledger_hash
persistence_replay_contract_v2_hash
lifecycle_semantic_root_hash_v3
lifecycle_governance_authority_v4_hash
H40 source projection
H40 search space
```

Accepted exact values:

```text
persistence_replay_contract_v2_hash =
5f014b867be17019c2be91ff48f8e23a43ed29678fc5589430ba174046fceaae

lifecycle_semantic_root_hash_v3 =
f0aa35eb91055a76129eae5c00be38174fcc13ae9fe08521d9202e93dc5d43d4

lifecycle_governance_authority_v4_hash =
72924dd22b3c9283964cdf368f7a754bd6f703a110989187620b36076a25a511
```

Do not edit H40 lifecycle code unless a purely mechanical import/type compatibility change is unavoidable.

No H40 outcomes may be accessed.

---

# 16. Local validation

Run:

```bash
ruff check .
mypy
pytest -q tests/test_execution.py tests/test_execution_guard.py
pytest -q tests/test_execution_environment_authority.py
pytest -q
python -m compileall -q src skill-template/scripts
git diff --check
```

If the exact named test file differs, run/report the equivalent focused test command.

Also print exact test counts.

Do not:

```text
trigger CI
rerun CI
poll CI
wait for CI
modify CI
```

Normal push is allowed.

If automatic Actions cannot run due quota:

```text
REMOTE_CI_DEFERRED_ACTIONS_QUOTA
```

Do not retry.

---

# 17. Git discipline

Implement directly on:

```text
v0.5-refactor
```

from exact starting SHA:

```text
e5ef1e1b76125527dc6c524e21aa294ea1504a15
```

Do not rewrite F01R3 history.

Do not modify docs branch.

Do not merge:

```text
forward-evidence-health-lightweight
```

No broad v0.6 refactor.

Commit and push.

Suggested commit:

```text
fix(execution): enforce research-disabled environment authority
```

---

# 18. Allowed completion state

If implementation and all local gates pass:

```text
EXECUTION_B04_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_REVIEW

RESEARCH_DISABLED_V1_IMPLEMENTED
OFFLINE_EXECUTION_ENVIRONMENT_AUTHORITY_V1_IMPLEMENTED
PLAN_ORDER_ENVIRONMENT_BINDING_IMPLEMENTED
SIGNED_CAPABILITY_FENCE_IMPLEMENTED

EXECUTION_DISABLED
LIVE_TRADING_NOT_AUTHORIZED
TESTNET_WRITES_NOT_AUTHORIZED
AUTO_EXECUTION_NOT_AUTHORIZED
```

Do NOT claim:

```text
LIVE_EXECUTION_SAFE
LIVE_TRADING_AUTHORIZED
TESTNET_EXECUTION_AUTHORIZED
POLICY_B_AUTHORIZED
ASTRA_PRE_DISCOVERY_PASS
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_ACCEPTED
H40_P3_AUTHORIZED
H40_DISCOVERY_AUTHORIZED
```

---

# 19. Final response format

Return exactly:

```text
starting_sha:
final_commit_sha:
changed_files:

execution_policy:
environment_authority_schema:
plan_binding:
order_receipt_binding:
signed_capability_fence:
paper_path_status:
legacy_plan_migration:

E01_E36_complete:
targeted_tests:
full_pytest:
ruff:
mypy:
compileall:
git_diff_check:

signed_network_calls_in_tests: 0
secrets_read_or_persisted: NO
live_authorized: NO
testnet_writes_authorized: NO
auto_execution_authorized: NO
ci_triggered_or_polled: NO

h40_hashes_unchanged: YES
f01r3_code_baseline_preserved: YES

final_state:
EXECUTION_B04_IMPLEMENTATION_COMPLETE_PENDING_INDEPENDENT_REVIEW
```

Recommended executor:

```text
Gemini 3.8 Flash
or
Codex GPT-5.6 Sol Medium/High
```

This Track B task is sufficiently bounded for Gemini 3.8 Flash, provided it follows this prompt exactly. Final independent review remains GPT-5.6 Sol.
