# V0.5.1 H40 Stage 2 — Repair A+B Cumulative Exact-SHA Validation and Evidence

## Executor

**Recommended executor: Gemini 3.8 Flash**

This is a long validation/evidence task, not an implementation task.

The implementation is already frozen for this stage. Do not modify production code or tests unless
the task discovers a genuine blocker; if a blocker is discovered, stop and report it instead of
repairing it in place.

The purpose of this stage is to bind one exact Repair A+B implementation SHA to exhaustive,
content-addressed validation evidence so that the independent Controller can then republish the
Lifecycle Implementation Authority.

The executor does **not** authorize itself and does **not** publish
`ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH`.

---

# Repository and exact frozen implementation

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

The exact implementation SHA under validation is:

```text
7413c96301067c59b9663d8eeaeddf975facb976
```

This SHA is immutable for Stage 2.

Before any validation:

```bash
git fetch origin
git checkout v0.5-refactor
git reset --hard 7413c96301067c59b9663d8eeaeddf975facb976
git status --short
git rev-parse HEAD
git ls-remote origin refs/heads/v0.5-refactor
```

Required:

```text
working tree clean
local HEAD = 7413c96301067c59b9663d8eeaeddf975facb976
remote v0.5-refactor = 7413c96301067c59b9663d8eeaeddf975facb976
```

If implementation branch drift exists, stop.

Do not commit any code/test change to `v0.5-refactor`.

---

# Controller acceptances already completed

Repair A F01/F04 independent acceptance:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_A_F01_F04_INDEPENDENT_ACCEPTANCE.md

commit =
890d1bc24da637295bc382be3de8e130227d6cfd
```

Repair B F02/F03 final independent acceptance:

```text
reviews/v0.5/V0.5.1_H40_REPAIR_B_FINAL_CLOSURE_INDEPENDENT_ACCEPTANCE.md

commit =
3b90010959827f1e1fbe14f3a439ea3e5181be8c
```

Repair Contract Freeze R2:

```text
cf7b1c9b9547f3e9e6dfab964d7d2c6d1bb35de3
```

Lifecycle V5 materialization:

```text
e1f368e85bbf1831763c688ca75ef4249971cf59
```

---

# Frozen identities to replay

Must reproduce exactly:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

discovery_provenance_contract =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

lifecycle semantic root V5 =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle governance authority V5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf
```

During validation the implementation must still contain:

```python
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

and:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

---

# Stage 2 goal

Produce one cumulative exact-SHA evidence package proving that the exact implementation:

```text
7413c96301067c59b9663d8eeaeddf975facb976
```

simultaneously preserves:

```text
Repair A:
F01 controller/run-grant/current-authority boundary
F04 service authority lifetime / stale authority rejection / synthetic isolation

Repair B:
F02 sealed source-byte economic provenance
F03 strict causal PIT prefit reconstruction
```

and passes the complete repository quality/test gates.

This stage must not compute real H40 Discovery results.

---

# Evidence directory and manifest

Use:

```text
evidence/v0.5/h40/ab_cumulative_7413c963/
```

Required manifest:

```text
evidence/v0.5/h40/V0.5.1_H40_AB_CUMULATIVE_EXACT_SHA_EVIDENCE_7413c963.json
```

All raw logs referenced by the manifest must be stored under the evidence directory above.

The final manifest itself must be canonical JSON compatible with repository
`canonical_json(..., sort_keys=True, allow_nan=False)`.

Compute:

```text
evidence_manifest_sha256 =
SHA256(exact UTF-8 bytes of the committed manifest file)
```

Report the exact digest.

The evidence package belongs on:

```text
v0.5-refactor-docs
```

not the implementation branch.

---

# Required provenance metadata

The manifest must include at minimum:

```text
schema_id
generated_at_utc
tested_commit_sha
implementation_remote_branch_sha
docs_authority_inputs
python_version
pytest_version
import_provenance
static_gate_results
focused_results
full_pytest_runs
identity_replay
repair_a_preservation
repair_b_preservation
execution_fence_result
protected_surface_attestation
raw_log_sha256
```

Exact:

```text
tested_commit_sha =
7413c96301067c59b9663d8eeaeddf975facb976
```

Do not use a later docs commit or working-tree SHA as the tested implementation SHA.

---

# Import provenance

Before testing, record:

```bash
python - <<'PY'
import sys
import btc_quant_agent
print(sys.executable)
print(btc_quant_agent.__file__)
PY
```

Evidence must prove tests imported the checked-out exact repository, not another installed package.

Record:

```text
python_executable
module_file
cwd
module path relative to cwd = true/false
```

If import provenance is foreign, stop.

---

# Static quality gates

Run and preserve raw logs for:

```bash
git diff --check
python -m compileall -q src
ruff check .
mypy src
```

If repository-standard mypy command differs, record the exact command.

Every gate must exit 0.

Do not hide warnings/errors with new ignores or code edits.

---

# Identity replay

Create a raw identity log that computes/replays from code rather than merely grepping constants.

At minimum independently obtain:

```text
compute_protocol_authority_hash()
compute_semantic_root_hash()
materialize_h40_search_space_production().structural_ledger_hash
compute_lifecycle_semantic_root_hash()
compute_lifecycle_governance_authority_hash()
DISCOVERY_PROVENANCE_CONTRACT_HASH
CURRENT_EXECUTION_POLICY
ACCEPTED_EXECUTION_WRITE_AUTHORITY
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH
```

Required exact results are the frozen identities listed above, with both H40 accepted authority
constants still null/None.

Also independently compare all nine Lifecycle V5 child hashes to the materialization evidence.

Manifest must record 9/9 exact child matches.

---

# Repair A cumulative focused validation

Run a concentrated lifecycle/F01/F04 suite covering at minimum:

```text
tests/test_v051_h40_repair_a_lifecycle_authority.py
tests/test_v051_h40_f01_lifecycle_authority.py
tests/test_v051_h40_f01r3r1_durable_authority.py
tests/test_v051_h40_f01r3r2_commit_authority.py
tests/test_v051_h40_f01r3r3_api_seal.py
tests/test_v051_h40_f01r3r4_lineage_durability.py
tests/test_v051_h40_implementation_authority_publication.py
```

The focused evidence must explicitly attest:

```text
synthetic controller registry unreachable from production constructor
synthetic verifier/resolver cannot cross into production
V3 Discovery receipt semantics intact
V2 persistence context semantics intact
V2 Discovery receipt rejected by production
V1 persistence context rejected by production
stale implementation anchor rejected
stale controller anchor rejected
cold restore revalidates current authority
service lifetime revalidation remains fail-closed
hot authority replacement does not silently mutate existing service authority
```

Do not monkeypatch committed production constants for the purpose of claiming production is
authorized. Existing tests may use isolated monkeypatch fixtures to exercise typed authority paths;
record that those are synthetic/test-only.

---

# Repair B cumulative focused validation

Run at minimum:

```text
tests/test_v051_h40_repair_b_provenance_pit.py
tests/test_v051_h40_p3b_production_discovery_verifier.py
tests/test_v051_h40_p3b_production_verifier_behavior_seal.py
tests/test_v051_h40_p3b_production_verifier_identity_seal.py
tests/test_v051_h40_p2r1_contract_materialization.py
tests/test_v051_h40_p2_search_space.py
```

The evidence must explicitly attest the following from passing tests/source review:

## F02

```text
H40_P3B_SOURCE_DERIVATION_V2 exact schema
whole sealed source file SHA binds full artifact
H40 support projection = [2021-01-01, 2023-02-01)
post-support physical rows excluded from economic digest, not from whole-file authority
lookback reserve excluded from decision denominator
active required source set bound to runtime seal
source record hash bound
source validation receipt hash bound
locator/product/cadence/timestamp_field bound
economic digest/count independently re-extracted
transported OHLC is not authority
unsupported adapter -> NOT_TESTABLE
V1 source-hours rejected in production
```

## F03

```text
H40_P3B_RAW_DECISIONS_V2 exact schema
transported regime/opportunity/raw_score/secondary_filter rejected
strict close_time < decision_t
D1 4h/12h exact
D2 24h/72h open-left + signal exclusion exact
D3 24h/72h immediate close-back exact
R_VOL 24 closes / 23 returns / ddof=1 exact
REGIME_VOL_LOW/MID/HIGH identity exact
O_RANGE standard True Range with mandatory prior close exact
O_NONE/O_WATCH/O_ELIGIBLE identity exact
empirical percentile tie/min-N behavior exact
unknown registered direction fails closed
current 18 REGISTERED roster only maps to explicitly supported contracts
prefit cache remains derivative-only and identity-bound
r_h/r_net independently reconstructed
complete base-universe coverage required
future observations cannot change earlier prefit
```

---

# Exact schema comparison against Repair Contract R2

Read:

```text
evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2.json
at commit cf7b1c9b...
```

Programmatically compare the current source/tested semantics to:

```text
source_derivation_v2_schema
raw_decisions_v2_schema
discovery_provenance_contract_preimage
```

At minimum record exact equality of the V2 field sets and the frozen provenance hash.

Do not infer from test names.

---

# Execution fence validation

Run the repository's execution-fence tests, at minimum:

```bash
pytest -q tests/test_execution.py tests/test_execution_environment_authority.py
```

or the current repository-equivalent exact paths.

Evidence must prove:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
signed network execution requests = 0
```

No trading/execution action is authorized.

---

# Protected-surface attestation

The validation process itself must not inspect protected outcomes.

Manifest must include booleans:

```json
{
  "real_h40_outcomes_accessed": false,
  "wf1_calibration_outcomes_accessed_for_authoring_or_selection": false,
  "confirmation_accessed": false,
  "h39_accessed": false,
  "final_holdout_accessed": false,
  "signed_network_requests": 0
}
```

Testing synthetic fixtures is not protected outcome access.

If a test unexpectedly requires protected real outcomes, stop.

---

# Full repository tests

Run the complete suite at least **two cold/separate times** from the exact same implementation SHA.

Preferred:

```bash
/usr/bin/time -v pytest -q --durations=50 --durations-min=0.25
/usr/bin/time -v pytest -q --durations=50 --durations-min=0.25
```

Use the repository virtualenv interpreter if present.

Record for each run:

```text
command
exit_code
passed_count
failed_count
warning_count
pytest_seconds
wall_clock_seconds
max_rss_kbytes if available
user_cpu_seconds if available
system_cpu_seconds if available
cpu_percent if available
top slow tests
```

Both complete runs must exit 0.

If one fails nondeterministically, investigate and BLOCK rather than averaging it away.

---

# Determinism / replay checks

In addition to the full suite, explicitly repeat at least:

```text
Lifecycle identity replay twice
source economic digest reconstruction twice
a representative Repair B prefit reconstruction twice
production search-space materialization twice
```

Identical input must reproduce identical identity/output.

No real Discovery run.

---

# Raw log hashing

Every persisted raw log must have SHA-256 recorded in:

```text
raw_log_sha256
```

Re-read each persisted log from disk before hashing.

No claimed command result may exist only in prose.

---

# Git / working-tree invariants

At the end of all implementation validation, verify again:

```bash
git status --short
git rev-parse HEAD
git diff 7413c96301067c59b9663d8eeaeddf975facb976 -- src tests
```

Required:

```text
implementation working tree clean
HEAD still 7413c96301067c59b9663d8eeaeddf975facb976
no src/tests changes
```

Evidence generation must happen on the docs/evidence branch/worktree, not by dirtying the frozen
implementation worktree.

---

# Evidence manifest self-hash rule

Do not put the manifest's own SHA-256 inside itself if that creates self-reference.

Instead:

1. materialize exact manifest content;
2. write manifest;
3. compute SHA-256 of exact committed file bytes;
4. report that digest in executor output;
5. Controller independently recomputes it from the remote committed file.

Raw log hashes remain inside the manifest.

---

# Docs commit / push

Commit only evidence/log artifacts to:

```text
v0.5-refactor-docs
```

Suggested commit message:

```text
evidence(h40): validate repair A+B exact implementation sha
```

Do not modify existing accepted reviews/contracts.

After push verify:

```bash
git rev-parse HEAD
git ls-remote origin refs/heads/v0.5-refactor-docs
```

Report exact docs evidence commit SHA and remote SHA.

Implementation remote branch must still be:

```text
7413c96301067c59b9663d8eeaeddf975facb976
```

---

# Do NOT publish implementation authority in this executor task

The following must still be true at task completion:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

Do not edit `lifecycle_authority.py` to publish a hash.

Do not create a controller authority.

Do not create a real Discovery run grant.

The independent Controller will:

```text
1. verify remote evidence bytes/hashes;
2. verify the exact tested implementation SHA;
3. issue cumulative exact-SHA Controller Acceptance;
4. materialize the new H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1 object/hash;
5. authorize a narrow publication commit;
6. verify publication;
7. only then hand the result to Astra for the F01-F04 pre-Discovery deep audit.
```

---

# BLOCK conditions

Block immediately if any of these occur:

```text
implementation branch drift
dirty src/tests worktree
static quality gate failure
focused test failure
full-suite failure
nondeterministic identity replay
Lifecycle V5 identity mismatch
scientific identity mismatch
Repair A regression
Repair B regression
execution fence regression
protected data requirement
foreign import provenance
evidence/log hash inconsistency
```

Formal block:

```text
AB_CUMULATIVE_EXACT_SHA_VALIDATION_BLOCKED
IMPLEMENTATION_AUTHORITY_NOT_REPUBLISHED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Do not fix the implementation inside this task.

---

# Required successful final state

```text
AB_CUMULATIVE_EXACT_SHA_VALIDATION_COMPLETE

TESTED_IMPLEMENTATION_SHA =
7413c96301067c59b9663d8eeaeddf975facb976

REPAIR_A_CUMULATIVE_VALIDATION = PASS
REPAIR_B_CUMULATIVE_VALIDATION = PASS

FULL_SUITE_RUN_1 = PASS
FULL_SUITE_RUN_2 = PASS
STATIC_GATES = PASS
EXECUTION_FENCES = PASS
IDENTITY_REPLAY = PASS
PROTECTED_SURFACES_ACCESSED = NO

EVIDENCE_MANIFEST_MATERIALIZED = YES
EVIDENCE_MANIFEST_SHA256 = <report exact digest>

IMPLEMENTATION_BRANCH_CHANGED = NO

ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

NEXT_STAGE =
CONTROLLER_EXACT_SHA_ACCEPTANCE_AND_IMPLEMENTATION_AUTHORITY_REPUBLICATION
```

---

# Final executor response

Return:

```text
decision:
tested_implementation_sha:
implementation_remote_sha:
implementation_worktree_clean:

docs_evidence_commit_sha:
docs_remote_sha:
evidence_manifest_path:
evidence_manifest_sha256:

static_gates:
focused_repair_a:
focused_repair_b:
execution_fences:
full_suite_run_1:
full_suite_run_2:
determinism_replay:

identity_replay:
protocol_authority_hash:
scientific_semantic_root:
structural_ledger_hash:
discovery_provenance_contract_hash:
lifecycle_semantic_root:
lifecycle_governance_authority_hash:
lifecycle_child_hashes_9_of_9:

accepted_lifecycle_implementation_authority_hash: None
accepted_h40_p3_controller_authority_hash: None
execution_policy: RESEARCH_DISABLED_V1
accepted_execution_write_authority: None

protected_surface_attestation:
implementation_branch_changed: NO

formal_final_state:
next_stage:
CONTROLLER_EXACT_SHA_ACCEPTANCE_AND_IMPLEMENTATION_AUTHORITY_REPUBLICATION
```
