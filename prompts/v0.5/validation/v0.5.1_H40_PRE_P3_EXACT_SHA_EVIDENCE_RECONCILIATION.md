# V0.5.1 H40 — Pre-P3 Exact-SHA Evidence Reconciliation

## Executor

Recommended: **Gemini 3.8 Flash**

This is a narrow evidence-reconciliation task.

Do not start H40 Discovery.  
Do not publish P3 Controller Authority.  
Do not publish lifecycle implementation authority.  
Do not access protected outcomes.

The primary target is to determine whether the current implementation SHA can be accepted as-is or
whether a final test-only Ruff cleanup commit is required.

---

# 1. Exact state

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Current implementation branch:

```text
v0.5-refactor
```

Current implementation SHA:

```text
f3678676265ba53e1d874e27bc0c922c7783acaa
```

Previous publication SHA:

```text
c31586c80130693c118c150a4e06c9573ff0053d
```

Rejected evidence commit:

```text
0920ab1b21a94c3e10e4028212335896b2fe5fcf
```

Controller blocker:

```text
reviews/v0.5/V0.5.1_H40_PRE_P3_CLOSURE_CONTROLLER_EVIDENCE_BLOCKER.md
```

Controller blocker commit:

```text
33058e9368a82fdc52fd88e6ea407a7c3aaaa596
```

Read the blocker in full before doing anything.

---

# 2. Known implementation state

The Controller independently reviewed the exact implementation diff.

Current provisional result:

```text
MED-01 implementation repair = structurally correct
MED-02 production behavior = unchanged and regression-covered
implementation authority = None
P3 controller authority = None
no production semantic blocker currently identified
```

Do not rewrite production H40 logic unless the Ruff reconciliation below forces only test/code-style
changes outside scientific semantics.

---

# 3. Evidence defects that must be reconciled

## E01

Committed:

```text
authority_stage_state.log
```

ends:

```text
TypeError: Object of type mappingproxy is not JSON serializable
exit_code: 1
```

while the manifest treated the authority stage as accepted evidence.

Fix the evidence script so the resulting raw log is valid JSON/text and exits 0.

Do not suppress the serialization error. Convert values into a serializable deterministic structure
before writing.

The successful authority-stage evidence must prove:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## E02

Committed:

```text
import_provenance.log
```

ends:

```text
AttributeError: module 'ruff' has no attribute '__version__'
exit_code: 1
```

Do not obtain Ruff version through `ruff.__version__`.

Capture tool provenance using executable commands, e.g.:

```bash
command -v python
python --version
command -v pytest
pytest --version
command -v ruff
ruff --version
command -v mypy
mypy --version
```

Also capture Python import locations for:

```text
btc_quant_agent
pyarrow
pytest
```

and the exact implementation worktree path/SHA.

Result must exit 0.

## E03/E04 — Ruff reconciliation

The accepted deep audit recorded:

```text
Ruff 0.16.6
10 diagnostics
```

at implementation:

```text
c31586c80130693c118c150a4e06c9573ff0053d
```

The new evidence claims:

```text
f3678676... => 0 diagnostics
```

but current provenance did not establish the Ruff version and the prior audit attributed diagnostics
to files not changed by the implementation diff.

This must be resolved empirically.

---

# 4. Create two isolated clean Ruff worktrees

Create clean worktrees or clean clones for:

```text
A = c31586c80130693c118c150a4e06c9573ff0053d
B = f3678676265ba53e1d874e27bc0c922c7783acaa
```

Do not modify either before the first comparison.

Use exactly the same Ruff executable:

```text
Ruff 0.16.6
```

If the environment does not currently provide exactly 0.16.6, use an isolated temporary tool
environment to run that exact version. Do not alter project dependency files merely to perform this
validation.

For both A and B capture:

```text
git rev-parse HEAD
git status --short
which/command -v ruff
ruff --version
sha256 of pyproject.toml
ruff check .
exact stdout/stderr
exit code
```

Persist separate logs:

```text
ruff_c31586c8_v0166.log
ruff_f3678676_v0166.log
```

Do not summarize before storing the raw outputs.

---

# 5. Branch decision

## PATH A — f367 is clean with Ruff 0.16.6

If:

```text
c31586c8 => prior diagnostics
f3678676 => 0 diagnostics
```

under the same Ruff 0.16.6 and same config identity:

1. explain exactly which changed files/lines account for every disappearing diagnostic;
2. do not modify implementation;
3. current implementation SHA remains:

```text
f3678676265ba53e1d874e27bc0c922c7783acaa
```

4. regenerate only the defective/missing evidence and manifest.

## PATH B — f367 still has Ruff diagnostics with Ruff 0.16.6

If any diagnostics remain:

1. record exact diagnostics first;
2. modify only the files/rules necessary to make Ruff 0.16.6 clean;
3. no production scientific/authority semantics may change;
4. keep:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

5. commit to `v0.5-refactor` with suggested message:

```text
test(h40): close pre-P3 lint evidence gap
```

6. push and freeze the resulting new exact SHA;
7. because implementation SHA changed, rerun all validation required below against the new SHA.

---

# 6. Validation required

Call the final implementation SHA:

```text
FINAL_IMPLEMENTATION_SHA
```

## Static

All must PASS under recorded tool versions:

```bash
git diff --check
python -m compileall -q src
ruff check .
mypy src
```

## Focused

Run the same 11-file focused H40 suite used in the prior package, including:

```text
MED-01 regression
MED-02 regression
authority revocation/publication tests
F01/F04 durability tests
production verifier tests
```

Expected no failures.

## Full suite

If PATH A and implementation remains exactly `f3678676...`:

- the already committed two full-suite logs may be retained **only after** verifying:
  - exact SHA provenance;
  - exit_code 0;
  - pass counts present;
  - their raw SHA-256 values match the manifest entries.

A fresh full-suite rerun is optional if all retained evidence validates.

If PATH B creates a new implementation SHA:

- run two new full-suite executions against the new SHA.

## Identity replay

Run two independent executions and persist separately:

```text
identity_replay_run_1.log
identity_replay_run_2.log
```

Both must independently report the same frozen identities.

Do not merely copy one output file to the other.

Expected:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

discovery_provenance_contract =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

lifecycle semantic root V5 =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle governance V5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf

all 9 child hashes exact
```

Current authority stage:

```text
implementation authority = None
P3 controller authority = None
execution write authority = None
execution policy = RESEARCH_DISABLED_V1
```

---

# 7. Raw-log integrity audit

Before creating the new manifest, inspect **every** raw log that the manifest will cite.

For each:

1. read exact bytes;
2. compute SHA-256;
3. parse/inspect the final exit code;
4. ensure the manifest's claimed status agrees with the raw log;
5. reject any traceback in an item claimed PASS;
6. reject any `exit_code != 0` in an item claimed PASS.

Generate an internal table:

```text
log_path | sha256 | exit_code | manifest_status | consistent
```

All rows must be consistent.

The manifest must not claim PASS if the underlying log failed.

---

# 8. Corrected evidence package

Do not overwrite historical rejected evidence.

Create a new package.

If final implementation is still `f3678676...`, use e.g.:

```text
evidence/v0.5/h40/pre_p3_closure_f3678676_r1/
evidence/v0.5/h40/V0.5.1_H40_PRE_P3_CLOSURE_EXACT_SHA_EVIDENCE_f3678676_R1.json
```

If a new implementation SHA exists, name paths using its short SHA.

Include at minimum:

```text
schema_id
supersedes_rejected_evidence_commit
controller_blocker_commit
tested_commit_sha
implementation_remote_branch_sha
tool_provenance
ruff_reconciliation
changed_files
med_01_closure
med_02_ratification_regression
authority_stage_state
static_gate_results
focused_results
full_pytest_runs
identity_replay_run_1
identity_replay_run_2
protected_surface_attestation
raw_log_integrity_table
raw_log_sha256
```

Explicitly record:

```text
rejected evidence commit =
0920ab1b21a94c3e10e4028212335896b2fe5fcf

controller blocker =
33058e9368a82fdc52fd88e6ea407a7c3aaaa596
```

After the final manifest is complete:

```text
EVIDENCE_MANIFEST_SHA256 =
SHA256(exact committed UTF-8 manifest bytes)
```

Do not include the manifest's own SHA inside its hash preimage.

---

# 9. Protected boundaries

Never access:

```text
real H40 Discovery outcomes
WF1 calibration outcomes for authoring/selection
Confirmation outcomes
H39 protected outcomes
Final Holdout
execution credentials/order interfaces
```

Keep:

```text
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
RESEARCH_DISABLED_V1
```

---

# 10. Docs commit

Commit corrected evidence only to:

```text
v0.5-refactor-docs
```

Suggested message:

```text
evidence(h40): reconcile pre-P3 exact-SHA evidence
```

Do not modify the Controller blocker.

Push and verify remote docs SHA.

---

# 11. Successful end state

```text
PRE_P3_EXACT_SHA_EVIDENCE_RECONCILED
PENDING_CONTROLLER_EXACT_SHA_ACCEPTANCE

RUFF_VERSION_RECONCILED = YES
IMPORT_PROVENANCE = PASS
AUTHORITY_STAGE_EVIDENCE = PASS
RAW_LOG_STATUS_CONSISTENCY = PASS

ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1
```

---

# 12. Required final response

Return:

```text
decision:

path_taken: A | B

start_implementation_sha:
final_implementation_sha:
implementation_remote_sha:

ruff_executable:
ruff_version:
pyproject_sha256:

ruff_c315_result:
ruff_c315_diagnostics:
ruff_f367_or_final_result:
ruff_f367_or_final_diagnostics:
ruff_reconciliation_explanation:

import_provenance:
authority_stage_evidence:
raw_log_status_consistency:

static_gates:
focused_tests:
full_suite_run_1:
full_suite_run_2:

identity_replay_run_1:
identity_replay_run_2:

accepted_lifecycle_implementation_authority_hash: None
accepted_h40_p3_controller_authority_hash: None
execution_policy: RESEARCH_DISABLED_V1

docs_evidence_commit_sha:
remote_docs_sha:
evidence_manifest_path:
evidence_manifest_sha256:

protected_surface_attestation:

formal_final_state:
next_stage:
CONTROLLER_EXACT_SHA_ACCEPTANCE_AND_REPAIRED_IMPLEMENTATION_AUTHORITY_REPUBLICATION
```
