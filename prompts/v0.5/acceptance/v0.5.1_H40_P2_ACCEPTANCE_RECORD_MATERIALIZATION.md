# H40 P2 Acceptance Record Materialization

## Repository

EXASHXE/btc-quant-agent

## Branches

Code branch to inspect/modify only where explicitly allowed:

```text
v0.5-refactor
```

Prompt/review authority branch:

```text
v0.5-refactor-docs
```

## Reviewed implementation baseline

```text
255148c49754b70365816106d97dc21fcd1955ce
```

State at review:

```text
H40_P2R1_IMPLEMENTATION_REPAIR_COMPLETE
H40_P2_PENDING_INDEPENDENT_ACCEPTANCE
H40_P3_NOT_AUTHORIZED
EXECUTION_DISABLED
```

## Objective

Materialize the independent acceptance record for H40 P2.

This task is acceptance-record materialization only. It must not redesign protocol semantics, change the frozen search space, alter ledger materialization, or authorize P3 implementation.

## Independent review conclusion to materialize

The independent review of commit
`255148c49754b70365816106d97dc21fcd1955ce`
found no blocking hidden degrees of freedom in P2 contract materialization and accepted the implementation subject to an auditable acceptance record.

Accepted immutable identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Current materialization:

```text
REGISTERED = 18
NOT_TESTABLE = 150
TOTAL = 168
```

## Required deliverable

Create an auditable acceptance artifact under:

```text
docs/h40/
```

The artifact must record at minimum:

- reviewed implementation SHA;
- protocol authority hash;
- semantic root hash;
- structural ledger hash;
- exact P2 acceptance decision;
- validation summary;
- known CI/Python-version evidence without overstating coverage;
- confirmation that no H40/protected outcomes were accessed;
- confirmation that execution remained disabled;
- remaining conditions before any P3 authorization.

Use an explicit filename such as:

```text
docs/h40/H40_P2_ACCEPTANCE_RECORD.md
```

## Hard constraints

Do not:

- enter or implement P3;
- modify H40 protocol definitions;
- modify feature semantics or registries;
- modify the 168-row configuration grid;
- modify search-space ordering;
- change source authority merely to increase testability;
- run discovery, backtest, walk-forward, confirmation, shadow, paper, testnet, or live execution;
- inspect or compute real labels / forward returns;
- inspect or compute realized MFE/MAE;
- inspect protected H39 outcomes;
- inspect confirmation outcomes;
- unseal or inspect Final Holdout;
- make an alpha claim.

Do not alter production code simply to make the acceptance document say PASS.

## Validation

Before committing the acceptance record:

1. Verify the reviewed commit exists and corresponds to P2R1 implementation repair.
2. Verify the accepted hashes are still represented by the implementation/oracles and were naturally computed rather than returned as hard-coded function results.
3. Verify the exact 168-row structural materialization remains intact.
4. Verify current runtime status remains 18 REGISTERED / 150 NOT_TESTABLE if the existing deterministic tests expose these counts.
5. Run the relevant H40 P2/P2R1 tests.
6. Run repository-required quality gates if this docs change participates in them.
7. Inspect the exact resulting code-branch HEAD and CI evidence.
8. Do not claim Python 3.11/3.12/3.13 all-green unless each version is actually evidenced.

If repository invariants require `prompts/` and `reviews/` to remain only on `v0.5-refactor-docs`, preserve that invariant. Do not copy this prompt onto the code branch.

## Expected acceptance state

Only if validation remains consistent with the independent review, record:

```text
H40_P2_ACCEPTED
H40_P3_NOT_AUTHORIZED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
NO_ALPHA_CLAIM
```

The acceptance record closes P2 acceptance. It does **not** itself authorize P3.

## Commit / push

Commit the acceptance artifact to the established repository branch dictated by the existing project workflow and R01 branch-surface invariant.

If the acceptance artifact belongs on `v0.5-refactor`, commit/push it there. Keep all task prompts/review artifacts on `v0.5-refactor-docs`.

Do not silently move prompt/review surfaces onto the code branch.

## Final report

Report:

```text
acceptance artifact path
exact final commit SHA
files changed
relevant H40 test results
ruff result if run
mypy result if run
full pytest result if run
exact CI run(s)
Python versions actually exercised
protocol_authority_hash
semantic_root_hash
structural_ledger_hash
REGISTERED/NOT_TESTABLE counts
confirmation: no protected/outcome data accessed
confirmation: execution remained disabled
```

If a blocker is found, do not claim `H40_P2_ACCEPTED`; report the blocker and leave P3 unauthorized.
