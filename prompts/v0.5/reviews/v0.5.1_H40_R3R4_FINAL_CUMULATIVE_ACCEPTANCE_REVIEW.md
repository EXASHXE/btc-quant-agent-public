# Task Prompt — H40 R3R4 Final Cumulative Independent Acceptance Review

## Role

Act as the independent final protocol reviewer for H40 before implementation authorization.

Your task is NOT to design new research rules. Your task is to determine whether the cumulative H40 protocol chain is sufficiently closed to authorize P2R1 implementation.

## Repository

Repository:

```text
EXASHXE/btc-quant-agent
```

Docs branch:

```text
v0.5-refactor-docs
```

Code branch is read-only:

```text
v0.5-refactor
HEAD = 52ac1dcb0843458b1b20b943be920f139dc832d1
```

## Review authority chain

Read and preserve:

- Frozen kernel
- BASE
- R1
- R2
- R2 Sol acceptance
- P1 accepted baseline
- P1 final acceptance
- R3
- R3R1
- Full pre-outcome system audit
- R3R2
- R3R3
- R3R4 Statistical Decision Final Closure

Do not reopen already accepted decisions unless a direct contradiction exists.

## Hard boundary

This is strictly pre-outcome review.

Do not access or use:

- labels
- forward returns
- MFE/MAE
- backtest results
- precision
- expectancy
- PF
- ECE/Brier outcomes
- bootstrap outcomes
- WF results
- confirmation outcomes
- H39 protected outcomes
- Final Holdout

If encountered, stop:

```text
H40_FINAL_ACCEPTANCE_OUTCOME_EXPOSURE_ABORT
```

## Review objective

Answer one question:

```text
Is H40_PROTOCOL_V1_R3 now implementation-authorized?
```

## Acceptance checklist

Verify all of the following:

### 1. Configuration determinism

Confirm every active configuration has a unique reconstruction path:

- direction owner
- horizon
- scope
- regime
- opportunity
- geometry
- calibration
- threshold
- feature semantics

No runtime choice may remain.

### 2. Direction/calibration separation

Confirm:

- raw_score determines proposed side;
- calibration only determines confidence;
- calibration cannot reverse LONG/SHORT ownership;
- Platt and isotonic are deterministic.

### 3. Statistical determinism

Confirm:

- ECE algorithm is fully specified;
- p0 contexts are frozen;
- discovery ranking universe is frozen;
- complexity tie-break is frozen;
- correction universe is deterministic.

### 4. Benchmark determinism

Confirm RANDOM_MATCHED fully specifies:

- universe
- strata
- sample count
- side matching
- RNG
- seed derivation
- replicate count
- percentile extraction

### 5. Authority separation

Confirm:

- structural protocol identity is separated from runtime authority snapshot;
- source promotion cannot silently mutate historical configuration identity;
- semantic hashes are reproducible.

### 6. Implementation authorization

Only if all above pass, create:

```text
reviews/v0.5/V0.5.1_H40_R3R4_SOL_FINAL_CUMULATIVE_ACCEPTANCE.md
```

The acceptance document must contain:

```text
H40_PROTOCOL_V1_R3_FINAL_ACCEPTED

protocol_authority_hash:
<value>

semantic_root_hash:
<value>

configuration_ledger_identity:
<value>

P2R1_AUTHORIZATION:
AUTHORIZED
```

## If rejected

Do not create broad redesign prompts.

Only report:

- blocker ID
- exact unresolved ambiguity
- why it changes reproducibility or authority

## Forbidden actions

Do not:

- modify code
- create runner
- add tests
- run backtest
- change 168 search space
- authorize P2R1 without acceptance

Commit only the review artifact if completed.
