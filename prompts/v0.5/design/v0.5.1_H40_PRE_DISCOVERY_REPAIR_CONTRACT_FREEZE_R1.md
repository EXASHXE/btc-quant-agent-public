# Agent Prompt — V0.5.1 H40 Pre-Discovery Repair Contract Freeze R1

## Executor

```text
GPT-6 Sol High
```

## Task type

```text
ARCHITECTURE / AUTHORITY / SCIENTIFIC-PROVENANCE CONTRACT FREEZE
NO IMPLEMENTATION CHANGES
NO TEST CHANGES
NO REAL OUTCOME ACCESS
```

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
```

Required implementation HEAD:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Docs branch:

```text
v0.5-refactor-docs
```

Required docs lineage must include:

```text
4f5786a25a5e25db32505a99445a3a42c62f7446
6f65db5fa7816a2fdbf340192b6a2a34847ae037
```

Astra audit:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_ASTRA_CUMULATIVE_AUDIT_R2.md
evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_ASTRA_AUDIT_R2.json
```

Controller triage:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_ASTRA_AUDIT_R2_CONTROLLER_TRIAGE.md
```

Current state:

```text
PRE_DISCOVERY_ASTRA_AUDIT_FAIL_ACCEPTED
FOUR_HIGH_FINDINGS_OPEN
REPAIR_CONTRACT_FREEZE_REQUIRED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

# 1. Mission

Freeze the exact repair contract for the four Astra HIGH findings:

```text
R2-F01 — controller authorization boundary missing
R2-F02 — source economic values not proven derived from sealed source bytes
R2-F03 — causal decision-time feature/score derivation not proven
R2-F04 — service implementation authority lifetime not fully enforced
```

This task must decide the exact schemas, identities, invariants, state-machine boundaries, producer/reconstruction rules, compatibility behavior, and required regression evidence BEFORE implementation begins.

Do not repair code in this task.

---

# 2. Non-negotiable preserved science

Do NOT change or reinterpret:

```text
candidate universe
registered/not-testable roster semantics
fit-invalid family correction
fixed K=6 Holm behavior
bootstrap seed identity
proxy net return formula
bootstrap block boundary
PCG64 start sampler
coverage population definition
retrospective WF1_CALIBRATION calibration semantics
Confirmation/H39/Final Holdout separation
execution-disabled policy
```

Current exact frozen scientific identities are evidence inputs, not automatically editable values:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

If a repair genuinely requires one of these identities to change, identify it explicitly and explain why.
Do not silently preserve or silently mutate it.

---

# 3. Authority identities currently published

```text
lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

accepted lifecycle implementation authority =
d3a304ddc6bcb7b7fc398ccf45a751a0afa3f91634a09ddff8e199af1970e440
```

The contract freeze must decide whether F01/F04 require:

```text
new lifecycle child contract only
new lifecycle semantic root
new lifecycle governance authority
new implementation authority only
or some exact combination
```

Do not guess.

Derive the answer from how the current lifecycle semantic root/governance hash is computed.

---

# 4. Required design principle

Prefer the smallest design that makes the missing invariant **locally verifiable**.

Do not solve a missing derivation by merely adding another label that asserts the derivation happened.

A new manifest/receipt/authority is useful only if either:

1. the verifier can independently recompute the relation; or
2. the producer itself is a separately accepted deterministic authority whose exact implementation/input/output identity is bound and replay-verifiable.

Prefer independent recomputation when practical.

---

# 5. R2-F01 contract — Controller P3/Discovery authorization

## Required invariant

No production Discovery receipt, production outcome-evidence read, or cold-restored Discovery authority may become authoritative without a separately accepted controller authorization capability.

Implementation acceptance is necessary but not sufficient.

## Design questions to resolve

Define whether the project should introduce a typed object such as conceptually:

```text
H40P3DiscoveryAuthorizationGrantV1
```

or reuse an existing typed authority if one already models this domain.

Do not create a duplicate domain if an existing object can be extended cleanly.

The frozen contract must specify exact canonical fields.

At minimum, assess binding to:

```text
schema_id
protocol_authority_hash
scientific_semantic_root
structural_ledger_hash
lifecycle_governance_authority_hash
accepted lifecycle implementation authority hash
accepted implementation baseline/tested identity if needed
runtime authority snapshot / run authority
source manifest hash
split manifest hash
split attestation hash
sealed registered roster hash
permitted lifecycle transition(s)
permitted partition(s)
execution_disabled = true
upstream controller acceptance identity
audit timestamp metadata
```

Do not automatically add time expiry.
If expiration is necessary, justify it as an authority rule.
If not, define stale/foreign rejection through bound identities rather than wall-clock expiration.

## Required enforcement points

Freeze whether the grant must be checked at:

```text
H40LifecycleAuthorityService.production
authorize_discovery
Discovery receipt construction
production Discovery verifier construction
first outcome-evidence load
revalidate_authorization
persist_authorization
cold restore
candidate lock predecessor verification
```

For each point state:

```text
REQUIRED
DERIVED_FROM_PREDECESSOR
NOT_REQUIRED
```

and why.

## Synthetic path

Synthetic/test authority must remain explicitly separate.

Define how synthetic tests construct synthetic grants without making them valid in production.

---

# 6. R2-F04 contract — Service authority lifetime

Freeze exact semantics for a production `H40LifecycleAuthorityService`.

Required questions:

1. Which fields are immutable after construction?
2. Which current published anchors must be independently rechecked at every authoritative operation?
3. Which checks are needed on persistence?
4. Which checks are needed on restore?
5. Does restore reconstruct a new service or validate an existing service?
6. How is the historical implementation authority rejected after republication?
7. What happens if a process remains alive across an implementation-authority republication?

Prefer a deterministic fail-closed rule.

For example, decide whether an already-created production service whose accepted hash no longer equals the module's currently published accepted hash must:

```text
fail all new authoritative operations
```

until recreated/revalidated.

Do not rely only on object mutability restrictions; current acceptance must be checked independently where required.

---

# 7. R2-F02 contract — Economic source-value derivation

## Required invariant

Every OHLC/value used by formal H40 science must be demonstrably derived from the exact source bytes whose content identity was accepted by source/runtime authority.

Two independently hashed objects are not sufficient unless the derivation relation is verified.

## Preferred minimal design evaluation

Evaluate first whether the verifier can deterministically reconstruct the required H40 source-hour economic values directly from:

```text
accepted source records
validated source artifact bytes
product/cadence/timestamp contract
```

and compare the reconstructed canonical result with the evidence object.

If practical, prefer this over introducing a trusted producer.

If independent reconstruction is impractical, define a typed deterministic extraction authority with exact implementation/input/output identity and cold replay semantics.

## Freeze exact extraction semantics

Specify:

- exact input artifact identity;
- file format/reader semantics;
- timestamp field;
- timezone normalization;
- bar/product/cadence identity;
- OHLC field mapping;
- numeric type/precision;
- duplicate behavior;
- ordering;
- missing-row behavior;
- canonical serialization;
- resulting content hash;
- relation to source_manifest_hash;
- relation to runtime source/split attestation;
- whether the source-hour evidence object changes schema.

Do not alter the accepted return formula.

## Lookback extension

The contract must account for F03's observation that the current source-hours lower bound may not include enough pre-training lookback to reconstruct earliest features.

Define whether source-value evidence must include:

```text
decision partition
plus exact required pre-decision lookback support
plus outcome horizon support
```

without changing the formal decision population.

---

# 8. R2-F03 contract — Causal feature / score provenance

## Required invariant

For every formal decision row, the prefit values:

```text
raw_score
regime_state
opportunity_state
secondary_filter_state
eligibility inputs
```

must be reproducible from accepted source data available strictly according to the frozen decision-time availability rule.

Later observations/outcomes must not alter an earlier prefit row.

## Contract requirements

Read the exact D1/D2/D3 active feature contracts and define:

- feature producer inputs;
- lookback window;
- closed-bar availability rule;
- decision timestamp semantics;
- boundary handling;
- warmup behavior;
- numeric precision;
- candidate structural configuration binding;
- source/split/run binding;
- feature contract identity;
- producer implementation identity if needed;
- feature/cache content hash;
- raw score derivation;
- state derivation;
- cache key and invalidation rules;
- deterministic replay rule.

Do not redesign the features.

The contract must implement the already accepted feature definitions.

## Required causality property

Freeze a formal property equivalent to:

```text
For a fixed candidate and decision timestamp t,
prefit_input(t) is a pure deterministic function only of the accepted
source observations whose availability time is < or <= the permitted
cutoff defined by the frozen feature contract.

Adding observations strictly after the cutoff cannot change prefit_input(t).
```

State the exact inequality from the existing feature contract; do not invent it.

## Retrospective calibration boundary

Explicitly preserve:

```text
prefit feature/score provenance = causal decision-time invariant

WF1_CALIBRATION calibration fit = accepted retrospective batch science
```

Do not conflate the two.

---

# 9. Evidence/schema inventory

Produce a complete inventory of current objects affected by the repair.

At minimum inspect:

```text
H40SourceManifest
H40SourceValidationReceipt
H40SplitManifest
H40RuntimeSourceSplitAttestation
H40RuntimeSnapshotSeal
H40RunAuthority
H40DiscoveryAuthorizationReceipt
H40DiscoveryEvidenceResolver
H40ProductionDiscoveryEvidenceVerifier
H40 P3B source-hours evidence
H40 raw-decision evidence
candidate/gate/metric/correction evidence
persisted lifecycle context/receipts
```

For each state:

```text
UNCHANGED
FIELD_ADDED
NEW_VERSION_REQUIRED
REPLACED_BY_NEW_OBJECT
```

Do not mutate V1 schemas in-place if their canonical identity contract requires versioning.

---

# 10. Identity-impact matrix

This is mandatory.

For every proposed repair change list whether it changes:

```text
protocol document identity
protocol_authority_hash
scientific_semantic_root
structural_ledger_hash
lifecycle child contract hash
lifecycle_semantic_root
lifecycle_governance_authority
implementation authority
source manifest identity
split identity
runtime seal identity
run authority identity
Discovery receipt identity
evidence schema/content identity
```

Use:

```text
UNCHANGED
MUST_CHANGE
CONDITIONALLY_CHANGES
NEW_SEPARATE_IDENTITY
```

with justification.

The repair cannot proceed to implementation until this matrix is unambiguous.

---

# 11. Migration / compatibility policy

Freeze how existing pre-Discovery artifacts are treated.

Because no real Discovery has been authorized, prefer fail-closed migration.

Determine explicitly whether existing artifacts created before the repair are:

```text
INVALID_FOR_PRODUCTION
SYNTHETIC_TEST_ONLY
REPLAYABLE_AFTER_REVALIDATION
or
UNCHANGED
```

Do not silently upgrade old authority/evidence objects.

Historical evidence must remain auditable.

---

# 12. Required regression plan

Define exact test concepts for all four HIGH findings.

Minimum set:

### F01
- production Discovery authorization without accepted controller grant refuses;
- outcome-evidence read without grant refuses before economic/outcome content is consumed;
- matching grant allows only the bound run/source/split/partitions;
- foreign/stale grant refuses;
- cold restore rechecks the grant.

### F04
- fresh service accepts current implementation authority;
- historical authority refuses;
- long-lived service rechecks current accepted anchor;
- persist/restore cannot turn stale implementation authority into current authority;
- synthetic service remains isolated.

### F02
- canonical synthetic raw source bytes deterministically reconstruct exact economic values;
- correct labels with non-derived values refuse;
- stale extraction/source pair refuses;
- cold replay reproduces same value identity.

### F03
- every active D1/D2/D3 prefit row is reproduced from causal source history;
- adding later observations does not change earlier prefit rows;
- lookback lower-bound/warmup behavior is exact;
- feature cache identity changes when causal inputs change;
- future horizon/outcome data is unavailable to prefit producer;
- retrospective calibration behavior remains unchanged.

Also specify required existing regression suites that must remain green.

---

# 13. Implementation staging recommendation

The contract must recommend one of:

```text
A. one atomic implementation commit
B. two implementation stages:
   1) F01/F04 authority-lifetime
   2) F02/F03 scientific provenance
C. another explicit sequence
```

Choose based on identity dependency, not convenience.

For each implementation stage specify:

- allowed source files;
- allowed test files;
- exact contract identities it must implement;
- whether implementation-authority republication is deferred until both stages finish;
- evidence required before the next stage.

Default preference is two stages unless the contract demonstrates atomic coupling is safer.

---

# 14. Do not authorize implementation yet

This task must end with:

```text
REPAIR_CONTRACT_FREEZE_READY_FOR_CONTROLLER_REVIEW
```

or:

```text
REPAIR_CONTRACT_FREEZE_BLOCKED
```

It must NOT say:

```text
IMPLEMENTATION_AUTHORIZED
H40_P3_AUTHORIZED
H40_DISCOVERY_AUTHORIZED
```

---

# 15. Deliverables

Create:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.md
```

Create machine-readable:

```text
evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.json
```

The JSON must contain:

- source audit finding commit;
- implementation SHA;
- four finding IDs;
- exact proposed schema objects and fields;
- canonical serialization rules;
- lifecycle enforcement-point matrix;
- source extraction contract;
- causal feature producer contract;
- schema inventory;
- identity-impact matrix;
- migration policy;
- regression plan;
- implementation staging recommendation;
- unresolved questions;
- final contract-freeze status.

Use stable deterministic JSON formatting.

---

# 16. Mandatory commit + push completion rule

The task is not complete locally.

After creating both deliverables:

```bash
git add   reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.md   evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.json

git commit -m "docs(v0.5.1): freeze pre-Discovery repair contracts R1"

git push origin v0.5-refactor-docs

git ls-remote origin refs/heads/v0.5-refactor-docs
```

Required:

```text
remote docs SHA == local docs commit SHA
```

If push is not verified:

```text
TASK_NOT_COMPLETE
DOCS_PUSH_NOT_VERIFIED
```

Do not claim completion.

Do not modify or push `v0.5-refactor`.

---

# 17. Final response format

```text
implementation_sha:
astra_audit_commit:
controller_triage_commit:

F01_contract:
F04_contract:
F02_contract:
F03_contract:

schema_versioning_decision:
identity_impact_summary:
migration_policy:
implementation_staging:

scientific_frozen_rules_changed: YES/NO
protocol_authority_hash_change_required:
scientific_semantic_root_change_required:
structural_ledger_hash_change_required:
lifecycle_semantic_root_change_required:
lifecycle_governance_authority_change_required:

unresolved_questions:

repair_contract_report_path:
repair_contract_json_path:

docs_commit_sha:
remote_docs_sha:
remote_push_verified: YES/NO

final_status:
```

---

## Recommended model

```text
GPT-6 Sol High
```

Reason:

```text
This is a design/authority adjudication stage, not mechanical implementation.
The hard part is deciding exact schema and identity consequences without
accidentally redefining frozen H40 science. GPT-6 Sol High is preferred for
cross-layer contract reasoning. Codex Sol High should implement only after
the controller accepts this frozen repair contract.
```
