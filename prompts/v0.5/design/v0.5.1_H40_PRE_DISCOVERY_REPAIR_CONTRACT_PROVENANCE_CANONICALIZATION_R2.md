# V0.5.1 H40 Pre-Discovery Repair Contract — Provenance Canonicalization R2

## Executor

**Recommended model: GPT-6 Sol High**

Reason: this is a docs-only authority/identity repair. The task is to resolve a canonical-hash
defect without reopening frozen scientific semantics, while preserving exact audit lineage and
fail-closed state. This requires authority reasoning and deterministic hash verification, not
implementation work. Do not use Astra for this stage; Astra remains reserved for the later
cumulative repair re-audit.

---

# Repository / exact starting state

Repository:

```text
https://github.com/EXASHXE/btc-quant-agent-public
```

Docs branch:

```text
v0.5-refactor-docs
```

Exact required docs starting SHA:

```text
72c944d4e9d2a8655f384b5d9ffd17e9996a7bd9
```

Implementation branch:

```text
v0.5-refactor
```

Exact implementation baseline:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Do **not** modify the implementation branch in this task.

Current controller blocker:

```text
reviews/v0.5/
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_BLOCKER.md

commit =
72c944d4e9d2a8655f384b5d9ffd17e9996a7bd9
```

Historical Repair Contract Freeze R1:

```text
reviews/v0.5/
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.md

report commit =
fd44362ad6dbf5d35b2d5e938f7c30beac6e19c0
```

Machine-readable R1:

```text
evidence/v0.5/h40/
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.json

docs commit containing final R1 report + JSON =
6dedefbd0776b36891cfaa553f3a27f23c604648
```

The previous Controller Acceptance + Lifecycle V5 materialization prompt is historical input only:

```text
prompts/v0.5/review/
v0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTANCE_AND_V5_HASH_MATERIALIZATION.md

commit =
018ec8cd6c58ed2c3effb95f60d972134453fb93
```

Do not execute its Phase 2 in this task.

---

# Current formal state

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_BLOCKED

BLOCKER =
DISCOVERY_PROVENANCE_CONTRACT_HASH_NOT_REPRODUCIBLE_FROM_R1_JSON

H40_LIFECYCLE_V5_HASHES_NOT_MATERIALIZED

REPAIR_A_IMPLEMENTATION_NOT_AUTHORIZED
REPAIR_B_IMPLEMENTATION_NOT_AUTHORIZED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

EXECUTION = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

This task may only repair the docs/evidence representation needed to make the repair contract
canonically reproducible.

---

# Mission

Create a **superseding Repair Contract Freeze R2** that fixes the canonical identity defect in
`H40_DISCOVERY_PROVENANCE_CONTRACT_V1`.

The goal is not to redesign F01-F04 and not to change scientific formulas.

The exact problem discovered by the controller is:

```text
R1 published provenance contract hash =
9a5a71101875829aff2959c93391648dbfd94ab26fd55ed31b720610fa7be81b

independent canonical recomputation of the provenance object represented by R1 JSON,
excluding the self-reported contract_hash =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

MATCH = NO
```

**Important:** `31fc3930...` is a diagnostic candidate only. It is **not** accepted authority.
Independently reconstruct the exact intended canonical preimage and compute the result yourself.

The R2 contract must make it impossible for a later reviewer/implementation to disagree about:

1. the exact preimage object;
2. which fields are and are not included;
3. canonical serialization;
4. UTF-8 encoding;
5. SHA-256 calculation;
6. the exact published provenance contract hash.

---

# Hard scope boundary

This is a **docs/evidence-only contract repair**.

Allowed:

- inspect committed docs/evidence/source code needed to reconstruct existing accepted identity rules;
- inspect the existing canonical JSON implementation;
- independently recompute hashes;
- create a superseding R2 review/contract document;
- create a superseding R2 machine-readable JSON;
- run docs/static/hash replay checks that do not touch protected outcomes;
- commit and push only to `v0.5-refactor-docs`.

Forbidden:

- no modification of `v0.5-refactor`;
- no Repair A implementation;
- no Repair B implementation;
- no Lifecycle V5 hash materialization;
- no controller acceptance artifact;
- no P3 authorization;
- no Discovery authorization;
- no real H40 outcome computation;
- no real WF1_CALIBRATION outcome computation;
- no candidate ranking;
- no Confirmation access;
- no H39 outcome access;
- no Final Holdout access;
- no signed exchange/network execution;
- no execution-authority changes;
- no change to the six frozen P3B closures;
- no scientific retuning.

If protected outcome access would be required:

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_R2_ABORT_PROTECTED_SURFACE
```

---

# Frozen scientific identities — MUST remain exact

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Also preserve:

```text
prior lifecycle semantic root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

prior lifecycle governance authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

R2 must **not** materialize replacements for the prior lifecycle root/governance authority.
That remains the next controller-review stage after R2 is independently accepted.

---

# Frozen R1 semantics that R2 must preserve

Unless a direct internal contradiction is demonstrated, preserve the R1 contract semantics for
all four HIGH findings.

## R2-F01 — controller authorization

Preserve the frozen schemas/concepts:

```text
H40_P3_CONTROLLER_AUTHORITY_V1
H40_DISCOVERY_RUN_GRANT_V1
H40_RECEIPT_DISCOVERY_AUTH_V3
H40_LIFECYCLE_PERSISTENCE_CONTEXT_V2
```

Preserve the rule that:

```text
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

until a later explicit P3 authorization publication.

Preserve production V2 Discovery receipt and V1 persistence-context rejection.

## R2-F04 — service authority lifetime

Preserve:

- production authority configuration is write-once after construction;
- every authoritative boundary rechecks current in-process accepted anchors;
- superseded long-lived services fail closed;
- persist/restore cannot revive historical authority;
- hot authority republication requires a new service/process identity;
- synthetic/test authority remains disjoint from production.

## R2-F02 — source-value provenance

Preserve:

```text
H40_SOURCE_VALUE_EXTRACTION_V1
H40_P3B_SOURCE_DERIVATION_V2
```

Preserve:

- economic OHLC is reconstructed from exact sealed source bytes;
- file SHA-256 and verified source receipt are authoritative;
- transported OHLC is not authoritative;
- timestamp field comes from verified receipt;
- finite IEEE-754 binary64 economic values;
- deterministic ordering/canonicalization;
- source support interval:
  [2021-01-01T00:00:00Z, 2023-02-01T00:00:00Z);
- pre-2021-01-31 support remains LOOKBACK_RESERVED only;
- missing accepted adapter => NOT_TESTABLE.

## R2-F03 — causal prefit provenance

Preserve:

```text
H40_CAUSAL_PREFIT_RECONSTRUCTION_V1
H40_P3B_RAW_DECISIONS_V2
```

Preserve:

- strict `close_time < decision_t`;
- verifier-derived `regime_state`;
- verifier-derived `opportunity_state`;
- verifier-derived `raw_score`;
- verifier-derived `secondary_filter_state`;
- transported prefit fields are not production authority;
- retrospective calibration semantics are unchanged;
- accepted R3R2 feature/formula semantics remain governing science.

Do not alter D1/D2/D3, R_VOL, O_RANGE, percentile/quantile, raw-score, calibration, threshold,
outcome, correction, selection, coverage, bootstrap or sampler semantics.

---

# Required canonical-hash repair

## 1. Determine the exact provenance preimage

Inspect:

- R1 MD;
- R1 JSON;
- controller blocker review;
- repository `canonical_json` / `canonical_sha256` implementation;
- analogous accepted content-addressed authority objects.

Freeze one exact machine-readable object:

```text
H40_DISCOVERY_PROVENANCE_CONTRACT_V1 canonical preimage
```

It must not depend on prose interpretation.

At minimum, explicitly adjudicate whether the intended preimage is exactly:

```json
{
  "schema_id": "H40_DISCOVERY_PROVENANCE_CONTRACT_V1",
  "source_value_extraction_contract": { ... },
  "prefit_reconstruction_contract": { ... }
}
```

or another exact field set already justified by the accepted R1 semantics.

Rules:

- the self-reported `contract_hash` must not silently be hashed into itself;
- no timestamp, file path, commit SHA, reviewer name, or other incidental publication metadata
  may enter the scientific provenance hash unless R1 already explicitly made it semantic;
- no new scientific choice may be introduced merely to obtain a preferred digest;
- field ordering must not be manually relied upon;
- arrays retain semantic order where R1 makes order meaningful.

If the only semantics-preserving interpretation is the R1 represented object with
`contract_hash` excluded, state that explicitly and independently confirm the resulting digest.
Do not copy the controller's diagnostic digest without recomputation.

If a different exact preimage is required, explain precisely which R1 clause establishes it and
why this is an identity representation correction rather than scientific retuning.

If no unique preimage can be justified without making a new scientific/governance choice, end:

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_R2_BLOCKED
```

and do not invent one.

---

# Canonical serialization — exact rule

Use repository canonical serialization exactly:

```text
canonical_json:
  sort_keys = true
  separators = (",", ":")
  ensure_ascii = false
  allow_nan = false

canonical_sha256(value):
  sha256(canonical_json(value).encode("utf-8")).hexdigest()
```

The R2 machine-readable artifact must publish both:

```text
discovery_provenance_contract_preimage
discovery_provenance_contract_hash
```

as separate fields.

Do not use a structure where the reader must infer whether
`discovery_provenance_contract_hash` participates in its own preimage.

---

# Mandatory independent replay checks

Before publishing R2, independently prove all of the following:

## A. SHA-256 implementation sanity

At minimum verify a standard SHA-256 test vector.

## B. Existing lifecycle identity sanity

Using the exact current nine-child canonical lifecycle objects at implementation SHA
`d63c1fbb...`, recompute:

```text
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e
```

and current V4 governance:

```text
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

Both must match exactly.

This protects against publishing a new R2 hash using an incompatible serializer.

## C. R2 provenance replay

Require:

```text
canonical_sha256(discovery_provenance_contract_preimage)
==
discovery_provenance_contract_hash
```

This must be true from the exact JSON bytes committed in R2.

## D. Human/JSON equality

The provenance hash shown in the R2 MD must equal the R2 JSON field exactly.

## E. Semantic preservation

Explicitly compare R1 and R2 nested F02/F03 contract semantics.

Any change other than canonical identity representation/clarification must be enumerated and
independently justified. The expected result is no scientific semantic change.

---

# Required R2 deliverables

Create new superseding artifacts; do **not** rewrite R1.

## Human-readable contract

```text
reviews/v0.5/
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2.md
```

It must include:

1. exact R1 lineage;
2. exact controller blocker lineage;
3. exact implementation baseline;
4. blocker root cause;
5. exact canonical provenance preimage;
6. exact newly computed provenance hash;
7. canonical serialization rule;
8. independent replay results;
9. F01-F04 semantic-preservation statement;
10. frozen-science identity statement;
11. lifecycle identity impact;
12. migration/supersession rule;
13. protected-surface attestation;
14. formal end state.

## Machine-readable contract

```text
evidence/v0.5/h40/
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2.json
```

It must contain at minimum:

```text
schema_id
supersedes_contract
r1_contract_docs_sha
controller_blocker_artifact_path
controller_blocker_commit_sha
implementation_baseline_sha

frozen_protocol_authority_hash
frozen_scientific_semantic_root_hash
frozen_structural_ledger_hash

finding_contracts

discovery_provenance_contract_preimage
discovery_provenance_contract_hash
canonical_hash_algorithm

provenance_replay_check
current_lifecycle_root_replay_check
current_lifecycle_governance_replay_check

semantic_change_summary
identity_impact_matrix
authorization_state
protected_surface_attestation
final_status
```

Recommended R2 schema identifier:

```text
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2_V1
```

The JSON must be strict valid JSON with no NaN/Infinity and no duplicate keys.

---

# Supersession / migration rule

R1 remains immutable historical evidence.

R2 must state:

```text
R1 = HISTORICAL_SUPERSEDED_FOR_FUTURE_REPAIR_CONTRACT_AUTHORITY

R2 = CURRENT_CONTRACT_CANDIDATE_PENDING_CONTROLLER_ACCEPTANCE
```

Do not pretend the R1 hash defect never existed.

Do not delete, rewrite or amend historical R1 artifacts.

The controller blocker review also remains historical evidence.

---

# Identity-impact requirement

Expected identity classification if the repair is canonicalization-only:

```text
protocol_authority_hash                UNCHANGED
scientific_semantic_root               UNCHANGED
structural_ledger_hash                 UNCHANGED

discovery_provenance_contract_hash     RE-MATERIALIZED / MUST BE EXPLICITLY ACCEPTED LATER

lifecycle child hashes                 NOT MATERIALIZED IN THIS TASK
lifecycle semantic root                NOT MATERIALIZED IN THIS TASK
lifecycle governance authority V5      NOT MATERIALIZED IN THIS TASK
lifecycle implementation authority     NOT PUBLISHED
```

Do not materialize Lifecycle V5 in R2.

The next independent controller stage will decide whether R2 is acceptable and, only after PASS,
perform the acceptance-commit-first / V5-materialization sequence.

---

# R3R2 sanitized-history note

The current public repository preserves the accepted R3R2 authority identifier:

```text
77601342ac9055b5c0bb47639d4c8ef7d83da154
```

inside the protocol authority chain, while that historical commit object is not resolvable in the
sanitized public repository history.

The R3R2 protocol document itself is present in the current docs lineage.

For this task:

- treat the identifier as an existing accepted opaque authority-chain value;
- do not rewrite it;
- do not attempt to create a replacement protocol authority;
- do not classify this portability detail as a new blocker unless you discover evidence that it
  changes the already-accepted protocol/scientific identity or prevents deterministic semantic
  verification required by this task.

---

# Acceptance criteria for R2 publication

Publish R2 only if all are true:

1. one exact provenance preimage is explicitly frozen;
2. its hash independently recomputes;
3. MD and JSON agree exactly;
4. current V4 lifecycle root/governance replay checks pass;
5. no scientific semantics changed;
6. F01-F04 frozen repair semantics remain complete;
7. R1 history remains immutable;
8. no protected outcomes were accessed;
9. implementation branch remains unchanged;
10. H40 P3/Discovery remain unauthorized;
11. no Lifecycle V5 hash is materialized;
12. the resulting docs commit is pushed and remotely verified.

If any of 1-10 fails, publish a docs-only R2 blocker instead of claiming READY.

---

# Required formal PASS state

If successful:

```text
REPAIR_CONTRACT_FREEZE_R2_READY_FOR_CONTROLLER_REVIEW

DISCOVERY_PROVENANCE_CANONICAL_PREIMAGE_FROZEN
DISCOVERY_PROVENANCE_CONTRACT_HASH_REPRODUCIBLE

R2_F01_CONTRACT_SEMANTICS_PRESERVED
R2_F02_CONTRACT_SEMANTICS_PRESERVED
R2_F03_CONTRACT_SEMANTICS_PRESERVED
R2_F04_CONTRACT_SEMANTICS_PRESERVED

SCIENTIFIC_FROZEN_RULES_CHANGED = NO
PROTOCOL_AUTHORITY_HASH_UNCHANGED
SCIENTIFIC_SEMANTIC_ROOT_UNCHANGED
STRUCTURAL_LEDGER_HASH_UNCHANGED

H40_LIFECYCLE_V5_HASHES_NOT_MATERIALIZED
REPAIR_A_IMPLEMENTATION_NOT_AUTHORIZED
REPAIR_B_IMPLEMENTATION_NOT_AUTHORIZED

IMPLEMENTATION_CHANGED = NO
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1

NEXT_STAGE =
INDEPENDENT_CONTROLLER_ACCEPTANCE_R2_THEN_LIFECYCLE_V5_HASH_MATERIALIZATION
```

Do not claim:

```text
REPAIR_CONTRACT_CONTROLLER_ACCEPTED
LIFECYCLE_V5_HASHES_MATERIALIZED
REPAIR_A_AUTHORIZED
REPAIR_A_IMPLEMENTED
REPAIR_B_IMPLEMENTED
H40_P3_AUTHORIZED
H40_DISCOVERY_AUTHORIZED
```

---

# BLOCK behavior

If the canonical preimage cannot be uniquely justified, or any scientific/governance ambiguity is
discovered, create a new docs-only blocker review describing:

- exact ambiguity;
- affected finding;
- competing preimages/semantics;
- identity impact;
- minimum docs-only resolution required.

End:

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_R2_BLOCKED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Do not proceed to Lifecycle V5.

---

# Commit / push / remote verification — mandatory

TASK COMPLETE only after remote push is verified.

Required sequence:

```bash
git status
git rev-parse HEAD

# verify starting docs SHA is exactly:
# 72c944d4e9d2a8655f384b5d9ffd17e9996a7bd9

# create R2 MD + JSON
# run deterministic hash/replay checks

git add reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2.md \
        evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2.json

git commit -m "docs(h40): canonicalize pre-discovery repair provenance contract r2"
git push origin v0.5-refactor-docs

git ls-remote origin refs/heads/v0.5-refactor-docs
```

Do not amend or rebase after publishing the reported final commit.

If remote branch SHA != local commit SHA:

```text
TASK_NOT_COMPLETE
REMOTE_PUSH_NOT_VERIFIED
```

---

# Final response required from executor

Return concise operational output:

```text
decision:
starting_docs_sha:
implementation_baseline_sha:
r2_contract_commit_sha:
remote_branch_sha:
remote_push_verified:
r2_report_path:
r2_json_path:
discovery_provenance_contract_hash:
provenance_replay_match:
current_lifecycle_root_replay_match:
current_lifecycle_governance_replay_match:
scientific_identity_changed: NO
implementation_changed: NO
lifecycle_v5_materialized: NO
H40_P3_AUTHORIZED: NO
H40_DISCOVERY_AUTHORIZED: NO
formal_final_state:
next_stage:
```
