# v0.4.3 — B13 Final Gate + Major Refactor Architecture Audit

Repository: `EXASHXE/btc-quant-agent`  
Branch: `v0.4`

## 0. Role and objective

Act as the final independent senior quantitative-research systems auditor and architecture reviewer for the repaired v0.4 research/economic kernel.

This task has two parts in **one review-only pass**:

1. perform a concise but independent **B13 final gate** on the repaired kernel;
2. spend the majority of the effort on a **fresh, repository-wide major refactor / simplification analysis and compressed implementation plan**.

This is **not an implementation task**. Do not modify production code, tests, configs, frozen protocols, research artifacts, H39 state, Final Holdout state, or execution settings. You may add only the requested review report when complete.

The B13 section should be short and decisive. Do not spend most of the audit repeating already closed A/B repair history unless new evidence contradicts it. The refactor analysis is the primary deliverable.

---

## 1. Exact repaired-kernel baseline and current branch relation

The repaired-kernel candidate baseline is:

```text
6710b6302bf224cdc178e6f3d817a95f79277f38
```

Exact-head CI associated with that kernel:

```text
GitHub Actions run: 34712576602
Python 3.11: SUCCESS
Python 3.12: SUCCESS
Python 3.13: SUCCESS
Reported full suite: 1108 passed, 73% coverage
```

Later commits are expected to be docs/prompt-only. At the start:

1. fetch `origin/v0.4`;
2. record exact current HEAD;
3. compare it with `6710b630...`;
4. verify that changes after the repaired kernel are docs/prompt/review only;
5. if production code, tests, config, frozen evidence or semantics changed after the kernel SHA, include them in the audit and do not silently reuse the baseline conclusion.

Known pre-final review posture entering this task:

```text
A_STAGE_GO
B01-B12_ACCEPTED
B1_CLOSED
B2_CLOSED
H1-H5_CLOSED
M1-M5_CLOSED
B13_GEMINI_PREAUDIT_PASS
B13_SOL_CROSS_REVIEW_PASS
P7_REPAIRED_KERNEL_PROVISIONAL_GO
P7_FINAL_GATE_PENDING_ASTRA
IMMUTABLE_CANDIDATE_BASELINE_SHA=6710b6302bf224cdc178e6f3d817a95f79277f38
```

Treat these as prior evidence, not as authority.

---

# PART A — concise B13 final gate

## 2. Purpose of B13 in this combined task

The goal is not to re-run every historical repair task line-by-line. The goal is to independently determine whether any remaining HIGH/BLOCKER invalidates freezing the repaired kernel before architectural convergence begins.

Return exactly one B13 verdict:

```text
GO
```

or

```text
REPAIR_REQUIRED
```

A `GO` means only that the research/economic qualification kernel is trustworthy enough to become the immutable baseline for further research-system development. It does **not** mean any alpha is profitable and does **not** authorize live/testnet/paper trading.

## 3. Minimum independent B13 verification

At minimum inspect the current integrated source and independently verify the following critical invariants.

### B1 — candidate economic replay

Confirm that a fully rehashed, internally coherent forged candidate cannot be promoted when execution differs from canonical replay against verified inputs. Pay particular attention to price, quantity, fee, funding, terminal state, empty-signal-with-trades, and registry atomicity.

### B2 — LIMIT causality

Confirm an unmarketable-at-open LIMIT whose OHLC bar later touches the limit cannot be backdated ahead of TIF expiry/funding when intrabar ordering is unsupported. Ambiguous timing must fail closed / be not testable.

### H1 — RANDOM_MATCHED reconstruction

Confirm acceptance reconstructs the full random experiment rather than trusting seed labels:

```text
opportunity preimage
-> set hash
-> master seed
-> per-trial seed
-> sample/tie ordering
-> directions
-> signals
-> replay bundle
-> canonical execution
-> distribution
```

### H2 — CASH reconstruction

Confirm CASH is independently reconstructed as a true no-trade/no-fee/no-funding/no-position baseline and a fully rehashed traded CASH cannot pass.

### H3/H4 — causal input boundaries

Confirm future completed-bar liquidity cannot affect an already-observed opening fill and incomplete/not-yet-available/`closed=False` market data cannot be promoted as formal COMPLETE evidence.

### H5 — H39 isolation

Confirm current readiness/diagnostic paths remain metadata-only where required, do not unblind or load prohibited outcomes, and do not access Final Holdout.

### M1-M5 — focused integrated review

Spot-check the integrated boundaries for:

- clock-domain separation and skew fail-closed behavior;
- durable publish + writer/finalizer ownership;
- explicit config failure with execution disabled by default;
- bounded coordinated REST retry/deadline behavior;
- SQLite schema compatibility and fail-closed unsupported versions.

Do not mechanically repeat every existing unit test. Inspect implementation and reproduce representative adversarial attack classes sufficient to independently assess end-to-end composition.

## 4. Cross-layer sanity check

Reason about or execute hermetic probes for at least these compositions:

1. evidence stripping + forged nonempty ledger;
2. RANDOM_BENCHMARK relabelled/synchronized into candidate role;
3. candidate producer/bundle/replay-context synchronized rewrite;
4. correct random draw + tampered execution;
5. fully rehashed candidate + benchmark + qualification substitution;
6. stale/competing finalizer around authority publication;
7. partial SQLite migration followed by active mutation/finalization.

If any new HIGH/BLOCKER is found, B13 must return `REPAIR_REQUIRED` and the later refactor plan must remain advisory only.

## 5. B13 output should be concise

The report's B13 section should contain only:

- exact audited kernel SHA/current HEAD relation;
- exact CI status;
- B1/B2/H1-H5/M1-M5 closure matrix with one-sentence evidence each;
- any new HIGH/BLOCKER;
- final verdict;
- immutable baseline declaration if and only if `GO`.

If `GO`, declare:

```text
B13_GO
P7_REPAIRED_KERNEL_GO
IMMUTABLE_KERNEL_BASELINE_SHA=6710b6302bf224cdc178e6f3d817a95f79277f38
```

Then continue to Part B.

---

# PART B — primary task: major refactor / simplification architecture audit

## 6. Re-evaluate the refactor from the repaired kernel, not the old plan

The historical documents:

- `reviews/v0.4/V0.4.2_V0.4.3_MAJOR_REFACTOR_BLUEPRINT.md`
- `reviews/v0.4/V0.4.2_V0.4.3_REFACTOR_TASKS.md`

were produced before B01-B12 completed. Treat them as hypotheses and inventory, not as a mandatory DAG.

Do **not** mechanically execute C01-F02.

Re-measure the actual current repository and design the smallest high-value convergence program appropriate to the repaired kernel and the user's real goal:

> a maintainable personal low-frequency BTC quantitative research system capable of rigorous alpha discovery, validation, Forward/Shadow testing, and eventually safe execution consideration — not an institution-scale generic platform.

Optimize for:

```text
scientific credibility
+ economic correctness
+ maintainability
+ iteration speed
```

not maximal framework generality.

## 7. Non-negotiable scientific contracts

The refactor may simplify implementation but must preserve or strengthen:

- PIT / causal data semantics;
- event/receipt/availability/decision time separation;
- B01 LIMIT ambiguity fail-closed behavior;
- B02 causal opening liquidity behavior;
- B03 closed/availability formal promotion boundary;
- B04 candidate/random producer authority;
- B05 canonical candidate execution replay;
- B06 random experiment reconstruction;
- B07 independent CASH reconstruction;
- P5/P6 canonical identity / evidence / append-only qualification semantics;
- preregistered hurdles, costs, seeds, trial budgets and statistical controls;
- durable publication, stale-writer/CAS and SQLite compatibility rules;
- H39 frozen/blind/consumed-state boundaries;
- historical negative/stopped/inconclusive research evidence;
- Final Holdout sealed;
- execution disabled by default.

Do not reduce LOC by deleting independent oracles/adversarial checks that are the only defense against self-consistent but false economic evidence.

## 8. Fresh measured repository inventory

Measure the **current** repaired repository. Do not reuse old counts without verification.

At minimum quantify:

1. active Python source files and physical LOC by subsystem;
2. top 20 largest source modules and oversized functions/classes;
3. active import graph / high fan-in semantic owners;
4. tests by subsystem, approximate node count and runtime concentration if discoverable;
5. CLI commands/subcommands;
6. API routes/services/background loops;
7. active collectors/timers/Forward/H39 processes;
8. research/hypothesis-specific runners still executable/imported;
9. historical-only runners still inside active package/runtime/CLI surface;
10. symbolic research/search/sandbox surface;
11. duplicate utility families: canonical serialization/hash, time validation, publication, WAL/SQLite/schema, manifests, gaps, retry/deadline, diagnostics;
12. duplicate economic semantics: fill, stops/TP/trailing, fees/slippage, funding, sizing, risk, ledger/PnL, terminal treatment, benchmark logic;
13. duplicate statistical/research utilities;
14. persisted stores/schema owners and compatibility obligations;
15. frozen protocol/evidence locations that constrain file/module moves.

Use concrete paths and numbers.

## 9. Classify every subsystem by future role

Use exactly these categories:

```text
A — CORE_ACTIVE
B — COMPATIBILITY_ONLY
C — CONVERGE_THEN_KEEP
D — ARCHIVE_OR_REMOVE_FROM_ACTIVE_SURFACE
```

For each meaningful subsystem/module family state:

- current purpose;
- current consumers;
- whether it owns a unique semantic contract;
- target owner after convergence;
- migration risk;
- compatibility requirement;
- archive/delete condition.

A Git SHA alone is not sufficient if reconstructing an old experiment requires dependencies/config/data-schema interpretation. Define a minimal archive manifest where necessary.

## 10. Explicitly answer the architectural question: how much should be refactored?

Do not assume “more refactor = better”. Compare at least these strategies:

1. minimal cleanup only;
2. bounded semantic convergence + archival;
3. broad modular reorganization;
4. partial subsystem rewrite;
5. repository-wide rewrite.

For each discuss:

- scientific regression risk;
- active LOC/entrypoint reduction;
- test burden;
- historical reproducibility;
- future alpha iteration speed;
- migration complexity.

Choose one and justify it.

Default preference should be:

```text
freeze repaired semantics
-> converge duplicate active authority
-> cut over consumers
-> archive obsolete executable research paths
-> simplify application/diagnostic surface
```

A clean-slate rewrite should be rejected unless there is unusually strong evidence incremental convergence is more dangerous.

## 11. Semantic convergence map

Build a precise before/after map for at least:

- fills / LIMIT / stops / TP / trailing;
- fees / slippage / market impact;
- funding;
- position sizing / risk / exposure;
- Portfolio / TradeEvent / PnL / terminal handling;
- candidate execution replay;
- CASH/PASSIVE/RANDOM benchmarks;
- metrics/qualification;
- PIT market-data contracts;
- evidence/registry identities;
- durable publication / storage finalization;
- clock/retry/network boundaries;
- H39/Forward status/diagnostic ownership;
- research hypothesis orchestration.

For each identify:

```text
canonical owner to keep
legacy/duplicate owners to demote
consumer cutover sequence
required goldens/adversarial tests
historical compatibility boundary
```

The goal is **one active economic meaning per formal concept**, while preserving independent verification where scientifically useful.

## 12. Historical research archival analysis

Review the existing historical runners and decide what should leave the active package/runtime surface.

At minimum assess:

- old v0.3 research runners;
- symbolic alpha search/evaluation/sandbox;
- legacy `backtest.py` / `shadow.py` paths;
- rejected directional scoring paths;
- old funnel/mechanism diagnostics;
- old H39 report/rendering utilities versus currently authoritative collector/ledger/evaluator paths.

For each archival family specify:

- exact evidence/protocol/deliverable roots preserving the historical conclusion;
- shared helpers that must be extracted first;
- compatibility reader/replay requirement;
- whether source should live in an archive package, tag, compatibility directory, or be removed from normal installation;
- how to prevent archived paths from becoming accidental future alpha authority.

Negative/inconclusive/stopped results are evidence. Do not relabel or silently regenerate them under the new engine.

## 13. Application-surface simplification

Analyze whether CLI/API/service/diagnostics have grown beyond the needs of the project.

Recommend a minimal application boundary around, where justified:

```text
collect
validate/status
research/replay
qualify
forward/shadow
execution-plan (disabled)
```

Identify aliases, historical commands, redundant status surfaces, duplicate read models and optional APIs that can be retired or made compatibility-only.

Do not remove operational observability needed to detect stale data, gaps, corrupted stores, disabled execution state or evidence-integrity failures.

## 14. Test architecture and feedback-loop reduction

The project now has a large test suite. Design a faster structure without weakening semantic coverage.

Separate conceptually:

```text
Tier 0: fast semantic/golden/adversarial kernel tests
Tier 1: subsystem/integration tests
Tier 2: archive compatibility tests
Tier 3: full exact-head CI matrix
```

Analyze:

- duplicated fixture construction;
- redundant test coverage versus genuinely independent oracles;
- slow historical runner tests that may move to compatibility CI;
- safe parallelization/caching opportunities;
- which tests must remain mandatory on every kernel change;
- which can run on archive-specific changes only.

Do not weaken B01-B12 adversarial coverage merely to reduce runtime.

## 15. Target architecture

Produce a concrete proposed target package/module tree.

It should make semantic ownership obvious. A possible shape may involve concepts such as:

```text
contracts/
data/
economics/
qualification/
research/
forward/
diagnostics/
execution/   # disabled boundary
compat/
```

but do not force this naming if current code suggests a better boundary.

For every target package identify:

- owned contracts;
- allowed dependency direction;
- forbidden dependency direction;
- persistence ownership;
- compatibility responsibilities.

Avoid namespace churn with no semantic reduction.

## 16. Quantified simplification target

Estimate, based on the measured current repository:

- active source LOC before/after;
- active source file count before/after;
- active research runner count before/after;
- active CLI command count before/after;
- API surface before/after;
- number of independent economic semantic implementations before/after;
- default test runtime / mandatory-test count before/after where estimable.

A 20%-30% active-maintenance reduction is a useful reference, not a mandatory quota. Prefer a smaller safe reduction over deleting scientific controls to hit a number.

## 17. Compressed implementation plan

Do not return dozens of tiny tasks.

Prefer **2 major implementation rounds**, with a third only if a real dependency makes two unsafe.

Expected conceptual split:

### Round C-lite — active semantic convergence

Likely themes:

- one canonical economic/replay authority;
- cut over new backtest/shadow/research consumers;
- remove duplicate economic authority from legacy modules;
- consolidate shared contracts/utilities where it reduces actual coupling;
- preserve compatibility adapters.

### Round D-lite — archival + application/test convergence

Likely themes:

- archive historical runners and symbolic search surfaces;
- reduce CLI/API/diagnostic duplication;
- separate compatibility CI from active-kernel feedback loop;
- remove obsolete default-runtime entrypoints.

But independently choose the scope from repository evidence.

For each round specify:

- objective;
- exact included/excluded module families;
- migration sequence;
- expected changed paths;
- authoritative semantic owner after cutover;
- compatibility strategy;
- mandatory characterization/golden/adversarial tests;
- full CI requirements;
- expected LOC/file/entrypoint reduction;
- rollback boundary;
- stop/go acceptance criteria;
- implementation model recommendation;
- independent review model recommendation.

Every implementation round must end at an exact report-inclusive HEAD with Python 3.11/3.12/3.13 CI green and independent review before the next round.

## 18. Decide when alpha research should resume

This is a key output.

Choose exactly one:

```text
A. resume alpha research immediately after B13 GO; refactor can proceed in parallel
B. require C-lite first; D-lite may proceed later
C. require C-lite + D-lite before new alpha research
```

Justify based on **scientific coupling and research iteration cost**, not code aesthetics.

Consider the user's actual next scientific objective:

```text
reliable PIT sample
-> explicit hypothesis
-> incremental information test
-> executable rule
-> after-cost expectancy
-> walk-forward / shadow
```

Do not authorize alpha tuning in this review task itself.

## 19. Explicit non-goals

Do not:

- search/tune alpha;
- unblind H39;
- access Final Holdout;
- change economic hurdles/costs/seeds/trial budgets;
- enable live/testnet/paper execution;
- redesign the repaired kernel solely for naming/style;
- create a generic distributed institutional platform;
- delete historical evidence to make the repository look smaller;
- introduce a second formal economic simulator;
- weaken fail-closed semantics for backward compatibility.

---

# PART C — required report

## 20. Create one combined review document

Create:

```text
reviews/v0.4/V0.4.3_B13_FINAL_AND_MAJOR_REFACTOR_ARCHITECTURE_AUDIT.md
```

The document should be weighted approximately:

```text
10-20%  B13 final gate
80-90%  major refactor / simplification analysis
```

Required sections:

1. Executive verdict.
2. Exact HEAD / audited-kernel relationship and CI.
3. Concise B13 closure matrix and `GO` / `REPAIR_REQUIRED`.
4. Immutable baseline declaration if `GO`.
5. Fresh measured repository inventory.
6. A/B/C/D subsystem classification.
7. Major architectural findings and current sources of complexity.
8. Semantic convergence map.
9. Historical research archival map.
10. Persistence/frozen-evidence migration boundaries.
11. Target architecture/package tree and dependency rules.
12. CLI/API/service/diagnostics simplification.
13. Test/CI architecture simplification.
14. Quantified before/after maintenance-surface estimate.
15. 2-3 round compressed implementation plan.
16. Risks, rollback boundaries and things that must not be simplified.
17. Decision on when alpha research resumes.
18. Model recommendation per implementation/review round.

If B13 is `REPAIR_REQUIRED`, put the repair finding first, do not declare an immutable baseline, and label all refactor recommendations provisional.

If B13 is `GO`, the report should clearly distinguish:

```text
kernel is trustworthy enough for research
!=
strategy is economically qualified
!=
live execution is authorized
```

## 21. Model / execution guidance

This task is intended for **GPT-6 Astra with high/maximum reasoning**.

Use mechanical scripts only for inventory/LOC/import/test counting. Architectural cut lines, authority boundaries, scientific-risk assessment and final recommendations must be reasoned independently from repository source and evidence.

Do not implement the refactor in this pass.
