# BTC Quant Agent v0.4.2 — A-Stage Astra Targeted Review

## 0. Role, repository and exact baseline

Act as an **independent senior quantitative-research systems auditor**. Your job is to adversarially review whether the completed v0.4.2 **A-stage preparation work** is trustworthy enough to begin the bounded Phase-B production correctness repairs.

Repository:

```text
EXASHXE/btc-quant-agent
```

Branch:

```text
v0.4
```

Semantic baseline exact HEAD immediately before this review prompt was committed:

```text
979190cfceb94f242e10ebb6da4cb41e30a4c3e5
```

That baseline already has a green exact-head CI on Python 3.11 / 3.12 / 3.13.

Before reviewing, fetch `origin/v0.4` and inspect the **actual latest exact HEAD**. The latest HEAD will include this review-prompt commit and therefore differ from the semantic baseline above. Treat a prompt/report-only commit as documentation, not a kernel semantic change. If unrelated implementation changes have appeared after the baseline, explicitly identify them and determine whether they invalidate the scope of this review.

This is a **review-only task**. Do not repair production code, tests, protocols, goldens, registries, or persistence logic while reviewing. You may create only the requested review deliverable after the analysis is complete.

---

# 1. Narrow objective

This is **not a repeat of the full P7 audit**.

The prior P7 Astra audit remains authoritative for the discovery of the still-open production defects B1, B2, H1, H2, H3 and H4. H5 was subsequently addressed in A03.

Your primary question is only:

> Are A01, A02, A03, A04 and A04R sufficiently correct, safe, independently justified and internally consistent to serve as the preparation/semantic baseline for implementing B01–B07 and the later Phase-B repaired-kernel gate?

Return exactly one top-level A-stage verdict:

```text
A_STAGE_GO
```

or

```text
A_STAGE_REPAIR_REQUIRED
```

`A_STAGE_GO` means only that it is safe to start Phase-B repairs. It does **not** mean P7 GO, P6 qualification acceptance, strategy profitability, H39 readiness, H39 unblind authorization, or trading authorization.

Do not demand that B1/B2/H1/H2/H3/H4 already be fixed. They are intentionally still open and assigned to B01–B07. The review must distinguish:

1. an **A-stage defect that must be repaired before B01 begins**, versus
2. an **already-known production defect correctly characterized and deferred to its B task**.

This distinction is essential.

---

# 2. Current scientific and safety posture that must remain unchanged

The project remains a research system, not a live trading system.

Assume and verify where relevant:

- no qualified actionable direction engine is established;
- execution remains disabled;
- no live or testnet trading is authorized;
- Final Holdout remains sealed;
- H39 remains governed by its existing frozen protocol and clarifications;
- no H39 unblind may occur during this review;
- no alpha search or parameter tuning may occur during this review;
- historical failed/stopped research remains evidence and must remain auditable;
- `OPPORTUNITY_ONLY` is not a direction engine;
- A-stage work must not rewrite historical results under corrected future semantics.

Do not infer alpha from engineering quality.

---

# 3. Prior P7 defects — context only, not A-stage completion requirements

The prior P7 stage-final audit found these still-open production defects:

## B1 — candidate execution provenance replay

A self-consistent candidate ledger can be rehashed even when the execution path is impossible under the declared signals, policy, model and market data. The canonical attack changes a candidate entry to approximately `1.0` while the candle range is around `99.5–100.5`, then rebuilds coherent accounting and hashes.

Future owners:

```text
B04 → versioned verified replay-input bundle
B05 → candidate canonical execution replay at acceptance
```

## B2 — LIMIT observable-time / expiry ambiguity

A full-bar OHLC low/high can currently be used to backdate a LIMIT touch before a material TIF/funding boundary, e.g. BUY LIMIT 95 submitted at bar open with a 1 ms expiry on a 15-minute bar whose eventual low is 90.

Future owner:

```text
B01
```

## H1 — random benchmark draw replay

Seeds/identities are insufficient if the eligible-opportunity preimage, sampled opportunities, directions/signals and resulting executions are not reconstructed.

Future owner:

```text
B06
```

## H2 — CASH reconstruction

A benchmark labeled as CASH must be independently reconstructed as the exact no-trade policy rather than accepting a coherent traded ledger.

Future owner:

```text
B07
```

## H3 — opening-fill future-volume leakage

An opening fill cannot depend on completed-bar volume accumulated after the fill timestamp.

Future owner:

```text
B02
```

## H4 — formal candle closed / PIT availability binding

Formal replay must preserve and verify instrument, interval, closed state and original information availability rather than silently reconstructing incomplete old candle payloads as complete.

Future owner:

```text
B03
```

## H5 — deprecated H39 status path constructing outcomes

This was the A03 repair target. A major objective of this review is to independently decide whether H5 is actually closed without altering the authoritative frozen H39 readiness/unblind path.

---

# 4. A-stage artifacts to inspect

Do not rely on implementation reports as proof. Inspect the actual source, tests, git diffs and relevant historical commits.

At minimum inspect:

## A01

```text
reviews/v0.4/V0.4.2_A01_SURFACE_CATALOG_REPORT.md
reviews/v0.4/V0.4.2_A01_SURFACE_CATALOG.json
tools/catalog_surfaces.py
```

A01 implementation commit:

```text
c12037669d8560baa04b7517f8187d115eb2f608
```

## A02

```text
reviews/v0.4/V0.4.2_A02_HERMETIC_TEST_ROOTS_REPORT.md
tests/conftest.py
tests/test_hermetic_runtime_guard.py
```

Also inspect A02-related changes to Forward/H39 tests and the narrow path/clock injection seams in production code.

A02 implementation baseline recorded by its report:

```text
bc7453bee164bfec980e42be20cbb26d19ce361c
```

## A03

```text
reviews/v0.4/V0.4.2_A03_H39_METADATA_DIAGNOSTIC_REPAIR_REPORT.md
src/btc_quant_agent/microstructure_research.py
tests/test_v0323_blind_validation.py
tests/test_v0325_one_shot_unblind_preregistration.py
```

A03 implementation commit:

```text
001472aa6fd6fea58989dde576d2ccaa079284f1
```

## A04 / A04R

```text
tests/goldens/v042_semantic_goldens.json
tests/helpers_v042_semantic_goldens.py
tests/test_v042_a04_semantic_goldens.py
reviews/v0.4/V0.4.2_A04_SEMANTIC_GOLDENS_REPORT.md
reviews/v0.4/V0.4.2_A04R_SEMANTIC_GOLDEN_ACCEPTANCE_REPAIR_REPORT.md
```

A04 implementation commit:

```text
eef01978dea10121e96382a0d6329eedf0259f27
```

A04R implementation commit:

```text
b0aadb8718f53a7238cfa2724498c7bc8dea1fc6
```

A04/A04R accepted semantic baseline before this prompt:

```text
979190cfceb94f242e10ebb6da4cb41e30a4c3e5
```

Also compare with the original P7 findings/report and the v0.4.2 refactor task plan where useful, but do not turn this into another full repository architecture audit.

---

# 5. Review A01 — surface catalog and future archival safety

Independently determine whether A01 is adequate as a **planning catalog**, not as permanent immutable truth.

Verify at minimum:

1. The catalog actually enumerates the current source surfaces in a reproducible way.
2. Its reported module/file/LOC, CLI/API and dependency inventory is plausible by spot-checking the implementation and repository rather than trusting the report.
3. `ARCHIVE_CANDIDATE` means candidate only; it must not authorize immediate deletion.
4. Active dependency blockers are materially represented, especially dependencies such as:
   - `data/funding.py` depending on legacy `backtest.FundingEvent`;
   - shared `IndexedOneMinuteSeries` / bootstrap helpers;
   - `_spearman` / shared statistics;
   - `movement_label`;
   - `opportunity_union` / resampling helpers;
   - historical regression tests importing old runner modules.
5. Frozen protocols, evidence, failed-study outputs, historical deliverables and replay-relevant artifacts are not classified in a way that would permit destructive migration.
6. The catalog records its source snapshot/provenance. A future D02/D03 archival task must refresh the dependency graph rather than assuming this A01 snapshot is eternally current.
7. Tooling must not read real operational data merely to inventory paths.

Do not fail A01 merely because the repository changes after its snapshot. Fail it if its semantic classification or dependency representation is unsafe for later convergence.

Return an explicit A01 verdict:

```text
A01_READY | A01_REPAIR_REQUIRED
```

---

# 6. Review A02 — hermetic test roots and forbidden-access guard

This is a safety-critical review because all later Phase-B tests will rely on this boundary.

Independently inspect `tests/conftest.py`, affected tests and the narrow constructor/function injection changes.

Attack at least these areas:

## 6.1 Operational filesystem isolation

Verify ordinary repository code paths exercised by pytest cannot accidentally access:

```text
data/forward/**
data/research/h39_validation/**
var/quant.db
Final Holdout
```

Check path canonicalization, relative paths, symlink resolution and SQLite `file:` URIs where applicable.

The guard is **not required to be an OS-level security sandbox**. A theoretical malicious test using raw syscalls is not by itself an A02 failure. The relevant question is whether normal project filesystem/SQLite paths can accidentally cross the boundary.

If you discover a normal project I/O route that bypasses the guard and can reach the protected stores, treat that as a material finding.

## 6.2 Network and execution isolation

Verify unmocked external exchange/network access and Binance live/testnet signed execution cannot occur during tests through the actual clients used by the repository.

Do not require the tests to prevent every imaginable Python networking library if the repository does not use it. Do require the actual current network/transport surfaces to be guarded.

## 6.3 H39 outcome access spy

Verify that the opt-in H39 spy really fails if metadata-only H39 tests attempt to call the forbidden outcome/future-candle/observation-building paths.

At minimum inspect protection around:

```text
load_outcomes_from_opportunity_shadow
get_canonical_1m_candles
build_observations_for_partition
```

Check that relevant tests call the real metadata/status entrypoint with the spy installed; a self-test of the spy alone is insufficient.

## 6.4 Test semantic preservation

Verify A02's large runtime reduction came from isolation/path injection rather than:

- deleting tests;
- blanket skipping/xfail;
- weakening maturity/statistical requirements;
- shrinking samples or protocol thresholds;
- bypassing meaningful assertions.

Check that production defaults remain unchanged and only explicit injection seams were added where intended.

Return:

```text
A02_READY | A02_REPAIR_REQUIRED
```

---

# 7. Review A03 — prove H5 is really closed

Trace `H39ResearchEngine.evaluate_validation_status()` from the current source.

The deprecated diagnostic must be **metadata-only**.

Prove or falsify that it no longer invokes, directly or indirectly:

- future-return loaders;
- `H39OutcomeRow` construction;
- canonical future-candle retrieval;
- `build_observations_for_partition`;
- formal statistical evaluation/ranking;
- one-shot/unblind authorization logic.

A metadata-only implementation may inspect safe partition metadata such as min/max event time.

Verify the returned payload remains explicitly non-authoritative and fail-closed. It is acceptable for quantities that cannot safely be known without outcome/eligibility traversal to be `None`/unknown and for maturity to remain false.

Independently verify:

- `candidate_promotion_allowed` remains false;
- execution remains disabled;
- the deprecated diagnostic cannot authorize unblind;
- the actual authoritative readiness route remains `check_unblind_readiness()` or the currently frozen equivalent;
- frozen H39 protocol/clarification bytes and statistical definitions were not modified as part of A03.

Do not interpret the deprecated diagnostic becoming less informative as a regression if that information previously required unsafe outcome construction.

Return:

```text
A03_H5_CLOSED | A03_H5_REPAIR_REQUIRED
```

---

# 8. Review A04/A04R — semantic goldens must be independent truth, not snapshots

This is the most important part of the targeted review.

The goldens are intended to become the correctness oracle for Phase B. A wrong golden can make a wrong implementation look verified.

## 8.1 Oracle independence

Verify that expected arithmetic/semantic outcomes are independently derived rather than copied from the production function under test.

Production calls may be used as a secondary comparison for currently-correct behavior, but they must not be the sole oracle.

Inspect especially:

```text
tests/helpers_v042_semantic_goldens.py
tests/test_v042_a04_semantic_goldens.py
```

Recalculate important values yourself.

## 8.2 Recalculate the valid accounting/funding/terminal controls

At minimum independently verify:

- `G01-LONG-ACCOUNTING`;
- `G02-SHORT-ACCOUNTING`;
- `G03-PARTIAL-WEIGHTED-ROUNDTRIP`;
- `G04-FUNDING-SIGN-TIE`;
- `G05-TERMINAL-POLICIES`;
- `G06-CASH-NO-TRADE`;
- `G07-PASSIVE-SIMPLE-ZERO-COST`.

Check signs, quantities, weighted entry, partial-realized versus unrealized PnL, fee treatment, funding sign, final cash/equity and terminal state.

For G06 verify the canonical identity is exactly:

```text
USDT_CASH_NO_TRADE
```

and that the desired CASH semantic is truly zero events/fills/fees/funding/turnover/exposure/PnL/position with constant initial equity.

## 8.3 Causal event-order goldens

Verify A04R did not encode a false universal relationship such as:

```text
order <= funding <= fill
```

The intended contract is:

```text
market availability <= signal observation <= decision <= order <= fill <= terminal
```

for the base lifecycle, while funding is an independently timestamped event when present.

When:

```text
funding_timestamp == fill_timestamp
```

the accepted tie rule is:

```text
FUNDING_BEFORE_FILL
```

A lifecycle with no funding must not invent one.

For unresolved intrabar OHLC ordering, the golden must preserve ambiguity rather than choose a favorable sequence.

## 8.4 LIMIT goldens

Verify:

- `D-B2-LIMIT-TOUCH-UNRESOLVED` correctly captures the 15m / 1ms TIF non-identifiability;
- `G10-MARKETABLE-LIMIT-AT-OPEN` does not use later OHLC extrema and requires contemporaneously observable opening marketability evidence.

Do **not** require B01 to already be implemented.

## 8.5 PIT/data goldens

Review `PIT-01` through `PIT-07` and D-H4.

The intended distinctions must include where applicable:

- product/instrument identity;
- interval identity;
- event/open time;
- close time;
- closed/completed state;
- receipt/availability time;
- original decision boundary;
- overlap/gap semantics;
- nonfinite values.

Pay special attention to `available_at_ms`: if the current `Candle` type does not yet natively carry this field, it is acceptable for A04 to use it as a **future B03 contract input**, provided the golden does not falsely claim the current production object already binds it.

A later wall clock must not retroactively make data available at the original decision time.

## 8.6 Candidate-provenance defect golden

Verify `D-B1-FORGED-CANDIDATE-REPLAY` remains a `KNOWN_DEFECT_EXPECT_REJECTION` and correctly expresses why a price around `1.0` cannot be made valid merely by rebuilding a coherent ledger/hash when market evidence is around 100.

A04 must not bless the current vulnerable acceptance behavior.

## 8.7 Random benchmark goldens

Independently check `G08-DETERMINISTIC-RANDOM-PRIMITIVE`, including the master/trial seed derivation and stdlib sampling/shuffle rule, without using the production random benchmark generator as the oracle.

Then inspect:

```text
G08B-RANDOM-EQUAL-TIMESTAMPS-AMBIGUITY
```

It should remain `CHARACTERIZATION_ONLY` if the post-sample equal-timestamp tie semantics are not yet formally frozen. A04R must not silently choose a new B06 rule.

It is acceptable and desirable for B06 to own formalizing/versioning the deterministic replay contract later.

Also consider cross-version determinism risk for Python 3.11/3.12/3.13. If the current frozen P6 rule explicitly relies on CPython stdlib behavior, document the implications rather than inventing a replacement during this review.

## 8.8 CASH defect golden

Verify `D-H2-TRADED-CASH` is a rejection specification, not a green snapshot of the vulnerable verifier.

Any traded/funded/exposed/turnover-bearing ledger labeled CASH must ultimately fail B07 regardless of coherent arithmetic.

## 8.9 Machine-readable consumer map

Verify exact B01–B07 mapping is internally consistent and references real case IDs.

Expected minimum semantic mapping:

```text
B01 → D-B2-LIMIT-TOUCH-UNRESOLVED
       G09-CAUSAL-EVENT-ORDER
       G10-MARKETABLE-LIMIT-AT-OPEN

B02 → D-H3-FUTURE-VOLUME-INVARIANCE

B03 → D-H4-CLOSED-FALSE-NOT-COMPLETE
       PIT-01-CLOSED-POSITIVE
       PIT-02-PRODUCT-MISMATCH
       PIT-03-INTERVAL-MISMATCH
       PIT-04-OVERLAP
       PIT-05-GAP
       PIT-06-NONFINITE
       PIT-07-NOT-YET-AVAILABLE

B04 → D-B1-FORGED-CANDIDATE-REPLAY
       D-H1-CLONED-RANDOM-TRIALS

B05 → D-B1-FORGED-CANDIDATE-REPLAY
       G01-LONG-ACCOUNTING
       G02-SHORT-ACCOUNTING
       G03-PARTIAL-WEIGHTED-ROUNDTRIP
       G04-FUNDING-SIGN-TIE
       G05-TERMINAL-POLICIES
       G07-PASSIVE-SIMPLE-ZERO-COST

B06 → D-H1-CLONED-RANDOM-TRIALS
       G08-DETERMINISTIC-RANDOM-PRIMITIVE

B07 → D-H2-TRADED-CASH
       G06-CASH-NO-TRADE
```

`G08B` may remain an additional B06 characterization even if not in the minimum consumer list, but the report must make clear that B06 must resolve the ambiguity before formal random replay qualification.

Return:

```text
A04_GOLDENS_READY | A04_GOLDENS_REPAIR_REQUIRED
```

---

# 9. Required adversarial mindset

Do not merely confirm that tests are green. Attempt to falsify the A-stage assumptions.

At minimum ask:

1. Can a normal pytest code path still touch a protected operational store despite A02?
2. Can `evaluate_validation_status()` still construct outcome/future-return information indirectly?
3. Can the deprecated H39 diagnostic authorize anything consequential?
4. Does any VALID_GOLDEN encode an arithmetic/sign/identity mistake?
5. Does any KNOWN_DEFECT case accidentally bless current vulnerable behavior?
6. Does the event-order contract overconstrain or invent funding?
7. Does PIT-01 incorrectly claim availability support that current production types do not possess?
8. Does G08 depend on production output rather than an independent deterministic transcription?
9. Is the equal-timestamp random behavior incorrectly promoted from characterization into normative truth?
10. Could the A01 archive classification cause deletion of a module still needed for active replay/evidence semantics?
11. Are any frozen protocol/evidence/history bytes being changed under the guise of A-stage preparation?
12. Are historical artifacts being silently re-evaluated under future corrected semantics?

You may add other synthetic adversarial probes if useful.

Do not use real Forward/H39/Final Holdout data to perform these probes.

---

# 10. Severity and finding classification

If you find issues, use:

```text
BLOCKER
HIGH
MEDIUM
LOW
```

For every BLOCKER/HIGH finding provide:

- exact file/function or artifact path;
- concrete failure/attack scenario;
- consequence for Phase-B correctness or research validity;
- whether it is an A-stage prerequisite defect or intentionally deferred Phase-B defect;
- smallest safe repair boundary;
- adversarial regression required.

Do not mark known B1/B2/H1/H2/H3/H4 behavior as a new A-stage BLOCKER merely because it remains reproducible. Those defects should be reported under **Deferred Phase-B defects** unless A04 mischaracterizes them or the preparation is insufficient to repair them safely.

An A-stage BLOCKER/HIGH should mean something like:

- a materially wrong semantic golden;
- a self-oracle presented as independent truth;
- normal test execution can still leak into real protected data;
- H5 is not actually closed;
- an archive classification would destroy required evidence/replay semantics;
- a B task lacks enough semantic specification to know what correctness means;
- frozen protocol/evidence was improperly mutated.

---

# 11. A-stage GO criteria

Return `A_STAGE_GO` only if all of the following are true:

1. **A01** is adequate and safe as a refreshable planning catalog.
2. **A02** is strong enough to make repeated Phase-B test execution hermetic against the project's actual operational/network/execution surfaces.
3. **A03** closes H5: deprecated H39 status evaluation is metadata-only and non-authoritative.
4. **A04/A04R** valid goldens are independently defensible and materially correct.
5. Known P7 defects remain clearly separated as expected-rejection/deferred repair cases rather than accepted outputs.
6. The B01–B07 consumer map provides enough explicit correctness targets for the next implementation stage.
7. No frozen H39 protocol/evidence/history was altered or unblinded.
8. Execution/live/testnet remains disabled.
9. There is no A-stage finding of severity BLOCKER or HIGH requiring repair before B01.

MEDIUM/LOW observations may coexist with `A_STAGE_GO` only if they do not create ambiguity in Phase-B correctness and are explicitly assigned to an appropriate future task.

If a semantic golden is materially wrong or ambiguous in a way that would cause B01–B07 to implement the wrong behavior, return `A_STAGE_REPAIR_REQUIRED`.

---

# 12. Explicit meaning of A_STAGE_GO

If you return `A_STAGE_GO`, state prominently that it **does not mean** any of the following:

```text
P7_GO
formal economic qualification accepted
candidate strategy qualified
profitable alpha found
B1 fixed
B2 fixed
H1 fixed
H2 fixed
H3 fixed
H4 fixed
H39 mature
H39 ready to unblind
Final Holdout accessible
paper/testnet/live execution allowed
```

The only authorized next state is:

```text
Begin bounded Phase-B correctness repairs, starting with B01.
```

---

# 13. Verification commands and safety

Use synthetic/temp inputs only.

At minimum run or independently inspect equivalent evidence for:

```bash
pytest -q tests/test_hermetic_runtime_guard.py
pytest -q tests/test_v0323_blind_validation.py tests/test_v0325_one_shot_unblind_preregistration.py tests/test_v0326_h39_blocker_repairs.py
pytest -q tests/test_v042_a04_semantic_goldens.py
ruff check .
mypy
python -m compileall -q src skill-template/scripts tools
git diff --check
```

Because A02 exists specifically to make the repository suite safe, also run:

```bash
pytest -q
```

and, if practical in the environment:

```bash
pytest --cov=btc_quant_agent --cov-report=term-missing
```

If a test attempts protected operational/H39/Final-Holdout access, stop that probe, record the exact guard result, and do not weaken the guard to make the test pass.

Do not invoke commands that:

- unblind H39;
- read Final Holdout;
- consume real validation labels;
- access live/testnet exchange execution;
- perform alpha/parameter search.

Also inspect the GitHub Actions run for the exact final review-report HEAD after the report is committed and pushed.

---

# 14. Required deliverable

Create exactly this primary review report:

```text
reviews/v0.4/V0.4.2_A_STAGE_ASTRA_TARGETED_REVIEW.md
```

The report must include these sections:

1. **Top-level verdict** — exactly `A_STAGE_GO` or `A_STAGE_REPAIR_REQUIRED`.
2. **Exact reviewed HEAD(s)** — distinguish semantic baseline, review-prompt HEAD and final report HEAD.
3. **Scope integrity** — prove this remained an A-stage review and did not implement B repairs.
4. **Findings table** — severity, owner, path, status.
5. **A01 verdict** — catalog/refactor-readiness assessment.
6. **A02 verdict** — hermeticity/adversarial boundary assessment.
7. **A03 verdict** — explicit H5 closure judgment.
8. **A04/A04R verdict** — independent semantic-golden assessment.
9. **B01–B07 readiness matrix** — for each B task state which A04 cases constrain it and whether the task is sufficiently specified to begin when its dependencies are satisfied.
10. **Deferred P7 defects** — explicitly list B1, B2, H1, H2, H3, H4 as still open unless repository evidence shows an intentional separately-reviewed repair.
11. **Safety/frozen-evidence attestation** — no H39 unblind, no Final Holdout, no live/testnet, no alpha search, no frozen protocol mutation.
12. **Verification evidence** — targeted tests, full tests/static checks and exact final-head CI.
13. **Next authorized action** — either `START_B01` for GO or a finite A-stage repair list for REPAIR_REQUIRED.

If `A_STAGE_REPAIR_REQUIRED`, provide a **small, finite repair list**. Do not propose a new broad architecture project.

---

# 15. Commit / push / CI requirements

After the audit is complete:

1. Add only the requested review report unless a tiny machine-readable audit appendix is absolutely necessary.
2. Do not modify `src/**`, tests, goldens, configs, protocols, historical artifacts or workflow files.
3. Commit the report to `v0.4`.
4. Push to `origin/v0.4`.
5. Inspect GitHub Actions for the **exact final report HEAD**.
6. Python 3.11 / 3.12 / 3.13 CI must all be green before declaring the audit delivered.
7. If a CI failure is caused only by report formatting/lint requirements, repair only the report and re-push. Do not alter production semantics as part of this audit.

Final response must provide:

```text
A-stage verdict:
Reviewed semantic baseline:
Review report commit SHA:
Final HEAD SHA:
Report path:
Exact-head CI run:
Python 3.11:
Python 3.12:
Python 3.13:
BLOCKER/HIGH findings:
Next authorized task:
```

---

# 16. Final review principle

Be adversarial but scope-disciplined.

A-stage preparation is successful if it gives Phase B a trustworthy map, a hermetic laboratory, a closed H39 diagnostic leak, and independently defensible semantic oracles.

Do not require the laboratory to have already repaired the defects it was built to repair.

Conversely, do not grant `A_STAGE_GO` merely because CI is green. If a golden is wrong, a test can touch real protected evidence through a normal project path, or H5 still constructs outcomes, the correct verdict is `A_STAGE_REPAIR_REQUIRED`.
