# BTC Quant Agent v0.4.1D — CI Acceptance Repair

## 0. Task identity

- Branch: `v0.4`
- Baseline implementation commit: `b5553f9bb018b18f7bb97f5408dfa5b7d1d8d035`
- Failed GitHub Actions run: `34307704771`
- Scope: CI acceptance repair only. Do not start P6, alpha research, H39 work, benchmark qualification, or unrelated refactors.

## 1. Goal

The P5 implementation is not complete until the pushed branch passes the repository GitHub Actions CI.

Current remote failure is at `ruff check .` for all Python 3.11 / 3.12 / 3.13 matrix jobs. The failed run reports 20 Ruff findings including import ordering, sorted `__all__`, stale `noqa`, `C408`, `B008`, `FURB162`, `TRY004`, `UP035`, and `SIM117` findings.

Repair the actual source/test/review files so that the repository passes the current CI toolchain. Do not weaken CI, disable Ruff rules globally, or pin an older Ruff merely to hide valid findings.

## 2. Required work

1. Reproduce against the CI-compatible environment as closely as practical:
   - install project with `python -m pip install -e '.[dev,research]'`;
   - run `ruff check .`;
   - run the full CI command sequence after repairs.
2. Fix all Ruff findings conservatively.
3. Preserve P0–P5 semantics and all frozen research/history behavior.
4. Do not modify H39 protocol, holdout data, Forward raw data, alpha parameters, trading enablement, or benchmark qualification.
5. Do not change tests merely to bypass behavior. Mechanical style fixes are allowed; semantic changes require an explicit justification in the repair report.
6. Specifically inspect the CI-reported files, including at least:
   - `reviews/v0.4/v040_review_checks.py`
   - `src/btc_quant_agent/economic/__init__.py`
   - `src/btc_quant_agent/research_contract/__init__.py`
   - `src/btc_quant_agent/research_contract/canonical.py`
   - `src/btc_quant_agent/research_contract/models.py`
   - `src/btc_quant_agent/research_contract/registry.py`
   - `tests/test_v040_research_kernel_economic_validation.py`
   - `tests/test_v041_c2_intrabar_exit_slippage_bounds.py`
   - `tests/test_v041_p0_p1_cash_ledger.py`
   - `tests/test_v041_p5_protocol_evidence_persistence.py`

## 3. Local acceptance commands

Run at minimum:

```bash
ruff check .
mypy
pytest -q
pytest --cov=btc_quant_agent --cov-report=term-missing
python -m compileall -q src skill-template/scripts
git diff --check
```

All must pass locally before push.

## 4. Mandatory Git / GitHub completion contract

This task is NOT complete after local tests.

The agent must:

1. Commit all required changes on branch `v0.4` with an appropriate message, suggested:
   - `fix(ci): restore v0.4.1D quality gate compatibility`
2. Push the commit to `origin/v0.4` itself.
3. Verify that the pushed commit is the remote branch HEAD.
4. Monitor the GitHub Actions `CI` workflow for that exact pushed commit.
5. Require all matrix jobs (Python 3.11, 3.12, 3.13) to complete successfully.
6. If CI fails, inspect the failing job/log, repair, commit, push again, and repeat until the latest pushed commit is GREEN.
7. Do not report the task as complete while CI is pending, cancelled, skipped unexpectedly, or failing.

If `gh` is available/authenticated, use commands such as:

```bash
gh run list --branch v0.4 --workflow CI --limit 5
gh run view <run-id> --log-failed
```

Otherwise use the available GitHub integration/API. The important requirement is evidence that CI for the exact final commit succeeded.

## 5. Deliverable

Create/update:

`reviews/v0.4/V0.4.1D_CI_ACCEPTANCE_REPAIR_REPORT.md`

The report must include:

- baseline failed commit and run id;
- exact Ruff/CI failure categories repaired;
- files changed;
- local acceptance command results;
- final commit SHA;
- confirmation that it was pushed to `origin/v0.4`;
- final GitHub Actions CI run id;
- final CI conclusion `success`;
- confirmation that Python 3.11 / 3.12 / 3.13 jobs all passed.

## 6. Definition of done

PASS only if all are true:

- `ruff check .` passes;
- `mypy` passes;
- full pytest passes;
- coverage pytest passes;
- compileall passes;
- diff check passes;
- P0–P5 behavior remains intact;
- changes are committed and pushed by the agent;
- remote `v0.4` HEAD equals the final repair commit;
- GitHub Actions CI for that exact commit is fully GREEN across all matrix jobs.

Only after this gate passes may v0.4.1E / P6 begin.
