# BTC Quant Agent v0.5.1 — Current Phase Handover — 2026-09-27

## 0. Purpose and authority

This document is the current controller handover for:

```text
EXASHXE/btc-quant-agent-public
```

It supersedes for current operational/controller decisions:

```text
docs/handover/V0.5.1_QUANT_AGENT_PHASE_HANDOVER_2026-09-20.md
docs/handover/V0.5.1_QUANT_AGENT_PHASE_HANDOVER_2026-09-21.md
docs/handover/V0.5.1_QUANT_AGENT_PHASE_HANDOVER_2026-09-25.md
```

Older handovers remain immutable historical evidence.

This handover is not itself scientific authority, implementation authority, P3 authority, Discovery
authority, execution authority, or outcome evidence. Accepted versioned protocol/evidence/review/code
at exact referenced commits wins if any conflict exists.

---

# 1. Exact repository state at handover creation

Repository:

```text
https://github.com/EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
HEAD =
f3678676265ba53e1d874e27bc0c922c7783acaa
```

Docs branch immediately before this handover:

```text
v0.5-refactor-docs
HEAD =
9648772f5092f10d73030e773c28cd2789e25dcc
```

Current immediate next-stage prompt:

```text
prompts/v0.5/implementation/
v0.5.1_H40_PRE_P3_IMPLEMENTATION_AUTHORITY_REPUBLICATION.md

prompt commit =
9648772f5092f10d73030e773c28cd2789e25dcc
```

Required implementation start SHA:

```text
f3678676265ba53e1d874e27bc0c922c7783acaa
```

---

# 2. Current accepted pre-P3 state

The Pre-P3 source-byte / lookback-reference closure was independently accepted by the Controller.

Accepted implementation:

```text
f3678676265ba53e1d874e27bc0c922c7783acaa
```

Accepted exact-SHA evidence commit:

```text
a781d8c756913e948b32225334d2cb27e1bcd6fa
```

Accepted evidence manifest:

```text
evidence/v0.5/h40/
V0.5.1_H40_PRE_P3_CLOSURE_EXACT_SHA_EVIDENCE_f3678676_R1.json
```

Manifest SHA-256:

```text
609632e06c280adfa20ae5db5b0b3d0736792252ac07de93b1bf1e368ea210be
```

Controller exact-SHA acceptance:

```text
reviews/v0.5/
V0.5.1_H40_PRE_P3_CLOSURE_EXACT_SHA_CONTROLLER_ACCEPTANCE.md

commit =
8ae06121d1e83db8df612951914629c62ed70c4b
```

Closed items:

```text
MED-01 Parquet path reopen TOCTOU =
CLOSED

MED-02 720h lookback-reference ambiguity =
H40_WF1_LOOKBACK_REFERENCE_INTERPRETATION_A
RATIFIED + REGRESSION LOCKED

Ruff evidence contradiction =
CLOSED by pinned Ruff 0.16.6 replay

exact-SHA evidence reconciliation =
ACCEPTED
```

Focused validation:

```text
408 passed
```

Full-suite validation:

```text
1698 passed / 0 failed / 9 warnings
1698 passed / 0 failed / 9 warnings
```

No GitHub Actions workflow/status was exposed for the accepted implementation SHA. Acceptance is
therefore explicitly based on the committed content-addressed exact-SHA local evidence package, not a
claim of observed remote CI.

---

# 3. Current frozen identities

Preserve exactly:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

discovery_provenance_contract_hash =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

lifecycle_semantic_root_v5 =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle_governance_authority_v5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf
```

Lifecycle V5 child hashes remain 9/9 exact.

Current structural universe:

```text
total slots = 168
REGISTERED = 18
NOT_TESTABLE = 150
```

---

# 4. Current authority publication state

The old historical implementation authority:

```text
a23ceec50ff5703f4dff703a7b44e715501164a48ec9be48ae096f8bd4d51fca
```

remains historical evidence and must not be treated as current.

A new typed implementation authority has been Controller-materialized for the accepted repaired SHA.

Materialized authority hash:

```text
088b1210c17171141e232219345fa890e182282445fd9b2a70804b177d808b96
```

Materialization artifact:

```text
reviews/v0.5/
V0.5.1_H40_PRE_P3_REPAIRED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_MATERIALIZATION.md
```

Current production constants are still:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

Therefore the immediate next task is only to republish:

```text
088b1210c17171141e232219345fa890e182282445fd9b2a70804b177d808b96
```

as the accepted lifecycle implementation authority.

P3 Controller Authority must remain `None`.

---

# 5. Permanent protected state

Unless a later exact accepted authority explicitly changes a field:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None

live trading = NOT AUTHORIZED

H40_P3 = NOT AUTHORIZED
H40_DISCOVERY = NOT AUTHORIZED

H39 = PROTECTED
Confirmation = SEALED
Final Holdout = SEALED

NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```

Do not access real H40 outcomes before explicit Discovery authorization.

Do not infer execution authority from research/Discovery/Playbook acceptance.

---

# 6. Project priority reset — STRATEGY FIRST

The project priority is now explicitly:

> Find at least one viable, reproducible, PIT-valid, cost-aware, walk-forward validated trading
> Playbook before spending major effort on orchestration frameworks or broad refactoring.

Current mainline objective:

```text
FIRST_VALIDATED_PLAYBOOK_V1
```

The project is still deterministic research infrastructure, but infrastructure/governance work is no
longer an end in itself.

After the current pre-Discovery authority closure, governance expansion must stop unless a real
correctness/authority defect is discovered.

Default priority:

```text
P0  Research correctness / PIT / evidence integrity
P1  Find viable strategy / Validated Playbook
P2  Data and feature expansion serving P1
P3  Runtime performance required by research
P4  Architecture cleanup
P5  TradingAgents / multi-agent integration
P6  Automated execution
```

Do not let P4/P5 block P1.

---

# 7. Mainline stage plan

## M0 — Governance closure

Current stage.

Sequence:

```text
Pre-P3 exact-SHA acceptance              DONE
new implementation authority materialize DONE
implementation authority republication   NEXT
Controller publication acceptance        REQUIRED
```

Exit:

```text
implementation authority = published
P3 controller authority = None
Discovery = unauthorized
execution = RESEARCH_DISABLED_V1
```

After M0, no proactive governance redesign.

---

## M1 — Architecture & Data Baseline R2

M1 is mandatory before formal research/Discovery starts, but it is a **read-only research-capability
inventory**, not a refactor project.

Primary purpose:

> Determine exactly which data, features, research machinery and bottlenecks are already available to
> find viable strategies.

Required outputs:

```text
AS_IS_ARCHITECTURE.md
DATA_ARCHITECTURE.md
MODULE_OWNERSHIP_MATRIX.md
architecture_inventory.json
RESEARCH_CAPABILITY_MATRIX.md
DATA_FEATURE_SIGNAL_PLAYBOOK_MAP.md
REFACTOR_BACKLOG.md
```

Research capability inventory should explicitly map:

```text
market data
  -> PIT/source authority
  -> feature availability
  -> signal hypothesis
  -> eligible research consumer
  -> Playbook opportunity
```

Inventory at minimum:

```text
OHLCV
Mark Price
Funding
OI
aggTrade
L2 Depth
long/short ratio
top-trader ratio if available
forward microstructure
cross-asset data

trend/momentum
volatility/range
breakout/reversal
funding/crowding
OI/positioning
volume/order flow
microstructure
```

M1 must not:

```text
modify production code
change science
change authority
access protected outcomes
run real Discovery
perform broad refactoring
integrate TradingAgents
```

M1 creates a factual baseline and research roadmap only.

---

## M2 — P3 Controller Authority + Discovery Readiness

After M1:

```text
published implementation authority
-> P3 Controller Authority
-> Discovery Run Grant
-> runtime snapshot seal
-> production-shaped negative/dry-run
```

Exit:

```text
H40_DISCOVERY_READY_AWAITING_EXPLICIT_GO
```

No real Discovery result access yet.

---

## M3 — First Real Discovery

After explicit GO only.

Run the currently frozen/registered search space first:

```text
168 structural slots
18 registered
150 NOT_TESTABLE
```

Do not expand the search space merely because the first results are weak.

Primary question:

> Does any currently preregistered family exhibit reproducible structural edge under the frozen
> protocol?

Forbidden:

```text
best-backtest-wins
manual cherry-pick
post-result threshold tuning
retrospective feature invention
post-outcome scientific-rule selection
```

Deliver:

```text
complete Discovery population
eligible/rejected families
family-level evidence
candidate ranking under frozen rules
Candidate-Lock eligible set
```

---

## M4 — Candidate Lock

Lock the exact candidate identity before WF validation.

Bind:

```text
candidate_id
feature family
direction
regime
opportunity
parameters
cost assumptions
source identity
validation protocol
```

Do not tune after seeing downstream validation.

---

## M5 — Post-Discovery / Pre-WF independent audit

This is the next mandatory major Astra milestone.

Audit focus:

```text
look-ahead / PIT
candidate cherry-pick
multiple testing / correction
complete search-space accounting
Candidate Lock correctness
source/economic-value identity
cost/funding assumptions
selection leakage
protected outcome boundaries
```

PASS does not itself authorize execution.

If blocked, produce a narrow repair contract.

---

## M6 — Walk-Forward Validation

Each locked candidate enters chronological:

```text
TRAIN
-> CALIBRATION
-> VALIDATION
```

Primary evaluation surfaces include:

```text
trade count
direction balance
hit rate
expectancy
net return
cost sensitivity
max drawdown
fold stability
year stability
regime stability
turnover
funding drag
```

PASS/FAIL comes from the accepted frozen protocol, not post-hoc judgment.

---

## M7 — Playbook Qualification

Lifecycle:

```text
Hypothesis
-> Discovery Candidate
-> Locked Candidate
-> WF Validated Candidate
-> VALIDATED_PLAYBOOK_V1
```

A validated Playbook must be expressible as:

```text
WHEN:
market/regime/setup state

IF:
observable causal conditions

THEN:
LONG / SHORT / FLAT

ENTRY:
...

EXIT:
...

RISK:
...

INVALIDATION:
...

EXPECTED BEHAVIOR:
...
```

The first `VALIDATED_PLAYBOOK_V1` is the current main project milestone.

---

# 8. Research-expansion loop if current roster fails

If the current 18 registered slots produce no qualified Playbook, do not blindly add hundreds of
indicators.

Use failure evidence to preregister the next hypothesis family.

Priority:

## R1 — Trend / structure

```text
MA/EMA slope and alignment
ADX / trend strength
breakout structure
ATR / volatility regime
candlestick / market-structure state
multi-horizon trend confirmation
```

## R2 — Derivatives positioning

```text
OI
Delta OI
Funding
long/short ratio
top-trader ratio
basis
price x OI state
crowding / liquidation proxy where PIT-valid
```

Natural state decomposition includes:

```text
Price up + OI up
Price up + OI down
Price down + OI up
Price down + OI down
```

## R3 — Microstructure

```text
L2 imbalance
OFI
aggTrade imbalance
spread/depth
microprice
short-horizon liquidity
false-breakout confirmation
entry timing
```

Use microstructure primarily as confirmation/timing before treating it as an independent medium-horizon
directional predictor.

Each expansion cycle must be preregistered before outcome inspection and then return to M3.

---

# 9. M8/M9 after viable Playbooks exist

Do not build the main Policy Agent around unvalidated ideas.

Target first:

```text
2-3 independently validated Playbooks
```

Then:

## M8 — Playbook portfolio

Build a registry/portfolio of validated strategies with explicit eligibility and conflict semantics.

## M9 — Validated Playbook Policy Agent

Agent role:

```text
current market state
-> eligible validated Playbooks
-> conflict resolution
-> risk budget
-> policy decision
```

LLM/Agent may propose/explain/route.

It must not become the scientific source of truth.

---

# 10. TradingAgents parallel track — non-blocking

TradingAgents is no longer on the mainline critical path.

Run independently as:

```text
TA-1 Architecture study
-> TA-2 Compatibility / abstraction mapping
-> TA-3 Prototype orchestration
```

Allowed topics:

```text
LangGraph orchestration
role decomposition
debate topology
checkpoint/recovery
memory/reflection
multi-provider LLM abstraction
portfolio context
```

Do not allow the TradingAgents track to:

```text
replace QuantCore
control H40 Discovery
change PIT/source semantics
change accepted research protocol
become scientific authority
authorize Playbooks
connect execution
block M1-M7
```

Any useful result remains a candidate for later M8/M9 adoption.

---

# 11. Refactor parallel track — non-blocking

M1 may produce a `REFACTOR_BACKLOG`.

A refactor enters the mainline immediately only if it materially affects:

```text
correctness
research credibility
PIT/evidence integrity
critical runtime/resource feasibility
```

Code cleanliness, naming, folder aesthetics, generalized abstractions, or TradingAgents compatibility
alone must not block strategy discovery.

---

# 12. Model allocation — when to use Gemini, Codex Sol, Controller Sol, Astra

## 12.1 Gemini 3.8 Flash — default long/mechanical executor

Use Gemini 3.8 Flash for:

```text
long repository/data inventory
Architecture & Data Baseline raw scan
mechanical implementation with frozen semantics
evidence/log generation
exact-SHA validation
test/static-gate execution
large repetitive audits
documentation/inventory generation
low-risk maintenance
```

Good default stages:

```text
M0 authority republication implementation
M1 raw architecture/data inventory
routine evidence refresh
mechanical regression additions
TA-1 repository comparison/inventory
```

Gemini output is never automatically final scientific/controller authority.

---

## 12.2 Codex Sol High — use when implementation requires real engineering judgment

Escalate to **Codex Sol High** when the task changes or introduces non-trivial code behavior across
modules.

Use Codex Sol High for:

```text
new research-feature implementation
new preregistered R1/R2/R3 hypothesis-family code
cross-module data/feature pipeline changes
PIT-sensitive feature logic
complex cache/state/storage changes
performance work touching production semantics
core refactor required to make research feasible
non-trivial debugging after Gemini fails/repeats
Playbook registry / policy implementation
M8/M9 production-quality architecture work
```

Specific planned trigger points:

```text
M1:
not required for raw inventory;
use Codex Sol only if a critical architecture/data defect requires code repair.

M2:
use Codex Sol if P3/run-grant publication requires non-mechanical source/test changes.
If publication is constant/test-state only, Gemini is enough.

M3:
normally no implementation if frozen Discovery runner already exists.
Use Codex Sol only for real code defects found before/while launching Discovery.

R1/R2/R3 research expansion:
Codex Sol High strongly preferred for implementing new feature families and causal data plumbing.

M8/M9:
Codex Sol High expected for substantive implementation.
```

Codex must not invent scientific semantics. Missing research semantics go back to a docs-only
preregistration/contract stage first.

---

## 12.3 Controller Sol High — use for semantic decisions and acceptance

Use strong Sol reasoning for:

```text
scientific/causal/statistical ambiguity
authority design
PIT semantic adjudication
research-hypothesis design
candidate-lock review
exact evidence acceptance
architecture synthesis after M1
deciding whether a failed Discovery implies new hypothesis-family research
reviewing Codex/Gemini implementation before advancing
```

M1 recommended workflow:

```text
Gemini 3.8 Flash =
exhaustive factual inventory

Controller Sol High =
synthesize Research Capability Matrix
prioritize strategy hypotheses
decide what is mainline vs backlog
```

---

## 12.4 GPT-6 Astra — rare cumulative auditor only

Astra is reserved for high-value milestone audits, not routine implementation.

Mandatory/expected next use:

```text
M5 =
Post-Discovery / Pre-WF independent adversarial audit
```

Use Astra again when one of these conditions occurs:

```text
a major scientific-protocol redesign
a major PIT/source-provenance redesign
a major authority/lifecycle redesign
a broad core refactor touching multiple trusted research boundaries
before promoting a set of validated Playbooks into a policy layer, if cumulative risk justifies it
```

Do not use Astra for:

```text
routine lint
mechanical tests
ordinary docs
simple authority constant publication
M1 inventory
minor refactors
every Candidate/Playbook iteration
```

Astra output must always be independently triaged by the Controller.

All future Astra prompts must preserve the safe defensive-output constraints already established in:

```text
prompts/v0.5/audit/
v0.5.1_H40_ASTRA_PRE_DISCOVERY_F01_F04_DEEP_AUDIT.md

safe version lineage:
2c46e18bf7457fa170558c9cfdbef07eb0d2e430
```

Do not request secrets, protected outcomes, exploit payloads, or operational offensive instructions.

---

# 13. Model assignment by planned stage

```text
M0 authority republication implementation:
Gemini 3.8 Flash
Controller acceptance:
Sol High

M1 Architecture & Data Baseline R2:
Gemini 3.8 Flash exhaustive scan
Sol High synthesis/acceptance
Codex Sol only for a discovered critical repair
Astra: NO

M2 P3 + Discovery Readiness:
Gemini for mechanical publication
Sol High for authority/readiness acceptance
Codex Sol if implementation becomes non-trivial
Astra: normally NO

M3 First Real Discovery:
existing deterministic code
Sol High controller supervision/interpretation
Astra: NO during outcome generation

M4 Candidate Lock:
Sol High controller
Gemini for mechanical evidence
Codex Sol only if code repair needed

M5 Post-Discovery / Pre-WF:
GPT-6 Astra + Controller Sol adjudication

M6 Walk-Forward:
deterministic runner
Gemini for long execution/evidence
Sol High for acceptance
Codex Sol for code defects only

M7 Playbook Qualification:
Sol High scientific/controller review
Gemini evidence support
Astra only if a genuinely cumulative high-risk ambiguity appears

R1/R2/R3 expansion:
Sol High preregistration/design
Codex Sol High implementation
Gemini long validation/evidence
Astra only after major cumulative semantic change

M8/M9:
Sol High architecture
Codex Sol High implementation
Astra optional cumulative pre-policy audit
```

---

# 14. Controller operating rules — mandatory

The Controller must preserve the following rules from previous handovers.

## 14.1 Remote repository is source of execution truth

When the user says done/pushed/completed, inspect GitHub directly.

Verify:

```text
remote branch HEAD
parent/start SHA
actual diff
artifact paths
evidence identity
test/static results
remote commit existence
```

User/executor summaries are leads, not authority.

## 14.2 Exact-SHA evidence

Formal acceptance prefers:

```text
commit code
-> test exact committed SHA
-> commit content-addressed evidence
-> Controller independently verifies
```

Any post-validation code change invalidates the prior exact-SHA evidence.

## 14.3 Scientific ambiguity fail-stop

If a causal/statistical/scientific rule is ambiguous:

```text
BLOCK
-> docs-only preregistration/contract closure
-> independent acceptance
-> implementation
```

Do not let an executor choose semantics after outcomes exist.

## 14.4 Protected outcomes

Do not open:

```text
real H40 Discovery outcomes before authorization
WF calibration outcomes outside authorized lifecycle
H39 protected outcomes
Confirmation
Final Holdout
```

## 14.5 Execution remains independent

Research, P3, Discovery, Candidate Lock, WF or Playbook acceptance does not authorize execution.

## 14.6 Major decisions become docs artifacts

Commit Controller:

```text
acceptance
blocker triage
authority acceptance
scientific contract decision
P3/Discovery authorization
major audit adjudication
```

to the docs branch.

## 14.7 Docs vs implementation branches

```text
v0.5-refactor =
source/test implementation

v0.5-refactor-docs =
prompts/reviews/evidence/docs
```

Preserve historical artifacts; add superseding artifacts rather than rewriting history.

## 14.8 Model output is not authority

Gemini, Codex Sol and Astra findings require Controller verification before stage advancement.

## 14.9 Strategy-first discipline

After M0:

- do not create new governance layers without a demonstrated blocker;
- do not perform broad refactors unless required for correctness/research feasibility;
- do not let TradingAgents work block M1-M7;
- prefer falsifying/qualifying strategy hypotheses over framework expansion.

---

# 15. Immediate next instruction

Do not start M1 yet.

Execute first:

```text
prompts/v0.5/implementation/
v0.5.1_H40_PRE_P3_IMPLEMENTATION_AUTHORITY_REPUBLICATION.md
```

Required start:

```text
f3678676265ba53e1d874e27bc0c922c7783acaa
```

Publish exactly:

```text
088b1210c17171141e232219345fa890e182282445fd9b2a70804b177d808b96
```

Recommended executor:

```text
Gemini 3.8 Flash
```

After push:

1. Controller independently reviews publication diff/tests;
2. if PASS, Controller commits publication acceptance;
3. then create and execute the read-only **Architecture & Data Baseline R2** prompt;
4. M1 must finish before P3/real Discovery starts;
5. TradingAgents work may continue independently and must not block this sequence.

Formal current state:

```text
PRE_P3_EXACT_SHA_ACCEPTED
NEW_IMPLEMENTATION_AUTHORITY_MATERIALIZED
IMPLEMENTATION_AUTHORITY_REPUBLICATION_PENDING

ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1

MAIN_MILESTONE =
FIRST_VALIDATED_PLAYBOOK_V1
```
