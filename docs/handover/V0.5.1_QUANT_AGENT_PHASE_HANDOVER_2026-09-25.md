# BTC Quant Agent v0.5.1 — Current Phase Handover — 2026-09-25

## 0. Purpose and authority

This document is the current controller handover for `EXASHXE/btc-quant-agent-public`.

It supersedes, for current operational/controller decisions:

```text
docs/handover/V0.5.1_QUANT_AGENT_PHASE_HANDOVER_2026-09-20.md
docs/handover/V0.5.1_QUANT_AGENT_PHASE_HANDOVER_2026-09-21.md
```

Those older files remain immutable historical evidence and must not be deleted or rewritten to make history appear cleaner.

This handover is **not itself scientific authority, lifecycle authority, implementation authority, P3 authority, or Discovery authority**. It is the controller's exact project-state and operating-rules snapshot.

When this handover conflicts with an accepted versioned protocol, accepted evidence manifest, accepted controller review, or exact repository code at the referenced commit, the accepted content-addressed artifact wins.

---

# 1. Repository and current exact branch state

Repository:

```text
https://github.com/EXASHXE/btc-quant-agent-public
```

Implementation branch:

```text
v0.5-refactor
HEAD =
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Docs / prompts / reviews / evidence branch:

```text
v0.5-refactor-docs
HEAD at handover creation =
018ec8cd6c58ed2c3effb95f60d972134453fb93
```

Current next-stage prompt:

```text
prompts/v0.5/review/
v0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTANCE_AND_V5_HASH_MATERIALIZATION.md

prompt commit =
018ec8cd6c58ed2c3effb95f60d972134453fb93
```

That prompt has been created but **not yet executed** as of this handover.

---

# 2. Project role and architecture

The repository is a deterministic quantitative-research infrastructure project, not a live trading bot.

Current project direction:

```text
Validated Playbook Policy Agent
```

rather than:

```text
single unrestricted Alpha search
```

Long-run architecture:

```text
auditable market data
    ->
deterministic quantitative core
    ->
validated playbook / research evidence
    ->
deterministic policy/router
    ->
risk engine
    ->
optional agent orchestration/explanation
    ->
user
```

The LLM/Agent must not be the numerical source of truth.

Deterministic code/evidence owns:

- source provenance;
- Point-in-Time semantics;
- features;
- candidate universe;
- calibration;
- costs;
- outcome definitions;
- statistical correction;
- selection;
- lifecycle;
- replay;
- authority identities.

An LLM may:

- plan;
- independently review;
- generate implementation prompts;
- inspect exact repository code;
- compare evidence;
- create controller review artifacts;
- explain results.

It may not silently replace frozen scientific logic with intuition.

---

# 3. Permanent safety and protected-surface state

Unless an explicit later accepted authority artifact changes a specific field, preserve:

```text
execution policy =
RESEARCH_DISABLED_V1

ACCEPTED_EXECUTION_WRITE_AUTHORITY =
None

auto_execute =
false

live trading =
NOT AUTHORIZED

H39 =
PROTECTED

Confirmation =
SEALED

Final Holdout =
SEALED

H40_P3 =
NOT AUTHORIZED

H40_DISCOVERY =
NOT AUTHORIZED

NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```

Do not access real H40 outcomes merely because code exists.

Do not access real WF1_CALIBRATION outcomes merely because an implementation authority exists.

No Discovery run is allowed until explicit controller P3/Discovery authority is published after the repair/re-audit sequence.

---

# 4. Current frozen H40 scientific identities

These identities remain frozen and were explicitly preserved by the latest Repair Contract Freeze R1:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Scientific identity interpretation:

```text
protocol_authority_hash
    = accepted cumulative H40 protocol identity

scientific_semantic_root
    = accepted active scientific formula/contract semantics

structural_ledger_hash
    = exact 168-slot structural search-space ledger
```

Do not silently re-hash or redefine these because a verifier/evidence implementation is being repaired.

The current repair is intended to make the verifier prove the already-accepted science, not to retune that science.

---

# 5. Search-space / runtime snapshot facts

Current accepted structural universe:

```text
total slots       = 168
REGISTERED        = 18
NOT_TESTABLE      = 150
```

Current active required source union:

```text
ETHUSDT_USD_M_1H
```

The 168 rows stay structurally present even if source authority changes.

A runtime source promotion must not change structural configuration hashes.

A source that is not accepted for production cannot be silently treated as available because a local file exists.

---

# 6. Six closed P3B scientific ambiguities

These were closed before the latest Astra audit and remain closed.

Do **not** reopen them during current repair unless a direct contradiction is proven.

## 6.1 Fit-invalid correction policy

Authority:

```text
7de7c706...
```

Frozen rule:

```text
any fit-invalid REGISTERED member
=> whole family NO_REPRESENTATIVE for both governing metrics

family remains in correction universe
K = 6 fixed Holm families
NO_REPRESENTATIVE contributes p = 1
```

## 6.2 Bootstrap seed identity

Authority:

```text
7b2af0e0...
```

Canonical seed preimage:

```text
[
  protocol_authority_hash,
  structural_configuration_hash,
  WF1_CALIBRATION,
  metric_id
]
```

## 6.3 Proxy net-return formula

Authority:

```text
8a016bcf...
```

Frozen formula:

```text
gross = action_sign * expm1(r_h)
r_net = gross - 0.0012
```

## 6.4 Bootstrap block boundary

Authority:

```text
0958dfa8...
```

Frozen rule:

```text
source-anchored observation bundles
no synthetic endpoint crossing
```

## 6.5 Start sampler

Authority:

```text
25d1af12...
```

Frozen implementation:

```text
PCG64
one rng.integers call
M = 10000
L = 168
NumPy replay version must match
```

## 6.6 Coverage population

Authority:

```text
a9f4d5a0...
```

Frozen rule:

```text
WF1_CALIBRATION final LONG/SHORT
over all base-eligible decision rows
active scope only
exact integer 0.25%-10% gates
```

Latest Astra R2 explicitly classified all six as MATCH.

---

# 7. Accepted implementation-authority lineage before latest repair

Previously accepted tested implementation baseline:

```text
d73e8984446f933059b978e36c4cabd55de517aa
```

Final exact-SHA evidence:

```text
evidence/v0.5/h40/
V0.5.1_H40_FINAL_REACCEPTANCE_EVIDENCE_d73e898.json

manifest SHA-256 =
5cd591ef7f3f43c7897711ab4e75cb158ac048d328e1f8b0dac28a0a6bcb6540
```

Controller final reacceptance:

```text
reviews/v0.5/
V0.5.1_H40_FINAL_EXACT_SHA_REACCEPTANCE_CONTROLLER_ACCEPTANCE.md

commit =
0cbb20a7806f2336864e430910bf6a4ff4a99df9
```

Published lifecycle implementation authority:

```text
d3a304ddc6bcb7b7fc398ccf45a751a0afa3f91634a09ddff8e199af1970e440
```

Authority publication implementation commit:

```text
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Authority republication docs commit:

```text
fb0852882f2333ccf718a56f8d627b059fa9e3f9
```

Controller publication acceptance:

```text
reviews/v0.5/
V0.5.1_H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_REPUBLICATION_CONTROLLER_ACCEPTANCE.md

commit =
0a45886a07335aef3df83bf2223ffa00b03ffb8b
```

This implementation-authority publication was valid for the then-current lifecycle semantics.

The latest repair contract now requires lifecycle semantic/governance changes, so a **new implementation authority will eventually be required** after both Repair A and Repair B are accepted.

Do not assume `d3a304...` can remain the final current implementation authority after lifecycle V5 materialization.

---

# 8. Performance/evidence history

The expensive H40 test suite was optimized without changing production scientific semantics.

Important accepted evidence:

Historical same-host full-suite baseline:

```text
657.69s median wall
```

Accepted R2 optimized exact-SHA evidence:

```text
candidate median wall =
467.47s

reduction =
28.92%

mapped top-five:
121.58s -> 25.51s
reduction = 79.02%
```

Later authority publication full suite:

```text
1588 passed
wall ~379.39s
```

Performance evidence is not scientific authority.

Do not weaken tests to preserve performance targets.

---

# 9. Latest Astra pre-Discovery audit R2

Audit artifacts:

```text
reviews/v0.5/
V0.5.1_H40_PRE_DISCOVERY_ASTRA_CUMULATIVE_AUDIT_R2.md

evidence/v0.5/h40/
V0.5.1_H40_PRE_DISCOVERY_ASTRA_AUDIT_R2.json
```

Audit docs commit:

```text
4f5786a25a5e25db32505a99445a3a42c62f7446
```

CI:

```text
GitHub Actions run:
36101797128

conclusion:
SUCCESS
```

Verdict:

```text
PRE_DISCOVERY_ASTRA_AUDIT_FAIL
```

Counts:

```text
CRITICAL = 0
HIGH     = 4
MEDIUM   = 0
LOW      = 1
INFO     = 1
```

The four HIGH findings were accepted by the controller as real blockers.

---

# 10. Current four HIGH findings

## R2-F01 — Controller P3/Discovery authorization boundary

A production lifecycle service could reach Discovery authority without a separately accepted controller-level P3/Discovery grant being a runtime prerequisite.

Status:

```text
OPEN HIGH
CONTRACT FROZEN
IMPLEMENTATION NOT YET REPAIRED
```

## R2-F02 — source economic-value binding

Existing source authority proved source bytes / timestamps / source identity, but the scientific OHLC values consumed by P3B evidence were not independently proven to be deterministic extraction from those sealed bytes.

Status:

```text
OPEN HIGH
CONTRACT FROZEN
IMPLEMENTATION NOT YET REPAIRED
```

## R2-F03 — causal decision-time provenance

Existing science was already preregistered, but the production verifier accepted lower-level prefit values instead of independently reconstructing causal:

```text
regime_state
opportunity_state
raw_score
secondary_filter_state
```

from accepted source history under frozen R3R2 formulas.

Status:

```text
OPEN HIGH
CONTRACT FROZEN
IMPLEMENTATION NOT YET REPAIRED
```

## R2-F04 — service authority lifetime consistency

Implementation authority was checked at construction, but authoritative operation across service lifetime was not sufficiently guaranteed to revalidate the currently published accepted anchor.

Status:

```text
OPEN HIGH
CONTRACT FROZEN
IMPLEMENTATION NOT YET REPAIRED
```

---

# 11. Repair Contract Freeze R1

Controller execution produced:

```text
reviews/v0.5/
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.md

report commit =
fd44362ad6dbf5d35b2d5e938f7c30beac6e19c0
```

Machine-readable contract:

```text
evidence/v0.5/h40/
V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1.json

commit =
6dedefbd0776b36891cfaa553f3a27f23c604648
```

Formal status:

```text
REPAIR_CONTRACT_FREEZE_READY_FOR_CONTROLLER_REVIEW

SCIENTIFIC_FROZEN_RULES_CHANGED = NO

PROTOCOL_AUTHORITY_HASH_CHANGE_REQUIRED = NO
SCIENTIFIC_SEMANTIC_ROOT_CHANGE_REQUIRED = NO
STRUCTURAL_LEDGER_HASH_CHANGE_REQUIRED = NO

LIFECYCLE_SEMANTIC_ROOT_CHANGE_REQUIRED = YES
LIFECYCLE_GOVERNANCE_AUTHORITY_CHANGE_REQUIRED = YES

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

---

# 12. Repair Contract R1 — authority/lifecycle design

## 12.1 New static controller authority

Frozen schema concept:

```text
H40_P3_CONTROLLER_AUTHORITY_V1
```

It binds:

- controller acceptance identity;
- protocol authority;
- scientific semantic root;
- structural ledger;
- lifecycle semantic root;
- lifecycle governance authority;
- lifecycle implementation authority;
- Discovery provenance contract;
- selection/correction contract;
- permitted partitions;
- exact source/target lifecycle transition;
- execution-disabled state.

Before final P3 authorization:

```text
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

must remain fail-closed.

## 12.2 New per-run grant

Frozen schema:

```text
H40_DISCOVERY_RUN_GRANT_V1
```

It binds controller authority to the exact run:

- run authority ID;
- materialized run authority;
- runtime snapshot;
- source manifest;
- split manifest;
- split attestation;
- sealed registered roster;
- permitted partitions;
- execution-disabled state.

## 12.3 Discovery receipt upgrade

Production Discovery receipt:

```text
H40_RECEIPT_DISCOVERY_AUTH_V3
```

adds:

```text
controller_authority_hash
discovery_run_grant_hash
```

V2 remains historical/synthetic only.

## 12.4 Persistence context upgrade

Production persistence context:

```text
H40_LIFECYCLE_PERSISTENCE_CONTEXT_V2
```

adds the same grant/controller hashes.

V1 remains historical/synthetic only.

## 12.5 Service-lifetime rule

Every authoritative operation must independently compare stored production authority with the current in-process accepted authority constants.

Hot authority republication inside a running authoritative process is not supported.

A new publication requires a new authoritative service/process identity.

A stale service fails closed.

---

# 13. Repair Contract R1 — scientific provenance design

## 13.1 Discovery provenance contract

Frozen new identity:

```text
H40_DISCOVERY_PROVENANCE_CONTRACT_V1

hash =
9a5a71101875829aff2959c93391648dbfd94ab26fd55ed31b720610fa7be81b
```

It contains:

```text
H40_SOURCE_VALUE_EXTRACTION_V1

H40_CAUSAL_PREFIT_RECONSTRUCTION_V1
```

This is a **new provenance/evidence identity**, not a scientific retuning identity.

## 13.2 Source-value reconstruction

The production verifier must reconstruct OHLC directly from exact sealed source bytes.

Transported OHLC is not authoritative by itself.

Frozen first-Discovery support interval:

```text
[2021-01-01T00:00:00Z, 2023-02-01T00:00:00Z)
```

The pre-2021-01-31 region is only:

```text
LOOKBACK_RESERVED support
```

and does not enter the decision population.

New source evidence:

```text
H40_P3B_SOURCE_DERIVATION_V2
```

Existing:

```text
H40_P3B_SOURCE_HOURS_V1
```

becomes historical/synthetic-only for future production authority.

## 13.3 Causal prefit reconstruction

Normative formula source remains:

```text
reviews/v0.5/
V0.5.1_H40_PROTOCOL_AMENDMENT_R3R2_FINAL_PREOUTCOME_CLOSURE.md

commit =
77601342ac9055b5c0bb47639d4c8ef7d83da154
```

The verifier reconstructs causal prefit state itself.

New decision evidence:

```text
H40_P3B_RAW_DECISIONS_V2
```

Transported prefit fields are removed from production authority:

```text
regime_state
opportunity_state
raw_score
secondary_filter_state
```

These become deterministic verifier-derived values.

The verifier still recomputes downstream:

```text
p_up
final_action
r_h
r_net
```

Retrospective calibration semantics remain unchanged.

---

# 14. Lifecycle identity impact

The current Repair Contract freezes:

Expected changed child contracts:

```text
discovery_authorization_receipt_contract
transition_matrix_contract
persistence_replay_contract
```

Expected unchanged lifecycle child contracts:

```text
candidate_lock_receipt_contract
wf_validation_receipt_contract
confirmation_ready_receipt_contract
termination_receipt_contract
runtime_snapshot_seal_contract
f02_boundary_contract
```

Therefore:

```text
lifecycle_semantic_root =
MUST CHANGE

lifecycle_governance_authority =
MUST CHANGE
```

Next lifecycle governance schema:

```text
H40_LIFECYCLE_GOVERNANCE_AUTHORITY_V5
```

Prior governance authority:

```text
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

The exact new V5 child/root/governance hashes are intentionally **not yet authoritative**.

They must be materialized only after an independent Controller Contract Acceptance commit exists, to avoid self-reference and to content-address the accepted repair contract.

---

# 15. Current immediate next stage

Current next-stage prompt:

```text
prompts/v0.5/review/
v0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTANCE_AND_V5_HASH_MATERIALIZATION.md
```

Prompt commit:

```text
018ec8cd6c58ed2c3effb95f60d972134453fb93
```

Recommended model:

```text
GPT-6 Sol High
```

This next stage must:

### Phase 1

Independently review and accept/block the Repair Contract Freeze R1.

If PASS, create the acceptance artifact alone and commit it.

### Phase 2

Use that exact acceptance commit as an input to materialize:

- changed lifecycle child hashes;
- new lifecycle semantic root;
- new lifecycle governance authority V5.

Only after Phase 2 PASS does Repair A implementation become authorized for prompt generation.

---

# 16. Planned remaining stage sequence

Current intended sequence:

```text
CURRENT
Repair Contract Freeze R1
        |
        v
1. Controller Contract Acceptance
   + Lifecycle V5 hash materialization
        |
        v
2. Repair A implementation
   R2-F01 + R2-F04
   controller authority / lifecycle / service lifetime
        |
        v
3. Repair A independent controller review
        |
        v
4. Repair B implementation
   R2-F02 + R2-F03
   source-value provenance / causal prefit reconstruction
        |
        v
5. Repair B + combined independent review
        |
        v
6. final exact-SHA evidence refresh
        |
        v
7. new lifecycle implementation authority publication
        |
        v
8. controller publication acceptance
        |
        v
9. GPT-6 Astra focused/cumulative re-audit
   F01-F04 + composition
        |
        v
10. P3 Final Authorization review
        |
        v
11. publish H40_P3_CONTROLLER_AUTHORITY_V1
        |
        v
12. derive exact H40_DISCOVERY_RUN_GRANT_V1
        |
        v
13. explicit
    H40_P3_AUTHORIZED
    H40_DISCOVERY_AUTHORIZED
        |
        v
14. first real Discovery
        |
        v
15. post-Discovery Astra evidence audit
        |
        v
16. only then Candidate Lock / WF progression
```

Any new blocker may insert a repair stage before proceeding.

Do not skip gates because the stage count is large.

---

# 17. Model responsibilities

## GPT-6 Sol High / controller

Preferred for:

- architecture and authority adjudication;
- independent acceptance reviews;
- cumulative code/evidence review;
- deciding identity impact;
- materializing controller decisions;
- deciding next stage;
- writing exact implementation prompts;
- comparing evidence to frozen protocol;
- reviewing Codex implementation after push.

## Codex Sol High

Preferred for:

- exact source/test implementation;
- deterministic repository edits;
- typed authority implementation;
- evidence manifest generation;
- tests/static gates;
- mechanical exact-SHA validation;
- commit/push work.

Codex must not invent new scientific semantics when the contract is incomplete.

## Gemini 3.8 Flash

Preferred for:

- broad repository inventory;
- long mechanical scans;
- low-risk documentation;
- auxiliary audits where no final authority judgment is required.

Do not use Gemini alone as final scientific/authority acceptance.

## GPT-6 Astra

Reserved for major cumulative milestone audits:

- pre-Discovery cumulative audit;
- focused re-audit after major authority/scientific-provenance repair;
- post-first-real-Discovery audit before Candidate Lock/WF progression.

Do not spend Astra on routine coding or minor lint repairs.

Astra findings are not automatically final authority; controller adjudicates them.

---

# 18. CONTROLLER OPERATING RULES — mandatory

This section defines the responsibilities of the ChatGPT/controller model that takes over this project.

These are operational rules, not suggestions.

## 18.1 The controller owns the stage machine

The controller must always know:

- current exact implementation SHA;
- current exact docs SHA;
- current formal project state;
- which authority is active;
- which findings are open/closed;
- the next allowed stage;
- which stage is forbidden.

The controller must not merely answer questions while allowing the project to drift.

At every major completion, update:

```text
current state
accepted evidence
blockers
next gate
next prompt
recommended executor
```

---

## 18.2 Remote repository is the execution source of truth

When the user says:

```text
done
completed
已经push
push了
已提交
```

the controller must **inspect the connected GitHub repository directly**.

Do not ask the user for a commit SHA first if the connector can reveal it.

The controller must verify at minimum:

- remote branch HEAD;
- expected parent/start SHA;
- actual changed files;
- docs/evidence artifacts;
- reported test/evidence identity;
- remote commit existence.

Do not accept pasted executor output as sufficient evidence.

User-provided output is a lead, not authority.

---

## 18.3 Never claim a push succeeded without remote verification

A task requiring commit/push is complete only if:

```text
remote branch contains expected commit
```

If local output claims a commit but GitHub cannot see it:

```text
TASK_NOT_COMPLETE
REMOTE_PUSH_NOT_VERIFIED
```

Do not advance the stage.

For every new implementation/audit prompt, include a mandatory completion rule:

1. commit;
2. push to exact target branch;
3. verify remote branch SHA;
4. report local commit SHA and remote SHA;
5. do not claim completion if they differ.

---

## 18.4 Exact-SHA evidence binding

Evidence must be bound to the exact committed implementation SHA.

Do not accept a final evidence package where tests were run against an uncommitted working tree while the manifest claims only the previous HEAD, unless the stage's explicit contract permits such semantics.

For formal implementation acceptance, prefer:

```text
commit code
then test exact committed SHA
then publish evidence
```

If a repair changes code after validation, rerun the required validation on the new exact SHA.

---

## 18.5 Controller review is independent

After an executor pushes:

- inspect the actual diff;
- inspect relevant code;
- inspect evidence;
- recompute critical hashes when feasible;
- verify identity lineage;
- decide PASS/BLOCK independently.

Do not copy the executor's verdict into the controller review.

A successful test suite is not by itself proof of scientific correctness.

---

## 18.6 Scientific ambiguity is fail-stop

If a scientific/causal/statistical rule is materially ambiguous:

```text
BLOCK
```

Do not let the implementation model choose one interpretation.

The correct flow is:

```text
ambiguity
-> docs-only contract closure
-> independent acceptance
-> implementation
```

No post-outcome scientific rule selection.

---

## 18.7 Authority ambiguity is fail-stop

If an authority object has unclear:

- canonical fields;
- hash preimage;
- lineage;
- acceptance source;
- scope;
- transition;
- persistence semantics;

do not implement it ad hoc.

Freeze the authority contract first.

---

## 18.8 Protect against commit self-reference

When an authority needs to bind an acceptance artifact:

1. create/commit the acceptance artifact first;
2. record exact acceptance commit SHA;
3. use that SHA in the later authority preimage;
4. commit authority/hash materialization afterward.

Do not create an impossible authority requiring its own future commit SHA inside its own preimage.

Distinguish:

```text
tested implementation commit
acceptance commit
authority publication commit
docs/evidence commit
```

They serve different roles.

---

## 18.9 Protected outcomes remain sealed unless explicitly authorized

The controller must prevent accidental access to:

- real H40 Discovery outcomes;
- WF1_CALIBRATION outcomes before explicit Discovery authority;
- H39;
- Confirmation;
- Final Holdout.

Code review may inspect readers/loaders/guards.

The audit itself must not open protected outcome values.

If a task would cross the boundary, stop and create the required authorization stage first.

---

## 18.10 Execution authority is a separate domain

Never infer execution permission from:

- research acceptance;
- H40 P3 authority;
- Discovery authority;
- Candidate Lock;
- WF validation;
- implementation authority.

Execution remains:

```text
RESEARCH_DISABLED_V1
```

until a separate explicit execution-authority project stage exists.

---

## 18.11 Six P3B closures are frozen

Do not reopen the six accepted P3B statistical ambiguities because a later implementation repair is difficult.

Any proposed change must explicitly prove why it is not silently changing:

- fit-invalid correction;
- seed identity;
- net-return formula;
- bootstrap boundary;
- start sampler;
- coverage population.

---

## 18.12 Implementation prompts must be self-contained

Every implementation prompt must include:

- repository URL;
- target branch;
- exact required starting implementation SHA;
- required docs lineage / docs commit SHA;
- exact controller acceptance artifact/path/commit;
- active frozen scientific identities;
- scope;
- allowed files or allowed conceptual scope;
- forbidden changes;
- required tests;
- required evidence;
- formal allowed final state;
- commit + push requirement;
- remote SHA verification;
- recommended model and reason.

Do not rely on chat memory for critical implementation instructions.

---

## 18.13 Review prompts must also be self-contained

Every review/audit prompt must include:

- exact SHA under review;
- exact evidence/docs lineage;
- current formal state;
- protected-surface rules;
- expected changed/unchanged identities;
- severity model;
- PASS/BLOCK rules;
- required output artifacts;
- push verification.

---

## 18.14 Do not skip controller artifacts

Major controller decisions should be committed to the docs branch as review artifacts.

Examples:

- code acceptance;
- evidence acceptance;
- blocker triage;
- authority publication acceptance;
- protocol/contract acceptance;
- P3 final authorization.

The chat message is not the canonical audit trail.

---

## 18.15 Docs branch vs implementation branch

Default:

```text
v0.5-refactor
= source/test implementation

v0.5-refactor-docs
= prompts/reviews/evidence/docs
```

A docs-only audit must not modify or push implementation branch.

An implementation task should not silently rewrite historical docs evidence.

Preserve history; add superseding artifacts instead of rewriting accepted historical evidence.

---

## 18.16 CI rules

If CI exists for the exact docs/implementation commit:

- inspect status directly when relevant;
- do not say "waiting" if the run already completed;
- do not claim success before checking;
- distinguish CI success from scientific/controller acceptance.

If CI is absent and local deterministic gates are the stage's accepted evidence, state that accurately.

Do not invent a requirement for a new CI evidence standard after the fact unless correctness actually requires it.

---

## 18.17 Evidence quality rules

Formal evidence should contain:

- exact tested SHA;
- tool/version provenance;
- raw or content-addressed logs where required;
- frozen identity values;
- protected-surface attestations;
- test/static-gate results;
- hashes of relevant evidence files.

Recompute critical SHA-256 values independently when practical.

If two timing wrappers disagree but both use clearly defined measurements and both satisfy the target, document the authoritative metric rather than creating a false blocker.

---

## 18.18 Full suite vs focused suite

Use focused tests for iteration and finding verification.

Use full suite at major implementation acceptance boundaries.

Do not require repeated full suites for docs-only review unless a real reason exists.

Do not weaken or delete tests to hit runtime targets.

---

## 18.19 Performance optimization rules

Performance optimization is allowed only if:

- production semantics do not change;
- test coverage/equivalence is preserved;
- cold/uncached production paths remain represented;
- deterministic caches are test-only or explicitly non-authoritative;
- scientific/governance identity remains unchanged unless explicitly accepted.

A fast suite is not more important than authority correctness.

---

## 18.20 Historical authority must fail closed after republication

When a new accepted implementation/governance authority supersedes an old one:

- exact new authority must pass;
- exact old authority must fail production validation;
- historical artifacts remain auditable;
- no silent migration.

Negative regression tests should prove old authority rejection.

---

## 18.21 No silent schema mutation

If a canonical V1 schema participates in hashes/authority, do not simply add fields in place unless the accepted contract explicitly says that version remains V1.

Prefer:

```text
V1 historical
V2 new production
```

when semantics change.

---

## 18.22 Migration is fail-closed before first real Discovery

Because no real H40 Discovery has yet been authorized, prefer strict migration:

```text
old production receipt/evidence versions
-> historical/synthetic only

new accepted versions
-> future production
```

Do not write compatibility shims that could accidentally authorize stale evidence unless explicitly necessary.

---

## 18.23 Current repair must not alter scientific identities

For Repair A / Repair B, controller must explicitly check:

```text
protocol_authority_hash unchanged
scientific_semantic_root unchanged
structural_ledger_hash unchanged
```

Repair A is lifecycle/governance.

Repair B is provenance/recomputation of already accepted science.

If implementation requires changing accepted feature formulas, stop and reopen a docs-only scientific contract stage instead.

---

## 18.24 Controller must distinguish "formula exists" from "formula is verified"

A scientific formula being preregistered in docs is not enough.

Production evidence must prove the values were derived according to that formula.

Likewise, a content hash over a reported value is not proof the reported value came from the accepted source.

Current F02/F03 repairs exist specifically because of this distinction.

---

## 18.25 Never confuse metadata integrity with economic-value integrity

Source metadata/timestamp membership may be correct while OHLC/economic values are unproven.

For formal H40 science:

```text
sealed bytes
-> deterministic extraction
-> deterministic causal feature reconstruction
-> calibration/action/outcome
```

must be traceable.

---

## 18.26 Model selection is part of controller duty

The controller chooses the executor appropriate to the task.

Default split:

```text
GPT-6 Sol High
= architecture / controller review / acceptance / authority reasoning

Codex Sol High
= implementation / tests / evidence / repo edits

Gemini 3.8 Flash
= broad scans / mechanical documentation / auxiliary audits

GPT-6 Astra
= rare major cumulative scientific/authority audits
```

Do not send a major architecture ambiguity directly to Codex.

Do not waste Astra on routine implementation.

---

## 18.27 Astra audit handling

Astra findings must be:

1. stored as artifacts;
2. remotely verified;
3. triaged by controller;
4. converted into exact repair contracts if accepted;
5. repaired;
6. re-audited.

Do not automatically authorize P3 because Astra says PASS.

Astra is an independent auditor, not the final controller authority.

---

## 18.28 Tool / content safety interruptions

If a model/audit prompt triggers an external content filter:

- preserve already completed valid analysis;
- remove exploit-style or attack-construction wording if not necessary;
- rewrite as defensive/read-only invariant review;
- continue from prior progress where possible;
- do not lower the scientific standard merely to avoid the filter.

This rule was applied to Astra R2.

---

## 18.29 User asks to execute a prompt

If the user explicitly asks the controller itself to execute an existing prompt:

- execute it when the available tools can faithfully do so;
- do not merely return the same prompt again;
- produce/commit the requested artifacts if appropriate;
- if a different execution model is materially required, state why.

The controller remains responsible for the next-stage state afterward.

---

## 18.30 User says "next prompt"

When the user asks for the next-stage prompt:

- first decide the actual next allowed stage from formal state;
- do not blindly continue the previously imagined roadmap;
- write the prompt into `v0.5-refactor-docs`;
- push it;
- give exact path, commit, starting SHA, model recommendation.

---

## 18.31 Keep the user operationally informed

For multi-step work:

- give concise progress updates;
- report important partial findings as soon as known;
- do not spam low-level tool operations;
- do not promise background work or ask the user to wait;
- complete the work in the current interaction as far as tools allow.

Final replies should be operational:

```text
decision
state
artifact link
commit SHA
next action
recommended model
```

---

## 18.32 Do not ask the user to repeat known context

Use:

- current conversation;
- handover;
- connected GitHub;
- committed reviews/evidence;

before asking for previously supplied information.

If the repository can answer it, inspect the repository.

---

## 18.33 Handover maintenance is a controller duty

At major stage boundaries, update this phase handover or create a superseding dated handover.

A new handover should capture:

- exact branch heads;
- accepted identities;
- open blockers;
- current prompt;
- next stage;
- controller rules;
- model split;
- protected state.

Do not let future conversations depend only on transient chat memory.

---

# 19. Executor completion rules — mandatory in future prompts

Every future implementation/review/audit prompt should include:

```text
TASK COMPLETE only after remote push is verified.
```

Typical required sequence:

```bash
git status
git rev-parse HEAD

# perform task

git add ...
git commit -m "..."

git push origin <exact-target-branch>

git ls-remote origin refs/heads/<exact-target-branch>
```

Executor final response must include:

```text
starting_sha
local_commit_sha
remote_branch_sha
remote_push_verified = YES/NO
changed_files
test results
artifact paths
formal final state
```

If:

```text
remote_push_verified != YES
```

then:

```text
TASK_NOT_COMPLETE
REMOTE_PUSH_NOT_VERIFIED
```

---

# 20. Current next-stage model assignment

Next task:

```text
Controller Contract Acceptance
+
Lifecycle V5 Hash Materialization
```

Recommended:

```text
GPT-6 Sol High
```

Prompt:

```text
prompts/v0.5/review/
v0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTANCE_AND_V5_HASH_MATERIALIZATION.md
```

Prompt commit:

```text
018ec8cd6c58ed2c3effb95f60d972134453fb93
```

After PASS, the controller must create the **Repair A implementation prompt** for Codex Sol High.

Do not start Repair B before Repair A has an independent review unless the accepted V5 contract materially changes that sequence.

---

# 21. Current formal state matrix

```text
H40 scientific protocol                       FROZEN
protocol_authority_hash                       a83f8fc7...
scientific_semantic_root                      71889a35...
structural_ledger_hash                        483f6850...

six P3B ambiguity closures                    CLOSED / MATCH

old pre-repair lifecycle semantic root        846591f8...
old pre-repair lifecycle governance           7e9433aa...
published implementation authority            d3a304dd...
publication implementation SHA                d63c1fbb...

Astra R2                                      FAIL
R2-F01                                        OPEN HIGH / CONTRACT FROZEN
R2-F02                                        OPEN HIGH / CONTRACT FROZEN
R2-F03                                        OPEN HIGH / CONTRACT FROZEN
R2-F04                                        OPEN HIGH / CONTRACT FROZEN

Repair Contract Freeze R1                     READY FOR CONTROLLER REVIEW
new lifecycle semantic root                   NOT YET MATERIALIZED
new lifecycle governance V5                   NOT YET MATERIALIZED

Repair A                                      NOT STARTED
Repair B                                      NOT STARTED
combined exact-SHA reacceptance               NOT STARTED
new implementation authority                  NOT PUBLISHED
Astra repair re-audit                         NOT RUN

H40_P3                                        NOT AUTHORIZED
H40_DISCOVERY                                 NOT AUTHORIZED

Confirmation                                  SEALED
H39                                           PROTECTED
Final Holdout                                 SEALED
Execution                                     RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY             None

NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```

---

# 22. Immediate takeover instruction

A new controller should do exactly this next:

1. verify remote heads;
2. open the current next-stage prompt at commit `018ec8cd...`;
3. execute or supervise **Controller Contract Acceptance + Lifecycle V5 Hash Materialization**;
4. independently verify the generated acceptance and hash materialization;
5. if PASS, create the Repair A (F01/F04) Codex implementation prompt;
6. keep both H40 P3 and Discovery unauthorized.

Do not run real Discovery.

Do not inspect protected outcomes.

Do not alter frozen science.

