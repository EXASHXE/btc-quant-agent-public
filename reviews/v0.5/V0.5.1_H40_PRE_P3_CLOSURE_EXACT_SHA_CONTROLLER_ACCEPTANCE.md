# V0.5.1 H40 Pre-P3 Closure — Exact-SHA Controller Acceptance

## Decision

```text
PRE_P3_CLOSURE_EXACT_SHA_CONTROLLER_ACCEPTED

ACCEPTED_IMPLEMENTATION_SHA =
f3678676265ba53e1d874e27bc0c922c7783acaa

ACCEPTED_EVIDENCE_COMMIT =
a781d8c756913e948b32225334d2cb27e1bcd6fa

ACCEPTED_EVIDENCE_MANIFEST =
evidence/v0.5/h40/V0.5.1_H40_PRE_P3_CLOSURE_EXACT_SHA_EVIDENCE_f3678676_R1.json

ACCEPTED_EVIDENCE_MANIFEST_SHA256 =
609632e06c280adfa20ae5db5b0b3d0736792252ac07de93b1bf1e368ea210be

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1
```

## Scope

This acceptance closes the evidence blocker recorded in:

```text
reviews/v0.5/V0.5.1_H40_PRE_P3_CLOSURE_CONTROLLER_EVIDENCE_BLOCKER.md
blocker commit =
33058e9368a82fdc52fd88e6ea407a7c3aaaa596
```

and accepts the reconciled exact-SHA evidence committed at:

```text
a781d8c756913e948b32225334d2cb27e1bcd6fa
```

The rejected evidence commit:

```text
0920ab1b21a94c3e10e4028212335896b2fe5fcf
```

remains historical and must not be used as implementation-authority evidence.

## Independent Controller verification

### Remote identity

```text
v0.5-refactor HEAD =
f3678676265ba53e1d874e27bc0c922c7783acaa

v0.5-refactor-docs HEAD at evidence review =
a781d8c756913e948b32225334d2cb27e1bcd6fa
```

Implementation lineage:

```text
base =
c31586c80130693c118c150a4e06c9573ff0053d

head =
f3678676265ba53e1d874e27bc0c922c7783acaa

ahead_by = 1
behind_by = 0
```

No additional implementation commit exists between the reviewed publication baseline and the
accepted repaired implementation.

### MED-01 closure

Production Parquet extraction changed from reopening the source path:

```python
pq.read_table(file_path)
```

to consuming the already captured and whole-file-hashed source bytes:

```python
pq.read_table(pa.BufferReader(raw_bytes))
```

This conforms to the frozen R2-F02 invariant that the verifier re-extract economic values from the
same sealed source bytes whose source-file SHA-256 is authoritative.

Apache Arrow documentation confirms that `BufferReader` is an in-memory reader over Arrow buffers;
the source-selection layer may still use path metadata, but economic parsing is no longer dependent
on reopening the mutable filesystem path.

### MED-02 closure

Controller-ratified:

```text
H40_WF1_LOOKBACK_REFERENCE_INTERPRETATION_A
```

is locked by regression without a production scientific-semantic change:

```text
720h reserve =
non-evaluating source-history support

allowed =
causal contribution to initial WF1 expanding percentile reference

forbidden =
base/evaluation/decision/calibration/validation/candidate-ranking membership
```

No frozen scientific identity changed.

## Ruff reconciliation

Pinned tool:

```text
Ruff 0.16.6
```

Exact config identity in both clean worktrees:

```text
pyproject.toml SHA-256 =
78c40cae6799d61e95ccf91b3c27df94ee7c1925a370f827d8e32a731245fb70
```

Replayed result:

```text
c31586c8...:
10 diagnostics
exit_code = 1

f3678676...:
0 diagnostics
exit_code = 0
```

The 10 historical diagnostics were independently shown to reside in exactly four files modified by
`f3678676...`:

```text
tests/test_v051_h40_f01r3r1_durable_authority.py
tests/test_v051_h40_f01r3r2_commit_authority.py
tests/test_v051_h40_f01r3r3_api_seal.py
tests/test_v051_h40_repair_b_provenance_pit.py
```

The earlier mixed-model deep-audit narrative incorrectly attributed those diagnostics to other
legacy test files. That attribution error is superseded by the exact pinned-Ruff replay above.

Ruff CLI semantics were cross-checked against current Astral documentation: `ruff --version` /
`ruff version` are supported version-reporting surfaces and `ruff check` is the lint entrypoint;
diagnostics produce a non-zero status unless special fix-only behavior is requested.

## Evidence reconciliation

The Controller independently recomputed:

```text
manifest SHA-256 =
609632e06c280adfa20ae5db5b0b3d0736792252ac07de93b1bf1e368ea210be
```

from the exact committed UTF-8 manifest content.

All 13 referenced raw-log SHA-256 values were recomputed from the exact committed log content:

```text
13 / 13 exact match
mismatches = 0
```

The corrected evidence package establishes:

```text
import_provenance = PASS
authority_stage_state = PASS
raw_log_status_consistency = PASS

focused tests =
408 passed / 0 failed

full suite run 1 =
1698 passed / 0 failed / 9 warnings

full suite run 2 =
1698 passed / 0 failed / 9 warnings

compileall = PASS
ruff = PASS
mypy = PASS
git diff --check = PASS
```

Two identity-replay logs are separate executions with distinct execution timestamps and distinct raw
file hashes, while reproducing the same accepted identity values.

## Frozen identity replay

Accepted exact identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

discovery_provenance_contract_hash =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

lifecycle_semantic_root_v5 =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle_governance_authority_v5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf

Lifecycle V5 children =
9 / 9 exact
```

## Authority state

At the accepted implementation SHA:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

Therefore acceptance of the implementation does not itself enable P3 or Discovery.

## Protected surfaces

Accepted evidence records:

```text
real_h40_outcomes_accessed = false
wf1_calibration_outcomes_accessed_for_authoring_or_selection = false
confirmation_accessed = false
h39_accessed = false
final_holdout_accessed = false
execution_credentials_or_order_interfaces_accessed = false
signed_network_requests = 0
```

## GitHub Actions qualification

No GitHub Actions workflow runs or combined commit statuses were exposed for:

```text
f3678676265ba53e1d874e27bc0c922c7783acaa
```

Therefore this acceptance is explicitly based on the content-addressed local exact-SHA evidence
package above. It does **not** claim that remote CI was observed.

## Controller conclusion

```text
PRE_P3_CLOSURE_EXACT_SHA_CONTROLLER_ACCEPTED

MED_01 = CLOSED
MED_02 = RATIFIED_AND_REGRESSION_LOCKED
RUFF_RECONCILIATION = CLOSED
EVIDENCE_RECONCILIATION = CLOSED

IMPLEMENTATION_AUTHORITY_REPUBLICATION_AUTHORIZED_AS_NEXT_NARROW_STAGE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1

NEXT_STAGE =
MATERIALIZE_AND_PUBLISH_REPAIRED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_FOR_f3678676
```
