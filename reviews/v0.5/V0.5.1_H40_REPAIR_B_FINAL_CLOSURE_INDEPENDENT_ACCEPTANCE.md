# V0.5.1 H40 Repair B Final Closure — Independent Acceptance

## Decision

```text
REPAIR_B_F02_F03_INDEPENDENTLY_ACCEPTED

F02_SEALED_SOURCE_PROVENANCE_IMPLEMENTED = YES
F03_CAUSAL_PREFIT_RECONSTRUCTION_IMPLEMENTED = YES

B01_SOURCE_SUPPORT_PROJECTION = CLOSED
B02_D2_OPEN_LEFT_BOUNDARY = CLOSED
B03_EXACT_REGIME_STATE_IDENTITY = CLOSED
B04_O_RANGE_STANDARD_TRUE_RANGE = CLOSED

TRANSPORTED_OHLC_PRODUCTION_AUTHORITY = NO
TRANSPORTED_PREFIT_PRODUCTION_AUTHORITY = NO
STRICT_CLOSE_TIME_LT_DECISION_T = YES

REPAIR_A_PRESERVED = YES
LIFECYCLE_V5_PRESERVED = YES
SCIENTIFIC_FROZEN_RULES_CHANGED = NO

ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1

NEXT_STAGE =
AB_CUMULATIVE_EXACT_SHA_VALIDATION_AND_IMPLEMENTATION_AUTHORITY_REPUBLICATION
```

## Exact reviewed implementation lineage

Repair A independently accepted implementation:

```text
de7a2c0675a68a610cff84253dba553173527a8f
```

Initial Repair B:

```text
0577ad2164903e1bfa472ef9a4b2dd8e842dbc54
```

Repair B R1:

```text
2b484f3ad582ee1d0173a902876e5556040ec294
```

Final Repair B closure / exact reviewed remote implementation HEAD:

```text
7413c96301067c59b9663d8eeaeddf975facb976
```

The final closure is exactly one implementation commit ahead of the R1 baseline.

Changed files in the final closure:

```text
src/btc_quant_agent/h40/discovery_evidence.py
tests/test_v051_h40_repair_b_provenance_pit.py
```

No Lifecycle V5 authority source was changed by Repair B final closure.

## Governing accepted authority

Repair Contract Freeze R2:

```text
cf7b1c9b9547f3e9e6dfab964d7d2c6d1bb35de3
```

Controller Acceptance R2:

```text
2229d44c5cc9b88ba515d0b2931d20f05936c7e8
```

Lifecycle V5 materialization:

```text
e1f368e85bbf1831763c688ca75ef4249971cf59
```

Repair A independent acceptance:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_A_F01_F04_INDEPENDENT_ACCEPTANCE.md
commit =
890d1bc24da637295bc382be3de8e130227d6cfd
```

Normative R3R2 science:

```text
reviews/v0.5/V0.5.1_H40_PROTOCOL_AMENDMENT_R3R2_FINAL_PREOUTCOME_CLOSURE.md

accepted authority-chain identifier =
77601342ac9055b5c0bb47639d4c8ef7d83da154
```

## Frozen identities preserved

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

discovery_provenance_contract =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

lifecycle semantic root V5 =
36cbda530352cffaa475bd835b3b629df47351ca284e09bdf31a8fc884d48b4b

lifecycle governance authority V5 =
bb367f2af726be105bddf42636c97652402014c8a671d43ab81b1964c258e5cf
```

Independent source comparison confirms the complete
`src/btc_quant_agent/h40/lifecycle_authority.py` at final Repair B SHA is byte-identical to the
independently accepted Repair A implementation at:

```text
de7a2c0675a68a610cff84253dba553173527a8f
```

Therefore the accepted Lifecycle V5 contract material and the Repair A F01/F04 implementation were
not altered by Repair B.

Committed production constants remain:

```python
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH: str | None = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH: str | None = None
```

## F02 acceptance — source economic authority

The final implementation uses production V2 source evidence:

```text
H40_P3B_SOURCE_DERIVATION_V2
```

with the exact R2 top-level and active-source entry shapes.

Production authority is reconstructed from the sealed runtime chain and exact local source bytes.

The implementation verifies, where production authority is present:

```text
active required source set
source record hash
source validation receipt hash
source derivation receipt hash
source file SHA-256
locator
product
timestamp field
source manifest
split manifest
split attestation
```

The full source file SHA-256 binds the complete accepted source artifact.

H40 economic rows are independently re-extracted only from the frozen support projection:

```text
[2021-01-01T00:00:00Z, 2023-02-01T00:00:00Z)
```

Valid physical source rows outside that interval do not enter the H40 economic digest/count and do
not invalidate the whole sealed file merely because they are outside First-Discovery support.

The lookback reserve:

```text
[2021-01-01T00:00:00Z, 2021-01-31T00:00:00Z)
```

may support causal feature construction but is prohibited from base decision membership.

Production economic digest/count are recomputed from exact source bytes; transported OHLC is not
production authority.

Unsupported active-source adapter behavior remains fail-closed / NOT_TESTABLE.

## F03 acceptance — V2 decisions and causal prefit

Production decision evidence is:

```text
H40_P3B_RAW_DECISIONS_V2
```

with exact row fields:

```text
timestamp
product
p_up
final_action
r_h
r_net
```

The following transported fields are explicitly rejected:

```text
regime_state
opportunity_state
raw_score
secondary_filter_state
```

The verifier reconstructs those prefit values from source-derived bars and accepted structural
configuration.

Strict prefit availability remains:

```text
close_time < decision_t
```

and the regression suite includes future/equal-time contamination checks.

### D1

The implementation retains exact endpoint semantics:

```text
last closed bar before t
versus
bar opened at t-W
```

for both 4h and 12h contracts.

### D2

The prior R1 defect is closed. The reference window is open-left and excludes the last signal bar:

```text
open_time > t-W
open_time < t
close_time < t
signal bar excluded
```

Both 24h and 72h variants have boundary sentinel tests.

### D3

The implementation uses:

```text
b1 = break bar
b2 = immediately following close-back bar
prior range strictly before b1
strict re-entry
```

and includes 24h/72h boundary tests.

### R_VOL

The implementation preserves the explicit R3R2 count/formula:

```text
24 closes
23 adjacent log returns
sample standard deviation
ddof = 1
denominator = 22
```

### Exact regime identity

The previous `REGIME_REJECT` collapse is removed for valid percentile states.

Valid reconstructed state is now exactly:

```text
p < 0.40         -> REGIME_VOL_LOW
0.40 <= p < .60  -> REGIME_VOL_MID
p >= .60         -> REGIME_VOL_HIGH
```

Only MID remains eligible.

Undefined regime computation is kept separate as `REGIME_UNAVAILABLE`, rather than being
mistaken for a valid LOW/HIGH state.

### O_RANGE

The previous H-L fallback is removed.

The immediately preceding close required to compute the earliest reference bar's standard True
Range is now mandatory and PIT-valid.

If the preceding close is absent or invalid:

```text
O_RANGE -> undefined / fail closed
```

No alternate formula is substituted.

The implementation preserves:

```text
24 reference TR values
1 last-bar TR numerator
standard TR definition
last bar excluded from reference mean
zero denominator -> undefined
```

### Opportunity identity

The implementation preserves the exact accepted state boundaries:

```text
O_NONE      [0.00, 0.60)
O_WATCH     [0.60, 0.80)
O_ELIGIBLE  [0.80, 1.00]
```

Only O_ELIGIBLE proceeds.

### Unsupported direction/filter hardening

The final closure removes the prior implicit unknown-direction -> D1 fallback.

An unsupported direction contract now fails closed as NOT_TESTABLE.

The current production ledger regression confirms that the 18 REGISTERED slots are ETH-only and
use only explicitly supported D1/D2/D3 direction contracts.

Unsupported secondary-filter contracts likewise fail closed; the current single-family D1/D2/D3
registered surface remains PASS for the no-secondary-filter case.

## Cache and outcome reconstruction

The prefit cache remains derivative-only and is keyed by protocol/provenance/source/candidate/
partition/timestamp/product identity.

Source-evidence identity and structural candidate identity changes therefore do not reuse the prior
cached prefit value.

The verifier independently recomputes:

```text
r_h
r_net
```

from source-derived prices and checks transported values against those calculations.

## Repair A preservation

Independent comparison confirms Repair B did not alter the accepted Lifecycle V5/F01/F04
implementation.

The Repair A synthetic-domain isolation remains part of the preservation regression matrix.

The final Repair B suite also retains regressions for:

```text
V2 Discovery receipt invalid in production
V1 persistence context invalid in production
stale/unaccepted service authority fails closed
protected surfaces remain sealed
execution remains RESEARCH_DISABLED_V1
```

## Test/CI evidence limitation

No GitHub workflow run or combined commit status is attached to:

```text
7413c96301067c59b9663d8eeaeddf975facb976
```

This acceptance is therefore based on independent exact remote diff review, accepted contract
comparison, authority-preservation checks, and adversarial test-content review.

It does **not** claim independent remote CI execution.

The immediately following cumulative exact-SHA stage is mandatory and must produce a
content-addressed test/CI evidence manifest whose:

```text
tested_commit_sha =
7413c96301067c59b9663d8eeaeddf975facb976
```

before repaired lifecycle implementation authority can be republished.

## Protected state

```text
SCIENTIFIC_FROZEN_RULES_CHANGED = NO
LIFECYCLE_V5_HASHES_CHANGED = NO

ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED

EXECUTION = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

## Formal end state

```text
REPAIR_B_F02_F03_INDEPENDENTLY_ACCEPTED

REPAIR_B_IMPLEMENTATION_SHA =
7413c96301067c59b9663d8eeaeddf975facb976

REPAIR_A_PLUS_B_IMPLEMENTATION_COMPLETE = YES
EXACT_SHA_TEST_EVIDENCE_NOT_YET_MATERIALIZED = YES
IMPLEMENTATION_AUTHORITY_NOT_YET_REPUBLISHED = YES

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

NEXT_STAGE =
AB_CUMULATIVE_EXACT_SHA_VALIDATION_AND_IMPLEMENTATION_AUTHORITY_REPUBLICATION
```
