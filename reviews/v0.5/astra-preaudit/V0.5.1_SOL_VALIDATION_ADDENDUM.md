# V0.5.1 Astra Pre-Audit Package — Sol Validation Addendum

## Status

~~~text
DOCUMENT_TYPE = INDEPENDENT_FACT_VALIDATION_ADDENDUM
TARGET_PACKAGE_COMMIT = 526742bfb55be68382fb35e7f480186efca6b6a6
PACKAGE_DISPOSITION = USE_WITH_CORRECTIONS
NOT_AN_AUTHORITY_REPLACEMENT = TRUE
REMOTE_CI = DEFERRED
~~~

This addendum independently validates the Gemini pre-audit package against current accepted H40 authority, current code, and the current controller review state.

The Gemini package is materially useful and should be retained. It provides strong repository/data/runtime inventory coverage. However, several claims are stale or materially inaccurate. Astra must read this addendum before treating the package as a factual launch surface.

---

## 1. Current F01 status — CORRECTION REQUIRED

The package repeatedly states:

~~~text
F01 implemented on d3dae91
pending independent review
~~~

That is stale.

Independent review already exists:

~~~text
reviews/v0.5/V0.5.1_H40_F01_SOL_INDEPENDENT_IMPLEMENTATION_REVIEW.md

review commit:
a6a24af6bbe2794cdb86f67408c548ae47096742
~~~

Current verdict:

~~~text
H40_FSA_F01_IMPLEMENTATION_NOT_ACCEPTED
H40_F01R1_REPAIR_REQUIRED

H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_ACCEPTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
~~~

The implementation commit under review remains:

~~~text
d3dae91f0351879364ca41ff88ddb3730630cab1
~~~

The accepted repair prompt is:

~~~text
prompts/v0.5/implementation/
v0.5.1_H40_F01R1_LIFECYCLE_AUTHORITY_PRODUCTION_SEAL_AND_RESTORE_REPAIR.md

prompt commit:
bfbf2f90168b335f417b4e982644f5b55845a64c
~~~

Astra must not treat d3dae91 as accepted F01 implementation authority.

---

## 2. F01 blockers absent from Gemini risk summary — ADD

The current independent F01 review found production-authority defects that should be treated as first-class Astra inputs:

~~~text
F01-SOL-01 BLOCKER
production runtime seal accepts caller-owned source/split/attestation hashes
instead of deriving them from verified typed source/split authority.

F01-SOL-02 BLOCKER
production roster verification checks supplied membership but does not prove
the complete accepted REGISTERED roster or recompute the runtime snapshot identity.

F01-SOL-03 HIGH
persistence "restore" requires an existing in-memory expected_authorization;
it is not a true cold process restore/replay.

F01-SOL-04 HIGH
the lifecycle core has no clean typed seam for a future accepted production
scientific evidence verifier; production currently forces verifier=None.

F01-SOL-05 MEDIUM
the synthetic test called "exact R3R4 selection order" verifies only final
ordering mechanics, not the full within-family max-stat + cross-family Holm science.
~~~

These findings are more current and more authority-relevant than the package's generic F01 testing-risk statements.

---

## 3. Discovery partition semantics — MATERIAL CORRECTION

The package's lifecycle description says Discovery evaluates on the WF1 TRAIN partition.

That is incorrect.

Accepted R3R4 science states:

~~~text
WF1 TRAINING:
fit feature/gate distributions, geometry, and training-only null inputs.

WF1 CALIBRATION:
fit the calibrator and provide the ONLY discovery outcome partition used for
candidate-ranking precision / net-expectancy statistics.

WF1 VALIDATION and later partitions:
inaccessible before candidate lock.
~~~

P3R0R1 also freezes:

~~~text
discovery_partition = WF1_CALIBRATION
~~~

Therefore Astra must use:

~~~text
TRAIN != candidate-ranking outcome surface
CALIBRATION == Discovery ranking outcome surface
VALIDATION == inaccessible until candidate lock
~~~

---

## 4. F02 boundary — MATERIAL CORRECTION

The Gemini lifecycle diagram labels:

~~~text
H40_WALK_FORWARD_VALIDATED
-> H40_CONFIRMATION_READY
as requiring "F02 Protocol Approved"
~~~

That is wrong.

Accepted lifecycle semantics are:

~~~text
F01:
H40_WALK_FORWARD_VALIDATED
-> H40_CONFIRMATION_READY

The resulting state is a sealed waiting state.
f02_blocker_status = OPEN_SEALED
~~~

F02 governs the later boundary:

~~~text
H40_CONFIRMATION_READY
-> H40_CONFIRMATION_EVALUATED_ONCE
~~~

F02 must remain unimplemented and sealed during F01/F01R1 and P3/WF work.

---

## 5. H39 historical status — MATERIAL CORRECTION

The package describes H39 as effectively:

~~~text
SEALED / COMPLETED
~~~

That overstates its historical state.

The accepted project handover records:

~~~text
FORWARD_DATA_INSUFFICIENT
REFUSED_NOT_MATURE
~~~

Correct interpretation:

~~~text
H39 = independent protected forward-validation experiment
H39 = frozen/sealed
H39 != completed successful validation
H39 != available evidence for H40
~~~

Its protected outcomes must remain isolated.

---

## 6. Historical hypothesis numbering — DO NOT TRUST WITHOUT PRIMARY ARTIFACT

The Gemini history maps several claims to H identifiers such as:

~~~text
H9  = opportunity magnitude
H18 = funding crowding
H26 = cross-asset
...
~~~

This conflicts with the current accepted handover history, which records:

~~~text
v0.3.6:
H14-H17 falsified
H18 Opportunity Layer supported
TP/BR predictive of movement magnitude, not reliable direction
~~~

Therefore:

~~~text
historical concept summaries may be useful
historical H-number labels in the Gemini package are NOT authority
~~~

Before Astra relies on a specific H-number, it must anchor the claim to the original versioned protocol/review artifact or exact historical commit.

Do not infer scientific lineage from the Gemini H-number table alone.

---

## 7. H40 scope — CORRECTION

The Gemini campaign table describes H40 as testing a directional edge "on BTC".

Frozen H40 scope is:

~~~text
BTCUSDT
ETHUSDT

decision cadence = 1h

primary horizons:
4h / 8h / 12h

diagnostic:
24h

72h:
feature/lookback only
~~~

The search space includes BTC-only, ETH-only and pooled/cross-asset structures subject to source authority.

Astra should not reduce the campaign to BTC-only.

---

## 8. Local data existence != H40 production source authority — MATERIAL CORRECTION

The local-data report labels some locally present datasets as accepted P1 sources.

This conflates:

~~~text
data exists locally
~~~

with:

~~~text
data is production-authoritative for the current H40 run
~~~

Current accepted P1 runtime source authority in code is:

~~~text
BTCUSDT_USD_M_1H                 NOT_TESTABLE
ETHUSDT_USD_M_1H                 VERIFIED
BTCUSDT_OFFICIAL_DERIVATIVES     NOT_TESTABLE
BTCUSDT_SPOT_FLOW                NOT_TESTABLE
ETHUSDT_DERIVATIVES_FLOW         NOT_TESTABLE
~~~

This projection is encoded in:

~~~text
src/btc_quant_agent/h40/protocol_authority.py
P1_SOURCE_AUTHORITY_STATES
~~~

Consequently the current frozen runtime projection is approximately:

~~~text
18 REGISTERED
150 NOT_TESTABLE
~~~

The existence of:

~~~text
BTC 1m Parquet
official derivatives archives
spot-flow archives
forward microstructure
~~~

does not automatically promote those sources into the H40 production authority snapshot.

This distinction is central to the current F01-SOL-01/F01-SOL-02 blockers.

---

## 9. Protected-surface guard description — CORRECTION

The package refers to a symbol:

~~~text
FORBIDDEN_PATH_PATTERNS
~~~

Current code actually uses:

~~~text
H40ProtectedSurfaceGuard.FORBIDDEN_LOCATORS
H40ProtectedSurfaceGuard.FORBIDDEN_SOURCE_IDS
~~~

Current path guard already checks:

~~~text
raw locator
os.path.normpath(...)
Path.resolve(...)
~~~

Therefore the package's claim that path normalization is absent is incorrect.

Also, confirmation protection is not implemented by a "confirmation_holdout" path substring in H40ProtectedSurfaceGuard.

Current architecture separates:

~~~text
H39 / Final Holdout path/source protection
-> H40ProtectedSurfaceGuard

Confirmation outcome protection
-> H40ConfirmationGuard
-> lifecycle/F02 authority
~~~

The guard raises H40GuardError; it does not inherently "terminate the process" as the package states.

Astra should still audit capability-based hardening, but from the correct implementation facts.

---

## 10. Cost-model wording — QUALIFICATION REQUIRED

The package often states that the economic kernel itself has an invariant default of:

~~~text
5 bps taker fee + 2 bps slippage per side
~~~

This is too broad.

Current facts include:

~~~text
configs/default.toml:
taker_fee_rate = 0.0005
slippage_bps_per_side = 2.0

economic/FeeModel default:
taker_fee_rate = 0.0005
fixed_slippage_bps = 1.0
~~~

Historical research protocols also freeze their own cost assumptions.

Therefore Astra must distinguish:

~~~text
legacy/runtime RiskConfig cost assumptions
formal economic FeeModel defaults
campaign-specific frozen research costs
H40-specific cost proxy contracts
~~~

Do not treat "5 + 2 bps" as an intrinsic universal constant of every economic-kernel path.

---

## 11. Forward-health maintenance branch — NOT AN H40 SCIENTIFIC PREREQUISITE BY DEFAULT

The Gemini package lists the lightweight Forward health merge as required "Before Discovery".

That is stronger than current accepted authority.

Current status is:

~~~text
forward-evidence-health-lightweight
743c936e6fcb93346435c99bc00abd0b4f69c969

IMPLEMENTED_ON_SEPARATE_BRANCH
REAL_RUNTIME_ACCEPTANCE_PENDING
NOT_MERGED_TO_v0.5-refactor
~~~

It is an important operational maintenance stream.

But unless a future accepted gate explicitly binds it, it must not be silently promoted into an H40 scientific Discovery prerequisite.

Treat separately:

~~~text
H40 scientific/lifecycle authority
!=
Forward operational-health maintenance acceptance
~~~

Astra may recommend integration, but should not rewrite the current authority graph around it without explicit protocol change.

---

## 12. Repository metrics — USE AS SNAPSHOT, NOT AUTHORITY

The package mixes:

~~~text
local baseline cfefdba
remote F01 d3dae91
docs branch state
local filesystem/runtime state
~~~

This is legitimate for inventory, but figures such as:

~~~text
module count
LOC
tracked-file count
H40 module count
economic module count
~~~

must always be attached to the exact branch/SHA they were measured from.

For example, remote d3dae91 includes lifecycle_authority.py and therefore has a different H40 module/LOC surface than cfefdba.

Do not compare cross-SHA counts as if they were one repository state.

---

## 13. What remains high-value in the Gemini package

The following portions are useful Astra inputs and should be retained:

~~~text
- top-level repository census
- module dependency inventory
- CLI/config/test catalog
- local filesystem data-size inventory
- representative SQLite/Parquet schema inspection
- runtime/systemd/timer inventory
- Forward health resource-history reconstruction
- code hotspot map
- contradiction register concept
- v0.6 validated-playbook / policy fit analysis
- separation of data / feature / alpha / policy / risk / execution layers
- explicit protected-surface non-access
~~~

The machine-readable inventories are particularly useful as navigation indices, not as authority.

---

## 14. Revised Astra reading order

Astra should read in this order:

~~~text
0. THIS ADDENDUM
   V0.5.1_SOL_VALIDATION_ADDENDUM.md

1. Current F01 independent review
   ../V0.5.1_H40_F01_SOL_INDEPENDENT_IMPLEMENTATION_REVIEW.md

2. Current F01R1 repair prompt
   ../../prompts/v0.5/implementation/
   v0.5.1_H40_F01R1_LIFECYCLE_AUTHORITY_PRODUCTION_SEAL_AND_RESTORE_REPAIR.md

3. Gemini Astra Full Audit Briefing
   V0.5.1_ASTRA_FULL_AUDIT_BRIEFING.md

4. Research History / Authority map
   with corrections in this addendum applied

5. Repository Architecture / Code Map

6. Local Data / Lineage Inventory
   while strictly separating local existence from authority

7. Runtime / Forward Operations Map

8. Tech Debt / Risk Register

9. Validated Playbook / Trading Policy Architecture Fit

10. Current accepted roadmap:
    docs/roadmap/V0.6_VALIDATED_PLAYBOOK_TRADING_POLICY_DIRECTION.md
~~~

---

## 15. Package disposition

~~~text
GEMINI_PREAUDIT_PACKAGE = USEFUL
REPOSITORY_CENSUS = ACCEPTABLE_AS_NAVIGATION_INPUT
LOCAL_DATA_INVENTORY = HIGH_VALUE_WITH_AUTHORITY_CORRECTIONS
ARCHITECTURE_MAP = HIGH_VALUE
HISTORICAL_HYPOTHESIS_NUMBERING = UNTRUSTED_UNTIL_PRIMARY_ARTIFACT_CHECK
H40_LIFECYCLE_DESCRIPTION = REQUIRES_THIS_ADDENDUM
CURRENT_F01_STATUS = SUPERSEDED_BY_SOL_REVIEW
ASTRA_INPUT_READINESS = READY_AFTER_APPLYING_ADDENDUM
~~~

The package should not be regenerated solely because of these issues. Preserve it as the Gemini fact-gathering snapshot and layer current accepted authority/review facts above it.

---

## 16. Current controller state

~~~text
H40_P3R0_CUMULATIVE_ACCEPTED

H40_FSA_F01_PROTOCOL_SEMANTICS_CLOSED
H40_FSA_F01_IMPLEMENTATION_NOT_ACCEPTED
H40_F01R1_REPAIR_REQUIRED

H40_LIFECYCLE_GOVERNANCE_AUTHORITY_ACCEPTED
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_ACCEPTED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

GEMINI_PREAUDIT_COMPLETE
ASTRA_PRE_DISCOVERY_AUDIT_INPUT_PACKAGE_AVAILABLE
ASTRA_FORMAL_PRE_DISCOVERY_GATE_SHOULD_WAIT_FOR_F01R1_REVIEW

H40_FSA_F02_OPEN_SEALED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED

REMOTE_CI_DEFERRED_ACTIONS_QUOTA
NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
~~~
