# V0.5.1 H40 Pytest Runtime Optimization R2 — Controller Review

## Reviewed implementation

```text
starting_sha = 341a19287a90405aab6a196d68b17720ae1cfff3
optimized_sha = d73e8984446f933059b978e36c4cabd55de517aa
docs_report_commit = f7494f3899232f174459bb61a9d2a379048fa205
```

## Decision

```text
H40_TEST_RUNTIME_OPTIMIZATION_R2_CODE_REVIEW_PASS
FULL_PYTEST_PERFORMANCE_TARGETS_MET

FINAL_EXACT_SHA_EVIDENCE_REFRESH_REQUIRED
BLOCKER = FINAL_BENCHMARK_LOGS_PRECEDE_COMMIT_AND_IDENTIFY_PARENT_SHA

IMPLEMENTATION_AUTHORITY_PUBLICATION_DEFERRED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## 1. Scope review

The implementation diff from `341a192...` to `d73e898...` changes exactly one file:

```text
tests/test_v051_h40_f01_lifecycle_authority.py
```

No `src/**`, protocol, scientific, governance, configuration, or accepted-authority constant changes are present.

The optimization is therefore test-only as authorized.

## 2. Cache design review

The patch caches deterministic test inputs rather than production results.

Cached state:

```text
immutable hourly calendar tuple
canonical serialized H40 configuration-ledger template
immutable accepted production roster tuple
```

Fresh state returned/constructed per test includes:

```text
calendar list copies
deserialized H40ConfigurationLedger objects and slots
cold_state
monkeypatch bindings
source/split/attestation objects
runtime seals
lifecycle services/stores
evidence roots
resolvers
production/synthetic verifier instances
scientific caches
```

The production verifier and its scientific result state are not cached.

No repository evidence was found of tests mutating cached roster entries through `object.__setattr__`; `H40RuntimeRosterEntry` is a frozen dataclass in production code.

## 3. Full-cold-path preservation

The patch explicitly preserves uncached production paths.

Examples:

```text
test_a00_production_seal_derives_typed_verified_authority
    cached_calendar=False

test_a05_split_assert_authoritative_failure_propagates
    cached_calendar=False
```

Additional default-suite coverage retains:

- production source/split authority materialization;
- production runtime-seal construction/verification;
- cold source validation;
- source disappearance failure;
- source mutation failure;
- split/attestation mismatch rejection;
- constructor-produced P3B verifier science;
- production candidate-lock verifier identity/integrity;
- production cold-restore same-type tamper rejection;
- production WF NOT_TESTABLE fence;
- durable replay/lineage.

The new equivalence test compares cached and full paths by exact hashes/identities and checks fresh mutable state.

The new production-verifier fixture-isolation test proves separate resolver/evidence roots and filesystem mutation isolation.

## 4. Coverage review

Pre-optimization full suite:

```text
1585 passed
```

Final optimized working tree:

```text
1587 passed
```

The increase is two new equivalence/isolation tests.

No test deletion, skip, xfail, default-suite exclusion, or assertion weakening is present in the reviewed diff.

## 5. Performance result

Accepted historical same-host baseline:

```text
6bd63db...
wall median = 657.69s
```

Reported final optimized-tree runs:

```text
run 1:
  1587 passed
  pytest = 417.89s
  wall = 466.07s
  max RSS = 248328 KiB

run 2:
  1587 passed
  pytest = 413.04s
  wall = 461.75s
  max RSS = 259784 KiB

median wall = 463.91s
reduction vs baseline = 29.46%
```

Frozen primary target:

```text
wall median <= 480s
OR
>=25% reduction vs baseline
```

Both alternatives are met.

Mapped top-five:

```text
baseline combined = 121.58s
optimized combined = 25.01s
reduction = 79.43%
```

Frozen mapped top-five target:

```text
>=40% reduction
```

Target is met.

Therefore:

```text
FULL_PYTEST_PERFORMANCE_TARGETS_MET
```

at the tested working-tree level.

## 6. Static/focused gates

Reported final optimized working-tree gates:

```text
P3B focused = 112 passed
lifecycle/governance focused = 185 passed
ruff check . = PASS
mypy = PASS
compileall = PASS
git diff --check = PASS
```

Frozen protocol/science/governance identities remain unchanged and protected outcomes were not accessed.

## 7. Remaining evidence-binding issue

The raw final logs explicitly identify:

```text
tested SHA = 341a19287a90405aab6a196d68b17720ae1cfff3
```

because tests were executed after editing the working tree but before creating:

```text
d73e8984446f933059b978e36c4cabd55de517aa
```

The report states that the working tree was then committed unchanged and that a post-commit diff check was empty. This is sufficient to review and accept the optimization design/performance result, but it is not the preferred authority evidence identity.

The lifecycle implementation-authority schema requires exact binding between the tested commit identity and the implementation commit identity.

Therefore final authority publication must not use the pre-commit log identity.

## 8. Required next stage

Run a final exact-SHA evidence refresh on:

```text
d73e8984446f933059b978e36c4cabd55de517aa
```

No code changes are authorized.

The refresh must prove:

- checkout HEAD exactly `d73e898...`;
- clean implementation/test tree;
- import provenance;
- focused P3B and lifecycle regression;
- execution fences;
- repository-wide static gates;
- two serial full-suite runs;
- performance remains within frozen target;
- mapped top-five remains within target;
- frozen identities unchanged;
- protected surfaces sealed.

Publish a machine-readable final evidence manifest whose `tested_commit_sha` is exactly `d73e898...`.

After independent controller acceptance of that manifest, proceed directly to new lifecycle implementation-authority publication.

## Formal state

```text
P3B_PRODUCTION_VERIFIER_IMPLEMENTATION_REACCEPTANCE_PASS
H40_TEST_RUNTIME_OPTIMIZATION_R2_CODE_REVIEW_PASS
FULL_PYTEST_PERFORMANCE_TARGETS_MET

FINAL_EXACT_SHA_EVIDENCE_REFRESH_REQUIRED
IMPLEMENTATION_AUTHORITY_PUBLICATION_DEFERRED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```
