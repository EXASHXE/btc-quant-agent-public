# V0.5.1 H40 Pre-P3 Closure — Controller Evidence Blocker

## Decision

```text
PRE_P3_CLOSURE_IMPLEMENTATION_REVIEW_PROVISIONALLY_CLEAN
EXACT_SHA_EVIDENCE_REJECTED_PENDING_RECONCILIATION

REVIEWED_IMPLEMENTATION_SHA =
f3678676265ba53e1d874e27bc0c922c7783acaa

REVIEWED_EVIDENCE_COMMIT =
0920ab1b21a94c3e10e4028212335896b2fe5fcf

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1
```

The production implementation diff is narrow and currently shows no Controller-identified semantic
blocker. However, the exact-SHA evidence package contains internally contradictory raw evidence and
cannot be accepted as authority input.

## Implementation review

Remote implementation branch:

```text
v0.5-refactor =
f3678676265ba53e1d874e27bc0c922c7783acaa
```

The commit is exactly one commit ahead of:

```text
c31586c80130693c118c150a4e06c9573ff0053d
```

Production mutations are exactly:

### MED-01

```python
pq.read_table(file_path)
```

became:

```python
pq.read_table(pa.BufferReader(raw_bytes))
```

This conforms to the frozen R2-F02 requirement that economic extraction consume the same sealed
source bytes whose whole-file SHA was verified.

### Authority staging

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
```

The historical `a23ceec...` implementation authority is not treated as current.

No Controller-identified production semantic regression is visible in the exact diff.

## BLOCKER E01 — authority-stage raw evidence failed

Manifest claims:

```text
authority_stage_state:
status equivalent = PASS / accepted stage state
raw_log_sha256 =
41843bdb59f4b7535cc78011e7b5e64b303a7c8757db5b5a45cbcb2084fbca6c
```

But committed raw log:

```text
evidence/v0.5/h40/pre_p3_closure_f3678676/authority_stage_state.log
```

ends with:

```text
TypeError: Object of type mappingproxy is not JSON serializable
exit_code: 1
```

Therefore the manifest reports a successful authority-stage evidence item whose bound raw evidence
is a failed execution.

This is an evidence-integrity blocker.

## BLOCKER E02 — import provenance raw evidence failed

Manifest claims:

```text
import_provenance.status = PASS
```

but the committed raw log:

```text
evidence/v0.5/h40/pre_p3_closure_f3678676/import_provenance.log
```

ends with:

```text
AttributeError: module 'ruff' has no attribute '__version__'
exit_code: 1
```

Therefore import/tool provenance was not successfully completed.

This matters directly because Ruff-version reconciliation is required below.

## BLOCKER E03 — Ruff result is not reconciled with the accepted prior audit

The accepted mixed-model deep audit at:

```text
00d8e339d51d3da66dcfbf1e39c4d62293c3a37c
```

recorded:

```text
Ruff 0.16.6
10 diagnostics
PUBLICATION_REGRESSION_FROM_RUFF = NO
```

and identified affected legacy test surfaces including:

```text
tests/test_v0320_forward_data.py
tests/test_v0323_blind_validation.py
tests/test_v0324_blind_accumulation.py
```

The implementation diff from `c31586c8...` to `f3678676...` does not modify those files and
does not modify Ruff configuration.

The new evidence nevertheless claims:

```text
ruff_before = 10
ruff_after = 0
ruff check . = PASS
```

while failing to record the active Ruff version because `import_provenance.log` aborted.

Thus one of the following must be true:

1. the prior audit's exact diagnostic-file attribution was inaccurate;
2. the new run used a materially different Ruff executable/version/rule behavior;
3. the new evidence omitted the actual changes needed to explain the difference;
4. another environment/configuration difference exists.

The Controller will not choose among these possibilities without exact evidence.

## BLOCKER E04 — missing before/after Ruff raw comparison

The stage prompt required running Ruff before cleanup and recording the exact diagnostics.

The committed evidence package contains:

```text
static_ruff.log
```

for the final PASS, but no content-addressed raw log that independently establishes the exact
`ruff_before = 10` diagnostic set under the same Ruff executable/version.

Therefore the before/after claim is not replayable from committed evidence.

## Non-blocking observation — deterministic replay logs

`identity_replay.log` and `determinism_replay.log` are byte-identical.

That is not by itself an error: a deterministic script run twice can legitimately emit identical
bytes. The reconciliation task should explicitly record that they are two independently executed
runs, preferably with separate command provenance while keeping deterministic payload output
unchanged.

## Controller consequence

The current evidence manifest:

```text
evidence/v0.5/h40/V0.5.1_H40_PRE_P3_CLOSURE_EXACT_SHA_EVIDENCE_f3678676.json
```

and its reported SHA:

```text
f9556dd3cce44dea2a7397acf771982f5fe459d3c9202c7e5a52b6306aa420c9
```

must **not** be used as the evidence identity for a new lifecycle implementation authority.

No new implementation authority may be materialized until the evidence is reconciled.

## Required reconciliation

First reproduce exact Ruff behavior using the same pinned Ruff version:

```text
ruff 0.16.6
```

against clean worktrees at both:

```text
c31586c80130693c118c150a4e06c9573ff0053d
f3678676265ba53e1d874e27bc0c922c7783acaa
```

Capture:

```text
which ruff
ruff --version
python executable/version
pyproject/config identity
exact diagnostics
exit codes
```

Then:

### Path A — f367 is clean under pinned Ruff 0.16.6

If `f3678676...` is genuinely clean under the same Ruff 0.16.6:

- do not change implementation;
- regenerate successful import provenance;
- regenerate successful authority-stage state;
- commit exact before/after Ruff logs;
- validate every retained raw log status/exit code against manifest;
- publish a corrected exact-SHA evidence manifest tied to the same implementation SHA.

### Path B — f367 still has Ruff diagnostics under pinned Ruff 0.16.6

If diagnostics remain:

- the claimed Ruff closure is not complete;
- apply only the minimum test-only lint fixes;
- create a new implementation SHA;
- keep both accepted lifecycle constants `None`;
- rerun required static/focused/full validation for the new SHA;
- generate a completely new exact-SHA evidence package.

## Protected state

Throughout reconciliation:

```text
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH = None
ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH = None
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

No real outcomes or protected holdouts may be accessed.

## Formal state

```text
PRE_P3_CLOSURE_EXACT_SHA_ACCEPTANCE_BLOCKED_BY_EVIDENCE

IMPLEMENTATION_SEMANTIC_REVIEW =
PROVISIONALLY_CLEAN

E01_AUTHORITY_STAGE_RAW_LOG_FAILED = BLOCKER
E02_IMPORT_PROVENANCE_RAW_LOG_FAILED = BLOCKER
E03_RUFF_VERSION_RESULT_CONTRADICTION = BLOCKER
E04_RUFF_BEFORE_RAW_EVIDENCE_MISSING = BLOCKER

NEXT_STAGE =
PRE_P3_EXACT_SHA_EVIDENCE_RECONCILIATION
```
