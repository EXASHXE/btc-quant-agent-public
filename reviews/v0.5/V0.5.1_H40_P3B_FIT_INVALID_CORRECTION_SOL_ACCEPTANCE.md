# V0.5.1 H40 P3B Fit-Invalid Correction Closure — Sol Acceptance

## Verdict

```text
H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_CLOSURE_ACCEPTED

POLICY_ID =
H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_V1

POLICY_HASH =
7de7c7060c150518805562a473b154d3e91ba5fbc13b5488342ad358744d728a

P3B_IMPLEMENTATION_MAY_RESUME

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Accepted closure artifact:

```text
reviews/v0.5/V0.5.1_H40_P3B_FIT_INVALID_CORRECTION_FAIL_CLOSED_CLOSURE.md
```

Closure commit:

```text
21910cbb6b8a1f693caeccb46a705793e93fa8a4
```

## 1. Gap adjudication

The executor STOP was scientifically correct.

Existing authority fixes:
- the sealed REGISTERED correction universe;
- no substitution;
- separate PRECISION / NET_EXPECTANCY max-stat;
- fit-invalid candidate remains a failed ledger member.

But R1 max-stat requires a valid point estimate and bootstrap vector for every included candidate.

No accepted authority uniquely defined an artificial statistic for a fit-invalid candidate, nor authorized dropping that candidate and shrinking K.

Therefore implementation could not safely choose a numerical rule.

## 2. Accepted closure

The accepted resolution is deliberately fail-closed rather than imputed:

```text
any fit-invalid REGISTERED member
->
its whole correction family = NO_REPRESENTATIVE
for both governing metrics
```

Consequences:

- candidate remains in sealed ledger;
- no replacement;
- no reduced-K max-stat;
- no artificial metric/LCB/bootstrap vector;
- other complete families may still proceed.

This preserves the frozen within-family universe rather than changing multiplicity after observing fit validity.

## 3. Cross-family multiplicity

Cross-family Holm is fixed to six family slots for each metric.

A NO_REPRESENTATIVE family contributes:

```text
p = 1.0
```

to Holm bookkeeping.

Thus missing/failed families cannot reduce K and make surviving families easier to pass.

This is conservative and deterministic.

## 4. Statistical identities

For complete valid families, no scientific estimator, threshold, null, seed, bootstrap, quantile, alpha, hard gate, or final ranking rule changes.

Accepted frozen scientific identities remain:

```text
protocol =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural ledger =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

This closure is accepted as a fail-closed evidence-availability/implementation rule.

The policy identity must be explicitly bound into P3B production verifier evidence/replay.

If implementation discovers that satisfying this closure requires changing a valid-family scientific formula, it must STOP rather than change the frozen hashes.

## 5. Terminal distinction

Accepted:

```text
zero eligible representatives because scientific inputs are unavailable/fit-invalid
-> NOT_TESTABLE

complete evidence exists but accepted gates/corrections yield no survivor
-> H40_NO_GO / THRESHOLD_UNMET
```

This prevents missing scientific evidence from being interpreted as evidence of no edge.

## 6. Scope

Authorized next:

```text
resume P3B production Discovery evidence verifier implementation
using only synthetic/adversarial fixtures
```

Still prohibited:

```text
real H40 outcome read
P3 authorization
Discovery execution
WF production verification
Confirmation
H39
Final Holdout
signed execution
```
