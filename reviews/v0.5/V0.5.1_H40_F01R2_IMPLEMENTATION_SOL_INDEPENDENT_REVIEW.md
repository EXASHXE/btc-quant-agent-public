# V0.5.1 H40 F01R2 Implementation — Sol Independent Review

## Verdict

```text
H40_F01R2_IMPLEMENTATION_SOURCE_CONTRACT_REVIEW_ACCEPTED
NO_NEW_BLOCKER_OR_HIGH_FOUND_IN_STATIC_AUTHORITY_REVIEW

H40_FSA_F01_IMPLEMENTATION_COMPLETE_PENDING_TEST_EVIDENCE_AND_FINAL_AUTHORITY
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_ACCEPTED

ASTRA_PRE_DISCOVERY_FULL_AUDIT_AUTHORIZED_AS_READ_ONLY_REVIEW
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_FSA_F02_OPEN_SEALED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED

REMOTE_CI_DEFERRED_ACTIONS_QUOTA
NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```

Reviewed implementation branch:

```text
v0.5-refactor
```

Reviewed exact commit:

```text
1dad1b6eba6a1610a6af365d1fbeac0376dab5d1

Implement H40 F01R2 runtime authority
```

Parent:

```text
3eec76fdbc451c6e8a17398b39866558abbd93a3
```

Exact code delta is one commit touching:

```text
src/btc_quant_agent/h40/__init__.py
src/btc_quant_agent/h40/lifecycle_authority.py
src/btc_quant_agent/h40/search_space.py
src/btc_quant_agent/h40/source_manifest.py
src/btc_quant_agent/h40/split_manifest.py
tests/test_v051_h40_f01_lifecycle_authority.py
```

No GitHub Actions workflow was triggered, rerun, polled, or waited on during this review.

---

## 1. Accepted authority target

The implementation was reviewed against:

```text
F01R2 amendment:
e1263f4babc067bb56623081ee27769ac61b7e00

F01R2 independent acceptance:
c59e153b14d88dff26db705fc0c72218a1a2bb76
```

Accepted identities:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

runtime_snapshot_seal_contract_hash =
76a0732742707c78f65da26263076bed7b586ea67d4f5ddd2dac33761e37e612

lifecycle_semantic_root_hash =
fad50703c12da9af35366ceb8a774794b4ff2f3163a1adb6b624d3b69316e406

lifecycle_governance_authority_hash =
7edce39c421ad6c487580483d2fa674b1f199e21640c33b1c08f85dddc6f64fc
```

The code binds the amended lifecycle root/governance values and leaves all three scientific identities unchanged.

---

## 2. Runtime source/split attestation — PASS

The implementation adds typed immutable authority objects for:

```text
H40SourceAuthorityStateEntry
H40ActiveSourceEvidenceEntry
H40RuntimeSourceSplitAttestation
```

The runtime attestation uses:

```text
schema_id = H40_RUNTIME_SOURCE_SPLIT_ATTESTATION_V1
```

and exact-key canonical serialization.

It binds:

- accepted reference source-manifest hash;
- accepted reference split-manifest hash;
- protocol identity;
- runtime snapshot ID/hash;
- complete accepted source-state entries;
- runtime source manifest hash;
- structural ledger hash;
- sealed REGISTERED roster hash;
- 18/150/168 counts;
- roster-derived active source IDs;
- NOT_TESTABLE source IDs;
- active source evidence;
- runtime split manifest hash.

The attestation hash is naturally derived through repository canonical SHA-256.

No path string or caller-supplied digest alone can issue this authority.

---

## 3. Canonical identity vs production availability — PASS

`source_manifest.py` now separates:

```text
assert_canonical_source_identity(...)
```

from:

```text
assert_canonical_source_record(...)
```

The identity-only path validates frozen source identity without granting production availability.

The original production-record assertion still requires accepted VERIFIED authority.

This preserves the required distinction:

```text
canonical identity
!=
production availability
```

and permits:

```text
BTC identity valid
BTC production state = NOT_TESTABLE
```

without promotion.

No weakening of the original P1 production-verification function was found.

---

## 4. Runtime source-state derivation — PASS

Runtime state is derived from:

```text
current_p1_authority_snapshot()
```

whose canonical preimage remains:

```text
snapshot_id = P1_ACCEPTED_3fc89541

BTCUSDT_USD_M_1H                 NOT_TESTABLE
ETHUSDT_USD_M_1H                 VERIFIED
BTCUSDT_OFFICIAL_DERIVATIVES     NOT_TESTABLE
BTCUSDT_SPOT_FLOW                NOT_TESTABLE
ETHUSDT_DERIVATIVES_FLOW         NOT_TESTABLE
```

The new code does not accept a caller-owned state map.

The runtime source manifest must preserve the exact ordered preregistered source inventory and its active source statuses must equal the accepted snapshot.

A caller attempting to mark BTC VERIFIED fails closed.

---

## 5. REGISTERED roster and active source union — PASS

The exact production search space remains mechanically materialized from accepted P2 authority:

```text
TOTAL = 168
REGISTERED = 18
NOT_TESTABLE = 150
```

The implementation derives active source IDs by iterating every REGISTERED slot and calling the source-projection helper.

The runtime materialization function does not hard-code:

```text
ETHUSDT_USD_M_1H
```

as its active source result.

For the current accepted projection, the natural union is:

```json
["ETHUSDT_USD_M_1H"]
```

Every active source is required to be VERIFIED in the accepted snapshot.

No NOT_TESTABLE source may appear in active evidence.

---

## 6. Active-source cold verification — PASS

For each active source the implementation requires:

- typed VERIFIED record;
- typed VERIFIED receipt;
- cold artifact validation;
- exact cold receipt equality;
- unique canonical timestamp ordering;
- recomputed timestamp membership hash;
- matching timestamp count;
- exact source-record hash;
- exact validation-receipt hash;
- exact file SHA.

Only the roster-derived active source set is cold-read for runtime authority.

Thus current BTC local presence cannot grant authority merely because bytes exist.

---

## 7. Runtime split — PASS

A dedicated runtime split constructor now builds from:

```text
active_source_timestamps
```

rather than the legacy mandatory BTC/ETH pair.

The static partition calendar is independently compared to the preregistered reference split.

Availability-dependent fields are reconstructed from the exact active source intersection.

For the current projection, the split is therefore based on ETH-only verified runtime timestamps.

The legacy `H40SplitAttestation` remains present for the historical P1 authority surface but is not accepted as the F01R2 runtime attestation.

---

## 8. is_authoritative reachability audit — PASS

The new runtime split constructor returns:

```text
is_authoritative = true
attestation = null
```

This required an explicit bypass review.

Every `is_authoritative` occurrence under the current `src/btc_quant_agent/h40/` tree was inspected.

No external H40 production module was found granting lifecycle/run authority solely from that boolean.

The historical `H40SplitManifest.assert_authoritative()` path additionally requires a legacy production attestation and therefore rejects the new runtime split.

The F01R2 production path instead requires:

```text
H40RuntimeSourceSplitAttestation
+
verify_runtime_source_split_authority(...)
```

and recomputes the exact expected runtime split/attestation.

Therefore the boolean is not currently a production authority bypass.

Its naming remains potentially confusing technical debt, but it is not a BLOCKER/HIGH under the current reachability graph.

---

## 9. Production seal — PASS

`H40RuntimeSnapshotSeal.from_verified_authority(...)` now requires typed:

- runtime source manifest;
- runtime split manifest;
- H40RuntimeSourceSplitAttestation;
- repo root.

It does not accept caller source/split/attestation hashes.

Production construction revalidates the runtime source/split authority and derives:

```text
runtime_authority_snapshot_hash
source_manifest_hash
split_manifest_hash
split_attestation_hash
complete REGISTERED roster
18/150/168 counts
```

The internal production construction token remains in place.

Direct construction cannot manufacture a production seal.

---

## 10. Lifecycle hash tree / governance — PASS

The implementation updates only the accepted F01R2 runtime-snapshot child semantics.

The tests require natural recomputation of:

```text
runtime child =
76a0732742707c78f65da26263076bed7b586ea67d4f5ddd2dac33761e37e612

lifecycle root =
fad50703c12da9af35366ceb8a774794b4ff2f3163a1adb6b624d3b69316e406

governance V3 =
7edce39c421ad6c487580483d2fa674b1f199e21640c33b1c08f85dddc6f64fc
```

The governance object binds the exact committed F01R2 amendment:

```text
e1263f4babc067bb56623081ee27769ac61b7e00
```

No self-reference was introduced.

---

## 11. Persistence / replay — PASS at accepted F01 resolver boundary

The F01R1 cold-restore model is retained.

A production runtime seal now carries typed runtime authority context sufficient for its resolver to re-run:

```text
verify_runtime_source_split_authority(...)
```

during seal revalidation.

Tests exercise:

- cold restore after process-local authorization object removal;
- active source disappearance after persistence;
- runtime seal revalidation;
- substitution/replay denial.

The current code still relies on the accepted external-authority-resolver model rather than implementing a complete production authority database. That is consistent with the previously accepted F01 Model-B boundary and is not reopened here.

---

## 12. Scientific verifier seam — PASS

The F01R1 typed scientific-verifier seam remains intact.

F01R2 does not implement real:

- Discovery statistics;
- max-stat corrections;
- Holm;
- candidate ranking science;
- WF metric science.

Production remains fail-closed until a separately accepted non-synthetic P3 verifier exists.

---

## 13. Adversarial coverage — PASS by source inspection

The implementation adds/updates coverage corresponding to the accepted F01R2 repair set, including:

- amended lifecycle hashes;
- unchanged scientific hashes;
- exact 18/150 projection;
- naturally derived active source union;
- no ETH hard-code in materialization;
- BTC identity without promotion;
- runtime source-state schema tamper;
- active evidence missing/extra/tampered;
- legacy/synthetic/schedule-only authority denial;
- split boundary/fold/count/membership mutation;
- source/split/attestation cross-pairing;
- post-seal source changes;
- cold restore and source disappearance;
- direct production constructor denial;
- F02/H39/Final Holdout protection.

The positive production path monkeypatches only synthetic filesystem/cold-validation I/O for the test fixture. It does not monkeypatch the F01R2 runtime authority verifier or the old split-authority assertion to force acceptance.

This is materially different from the rejected F01R1 positive test.

---

## 14. Test execution evidence limitation

This independent review inspected source and test definitions at exact commit:

```text
1dad1b6eba6a1610a6af365d1fbeac0376dab5d1
```

The commit does not contain a separate local-gate execution report, and no executor output containing exact local:

```text
ruff
mypy
targeted pytest
full pytest
compileall
git diff --check
```

results was supplied to this review session.

Therefore this review does **not** claim those commands were independently observed as passing.

This is an evidence limitation, not a code defect.

Remote CI remains intentionally deferred due Actions quota.

---

## 15. Implementation-authority boundary

The accepted lifecycle implementation-authority schema still requires:

```text
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_V1
```

including exact implementation commit, independent acceptance identity, and required test/CI evidence identity.

Because the required remote CI evidence is currently unavailable and local gate evidence has not been independently supplied here:

```text
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_ACCEPTED
```

must remain true.

This static/source acceptance must not be converted into P3 authorization.

---

## 16. Astra gate status

The implementation is sufficiently mature for the planned **read-only, pre-outcome Astra full-system audit**.

Astra may now audit:

- repository architecture;
- F01/F01R2 authority graph;
- PIT/source boundaries;
- replay/persistence attack surfaces;
- Discovery verifier seam;
- H39/Final Holdout isolation;
- future validated-playbook / Trading Policy architecture.

Astra must not:

- authorize P3;
- run Discovery;
- access H40 outcomes;
- access protected H39/Final Holdout outcomes;
- implement F02;
- infer missing CI evidence.

---

## 17. Final state

```text
F01R2_IMPLEMENTATION_COMMIT =
1dad1b6eba6a1610a6af365d1fbeac0376dab5d1

H40_F01R2_IMPLEMENTATION_SOURCE_CONTRACT_REVIEW_ACCEPTED
NO_NEW_BLOCKER_OR_HIGH_FOUND_IN_STATIC_AUTHORITY_REVIEW

H40_FSA_F01_IMPLEMENTATION_COMPLETE_PENDING_TEST_EVIDENCE_AND_FINAL_AUTHORITY
H40_LIFECYCLE_IMPLEMENTATION_AUTHORITY_NOT_YET_ACCEPTED

ASTRA_PRE_DISCOVERY_FULL_AUDIT_AUTHORIZED_AS_READ_ONLY_REVIEW

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_FSA_F02_OPEN_SEALED
H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED

REMOTE_CI_DEFERRED_ACTIONS_QUOTA
NO_ALPHA_CLAIM
NO_PROFITABILITY_CLAIM
```
