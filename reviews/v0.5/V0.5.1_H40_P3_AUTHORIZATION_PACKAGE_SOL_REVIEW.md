# V0.5.1 H40 P3 Authorization Package — Sol Review

## Verdict

```text
H40_P3_AUTHORIZATION_PACKAGE_ACCEPTED_AS_PREPARATION

PUBLICATION_VALIDATION_DEFERRED_BY_USER

CURRENT_FUNCTIONAL_BLOCKER =
ACCEPTED_PRODUCTION_DISCOVERY_EVIDENCE_VERIFIER_NOT_MATERIALIZED

H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Reviewed docs commit:

```text
e280194e41f6e9db45c0ea1951014a2db002ec41
```

Reviewed artifacts:

```text
reviews/v0.5/V0.5.1_H40_P3_AUTHORIZATION_READINESS_REPORT.md
evidence/v0.5/h40/V0.5.1_H40_P3_AUTHORIZATION_CANDIDATE.json
docs/h40/V0.5.1_H40_DISCOVERY_LAUNCH_RUNBOOK.md
```

## 1. Authority/readiness package

The package correctly preserves:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic_root_hash =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

lifecycle_implementation_authority =
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5

168 / 18 / 150
active source union = ETHUSDT_USD_M_1H
execution policy = RESEARCH_DISABLED_V1
```

The P3 candidate remains correctly non-authorizing:

```text
authorization_status = PENDING_PUBLICATION_VALIDATION
```

No real outcome surface was accessed.

## 2. Newly identified execution blocker independently confirmed

Repository code search at implementation head:

```text
0131a9775912e868a4f66d0593e4ed425c699d9e
```

finds:

- the runtime-checkable `H40LifecycleEvidenceVerifier` Protocol;
- `H40SyntheticEvidenceVerifier`, explicitly marked synthetic-only;
- tests/fakes implementing the seam;
- no production scientific verifier implementation.

The production candidate-lock path requires:

```text
verify_discovery_manifest(...)
verify_candidate(...)
```

and fails closed when no accepted verifier exists.

Therefore the blocker classification is accepted:

```text
ACCEPTED_PRODUCTION_DISCOVERY_EVIDENCE_VERIFIER_NOT_MATERIALIZED
```

It blocks scientific candidate-lock/NO-GO adjudication after Discovery evidence is produced.

It does not justify accessing outcomes before the verifier itself is independently implemented and accepted.

## 3. Frozen Discovery selection semantics

The production implementation must conform exactly to the accepted H40 R1/R2/R3R4 chain.

R3R4 fixes:

- Discovery ranking partition = WF1 CALIBRATION only;
- correction universe = REGISTERED rows of each family in the sealed snapshot;
- fit-invalid candidates remain failed members, never substituted;
- separate within-family block max-stat correction for PRECISION and NET_EXPECTANCY;
- five single direction families plus one pair family for cross-family representative control;
- cross-family Holm applied separately to the existing one-sided marginal p-values for precision and net expectancy;
- alpha remains 0.05;
- representative must satisfy both governing metric Holm controls;
- no new numeric threshold;
- final order = all hard gates, highest adjusted LCB net expectancy, highest adjusted LCB precision, lower complexity, ascending structural configuration hash/config ID;
- no runner-up promotion.

All lower-level bootstrap/null/LCB/hard-gate formulas remain those already frozen by prior accepted H40 amendments. No implementation agent may infer or invent missing formulas.

## 4. Next authorized scope

Next task is bounded implementation of a non-synthetic production Discovery evidence verifier.

It must:
- load evidence only through content-addressed identities;
- recompute rather than trust caller summaries;
- implement the exact accepted Discovery correction/selection semantics;
- use synthetic/adversarial fixtures only during implementation/tests;
- fail closed on any missing accepted scientific definition;
- keep WF verification fail-closed unless explicitly implemented from equally frozen authority.

It must NOT:
- run real Discovery;
- read real H40 labels/returns/MFE/MAE;
- change protocol science;
- implement Confirmation/F02;
- access H39/Final Holdout;
- enable execution.

## 5. Current state

```text
H40_P3_AUTHORIZATION_PACKAGE_READY
P3_PACKAGE_PREPARATION_ACCEPTED

CURRENT_BLOCKER =
ACCEPTED_PRODUCTION_DISCOVERY_EVIDENCE_VERIFIER_NOT_MATERIALIZED

H40_P3_NOT_YET_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```
