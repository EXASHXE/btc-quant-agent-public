# v0.5.0 Local-Agent Simplified-Kernel Adversarial Re-Gate Report

## Verdict

```text
V0_5_LOCAL_EXECUTABLE_RE_GATE_GO
KERNEL_FREEZE_AUTHORIZED
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
NO_NEW_HIGH_OR_MEDIUM
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
H40_NOT_AUTHORIZED_UNTIL_FREEZE_RECORD
```

Audited Code Branch: `v0.5-refactor`
Exact Candidate HEAD: `6838e9db8d5c369b5da87354821d9c8e79c2a879`
Model / Agent: `Antigravity / Gemini 2.5 Pro`
Docs Branch: `v0.5-refactor-docs`

---

## 1. Audited SHA and Working-Tree Cleanliness

- **Exact Code Branch**: `v0.5-refactor`
- **Candidate SHA**: `6838e9db8d5c369b5da87354821d9c8e79c2a879`
- **Remote Preflight**: Fetched `origin/v0.5-refactor` and confirmed `origin/v0.5-refactor` == `6838e9db8d5c369b5da87354821d9c8e79c2a879` (0 commits ahead, 0 commits behind).
- **Working-Tree Status**:
  - `git diff 6838e9db8d5c369b5da87354821d9c8e79c2a879`: Clean, zero modified tracked files.
  - Stale untracked pycache namespace (`src/btc_quant_agent/symbolic_alpha/__pycache__` remaining from pre-R01) was removed, ensuring clean namespace package semantics.
  - No changes pushed or committed to `v0.5-refactor`.

---

## 2. Model and Execution Environment

- **Parent Agent**: `Antigravity` (Gemini 2.5 Pro)
- **Local Runtime Environment**: Linux x86_64 (WSL2 / Ubuntu), Python 3.12.3, pytest 8.4.2, ruff 0.8.2, mypy 1.13.0
- **Virtual Environment**: `/root/workspace/project/Quant-agent/.venv`

---

## 3. Temporary Probe Inventory & Cleanliness

- Temporary probe script created outside the repository: `/tmp/quant_regate_probes/test_appendix_b.py`
  - Re-executed the exact 8 Astra Appendix B fully rehashed accounting attacks (`quantity`, `slippage`, `order`, `curve`, `round_trip`, `pending`, `terminal`, `fee`).
  - Re-executed the 32-combination cross-product identity matrix (`BTCUSDT`/`ETHUSDT` x `4h`/`8h`/`12h`/`24h` x `r1`/`r2` x `RETURN_SIGN`/`MOMENTUM_THRESHOLD`).
- **Deletion Verification**: `/tmp/quant_regate_probes` was completely deleted via `rm -rf /tmp/quant_regate_probes`. No probe files exist in or outside the repository.

---

## 4. Mandatory Executable Attack Matrix (A–N)

| Domain | Assessment | Probe Status | Details & Invariant Verified |
|---|---|---|---|
| **A. AG-01 Funding Authority** | Resistant | **PASS** | Evaluated attacks 1–10: funding-rate sign swap, event deletion, addition, timestamp tampering, mark-price tampering, persisted rehash, unrelated COMPLETE substitution, empty caller list with active settlement, and missing evidence fail closed with explicit validation errors. Zero-event positive control passes. Cold replay strictly reloads and verifies raw evidence bytes via `load_and_validate_funding_evidence` against `ComparisonContract.funding_interval` hash binding. |
| **B. AG-02 Material-Input Selector Authority** | Resistant | **PASS** | Recreated the Astra momentum attack with frozen dataset and `lookback_bars=2`. Authoritative latest window `[2, 3]` passes cleanly. Non-contiguous `[0, 3]`, extra older `[0, 1, 3]`, stale `[1, 2]`, extra earlier `[1, 2, 3]`, late availability, cadence gaps, and reordered/duplicate open times are all rejected. Direct cold validation of a mutated persisted bundle with recomputed outer hashes is rejected by independent window reconstruction. |
| **C. AG-03 Producer-Contract Immutability** | Resistant | **PASS** | Caller-owned dict/list aliases are detached at construction (`MappingProxyType` / frozen tuple). Top-level and nested item assignments fail with `TypeError`. Serialized `semantic_payload()` and `to_dict()` outputs are detached copies. Exact duplicates are idempotent; conflicting same-ID registrations fail atomically. Hostile `object.__setattr__` stale-preimage replacement is detected by `get_contract()` with `ContractHashTamperError`. Original momentum threshold LONG→NO_TRADE attack is rejected. |
| **D. AG-04 GET `/health` Read-Only Behavior** | Resistant | **PASS** | Real `QuantService.health` endpoint verified without monkeypatching `health` itself. Missing parent directory remains absent; missing DB creates no file; populated DB maintains exact byte and SHA-256 preservation with zero sidecar creation. Incomplete schema degrades without migration. Real non-empty WAL + missing SHM does not create SHM and degrades gracefully. Real non-empty WAL + existing SHM modifies zero sidecars. Writer constructor initializes normally as positive control. |
| **E. P5 Evidence-Chain Truncation / Substitution** | Resistant | **PASS** | Tested qualification chains attempting omission or replacement of required evidence: omitting bound funding ID fails at evaluator, post-init, cold verifier, and registry. Omitting dataset, run, or benchmark evidence fails atomically in `record_economic_qualification`. Replacing funding evidence with unrelated COMPLETE evidence or content-different run/benchmark fails. Registry generation, byte size, and event counts remain strictly unchanged across all rejected attempts. |
| **F. Accounting / Benchmark Forgery Regressions** | Resistant | **PASS** | All 8 rehashed accounting forgery attacks from Astra Appendix B (`quantity`, `slippage`, `order`, `curve`, `round_trip`, `pending`, `terminal`, `fee`) fail closed on independent replay/reconciliation, rejecting attempts to claim formal qualification with forged ledgers. Reconstructed benchmark authority cannot be overridden by submitted random trials. |
| **G. PIT / Information-Set Regressions** | Resistant | **PASS** | Unclosed candles, missing `available_at_ms`, availability timestamps strictly after signal timestamps, product/interval mismatches, cadence gaps/overlaps, and future row selections are strictly rejected. Legacy diagnostic inputs cannot silently gain promotion authority. |
| **H. RANDOM_MATCHED / CASH Controls** | Resistant | **PASS** | CASH benchmark maintains strictly zero activity and constant equity. RANDOM_MATCHED produces deterministic execution under identical preregistered seeds/opportunities. Swapping candidate and benchmark roles is rejected. Submitted random trials cannot override reconstructed benchmark authority. |
| **I. Legacy / Formal Boundary** | Resistant | **PASS** | Legacy diagnostic modules and bindings (`shadow.update_shadow`, non-authoritative benchmark artifacts) cannot be relabeled or submitted as current formal qualification evidence. |
| **J. Persistence / Publication Regressions** | Resistant | **PASS** | Idempotent duplicate publication verified; conflicting publication rejected atomically; CAS / append-only invariants held across SQLite and JSON-backed registries. |
| **K. Protected Surfaces** | Invariant Intact | **PASS** (Protected) | Confirmed zero diff across `src/btc_quant_agent/research/` and H39 tests throughout R05. Final Holdout remains sealed. No H40 research code exists. Safe synthetic invariant assertions verified; actual protected unblind outcomes remained uncalled (`NOT_RUN_PROTECTED`). |
| **L. Execution-Disabled Boundary** | Enforced | **PASS** | Default-deny execution verified. Formal research pipelines cannot access execution/order submission endpoints. |
| **M. API Observational Read Boundary** | Enforced | **PASS** | Retained GET endpoints (`/health`, `/signals`) perform zero state mutations, zero DDL, zero schema migrations, and zero sidecar additions. |
| **N. Identity Matrix / Cross-Product Isolation** | Resistant | **PASS** | Evaluated 32 combinations (2 assets x 4 horizons x 2 revisions x 2 rules). Verified 32 unique protocol hashes, 32 unique signal hashes, and 16 unique contract hashes. Distinct semantic inputs never collapse to a single authority identity. Calling `verify_formal_qualification` with mismatched protocol fails closed with `authoritative protocol`. |

---

## 5. Exact Commands and Local Validation Counts

### A. R05 Regression Battery (14 targeted test suites)
```bash
.venv/bin/pytest -q \
  tests/test_v050_r05a_ag03_producer_contract_immutability.py \
  tests/test_v050_r05a_ag04_health_read_only_inspection.py \
  tests/test_v050_r05b_material_input_selector_authority.py \
  tests/test_v050_r05c_funding_evidence_authority.py \
  tests/test_v042_b04_verified_replay_input_bundle.py \
  tests/test_v042_b04r_replay_bundle_acceptance_repair.py \
  tests/test_v042_b04r2_authority_acceptance_repair.py \
  tests/test_v042_b04r3_protocol_producer_contract_fail_closed.py \
  tests/test_v042_b04r4_random_benchmark_authority_boundary.py \
  tests/test_v042_b04r5_candidate_identity_relabel_fail_closed.py \
  tests/test_v042_b05_candidate_execution_replay.py \
  tests/test_v042_b06_random_experiment_replay.py \
  tests/test_v043_c_lite_formal_consumers.py \
  tests/test_v050_r04_kernel_convergence.py
```
- **Result**: `222 passed in 90.78s`

### B. Quality Gates
```bash
.venv/bin/ruff check .
.venv/bin/mypy
python3 -m compileall -q src skill-template/scripts
git diff --check
```
- **Ruff**: `All checks passed!`
- **Mypy**: `Success: no issues found in 85 source files`
- **Compileall**: Clean exit (code 0)
- **Git diff --check**: Clean exit (code 0)

### C. Full Test Suite
```bash
.venv/bin/pytest -q
```
- **Result**: `1086 passed in 204.34s`

### D. Additional Targeted Subsystems
- **P6 Qualification & Registry**: `tests/test_v041_p6_metrics_benchmarks_qualification.py` -> `39 passed in 39.75s`
- **Orchestration & Health Guards**: `tests/test_v050_r02_orchestration_guards.py tests/test_v050_r05a_ag04_health_read_only_inspection.py` -> `36 passed in 2.21s`
- **Surface Reduction Invariants**: `tests/test_v050_r01_surface_reduction.py` -> `13 passed in 4.53s`
- **Protected Surface Guard**: `tests/test_v0323_blind_validation.py -k final_holdout_sealed_and_execution_disabled` -> `1 passed`
- **Astra Appendix B Dynamic Probe**: `PYTHONPATH=tests:src .venv/bin/pytest -q -s /tmp/quant_regate_probes/test_appendix_b.py` -> `9 passed in 12.56s`

---

## 6. Hostile Attack Outcomes & Reproducers

- **Successful Attacks against Current Kernel**: **0** (NONE).
- All attempted evasions, truncation attacks, rehashed forge attacks, parameter mutations, and state-corrupting reads failed closed.
- Findings `AG-01`, `AG-02`, `AG-03`, and `AG-04` are definitively confirmed closed.
- No new BLOCKER, HIGH, or MEDIUM issues were discovered.

---

## 7. Verification of Scope and Safety Invariants

1. **No production or test code modified**: `git diff 6838e9db8d5c369b5da87354821d9c8e79c2a879` confirms zero changes to tracked files in `v0.5-refactor`.
2. **GitHub Actions was not triggered**: No `workflow_dispatch` was called; no commits were pushed to `origin/v0.5-refactor`.
3. **Existing Full Compatibility Receipt Verified**:
   - Workflow Run ID: [`35001541879`](https://github.com/EXASHXE/btc-quant-agent/actions/runs/35001541879)
   - Ref / Commit: `v0.5-refactor` @ `6838e9db8d5c369b5da87354821d9c8e79c2a879`
   - Event: `workflow_dispatch`
   - Python 3.11: PASS (`pytest -q`, `compileall`)
   - Python 3.12: PASS (`pytest -q`, `compileall`)
   - Python 3.13: PASS (`ruff`, `mypy`, `pytest --cov`, `compileall`)
   - Marker: `FULL_COMPAT_CI_GREEN_PY311_PY312_PY313`
4. **Protected Surfaces Sealed**:
   - `H39_PROTECTED_UNCHANGED`
   - `FINAL_HOLDOUT_SEALED`
   - `EXECUTION_DISABLED`
   - `H40_NOT_AUTHORIZED_UNTIL_FREEZE_RECORD`

---

## 8. Final Decision & Authorization

The simplified research kernel at commit `6838e9db8d5c369b5da87354821d9c8e79c2a879` is mathematically consistent, causal, information-sound, and fail-closed against adversarial tampering. It is fully qualified for kernel freeze.

```text
V0_5_LOCAL_EXECUTABLE_RE_GATE_GO
KERNEL_FREEZE_AUTHORIZED
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
NO_NEW_HIGH_OR_MEDIUM
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
H40_NOT_AUTHORIZED_UNTIL_FREEZE_RECORD
```
