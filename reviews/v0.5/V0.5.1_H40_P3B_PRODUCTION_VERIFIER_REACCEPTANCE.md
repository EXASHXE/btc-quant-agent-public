# V0.5.1 H40 P3B Production Verifier Reacceptance — Independent Controller Review

## Reviewed identities

```text
tested_implementation_sha =
341a19287a90405aab6a196d68b17720ae1cfff3

behavior_seal_parent_sha =
a858835ac3239f919bb2dcdc87f74db894f899cf

performance_baseline_sha =
6bd63db96e5b54b9cec27a74cd2f728e19fc5b7d

evidence_docs_commit =
93673be989128621fe54e637b8fffc611030260b

evidence_manifest_sha256 =
5f2aac9b68ff1f358b2a969089298976900fa26dfa7642d8020734009fad95f5
```

## Decision

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_REACCEPTANCE_EVIDENCE_ACCEPTED
P3B_PRODUCTION_VERIFIER_IMPLEMENTATION_REACCEPTANCE_PASS

FULL_PYTEST_PERFORMANCE_ACCEPTANCE_BLOCKED
BLOCKER = FULL_SUITE_RUNTIME_TARGET_NOT_MET

IMPLEMENTATION_AUTHORITY_PUBLICATION_DEFERRED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## 1. Correctness evidence accepted

The final evidence package is bound to the exact tested commit:

```text
341a19287a90405aab6a196d68b17720ae1cfff3
```

Import provenance was independently recorded before benchmark execution and resolves to the intended candidate checkout.

Focused verification is complete:

```text
behavior seal:
  collected 17
  17 passed in 21.29s

identity seal:
  collected 9
  9 passed in 27.00s

production Discovery verifier/science:
  collected 86
  86 passed in 42.61s

focused lifecycle/governance:
  183 passed in 184.86s

execution fences:
  41 passed in 1.83s
```

Repository-wide static gates on the exact candidate are green:

```text
ruff check . = PASS
mypy = PASS
compileall = PASS
git diff --check = PASS
```

Both candidate full-suite runs passed:

```text
run 1:
  1585 passed
  pytest = 443.24s
  wall = 495.89s
  max RSS = 234864 KB

run 2:
  1585 passed
  pytest = 517.27s
  wall = 576.38s
  max RSS = 241052 KB
```

No correctness or regression blocker remains for the P3B production verifier implementation.

## 2. Runtime behavior seal reacceptance

The evidence confirms the reviewed production behavior seal continues to close the previously identified same-exact-type substitution class:

- instance method shadowing;
- `object.__new__` / partial initialization;
- authority-bearing resolver/seal/receipt/split replacement;
- resolver-root redirection;
- transitive runtime-seal context mutation;
- class method replacement;
- post-verification scientific-result cache mutation;
- foreign verifier / subclass / proxy substitution;
- cold-restore/replay tampered verifier substitution.

The fixture restructuring preserves the required production fences while removing the previous forged exact-type production test double.

Therefore:

```text
P3B_PRODUCTION_VERIFIER_IMPLEMENTATION_REACCEPTANCE_PASS
```

is granted for correctness and authority-behavior semantics.

## 3. Frozen identities remain unchanged

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f

lifecycle_semantic_root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle_governance_authority =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

The currently published lifecycle implementation authority remains the historical authority:

```text
6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

It has not yet been replaced.

## 4. Protected surfaces remain sealed

Evidence records:

```text
real_h40_outcomes_accessed = false
wf1_calibration_outcomes_accessed = false
confirmation_accessed = false
h39_accessed = false
final_holdout_accessed = false
signed_network_requests = 0

CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

No protected outcome authority is granted by this review.

## 5. Performance blocker remains open

Same-host baseline:

```text
baseline run 1 wall = 658.19s
baseline run 2 wall = 657.19s
baseline median wall = 657.69s
```

Candidate:

```text
candidate run 1 wall = 495.89s
candidate run 2 wall = 576.38s
candidate median wall = 536.135s
```

Improvement:

```text
18.48%
```

Frozen primary target:

```text
candidate median <= 480s
OR
>= 25% comparable baseline reduction
```

Neither condition is met.

The mapped top-five H40 comparison is also not accepted:

```text
baseline combined = 121.58s
candidate combined = 172.63s
reduction = -41.99%
```

The mapped candidate set is intentionally broader because forged production fixtures were split into synthetic-governance and real-production-fence coverage. That explains part of the increase, but does not satisfy the frozen performance gate.

Therefore:

```text
FULL_PYTEST_PERFORMANCE_ACCEPTANCE_BLOCKED
```

remains open.

## 6. Why implementation-authority publication is deferred

Publishing a new lifecycle implementation authority now would bind it to:

```text
tested_commit_sha = 341a192...
```

The next required task is test-only performance optimization. Any accepted test change will create a new repository commit SHA and require the final test evidence to bind that new SHA.

To avoid publishing and immediately superseding an implementation-authority object, publication is intentionally deferred until the performance blocker is either:

1. closed by a test-only optimization commit; or
2. explicitly waived by a separate controller governance decision.

No waiver is granted here.

## 7. Required next stage

Perform a second, tightly scoped H40 pytest-runtime optimization:

```text
TEST FILES ONLY
NO src/btc_quant_agent/h40/* CHANGES
NO SCIENCE/GOVERNANCE CHANGES
NO TEST REMOVAL/SKIP/XFAIL/ASSERTION WEAKENING
```

Target repeated deterministic authority-fixture construction and duplicate expensive setup while retaining real cold paths where they are semantically required.

After that optimization:

1. rerun full evidence on the new SHA;
2. accept performance if the frozen target is met;
3. publish the new lifecycle implementation authority;
4. run independent publication review;
5. invoke Astra for cumulative pre-Discovery audit;
6. perform P3 Final Authorization.

## Formal state

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_REACCEPTANCE_EVIDENCE_ACCEPTED
P3B_PRODUCTION_VERIFIER_IMPLEMENTATION_REACCEPTANCE_PASS

FULL_PYTEST_PERFORMANCE_ACCEPTANCE_BLOCKED
IMPLEMENTATION_AUTHORITY_PUBLICATION_DEFERRED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```
