# v0.5.0 R04 — Sol Independent Review

## Verdict

```text
R04_ACCEPTED_PENDING_ASTRA_GATE
SIMPLIFIED_KERNEL_CANDIDATE_FROZEN
NO_NEW_HIGH_OR_BLOCKER
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
ALPHA_RESEARCH_NOT_STARTED
```

This is an independent review of the exact remote implementation HEAD. It does not accept the implementation report by assertion and it does not replace the required Astra stage-final adversarial gate.

## Exact reviewed state

- Repository: `EXASHXE/btc-quant-agent`
- Implementation branch: `v0.5-refactor`
- R04 start: `0beb85337bb8b9cac4e63ac9c53ac7b1d32084af`
- Reviewed exact HEAD: `0203e0ffc5cf963518977efcfccb6f2289f10607`
- Accepted C-lite comparison baseline: `31521b620292064997384261e90cd09a2e806f98`
- Immutable repaired-kernel semantic anchor: `6710b6302bf224cdc178e6f3d817a95f79277f38`

R04 contains two commits after the accepted R03 head. The production diff is restricted to the retained historical `research.py` and `mechanism_research.py` diagnostic surfaces; the remaining diff is a dedicated R04 guard test. There is no R04 diff in the formal economic kernel, `research_contract/`, publication/storage authority, H39 protocol/implementation, holdout state, execution package, or default execution configuration.

## Independent findings

### 1. Legacy research output is now fail-closed at the result/report boundary

`mark_legacy_diagnostic()` returns the caller payload first and then overwrites the authority fields with:

```text
classification = LEGACY_DIAGNOSTIC_ONLY
promotion_eligibility = NOT_TESTABLE_FOR_NEW_PROMOTION
can_promote = false
```

The ordering is important: a caller-supplied forged `classification` or `can_promote=true` cannot override the marker.

The historical `research_summary`, `run_full_suite`, persisted research JSON/Markdown, v0.3.2 mechanism aggregate result, and generated mechanism diagnostic JSON files are explicitly marked. Retained raw CSV/Parquet/config/input copies are not formal P5/P6 artifacts and no new formal-registry edge was introduced.

### 2. One current formal promotion authority remains

The current authority path remains:

```text
PIT / causal evidence
  -> registered signal producer + exact decision inputs
  -> ReplayInputBundle
  -> canonical execution/accounting replay
  -> independently reconstructed CASH / RANDOM_MATCHED
  -> preregistered economic qualification
  -> append-only ResearchContractRegistry decision
```

R04 did not modify `economic/qualification.py`, `economic/replay_bundle.py`, `economic/acceptance_verifier.py`, `formal_research.py`, the canonical economic formulas, or `research_contract/` decision semantics. The operational `ResearchRegistry` remains separate and the new R04 test explicitly rejects a direct operational-to-formal decision bridge.

### 3. R03C KEEP decisions were preserved

R04 does not remove `legacy_directional_role`, does not merge the two registries, does not remove `SimulationSummary.round_trips`, and does not weaken legacy market-data v1 fail-closed behavior.

### 4. H39, Final Holdout and execution boundaries remain unchanged

No R04 code diff touches H39 implementation/protocol paths. H39 remains protected and frozen. Final Holdout remains sealed. Execution interfaces remain present but disabled and separately gated; R04 introduced no activation path.

### 5. Genuine cross-checkout semantic parity is present

Unlike the earlier smoke-style R03B probe receipt, R04 used the same deterministic C-lite probe across isolated checkouts of baseline `31521b...` and final `0203e0f...`, with the same facade mode, input fixture and resolved artifact-root string.

Reported canonical snapshot equality:

```text
baseline canonical snapshot hash = d296ad5dbfd455643bb7f2c070e0f3ed72c507f20ec315116b05ce8f4ea85c5a
final    canonical snapshot hash = d296ad5dbfd455643bb7f2c070e0f3ed72c507f20ec315116b05ce8f4ea85c5a

baseline snapshot file sha256 = dcbce60200f4391292533b97a5fab7ceb7c8ae4f28de5a6afb40e4cddc2a9878
final snapshot file sha256    = dcbce60200f4391292533b97a5fab7ceb7c8ae4f28de5a6afb40e4cddc2a9878
byte comparison = equal
```

The comparison covers candidate run, benchmark suite, qualification, evidence identities, registry decision/state, resampling and funding compatibility outputs. No frozen expected value was changed for the comparison.

### 6. Exact-head full compatibility gate passed

Final exact HEAD `0203e0ffc5cf963518977efcfccb6f2289f10607` has:

- push run `34920619464`: success;
- manual full run `34920851780`: success;
- Python 3.11: full pytest + compileall success;
- Python 3.12: full pytest + compileall success;
- Python 3.13: Ruff + mypy + full coverage suite + compileall success.

Therefore:

```text
FULL_COMPAT_CI_GREEN_PY311_PY312_PY313
```

The earlier intermediate Ruff failure is superseded by the final exact-head repair and is not hidden.

## Residual risks for Astra to attack

No HIGH/BLOCKER was found in this review. The following remain appropriate adversarial targets rather than current findings:

1. historical pre-R04 diagnostic artifacts may not carry the new marker even though they have no formal artifact/registry authority;
2. raw legacy CSV/Parquet diagnostic outputs rely on authority separation rather than an embedded marker;
3. future multi-asset/multi-horizon research must preserve exact product/horizon identity separation;
4. the operational registry and formal registry remain intentionally separate and must not acquire an implicit bridge later;
5. all synchronized rehash, candidate/random relabel, submitted accounting, benchmark substitution, publication/finalization and PIT attacks must be re-attacked against the simplified tree rather than inferred safe from previous tests.

## Independent conclusion

R04 has closed the v0.5 ordinary implementation/refactor sequence without redesigning the accepted economic kernel. The simplified-kernel candidate is suitable for the required independent Astra adversarial gate.

```text
R04_ACCEPTED_PENDING_ASTRA_GATE
```
