# v0.5.0 R04 Final Kernel Convergence Report

## Verdict

R04 converged the last identified presentation/persistence bypass without changing the accepted formal economics. The current generic research surface has one formal authority chain:

```text
PIT / causal evidence
  -> registered signal producer + exact decision inputs
  -> ReplayInputBundle
  -> execute_bound_run / EconomicSimulationEngine
  -> canonical accounting
  -> independently reconstructed CASH / RANDOM_MATCHED
  -> preregistered qualification
  -> append-only ResearchContractRegistry decision
```

The remaining historical research and mechanism writers are retained for diagnostics, but their strategy-like result dictionaries and JSON/Markdown reports now carry an overriding, fail-closed marker:

```json
{
  "classification": "LEGACY_DIAGNOSTIC_ONLY",
  "promotion_eligibility": "NOT_TESTABLE_FOR_NEW_PROMOTION",
  "can_promote": false
}
```

No formal formula, identity, benchmark, qualification, publication, SQLite, H39, Final Holdout, or execution semantic was changed. No deletion was justified by the reachability inventory.

## Exact revisions and branch contract

- Implementation branch: `v0.5-refactor`
- Required and observed start: `0beb85337bb8b9cac4e63ac9c53ac7b1d32084af`
- Accepted semantic anchor: `6710b6302bf224cdc178e6f3d817a95f79277f38`
- Accepted C-lite parity baseline: `31521b620292064997384261e90cd09a2e806f98`
- Final implementation HEAD: `0203e0ffc5cf963518977efcfccb6f2289f10607`
- Implementation commits:
  - `b54d3b41e43ac51203169c2c915476a96a6076a7` — `fix(r04): label legacy research outputs as non-promoting`
  - `0203e0ffc5cf963518977efcfccb6f2289f10607` — `style(r04): satisfy current Ruff formatting`
- Prompt/review branch: `v0.5-refactor-docs`
- Prompt/review branch HEAD before this report: `8471ee134bbc7d51112bc1094115bb107eb2da15`
- Prompt blob: `854cd4723b0dacb128c9eb162d549acadbe1015e`

The implementation checkout and docs checkout were isolated. No `prompts/` or `reviews/` content was created on the code branch.

## Files changed or removed

Relative to the exact implementation start:

```text
M  src/btc_quant_agent/research.py
M  src/btc_quant_agent/mechanism_research.py
A  tests/test_v050_r04_kernel_convergence.py
```

Diff size: 162 insertions, 10 deletions. Files removed: none.

The production change is limited to legacy output labeling:

- `research.py` adds `mark_legacy_diagnostic`, applies it to `research_summary`, `run_full_suite`, and the persisted research report, and labels the Markdown report.
- `mechanism_research.py` applies the same marker to experiment results, the aggregate report, and every generated diagnostic JSON. Copied input protocol/manifest files remain unmodified inputs; diagnostic Parquet row sets remain auxiliary records rather than qualification artifacts.
- The marker is applied after caller content, so caller-supplied `classification` or `can_promote` values cannot override it.

## Current-authority inventory

Every reachable research/economic path inspected for R04 was assigned exactly one disposition. Supporting modules are grouped with the path that owns their authority; a group assignment is not a claim that every module independently makes decisions.

### `FORMAL_CURRENT_AUTHORITY`

- Formal facade and entrypoint: `src/btc_quant_agent/formal_research.py`, `tools/run_formal_research.py`, the `backtest`/`research`/`replay` handlers in `src/btc_quant_agent/cli.py`, and `shadow.update_formal_shadow`.
- Replay/execution/accounting kernel: every module under `src/btc_quant_agent/economic/` (`replay_bundle`, `signal_producer`, `signal`, `order`, `execution_model`, `execution_replay`, `simulator`, `portfolio`, `trade_event`, `fee_model`, `funding`, `metrics`, `policy`, `qualification`, and `acceptance_verifier`).
- Formal identity and decision store: `src/btc_quant_agent/research_contract/{canonical,models,registry}.py` and `src/btc_quant_agent/publication.py`.
- Formal evidence compatibility support: `src/btc_quant_agent/data/{csvio,funding,resample,manifest,quality,historical_provenance,vendor_contract}.py`.

Only `formal_research.py` imports `ResearchContractRegistry` in the application orchestration surface. It can call `record_economic_qualification` only after bundle construction, formal suite execution, qualification production, persisted artifact evidence, and cold acceptance verification.

### `PROTECTED_H39_AUTHORITY`

- `src/btc_quant_agent/{microstructure,microstructure_research,h39_baseline,h39_input,h39_statistics}.py`
- `tools/run_h39_{research,blind_operations,blind_validation,one_shot_unblind}.py`
- `configs/research/v0.3.22_microstructure_h39_protocol.json`
- Existing H39 frozen/ledger operational surfaces and their established guards.

This is a separate, protected authority. It was not routed into P5/P6 formal promotion and was not modified, regenerated, unblinded, or inspected for protected outcomes.

### `DIAGNOSTIC_ONLY`

- Historical economics: `src/btc_quant_agent/{backtest,research,mechanism,mechanism_research}.py`.
- Legacy shadow path: `shadow.update_shadow` and legacy shadow reporting. The distinct `update_formal_shadow` adapter belongs to formal authority.
- Descriptive/diagnostic helpers: `forward_diagnostics.py`, `directional_episode.py`, `explain.py`, `funnel.py`, and the legacy holdout discipline in `research_protocol.py`.
- `configs/frozen/v0.2.2.toml` and `configs/research/v0.3.2_mechanism_protocol.json` remain historical inputs, not current qualification authority.

These modules remain importable because historical diagnostics and accepted compatibility tests use them. Their research/mechanism result writers are now explicitly non-promoting. The R03B legacy benchmark module remains absent and was not recreated.

### `OPERATIONAL_NON_PROMOTING`

- Runtime and read surfaces: `engine.py`, `service.py`, `api.py`, `cli.py` outside the formal handlers, `config.py`, `storage.py`, and `sqlite_schema.py`.
- Operational registry: `research_registry.py` and `configs/research_registry.json`.
- Forward collection/evidence: `opportunity_forward.py`, `forward_evidence.py`, `evidence_epoch.py`, and all `configs/forward/*` campaign/epoch declarations.
- Market acquisition/storage: `data/{binance,binance_archive,binance_market_archive,binance_spot_archive,collector,derivatives,forward_store,network_diagnostic,official_derivatives_features,rest}.py`.
- Signal-analysis/runtime support: `features.py`, `indicators.py`, `multifactor.py`, `regime.py`, `risk.py`, `strategies.py`, `structure.py`, `time_boundary.py`, `domain.py`, and notification/package launch support.

These paths can collect, display, or operationally gate state. They do not create a formal economic qualification or append a formal decision. Operational labels such as `SUPPORTED_MOVEMENT`, `VALIDATED_FORWARD`, `LIVE_ELIGIBLE`, and `qualified_direction_engine_count` are not P5 qualification attestations.

### `EXECUTION_DISABLED_SURFACE`

- `src/btc_quant_agent/execution/{guard,models,service,binance_signed}.py`
- Execution command handlers and the separately gated auto-submission branch in `service.py`.
- Default execution configuration.

The interface remains available for explicit separately gated operation, but the default configuration is `mode = "disabled"`, `auto_execute = false`, and `allow_live = false`. Formal research does not activate it.

### `DEAD_SAFE_TO_REMOVE`

None. All plausible candidates had an accepted historical diagnostic, compatibility, protected H39, or operational consumer. R04 therefore did not manufacture a deletion for a LOC target.

## Public entrypoint convergence

Static and dynamic receipts establish the generic surface:

- `quantctl backtest`, `quantctl research`, and `quantctl replay` share the same parser contract: required versioned `--job` and new immutable `--output`; the only implementation dispatch is `run_formal_job_file`.
- The three commands reject implicit legacy/old CSV invocation rather than selecting `backtest.py` or `research.py`.
- `tools/run_formal_research.py` calls the same formal facade.
- `shadow.update_formal_shadow` delegates through the formal job path. `shadow.update_shadow` remains separately named and explicitly legacy diagnostic.
- The surface catalog resolves 51 Python leaves without a dangling import; no generic entrypoint imports the legacy economic engines.
- Existing C-lite guards `test_t1_t2_generic_commands_use_formal_without_service_or_legacy` and `test_t1_t2_implicit_legacy_cli_inputs_are_refused` passed on the final tree.

Thus no generic caller can silently obtain current authority from the retained legacy engine.

## Legacy diagnostic non-promotion

New R04 tests prove:

1. `research_summary` is marked, and a forged caller marker is overwritten fail-closed.
2. `write_research_artifacts` persists both JSON and Markdown with explicit legacy/non-promoting authority.
3. Every generated mechanism diagnostic JSON is marked; copied protocol and data-manifest inputs are not misrepresented as generated results.
4. The marker never claims a formal classification, never writes a formal registry event, and cannot be interpreted as eligible for new promotion.

The historical calculations were not changed. Only their authority envelope at the return/persistence boundary changed.

## Registry separation

`ResearchRegistry` remains a loader/validator/status accessor for operational eligibility. It has no import edge to `btc_quant_agent.research_contract`, no method that creates a qualification, and no bridge to `ResearchContractRegistry.record_economic_qualification`.

`test_operational_registry_has_no_formal_decision_bridge` replaces the formal record method with a forbidden sentinel, then exercises operational registry load, validate, status, lookup, and the `research-registry status` CLI. The sentinel is never called; source import cataloging also confirms no formal-registry dependency.

Conversely, the append-only formal decision is still owned by `ResearchContractRegistry` through the accepted formal facade and cold verifier. The R03C decision to keep both registries separate is preserved. `SimulationSummary.round_trips` and `legacy_directional_role` are also retained unchanged.

## API and execution receipts

The retained HTTP API exposes six routes, all `GET`:

```text
/health
/signals/latest
/signals/{signal_id}
/signals/{signal_id}/explanation
/performance
/execution/status
```

R02 storage-level read-only tests passed for every route; service construction and requests do not mutate repository state or create formal decisions. The `/performance` path reports legacy shadow diagnostics and does not convert them into formal authority.

Execution protection receipts passed in the final full suite, including R01/R02 default guards, hermetic live/testnet transport rejection, C-lite formal-job non-activation, and the established execution-plan gates. No R04 code touched the execution package or default configuration.

## H39 and Final Holdout preservation

- `git diff 0beb853..0203e0f` is empty for H39 implementation/tools/protocol paths.
- The H39 protocol file SHA-256 remains `1b7d61409078f779585675e9f657a60ea1ef5384a7707c33bbac07535feaa979`.
- Hermetic runtime guards, blind-validation guards, one-shot preregistration guards, C-lite H39 isolation, and sealed-holdout tests passed.
- No protected result/outcome file was opened, regenerated, or used in an R04 design choice.
- No alpha/H40/H41/H42/H43 research was performed.

## Hard semantic freeze receipt

There is no diff from the R04 start to final HEAD in:

- `economic/qualification.py`
- `economic/replay_bundle.py`
- `economic/acceptance_verifier.py`
- the rest of the canonical economic package
- `research_contract/`
- `publication.py`
- registry/default configuration
- protected H39 paths.

Selected final SHA-256 receipts:

```text
economic/qualification.py              c9a9ac770ae4a46f356e14bb4ee20c45a4458d27332f752a49e115eaa147324d
economic/replay_bundle.py              e469ee2415f2d1730b77ce549891147faab44bdab9df134083acc147c4c65352
economic/acceptance_verifier.py         1d96cc99966d2df4bc503c1d61ff495682b2ae8c7e0458e6ec286d5a83cafadd
economic/metrics.py                     a62c4bfb5ffd239037d4f1e148d84fbc9e79058ec3a72fdcda859bc4c64178dd
research_registry.py                    8a0c983a311796c9195dfc13f780634b49b57e6a88ff027b7741cb5eac43d559
research_contract/registry.py           b64316ee68d0837f638b764f368a2181b1ab20acc209ed5f332bfecfabdb7a7f
configs/research_registry.json          018c46a34cc960dc53d53e96768d11ede39e5d973fd25175cbdaee240d170334
configs/default.toml                    d2fc9b8415e9cb6f1d6f11bd4ca973811bc3b9ebc139dbebfa723240eacd7206
```

The controlling receipt is the empty path-scoped Git diff between the exact SHAs; the hashes above provide a second exact check.

## Adversarial acceptance matrix

The final `1006`-test suite includes the accepted adversarial families and the new R04 boundary:

| Required attack | Final receipt |
|---|---|
| Impossible candidate fill | B05 `test_fully_rehashed_impossible_price_registry_attack_is_atomic` passed; no registry mutation. |
| Forged submitted fee/funding/PnL | B05 T1/T2/T3/T6 parameterized coherent-ledger attacks passed. |
| Empty signal with forged trade | B05 T8 evidence-stripping attack passed. |
| Observable-time/intrabar LIMIT | B01/B02 causal execution and LIMIT expiry/arrival guards passed. |
| Future liquidity / unavailable or unclosed evidence | B02 future-liquidity, B03 PIT boundary, BAR persisted-acceptance, and C-lite incomplete-availability attacks passed. |
| RANDOM relabel/clone/seed/direction manipulation | B04R4/B04R5 and all B06 adversarial replay tests passed. |
| Traded/nonzero CASH substitution | All B07 T1–T7 attacks and persisted-registry atomicity guards passed. |
| Synchronized wrapper rehash / cold replay | P6 forged run/gate/random families and C-lite T7/T9/T10 passed. |
| Operational registry to P5 qualification | New R04 forbidden-sentinel and import-edge test passed. |
| Legacy market-data v1 promotion | B03 legacy-schema no-default-proof and B11 explicit adapter non-promotion passed. |
| Publication/finalization ownership races | B09 durable publication and B10 writer finalization suites passed. |
| REST amplification | B12 bounded REST suite passed. |
| H39 / Final Holdout bleed | H39, hermetic, blind-validation, one-shot, and C-lite isolation tests passed. |
| Execution activation | Default, hermetic, R01/R02, and C-lite execution-disabled tests passed. |

No expected value or frozen golden was changed to make these tests pass.

## Cross-checkout semantic parity

### Method

Two isolated, clean checkouts were used:

- Baseline: detached `31521b620292064997384261e90cd09a2e806f98`
- Candidate: final R04 content (`0203e0ffc5cf963518977efcfccb6f2289f10607`; the second commit is a one-line test-only Ruff whitespace correction)

Both executed the exact same probe implementation, `tests/probe_v043_c_lite_differential.py`, through one shared runner. Both used `--facade`, identical deterministic inputs, and the same resolved absolute artifact-root string. The baseline ran first; its artifact tree was moved aside, the identical root was recreated, and the candidate then ran. Canonical snapshot files were compared byte-for-byte.

### Result

```text
baseline canonical snapshot hash: d296ad5dbfd455643bb7f2c070e0f3ed72c507f20ec315116b05ce8f4ea85c5a
final    canonical snapshot hash: d296ad5dbfd455643bb7f2c070e0f3ed72c507f20ec315116b05ce8f4ea85c5a

baseline snapshot file sha256:    dcbce60200f4391292533b97a5fab7ceb7c8ae4f28de5a6afb40e4cddc2a9878
final snapshot file sha256:       dcbce60200f4391292533b97a5fab7ceb7c8ae4f28de5a6afb40e4cddc2a9878
byte comparison:                  equal
```

Component canonical hashes were equal in both checkouts:

| Component | No-trade fixture | Trading fixture |
|---|---|---|
| candidate run | `f46beb4c...95370c8` | `3dbb6475...2e3503d` |
| benchmark suite | `adae93ef...ae6fb6` | `1ea8f9c2...c3a31fb` |
| qualification | `9e464c49...59a7c2` | `596e8342...8ae11` |
| evidence identities | `dcde897d...60eb8d1` | `1bd18d35...b0caf5f` |
| registry decision/state | `2fb89fe0...90a4d` | `dbc37782...50f7e75` |
| resample | `1783bf5f...6a453b4` | `1783bf5f...6a453b4` |
| gap resample | `d928cf7d...edd28a` | `d928cf7d...edd28a` |
| funding CSV | `754ee973...a5d1e18` | `754ee973...a5d1e18` |

This is authority-level cross-checkout equality, not merely proof that each checkout emitted a file.

## Future research compatibility

A synthetic identity probe bound both `BTCUSDT` and `ETHUSDT` with explicit 4h, 8h, 12h, and 24h horizons. It produced eight distinct signal identities; a direction-zero input remained non-actionable. The generic authority therefore does not collapse product/horizon identities or force BTC-only, H39-only, 1h-only, or one prediction horizon.

This is compatibility evidence only. It does not implement H40, define future research constants, tune a threshold, or claim alpha.

## Local validation

Final implementation checkout:

```text
ruff 0.16.7 check .                         PASS
mypy                                       PASS (84 source files)
pytest -q                                  PASS (1006 passed, 9 warnings, 176.38s)
python -m compileall -q src skill-template/scripts
                                            PASS
git diff --check                           PASS
```

The nine warnings are existing explicit deprecation warnings for non-authoritative diagnostic APIs. There were no test failures.

A first local Ruff invocation resolved an older system Ruff (`0.15.17`) and did not enforce the current import-format rule. CI correctly exposed one blank-line `I001` violation in the new test. The correct project environment (`Ruff 0.16.7`) removed that single blank line in the separate `0203e0f` commit; all local gates above were rerun on the final tree.

## CI receipts

### Pre-R04 exact-head receipt

- Start SHA `0beb85337bb8b9cac4e63ac9c53ac7b1d32084af`
- Push run `34876254387`: SUCCESS
- URL: <https://github.com/EXASHXE/btc-quant-agent/actions/runs/34876254387>

### Intermediate receipts

- Push run `34920110573` on `b54d3b4`: CANCELLED by workflow concurrency when the first manual dispatch began.
- Manual run `34920129601` on `b54d3b4`: FAILED in Python 3.13 Ruff at the single `I001` import-spacing violation described above. This is superseded by the repair commit and final exact-head runs; it is recorded rather than hidden.

### Final exact-head fast CI

- SHA: `0203e0ffc5cf963518977efcfccb6f2289f10607`
- Push run: `34920619464`
- URL: <https://github.com/EXASHXE/btc-quant-agent/actions/runs/34920619464>
- `quality (3.13)`: SUCCESS — install, Ruff, mypy, full test suite, compileall.
- Compatibility job: SKIPPED by the intentional push-event fast-gate policy.

### Final exact-head full compatibility CI

- SHA: `0203e0ffc5cf963518977efcfccb6f2289f10607`
- `workflow_dispatch` run: `34920851780`
- URL: <https://github.com/EXASHXE/btc-quant-agent/actions/runs/34920851780>
- `compatibility (3.11)`: SUCCESS — full pytest and compileall.
- `compatibility (3.12)`: SUCCESS — full pytest and compileall.
- `quality (3.13)`: SUCCESS — Ruff, mypy, full coverage suite, and compileall.

```text
FULL_COMPAT_CI_GREEN_PY311_PY312_PY313
```

## Deferrals and known limitations

- The retained legacy modules are not deleted; accepted historical diagnostics and compatibility consumers still use them. They are now explicitly non-promoting at the result/report boundary.
- Diagnostic Parquet rows are not wrapped in a new universal envelope. Creating such an envelope is outside R04; the generated JSON/Markdown authority-bearing reports are fail-closed, and no diagnostic writer has a formal registry edge.
- Operational `ResearchRegistry` and formal `ResearchContractRegistry` intentionally remain separate; no merge or schema redesign was attempted.
- Execution interfaces remain present but disabled and separately gated.
- H39 remains frozen/protected and Final Holdout remains sealed.
- The future BTC/ETH multi-horizon probe establishes collision-free identity binding only; no research protocol or alpha result was created.
- Independent review and the Astra adversarial gate are still required. This implementation report does not self-declare either gate passed.

```text
V0_5_R04_IMPLEMENTED_PENDING_INDEPENDENT_REVIEW_AND_ASTRA_GATE
SIMPLIFIED_KERNEL_CANDIDATE_FROZEN
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
ALPHA_RESEARCH_NOT_STARTED
```
