# V0.5.1 H40 P3B Bootstrap Seed Identity Binding — Sol Acceptance

## Verdict

```text
H40_P3B_BOOTSTRAP_SEED_IDENTITY_BINDING_ACCEPTED

POLICY_ID =
H40_P3B_BOOTSTRAP_SEED_IDENTITY_BINDING_V1

POLICY_HASH =
7b2af0e0e7bde22ba10e81a7b356b6489614d42e6dc3a14a4b8d901423868d2c

protocol_id =
protocol_authority_hash

candidate_id =
structural_configuration_hash

P3B_IMPLEMENTATION_MAY_RESUME
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Accepted closure:

```text
reviews/v0.5/V0.5.1_H40_P3B_BOOTSTRAP_SEED_IDENTITY_BINDING_CLOSURE.md
commit 63403bfb2cd5fc5453c6bbd27a13b9b55da83f91
```

## 1. Gap adjudication

The executor STOP is accepted.

R2 §3.3 froze the seed preimage structure but left `protocol_id` and `candidate_id` abstract.

Current accepted repository authority contains multiple distinct deterministic identifiers, and choosing among them changes the PCG64 seed and therefore bootstrap output.

No implementation agent may choose implicitly.

## 2. Accepted binding

The canonical binding is:

```text
protocol_id =
compute_protocol_authority_hash()

candidate_id =
accepted structural_configuration_hash
```

For current H40:

```text
protocol_id =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce
```

Discovery:

```text
partition_id = WF1_CALIBRATION
metric_id ∈ {PRECISION, NET_EXPECTANCY}
```

The accepted R2 hash/PCG64 procedure is otherwise unchanged.

## 3. Why alternative identifiers are rejected

`H40ProtocolIdentity.protocol_hash` is not the cumulative protocol-authority identity used by the final accepted H40 authority chain.

`config_id` is not the later accepted canonical structural candidate identity.

`slot_hash`, slot index, run authority, source/split identities, and materialized runtime identities describe placement or runtime authority and therefore must not perturb the scientific bootstrap seed of the same accepted structural candidate.

## 4. Required replay property

For fixed:

```text
protocol_authority_hash
structural_configuration_hash
partition_id
metric_id
```

bootstrap seed identity must be invariant under:

```text
new lifecycle run
new run_authority_id
runtime snapshot rematerialization
source/split artifact location changes
slot presentation/order changes that do not change structural candidate identity
```

Any mismatch fails closed.

## 5. Scientific identity status

This acceptance binds abstract R2 identity names to already-accepted canonical identities.

It does not modify the accepted bootstrap algorithm or valid-candidate statistical formulas.

Existing frozen identities remain unchanged:

```text
protocol =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural ledger =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

The policy ID/hash must be included in production verifier evidence lineage.

## 6. Scope

Authorized:

```text
resume P3B production Discovery verifier implementation
using synthetic/adversarial fixtures only
```

Still unauthorized:

```text
real H40 outcomes
P3 authorization
Discovery execution
production WF verification
Confirmation
H39
Final Holdout
signed execution
```
