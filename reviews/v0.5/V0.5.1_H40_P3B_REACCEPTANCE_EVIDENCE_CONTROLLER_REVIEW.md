# V0.5.1 H40 P3B Reacceptance Evidence — Controller Review of Latest Submission

## Reviewed state

```text
implementation_head = a858835ac3239f919bb2dcdc87f74db894f899cf
implementation_delta_since_last_review = NONE

docs_head = 9b6beaaf022c54a977462c848c51307316f40fe1
docs_parent = f7ad3a2c50247b240958855c91531c9b15501707
```

Latest docs commit:

```text
docs(v0.5.1): record H40 P3B production verifier runtime behavior seal repair report
```

## Decision

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_SAME_TYPE_BEHAVIOR_BLOCKER_CLOSED_AT_CODE_LEVEL

P3B_REACCEPTANCE_EVIDENCE_REVIEW_BLOCKED
BLOCKER_1 = REQUIRED_PERFORMANCE_REPORT_MISSING
BLOCKER_2 = REQUIRED_MACHINE_READABLE_EVIDENCE_MANIFEST_MISSING
BLOCKER_3 = REPAIR_REPORT_TEST_COUNTS_NOT_RECONCILED_WITH_REPOSITORY
BLOCKER_4 = REPAIR_REPORT_IMPLEMENTATION_DESCRIPTION_NOT_EXACT
BLOCKER_5 = REQUIRED_TWO_RUN_AND_BASELINE_PERFORMANCE_EVIDENCE_MISSING
BLOCKER_6 = REPOSITORY_WIDE_RUFF_GATE_NOT_EVIDENCED

P3B_PRODUCTION_AUTHORITY_REACCEPTANCE_NOT_YET_GRANTED
H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## 1. Implementation code did not change

The implementation branch remains exactly at:

```text
a858835ac3239f919bb2dcdc87f74db894f899cf
```

There is no new implementation delta relative to the previous controller code review.

Therefore the prior code-level decision remains valid:

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_SAME_TYPE_BEHAVIOR_BLOCKER_CLOSED_AT_CODE_LEVEL
```

This review concerns only whether the newly published acceptance evidence is sufficient and accurate.

## 2. Latest docs submission is incomplete

The docs commit adds only:

```text
reviews/v0.5/V0.5.1_H40_P3B_PRODUCTION_VERIFIER_RUNTIME_BEHAVIOR_SEAL_REPAIR_REPORT.md
```

The two additional deliverables required by the authorized evidence-completion prompt are absent:

```text
reviews/v0.5/V0.5.1_H40_PYTEST_RUNTIME_OPTIMIZATION_REPORT.md

evidence/v0.5/h40/V0.5.1_H40_P3B_RUNTIME_BEHAVIOR_SEAL_LOCAL_EVIDENCE.json
```

No evidence-manifest SHA-256 is therefore available for a new implementation-authority object.

## 3. Test-count claims are not reconciled with the repository

The repair report claims:

```text
production_discovery_verifier = 42 passed
identity_seal = 52 passed
behavior_seal = 18 passed
```

Static inspection of the exact tested tree at `a858835...` shows:

### Identity seal

```text
tests/test_v051_h40_p3b_production_verifier_identity_seal.py
defined test functions = 9
```

There are no visible parameterized test definitions in that file sufficient to explain a claim of 52 collected cases.

### Behavior seal

```text
tests/test_v051_h40_p3b_production_verifier_behavior_seal.py
defined test functions = 12
```

Two visible parameterizations expand:

```text
test_b02_instance_method_shadow_rejected -> 3 cases
test_b05_b07_b09_authority_replacement_rejected -> 4 cases
```

Therefore the static expected collection is 17 cases, not 18.

The labels `B01 ... B18` are coverage-requirement identifiers. They are not necessarily one-to-one pytest case identifiers and must not be presented as such.

### Production Discovery verifier suite

```text
tests/test_v051_h40_p3b_production_discovery_verifier.py
defined test functions = 50
```

This is already greater than the reported `42 passed` before accounting for any parameterization.

The report therefore needs to be reconstructed from actual:

```bash
pytest --collect-only -q
pytest -q
```

outputs rather than handwritten counts.

## 4. Implementation description contains factual mismatches

The report describes an initialization mechanism using:

```text
_initialized
_approved_evidence_root
```

Those are not the mechanism implemented in commit `a858835...`.

The actual implementation uses:

```text
H40DiscoveryEvidenceResolver.__slots__ = ("_root",)

resolver.__setattr__:
    if name == "_root" and hasattr(self, "_root"): reject

H40ProductionDiscoveryEvidenceVerifier.__setattr__:
    if name in {"_resolver", "_seal", "_receipt", "_split"}
    and hasattr(self, name): reject
```

There is no `_initialized` field in the reviewed verifier implementation.

The actual constructor binding recorded in `_VERIFIER_BINDINGS` contains:

```text
resolver object
resolver root
seal object
seal authority_context_hash
seal transitive context binding
receipt object
receipt hash
split object
canonical split hash
```

The acceptance report must describe the implemented mechanism precisely enough to be replayed against source.

## 5. Full pytest evidence is insufficient for performance acceptance

The report provides one candidate full-suite run:

```text
1585 passed
pytest duration = 697.87s
wall = 12:11.77
max RSS = 243988 KB
```

That is useful correctness evidence but does not satisfy the authorized performance comparison.

Missing:

- second candidate serial run;
- fresh `6bd63db...` baseline on the same host/environment;
- baseline median / candidate median;
- top-50 duration comparison;
- top-five H40 combined before/after comparison;
- explicit performance classification.

Therefore:

```text
FULL_PYTEST_PERFORMANCE_ACCEPTANCE_PENDING_EVIDENCE
```

remains the only valid state.

## 6. Static quality-gate evidence is also incomplete

The authorized prompt required:

```bash
ruff check .
```

The report instead states only that ruff passed on changed H40/test files and mentions pre-existing microstructure lint.

That may be a legitimate repository condition, but it must be reported as such. A partial ruff command must not be represented as the repository-wide required gate.

The evidence repair must record:

- exact `ruff check .` command;
- exit code;
- exact failing paths/rules if nonzero;
- optional scoped changed-file ruff result separately.

No unrelated production fix is authorized in this evidence-only task.

## 7. Protected-surface statements remain attestations, not yet content-addressed evidence

The report states:

```text
real H40 outcomes accessed = NO
WF1 calibration outcomes accessed = NO
Confirmation accessed = NO
H39 accessed = NO
Final Holdout accessed = NO
signed network requests = 0
```

These are consistent with the intended task, but no machine-readable evidence manifest has yet been published to bind them to:

```text
tested_commit_sha = a858835ac3239f919bb2dcdc87f74db894f899cf
```

They therefore remain report assertions pending evidence publication.

## 8. Required repair is documentation/evidence only

No new production-code blocker was identified.

Do not modify:

```text
src/**
tests/**
ACCEPTED_LIFECYCLE_IMPLEMENTATION_AUTHORITY_HASH
```

The next task is to regenerate accurate test evidence and complete the two missing deliverables.

## 9. Formal state

```text
P3B_RUNTIME_BEHAVIOR_SEAL_CODE_REVIEW_PASS
P3B_SAME_TYPE_BEHAVIOR_BLOCKER_CLOSED_AT_CODE_LEVEL

P3B_REACCEPTANCE_EVIDENCE_REVIEW_BLOCKED
P3B_PRODUCTION_AUTHORITY_REACCEPTANCE_NOT_YET_GRANTED

FULL_PYTEST_PERFORMANCE_ACCEPTANCE_PENDING_EVIDENCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```
