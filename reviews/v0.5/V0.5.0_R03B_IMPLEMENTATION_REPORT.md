# V0.5.0 R03B Implementation Report: Legacy Benchmark Surface Removal

## 1. Executive Summary

Round R03B executed the authorized narrow mechanical cut to remove the inactive legacy benchmark surface identified and audited during R03A:
- Verified complete reachability proof confirming 0 active production consumers.
- Deleted `src/btc_quant_agent/economic/benchmarks.py` (`BenchmarkEngine`, `BenchmarkResult`).
- Pruned exports from `src/btc_quant_agent/economic/__init__.py`.
- Pruned legacy diagnostic tests across 4 test suites while preserving all canonical golden and formal semantics.
- Added comprehensive T1–T6 negative guards and regression verifications in `tests/test_v050_r03b_legacy_benchmark_guards.py`.
- Verified local gates (1003 tests passed, ruff clean, mypy clean, compileall clean, git diff check clean).
- Pushed to `origin/v0.5-refactor` and verified GitHub Actions CI run `34876254387` passed green (`FAST_CI_GREEN_PY313`).

---

## 2. Commit & Repository Tracking

- **Repository**: `EXASHXE/btc-quant-agent`
- **Implementation Branch**: `v0.5-refactor`
- **Docs Branch**: `v0.5-refactor-docs`
- **Starting Implementation HEAD**: `78a5e38b7631b9926f5072b426cd4d24a706f39e`
- **Intermediate Commit**: `b8a00958e578bb119ddab5ce8a6e87f17b355819` (`refactor(economic): remove legacy benchmark surface`)
- **Final Implementation HEAD**: `0beb85337bb8b9cac4e63ac9c53ac7b1d32084af` (`test(r03b): prune legacy benchmark tests and add negative guards`)
- **CI Run ID**: `34876254387` (`quality (3.13)` job ID `104084074924`, status `completed / success`, duration `2m52s`)

---

## 3. Preflight Reachability Proof & Inventory

Every occurrence of `BenchmarkEngine`, `BenchmarkResult`, and `btc_quant_agent.economic.benchmarks` was audited prior to modification:

| Symbol / Module | File Location | Classification | Post-Cut Disposition |
|---|---|---|---|
| `btc_quant_agent.economic.benchmarks` | `src/btc_quant_agent/economic/benchmarks.py` | DEAD_OR_HISTORICAL | File deleted |
| `BenchmarkEngine`, `BenchmarkResult` | `src/btc_quant_agent/economic/__init__.py:3, 93, 96` | PACKAGE_EXPORT_ONLY | Imports and `__all__` exports removed |
| `BenchmarkEngine` | `tests/test_v040_research_kernel_economic_validation.py:8, 456` | TEST_ONLY | Pruned import and legacy diagnostic test |
| `BenchmarkEngine` | `tests/test_v041_p0_p1_cash_ledger.py:11, 275` | TEST_ONLY | Pruned import and legacy diagnostic test |
| `BenchmarkEngine` | `tests/test_v041_p6_metrics_benchmarks_qualification.py:16, 804` | TEST_ONLY | Pruned import and diagnostic evaluate assertion |
| `BenchmarkEngine` | `tests/test_v042_a04_semantic_goldens.py:19, 323, 353` | TEST_ONLY | Pruned import; updated G06/G07 to assert against canonical accounting directly |
| `src/**` (production code) | *all other files* | ACTIVE_PRODUCTION_CONSUMER | 0 occurrences |
| `tools/**` | *all files* | ACTIVE_PRODUCTION_CONSUMER | 0 occurrences |
| `skill-template/**` | *all files* | ACTIVE_PRODUCTION_CONSUMER | 0 occurrences |
| `pyproject.toml` | entrypoints | ACTIVE_PRODUCTION_CONSUMER | 0 occurrences |

**Conclusion**: ZERO active production consumers existed. No compatibility shims, deprecation wrappers, or alias facades were created.

---

## 4. Files Modified & Deleted

### Deleted
- `src/btc_quant_agent/economic/benchmarks.py` (-227 lines)

### Modified
- `src/btc_quant_agent/economic/__init__.py` (-3 lines: removed `BenchmarkEngine` and `BenchmarkResult` from imports and `__all__`)
- `tests/test_v040_research_kernel_economic_validation.py` (-49 lines: removed `BenchmarkEngine` import, unused `SimulationSummary` import, and Section 7 legacy diagnostic test)
- `tests/test_v041_p0_p1_cash_ledger.py` (-19 lines: removed `BenchmarkEngine` and unused `FeeModel, SlippageMode` imports; removed `test_p0_unverified_summary_never_qualifies`)
- `tests/test_v041_p6_metrics_benchmarks_qualification.py` (-24 lines: removed `BenchmarkEngine` and unused `SimulationSummary` imports; pruned legacy diagnostic assertion while preserving fail-closed registry bypass check)
- `tests/test_v042_a04_semantic_goldens.py` (+24, -14 lines: removed `BenchmarkEngine` and `FeeModel, SlippageMode` imports; updated G06 to assert directly against canonical `summarize_ledger`, and updated G07 to assert zero-cost passive arithmetic directly against golden values)

### Created
- `tests/test_v050_r03b_legacy_benchmark_guards.py` (+169 lines: formal guards T1 through T6)

---

## 5. Verification & Negative Guard Receipts

A dedicated test suite `tests/test_v050_r03b_legacy_benchmark_guards.py` validates all required invariants:

- **T1 — Legacy module is gone**:
  - `importlib.import_module("btc_quant_agent.economic.benchmarks")` raises `ModuleNotFoundError`.
  - `importlib.util.find_spec("btc_quant_agent.economic.benchmarks") is None`.
  - Neither `BenchmarkEngine` nor `BenchmarkResult` is in `dir(btc_quant_agent.economic)` or `__all__`.
- **T2 — No active references remain**:
  - Deterministic word-boundary regex checks across `src/`, `tools/`, `skill-template/`, and `pyproject.toml` verify 0 occurrences of `BenchmarkEngine`, `BenchmarkResult`, or `economic.benchmarks`.
- **T3 — Canonical CASH benchmark unchanged**:
  - Canonical `_cash_benchmark(comparison, candles)` verified to preserve initial capital, strictly zero trades, zero fees, zero funding, zero turnover, zero exposure, and `comparable=True`.
- **T4 — Canonical RANDOM benchmark unchanged**:
  - Canonical `preregistered_random_draws` verified to produce exact deterministic draws matching frozen golden case `G08-DETERMINISTIC-RANDOM-PRIMITIVE`.
- **T5 — Qualification binding unchanged**:
  - `FormalBenchmarkSuite` verified to bind candidate run result and comparison contract identities; `evaluate_formal_economic_qualification` executes and produces valid `QualificationVerdict`.
- **T6 — C-lite formal differential remains stable**:
  - `tests/probe_v043_c_lite_differential.py` executed across synthetic datasets, proving stable snapshot hashes and identical differential behavior.

### Local Gate Results
```text
$ ruff check .
All checks passed!

$ mypy
Success: no issues found in 84 source files

$ pytest -q
1003 passed, 9 warnings in 199.18s

$ python -m compileall -q src skill-template/scripts
(exit code 0)

$ git diff --check
(exit code 0)
```

### Remote CI Status
- GitHub Actions run ID `34876254387` passed `quality (3.13)` in 2m48s.
- `FAST_CI_GREEN_PY313` satisfied.

---

## 6. Explicit Confirmation of Non-Interference

1. **Rejected R03A Proposals NOT Implemented**:
   - `SIMP-02` (`legacy_directional_role` removal): **NOT implemented** (DEFERRED / SEMANTIC_CUT_REQUIRES_SOL; active research registry retains opportunity evidence distinctions).
   - `SIMP-03` (`legacy market-data v1` readability): **NOT implemented** (RETAINED as security boundary; acceptance verifier deliberately fail-closes v1 schemas lacking availability proof).
   - `SIMP-04` (registry semantic merge): **NOT implemented** (DEFERRED; registry separation preserved).
   - `SIMP-05` (`round_trips` removal): **NOT implemented** (DEFERRED; attribution semantics preserved).

2. **Core System Semantic Freeze**:
   - `FormalBenchmarkSuite`, `FormalBenchmarkResult`, `ComparisonContract`, and `RandomBenchmarkDistribution` remain byte-frozen and unmodified.
   - `economic/qualification.py`, `formal_research.py`, `replay_bundle.py`, and `acceptance_verifier.py` remain untouched.
   - H39 statistical gate, Final Holdout partition, durable publication, and execution safety invariants remain fully intact.

---

V0_5_R03B_IMPLEMENTED_PENDING_INDEPENDENT_REVIEW
