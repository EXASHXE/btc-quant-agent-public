# V0.5.1 H40 Pre-Discovery Astra Audit R2 — Controller Triage

## Reviewed evidence

```text
audited implementation =
d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6

Astra audit docs commit =
4f5786a25a5e25db32505a99445a3a42c62f7446

docs CI run =
36101797128

docs CI conclusion =
SUCCESS
```

Audit artifacts:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_ASTRA_CUMULATIVE_AUDIT_R2.md
evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_ASTRA_AUDIT_R2.json
```

## Controller decision

```text
PRE_DISCOVERY_ASTRA_AUDIT_FAIL_ACCEPTED
BLOCKING_FINDINGS = R2-F01,R2-F02,R2-F03,R2-F04

REPAIR_REQUIRED = YES
DIRECT_IMPLEMENTATION_WITHOUT_CONTRACT_FREEZE = NOT_AUTHORIZED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

The Astra verdict is accepted as a real blocker set.

No evidence requires reopening the six frozen P3B statistical closures. All six remain MATCH.

Execution separation remains accepted:

```text
CURRENT_EXECUTION_POLICY = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

## Finding groups

### Authority / lifecycle group

```text
R2-F01 — HIGH
controller P3/Discovery grant is not a checked runtime prerequisite

R2-F04 — HIGH
current implementation authority is anchored at service construction
but not independently revalidated across the full service lifetime
```

These two findings are coupled.

A repair must define:

1. the typed controller grant/capability;
2. its issuance/acceptance identity;
3. exact binding to implementation/science/run/source/split/partitions;
4. authoritative-use validation;
5. persistence/cold-restore validation;
6. service authority immutability/current-anchor revalidation;
7. explicit synthetic/test separation.

R2-F01 may require a new lifecycle-governance child/schema identity. That must be decided before code implementation.

R2-F04 alone does not require scientific changes.

### Scientific provenance / PIT group

```text
R2-F02 — HIGH
scientific market values are not authenticated as deterministic extraction
from the sealed source artifact

R2-F03 — HIGH
decision-time feature/score derivation is not established by the evidence chain
```

These are also coupled.

A repair must define:

1. how economic OHLC/value rows are deterministically extracted from exact sealed source bytes;
2. how that extraction is content-addressed and authority-bound;
3. how feature lookback inputs and availability cutoffs are represented;
4. how raw_score/regime/opportunity/filter states are reproduced from causal source data;
5. how feature/cache provenance is bound;
6. how later observations are prevented from changing earlier prefit inputs;
7. how retrospective calibration remains exactly as already accepted.

No change is authorized to:

```text
fit-invalid family rule
bootstrap seed identity
proxy net-return formula
bootstrap block boundary
start sampler
coverage population
candidate universe
retrospective calibration semantics
execution policy
```

## Why implementation is not authorized yet

The Astra report explicitly notes that:

- R2-F01 may require a new controller capability / lifecycle contract / governance schema identity;
- R2-F02 may require new extraction/evidence identities;
- R2-F03 requires an accepted causal producer and provenance representation;
- any frozen schema change must be separately adjudicated.

Therefore an implementation task that invents these schemas while coding would merge scientific/governance design with implementation and make later audit identities ambiguous.

The next stage is a contract-freeze task only.

## Required next stage

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R1
```

Its deliverable must specify the exact typed schemas, canonical hash preimages, state-machine boundaries, producer provenance contracts, compatibility/migration behavior, and identity-impact matrix for all four HIGH findings.

Only after controller acceptance of that repair contract may source implementation changes begin.

## Expected sequencing after contract freeze

```text
Repair Contract Freeze
→ Controller Contract Acceptance
→ Repair Implementation A: Authority/Lifecycle (F01/F04)
→ Repair Implementation B: Scientific Provenance/PIT (F02/F03)
→ exact-SHA evidence refresh
→ implementation-authority republication
→ focused Astra re-audit of F01-F04
→ P3 Final Authorization review
```

The implementation stages may be merged later only if the frozen contract proves they can be changed atomically without identity ambiguity.

## Formal state

```text
PRE_DISCOVERY_ASTRA_AUDIT_FAIL_ACCEPTED
FOUR_HIGH_FINDINGS_OPEN
REPAIR_CONTRACT_FREEZE_REQUIRED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```
