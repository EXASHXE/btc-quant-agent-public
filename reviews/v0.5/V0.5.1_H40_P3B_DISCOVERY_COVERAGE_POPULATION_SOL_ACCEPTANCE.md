# V0.5.1 H40 P3B Discovery Coverage Population — Sol Acceptance

## Verdict

```text
H40_P3B_DISCOVERY_COVERAGE_POPULATION_ACCEPTED

POLICY_ID =
H40_P3B_DISCOVERY_COVERAGE_POPULATION_V1

POLICY_HASH =
a9f4d5a0576f499fc4ee8433d170c33c13d80cbf2c0a179115f91172fa6e30c0

candidate_lock_coverage_population =
WF1_CALIBRATION_FINAL_ACTIONS

training_proposed_side_coverage_gate =
NONE

P3B_IMPLEMENTATION_MAY_RESUME

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

Accepted gap report:

```text
reviews/v0.5/V0.5.1_H40_P3B_SPEC_GAP_REPORT.md
commit 34434377337cdfaf050dc2f07ac1152ba9bea139
```

Accepted closure:

```text
reviews/v0.5/V0.5.1_H40_P3B_DISCOVERY_COVERAGE_POPULATION_CLOSURE.md
commit 7d8a5ac3445bfeca084efd9356c8bff3cd48807b
```

## 1. Gap adjudication

The R5 executor STOP is accepted.

The accepted protocol fixes the scientific coverage interval and separately fixes:
- a training pre-calibration proposed-side population for fit-support n_eff;
- final calibrated actions on WF1 Calibration;
- WF1 Calibration as the sole Discovery ranking outcome partition.

The previous authority did not explicitly say which of these populations owns the coverage hard gate.

Because that decision changes hard-gate eligibility and potentially candidate lock, implementation could not infer it.

## 2. Accepted population

Candidate-lock coverage is exactly:

```text
final emitted LONG/SHORT actions on WF1_CALIBRATION
/
all base-eligible WF1_CALIBRATION decision rows
within the candidate active asset scope
```

This is the direct stage-specific realization of BASE §10.1's accepted metric definition.

The WF1 Training proposed-side population is reserved for the separately accepted training fit-support/sample-support floor.

There is no second training coverage gate.

## 3. Denominator authority

The denominator exists before candidate-specific selection stages.

A downstream candidate abstention remains a denominator row when the row is otherwise base-eligible.

Therefore candidate-specific filtering cannot artificially raise coverage by shrinking its own denominator.

For pooled scope, each product/timestamp decision row is counted separately and pooled by row counts.

No mean of BTC and ETH percentages is used.

## 4. Exact hard-gate comparison

The interval is inclusive:

```text
1/400 <= N_accept / N_base <= 1/10
```

Canonical integer comparisons:

```text
400 * N_accept >= N_base
10 * N_accept <= N_base
```

avoid floating-point boundary ambiguity.

## 5. Edge cases

```text
N_base == 0
-> NOT_TESTABLE

N_base > 0 and N_accept == 0
-> valid observed coverage = 0
-> lower hard gate FAIL
```

An accepted action outside the authoritative base-eligible universe fails evidence verification.

## 6. Stage consistency

When later authorized, the same BASE metric definition is stage-local:
- validation uses final validation actions over base-eligible validation rows;
- confirmation uses final confirmation actions over base-eligible confirmation rows.

This statement does not authorize those protected stages.

## 7. Scientific identity status

No numeric threshold, estimator, bootstrap rule, sample-support floor, action threshold, correction rule, or selection order changes.

Frozen identities remain:

```text
protocol =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

semantic =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural ledger =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

The new coverage policy ID/hash must be bound into production P3B evidence/replay.

## 8. Scope

Authorized:
- resume P3B production Discovery verifier implementation with synthetic/adversarial fixtures only.

Still unauthorized:
- real H40 outcomes;
- P3 authorization;
- Discovery execution;
- production WF verification;
- Confirmation;
- H39;
- Final Holdout;
- signed execution.
