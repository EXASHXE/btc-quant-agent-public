# V0.5.1 H40 P3B Production Verifier Identity Seal Repair Report

## Decision and identity

```text
starting_sha = 3f85d5f1d4e09eeb5e3c8bdd5727ddeeec9ba346
repair_sha = b653a2eae2835999d3631b29860e09b6281952ee
implementation_branch = v0.5-refactor
repository = EXASHXE/btc-quant-agent-public

P3B_PRODUCTION_VERIFIER_IDENTITY_SEAL_REPAIRED
PENDING_INDEPENDENT_SOL_REACCEPTANCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```

## 1. Context and blocker closure

Sol independent review (`reviews/v0.5/V0.5.1_H40_P3B_SOL_INDEPENDENT_REVIEW.md`, commit `a783b3f38dcf26240c97bf267a04c5f28e0f3c5d`) passed P3B scientific implementation review but identified blocker `PRODUCTION_DISCOVERY_VERIFIER_IDENTITY_NOT_ENFORCED`. Specifically, `H40LifecycleAuthorityService.production` accepted any runtime object satisfying `H40LifecycleEvidenceVerifier` with `synthetic_only=False`, allowing arbitrary non-synthetic protocol implementations, subclasses, or proxies to confer production candidate-lock authority.

This repair closes the injection path by establishing an in-process concrete identity seal requiring exact `H40ProductionDiscoveryEvidenceVerifier` runtime type before candidate-lock scientific verification methods are invoked.

## 2. Changed files

Exact changed files:

- `src/btc_quant_agent/h40/lifecycle_authority.py`: Enforces runtime concrete type seal in `authorize_candidate_lock`. Uses runtime-local import to preserve existing module import hierarchy without circular import issues.
- `tests/test_v051_h40_f01_lifecycle_authority.py`: Updates test fixture `_ProductionEvidenceVerifier` to instantiate exact `H40ProductionDiscoveryEvidenceVerifier` via `object.__new__`, preserving lifecycle regression coverage across all transitions while respecting concrete type enforcement.
- `tests/test_v051_h40_p3b_production_verifier_identity_seal.py`: New adversarial suite (A01–A10) validating exact type acceptance, rejection of arbitrary protocol doubles, all-pass rogue verifiers, subclasses, delegating proxies, synthetic verifiers in production, and cold restore integrity.

## 3. Chosen seal mechanism

Inside `H40LifecycleAuthorityService.authorize_candidate_lock`:
When `self.synthetic_test_mode is False`:
1. Runtime-local import `from .discovery_evidence import H40ProductionDiscoveryEvidenceVerifier` avoids import cycles.
2. Exact concrete type check:
   ```python
   if type(verifier) is not H40ProductionDiscoveryEvidenceVerifier:
       raise H40GuardError(
           H40ReasonCode.CONFIG_IDENTITY_CONFLICT,
           "unaccepted production Discovery verifier implementation",
       )
   ```
3. Rejection occurs prior to calling any scientific verifier method (`verify_discovery_manifest`, `verify_candidate`).
4. Synthetic test service mode remains separate (`H40LifecycleAuthorityService.synthetic_for_tests` requires `H40SyntheticEvidenceVerifier`).
5. Production service without verifier remains fail-closed (`NOT_TESTABLE`).

## 4. Adversarial verification evidence

The 10 mandatory adversarial checks in `tests/test_v051_h40_p3b_production_verifier_identity_seal.py` establish:

- **A01 Exact production verifier accepted**: Real `H40ProductionDiscoveryEvidenceVerifier` passes the type seal and reaches the scientific verification path (`_manifest_hash` bound).
- **A02 / A03 Rogue verifier rejected**: `RogueVerifier` implementing `H40LifecycleEvidenceVerifier` with `synthetic_only=False` and returning all-pass / high-LCB values is rejected with `CONFIG_IDENTITY_CONFLICT` before any method is invoked; call counters confirm `(manifest_calls, candidate_calls) == (0, 0)`.
- **A04 Subclass rejected**: `VerifierSubclass(H40ProductionDiscoveryEvidenceVerifier)` fails exact-type identity check.
- **A05 Delegating proxy rejected**: `VerifierProxy` wrapping a real verifier is rejected with 0 delegated calls.
- **A06 Synthetic verifier rejected in production**: Production service constructor continues to reject `H40SyntheticEvidenceVerifier`.
- **A07 Synthetic test mode preserved**: Synthetic lifecycle authority chain continues to function without regression.
- **A08 No verifier fail-closed**: Production service with `evidence_verifier=None` raises `H40GuardError(NOT_TESTABLE, ...)`.
- **A09 Cold restore foreign verifier rejected**: Cold restore of a persisted candidate-lock transition using a service configured with a foreign verifier fails closed with `CONFIG_IDENTITY_CONFLICT` without calling rogue verifier methods.
- **A10 Receipt verifier ID unchanged**: Candidate-lock receipt continues to emit `selection_verifier_id = "H40_DISCOVERY_SELECTION_VERIFIER_V1"`.

## 5. Local validation results

```text
P3B dedicated verifier suite: 86 passed
Authority seal adversarial suite: 9 passed
Execution B04 suite: 41 passed
Focused lifecycle regression suite: 183 passed
Full repository test suite: 1542 passed, 9 deprecation warnings
ruff check .: PASS
mypy: PASS (98 source files)
python -m compileall -q src skill-template/scripts: PASS
git diff --check: PASS
```

## 6. Frozen identities

All six frozen identities verified unchanged:

```text
protocol_authority_hash = a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce
semantic_root_hash = 71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1
structural_ledger_hash = 483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
lifecycle_semantic_root = 846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e
lifecycle_governance_authority = 7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
accepted_lifecycle_implementation_authority = 6d61054a6a8d9bdaaf7e7d648cb941bbb893b67a1fb6a0ed0742a0cdd40697e5
```

## 7. Protected surfaces

- Real H40 outcomes accessed: NO
- Discovery execution: NO
- WF outcomes accessed: NO
- Confirmation accessed: NO
- H39 accessed: NO
- Final Holdout accessed: NO
- Signed network requests: 0
- Execution environment: RESEARCH_DISABLED_V1 (`ACCEPTED_EXECUTION_WRITE_AUTHORITY = None`)

## 8. Conclusion

```text
P3B_PRODUCTION_VERIFIER_IDENTITY_SEAL_REPAIRED
PENDING_INDEPENDENT_SOL_REACCEPTANCE

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
```
