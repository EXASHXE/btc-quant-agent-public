# v0.5.0 Local Executable Re-Gate — Sol Independent Acceptance

## Verdict

```text
V0_5_LOCAL_EXECUTABLE_RE_GATE_ACCEPTED
KERNEL_FREEZE_AUTHORIZED
AG01_CLOSED
AG02_CLOSED
AG03_CLOSED
AG04_CLOSED
NO_NEW_HIGH_OR_MEDIUM
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED
EXECUTION_DISABLED
H40_NOT_AUTHORIZED_UNTIL_FREEZE_RECORD
```

Audited code branch: `v0.5-refactor`

Accepted exact code HEAD:

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

Local-agent report reviewed:

`reviews/v0.5/V0.5.0_LOCAL_AGENT_SIMPLIFIED_KERNEL_ADVERSARIAL_RE_GATE.md`

Local-agent report commit:

```text
a98b24c7972a7085bb3d6e5d45cb738c696ae97c
```

Existing full-compatibility receipt retained without rerun:

```text
GitHub Actions run 35001541879
Python 3.11 PASS
Python 3.12 PASS
Python 3.13 Ruff + mypy + coverage pytest + compileall PASS
```

## Independent review of the local-agent evidence

The report satisfies the substantive requirements of the local executable re-gate:

- exact remote code HEAD was checked before execution;
- no tracked production/test code was changed or pushed;
- GitHub Actions was not re-dispatched;
- the R05 repair suites were run locally (`222 passed`);
- the full local suite was run (`1086 passed`);
- Ruff, mypy, compileall, and `git diff --check` passed;
- the prior Astra fully-rehashed accounting attacks were executed through a temporary external probe (`9 passed`), then removed;
- AG01 funding evidence substitution, AG02 material-window substitution, AG03 producer mutation, AG04 health/SQLite side effects, P5 evidence truncation, PIT, benchmark, legacy/formal, persistence, protected-surface, execution-disabled, API-read, and identity-isolation families were rechecked with no successful attack reported;
- the existing exact-head full 3-version compatibility gate was verified rather than rerun.

No evidence in the report or the accepted code tree reopens AG01–AG04 or introduces a new BLOCKER/HIGH/MEDIUM issue.

## Two report-precision corrections

These are documentation-quality corrections only and do not alter the security verdict.

### 1. Producer parameter immutability implementation

The accepted implementation uses the repository's canonical recursive immutability machinery (`freeze_json` / `FrozenDict` plus tuple conversion), not Python `MappingProxyType` as stated in the local-agent report. The invariant being tested remains correct: caller-owned mutable aliases cannot mutate registered producer authority, serialization returns detached mutable JSON, and registry lookup recomputes semantic identity and fails closed on stale/tampered content.

### 2. Identity-matrix wording

The report states that the 32-case matrix produced "16 unique contract hashes". Built-in producer contract hashes are intentionally stable across product/horizon/revision. The meaningful isolation requirement is that distinct semantic protocol / comparison / signal inputs do not collapse to one formal authority identity. The report's protocol/signal uniqueness and mismatch rejection support that requirement; the exact producer-contract uniqueness count should not be used as a freeze criterion.

Neither wording issue is a successful authority bypass, PIT defect, evidence substitution, protected-surface breach, execution activation, or storage-side-effect defect.

## Freeze decision

The simplified research kernel at exact code SHA

```text
6838e9db8d5c369b5da87354821d9c8e79c2a879
```

has now passed:

1. R05 independent repair reviews;
2. exact-head Python 3.11/3.12/3.13 full compatibility gate;
3. Sol fresh simplified-kernel semantic/adversarial re-gate;
4. local executable A–N adversarial re-gate;
5. this independent acceptance review of the executable receipts.

Therefore the next authorized operation is a documentation/authority freeze record only. No further production-code repair is required before the freeze record.

Until that record is committed:

```text
V0_5_NOT_YET_FROZEN
H40_NOT_AUTHORIZED
```

After an exact-SHA freeze record is committed without code drift, H40 may be authorized as a new research phase while H39 remains protected, Final Holdout remains sealed, and execution remains disabled.
