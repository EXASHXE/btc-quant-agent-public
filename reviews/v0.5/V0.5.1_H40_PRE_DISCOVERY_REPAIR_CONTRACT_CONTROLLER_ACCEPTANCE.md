# V0.5.1 H40 Pre-Discovery Repair Contract — Controller Acceptance R2

## Decision

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTED
R2_F01_CONTRACT_ACCEPTED
R2_F02_CONTRACT_ACCEPTED
R2_F03_CONTRACT_ACCEPTED
R2_F04_CONTRACT_ACCEPTED

SCIENTIFIC_FROZEN_RULES_CHANGED = NO
PROTOCOL_AUTHORITY_HASH_UNCHANGED
SCIENTIFIC_SEMANTIC_ROOT_UNCHANGED
STRUCTURAL_LEDGER_HASH_UNCHANGED

IMPLEMENTATION_NOT_YET_CHANGED
H40_LIFECYCLE_V5_HASHES_NOT_YET_MATERIALIZED
REPAIR_A_IMPLEMENTATION_NOT_YET_AUTHORIZED_BY_IMPLEMENTATION_PROMPT
REPAIR_B_IMPLEMENTATION_NOT_AUTHORIZED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1
```

This is an independent docs-only controller acceptance of the superseding R2 repair contract.
No implementation, source, split, outcome, execution, Confirmation, H39, or Final Holdout surface
was modified or accessed.

## Exact reviewed state

Repository:

```text
EXASHXE/btc-quant-agent-public
```

Superseding R2 contract commit:

```text
cf7b1c9b9547f3e9e6dfab964d7d2c6d1bb35de3
```

R2 artifacts:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2.md
evidence/v0.5/h40/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_FREEZE_R2.json
```

Implementation baseline:

```text
branch = v0.5-refactor
SHA    = d63c1fbb471405e6b08eecdf544d39c7e4a1a9b6
```

Controller blocker closed by R2:

```text
reviews/v0.5/V0.5.1_H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_BLOCKER.md
commit = 72c944d4e9d2a8655f384b5d9ffd17e9996a7bd9

BLOCKER =
DISCOVERY_PROVENANCE_CONTRACT_HASH_NOT_REPRODUCIBLE_FROM_R1_JSON
```

R1 remains immutable historical evidence and is superseded for future repair-contract authority.

## Independent canonical replay

The controller independently reconstructed the exact R2 provenance preimage from the committed
R2 JSON and recomputed it with repository canonical JSON semantics:

```text
sort_keys = true
separators = (",", ":")
ensure_ascii = false
allow_nan = false
UTF-8
SHA-256
```

Result:

```text
H40_DISCOVERY_PROVENANCE_CONTRACT_V1
computed =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

published =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

MATCH = YES
```

The SHA-256 implementation was independently sanity-checked with the standard `"abc"` vector.

The same independent canonical implementation was applied to the exact current nine-child
lifecycle contract set from implementation SHA `d63c1fbb...`.

It reproduced:

```text
current lifecycle semantic root =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

MATCH = YES

current lifecycle governance authority V4 =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84

MATCH = YES
```

Therefore the R2 provenance digest is reproducible under the same canonicalization regime already
used by accepted lifecycle authority.

## R1 -> R2 semantic-preservation review

The controller compared the R1 and R2 machine-readable contracts.

The following frozen sections are semantically identical:

```text
controller_authorization_contract
finding_contracts
raw_decisions_v2_schema
source_derivation_v2_schema
regression_plan
implementation_staging
```

The R2 provenance preimage is canonically identical to the R1 represented provenance object after
excluding only R1's self-reported `contract_hash` field.

Therefore the R2 change is classified as:

```text
CANONICAL_IDENTITY_REPRESENTATION_REPAIR
SCIENTIFIC_SEMANTIC_CHANGE = NO
```

No new scientific choice was introduced.

## F01 disposition — ACCEPTED

Accepted frozen authority objects:

```text
H40_P3_CONTROLLER_AUTHORITY_V1
H40_DISCOVERY_RUN_GRANT_V1
H40_RECEIPT_DISCOVERY_AUTH_V3
H40_LIFECYCLE_PERSISTENCE_CONTEXT_V2
```

Accepted properties:

- the controller authority binds protocol, scientific, structural, lifecycle-governance,
  lifecycle-implementation, provenance, correction, partition, transition, and
  execution-disabled identities;
- the per-run grant binds the accepted controller authority to exact run/runtime/source/split/roster
  authority;
- production Discovery requires both the current accepted controller authority and the exact
  matching run grant;
- V2 Discovery receipts and V1 persistence contexts are invalid for new production authorization;
- caller-controlled labels, paths, booleans, or object presence do not substitute for accepted
  authority;
- production economic/outcome reads are downstream of validated authority/grant;
- `ACCEPTED_H40_P3_CONTROLLER_AUTHORITY_HASH` remains `None` until a later explicit publication.

No circular authority dependency is introduced because the static controller authority is
published only after repaired lifecycle implementation authority exists, while Lifecycle V5
governance binds this controller acceptance commit rather than a future controller-authority hash.

## F04 disposition — ACCEPTED

Accepted service-lifetime semantics:

- production authority configuration is immutable/write-once after construction;
- every authoritative boundary rechecks stored production authority against current in-process
  accepted anchors;
- superseded long-lived services fail closed;
- persistence/restore cannot revive historical authority;
- hot authority republication requires a new service/process identity;
- synthetic/test authority remains disjoint from production authority.

These are lifecycle/governance semantics and therefore correctly require lifecycle child/root
identity change.

## F02 disposition — ACCEPTED

Accepted provenance identity:

```text
H40_DISCOVERY_PROVENANCE_CONTRACT_V1 =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae
```

Accepted source-value contract:

```text
H40_SOURCE_VALUE_EXTRACTION_V1
H40_P3B_SOURCE_DERIVATION_V2
```

The production verifier must derive economic values from exact sealed source bytes and verified
source receipts. Transported OHLC is not authority. Source ordering, timestamp interpretation,
numeric canonicalization, source support, lookback-only support, and missing-adapter fail-closed
behavior remain exactly as frozen by R2.

## F03 disposition — ACCEPTED

Accepted causal reconstruction contract:

```text
H40_CAUSAL_PREFIT_RECONSTRUCTION_V1
H40_P3B_RAW_DECISIONS_V2
```

The verifier reconstructs `regime_state`, `opportunity_state`, `raw_score`, and
`secondary_filter_state` causally from accepted source history with strict:

```text
close_time < decision_t
```

Transported prefit values are not production authority.

The governing D1/D2/D3, R_VOL, O_RANGE, percentile/quantile, raw-score, calibration, threshold,
coverage, correction, outcome, bootstrap, and sampler science remains the already accepted R3R2
science. This acceptance introduces no formula or threshold change.

## Frozen scientific identities

Remain exact:

```text
protocol_authority_hash =
a83f8fc7a5ca7109fb8cd7fed114d87c3b2c968b5145c11cada203aa5111d1ce

scientific_semantic_root =
71889a35f227ed734272b712851174a69bf9dbb12ea3f87cd84c26e7c66f6af1

structural_ledger_hash =
483f68502b57972deae071c7bd4587ca3e57ccaa72bd99fb0de07b4f0ab8c85f
```

Prior lifecycle identities remain historical inputs:

```text
lifecycle semantic root V4-lineage =
846591f8eac0e1abbedaeeff4c2b0bb7648fe51bdaf981e3309c6f6c440aba8e

lifecycle governance authority V4 =
7e9433aa2ee706dda61871c6ad2b1a1aee4cf7a8f9b6365351942096b5347c84
```

## Identity-impact verdict

Accepted classification:

```text
protocol authority                       UNCHANGED
scientific semantic root                 UNCHANGED
structural ledger                        UNCHANGED

discovery provenance contract            ACCEPTED = 31fc3930...
lifecycle discovery child                MUST CHANGE
lifecycle transition-matrix child        MUST CHANGE
lifecycle persistence/replay child       MUST CHANGE
other six lifecycle children             MUST REMAIN UNCHANGED
lifecycle semantic root                  MUST CHANGE
lifecycle governance authority           MUST CHANGE TO V5
lifecycle implementation authority       MUST CHANGE AFTER A+B EXACT-SHA ACCEPTANCE
controller P3 authority                  NEW, NOT YET PUBLISHED
per-run Discovery grant                  NEW PER RUN, NOT YET USABLE
```

## Migration verdict

Accepted:

```text
R1 contract                               HISTORICAL_SUPERSEDED
R2 contract                               CONTROLLER_ACCEPTED
V2 Discovery receipt production use      INVALID
V1 persistence context production use    INVALID
V1 source-hours evidence production use  INVALID
V1 raw-decision evidence production use  INVALID
old run authority                         INVALID AFTER NEW GOVERNANCE/IMPLEMENTATION IDENTITY
old lifecycle implementation authority   SUPERSEDED AFTER REPAIR REPUBLICATION
historical evidence                       RETAIN_AUDITABLE
source/split/runtime seal                 REPLAYABLE ONLY AFTER COLD REVALIDATION
```

No silent migration is accepted.

## Protected-state verdict

```text
IMPLEMENTATION_CHANGED = NO

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED

H40_CONFIRMATION_SEALED
H39_PROTECTED_UNCHANGED
FINAL_HOLDOUT_SEALED

EXECUTION = RESEARCH_DISABLED_V1
ACCEPTED_EXECUTION_WRITE_AUTHORITY = None
```

## Next action

This acceptance artifact must be committed **alone** before Lifecycle V5 hash materialization.

The exact commit containing this artifact becomes the immutable
`pre_discovery_repair_contract_acceptance_identity.commit_sha` input to
`H40_LIFECYCLE_GOVERNANCE_AUTHORITY_V5`.

After that exact acceptance commit exists, materialize:

```text
1. the exact amended nine-child lifecycle canonical objects/hashes;
2. the new lifecycle semantic root;
3. H40_LIFECYCLE_GOVERNANCE_AUTHORITY_V5;
4. the new governance authority hash.
```

Only after those hashes are separately published and independently replayable may a Repair A
implementation prompt be issued.

## Formal end state

```text
H40_PRE_DISCOVERY_REPAIR_CONTRACT_CONTROLLER_ACCEPTED

R2_F01_CONTRACT_ACCEPTED
R2_F02_CONTRACT_ACCEPTED
R2_F03_CONTRACT_ACCEPTED
R2_F04_CONTRACT_ACCEPTED

SCIENTIFIC_FROZEN_RULES_CHANGED = NO
PROTOCOL_AUTHORITY_HASH_UNCHANGED
SCIENTIFIC_SEMANTIC_ROOT_UNCHANGED
STRUCTURAL_LEDGER_HASH_UNCHANGED

DISCOVERY_PROVENANCE_CONTRACT_HASH_ACCEPTED =
31fc3930e44149e6b3af54ddd3b11c630f95cbbd3f152b3d0d5e614690dcf7ae

H40_LIFECYCLE_V5_HASHES_NOT_YET_MATERIALIZED
IMPLEMENTATION_NOT_YET_CHANGED

H40_P3_NOT_AUTHORIZED
H40_DISCOVERY_NOT_AUTHORIZED
EXECUTION_REMAINS_RESEARCH_DISABLED_V1
```
