# V0.4.1E P6 Acceptance Repair Report

## Scope

Narrow acceptance repair for `prompts/v0.4/implementation/v0.4.1E_acceptance_repair_data_run_gate_binding.md`. P6 economics, preregistered hurdles, H39/Final Holdout, and execution enablement are unchanged.

## B1–B5 closure matrix

| Finding | Enforced invariant | Code path | Adversarial coverage |
|---|---|---|---|
| B1 runtime data substitution | Formal runtime candles use versioned canonical fields (`symbol`, `interval`, open/close timestamps, OHLC, base volume, quote volume) and must exactly match the content-hashed local dataset evidence before simulation. The same canonical payload/hash is persisted with every formal accounting result. | `economic/acceptance_verifier.py`, `execute_bound_run`, `_validate_protocol_and_runtime` | runtime OHLCV substitution rejects; deterministic exact-data replay |
| B2 instrument substitution | `len(product_scope)==1`, runtime symbol equals `product_scope[0]`, and passive/random benchmark vehicle is `<product>_LINEAR_PERPETUAL`. | `ComparisonContract`, runtime binding verifier, persisted semantic verifier | BTC protocol + ETH candles rejects |
| B3 forged accounting | Persisted runtime candles + immutable trade events are replayed through `Portfolio.from_events`; equity marks, terminal state, realized/unrealized PnL, fees/funding, return, round trips, win/loss/PF, turnover, exposure/notional, drawdown and cadence-dependent Sharpe are recomputed using the existing `summarize_ledger` contract. | `verify_formal_accounting` | forged turnover/accounting rejects after semantic hashes can be recomputed |
| B4 forged gates | Registry-side verifier derives observed value/testability/pass/reason for every preregistered hurdle from verified candidate/benchmark accounting and derives final verdict/reason codes. Random quantiles use individual verified trial returns. | `validate_persisted_qualification_semantics` called from registry `_validate_economic_attestation` | forged observed/pass/verdict rejects |
| B5 forged comparability | Required benchmark kind/vehicle, random master seed/trial count/contiguous trial id/deterministic trial seed, trial run-result identity, matching diagnostics and comparable flags are independently recomputed. | `validate_persisted_qualification_semantics`, `recompute_matching_diagnostics` | forged random matching diagnostics/comparable rejects |

## Schema / compatibility

`P6_RESULT_SCHEMA_VERSION` is revised explicitly from `1.0.0` to `1.1.0`. New formal P6 artifacts persist `formal_runtime_market_data_schema_version=1.0.0`, canonical runtime candles, their semantic SHA-256, and the run identity needed to reconstruct random-trial result identity. Existing `1.0.0` formal result artifacts are not silently reinterpreted as `1.1.0`; new promotion must use the repaired contract.

The generic `EconomicDecisionAttestation` boundary explicitly accepts both schema `1.0.0` and repaired schema `1.1.0`; the P6 artifact/registry path enforces active P6 schema `1.1.0` and does not reinterpret old artifact semantics.

Dataset evidence used by new formal P6 must expose the canonical runtime economic candle fields listed above. Existing dataset evidence hash verification remains mandatory and is not weakened.

## Validation

Implementation commit: `324d81d86b12c2e76dca857a1356c202bf15f32d`.

CI run `34337616797` on HEAD `82bb99f068ba449e77025393c152f2c563852a6b` failed at `ruff check .` on six `TRY004` findings in the new verifier. Those type-validation branches were corrected in commit `fe9268f8c51bf156e5eb1a3dfbe8773f7fce5821` without changing replay semantics.

CI run `34337926804` on HEAD `2aee8a9ee587b72fa373dcdc798a433ced6c40e0` passed `ruff` and `mypy` on Python 3.11/3.12/3.13, then found one shared replay ambiguity in full pytest: at a candle-close timestamp, a zero-cost close event can leave equity unchanged, so choosing the earliest matching ledger prefix incorrectly kept the position open and inflated independently reconstructed maximum notional exposure. Commit `dd6805895c81802eddb1d77e5c450cc292b40d67` changes the verifier tie-break to the latest reconstructable close-time prefix while preserving the same observed equity constraint.

CI run `34338285073` on HEAD `d324f08f682911749ca207d6c121364b171eeab8` again passed `ruff` and `mypy` on all three Python versions; full pytest reached `784 passed, 5 failed`, with every remaining failure caused by the generic economic attestation still accepting only schema `1.0.0`. Commit `955b91c4210a3e391d6bcaf7132a7c8dbfd54dc4` explicitly admits repaired schema `1.1.0` at that generic boundary.

CI run `34338611201` on HEAD `1ae2d657fda58d071a45b7b5aa720cb2be5ae8d3` passed `ruff` and `mypy` on all three Python versions; full pytest reached `785 passed, 4 failed`, with all remaining lifecycle failures caused by the registry's active P6 schema constant still being `1.0.0`. Commit `5d5848ff677878d0be1c1b2cb94392303aa1f7e8` aligns the registry's active P6 result schema to `1.1.0`.

CI run `34338833892` on HEAD `c653ce5a3449efe2d9f3d07c6e569988ded8243d` passed all quality gates (`ruff check .`, `mypy`, `pytest -q`, coverage, `compileall`) across Python 3.11, 3.12, and 3.13 matrix jobs.

In commit `f39951ecc72719d91cf9364e33689d2f65917d18`, adversarial regression tests covering B1–B5 were fully integrated into `tests/test_v041_p6_metrics_benchmarks_qualification.py` (`test_registry_rejects_rehashed_forged_run_accounting`, `test_registry_rejects_rehashed_forged_gate_and_verdict`, `test_registry_rejects_rehashed_forged_random_matching_diagnostics`), verifying that re-hashed semantic forgeries are strictly rejected by the registry's independent qualification and accounting replay verifiers.

Local validation on the repaired suite:
- `ruff check .`: 0 errors (all checks passed)
- `mypy`: 0 errors in 95 source files
- `pytest -q tests/test_v041_p6_metrics_benchmarks_qualification.py`: 27 passed in 8.77s
- `pytest -q`: 792 passed, 1 warning in 286.93s
- `python -m compileall -q src skill-template/scripts`: clean (exit code 0)

## Verdict

`P6_ACCEPTED`
